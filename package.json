{
  "name": "mjsql",
  "version": "0.1.0",
  "description": "LISP-style MongoDB aggregation expression transpiler",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "oxfmt ."
  },
  "devDependencies": {
    "oxfmt": "^0.47.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
