[
  {
    "slug": "order-eligibility-for-free-shipping",
    "title": "e-commerce: order eligibility for free shipping",
    "query": "$.cart.total >= 50 &&\n$.customer.status in [\"premium\", \"gold\", \"platinum\"] &&\n$.cart.items.length < 20 &&\n$.customer.region.trim().toLowerCase() === \"us\""
  },
  {
    "slug": "tiered-loyalty-discount-price",
    "title": "e-commerce: tiered loyalty discount price",
    "query": "$round(\n  $.price * (\n    $.loyalty.years >= 5 && $.loyalty.totalSpend >= 10000 ? 0.85 :\n    $.loyalty.years >= 2 ? 0.92 : 1\n  ),\n  2\n)"
  },
  {
    "slug": "seasonal-discount-with-eligibility-check",
    "title": "e-commerce: seasonal discount with eligibility check",
    "query": "$.quantity > 1 && $.price >= 10 && $.category in [\"sale\", \"clearance\"]\n  ? $.price * 0.8\n  : $.price"
  },
  {
    "slug": "discount-breakdown-via-iife-let",
    "title": "e-commerce: discount breakdown via IIFE → $let",
    "query": "((discount) => ({\n  finalPrice: $.price - discount,\n  savings: discount,\n  savingsPercent: Math.round((discount / $.price) * 100),\n}))($.price * (1 - $.loyalty.multiplier))"
  },
  {
    "slug": "cart-subtotal",
    "title": "e-commerce: cart subtotal",
    "query": "$.items.map(item => item.qty * item.price).reduce((acc, x) => acc + x, 0)"
  },
  {
    "slug": "email-domain-extraction",
    "title": "user analytics: email domain extraction",
    "query": "$.email.split(\"@\").at(1).toLowerCase()"
  },
  {
    "slug": "score-normalisation",
    "title": "user analytics: score normalisation",
    "query": "($.score - $.minScore) / ($.maxScore - $.minScore) * 100"
  },
  {
    "slug": "age-decade-bucket",
    "title": "user analytics: age decade bucket",
    "query": "Math.floor($.age / 10) * 10"
  },
  {
    "slug": "days-since-last-login",
    "title": "user analytics: days since last login",
    "query": "Math.abs($dateDiff({ startDate: $.lastLoginAt, endDate: new Date(), unit: 'day' }) ?? -1)"
  },
  {
    "slug": "url-slug",
    "title": "content pipeline: URL slug",
    "query": "String($.articleId) + \"-\" + $.title.toLowerCase().trim().replaceAll(\" \", \"-\")"
  },
  {
    "slug": "full-display-name-skipping-missing-parts",
    "title": "content pipeline: full display name (skipping missing parts)",
    "query": "[$.firstName, $.middleName, $.lastName].filter(Boolean).join(\" \")"
  },
  {
    "slug": "lowercase-display-name",
    "title": "content pipeline: lowercase display name",
    "query": "$toLower($.firstName + \" \" + $.lastName)"
  },
  {
    "slug": "formatted-date-label",
    "title": "reporting: formatted date label",
    "query": "$dateToString({ date: $.publishedAt, format: \"%Y-%m-%d\" }) ??\n$dateToString({ date: $.createdAt, format: \"%Y-%m-%d\" }) ??\n\"unknown\""
  },
  {
    "slug": "days-since-document-was-created",
    "title": "reporting: days since document was created",
    "query": "$dateDiff({ startDate: $.createdAt, endDate: new Date(), unit: \"day\" })"
  },
  {
    "slug": "stock-status-label",
    "title": "inventory: stock status label",
    "query": "$.stock >= $.reorderPoint ? \"ok\" : $.stock > 0 ? \"low\" : \"out-of-stock\""
  },
  {
    "slug": "reorder-alert",
    "title": "inventory: reorder alert",
    "query": "!$.discontinued && $.stock < $.baseReorder * 2 ** $.urgencyLevel"
  },
  {
    "slug": "invoice-line-total-with-compound-tax",
    "title": "financial: invoice line total with compound tax",
    "query": "$round($.quantity * ($.unitPrice + $.unitPrice * $.taxRate), 2)"
  },
  {
    "slug": "full-name-with-null-fallback",
    "title": "user display: full name with null fallback",
    "query": "$.firstName ?? $.aliases.at(0) ?? \"anonymous\""
  },
  {
    "slug": "full-address-formatter",
    "title": "location: full address formatter",
    "query": "[$.building && $.building + \",\", $.streetNo, $.street, $.suburb, $.state, $.country, $.postcode]\n  .filter(Boolean)\n  .join(\" \")"
  },
  {
    "slug": "csv-field-word-count",
    "title": "data quality: CSV field word count",
    "query": "$.tags.split(\",\").length"
  },
  {
    "slug": "normalise-string-vs-number-field",
    "title": "data quality: normalise string vs number field",
    "query": "typeof $.value === \"string\" ? $.value.trim() : String($.value)"
  },
  {
    "slug": "admin-permission-check",
    "title": "access control: admin permission check",
    "query": "$.active && $.role.toLowerCase().includes(\"admin\") && $.name.trim().length > 0"
  },
  {
    "slug": "parameterised-threshold-query",
    "title": "jsmql template-tag form: parameterised threshold query",
    "query": "$.score >= 75 &&\n$.grade in [\"A\", \"B\"] &&\n$.submitted === true"
  },
  {
    "slug": "invoice-line-greeting-template-literal-optional-chain-startswith",
    "title": "invoice line greeting (template literal + optional chain + .startsWith)",
    "query": "`Hi ${$.customer?.firstName ?? 'there'} — your ${$.invoice.id.startsWith('INV-VIP-') ? 'VIP ' : ''}invoice ${$.invoice.id} is ready`"
  },
  {
    "slug": "analytics-flatmap-math-max-gettime",
    "title": "analytics — flatMap + Math.max + .getTime",
    "query": "$.sessions\n  .flatMap(s => s.events)\n  .map(e => e.ts.getTime())\n  .reduce((acc, t) => Math.max(acc, t), 0)"
  },
  {
    "slug": "shopping-cart-total-with-numeric-separators-reduce",
    "title": "shopping cart total with numeric separators + .reduce",
    "query": "Math.min(10_000, $.lines.reduce((sum, l) => sum + l.qty * l.price, 0))"
  },
  {
    "slug": "pivot-table-row-computed-keys-object-fromentries",
    "title": "pivot table row (computed keys + Object.fromEntries)",
    "query": "Object.fromEntries($.metrics.map(m => [m.name, m.value]))"
  },
  {
    "slug": "flex-shape-accumulators-in-realistic-pipelines",
    "title": "flex-shape accumulators in realistic pipelines",
    "query": "$round($sum($.lineTotal), 2)"
  },
  {
    "slug": "file-upload-validation-includes-on-array-literal-endswith-numeric-separator",
    "title": "file upload validation (.includes on array literal + .endsWith + numeric separator)",
    "query": "[\".jpg\", \".png\", \".pdf\", \".docx\"].includes($.file.ext.toLowerCase()) &&\n$.file.name.endsWith($.file.ext) &&\n$.file.size <= 25_000_000"
  },
  {
    "slug": "score-range-with-spread-array-isarray-guard",
    "title": "score range with spread + Array.isArray guard",
    "query": "Array.isArray($.scores) ? Math.max(...$.scores) - Math.min(...$.scores) : 0"
  },
  {
    "slug": "moderator-membership-check-with-array-spread",
    "title": "moderator membership check with array spread",
    "query": "[...$.moderators, ...$.room.mods, \"root\"].includes($.userId)"
  },
  {
    "slug": "days-since-event-date-now-gettime-numeric-separator",
    "title": "days since event (Date.now + .getTime + numeric separator)",
    "query": "Math.floor((Date.now() - $.event.ts.getTime()) / 86_400_000)"
  },
  {
    "slug": "audit-log-line-template-literal-toisostring-charat-touppercase",
    "title": "audit log line (template literal + .toISOString + .charAt + .toUpperCase)",
    "query": "`${$.event.ts.toISOString()} [${$.event.level.charAt(0).toUpperCase()}] ${$.event.message}`"
  },
  {
    "slug": "tag-aggregation-flat-join",
    "title": "tag aggregation (.flat + .join)",
    "query": "$.posts.map(p => p.tags).flat().join(\", \")"
  },
  {
    "slug": "scientific-projection-math-hypot-math-log2-log10-math-sign-math-cbrt-math-pi-e",
    "title": "scientific projection (Math.hypot + Math.log2/log10 + Math.sign + Math.cbrt + Math.PI/E)",
    "query": "{\n  distance: Math.hypot($.point.x - $.origin.x, $.point.y - $.origin.y),\n  octave: Math.log2($.frequency / 440),\n  decibels: Math.log10($.amplitude) * 20,\n  fovRad: $.fovDeg * Math.PI / 180,\n  growthFactor: Math.E ** $.rate,\n  trend: Math.sign($.delta),\n  cubeSide: Math.cbrt($.volume)\n}"
  },
  {
    "slug": "dynamic-pivot-row-computed-key-in-literal-shorthand-property",
    "title": "dynamic pivot row (computed key in literal + shorthand property)",
    "query": "$.products.map(p => ({ [p.category]: p.price, p }))"
  },
  {
    "slug": "annotated-insurance-underwriting-rule-and-comments",
    "title": "annotated insurance underwriting rule (// and /* */ comments)",
    "query": "// age window: 25 to 70 inclusive\n$.driver.age >= 25 && $.driver.age <= 70 &&\n\n/* annual mileage cap — 30k km/year keeps us out of commercial-fleet pricing */\n$.policy.kmPerYear <= 30_000 &&\n\n// hard-list occupations that bump the applicant to the high-risk tier\n!($.driver.occupation in [\"stunt-double\", \"test-pilot\", \"demolition-engineer\"])"
  },
  {
    "slug": "top-orders-report-by-department",
    "title": "pipeline: top-orders report by department",
    "query": "[\n      $match($.status === \"shipped\" && $.placedAt >= \"2026-01-01\"),\n      $lookup({ from: \"users\", localField: \"userId\", foreignField: \"_id\", as: \"buyer\" }),\n      $unwind($.buyer),\n      $group({ _id: $.buyer.department, revenue: $sum($.total), orders: $sum(1) }),\n      $set({ avgOrder: $.revenue / $.orders }),\n      $sort({ revenue: -1 }),\n      $limit(3)\n    ]"
  },
  {
    "slug": "order-pricing-with-let-bindings-commentary",
    "title": "pipeline: order pricing with let bindings + commentary",
    "query": "let subtotal = $.price * $.qty;       // sub-total before tax/shipping\nlet withTax  = subtotal * 1.2;        // with tax\nlet withShip = withTax + $.shipping;  // with tax and shipping\n$project({ sku: 1, subtotal, withTax, final: withShip });"
  },
  {
    "slug": "count-orders-by-status-per-shop-accumulator-replacement",
    "title": "pipeline: count orders by status per shop ($accumulator replacement)",
    "query": "[\n      { $group: { _id: $.shopId, statuses: $push($.status) } },\n      { $project: {\n          counts: $.statuses.reduce((acc, s) => ({ ...acc, [s]: (acc[s] ?? 0) + 1 }), {})\n      } }\n    ]"
  },
  {
    "slug": "invoice-finalisation-pipeline-mutations-match",
    "title": "e-commerce: invoice finalisation pipeline (mutations + $match)",
    "query": "[\n      $match($.status === 'pending' && $.paidAt != null),\n      $.lineTotal = $.qty * $.unitPrice,\n      $.invoiceCount += 1,\n      delete $.tempToken,\n      delete $._processingState,\n      $.status = 'complete'\n    ]"
  },
  {
    "slug": "invoice-finalisation-pipeline-implicit-form",
    "title": "e-commerce: invoice finalisation pipeline (implicit `;` form)",
    "query": "[\n      $match($.status === 'pending' && $.paidAt != null),\n      $.lineTotal = $.qty * $.unitPrice,\n      $.invoiceCount += 1,\n      delete $.tempToken,\n      delete $._processingState,\n      $.status = 'complete'\n    ]"
  }
]
