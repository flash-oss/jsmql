{
  "reference": { "commit": "46afc8d", "srcTree": "14a6a3dbbef165649d32cc210f5ea16934cad355", "dirty": false },
  "rows": {
    "expr\u0000$$ = $$.countBy(String)": {
      "entry": "expr",
      "src": "$$ = $$.countBy(String)",
      "kind": "message",
      "was": ".countBy(key) takes a field name ('.countBy(\"status\")'), a bare-path arrow ('.countBy(d => d.status)'), a computed iteratee ('.countBy(d => d.status.toLowerCase())'), or a lodash matches shorthand ('{ active: true }' / '",
      "now": "jsmql.expr() returns one aggregation expression, but received a stream-replace `$$ = <expr>` (which compiles to pipeline stages). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged."
    },
    "expr\u0000$ = { a: 1 }": {
      "entry": "expr",
      "src": "$ = { a: 1 }",
      "kind": "now-rejected",
      "was": "[{\"$replaceWith\":{\"a\":1}}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a root-replace `$ = <expr>` (which compiles to a `$replaceWith` stage). Drop the `$ = ` to build the expression alone, or call jsmql.pipeline() / jsmql() for ",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged."
    },
    "expr\u0000$ = $.profile": {
      "entry": "expr",
      "src": "$ = $.profile",
      "kind": "now-rejected",
      "was": "[{\"$replaceWith\":\"$profile\"}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a root-replace `$ = <expr>` (which compiles to a `$replaceWith` stage). Drop the `$ = ` to build the expression alone, or call jsmql.pipeline() / jsmql() for ",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged."
    },
    "expr\u0000$ = { a: $.b }": {
      "entry": "expr",
      "src": "$ = { a: $.b }",
      "kind": "now-rejected",
      "was": "[{\"$replaceWith\":{\"a\":\"$b\"}}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a root-replace `$ = <expr>` (which compiles to a `$replaceWith` stage). Drop the `$ = ` to build the expression alone, or call jsmql.pipeline() / jsmql() for ",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged."
    },
    "expr\u0000$$ = $$.filter(d => d.x === 1)": {
      "entry": "expr",
      "src": "$$ = $$.filter(d => d.x === 1)",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"x\":1}}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a stream-replace `$$ = <expr>` (which compiles to pipeline stages). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged."
    },
    "expr\u0000$ = { a: $$.filter(d => d.x === 1) }": {
      "entry": "expr",
      "src": "$ = { a: $$.filter(d => d.x === 1) }",
      "kind": "now-rejected",
      "was": "[{\"$facet\":{\"a\":[{\"$match\":{\"x\":1}}]}}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a root-replace `$ = <expr>` (which compiles to a `$replaceWith` stage). Drop the `$ = ` to build the expression alone, or call jsmql.pipeline() / jsmql() for ",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged."
    },
    "expr\u0000$ = 5": {
      "entry": "expr",
      "src": "$ = 5",
      "kind": "message",
      "was": "Cannot replace root with a number — the new root must be a document. Did you mean to wrap it: '$ = { value: ... }'?",
      "now": "jsmql.expr() returns one aggregation expression, but received a root-replace `$ = <expr>` (which compiles to a `$replaceWith` stage). Drop the `$ = ` to build the expression alone, or call jsmql.pipeline() / jsmql() for ",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged."
    },
    "expr\u0000$ = [1, 2]": {
      "entry": "expr",
      "src": "$ = [1, 2]",
      "kind": "message",
      "was": "Cannot fan out an array of number — each array element becomes a document root, so elements must be documents. Did you mean to wrap them: '$ = [{ value: ... }]'?",
      "now": "jsmql.expr() returns one aggregation expression, but received a root-replace `$ = <expr>` (which compiles to a `$replaceWith` stage). Drop the `$ = ` to build the expression alone, or call jsmql.pipeline() / jsmql() for ",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged."
    },
    "expr\u0000$$ = $$.filter({ status: \"CLOSED\" }).sort({ createdAt: -1 }).take(10)": {
      "entry": "expr",
      "src": "$$ = $$.filter({ status: \"CLOSED\" }).sort({ createdAt: -1 }).take(10)",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"status\":\"CLOSED\"}},{\"$sort\":{\"createdAt\":-1}},{\"$limit\":10}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a stream-replace `$$ = <expr>` (which compiles to pipeline stages). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged."
    },
    "expr\u0000$$ = $$.filter(t => t.a === 1)": {
      "entry": "expr",
      "src": "$$ = $$.filter(t => t.a === 1)",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"a\":1}}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a stream-replace `$$ = <expr>` (which compiles to pipeline stages). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged."
    },
    "jsmql\u0000$.a.take(1.5)": {
      "entry": "jsmql",
      "src": "$.a.take(1.5)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$slice\":[\"$a\",1.5]},null]},null]},{\"$ne\":[{\"$slice\":[\"$a\",1.5]},false]},{\"$ne\":[{\"$slice\":[\"$a\",1.5]},\"\"]},{\"$ne\":[{\"$slice\":[\"$a\",1.5]},0]}]}}",
      "now": ".take(n) needs a whole number, but got 1.5.",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles."
    },
    "expr\u0000$.a.take(1.5)": {
      "entry": "expr",
      "src": "$.a.take(1.5)",
      "kind": "now-rejected",
      "was": "{\"$slice\":[\"$a\",1.5]}",
      "now": ".take(n) needs a whole number, but got 1.5.",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles."
    },
    "jsmql\u0000$.a.drop(1.5)": {
      "entry": "jsmql",
      "src": "$.a.drop(1.5)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlArr\":\"$a\"},\"in\":{\"$slice\":[\"$$jsmqlArr\",1.5,{\"$max\":[1,{\"$size\":\"$$jsmqlArr\"}]}]}}},null]},null]},{\"$ne\":[{\"$let\":{\"vars\":{\"jsmqlArr\":\"$a\"},\"in\":{\"$slice\":[\"$$",
      "now": ".drop(n) needs a whole number, but got 1.5.",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles."
    },
    "expr\u0000$.a.drop(1.5)": {
      "entry": "expr",
      "src": "$.a.drop(1.5)",
      "kind": "now-rejected",
      "was": "{\"$let\":{\"vars\":{\"jsmqlArr\":\"$a\"},\"in\":{\"$slice\":[\"$$jsmqlArr\",1.5,{\"$max\":[1,{\"$size\":\"$$jsmqlArr\"}]}]}}}",
      "now": ".drop(n) needs a whole number, but got 1.5.",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles."
    },
    "jsmql\u0000$.a.slice(0.5)": {
      "entry": "jsmql",
      "src": "$.a.slice(0.5)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$a\"},\"then\":{\"$let\":{\"vars\":{\"jsmqlArr\":\"$a\"},\"in\":{\"$slice\":[\"$$jsmqlArr\",0.5,{\"$max\":[1,{\"$size\":\"$$jsmqlArr\"}]}]}}},\"else\":{\"$substrCP\":[\"$a\",0.5,{\"$ma",
      "now": ".slice(start[, end]) needs a whole number, but got 0.5.",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles."
    },
    "expr\u0000$.a.slice(0.5)": {
      "entry": "expr",
      "src": "$.a.slice(0.5)",
      "kind": "now-rejected",
      "was": "{\"$cond\":{\"if\":{\"$isArray\":\"$a\"},\"then\":{\"$let\":{\"vars\":{\"jsmqlArr\":\"$a\"},\"in\":{\"$slice\":[\"$$jsmqlArr\",0.5,{\"$max\":[1,{\"$size\":\"$$jsmqlArr\"}]}]}}},\"else\":{\"$substrCP\":[\"$a\",0.5,{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":{\"$if",
      "now": ".slice(start[, end]) needs a whole number, but got 0.5.",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles."
    },
    "jsmql\u0000$.a.slice(0, 2.5)": {
      "entry": "jsmql",
      "src": "$.a.slice(0, 2.5)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$a\"},\"then\":{\"$let\":{\"vars\":{\"jsmqlArr\":\"$a\"},\"in\":{\"$slice\":[\"$$jsmqlArr\",{\"$cond\":[{\"$lt\":[2.5,0]},{\"$max\":[{\"$add\":[2.5,{\"$size\":\"$$jsmqlArr\"}]},0]},{\"",
      "now": ".slice(start[, end]) needs a whole number, but got 2.5.",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles."
    },
    "expr\u0000$.a.slice(0, 2.5)": {
      "entry": "expr",
      "src": "$.a.slice(0, 2.5)",
      "kind": "now-rejected",
      "was": "{\"$cond\":{\"if\":{\"$isArray\":\"$a\"},\"then\":{\"$let\":{\"vars\":{\"jsmqlArr\":\"$a\"},\"in\":{\"$slice\":[\"$$jsmqlArr\",{\"$cond\":[{\"$lt\":[2.5,0]},{\"$max\":[{\"$add\":[2.5,{\"$size\":\"$$jsmqlArr\"}]},0]},{\"$min\":[2.5,{\"$size\":\"$$jsmqlArr\"}]}]}]",
      "now": ".slice(start[, end]) needs a whole number, but got 2.5.",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles."
    },
    "jsmql\u0000$.a.sampleSize(1.5)": {
      "entry": "jsmql",
      "src": "$.a.sampleSize(1.5)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlShuffled\":{\"$sortArray\":{\"input\":{\"$map\":{\"input\":\"$a\",\"as\":\"jsmqlItem\",\"in\":{\"k\":{\"$rand\":{}},\"v\":\"$$jsmqlItem\"}}},\"sortBy\":{\"k\":1}}}},\"in\":{\"$map\":{\"input\":{",
      "now": ".sampleSize(n) needs a whole number, but got 1.5.",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles."
    },
    "expr\u0000$.a.sampleSize(1.5)": {
      "entry": "expr",
      "src": "$.a.sampleSize(1.5)",
      "kind": "now-rejected",
      "was": "{\"$let\":{\"vars\":{\"jsmqlShuffled\":{\"$sortArray\":{\"input\":{\"$map\":{\"input\":\"$a\",\"as\":\"jsmqlItem\",\"in\":{\"k\":{\"$rand\":{}},\"v\":\"$$jsmqlItem\"}}},\"sortBy\":{\"k\":1}}}},\"in\":{\"$map\":{\"input\":{\"$slice\":[\"$$jsmqlShuffled\",1.5]},\"as\"",
      "now": ".sampleSize(n) needs a whole number, but got 1.5.",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles."
    },
    "jsmql\u0000$.a.takeRight(2.5)": {
      "entry": "jsmql",
      "src": "$.a.takeRight(2.5)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$slice\":[\"$a\",-2.5]},null]},null]},{\"$ne\":[{\"$slice\":[\"$a\",-2.5]},false]},{\"$ne\":[{\"$slice\":[\"$a\",-2.5]},\"\"]},{\"$ne\":[{\"$slice\":[\"$a\",-2.5]},0]}]}}",
      "now": ".takeRight(n) needs a whole number, but got 2.5.",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles."
    },
    "expr\u0000$.a.takeRight(2.5)": {
      "entry": "expr",
      "src": "$.a.takeRight(2.5)",
      "kind": "now-rejected",
      "was": "{\"$slice\":[\"$a\",-2.5]}",
      "now": ".takeRight(n) needs a whole number, but got 2.5.",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles."
    },
    "jsmql\u0000$year({ date: $.d, timezone: \"UTC\" })": {
      "entry": "jsmql",
      "src": "$year({ date: $.d, timezone: \"UTC\" })",
      "kind": "now-accepted",
      "was": "'$year' expects a date, but got an object. Use a field path or new Date(…).",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$year\":{\"date\":\"$d\",\"timezone\":\"UTC\"}},null]},null]},{\"$ne\":[{\"$year\":{\"date\":\"$d\",\"timezone\":\"UTC\"}},false]},{\"$ne\":[{\"$year\":{\"date\":\"$d\",\"timezone\":\"UTC\"}},\"\"]},{\"$ne\":[{\"$year\"",
      "reason": "Intended, commit 4ce80f3. All 13 date accessors take a { date, timezone } document and the server accepts it -- verified on a live mongod. The old rejection made the documented spelling unreachable, and broke the literal-gating invariant by refusing something that is not certainly wrong."
    },
    "expr\u0000$year({ date: $.d, timezone: \"UTC\" })": {
      "entry": "expr",
      "src": "$year({ date: $.d, timezone: \"UTC\" })",
      "kind": "now-accepted",
      "was": "'$year' expects a date, but got an object. Use a field path or new Date(…).",
      "now": "{\"$year\":{\"date\":\"$d\",\"timezone\":\"UTC\"}}",
      "reason": "Intended, commit 4ce80f3. All 13 date accessors take a { date, timezone } document and the server accepts it -- verified on a live mongod. The old rejection made the documented spelling unreachable, and broke the literal-gating invariant by refusing something that is not certainly wrong."
    },
    "filter\u0000$year({ date: $.d, timezone: \"UTC\" })": {
      "entry": "filter",
      "src": "$year({ date: $.d, timezone: \"UTC\" })",
      "kind": "now-accepted",
      "was": "'$year' expects a date, but got an object. Use a field path or new Date(…).",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$year\":{\"date\":\"$d\",\"timezone\":\"UTC\"}},null]},null]},{\"$ne\":[{\"$year\":{\"date\":\"$d\",\"timezone\":\"UTC\"}},false]},{\"$ne\":[{\"$year\":{\"date\":\"$d\",\"timezone\":\"UTC\"}},\"\"]},{\"$ne\":[{\"$year\"",
      "reason": "Intended, commit 4ce80f3. All 13 date accessors take a { date, timezone } document and the server accepts it -- verified on a live mongod. The old rejection made the documented spelling unreachable, and broke the literal-gating invariant by refusing something that is not certainly wrong."
    },
    "jsmql\u0000$hour({ date: $.d })": {
      "entry": "jsmql",
      "src": "$hour({ date: $.d })",
      "kind": "now-accepted",
      "was": "'$hour' expects a date, but got an object. Use a field path or new Date(…).",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$hour\":{\"date\":\"$d\"}},null]},null]},{\"$ne\":[{\"$hour\":{\"date\":\"$d\"}},false]},{\"$ne\":[{\"$hour\":{\"date\":\"$d\"}},\"\"]},{\"$ne\":[{\"$hour\":{\"date\":\"$d\"}},0]}]}}",
      "reason": "Intended, commit 4ce80f3. All 13 date accessors take a { date, timezone } document and the server accepts it -- verified on a live mongod. The old rejection made the documented spelling unreachable, and broke the literal-gating invariant by refusing something that is not certainly wrong."
    },
    "expr\u0000$hour({ date: $.d })": {
      "entry": "expr",
      "src": "$hour({ date: $.d })",
      "kind": "now-accepted",
      "was": "'$hour' expects a date, but got an object. Use a field path or new Date(…).",
      "now": "{\"$hour\":{\"date\":\"$d\"}}",
      "reason": "Intended, commit 4ce80f3. All 13 date accessors take a { date, timezone } document and the server accepts it -- verified on a live mongod. The old rejection made the documented spelling unreachable, and broke the literal-gating invariant by refusing something that is not certainly wrong."
    },
    "filter\u0000$hour({ date: $.d })": {
      "entry": "filter",
      "src": "$hour({ date: $.d })",
      "kind": "now-accepted",
      "was": "'$hour' expects a date, but got an object. Use a field path or new Date(…).",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$hour\":{\"date\":\"$d\"}},null]},null]},{\"$ne\":[{\"$hour\":{\"date\":\"$d\"}},false]},{\"$ne\":[{\"$hour\":{\"date\":\"$d\"}},\"\"]},{\"$ne\":[{\"$hour\":{\"date\":\"$d\"}},0]}]}}",
      "reason": "Intended, commit 4ce80f3. All 13 date accessors take a { date, timezone } document and the server accepts it -- verified on a live mongod. The old rejection made the documented spelling unreachable, and broke the literal-gating invariant by refusing something that is not certainly wrong."
    },
    "jsmql\u0000$lookup({ from: \"o\", pipeline: [$ = { t: $.total }], as: \"o\" });": {
      "entry": "jsmql",
      "src": "$lookup({ from: \"o\", pipeline: [$ = { t: $.total }], as: \"o\" });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"o\",\"pipeline\":[{\"$set\":{\"\":{\"t\":\"$total\"}}}],\"as\":\"o\"}}]",
      "now": "'$ = …' isn't available inside a literal sub-pipeline array. Write the stage directly instead — '$replaceWith({ … })'.",
      "reason": "Intended, commit 90bc46e. This loop has no slot allocator, so buffering the sugar emitted `$set: {\"\": ...}` -- mongod refuses it with 'FieldPath cannot be constructed with empty string'. Rejected now, naming the stage-call spelling that works there."
    },
    "expr\u0000$lookup({ from: \"o\", pipeline: [$ = { t: $.total }], as: \"o\" });": {
      "entry": "expr",
      "src": "$lookup({ from: \"o\", pipeline: [$ = { t: $.total }], as: \"o\" });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"o\",\"pipeline\":[{\"$set\":{\"\":{\"t\":\"$total\"}}}],\"as\":\"o\"}}]",
      "now": "'$ = …' isn't available inside a literal sub-pipeline array. Write the stage directly instead — '$replaceWith({ … })'.",
      "reason": "Intended, commit 90bc46e. This loop has no slot allocator, so buffering the sugar emitted `$set: {\"\": ...}` -- mongod refuses it with 'FieldPath cannot be constructed with empty string'. Rejected now, naming the stage-call spelling that works there."
    },
    "update\u0000$lookup({ from: \"o\", pipeline: [$ = { t: $.total }], as: \"o\" });": {
      "entry": "update",
      "src": "$lookup({ from: \"o\", pipeline: [$ = { t: $.total }], as: \"o\" });",
      "kind": "message",
      "was": "jsmql.update() rejected '$lookup' (stage 0): MongoDB's aggregation-pipeline update form only accepts $addFields, $project, $replaceRoot, $replaceWith, $set, $unset. Use jsmql.pipeline() if you need other stages.",
      "now": "'$ = …' isn't available inside a literal sub-pipeline array. Write the stage directly instead — '$replaceWith({ … })'.",
      "reason": "Intended, commit 90bc46e. This loop has no slot allocator, so buffering the sugar emitted `$set: {\"\": ...}` -- mongod refuses it with 'FieldPath cannot be constructed with empty string'. Rejected now, naming the stage-call spelling that works there."
    },
    "jsmql\u0000$unionWith({ coll: \"o\", pipeline: [$ = { a: 1 }] });": {
      "entry": "jsmql",
      "src": "$unionWith({ coll: \"o\", pipeline: [$ = { a: 1 }] });",
      "kind": "now-rejected",
      "was": "[{\"$unionWith\":{\"coll\":\"o\",\"pipeline\":[{\"$set\":{\"\":{\"a\":1}}}]}}]",
      "now": "'$ = …' isn't available inside a literal sub-pipeline array. Write the stage directly instead — '$replaceWith({ … })'.",
      "reason": "Intended, commit 90bc46e. This loop has no slot allocator, so buffering the sugar emitted `$set: {\"\": ...}` -- mongod refuses it with 'FieldPath cannot be constructed with empty string'. Rejected now, naming the stage-call spelling that works there."
    },
    "expr\u0000$unionWith({ coll: \"o\", pipeline: [$ = { a: 1 }] });": {
      "entry": "expr",
      "src": "$unionWith({ coll: \"o\", pipeline: [$ = { a: 1 }] });",
      "kind": "now-rejected",
      "was": "[{\"$unionWith\":{\"coll\":\"o\",\"pipeline\":[{\"$set\":{\"\":{\"a\":1}}}]}}]",
      "now": "'$ = …' isn't available inside a literal sub-pipeline array. Write the stage directly instead — '$replaceWith({ … })'.",
      "reason": "Intended, commit 90bc46e. This loop has no slot allocator, so buffering the sugar emitted `$set: {\"\": ...}` -- mongod refuses it with 'FieldPath cannot be constructed with empty string'. Rejected now, naming the stage-call spelling that works there."
    },
    "pipeline\u0000$unionWith({ coll: \"o\", pipeline: [$ = { a: 1 }] });": {
      "entry": "pipeline",
      "src": "$unionWith({ coll: \"o\", pipeline: [$ = { a: 1 }] });",
      "kind": "now-rejected",
      "was": "[{\"$unionWith\":{\"coll\":\"o\",\"pipeline\":[{\"$set\":{\"\":{\"a\":1}}}]}}]",
      "now": "The outer document can't be written from inside a body over another collection — only read. Write the body's own document through its callback parameter ('o.x = …', 'delete o.x', 'o = { … }'), or as a stage ('$set({ x: …",
      "reason": "Intended, commit 90bc46e. This loop has no slot allocator, so buffering the sugar emitted `$set: {\"\": ...}` -- mongod refuses it with 'FieldPath cannot be constructed with empty string'. Rejected now, naming the stage-call spelling that works there."
    },
    "update\u0000$unionWith({ coll: \"o\", pipeline: [$ = { a: 1 }] });": {
      "entry": "update",
      "src": "$unionWith({ coll: \"o\", pipeline: [$ = { a: 1 }] });",
      "kind": "message",
      "was": "jsmql.update() rejected '$unionWith' (stage 0): MongoDB's aggregation-pipeline update form only accepts $addFields, $project, $replaceRoot, $replaceWith, $set, $unset. Use jsmql.pipeline() if you need other stages.",
      "now": "'$ = …' isn't available inside a literal sub-pipeline array. Write the stage directly instead — '$replaceWith({ … })'.",
      "reason": "Intended, commit 90bc46e. This loop has no slot allocator, so buffering the sugar emitted `$set: {\"\": ...}` -- mongod refuses it with 'FieldPath cannot be constructed with empty string'. Rejected now, naming the stage-call spelling that works there."
    },
    "expr\u0000$.x = $.a.map(d => { const q = $$.push({n:d}); return q; });": {
      "entry": "expr",
      "src": "$.x = $.a.map(d => { const q = $$.push({n:d}); return q; });",
      "kind": "message",
      "was": "'$$' (current collection) is statement-only and supports '.push(...)', '.filter(...)' in the facet pattern, and '$$ = <expr>' as a top-level assignment. Write `$$.push({...})`, `$$.push(...$$$.<coll>[.filter(pred)])`, or",
      "now": "jsmql.expr() does not allow '$$.push(...)' — collection unions are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() to compose '$unionWith' stages.",
      "reason": "Intended, commit a2438b2. The union gate walked the tree with a private copy that missed Lambda.exprBlock, so a $$.push inside a block-bodied callback escaped it and produced a misdirecting downstream error. Now uses the complete ast-walk traversal."
    },
    "update\u0000$.x = $.a.map(d => { const q = $$.push({n:d}); return q; });": {
      "entry": "update",
      "src": "$.x = $.a.map(d => { const q = $$.push({n:d}); return q; });",
      "kind": "message",
      "was": "'$$' (current collection) is statement-only and supports '.push(...)', '.filter(...)' in the facet pattern, and '$$ = <expr>' as a top-level assignment. Write `$$.push({...})`, `$$.push(...$$$.<coll>[.filter(pred)])`, or",
      "now": "jsmql.update() does not allow '$$.push(...)' (collection union): MongoDB's aggregation-pipeline update form only accepts $addFields, $project, $replaceRoot, $replaceWith, $set, $unset. Run the union in a regular aggregat",
      "reason": "Intended, commit a2438b2. The union gate walked the tree with a private copy that missed Lambda.exprBlock, so a $$.push inside a block-bodied callback escaped it and produced a misdirecting downstream error. Now uses the complete ast-walk traversal."
    },
    "jsmql\u0000let k = $.x; $$ = $$$.orders.map(o => ({ t: o.items.map(v => v + k) }));": {
      "entry": "jsmql",
      "src": "let k = $.x; $$ = $$$.orders.map(o => ({ t: o.items.map(v => v + k) }));",
      "kind": "message",
      "was": "Unknown identifier 'k'. Did you mean '$.k'?",
      "now": "`k` is a `let`/`const` declared before `$$ = $$$.orders`, which replaces the stream with a different collection (a `$unionWith`, which can't correlate) — so `k` (along with the outer document and the root `$$.length`) is",
      "reason": "Intended. extendCtx enumerated 19 of GenerateCtx's 23 fields and dropped sourceSwitch, so a `let` tombstone did not survive a second lambda level and the actionable source-switch message degraded to 'Unknown identifier'. A lambda body is inside everything its surroundings are inside, so the context now spreads. The reference emits the WORSE message; this row records the improvement."
    },
    "jsmql\u0000$.a.zip($.b)": {
      "entry": "jsmql",
      "src": "$.a.zip($.b)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlZip0\":\"$a\",\"jsmqlZip1\":\"$b\"},\"in\":{\"$map\":{\"input\":{\"$range\":[0,{\"$max\":[{\"$size\":\"$$jsmqlZip0\"},{\"$size\":\"$$jsmqlZip1\"}]}]},\"as\":\"jsmqlI\",\"in\":[{\"$arrayElemAt",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$zip\":{\"inputs\":[\"$a\",\"$b\"],\"useLongestLength\":true}},null]},null]},{\"$ne\":[{\"$zip\":{\"inputs\":[\"$a\",\"$b\"],\"useLongestLength\":true}},false]},{\"$ne\":[{\"$zip\":{\"inputs\":[\"$a\",\"$b\"],\"u",
      "reason": "Intended. `.zip`/`.zipWith` now lower to `$zip` with useLongestLength -- which IS this operation, and whose padding rule is lodash's -- instead of a hand-built $let + $range + $max + per-array $arrayElemAt. Results verified byte-identical on a live mongod across ragged, equal and empty inputs. $.a.zip($.b) drops 260 chars to 55."
    },
    "expr\u0000$.a.zip($.b)": {
      "entry": "expr",
      "src": "$.a.zip($.b)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlZip0\":\"$a\",\"jsmqlZip1\":\"$b\"},\"in\":{\"$map\":{\"input\":{\"$range\":[0,{\"$max\":[{\"$size\":\"$$jsmqlZip0\"},{\"$size\":\"$$jsmqlZip1\"}]}]},\"as\":\"jsmqlI\",\"in\":[{\"$arrayElemAt\":[\"$$jsmqlZip0\",\"$$jsmqlI\"]},{\"$array",
      "now": "{\"$zip\":{\"inputs\":[\"$a\",\"$b\"],\"useLongestLength\":true}}",
      "reason": "Intended. `.zip`/`.zipWith` now lower to `$zip` with useLongestLength -- which IS this operation, and whose padding rule is lodash's -- instead of a hand-built $let + $range + $max + per-array $arrayElemAt. Results verified byte-identical on a live mongod across ragged, equal and empty inputs. $.a.zip($.b) drops 260 chars to 55."
    },
    "jsmql\u0000$.a.zip($.b, $.c)": {
      "entry": "jsmql",
      "src": "$.a.zip($.b, $.c)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlZip0\":\"$a\",\"jsmqlZip1\":\"$b\",\"jsmqlZip2\":\"$c\"},\"in\":{\"$map\":{\"input\":{\"$range\":[0,{\"$max\":[{\"$size\":\"$$jsmqlZip0\"},{\"$size\":\"$$jsmqlZip1\"},{\"$size\":\"$$jsmqlZip2",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$zip\":{\"inputs\":[\"$a\",\"$b\",\"$c\"],\"useLongestLength\":true}},null]},null]},{\"$ne\":[{\"$zip\":{\"inputs\":[\"$a\",\"$b\",\"$c\"],\"useLongestLength\":true}},false]},{\"$ne\":[{\"$zip\":{\"inputs\":[\"$a",
      "reason": "Intended. `.zip`/`.zipWith` now lower to `$zip` with useLongestLength -- which IS this operation, and whose padding rule is lodash's -- instead of a hand-built $let + $range + $max + per-array $arrayElemAt. Results verified byte-identical on a live mongod across ragged, equal and empty inputs. $.a.zip($.b) drops 260 chars to 55."
    },
    "expr\u0000$.a.zip($.b, $.c)": {
      "entry": "expr",
      "src": "$.a.zip($.b, $.c)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlZip0\":\"$a\",\"jsmqlZip1\":\"$b\",\"jsmqlZip2\":\"$c\"},\"in\":{\"$map\":{\"input\":{\"$range\":[0,{\"$max\":[{\"$size\":\"$$jsmqlZip0\"},{\"$size\":\"$$jsmqlZip1\"},{\"$size\":\"$$jsmqlZip2\"}]}]},\"as\":\"jsmqlI\",\"in\":[{\"$arrayEle",
      "now": "{\"$zip\":{\"inputs\":[\"$a\",\"$b\",\"$c\"],\"useLongestLength\":true}}",
      "reason": "Intended. `.zip`/`.zipWith` now lower to `$zip` with useLongestLength -- which IS this operation, and whose padding rule is lodash's -- instead of a hand-built $let + $range + $max + per-array $arrayElemAt. Results verified byte-identical on a live mongod across ragged, equal and empty inputs. $.a.zip($.b) drops 260 chars to 55."
    },
    "jsmql\u0000$.a.zipWith($.b, (x, y) => x + y)": {
      "entry": "jsmql",
      "src": "$.a.zipWith($.b, (x, y) => x + y)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlZip0\":\"$a\",\"jsmqlZip1\":\"$b\"},\"in\":{\"$map\":{\"input\":{\"$range\":[0,{\"$max\":[{\"$size\":\"$$jsmqlZip0\"},{\"$size\":\"$$jsmqlZip1\"}]}]},\"as\":\"jsmqlI\",\"in\":{\"$let\":{\"vars\"",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$map\":{\"input\":{\"$zip\":{\"inputs\":[\"$a\",\"$b\"],\"useLongestLength\":true}},\"as\":\"jsmqlPair\",\"in\":{\"$let\":{\"vars\":{\"x\":{\"$arrayElemAt\":[\"$$jsmqlPair\",0]},\"y\":{\"$arrayElemAt\":[\"$$jsmqlPa",
      "reason": "Intended. `.zip`/`.zipWith` now lower to `$zip` with useLongestLength -- which IS this operation, and whose padding rule is lodash's -- instead of a hand-built $let + $range + $max + per-array $arrayElemAt. Results verified byte-identical on a live mongod across ragged, equal and empty inputs. $.a.zip($.b) drops 260 chars to 55."
    },
    "expr\u0000$.a.zipWith($.b, (x, y) => x + y)": {
      "entry": "expr",
      "src": "$.a.zipWith($.b, (x, y) => x + y)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlZip0\":\"$a\",\"jsmqlZip1\":\"$b\"},\"in\":{\"$map\":{\"input\":{\"$range\":[0,{\"$max\":[{\"$size\":\"$$jsmqlZip0\"},{\"$size\":\"$$jsmqlZip1\"}]}]},\"as\":\"jsmqlI\",\"in\":{\"$let\":{\"vars\":{\"x\":{\"$arrayElemAt\":[\"$$jsmqlZip0\",\"",
      "now": "{\"$map\":{\"input\":{\"$zip\":{\"inputs\":[\"$a\",\"$b\"],\"useLongestLength\":true}},\"as\":\"jsmqlPair\",\"in\":{\"$let\":{\"vars\":{\"x\":{\"$arrayElemAt\":[\"$$jsmqlPair\",0]},\"y\":{\"$arrayElemAt\":[\"$$jsmqlPair\",1]}},\"in\":{\"$add\":[\"$$x\",\"$$y\"]}}}",
      "reason": "Intended. `.zip`/`.zipWith` now lower to `$zip` with useLongestLength -- which IS this operation, and whose padding rule is lodash's -- instead of a hand-built $let + $range + $max + per-array $arrayElemAt. Results verified byte-identical on a live mongod across ragged, equal and empty inputs. $.a.zip($.b) drops 260 chars to 55."
    },
    "jsmql\u0000$.code.padStart(5, \"0\")": {
      "entry": "jsmql",
      "src": "$.code.padStart(5, \"0\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},5]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\"",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[5,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$conca",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.code.padStart(5, \"0\")": {
      "entry": "expr",
      "src": "$.code.padStart(5, \"0\")",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},5]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[5,{\"$strLenCP\":\"$$js",
      "now": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[5,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\"0\"]}}},\"$$jsmqlPad\"]}}}",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.code.padStart(5, \"US\")": {
      "entry": "jsmql",
      "src": "$.code.padStart(5, \"US\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},5]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"in",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[5,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\"",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.code.padStart(5, \"US\")": {
      "entry": "expr",
      "src": "$.code.padStart(5, \"US\")",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},5]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[5,{\"$s",
      "now": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[5,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\"US\"]}}},0",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.code.padStart(5, \"$sep\")": {
      "entry": "jsmql",
      "src": "$.code.padStart(5, \"$sep\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},5]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"in",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[5,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\"",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.code.padStart(5, \"$sep\")": {
      "entry": "expr",
      "src": "$.code.padStart(5, \"$sep\")",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},5]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[5,{\"$s",
      "now": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[5,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\"$sep\"]}}}",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.code.padStart(5, $.sep)": {
      "entry": "jsmql",
      "src": "$.code.padStart(5, $.sep)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},5]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"in",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[5,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\"",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.code.padStart(5, $.sep)": {
      "entry": "expr",
      "src": "$.code.padStart(5, $.sep)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},5]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[5,{\"$s",
      "now": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$code\",\"\"]}},\"in\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[5,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\"$sep\"]}}}",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.s.padStart(10)": {
      "entry": "jsmql",
      "src": "$.s.padStart(10)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},10]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[10,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\"",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.s.padStart(10)": {
      "entry": "expr",
      "src": "$.s.padStart(10)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},10]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[10,{\"$strLenCP\":\"$$jsm",
      "now": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[10,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\" \"]}}},\"$$jsmqlPad\"]}}}",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.s.padEnd(10, \"-\")": {
      "entry": "jsmql",
      "src": "$.s.padEnd(10, \"-\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},10]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[\"$$jsmqlPad\",{\"$reduce\":{\"input",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$concat\":[\"$$jsmqlPad\",{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[10,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"i",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.s.padEnd(10, \"-\")": {
      "entry": "expr",
      "src": "$.s.padEnd(10, \"-\")",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},10]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[\"$$jsmqlPad\",{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[10,{\"$str",
      "now": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$concat\":[\"$$jsmqlPad\",{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[10,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\"-\"]}}}]}}}",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.items.map(s => s.code.padStart(s.width, s.pad))": {
      "entry": "jsmql",
      "src": "$.items.map(s => s.code.padStart(s.width, s.pad))",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$map\":{\"input\":\"$items\",\"as\":\"s\",\"in\":{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$$s.code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},\"$$s.width\"]},\"then\":\"$$jsmql",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$map\":{\"input\":\"$items\",\"as\":\"s\",\"in\":{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$$s.code\",\"\"]}},\"in\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[\"$$s.wi",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.items.map(s => s.code.padStart(s.width, s.pad))": {
      "entry": "expr",
      "src": "$.items.map(s => s.code.padStart(s.width, s.pad))",
      "kind": "output",
      "was": "{\"$map\":{\"input\":\"$items\",\"as\":\"s\",\"in\":{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$$s.code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},\"$$s.width\"]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$substrCP\":[",
      "now": "{\"$map\":{\"input\":\"$items\",\"as\":\"s\",\"in\":{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$$s.code\",\"\"]}},\"in\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[\"$$s.width\",{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"i",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.items.map(jsmqlPad => jsmqlPad.code.padStart(jsmqlPad.width))": {
      "entry": "jsmql",
      "src": "$.items.map(jsmqlPad => jsmqlPad.code.padStart(jsmqlPad.width))",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$map\":{\"input\":\"$items\",\"as\":\"jsmqlPad\",\"in\":{\"$let\":{\"vars\":{\"jsmqlPad2\":{\"$ifNull\":[\"$$jsmqlPad.code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad2\"},\"$$jsmqlPad.w",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$map\":{\"input\":\"$items\",\"as\":\"jsmqlPad\",\"in\":{\"$let\":{\"vars\":{\"jsmqlPad2\":{\"$ifNull\":[\"$$jsmqlPad.code\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[\"$$jsm",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.items.map(jsmqlPad => jsmqlPad.code.padStart(jsmqlPad.width))": {
      "entry": "expr",
      "src": "$.items.map(jsmqlPad => jsmqlPad.code.padStart(jsmqlPad.width))",
      "kind": "output",
      "was": "{\"$map\":{\"input\":\"$items\",\"as\":\"jsmqlPad\",\"in\":{\"$let\":{\"vars\":{\"jsmqlPad2\":{\"$ifNull\":[\"$$jsmqlPad.code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad2\"},\"$$jsmqlPad.width\"]},\"then\":\"$$jsmqlPad2\",\"else\":{\"",
      "now": "{\"$map\":{\"input\":\"$items\",\"as\":\"jsmqlPad\",\"in\":{\"$let\":{\"vars\":{\"jsmqlPad2\":{\"$ifNull\":[\"$$jsmqlPad.code\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[\"$$jsmqlPad.width\",{\"$strLenCP\":\"$$jsmqlPad2",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.items.map(s => s.code.padStart(3, s.pad))": {
      "entry": "jsmql",
      "src": "$.items.map(s => s.code.padStart(3, s.pad))",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$map\":{\"input\":\"$items\",\"as\":\"s\",\"in\":{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$$s.code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},3]},\"then\":\"$$jsmqlPad\",\"else",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$map\":{\"input\":\"$items\",\"as\":\"s\",\"in\":{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$$s.code\",\"\"]}},\"in\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[3,{\"$st",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.items.map(s => s.code.padStart(3, s.pad))": {
      "entry": "expr",
      "src": "$.items.map(s => s.code.padStart(3, s.pad))",
      "kind": "output",
      "was": "{\"$map\":{\"input\":\"$items\",\"as\":\"s\",\"in\":{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$$s.code\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},3]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\"",
      "now": "{\"$map\":{\"input\":\"$items\",\"as\":\"s\",\"in\":{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$$s.code\",\"\"]}},\"in\":{\"$concat\":[{\"$substrCP\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[3,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValu",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.arr.padStart(4,'x')": {
      "entry": "jsmql",
      "src": "$.arr.padStart(4,'x')",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$arr\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},4]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$arr\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.arr.padStart(4,'x')": {
      "entry": "expr",
      "src": "$.arr.padStart(4,'x')",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$arr\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},4]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsm",
      "now": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$arr\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\"x\"]}}},\"$$jsmqlPad\"]}}}",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.s.padStart(4,'x')": {
      "entry": "jsmql",
      "src": "$.s.padStart(4,'x')",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},4]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.s.padStart(4,'x')": {
      "entry": "expr",
      "src": "$.s.padStart(4,'x')",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},4]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmql",
      "now": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\"x\"]}}},\"$$jsmqlPad\"]}}}",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.n.padStart(4,'x')": {
      "entry": "jsmql",
      "src": "$.n.padStart(4,'x')",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$n\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},4]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$n\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.n.padStart(4,'x')": {
      "entry": "expr",
      "src": "$.n.padStart(4,'x')",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$n\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},4]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmql",
      "now": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$n\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\"x\"]}}},\"$$jsmqlPad\"]}}}",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.obj.padStart(4,'x')": {
      "entry": "jsmql",
      "src": "$.obj.padStart(4,'x')",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$obj\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},4]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$obj\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000$.obj.padStart(4,'x')": {
      "entry": "expr",
      "src": "$.obj.padStart(4,'x')",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$obj\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},4]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsm",
      "now": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"$obj\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\"x\"]}}},\"$$jsmqlPad\"]}}}",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000\"abc\".padStart(4,'x')": {
      "entry": "jsmql",
      "src": "\"abc\".padStart(4,'x')",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"abc\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},4]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"abc\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\"",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "expr\u0000\"abc\".padStart(4,'x')": {
      "entry": "expr",
      "src": "\"abc\".padStart(4,'x')",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"abc\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},4]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmq",
      "now": "{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"abc\",\"\"]}},\"in\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[0,{\"$subtract\":[4,{\"$strLenCP\":\"$$jsmqlPad\"}]}]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\"x\"]}}},\"$$jsmqlPad\"]}}}",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "filter\u0000\"abc\".padStart(4,'x')": {
      "entry": "filter",
      "src": "\"abc\".padStart(4,'x')",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlPad\":{\"$ifNull\":[\"abc\",\"\"]}},\"in\":{\"$cond\":{\"if\":{\"$gte\":[{\"$strLenCP\":\"$$jsmqlPad\"},4]},\"then\":\"$$jsmqlPad\",\"else\":{\"$concat\":[{\"$reduce\":{\"input\":{\"$range\":[",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"xabc\",null]},null]},{\"$ne\":[\"xabc\",false]},{\"$ne\":[\"xabc\",\"\"]},{\"$ne\":[\"xabc\",0]}]}}",
      "reason": "Intended. .padStart/.padEnd carried a `$cond` on `$strLenCP >= target` selecting between two expressions that agree: when the receiver already reaches the target, `need` is <= 0, `$range: [0, need]` is empty, the filler is \"\", and the concat returns the receiver unchanged. Verified on a live mongod across over-long, exact, short, empty and missing receivers, single- and multi-character pads, both sides. Drops 84 chars per occurrence."
    },
    "jsmql\u0000$.ts.getUTCHours()": {
      "entry": "jsmql",
      "src": "$.ts.getUTCHours()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$hour\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},null]},null]},{\"$ne\":[{\"$hour\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},false]},{\"$ne\":[{\"$hour\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},\"\"]},{\"$ne\":[{\"$ho",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$hour\":\"$ts\"},null]},null]},{\"$ne\":[{\"$hour\":\"$ts\"},false]},{\"$ne\":[{\"$hour\":\"$ts\"},\"\"]},{\"$ne\":[{\"$hour\":\"$ts\"},0]}]}}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "expr\u0000$.ts.getUTCHours()": {
      "entry": "expr",
      "src": "$.ts.getUTCHours()",
      "kind": "output",
      "was": "{\"$hour\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}}",
      "now": "{\"$hour\":\"$ts\"}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "jsmql\u0000$.ts.getUTCFullYear()": {
      "entry": "jsmql",
      "src": "$.ts.getUTCFullYear()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$year\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},null]},null]},{\"$ne\":[{\"$year\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},false]},{\"$ne\":[{\"$year\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},\"\"]},{\"$ne\":[{\"$ye",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$year\":\"$ts\"},null]},null]},{\"$ne\":[{\"$year\":\"$ts\"},false]},{\"$ne\":[{\"$year\":\"$ts\"},\"\"]},{\"$ne\":[{\"$year\":\"$ts\"},0]}]}}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "expr\u0000$.ts.getUTCFullYear()": {
      "entry": "expr",
      "src": "$.ts.getUTCFullYear()",
      "kind": "output",
      "was": "{\"$year\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}}",
      "now": "{\"$year\":\"$ts\"}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "jsmql\u0000$.ts.getUTCMonth()": {
      "entry": "jsmql",
      "src": "$.ts.getUTCMonth()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$month\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},null]},null]},{\"$ne\":[{\"$month\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},false]},{\"$ne\":[{\"$month\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},\"\"]},{\"$ne\":[{\"",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$month\":\"$ts\"},null]},null]},{\"$ne\":[{\"$month\":\"$ts\"},false]},{\"$ne\":[{\"$month\":\"$ts\"},\"\"]},{\"$ne\":[{\"$month\":\"$ts\"},0]}]}}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "expr\u0000$.ts.getUTCMonth()": {
      "entry": "expr",
      "src": "$.ts.getUTCMonth()",
      "kind": "output",
      "was": "{\"$month\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}}",
      "now": "{\"$month\":\"$ts\"}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "jsmql\u0000$.ts.getUTCDate()": {
      "entry": "jsmql",
      "src": "$.ts.getUTCDate()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$dayOfMonth\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},null]},null]},{\"$ne\":[{\"$dayOfMonth\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},false]},{\"$ne\":[{\"$dayOfMonth\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$dayOfMonth\":\"$ts\"},null]},null]},{\"$ne\":[{\"$dayOfMonth\":\"$ts\"},false]},{\"$ne\":[{\"$dayOfMonth\":\"$ts\"},\"\"]},{\"$ne\":[{\"$dayOfMonth\":\"$ts\"},0]}]}}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "expr\u0000$.ts.getUTCDate()": {
      "entry": "expr",
      "src": "$.ts.getUTCDate()",
      "kind": "output",
      "was": "{\"$dayOfMonth\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}}",
      "now": "{\"$dayOfMonth\":\"$ts\"}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "jsmql\u0000$.ts.getUTCDay()": {
      "entry": "jsmql",
      "src": "$.ts.getUTCDay()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$dayOfWeek\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},null]},null]},{\"$ne\":[{\"$dayOfWeek\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},false]},{\"$ne\":[{\"$dayOfWeek\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},\"\"]",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$dayOfWeek\":\"$ts\"},null]},null]},{\"$ne\":[{\"$dayOfWeek\":\"$ts\"},false]},{\"$ne\":[{\"$dayOfWeek\":\"$ts\"},\"\"]},{\"$ne\":[{\"$dayOfWeek\":\"$ts\"},0]}]}}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "expr\u0000$.ts.getUTCDay()": {
      "entry": "expr",
      "src": "$.ts.getUTCDay()",
      "kind": "output",
      "was": "{\"$dayOfWeek\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}}",
      "now": "{\"$dayOfWeek\":\"$ts\"}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "jsmql\u0000$.ts.getUTCMinutes()": {
      "entry": "jsmql",
      "src": "$.ts.getUTCMinutes()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$minute\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},null]},null]},{\"$ne\":[{\"$minute\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},false]},{\"$ne\":[{\"$minute\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},\"\"]},{\"$ne\":",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$minute\":\"$ts\"},null]},null]},{\"$ne\":[{\"$minute\":\"$ts\"},false]},{\"$ne\":[{\"$minute\":\"$ts\"},\"\"]},{\"$ne\":[{\"$minute\":\"$ts\"},0]}]}}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "expr\u0000$.ts.getUTCMinutes()": {
      "entry": "expr",
      "src": "$.ts.getUTCMinutes()",
      "kind": "output",
      "was": "{\"$minute\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}}",
      "now": "{\"$minute\":\"$ts\"}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "jsmql\u0000$.ts.getUTCSeconds()": {
      "entry": "jsmql",
      "src": "$.ts.getUTCSeconds()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$second\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},null]},null]},{\"$ne\":[{\"$second\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},false]},{\"$ne\":[{\"$second\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},\"\"]},{\"$ne\":",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$second\":\"$ts\"},null]},null]},{\"$ne\":[{\"$second\":\"$ts\"},false]},{\"$ne\":[{\"$second\":\"$ts\"},\"\"]},{\"$ne\":[{\"$second\":\"$ts\"},0]}]}}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "expr\u0000$.ts.getUTCSeconds()": {
      "entry": "expr",
      "src": "$.ts.getUTCSeconds()",
      "kind": "output",
      "was": "{\"$second\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}}",
      "now": "{\"$second\":\"$ts\"}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "jsmql\u0000$.ts.getUTCMilliseconds()": {
      "entry": "jsmql",
      "src": "$.ts.getUTCMilliseconds()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$millisecond\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},null]},null]},{\"$ne\":[{\"$millisecond\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}},false]},{\"$ne\":[{\"$millisecond\":{\"date\":\"$ts\",\"timezone\":\"UTC\"",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$millisecond\":\"$ts\"},null]},null]},{\"$ne\":[{\"$millisecond\":\"$ts\"},false]},{\"$ne\":[{\"$millisecond\":\"$ts\"},\"\"]},{\"$ne\":[{\"$millisecond\":\"$ts\"},0]}]}}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "expr\u0000$.ts.getUTCMilliseconds()": {
      "entry": "expr",
      "src": "$.ts.getUTCMilliseconds()",
      "kind": "output",
      "was": "{\"$millisecond\":{\"date\":\"$ts\",\"timezone\":\"UTC\"}}",
      "now": "{\"$millisecond\":\"$ts\"}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "jsmql\u0000$.ts.toISOString()": {
      "entry": "jsmql",
      "src": "$.ts.toISOString()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$dateToString\":{\"date\":\"$ts\",\"format\":\"%Y-%m-%dT%H:%M:%S.%LZ\"}},null]},null]},{\"$ne\":[{\"$dateToString\":{\"date\":\"$ts\",\"format\":\"%Y-%m-%dT%H:%M:%S.%LZ\"}},false]},{\"$ne\":[{\"$dateToStr",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$dateToString\":{\"date\":\"$ts\"}},null]},null]},{\"$ne\":[{\"$dateToString\":{\"date\":\"$ts\"}},false]},{\"$ne\":[{\"$dateToString\":{\"date\":\"$ts\"}},\"\"]},{\"$ne\":[{\"$dateToString\":{\"date\":\"$ts\"}}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "expr\u0000$.ts.toISOString()": {
      "entry": "expr",
      "src": "$.ts.toISOString()",
      "kind": "output",
      "was": "{\"$dateToString\":{\"date\":\"$ts\",\"format\":\"%Y-%m-%dT%H:%M:%S.%LZ\"}}",
      "now": "{\"$dateToString\":{\"date\":\"$ts\"}}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form."
    },
    "jsmql\u0000$$.nosuchmethod({a:1})": {
      "entry": "jsmql",
      "src": "$$.nosuchmethod({a:1})",
      "kind": "message",
      "was": "'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSi",
      "now": "'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSi",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "pipeline\u0000$$.nosuchmethod({a:1})": {
      "entry": "pipeline",
      "src": "$$.nosuchmethod({a:1})",
      "kind": "message",
      "was": "'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSi",
      "now": "'.nosuchmethod()' is not a method of the stream '$$'. A stage is a link too: '$$.$match(…)'.",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "update\u0000$$.nosuchmethod({a:1})": {
      "entry": "update",
      "src": "$$.nosuchmethod({a:1})",
      "kind": "message",
      "was": "'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSi",
      "now": "'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSi",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "jsmql\u0000$$.pop({a:1})": {
      "entry": "jsmql",
      "src": "$$.pop({a:1})",
      "kind": "message",
      "was": "'.pop(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuff",
      "now": "'.pop(...)' isn't available on '$$' — it removes the last element by mutating. Take a prefix instead: '.slice(0, -1)'.",
      "reason": "Intended. An array-receiver method with no stream form now carries a WRITTEN reason (STREAM_UNSUPPORTED) instead of falling to the generic 'not a chainable stream method' list. The list says what else exists; it never said why this one is absent. Every reason names what to write instead."
    },
    "update\u0000$$.pop({a:1})": {
      "entry": "update",
      "src": "$$.pop({a:1})",
      "kind": "message",
      "was": "'.pop(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuff",
      "now": "'.pop(...)' isn't available on '$$' — it removes the last element by mutating. Take a prefix instead: '.slice(0, -1)'.",
      "reason": "Intended. An array-receiver method with no stream form now carries a WRITTEN reason (STREAM_UNSUPPORTED) instead of falling to the generic 'not a chainable stream method' list. The list says what else exists; it never said why this one is absent. Every reason names what to write instead."
    },
    "jsmql\u0000$$ = $$.slise(0, 5);": {
      "entry": "jsmql",
      "src": "$$ = $$.slise(0, 5);",
      "kind": "message",
      "was": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "now": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "expr\u0000$$ = $$.slise(0, 5);": {
      "entry": "expr",
      "src": "$$ = $$.slise(0, 5);",
      "kind": "message",
      "was": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "now": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "pipeline\u0000$$ = $$.slise(0, 5);": {
      "entry": "pipeline",
      "src": "$$ = $$.slise(0, 5);",
      "kind": "message",
      "was": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "now": "'.slise()' is not a method of the stream '$$'. Did you mean '.slice()'? A stage is a link too: '$$.$match(…)'.",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "update\u0000$$ = $$.slise(0, 5);": {
      "entry": "update",
      "src": "$$ = $$.slise(0, 5);",
      "kind": "message",
      "was": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "now": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "jsmql\u0000$$ = $$.filter(o => o.active).bogus();": {
      "entry": "jsmql",
      "src": "$$ = $$.filter(o => o.active).bogus();",
      "kind": "message",
      "was": "'.bogus(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSize, .co",
      "now": "'.bogus(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSize, .co",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "expr\u0000$$ = $$.filter(o => o.active).bogus();": {
      "entry": "expr",
      "src": "$$ = $$.filter(o => o.active).bogus();",
      "kind": "message",
      "was": "'.bogus(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSize, .co",
      "now": "'.bogus(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSize, .co",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "pipeline\u0000$$ = $$.filter(o => o.active).bogus();": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(o => o.active).bogus();",
      "kind": "message",
      "was": "'.bogus(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSize, .co",
      "now": "'.bogus()' is not a method of the stream '$$'. A stage is a link too: '$$.$match(…)'.",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "update\u0000$$ = $$.filter(o => o.active).bogus();": {
      "entry": "update",
      "src": "$$ = $$.filter(o => o.active).bogus();",
      "kind": "message",
      "was": "'.bogus(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSize, .co",
      "now": "'.bogus(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSize, .co",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "jsmql\u0000$$.slise(0, 5);": {
      "entry": "jsmql",
      "src": "$$.slise(0, 5);",
      "kind": "message",
      "was": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "now": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "expr\u0000$$.slise(0, 5);": {
      "entry": "expr",
      "src": "$$.slise(0, 5);",
      "kind": "message",
      "was": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "now": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "pipeline\u0000$$.slise(0, 5);": {
      "entry": "pipeline",
      "src": "$$.slise(0, 5);",
      "kind": "message",
      "was": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "now": "'.slise()' is not a method of the stream '$$'. Did you mean '.slice()'? A stage is a link too: '$$.$match(…)'.",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "update\u0000$$.slise(0, 5);": {
      "entry": "update",
      "src": "$$.slise(0, 5);",
      "kind": "message",
      "was": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "now": "'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .sh",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "jsmql\u0000$$.nosuchmethod(\"x\")": {
      "entry": "jsmql",
      "src": "$$.nosuchmethod(\"x\")",
      "kind": "message",
      "was": "'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSi",
      "now": "'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSi",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "pipeline\u0000$$.nosuchmethod(\"x\")": {
      "entry": "pipeline",
      "src": "$$.nosuchmethod(\"x\")",
      "kind": "message",
      "was": "'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSi",
      "now": "'.nosuchmethod()' is not a method of the stream '$$'. A stage is a link too: '$$.$match(…)'.",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "update\u0000$$.nosuchmethod(\"x\")": {
      "entry": "update",
      "src": "$$.nosuchmethod(\"x\")",
      "kind": "message",
      "was": "'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSi",
      "now": "'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuffle, .sampleSi",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "jsmql\u0000$$.pop(\"x\")": {
      "entry": "jsmql",
      "src": "$$.pop(\"x\")",
      "kind": "message",
      "was": "'.pop(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuff",
      "now": "'.pop(...)' isn't available on '$$' — it removes the last element by mutating. Take a prefix instead: '.slice(0, -1)'.",
      "reason": "Intended. An array-receiver method with no stream form now carries a WRITTEN reason (STREAM_UNSUPPORTED) instead of falling to the generic 'not a chainable stream method' list. The list says what else exists; it never said why this one is absent. Every reason names what to write instead."
    },
    "update\u0000$$.pop(\"x\")": {
      "entry": "update",
      "src": "$$.pop(\"x\")",
      "kind": "message",
      "was": "'.pop(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shuff",
      "now": "'.pop(...)' isn't available on '$$' — it removes the last element by mutating. Take a prefix instead: '.slice(0, -1)'.",
      "reason": "Intended. An array-receiver method with no stream form now carries a WRITTEN reason (STREAM_UNSUPPORTED) instead of falling to the generic 'not a chainable stream method' list. The list says what else exists; it never said why this one is absent. Every reason names what to write instead."
    },
    "jsmql\u0000$$ = $$.dropp();": {
      "entry": "jsmql",
      "src": "$$ = $$.dropp();",
      "kind": "message",
      "was": "'.dropp(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shu",
      "now": "'.dropp(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shu",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "expr\u0000$$ = $$.dropp();": {
      "entry": "expr",
      "src": "$$ = $$.dropp();",
      "kind": "message",
      "was": "'.dropp(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shu",
      "now": "'.dropp(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shu",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "pipeline\u0000$$ = $$.dropp();": {
      "entry": "pipeline",
      "src": "$$ = $$.dropp();",
      "kind": "message",
      "was": "'.dropp(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shu",
      "now": "'.dropp()' is not a method of the stream '$$'. Did you mean '.drop()'? A stage is a link too: '$$.$match(…)'.",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "update\u0000$$ = $$.dropp();": {
      "entry": "update",
      "src": "$$ = $$.dropp();",
      "kind": "message",
      "was": "'.dropp(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shu",
      "now": "'.dropp(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .dropWhile, .shu",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew."
    },
    "jsmql\u0000$sampleRate(0.1)": {
      "entry": "jsmql",
      "src": "$sampleRate(0.1)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$sampleRate\":0.1},null]},null]},{\"$ne\":[{\"$sampleRate\":0.1},false]},{\"$ne\":[{\"$sampleRate\":0.1},\"\"]},{\"$ne\":[{\"$sampleRate\":0.1},0]}]}}",
      "now": "{\"$sampleRate\":0.1}",
      "reason": "Intended. $sampleRate is query-position-only: MongoDB has no expression form, so the old $expr wrap emitted MQL the server refused with \"Unrecognized expression '$sampleRate'\". The new `matchOnly` flag on OperatorDef makes the match translator lower it to its query form and codegen reject it everywhere else. Verified on a live mongod in $match, in a compound predicate, and through find(). Closes the last known HR3 violation."
    },
    "expr\u0000$sampleRate(0.1)": {
      "entry": "expr",
      "src": "$sampleRate(0.1)",
      "kind": "now-rejected",
      "was": "{\"$sampleRate\":0.1}",
      "now": "$sampleRate is a query operator — it only works as a '$match' condition, and MongoDB has no expression form for it. Write it as a predicate: '$match($sampleRate(<value>))' or '$match($.field > 1 && $sampleRate(<value>))'",
      "reason": "Intended. $sampleRate is query-position-only: MongoDB has no expression form, so the old $expr wrap emitted MQL the server refused with \"Unrecognized expression '$sampleRate'\". The new `matchOnly` flag on OperatorDef makes the match translator lower it to its query form and codegen reject it everywhere else. Verified on a live mongod in $match, in a compound predicate, and through find(). Closes the last known HR3 violation."
    },
    "filter\u0000$sampleRate(0.1)": {
      "entry": "filter",
      "src": "$sampleRate(0.1)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$sampleRate\":0.1},null]},null]},{\"$ne\":[{\"$sampleRate\":0.1},false]},{\"$ne\":[{\"$sampleRate\":0.1},\"\"]},{\"$ne\":[{\"$sampleRate\":0.1},0]}]}}",
      "now": "{\"$sampleRate\":0.1}",
      "reason": "Intended. $sampleRate is query-position-only: MongoDB has no expression form, so the old $expr wrap emitted MQL the server refused with \"Unrecognized expression '$sampleRate'\". The new `matchOnly` flag on OperatorDef makes the match translator lower it to its query form and codegen reject it everywhere else. Verified on a live mongod in $match, in a compound predicate, and through find(). Closes the last known HR3 violation."
    },
    "jsmql\u0000$match($sampleRate(0.1));": {
      "entry": "jsmql",
      "src": "$match($sampleRate(0.1));",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$sampleRate\":0.1},null]},null]},{\"$ne\":[{\"$sampleRate\":0.1},false]},{\"$ne\":[{\"$sampleRate\":0.1},\"\"]},{\"$ne\":[{\"$sampleRate\":0.1},0]}]}}}]",
      "now": "[{\"$match\":{\"$sampleRate\":0.1}}]",
      "reason": "Intended. $sampleRate is query-position-only: MongoDB has no expression form, so the old $expr wrap emitted MQL the server refused with \"Unrecognized expression '$sampleRate'\". The new `matchOnly` flag on OperatorDef makes the match translator lower it to its query form and codegen reject it everywhere else. Verified on a live mongod in $match, in a compound predicate, and through find(). Closes the last known HR3 violation."
    },
    "expr\u0000$match($sampleRate(0.1));": {
      "entry": "expr",
      "src": "$match($sampleRate(0.1));",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$sampleRate\":0.1},null]},null]},{\"$ne\":[{\"$sampleRate\":0.1},false]},{\"$ne\":[{\"$sampleRate\":0.1},\"\"]},{\"$ne\":[{\"$sampleRate\":0.1},0]}]}}}]",
      "now": "[{\"$match\":{\"$sampleRate\":0.1}}]",
      "reason": "Intended. $sampleRate is query-position-only: MongoDB has no expression form, so the old $expr wrap emitted MQL the server refused with \"Unrecognized expression '$sampleRate'\". The new `matchOnly` flag on OperatorDef makes the match translator lower it to its query form and codegen reject it everywhere else. Verified on a live mongod in $match, in a compound predicate, and through find(). Closes the last known HR3 violation."
    },
    "pipeline\u0000$match($sampleRate(0.1));": {
      "entry": "pipeline",
      "src": "$match($sampleRate(0.1));",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$sampleRate\":0.1},null]},null]},{\"$ne\":[{\"$sampleRate\":0.1},false]},{\"$ne\":[{\"$sampleRate\":0.1},\"\"]},{\"$ne\":[{\"$sampleRate\":0.1},0]}]}}}]",
      "now": "[{\"$match\":{\"$sampleRate\":0.1}}]",
      "reason": "Intended. $sampleRate is query-position-only: MongoDB has no expression form, so the old $expr wrap emitted MQL the server refused with \"Unrecognized expression '$sampleRate'\". The new `matchOnly` flag on OperatorDef makes the match translator lower it to its query form and codegen reject it everywhere else. Verified on a live mongod in $match, in a compound predicate, and through find(). Closes the last known HR3 violation."
    },
    "jsmql\u0000$.age > 18 && $sampleRate(0.1)": {
      "entry": "jsmql",
      "src": "$.age > 18 && $sampleRate(0.1)",
      "kind": "output",
      "was": "{\"age\":{\"$gt\":18},\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$sampleRate\":0.1},null]},null]},{\"$ne\":[{\"$sampleRate\":0.1},false]},{\"$ne\":[{\"$sampleRate\":0.1},\"\"]},{\"$ne\":[{\"$sampleRate\":0.1},0]}]}}",
      "now": "{\"age\":{\"$gt\":18},\"$sampleRate\":0.1}",
      "reason": "Intended. $sampleRate is query-position-only: MongoDB has no expression form, so the old $expr wrap emitted MQL the server refused with \"Unrecognized expression '$sampleRate'\". The new `matchOnly` flag on OperatorDef makes the match translator lower it to its query form and codegen reject it everywhere else. Verified on a live mongod in $match, in a compound predicate, and through find(). Closes the last known HR3 violation."
    },
    "expr\u0000$.age > 18 && $sampleRate(0.1)": {
      "entry": "expr",
      "src": "$.age > 18 && $sampleRate(0.1)",
      "kind": "now-rejected",
      "was": "{\"$cond\":{\"if\":{\"$gt\":[\"$age\",18]},\"then\":{\"$sampleRate\":0.1},\"else\":{\"$gt\":[\"$age\",18]}}}",
      "now": "$sampleRate is a query operator — it only works as a '$match' condition, and MongoDB has no expression form for it. Write it as a predicate: '$match($sampleRate(<value>))' or '$match($.field > 1 && $sampleRate(<value>))'",
      "reason": "Intended. $sampleRate is query-position-only: MongoDB has no expression form, so the old $expr wrap emitted MQL the server refused with \"Unrecognized expression '$sampleRate'\". The new `matchOnly` flag on OperatorDef makes the match translator lower it to its query form and codegen reject it everywhere else. Verified on a live mongod in $match, in a compound predicate, and through find(). Closes the last known HR3 violation."
    },
    "filter\u0000$.age > 18 && $sampleRate(0.1)": {
      "entry": "filter",
      "src": "$.age > 18 && $sampleRate(0.1)",
      "kind": "output",
      "was": "{\"age\":{\"$gt\":18},\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$sampleRate\":0.1},null]},null]},{\"$ne\":[{\"$sampleRate\":0.1},false]},{\"$ne\":[{\"$sampleRate\":0.1},\"\"]},{\"$ne\":[{\"$sampleRate\":0.1},0]}]}}",
      "now": "{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}},\"$sampleRate\":0.1}",
      "reason": "Intended. $sampleRate is query-position-only: MongoDB has no expression form, so the old $expr wrap emitted MQL the server refused with \"Unrecognized expression '$sampleRate'\". The new `matchOnly` flag on OperatorDef makes the match translator lower it to its query form and codegen reject it everywhere else. Verified on a live mongod in $match, in a compound predicate, and through find(). Closes the last known HR3 violation."
    },
    "jsmql\u0000$.tags.uniq().at(-1)": {
      "entry": "jsmql",
      "src": "$.tags.uniq().at(-1)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$arrayElemAt\":[{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},-1]},null]},null]},{",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$arrayElemAt\":[{\"$setUnion\":\"$tags\"},-1]},null]},null]},{\"$ne\":[{\"$arrayElemAt\":[{\"$setUnion\":\"$tags\"},-1]},false]},{\"$ne\":[{\"$arrayElemAt\":[{\"$setUnion\":\"$tags\"},-1]},\"\"]},{\"$ne\":",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "expr\u0000$.tags.uniq().at(-1)": {
      "entry": "expr",
      "src": "$.tags.uniq().at(-1)",
      "kind": "output",
      "was": "{\"$arrayElemAt\":[{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},-1]}",
      "now": "{\"$arrayElemAt\":[{\"$setUnion\":\"$tags\"},-1]}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000$.tags.uniq()": {
      "entry": "jsmql",
      "src": "$.tags.uniq()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},null]},null]},{\"$ne\":[{\"$reduce\":{\"in",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$setUnion\":\"$tags\"},null]},null]},{\"$ne\":[{\"$setUnion\":\"$tags\"},false]},{\"$ne\":[{\"$setUnion\":\"$tags\"},\"\"]},{\"$ne\":[{\"$setUnion\":\"$tags\"},0]}]}}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "expr\u0000$.tags.uniq()": {
      "entry": "expr",
      "src": "$.tags.uniq()",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}",
      "now": "{\"$setUnion\":\"$tags\"}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000$.a.intersection($.b)": {
      "entry": "jsmql",
      "src": "$.a.intersection($.b)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$filter\":{\"input\":\"$a\",\"as\":\"jsmqlItem\",\"cond\":{\"$in\":[\"$$jsmqlItem\",\"$b\"]}}},null]},null]},{\"$ne\":[{\"$filter\":{\"input\":\"$a\",\"as\":\"jsmqlItem\",\"cond\":{\"$in\":[\"$$jsmqlItem\",\"$b\"]}}},",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$setIntersection\":[\"$a\",\"$b\"]},null]},null]},{\"$ne\":[{\"$setIntersection\":[\"$a\",\"$b\"]},false]},{\"$ne\":[{\"$setIntersection\":[\"$a\",\"$b\"]},\"\"]},{\"$ne\":[{\"$setIntersection\":[\"$a\",\"$b\"]}",
      "reason": "Intended, and a SET change not just an order change. lodash documents .intersection as returning UNIQUE values; the old $filter kept the receiver's duplicates, matching neither lodash nor MongoDB ([3,3,2] for a=[3,3,2,1] b=[3,2]). $setIntersection is unique, so this moves TOWARDS the documented contract. Verified on a live mongod."
    },
    "expr\u0000$.a.intersection($.b)": {
      "entry": "expr",
      "src": "$.a.intersection($.b)",
      "kind": "output",
      "was": "{\"$filter\":{\"input\":\"$a\",\"as\":\"jsmqlItem\",\"cond\":{\"$in\":[\"$$jsmqlItem\",\"$b\"]}}}",
      "now": "{\"$setIntersection\":[\"$a\",\"$b\"]}",
      "reason": "Intended, and a SET change not just an order change. lodash documents .intersection as returning UNIQUE values; the old $filter kept the receiver's duplicates, matching neither lodash nor MongoDB ([3,3,2] for a=[3,3,2,1] b=[3,2]). $setIntersection is unique, so this moves TOWARDS the documented contract. Verified on a live mongod."
    },
    "jsmql\u0000$.a.union($.b)": {
      "entry": "jsmql",
      "src": "$.a.union($.b)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":{\"$concatArrays\":[\"$a\",\"$b\"]},\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},null]},null]},{",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$setUnion\":[\"$a\",\"$b\"]},null]},null]},{\"$ne\":[{\"$setUnion\":[\"$a\",\"$b\"]},false]},{\"$ne\":[{\"$setUnion\":[\"$a\",\"$b\"]},\"\"]},{\"$ne\":[{\"$setUnion\":[\"$a\",\"$b\"]},0]}]}}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "expr\u0000$.a.union($.b)": {
      "entry": "expr",
      "src": "$.a.union($.b)",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":{\"$concatArrays\":[\"$a\",\"$b\"]},\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}",
      "now": "{\"$setUnion\":[\"$a\",\"$b\"]}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000$.a.uniq().nth(-1)": {
      "entry": "jsmql",
      "src": "$.a.uniq().nth(-1)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$arrayElemAt\":[{\"$reduce\":{\"input\":\"$a\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},-1]},null]},null]},{\"$n",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$arrayElemAt\":[{\"$setUnion\":\"$a\"},-1]},null]},null]},{\"$ne\":[{\"$arrayElemAt\":[{\"$setUnion\":\"$a\"},-1]},false]},{\"$ne\":[{\"$arrayElemAt\":[{\"$setUnion\":\"$a\"},-1]},\"\"]},{\"$ne\":[{\"$array",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "expr\u0000$.a.uniq().nth(-1)": {
      "entry": "expr",
      "src": "$.a.uniq().nth(-1)",
      "kind": "output",
      "was": "{\"$arrayElemAt\":[{\"$reduce\":{\"input\":\"$a\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},-1]}",
      "now": "{\"$arrayElemAt\":[{\"$setUnion\":\"$a\"},-1]}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000$.a.xor($.b)": {
      "entry": "jsmql",
      "src": "$.a.xor($.b)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlA\":\"$a\",\"jsmqlB\":\"$b\"},\"in\":{\"$reduce\":{\"input\":{\"$concatArrays\":[{\"$filter\":{\"input\":\"$$jsmqlA\",\"as\":\"jsmqlX\",\"cond\":{\"$not\":[{\"$in\":[\"$$jsmqlX\",\"$$jsmqlB\"]}]",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$setUnion\":[{\"$setDifference\":[\"$a\",\"$b\"]},{\"$setDifference\":[\"$b\",\"$a\"]}]},null]},null]},{\"$ne\":[{\"$setUnion\":[{\"$setDifference\":[\"$a\",\"$b\"]},{\"$setDifference\":[\"$b\",\"$a\"]}]},fals",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "expr\u0000$.a.xor($.b)": {
      "entry": "expr",
      "src": "$.a.xor($.b)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlA\":\"$a\",\"jsmqlB\":\"$b\"},\"in\":{\"$reduce\":{\"input\":{\"$concatArrays\":[{\"$filter\":{\"input\":\"$$jsmqlA\",\"as\":\"jsmqlX\",\"cond\":{\"$not\":[{\"$in\":[\"$$jsmqlX\",\"$$jsmqlB\"]}]}}},{\"$filter\":{\"input\":\"$$jsmqlB\",\"as",
      "now": "{\"$setUnion\":[{\"$setDifference\":[\"$a\",\"$b\"]},{\"$setDifference\":[\"$b\",\"$a\"]}]}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000$.a.sortedUniq()": {
      "entry": "jsmql",
      "src": "$.a.sortedUniq()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":\"$a\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},null]},null]},{\"$ne\":[{\"$reduce\":{\"input",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$setUnion\":\"$a\"},null]},null]},{\"$ne\":[{\"$setUnion\":\"$a\"},false]},{\"$ne\":[{\"$setUnion\":\"$a\"},\"\"]},{\"$ne\":[{\"$setUnion\":\"$a\"},0]}]}}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "expr\u0000$.a.sortedUniq()": {
      "entry": "expr",
      "src": "$.a.sortedUniq()",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":\"$a\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}",
      "now": "{\"$setUnion\":\"$a\"}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000$.a.uniq()": {
      "entry": "jsmql",
      "src": "$.a.uniq()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":\"$a\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},null]},null]},{\"$ne\":[{\"$reduce\":{\"input",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$setUnion\":\"$a\"},null]},null]},{\"$ne\":[{\"$setUnion\":\"$a\"},false]},{\"$ne\":[{\"$setUnion\":\"$a\"},\"\"]},{\"$ne\":[{\"$setUnion\":\"$a\"},0]}]}}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "expr\u0000$.a.uniq()": {
      "entry": "expr",
      "src": "$.a.uniq()",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":\"$a\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}",
      "now": "{\"$setUnion\":\"$a\"}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000$.r = $$$.orders.filter(o => o.userId === $._id).map(\"productIds\").flatten().uniq();": {
      "entry": "jsmql",
      "src": "$.r = $$$.orders.filter(o => o.userId === $._id).map(\"productIds\").flatten().uniq();",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":{\"$reduce\":{\"input\":{\"$reduce\":{\"input\":{\"$map\":{\"inpu",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":{\"$setUnion\":{\"$reduce\":{\"input\":{\"$map\":{\"input\":\"$__",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000$.arr.uniq()": {
      "entry": "jsmql",
      "src": "$.arr.uniq()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":\"$arr\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},null]},null]},{\"$ne\":[{\"$reduce\":{\"inp",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$setUnion\":\"$arr\"},null]},null]},{\"$ne\":[{\"$setUnion\":\"$arr\"},false]},{\"$ne\":[{\"$setUnion\":\"$arr\"},\"\"]},{\"$ne\":[{\"$setUnion\":\"$arr\"},0]}]}}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "expr\u0000$.arr.uniq()": {
      "entry": "expr",
      "src": "$.arr.uniq()",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":\"$arr\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}",
      "now": "{\"$setUnion\":\"$arr\"}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000$.s.uniq()": {
      "entry": "jsmql",
      "src": "$.s.uniq()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":\"$s\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},null]},null]},{\"$ne\":[{\"$reduce\":{\"input",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$setUnion\":\"$s\"},null]},null]},{\"$ne\":[{\"$setUnion\":\"$s\"},false]},{\"$ne\":[{\"$setUnion\":\"$s\"},\"\"]},{\"$ne\":[{\"$setUnion\":\"$s\"},0]}]}}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "expr\u0000$.s.uniq()": {
      "entry": "expr",
      "src": "$.s.uniq()",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":\"$s\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}",
      "now": "{\"$setUnion\":\"$s\"}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000$.n.uniq()": {
      "entry": "jsmql",
      "src": "$.n.uniq()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":\"$n\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},null]},null]},{\"$ne\":[{\"$reduce\":{\"input",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$setUnion\":\"$n\"},null]},null]},{\"$ne\":[{\"$setUnion\":\"$n\"},false]},{\"$ne\":[{\"$setUnion\":\"$n\"},\"\"]},{\"$ne\":[{\"$setUnion\":\"$n\"},0]}]}}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "expr\u0000$.n.uniq()": {
      "entry": "expr",
      "src": "$.n.uniq()",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":\"$n\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}",
      "now": "{\"$setUnion\":\"$n\"}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000$.obj.uniq()": {
      "entry": "jsmql",
      "src": "$.obj.uniq()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":\"$obj\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},null]},null]},{\"$ne\":[{\"$reduce\":{\"inp",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$setUnion\":\"$obj\"},null]},null]},{\"$ne\":[{\"$setUnion\":\"$obj\"},false]},{\"$ne\":[{\"$setUnion\":\"$obj\"},\"\"]},{\"$ne\":[{\"$setUnion\":\"$obj\"},0]}]}}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "expr\u0000$.obj.uniq()": {
      "entry": "expr",
      "src": "$.obj.uniq()",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":\"$obj\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}",
      "now": "{\"$setUnion\":\"$obj\"}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000[1,2,3].uniq()": {
      "entry": "jsmql",
      "src": "[1,2,3].uniq()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":[1,2,3],\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},null]},null]},{\"$ne\":[{\"$reduce\":{\"in",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$setUnion\":[[1,2,3]]},null]},null]},{\"$ne\":[{\"$setUnion\":[[1,2,3]]},false]},{\"$ne\":[{\"$setUnion\":[[1,2,3]]},\"\"]},{\"$ne\":[{\"$setUnion\":[[1,2,3]]},0]}]}}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "expr\u0000[1,2,3].uniq()": {
      "entry": "expr",
      "src": "[1,2,3].uniq()",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":[1,2,3],\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}",
      "now": "{\"$setUnion\":[[1,2,3]]}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "filter\u0000[1,2,3].uniq()": {
      "entry": "filter",
      "src": "[1,2,3].uniq()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":[1,2,3],\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}},null]},null]},{\"$ne\":[{\"$reduce\":{\"in",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[1,2,3],null]},null]},{\"$ne\":[[1,2,3],false]},{\"$ne\":[[1,2,3],\"\"]},{\"$ne\":[[1,2,3],0]}]}}",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set."
    },
    "jsmql\u0000$.s.trim(\"x\")": {
      "entry": "jsmql",
      "src": "$.s.trim(\"x\")",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$trim\":{\"input\":\"$s\"}},null]},null]},{\"$ne\":[{\"$trim\":{\"input\":\"$s\"}},false]},{\"$ne\":[{\"$trim\":{\"input\":\"$s\"}},\"\"]},{\"$ne\":[{\"$trim\":{\"input\":\"$s\"}},0]}]}}",
      "now": ".trim() takes no arguments, got 1",
      "reason": "Intended. `.trim`/`.toLowerCase` and friends moved to the declaration grid (src/methods/string.ts), and dispatch always applies a declaration's arity rule. Their old switch arms returned WITHOUT an arity check, so an argument was compiled and silently discarded — `.trim(\"x\")` emitted a plain $trim. JavaScript's trim takes no arguments, so rejecting is faithful to both languages; MQL's `chars` option stays reachable through the operator form $trim({input, chars})."
    },
    "expr\u0000$.s.trim(\"x\")": {
      "entry": "expr",
      "src": "$.s.trim(\"x\")",
      "kind": "now-rejected",
      "was": "{\"$trim\":{\"input\":\"$s\"}}",
      "now": ".trim() takes no arguments, got 1",
      "reason": "Intended. `.trim`/`.toLowerCase` and friends moved to the declaration grid (src/methods/string.ts), and dispatch always applies a declaration's arity rule. Their old switch arms returned WITHOUT an arity check, so an argument was compiled and silently discarded — `.trim(\"x\")` emitted a plain $trim. JavaScript's trim takes no arguments, so rejecting is faithful to both languages; MQL's `chars` option stays reachable through the operator form $trim({input, chars})."
    },
    "jsmql\u0000$.s.toLowerCase(1)": {
      "entry": "jsmql",
      "src": "$.s.toLowerCase(1)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$toLower\":\"$s\"},null]},null]},{\"$ne\":[{\"$toLower\":\"$s\"},false]},{\"$ne\":[{\"$toLower\":\"$s\"},\"\"]},{\"$ne\":[{\"$toLower\":\"$s\"},0]}]}}",
      "now": ".toLowerCase() takes no arguments, got 1",
      "reason": "Intended. `.trim`/`.toLowerCase` and friends moved to the declaration grid (src/methods/string.ts), and dispatch always applies a declaration's arity rule. Their old switch arms returned WITHOUT an arity check, so an argument was compiled and silently discarded — `.trim(\"x\")` emitted a plain $trim. JavaScript's trim takes no arguments, so rejecting is faithful to both languages; MQL's `chars` option stays reachable through the operator form $trim({input, chars})."
    },
    "expr\u0000$.s.toLowerCase(1)": {
      "entry": "expr",
      "src": "$.s.toLowerCase(1)",
      "kind": "now-rejected",
      "was": "{\"$toLower\":\"$s\"}",
      "now": ".toLowerCase() takes no arguments, got 1",
      "reason": "Intended. `.trim`/`.toLowerCase` and friends moved to the declaration grid (src/methods/string.ts), and dispatch always applies a declaration's arity rule. Their old switch arms returned WITHOUT an arity check, so an argument was compiled and silently discarded — `.trim(\"x\")` emitted a plain $trim. JavaScript's trim takes no arguments, so rejecting is faithful to both languages; MQL's `chars` option stays reachable through the operator form $trim({input, chars})."
    },
    "jsmql\u0000$.a.map(x => x, 1)": {
      "entry": "jsmql",
      "src": "$.a.map(x => x, 1)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$map\":{\"input\":\"$a\",\"as\":\"x\",\"in\":\"$$x\"}},null]},null]},{\"$ne\":[{\"$map\":{\"input\":\"$a\",\"as\":\"x\",\"in\":\"$$x\"}},false]},{\"$ne\":[{\"$map\":{\"input\":\"$a\",\"as\":\"x\",\"in\":\"$$x\"}},\"\"]},{\"$ne\":",
      "now": ".map(callback) takes one argument, got 2 — JavaScript's trailing 'thisArg' argument has no meaning in a jsmql expression, which has no 'this'.",
      "reason": "Intended. JavaScript's callback methods take a trailing `thisArg`, which has no meaning in an expression that has no `this` — jsmql used to accept and SILENTLY DISCARD it. `requireLambda` now rejects it, naming what the argument would have been. Same class as the `.trim(\"x\")` fix; found by migrating the callback family to the grid."
    },
    "expr\u0000$.a.map(x => x, 1)": {
      "entry": "expr",
      "src": "$.a.map(x => x, 1)",
      "kind": "now-rejected",
      "was": "{\"$map\":{\"input\":\"$a\",\"as\":\"x\",\"in\":\"$$x\"}}",
      "now": ".map(callback) takes one argument, got 2 — JavaScript's trailing 'thisArg' argument has no meaning in a jsmql expression, which has no 'this'.",
      "reason": "Intended. JavaScript's callback methods take a trailing `thisArg`, which has no meaning in an expression that has no `this` — jsmql used to accept and SILENTLY DISCARD it. `requireLambda` now rejects it, naming what the argument would have been. Same class as the `.trim(\"x\")` fix; found by migrating the callback family to the grid."
    },
    "jsmql\u0000$.a.filter(x => x, 1)": {
      "entry": "jsmql",
      "src": "$.a.filter(x => x, 1)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$filter\":{\"input\":\"$a\",\"as\":\"x\",\"cond\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}},null]},null]},{\"$ne\":[{\"$filt",
      "now": ".filter(callback) takes one argument, got 2 — JavaScript's trailing 'thisArg' argument has no meaning in a jsmql expression, which has no 'this'.",
      "reason": "Intended. JavaScript's callback methods take a trailing `thisArg`, which has no meaning in an expression that has no `this` — jsmql used to accept and SILENTLY DISCARD it. `requireLambda` now rejects it, naming what the argument would have been. Same class as the `.trim(\"x\")` fix; found by migrating the callback family to the grid."
    },
    "expr\u0000$.a.filter(x => x, 1)": {
      "entry": "expr",
      "src": "$.a.filter(x => x, 1)",
      "kind": "now-rejected",
      "was": "{\"$filter\":{\"input\":\"$a\",\"as\":\"x\",\"cond\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}",
      "now": ".filter(callback) takes one argument, got 2 — JavaScript's trailing 'thisArg' argument has no meaning in a jsmql expression, which has no 'this'.",
      "reason": "Intended. JavaScript's callback methods take a trailing `thisArg`, which has no meaning in an expression that has no `this` — jsmql used to accept and SILENTLY DISCARD it. `requireLambda` now rejects it, naming what the argument would have been. Same class as the `.trim(\"x\")` fix; found by migrating the callback family to the grid."
    },
    "jsmql\u0000$.a.findIndex(x => x, 1)": {
      "entry": "jsmql",
      "src": "$.a.findIndex(x => x, 1)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":{\"$zip\":{\"inputs\":[{\"$range\":[0,{\"$size\":\"$a\"}]},\"$a\"]}},\"initialValue\":-1,\"in\":{\"$let\":{\"vars\":{\"x\":{\"$arrayElemAt\":[\"$$this\",1]}},\"in\":{\"$cond\":{\"if\":{\"$and\":[{",
      "now": ".findIndex(callback) takes one argument, got 2 — JavaScript's trailing 'thisArg' argument has no meaning in a jsmql expression, which has no 'this'.",
      "reason": "Intended. JavaScript's callback methods take a trailing `thisArg`, which has no meaning in an expression that has no `this` — jsmql used to accept and SILENTLY DISCARD it. `requireLambda` now rejects it, naming what the argument would have been. Same class as the `.trim(\"x\")` fix; found by migrating the callback family to the grid."
    },
    "expr\u0000$.a.findIndex(x => x, 1)": {
      "entry": "expr",
      "src": "$.a.findIndex(x => x, 1)",
      "kind": "now-rejected",
      "was": "{\"$reduce\":{\"input\":{\"$zip\":{\"inputs\":[{\"$range\":[0,{\"$size\":\"$a\"}]},\"$a\"]}},\"initialValue\":-1,\"in\":{\"$let\":{\"vars\":{\"x\":{\"$arrayElemAt\":[\"$$this\",1]}},\"in\":{\"$cond\":{\"if\":{\"$and\":[{\"$eq\":[\"$$value\",-1]},{\"$and\":[{\"$ne\":",
      "now": ".findIndex(callback) takes one argument, got 2 — JavaScript's trailing 'thisArg' argument has no meaning in a jsmql expression, which has no 'this'.",
      "reason": "Intended. JavaScript's callback methods take a trailing `thisArg`, which has no meaning in an expression that has no `this` — jsmql used to accept and SILENTLY DISCARD it. `requireLambda` now rejects it, naming what the argument would have been. Same class as the `.trim(\"x\")` fix; found by migrating the callback family to the grid."
    },
    "jsmql\u0000const a = $.tags.uniq();\n       $set({ inc: a.includes(\"b\"), idx: a.indexOf(\"b\"), sl: a.slice(0, 2), cc: a.concat([\"z\"]),\n              sz: a.size(), len: a.length, str: a.toString() });": {
      "entry": "jsmql",
      "src": "const a = $.tags.uniq();\n       $set({ inc: a.includes(\"b\"), idx: a.indexOf(\"b\"), sl: a.slice(0, 2), cc: a.concat([\"z\"]),\n              sz: a.size(), len: a.length, str: a.toString() });",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.a\":{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}}},{\"$set\":{\"inc\":{\"$in\":[\"b\",\"$__jsmql.var.a\"]},\"",
      "now": "[{\"$set\":{\"__jsmql.var.a\":{\"$setUnion\":\"$tags\"}}},{\"$set\":{\"inc\":{\"$in\":[\"b\",\"$__jsmql.var.a\"]},\"idx\":{\"$indexOfArray\":[\"$__jsmql.var.a\",\"b\"]},\"sl\":{\"$slice\":[\"$__jsmql.var.a\",2]},\"cc\":{\"$concatArrays\":[\"$__jsmql.var.a\",",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "expr\u0000const a = $.tags.uniq();\n       $set({ inc: a.includes(\"b\"), idx: a.indexOf(\"b\"), sl: a.slice(0, 2), cc: a.concat([\"z\"]),\n              sz: a.size(), len: a.length, str: a.toString() });": {
      "entry": "expr",
      "src": "const a = $.tags.uniq();\n       $set({ inc: a.includes(\"b\"), idx: a.indexOf(\"b\"), sl: a.slice(0, 2), cc: a.concat([\"z\"]),\n              sz: a.size(), len: a.length, str: a.toString() });",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.a\":{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}}},{\"$set\":{\"inc\":{\"$in\":[\"b\",\"$__jsmql.var.a\"]},\"",
      "now": "[{\"$set\":{\"__jsmql.var.a\":{\"$setUnion\":\"$tags\"}}},{\"$set\":{\"inc\":{\"$in\":[\"b\",\"$__jsmql.var.a\"]},\"idx\":{\"$indexOfArray\":[\"$__jsmql.var.a\",\"b\"]},\"sl\":{\"$slice\":[\"$__jsmql.var.a\",2]},\"cc\":{\"$concatArrays\":[\"$__jsmql.var.a\",",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "update\u0000const a = $.tags.uniq();\n       $set({ inc: a.includes(\"b\"), idx: a.indexOf(\"b\"), sl: a.slice(0, 2), cc: a.concat([\"z\"]),\n              sz: a.size(), len: a.length, str: a.toString() });": {
      "entry": "update",
      "src": "const a = $.tags.uniq();\n       $set({ inc: a.includes(\"b\"), idx: a.indexOf(\"b\"), sl: a.slice(0, 2), cc: a.concat([\"z\"]),\n              sz: a.size(), len: a.length, str: a.toString() });",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.a\":{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}}},{\"$set\":{\"inc\":{\"$in\":[\"b\",\"$__jsmql.var.a\"]},\"",
      "now": "[{\"$set\":{\"__jsmql.var.a\":{\"$setUnion\":\"$tags\"}}},{\"$set\":{\"inc\":{\"$in\":[\"b\",\"$__jsmql.var.a\"]},\"idx\":{\"$indexOfArray\":[\"$__jsmql.var.a\",\"b\"]},\"sl\":{\"$slice\":[\"$__jsmql.var.a\",2]},\"cc\":{\"$concatArrays\":[\"$__jsmql.var.a\",",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "jsmql\u0000let a = $.tags.uniq(); $set({ inc: a.includes(\"b\") });": {
      "entry": "jsmql",
      "src": "let a = $.tags.uniq(); $set({ inc: a.includes(\"b\") });",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.a\":{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}}},{\"$set\":{\"inc\":{\"$cond\":{\"if\":{\"$isArray\":\"$__j",
      "now": "[{\"$set\":{\"__jsmql.var.a\":{\"$setUnion\":\"$tags\"}}},{\"$set\":{\"inc\":{\"$cond\":{\"if\":{\"$isArray\":\"$__jsmql.var.a\"},\"then\":{\"$in\":[\"b\",\"$__jsmql.var.a\"]},\"else\":{\"$gte\":[{\"$indexOfCP\":[\"$__jsmql.var.a\",\"b\"]},0]}}}}},{\"$unset\":",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "expr\u0000let a = $.tags.uniq(); $set({ inc: a.includes(\"b\") });": {
      "entry": "expr",
      "src": "let a = $.tags.uniq(); $set({ inc: a.includes(\"b\") });",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.a\":{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}}},{\"$set\":{\"inc\":{\"$cond\":{\"if\":{\"$isArray\":\"$__j",
      "now": "[{\"$set\":{\"__jsmql.var.a\":{\"$setUnion\":\"$tags\"}}},{\"$set\":{\"inc\":{\"$cond\":{\"if\":{\"$isArray\":\"$__jsmql.var.a\"},\"then\":{\"$in\":[\"b\",\"$__jsmql.var.a\"]},\"else\":{\"$gte\":[{\"$indexOfCP\":[\"$__jsmql.var.a\",\"b\"]},0]}}}}},{\"$unset\":",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "update\u0000let a = $.tags.uniq(); $set({ inc: a.includes(\"b\") });": {
      "entry": "update",
      "src": "let a = $.tags.uniq(); $set({ inc: a.includes(\"b\") });",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.a\":{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}}},{\"$set\":{\"inc\":{\"$cond\":{\"if\":{\"$isArray\":\"$__j",
      "now": "[{\"$set\":{\"__jsmql.var.a\":{\"$setUnion\":\"$tags\"}}},{\"$set\":{\"inc\":{\"$cond\":{\"if\":{\"$isArray\":\"$__jsmql.var.a\"},\"then\":{\"$in\":[\"b\",\"$__jsmql.var.a\"]},\"else\":{\"$gte\":[{\"$indexOfCP\":[\"$__jsmql.var.a\",\"b\"]},0]}}}}},{\"$unset\":",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "jsmql\u0000const a = $.tags.uniq(); const b = a.slice(1); $set({ inc: b.includes(\"b\") });": {
      "entry": "jsmql",
      "src": "const a = $.tags.uniq(); const b = a.slice(1); $set({ inc: b.includes(\"b\") });",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.a\":{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}}},{\"$set\":{\"__jsmql.var.b\":{\"$let\":{\"vars\":{\"jsmq",
      "now": "[{\"$set\":{\"__jsmql.var.a\":{\"$setUnion\":\"$tags\"}}},{\"$set\":{\"__jsmql.var.b\":{\"$let\":{\"vars\":{\"jsmqlArr\":\"$__jsmql.var.a\"},\"in\":{\"$slice\":[\"$$jsmqlArr\",1,{\"$max\":[1,{\"$size\":\"$$jsmqlArr\"}]}]}}}}},{\"$set\":{\"inc\":{\"$in\":[\"b\"",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "expr\u0000const a = $.tags.uniq(); const b = a.slice(1); $set({ inc: b.includes(\"b\") });": {
      "entry": "expr",
      "src": "const a = $.tags.uniq(); const b = a.slice(1); $set({ inc: b.includes(\"b\") });",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.a\":{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}}},{\"$set\":{\"__jsmql.var.b\":{\"$let\":{\"vars\":{\"jsmq",
      "now": "[{\"$set\":{\"__jsmql.var.a\":{\"$setUnion\":\"$tags\"}}},{\"$set\":{\"__jsmql.var.b\":{\"$let\":{\"vars\":{\"jsmqlArr\":\"$__jsmql.var.a\"},\"in\":{\"$slice\":[\"$$jsmqlArr\",1,{\"$max\":[1,{\"$size\":\"$$jsmqlArr\"}]}]}}}}},{\"$set\":{\"inc\":{\"$in\":[\"b\"",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "update\u0000const a = $.tags.uniq(); const b = a.slice(1); $set({ inc: b.includes(\"b\") });": {
      "entry": "update",
      "src": "const a = $.tags.uniq(); const b = a.slice(1); $set({ inc: b.includes(\"b\") });",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.a\":{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}}},{\"$set\":{\"__jsmql.var.b\":{\"$let\":{\"vars\":{\"jsmq",
      "now": "[{\"$set\":{\"__jsmql.var.a\":{\"$setUnion\":\"$tags\"}}},{\"$set\":{\"__jsmql.var.b\":{\"$let\":{\"vars\":{\"jsmqlArr\":\"$__jsmql.var.a\"},\"in\":{\"$slice\":[\"$$jsmqlArr\",1,{\"$max\":[1,{\"$size\":\"$$jsmqlArr\"}]}]}}}}},{\"$set\":{\"inc\":{\"$in\":[\"b\"",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "jsmql\u0000const ids = $.tags.uniq(); const r = $$$.orders.filter(o => o.pid.some(p => ids.includes(p)));": {
      "entry": "jsmql",
      "src": "const ids = $.tags.uniq(); const r = $$$.orders.filter(o => o.pid.some(p => ids.includes(p)));",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.ids\":{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_v0_",
      "now": "[{\"$set\":{\"__jsmql.var.ids\":{\"$setUnion\":\"$tags\"}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_v0_ids\":\"$__jsmql.var.ids\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$pid\",\"as\":\"p\",\"in\":{\"$in\":[\"$",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "jsmql\u0000const ids = $.tags.uniq();\n       const r = $$$.orders.aggregate(o => { const inner = $$$.items.filter(i => ids.includes(i.pid)); });": {
      "entry": "jsmql",
      "src": "const ids = $.tags.uniq();\n       const r = $$$.orders.aggregate(o => { const inner = $$$.items.filter(i => ids.includes(i.pid)); });",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.ids\":{\"$reduce\":{\"input\":\"$tags\",\"initialValue\":[],\"in\":{\"$cond\":[{\"$in\":[\"$$this\",\"$$value\"]},\"$$value\",{\"$concatArrays\":[\"$$value\",[\"$$this\"]]}]}}}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_v0_",
      "now": "[{\"$set\":{\"__jsmql.var.ids\":{\"$setUnion\":\"$tags\"}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_v0_ids\":\"$__jsmql.var.ids\"},\"pipeline\":[{\"$lookup\":{\"from\":\"items\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$in\":[\"$pid\",\"$$jsmql_v0_i",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "expr\u0000$$ = $$.filter({ a: 1 })": {
      "entry": "expr",
      "src": "$$ = $$.filter({ a: 1 })",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"a\":1}}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a stream-replace `$$ = <expr>` (which compiles to pipeline stages). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "expr\u0000$$ = $$.map(t => ({ x: t.x }))": {
      "entry": "expr",
      "src": "$$ = $$.map(t => ({ x: t.x }))",
      "kind": "now-rejected",
      "was": "[{\"$replaceWith\":{\"x\":\"$x\"}}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a stream-replace `$$ = <expr>` (which compiles to pipeline stages). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "expr\u0000$$ = $$.take(3)": {
      "entry": "expr",
      "src": "$$ = $$.take(3)",
      "kind": "now-rejected",
      "was": "[{\"$limit\":3}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a stream-replace `$$ = <expr>` (which compiles to pipeline stages). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "expr\u0000$$ = []": {
      "entry": "expr",
      "src": "$$ = []",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"$expr\":false}}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a stream-replace `$$ = <expr>` (which compiles to pipeline stages). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "expr\u0000$.a = 1, $$ = $$.filter({ b: 2 })": {
      "entry": "expr",
      "src": "$.a = 1, $$ = $$.filter({ b: 2 })",
      "kind": "now-rejected",
      "was": "[{\"$set\":{\"a\":1}},{\"$match\":{\"b\":2}}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a stream-replace `$$ = <expr>` (which compiles to pipeline stages). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "expr\u0000$$ = $$.filter({ b: 2 }), $.a = 1": {
      "entry": "expr",
      "src": "$$ = $$.filter({ b: 2 }), $.a = 1",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"b\":2}},{\"$set\":{\"a\":1}}]",
      "now": "jsmql.expr() returns one aggregation expression, but received a stream-replace `$$ = <expr>` (which compiles to pipeline stages). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "expr\u0000$$ = 5": {
      "entry": "expr",
      "src": "$$ = 5",
      "kind": "message",
      "was": "'$$ = …' RHS must be '$$.<streamMethod>…' (narrow/transform the current stream) or '$$$.<coll>.<streamMethod>…' (switch source to another collection). Any lodash stream method may head the chain (e.g. '.filter', '.toSort",
      "now": "jsmql.expr() returns one aggregation expression, but received a stream-replace `$$ = <expr>` (which compiles to pipeline stages). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "reason": "Intended, commit 42d3d7a. jsmql.expr() returns ONE aggregation expression. Root- and stream-replace lower to STAGES, so the expression-only entry point rejects them instead of handing back a stage array. jsmql() and jsmql.pipeline() lower them unchanged. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "jsmql\u0000$.event.ts.getUTCHours() >= 9 && $.event.ts.getUTCHours() < 17": {
      "entry": "jsmql",
      "src": "$.event.ts.getUTCHours() >= 9 && $.event.ts.getUTCHours() < 17",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$gte\":[{\"$hour\":{\"date\":\"$event.ts\",\"timezone\":\"UTC\"}},9]},{\"$lt\":[{\"$hour\":{\"date\":\"$event.ts\",\"timezone\":\"UTC\"}},17]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$gte\":[{\"$hour\":\"$event.ts\"},9]},{\"$lt\":[{\"$hour\":\"$event.ts\"},17]}]}}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "expr\u0000$.event.ts.getUTCHours() >= 9 && $.event.ts.getUTCHours() < 17": {
      "entry": "expr",
      "src": "$.event.ts.getUTCHours() >= 9 && $.event.ts.getUTCHours() < 17",
      "kind": "output",
      "was": "{\"$and\":[{\"$gte\":[{\"$hour\":{\"date\":\"$event.ts\",\"timezone\":\"UTC\"}},9]},{\"$lt\":[{\"$hour\":{\"date\":\"$event.ts\",\"timezone\":\"UTC\"}},17]}]}",
      "now": "{\"$and\":[{\"$gte\":[{\"$hour\":\"$event.ts\"},9]},{\"$lt\":[{\"$hour\":\"$event.ts\"},17]}]}",
      "reason": "Intended. Two restatements of a MongoDB default removed: `.getUTCxxx()` names UTC through its own method name and UTC is the operator default, so the single-argument form says the same thing; and `%Y-%m-%dT%H:%M:%S.%LZ` IS $dateToString's default format. Verified identical on a live mongod across ordinary dates, the epoch, and a missing field. A timezone the developer TYPED is still kept in the object form. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "jsmql\u0000\nconst userId = 0x507f1f77bcf86cd799439011;\n$$.filter({ _id: userId }); // limit the whole pipeline down to one pass\nassert($$.length === 1, \"More than one user with such ID found\");\n\nconst myProductIds = $$$.orders\n  .filter({ userId })\n  .$sort({ createdAt: -1 }) // same as _.sort()\n  .take(10)\n  .map(\"productIds\")\n  .flatten()\n  .uniq();\n\nconst candidateProductIdCounts = $$$.orders\n  .filter(o => o.productIds.some(p => myProductIds.includes(p)))\n  .toSorted({ createdAt: -1 })\n  .take(100) // a pipeline of co-purchase orders, most recent 100\n  .map(\"productIds\")\n  .flatten()\n  .filter(p => !myProductIds.includes(p))\n  .countBy(); // { ID: count } map\nconst candidateProductIds = Object.keys(candidateProductIdCounts).map(ObjectId);\n\nconst candidateProducts = $$$.products\n  .filter(pr => pr._id in candidateProductIds) //  { $in: [] } operator\n  .$limit(500); // same as .take(500)\n\n$ = candidateProductIds\n  .map(id => ({\n    productId: id,\n    score: candidateProductIdCounts[id],\n    name:  candidateProducts.find({ _id: id }).name,\n  }))\n  .orderBy({ score: -1 })\n  .take(10);\n      ": {
      "entry": "jsmql",
      "src": "\nconst userId = 0x507f1f77bcf86cd799439011;\n$$.filter({ _id: userId }); // limit the whole pipeline down to one pass\nassert($$.length === 1, \"More than one user with such ID found\");\n\nconst myProductIds = $$$.orders\n  .filter({ userId })\n  .$sort({ createdAt: -1 }) // same as _.sort()\n  .take(10)\n  .map(\"productIds\")\n  .flatten()\n  .uniq();\n\nconst candidateProductIdCounts = $$$.orders\n  .filter(o => o.productIds.some(p => myProductIds.includes(p)))\n  .toSorted({ createdAt: -1 })\n  .take(100) // a pipeline of co-purchase orders, most recent 100\n  .map(\"productIds\")\n  .flatten()\n  .filter(p => !myProductIds.includes(p))\n  .countBy(); // { ID: count } map\nconst candidateProductIds = Object.keys(candidateProductIdCounts).map(ObjectId);\n\nconst candidateProducts = $$$.products\n  .filter(pr => pr._id in candidateProductIds) //  { $in: [] } operator\n  .$limit(500); // same as .take(500)\n\n$ = candidateProductIds\n  .map(id => ({\n    productId: id,\n    score: candidateProductIdCounts[id],\n    name:  candidateProducts.find({ _id: id }).name,\n  }))\n  .orderBy({ score: -1 })\n  .take(10);\n      ",
      "kind": "output",
      "was": "[{\"$match\":{\"_id\":\"«oid:507f1f77bcf86cd799439011»\"}},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$match\":{\"$expr\":{\"$convert\":{\"input\":true,\"to\":{\"$cond\":[{\"$eq\":[\"$__jsmql.length\",1]},\"bool\",\"jsmq",
      "now": "[{\"$match\":{\"_id\":\"«oid:507f1f77bcf86cd799439011»\"}},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$match\":{\"$expr\":{\"$convert\":{\"input\":true,\"to\":{\"$cond\":[{\"$eq\":[\"$__jsmql.length\",1]},\"bool\",\"jsmq",
      "reason": "Intended, order-only. .uniq / .union / .xor now lower to the set operators MongoDB ships instead of hand-built order-preserving $reduce chains. Same set, verified on a live mongod across duplicate, empty, single and document-valued inputs. MongoDB does not define set-operator order and nobody writes an ordering when they write .uniq() -- SR2. $.tags.uniq() 144 chars -> 21; $.a.xor($.b) 403 -> 77. .difference deliberately KEEPS its $filter: lodash keeps the receiver's duplicates there, so $setDifference would change the set. (Same change, newly VISIBLE: the harness harvester now reads backtick call arguments and template-tag bodies, so sources that always diverged are finally in the corpus.)"
    },
    "validate\u0000$$.nosuchmethod({a:1})": {
      "entry": "validate",
      "src": "$$.nosuchmethod({a:1})",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeW",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeW",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$$.pop({a:1})": {
      "entry": "validate",
      "src": "$$.pop({a:1})",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"'.pop(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, ",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'.pop(...)' isn't available on '$$' — it removes the last element by mutating. Take a prefix instead: '.slice(0, -1)'.\",\"pos\":3,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended. An array-receiver method with no stream form now carries a WRITTEN reason (STREAM_UNSUPPORTED) instead of falling to the generic 'not a chainable stream method' list. The list says what else exists; it never said why this one is absent. Every reason names what to write instead. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000let k = $.x; $$ = $$$.orders.map(o => ({ t: o.items.map(v => v + k) }));": {
      "entry": "validate",
      "src": "let k = $.x; $$ = $$$.orders.map(o => ({ t: o.items.map(v => v + k) }));",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"Unknown identifier 'k'. Did you mean '$.k'?\",\"pos\":65,\"code\":\"CODEGEN_ERROR\"}]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"`k` is a `let`/`const` declared before `$$ = $$$.orders`, which replaces the stream with a different collection (a `$unionWith`, which can't correlate) — so `k` (along with the outer ",
      "reason": "Intended. extendCtx enumerated 19 of GenerateCtx's 23 fields and dropped sourceSwitch, so a `let` tombstone did not survive a second lambda level and the actionable source-switch message degraded to 'Unknown identifier'. A lambda body is inside everything its surroundings are inside, so the context now spreads. The reference emits the WORSE message; this row records the improvement. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$.s.trim(\"x\")": {
      "entry": "validate",
      "src": "$.s.trim(\"x\")",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\".trim() takes no arguments, got 1\",\"pos\":4,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended. `.trim`/`.toLowerCase` and friends moved to the declaration grid (src/methods/string.ts), and dispatch always applies a declaration's arity rule. Their old switch arms returned WITHOUT an arity check, so an argument was compiled and silently discarded — `.trim(\"x\")` emitted a plain $trim. JavaScript's trim takes no arguments, so rejecting is faithful to both languages; MQL's `chars` option stays reachable through the operator form $trim({input, chars}). (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$.s.toLowerCase(1)": {
      "entry": "validate",
      "src": "$.s.toLowerCase(1)",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\".toLowerCase() takes no arguments, got 1\",\"pos\":4,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended. `.trim`/`.toLowerCase` and friends moved to the declaration grid (src/methods/string.ts), and dispatch always applies a declaration's arity rule. Their old switch arms returned WITHOUT an arity check, so an argument was compiled and silently discarded — `.trim(\"x\")` emitted a plain $trim. JavaScript's trim takes no arguments, so rejecting is faithful to both languages; MQL's `chars` option stays reachable through the operator form $trim({input, chars}). (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$$ = $$.slise(0, 5);": {
      "entry": "validate",
      "src": "$$ = $$.slise(0, 5);",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .dro",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .dro",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$$ = $$.filter(o => o.active).bogus();": {
      "entry": "validate",
      "src": "$$ = $$.filter(o => o.active).bogus();",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"'.bogus(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'.bogus(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeWhile, .",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$$.slise(0, 5);": {
      "entry": "validate",
      "src": "$$.slise(0, 5);",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .dro",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'.slise(...)' is not a chainable stream method on '$$'. Did you mean '.slice'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .dro",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$$.nosuchmethod(\"x\")": {
      "entry": "validate",
      "src": "$$.nosuchmethod(\"x\")",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeW",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'.nosuchmethod(...)' is not a chainable stream method on '$$'. Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, .tail, .takeW",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$$.pop(\"x\")": {
      "entry": "validate",
      "src": "$$.pop(\"x\")",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"'.pop(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop, ",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'.pop(...)' isn't available on '$$' — it removes the last element by mutating. Take a prefix instead: '.slice(0, -1)'.\",\"pos\":3,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended. An array-receiver method with no stream form now carries a WRITTEN reason (STREAM_UNSUPPORTED) instead of falling to the generic 'not a chainable stream method' list. The list says what else exists; it never said why this one is absent. Every reason names what to write instead. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$$ = $$.dropp();": {
      "entry": "validate",
      "src": "$$ = $$.dropp();",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"'.dropp(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'.dropp(...)' is not a chainable stream method on '$$'. Did you mean '.drop'? Chainable methods — any may head OR extend the chain: '.filter', '.reject', .slice, .sample, .take, .drop",
      "reason": "Intended, knock-on. The generic 'not a chainable stream method' message lists what IS chainable, and .uniq / .sortedUniq / .sortedUniqBy joined that list when they gained stream lowerings. The rejection itself is unchanged; only the enumeration grew. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$.a.take(1.5)": {
      "entry": "validate",
      "src": "$.a.take(1.5)",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\".take(n) needs a whole number, but got 1.5.\",\"pos\":9,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$.a.drop(1.5)": {
      "entry": "validate",
      "src": "$.a.drop(1.5)",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\".drop(n) needs a whole number, but got 1.5.\",\"pos\":9,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$.a.slice(0.5)": {
      "entry": "validate",
      "src": "$.a.slice(0.5)",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\".slice(start[, end]) needs a whole number, but got 0.5.\",\"pos\":10,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$.a.slice(0, 2.5)": {
      "entry": "validate",
      "src": "$.a.slice(0, 2.5)",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\".slice(start[, end]) needs a whole number, but got 2.5.\",\"pos\":13,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$.a.sampleSize(1.5)": {
      "entry": "validate",
      "src": "$.a.sampleSize(1.5)",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\".sampleSize(n) needs a whole number, but got 1.5.\",\"pos\":15,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$.a.takeRight(2.5)": {
      "entry": "validate",
      "src": "$.a.takeRight(2.5)",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\".takeRight(n) needs a whole number, but got 2.5.\",\"pos\":14,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended, commit 1ad2b5b. $slice needs a 32-bit integer in every count/position slot, so a fractional literal aborted the query on the server. Rejected at compile time now, matching the stream form which already did. Literal-gated: an expression still compiles. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$year({ date: $.d, timezone: \"UTC\" })": {
      "entry": "validate",
      "src": "$year({ date: $.d, timezone: \"UTC\" })",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"'$year' expects a date, but got an object. Use a field path or new Date(…).\",\"pos\":6,\"code\":\"CODEGEN_ERROR\"}]}",
      "now": "{\"valid\":true,\"errors\":[]}",
      "reason": "Intended, commit 4ce80f3. All 13 date accessors take a { date, timezone } document and the server accepts it -- verified on a live mongod. The old rejection made the documented spelling unreachable, and broke the literal-gating invariant by refusing something that is not certainly wrong. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$hour({ date: $.d })": {
      "entry": "validate",
      "src": "$hour({ date: $.d })",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"'$hour' expects a date, but got an object. Use a field path or new Date(…).\",\"pos\":6,\"code\":\"CODEGEN_ERROR\"}]}",
      "now": "{\"valid\":true,\"errors\":[]}",
      "reason": "Intended, commit 4ce80f3. All 13 date accessors take a { date, timezone } document and the server accepts it -- verified on a live mongod. The old rejection made the documented spelling unreachable, and broke the literal-gating invariant by refusing something that is not certainly wrong. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$lookup({ from: \"o\", pipeline: [$ = { t: $.total }], as: \"o\" });": {
      "entry": "validate",
      "src": "$lookup({ from: \"o\", pipeline: [$ = { t: $.total }], as: \"o\" });",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'$ = …' isn't available inside a literal sub-pipeline array. Write the stage directly instead — '$replaceWith({ … })'.\",\"pos\":32,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended, commit 90bc46e. This loop has no slot allocator, so buffering the sugar emitted `$set: {\"\": ...}` -- mongod refuses it with 'FieldPath cannot be constructed with empty string'. Rejected now, naming the stage-call spelling that works there. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$unionWith({ coll: \"o\", pipeline: [$ = { a: 1 }] });": {
      "entry": "validate",
      "src": "$unionWith({ coll: \"o\", pipeline: [$ = { a: 1 }] });",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'$ = …' isn't available inside a literal sub-pipeline array. Write the stage directly instead — '$replaceWith({ … })'.\",\"pos\":35,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended, commit 90bc46e. This loop has no slot allocator, so buffering the sugar emitted `$set: {\"\": ...}` -- mongod refuses it with 'FieldPath cannot be constructed with empty string'. Rejected now, naming the stage-call spelling that works there. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$.a.map(x => x, 1)": {
      "entry": "validate",
      "src": "$.a.map(x => x, 1)",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\".map(callback) takes one argument, got 2 — JavaScript's trailing 'thisArg' argument has no meaning in a jsmql expression, which has no 'this'.\",\"pos\":16,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended. JavaScript's callback methods take a trailing `thisArg`, which has no meaning in an expression that has no `this` — jsmql used to accept and SILENTLY DISCARD it. `requireLambda` now rejects it, naming what the argument would have been. Same class as the `.trim(\"x\")` fix; found by migrating the callback family to the grid. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$.a.filter(x => x, 1)": {
      "entry": "validate",
      "src": "$.a.filter(x => x, 1)",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\".filter(callback) takes one argument, got 2 — JavaScript's trailing 'thisArg' argument has no meaning in a jsmql expression, which has no 'this'.\",\"pos\":19,\"code\":\"CODEGEN_ERROR\"}]}",
      "reason": "Intended. JavaScript's callback methods take a trailing `thisArg`, which has no meaning in an expression that has no `this` — jsmql used to accept and SILENTLY DISCARD it. `requireLambda` now rejects it, naming what the argument would have been. Same class as the `.trim(\"x\")` fix; found by migrating the callback family to the grid. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "validate\u0000$.a.findIndex(x => x, 1)": {
      "entry": "validate",
      "src": "$.a.findIndex(x => x, 1)",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\".findIndex(callback) takes one argument, got 2 — JavaScript's trailing 'thisArg' argument has no meaning in a jsmql expression, which has no 'this'.\",\"pos\":22,\"code\":\"CODEGEN_ERROR\"}]",
      "reason": "Intended. JavaScript's callback methods take a trailing `thisArg`, which has no meaning in an expression that has no `this` — jsmql used to accept and SILENTLY DISCARD it. `requireLambda` now rejects it, naming what the argument would have been. Same class as the `.trim(\"x\")` fix; found by migrating the callback family to the grid. (The `validate` view of the same change: it reports the message AND its `.pos` rather than throwing, which is why this entry diverges where the others already did.)"
    },
    "expr\u0000typeof $.a === \"boolean\"": {
      "entry": "expr",
      "src": "typeof $.a === \"boolean\"",
      "kind": "output",
      "was": "{\"$eq\":[{\"$type\":\"$a\"},\"boolean\"]}",
      "now": "{\"$eq\":[{\"$type\":\"$a\"},\"bool\"]}",
      "reason": "Intended, and a BUG FIX. MongoDB's `$type` expression returns \"bool\" and \"int\"/\"double\"/\"long\"/\"decimal\" — never JavaScript's \"boolean\" or the query-only umbrella \"number\". The expression cell compared `$type` against JavaScript's own spelling, so `typeof $.a === \"boolean\"` was FALSE for every document, including one where the field really was a boolean; the identical source in Filter position was correct, because the query side had an alias table and the expression side had none. Both cells now read the one vocabulary in src/predicate-ir.ts: \"boolean\" maps to \"bool\", and the group alias \"number\" becomes an $in over the concrete types $type can return. Verified on a live mongod — query and expr now select the same documents. Only \"boolean\" and \"number\" changed; \"string\"/\"object\" and unrecognised aliases such as \"function\" emit exactly what they did before."
    },
    "expr\u0000typeof $.a === \"number\"": {
      "entry": "expr",
      "src": "typeof $.a === \"number\"",
      "kind": "output",
      "was": "{\"$eq\":[{\"$type\":\"$a\"},\"number\"]}",
      "now": "{\"$in\":[{\"$type\":\"$a\"},[\"double\",\"int\",\"long\",\"decimal\"]]}",
      "reason": "Intended, and a BUG FIX. MongoDB's `$type` expression returns \"bool\" and \"int\"/\"double\"/\"long\"/\"decimal\" — never JavaScript's \"boolean\" or the query-only umbrella \"number\". The expression cell compared `$type` against JavaScript's own spelling, so `typeof $.a === \"boolean\"` was FALSE for every document, including one where the field really was a boolean; the identical source in Filter position was correct, because the query side had an alias table and the expression side had none. Both cells now read the one vocabulary in src/predicate-ir.ts: \"boolean\" maps to \"bool\", and the group alias \"number\" becomes an $in over the concrete types $type can return. Verified on a live mongod — query and expr now select the same documents. Only \"boolean\" and \"number\" changed; \"string\"/\"object\" and unrecognised aliases such as \"function\" emit exactly what they did before."
    },
    "expr\u0000typeof $.a !== \"boolean\"": {
      "entry": "expr",
      "src": "typeof $.a !== \"boolean\"",
      "kind": "output",
      "was": "{\"$ne\":[{\"$type\":\"$a\"},\"boolean\"]}",
      "now": "{\"$ne\":[{\"$type\":\"$a\"},\"bool\"]}",
      "reason": "Intended, and a BUG FIX. MongoDB's `$type` expression returns \"bool\" and \"int\"/\"double\"/\"long\"/\"decimal\" — never JavaScript's \"boolean\" or the query-only umbrella \"number\". The expression cell compared `$type` against JavaScript's own spelling, so `typeof $.a === \"boolean\"` was FALSE for every document, including one where the field really was a boolean; the identical source in Filter position was correct, because the query side had an alias table and the expression side had none. Both cells now read the one vocabulary in src/predicate-ir.ts: \"boolean\" maps to \"bool\", and the group alias \"number\" becomes an $in over the concrete types $type can return. Verified on a live mongod — query and expr now select the same documents. Only \"boolean\" and \"number\" changed; \"string\"/\"object\" and unrecognised aliases such as \"function\" emit exactly what they did before."
    },
    "expr\u0000typeof $.a !== \"number\"": {
      "entry": "expr",
      "src": "typeof $.a !== \"number\"",
      "kind": "output",
      "was": "{\"$ne\":[{\"$type\":\"$a\"},\"number\"]}",
      "now": "{\"$not\":[{\"$in\":[{\"$type\":\"$a\"},[\"double\",\"int\",\"long\",\"decimal\"]]}]}",
      "reason": "Intended, and a BUG FIX. MongoDB's `$type` expression returns \"bool\" and \"int\"/\"double\"/\"long\"/\"decimal\" — never JavaScript's \"boolean\" or the query-only umbrella \"number\". The expression cell compared `$type` against JavaScript's own spelling, so `typeof $.a === \"boolean\"` was FALSE for every document, including one where the field really was a boolean; the identical source in Filter position was correct, because the query side had an alias table and the expression side had none. Both cells now read the one vocabulary in src/predicate-ir.ts: \"boolean\" maps to \"bool\", and the group alias \"number\" becomes an $in over the concrete types $type can return. Verified on a live mongod — query and expr now select the same documents. Only \"boolean\" and \"number\" changed; \"string\"/\"object\" and unrecognised aliases such as \"function\" emit exactly what they did before."
    },
    "expr\u0000typeof $.a.b.c === \"number\"": {
      "entry": "expr",
      "src": "typeof $.a.b.c === \"number\"",
      "kind": "output",
      "was": "{\"$eq\":[{\"$type\":\"$a.b.c\"},\"number\"]}",
      "now": "{\"$in\":[{\"$type\":\"$a.b.c\"},[\"double\",\"int\",\"long\",\"decimal\"]]}",
      "reason": "Intended, and a BUG FIX. MongoDB's `$type` expression returns \"bool\" and \"int\"/\"double\"/\"long\"/\"decimal\" — never JavaScript's \"boolean\" or the query-only umbrella \"number\". The expression cell compared `$type` against JavaScript's own spelling, so `typeof $.a === \"boolean\"` was FALSE for every document, including one where the field really was a boolean; the identical source in Filter position was correct, because the query side had an alias table and the expression side had none. Both cells now read the one vocabulary in src/predicate-ir.ts: \"boolean\" maps to \"bool\", and the group alias \"number\" becomes an $in over the concrete types $type can return. Verified on a live mongod — query and expr now select the same documents. Only \"boolean\" and \"number\" changed; \"string\"/\"object\" and unrecognised aliases such as \"function\" emit exactly what they did before."
    },
    "jsmql\u0000$set({ t: typeof $.a === \"boolean\" });": {
      "entry": "jsmql",
      "src": "$set({ t: typeof $.a === \"boolean\" });",
      "kind": "output",
      "was": "[{\"$set\":{\"t\":{\"$eq\":[{\"$type\":\"$a\"},\"boolean\"]}}}]",
      "now": "[{\"$set\":{\"t\":{\"$eq\":[{\"$type\":\"$a\"},\"bool\"]}}}]",
      "reason": "Intended, and a BUG FIX. MongoDB's `$type` expression returns \"bool\" and \"int\"/\"double\"/\"long\"/\"decimal\" — never JavaScript's \"boolean\" or the query-only umbrella \"number\". The expression cell compared `$type` against JavaScript's own spelling, so `typeof $.a === \"boolean\"` was FALSE for every document, including one where the field really was a boolean; the identical source in Filter position was correct, because the query side had an alias table and the expression side had none. Both cells now read the one vocabulary in src/predicate-ir.ts: \"boolean\" maps to \"bool\", and the group alias \"number\" becomes an $in over the concrete types $type can return. Verified on a live mongod — query and expr now select the same documents. Only \"boolean\" and \"number\" changed; \"string\"/\"object\" and unrecognised aliases such as \"function\" emit exactly what they did before."
    },
    "expr\u0000$set({ t: typeof $.a === \"boolean\" });": {
      "entry": "expr",
      "src": "$set({ t: typeof $.a === \"boolean\" });",
      "kind": "output",
      "was": "[{\"$set\":{\"t\":{\"$eq\":[{\"$type\":\"$a\"},\"boolean\"]}}}]",
      "now": "[{\"$set\":{\"t\":{\"$eq\":[{\"$type\":\"$a\"},\"bool\"]}}}]",
      "reason": "Intended, and a BUG FIX. MongoDB's `$type` expression returns \"bool\" and \"int\"/\"double\"/\"long\"/\"decimal\" — never JavaScript's \"boolean\" or the query-only umbrella \"number\". The expression cell compared `$type` against JavaScript's own spelling, so `typeof $.a === \"boolean\"` was FALSE for every document, including one where the field really was a boolean; the identical source in Filter position was correct, because the query side had an alias table and the expression side had none. Both cells now read the one vocabulary in src/predicate-ir.ts: \"boolean\" maps to \"bool\", and the group alias \"number\" becomes an $in over the concrete types $type can return. Verified on a live mongod — query and expr now select the same documents. Only \"boolean\" and \"number\" changed; \"string\"/\"object\" and unrecognised aliases such as \"function\" emit exactly what they did before."
    },
    "pipeline\u0000$set({ t: typeof $.a === \"boolean\" });": {
      "entry": "pipeline",
      "src": "$set({ t: typeof $.a === \"boolean\" });",
      "kind": "now-rejected",
      "was": "[{\"$set\":{\"t\":{\"$eq\":[{\"$type\":\"$a\"},\"boolean\"]}}}]",
      "now": "'typeof' compares against one of MongoDB's type names, and \"boolean\" is not one. Did you mean 'bool'? For absence, write 'x === undefined'.",
      "reason": "Intended, and a BUG FIX. MongoDB's `$type` expression returns \"bool\" and \"int\"/\"double\"/\"long\"/\"decimal\" — never JavaScript's \"boolean\" or the query-only umbrella \"number\". The expression cell compared `$type` against JavaScript's own spelling, so `typeof $.a === \"boolean\"` was FALSE for every document, including one where the field really was a boolean; the identical source in Filter position was correct, because the query side had an alias table and the expression side had none. Both cells now read the one vocabulary in src/predicate-ir.ts: \"boolean\" maps to \"bool\", and the group alias \"number\" becomes an $in over the concrete types $type can return. Verified on a live mongod — query and expr now select the same documents. Only \"boolean\" and \"number\" changed; \"string\"/\"object\" and unrecognised aliases such as \"function\" emit exactly what they did before."
    },
    "update\u0000$set({ t: typeof $.a === \"boolean\" });": {
      "entry": "update",
      "src": "$set({ t: typeof $.a === \"boolean\" });",
      "kind": "output",
      "was": "[{\"$set\":{\"t\":{\"$eq\":[{\"$type\":\"$a\"},\"boolean\"]}}}]",
      "now": "[{\"$set\":{\"t\":{\"$eq\":[{\"$type\":\"$a\"},\"bool\"]}}}]",
      "reason": "Intended, and a BUG FIX. MongoDB's `$type` expression returns \"bool\" and \"int\"/\"double\"/\"long\"/\"decimal\" — never JavaScript's \"boolean\" or the query-only umbrella \"number\". The expression cell compared `$type` against JavaScript's own spelling, so `typeof $.a === \"boolean\"` was FALSE for every document, including one where the field really was a boolean; the identical source in Filter position was correct, because the query side had an alias table and the expression side had none. Both cells now read the one vocabulary in src/predicate-ir.ts: \"boolean\" maps to \"bool\", and the group alias \"number\" becomes an $in over the concrete types $type can return. Verified on a live mongod — query and expr now select the same documents. Only \"boolean\" and \"number\" changed; \"string\"/\"object\" and unrecognised aliases such as \"function\" emit exactly what they did before."
    },
    "expr\u0000typeof $.a.b.c === \"boolean\"": {
      "entry": "expr",
      "src": "typeof $.a.b.c === \"boolean\"",
      "kind": "output",
      "was": "{\"$eq\":[{\"$type\":\"$a.b.c\"},\"boolean\"]}",
      "now": "{\"$eq\":[{\"$type\":\"$a.b.c\"},\"bool\"]}",
      "reason": "Intended, and a BUG FIX. MongoDB's `$type` expression returns \"bool\" and \"int\"/\"double\"/\"long\"/\"decimal\" — never JavaScript's \"boolean\" or the query-only umbrella \"number\". The expression cell compared `$type` against JavaScript's own spelling, so `typeof $.a === \"boolean\"` was FALSE for every document, including one where the field really was a boolean; the identical source in Filter position was correct, because the query side had an alias table and the expression side had none. Both cells now read the one vocabulary in src/predicate-ir.ts: \"boolean\" maps to \"bool\", and the group alias \"number\" becomes an $in over the concrete types $type can return. Verified on a live mongod — query and expr now select the same documents. Only \"boolean\" and \"number\" changed; \"string\"/\"object\" and unrecognised aliases such as \"function\" emit exactly what they did before."
    },
    "jsmql\u0000$unionWith({ coll: \"c\", pipeline: [$match($.a > 0), $.o = $$$.orders.find(o => o.uid === 1)] });": {
      "entry": "jsmql",
      "src": "$unionWith({ coll: \"c\", pipeline: [$match($.a > 0), $.o = $$$.orders.find(o => o.uid === 1)] });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$unionWith\":{\"coll\":\"c\",\"pipeline\":[{\"$match\":{\"a\":{\"$gt\":0}}},{\"$set\":{\"o\":\"",
      "now": "'$.<field> = $$$.<coll>.find(…)' isn't available inside a literal sub-pipeline array — the '$lookup' it needs would have to run in the OUTER pipeline, against a different stream from this one. Write the '$lookup' stage d",
      "reason": "Intended, and a BUG FIX for a SILENT WRONG ANSWER. A lookup assignment written inside a literal sub-pipeline array used to have its `$lookup` hoisted into the OUTER pipeline while the reference to its result stayed inside — and the inner pipeline runs over a different collection, whose documents never carry the outer scratch slot, so the field read as MISSING on every document with no error anywhere. The extractor no longer descends into a stage's declared `subPipelineFields` (that is another pipeline's scope), and the sub-pipeline lowerer rejects the form with a message naming the two things that do work. Sibling of the `$ = …`, `$$ = …`, `$$$.<coll> = …` and `$$.push(...)` rejections already in that same guard. A lookup in an ORDINARY stage body still hoists exactly as before — `$project({ o: $$$.orders.find(...) })` is unchanged, and is now in the corpus to keep it that way."
    },
    "validate\u0000$unionWith({ coll: \"c\", pipeline: [$match($.a > 0), $.o = $$$.orders.find(o => o.uid === 1)] });": {
      "entry": "validate",
      "src": "$unionWith({ coll: \"c\", pipeline: [$match($.a > 0), $.o = $$$.orders.find(o => o.uid === 1)] });",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'$.<field> = $$$.<coll>.find(…)' isn't available inside a literal sub-pipeline array — the '$lookup' it needs would have to run in the OUTER pipeline, against a different stream from ",
      "reason": "Intended, and a BUG FIX for a SILENT WRONG ANSWER. A lookup assignment written inside a literal sub-pipeline array used to have its `$lookup` hoisted into the OUTER pipeline while the reference to its result stayed inside — and the inner pipeline runs over a different collection, whose documents never carry the outer scratch slot, so the field read as MISSING on every document with no error anywhere. The extractor no longer descends into a stage's declared `subPipelineFields` (that is another pipeline's scope), and the sub-pipeline lowerer rejects the form with a message naming the two things that do work. Sibling of the `$ = …`, `$$ = …`, `$$$.<coll> = …` and `$$.push(...)` rejections already in that same guard. A lookup in an ORDINARY stage body still hoists exactly as before — `$project({ o: $$$.orders.find(...) })` is unchanged, and is now in the corpus to keep it that way."
    },
    "jsmql\u0000$lookup({ from: \"o\", pipeline: [$.x = $$$.items.find(i => i.k === 1)], as: \"o\" });": {
      "entry": "jsmql",
      "src": "$lookup({ from: \"o\", pipeline: [$.x = $$$.items.find(i => i.k === 1)], as: \"o\" });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"items\",\"pipeline\":[{\"$match\":{\"k\":1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$lookup\":{\"from\":\"o\",\"pipeline\":[{\"$set\":{\"x\":\"$__jsmql.tmp.1\"}}],\"as\":\"o\"}},{\"$",
      "now": "'$.<field> = $$$.<coll>.find(…)' isn't available inside a literal sub-pipeline array — the '$lookup' it needs would have to run in the OUTER pipeline, against a different stream from this one. Write the '$lookup' stage d",
      "reason": "Intended, and a BUG FIX for a SILENT WRONG ANSWER. A lookup assignment written inside a literal sub-pipeline array used to have its `$lookup` hoisted into the OUTER pipeline while the reference to its result stayed inside — and the inner pipeline runs over a different collection, whose documents never carry the outer scratch slot, so the field read as MISSING on every document with no error anywhere. The extractor no longer descends into a stage's declared `subPipelineFields` (that is another pipeline's scope), and the sub-pipeline lowerer rejects the form with a message naming the two things that do work. Sibling of the `$ = …`, `$$ = …`, `$$$.<coll> = …` and `$$.push(...)` rejections already in that same guard. A lookup in an ORDINARY stage body still hoists exactly as before — `$project({ o: $$$.orders.find(...) })` is unchanged, and is now in the corpus to keep it that way."
    },
    "validate\u0000$lookup({ from: \"o\", pipeline: [$.x = $$$.items.find(i => i.k === 1)], as: \"o\" });": {
      "entry": "validate",
      "src": "$lookup({ from: \"o\", pipeline: [$.x = $$$.items.find(i => i.k === 1)], as: \"o\" });",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'$.<field> = $$$.<coll>.find(…)' isn't available inside a literal sub-pipeline array — the '$lookup' it needs would have to run in the OUTER pipeline, against a different stream from ",
      "reason": "Intended, and a BUG FIX for a SILENT WRONG ANSWER. A lookup assignment written inside a literal sub-pipeline array used to have its `$lookup` hoisted into the OUTER pipeline while the reference to its result stayed inside — and the inner pipeline runs over a different collection, whose documents never carry the outer scratch slot, so the field read as MISSING on every document with no error anywhere. The extractor no longer descends into a stage's declared `subPipelineFields` (that is another pipeline's scope), and the sub-pipeline lowerer rejects the form with a message naming the two things that do work. Sibling of the `$ = …`, `$$ = …`, `$$$.<coll> = …` and `$$.push(...)` rejections already in that same guard. A lookup in an ORDINARY stage body still hoists exactly as before — `$project({ o: $$$.orders.find(...) })` is unchanged, and is now in the corpus to keep it that way."
    },
    "jsmql\u0000$unionWith({ coll: \"c\", pipeline: [$.o = $$$.orders.find(o => o.uid === 1)] });": {
      "entry": "jsmql",
      "src": "$unionWith({ coll: \"c\", pipeline: [$.o = $$$.orders.find(o => o.uid === 1)] });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$unionWith\":{\"coll\":\"c\",\"pipeline\":[{\"$set\":{\"o\":\"$__jsmql.tmp.1\"}}]}},{\"$uns",
      "now": "'$.<field> = $$$.<coll>.find(…)' isn't available inside a literal sub-pipeline array — the '$lookup' it needs would have to run in the OUTER pipeline, against a different stream from this one. Write the '$lookup' stage d",
      "reason": "Intended, and a BUG FIX for a SILENT WRONG ANSWER. A lookup assignment written inside a literal sub-pipeline array used to have its `$lookup` hoisted into the OUTER pipeline while the reference to its result stayed inside — and the inner pipeline runs over a different collection, whose documents never carry the outer scratch slot, so the field read as MISSING on every document with no error anywhere. The extractor no longer descends into a stage's declared `subPipelineFields` (that is another pipeline's scope), and the sub-pipeline lowerer rejects the form with a message naming the two things that do work. Sibling of the `$ = …`, `$$ = …`, `$$$.<coll> = …` and `$$.push(...)` rejections already in that same guard. A lookup in an ORDINARY stage body still hoists exactly as before — `$project({ o: $$$.orders.find(...) })` is unchanged, and is now in the corpus to keep it that way."
    },
    "validate\u0000$unionWith({ coll: \"c\", pipeline: [$.o = $$$.orders.find(o => o.uid === 1)] });": {
      "entry": "validate",
      "src": "$unionWith({ coll: \"c\", pipeline: [$.o = $$$.orders.find(o => o.uid === 1)] });",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'$.<field> = $$$.<coll>.find(…)' isn't available inside a literal sub-pipeline array — the '$lookup' it needs would have to run in the OUTER pipeline, against a different stream from ",
      "reason": "Intended, and a BUG FIX for a SILENT WRONG ANSWER. A lookup assignment written inside a literal sub-pipeline array used to have its `$lookup` hoisted into the OUTER pipeline while the reference to its result stayed inside — and the inner pipeline runs over a different collection, whose documents never carry the outer scratch slot, so the field read as MISSING on every document with no error anywhere. The extractor no longer descends into a stage's declared `subPipelineFields` (that is another pipeline's scope), and the sub-pipeline lowerer rejects the form with a message naming the two things that do work. Sibling of the `$ = …`, `$$ = …`, `$$$.<coll> = …` and `$$.push(...)` rejections already in that same guard. A lookup in an ORDINARY stage body still hoists exactly as before — `$project({ o: $$$.orders.find(...) })` is unchanged, and is now in the corpus to keep it that way."
    },
    "jsmql\u0000$facet({ a: [$.o = $$$.orders.find(o => o.uid === 1)] });": {
      "entry": "jsmql",
      "src": "$facet({ a: [$.o = $$$.orders.find(o => o.uid === 1)] });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$facet\":{\"a\":[{\"$set\":{\"o\":\"$__jsmql.tmp.1\"}}]}}]",
      "now": "'$.<field> = $$$.<coll>.find(…)' isn't available inside a literal sub-pipeline array — the '$lookup' it needs would have to run in the OUTER pipeline, against a different stream from this one. Write the '$lookup' stage d",
      "reason": "Intended, and a BUG FIX for a SILENT WRONG ANSWER. A lookup assignment written inside a literal sub-pipeline array used to have its `$lookup` hoisted into the OUTER pipeline while the reference to its result stayed inside — and the inner pipeline runs over a different collection, whose documents never carry the outer scratch slot, so the field read as MISSING on every document with no error anywhere. The extractor no longer descends into a stage's declared `subPipelineFields` (that is another pipeline's scope), and the sub-pipeline lowerer rejects the form with a message naming the two things that do work. Sibling of the `$ = …`, `$$ = …`, `$$$.<coll> = …` and `$$.push(...)` rejections already in that same guard. A lookup in an ORDINARY stage body still hoists exactly as before — `$project({ o: $$$.orders.find(...) })` is unchanged, and is now in the corpus to keep it that way."
    },
    "validate\u0000$facet({ a: [$.o = $$$.orders.find(o => o.uid === 1)] });": {
      "entry": "validate",
      "src": "$facet({ a: [$.o = $$$.orders.find(o => o.uid === 1)] });",
      "kind": "output",
      "was": "{\"valid\":true,\"errors\":[]}",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'$.<field> = $$$.<coll>.find(…)' isn't available inside a literal sub-pipeline array — the '$lookup' it needs would have to run in the OUTER pipeline, against a different stream from ",
      "reason": "Intended, and a BUG FIX for a SILENT WRONG ANSWER. A lookup assignment written inside a literal sub-pipeline array used to have its `$lookup` hoisted into the OUTER pipeline while the reference to its result stayed inside — and the inner pipeline runs over a different collection, whose documents never carry the outer scratch slot, so the field read as MISSING on every document with no error anywhere. The extractor no longer descends into a stage's declared `subPipelineFields` (that is another pipeline's scope), and the sub-pipeline lowerer rejects the form with a message naming the two things that do work. Sibling of the `$ = …`, `$$ = …`, `$$$.<coll> = …` and `$$.push(...)` rejections already in that same guard. A lookup in an ORDINARY stage body still hoists exactly as before — `$project({ o: $$$.orders.find(...) })` is unchanged, and is now in the corpus to keep it that way."
    },
    "expr\u0000$.a === undefined": {
      "entry": "expr",
      "src": "$.a === undefined",
      "kind": "now-accepted",
      "was": "'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a '$match' stage.",
      "now": "{\"$eq\":[{\"$type\":\"$a\"},\"missing\"]}",
      "reason": "Intended, a NEW CAPABILITY the developer asked for: `x === undefined` is an EXISTENCE test in both targets. It already lowered to `$exists` in query position; expression position THREW, on the stated belief that the aggregation language cannot distinguish a missing field from a present-but-null one. It can: `$type` answers \"missing\" for an absent field and \"null\" for an explicit null, which is exactly the line `$exists` draws. The `Exists` node in src/predicate-ir.ts now owns both cells. Verified on a live mongod over {a:5}, {a:null}, {} — both targets select only the third for `=== undefined`, and `=== null` still selects only the second, so null is not collapsed into missing."
    },
    "expr\u0000$.a !== undefined": {
      "entry": "expr",
      "src": "$.a !== undefined",
      "kind": "now-accepted",
      "was": "'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a '$match' stage.",
      "now": "{\"$ne\":[{\"$type\":\"$a\"},\"missing\"]}",
      "reason": "Intended, a NEW CAPABILITY the developer asked for: `x === undefined` is an EXISTENCE test in both targets. It already lowered to `$exists` in query position; expression position THREW, on the stated belief that the aggregation language cannot distinguish a missing field from a present-but-null one. It can: `$type` answers \"missing\" for an absent field and \"null\" for an explicit null, which is exactly the line `$exists` draws. The `Exists` node in src/predicate-ir.ts now owns both cells. Verified on a live mongod over {a:5}, {a:null}, {} — both targets select only the third for `=== undefined`, and `=== null` still selects only the second, so null is not collapsed into missing."
    },
    "expr\u0000undefined === $.a": {
      "entry": "expr",
      "src": "undefined === $.a",
      "kind": "now-accepted",
      "was": "'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a '$match' stage.",
      "now": "{\"$eq\":[{\"$type\":\"$a\"},\"missing\"]}",
      "reason": "Intended, a NEW CAPABILITY the developer asked for: `x === undefined` is an EXISTENCE test in both targets. It already lowered to `$exists` in query position; expression position THREW, on the stated belief that the aggregation language cannot distinguish a missing field from a present-but-null one. It can: `$type` answers \"missing\" for an absent field and \"null\" for an explicit null, which is exactly the line `$exists` draws. The `Exists` node in src/predicate-ir.ts now owns both cells. Verified on a live mongod over {a:5}, {a:null}, {} — both targets select only the third for `=== undefined`, and `=== null` still selects only the second, so null is not collapsed into missing."
    },
    "expr\u0000$.a.b.c === undefined": {
      "entry": "expr",
      "src": "$.a.b.c === undefined",
      "kind": "now-accepted",
      "was": "'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a '$match' stage.",
      "now": "{\"$eq\":[{\"$type\":\"$a.b.c\"},\"missing\"]}",
      "reason": "Intended, a NEW CAPABILITY the developer asked for: `x === undefined` is an EXISTENCE test in both targets. It already lowered to `$exists` in query position; expression position THREW, on the stated belief that the aggregation language cannot distinguish a missing field from a present-but-null one. It can: `$type` answers \"missing\" for an absent field and \"null\" for an explicit null, which is exactly the line `$exists` draws. The `Exists` node in src/predicate-ir.ts now owns both cells. Verified on a live mongod over {a:5}, {a:null}, {} — both targets select only the third for `=== undefined`, and `=== null` still selects only the second, so null is not collapsed into missing."
    },
    "jsmql\u0000$.x === undefined ? 1 : 2": {
      "entry": "jsmql",
      "src": "$.x === undefined ? 1 : 2",
      "kind": "now-accepted",
      "was": "'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a '$match' stage.",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$eq\":[{\"$type\":\"$x\"},\"missing\"]},\"then\":1,\"else\":2}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$eq\":[{\"$type\":\"$x\"},\"missing\"]},\"then\":1,\"else\":2}},false]},{\"$ne\":[{\"$c",
      "reason": "Intended, a NEW CAPABILITY the developer asked for: `x === undefined` is an EXISTENCE test in both targets. It already lowered to `$exists` in query position; expression position THREW, on the stated belief that the aggregation language cannot distinguish a missing field from a present-but-null one. It can: `$type` answers \"missing\" for an absent field and \"null\" for an explicit null, which is exactly the line `$exists` draws. The `Exists` node in src/predicate-ir.ts now owns both cells. Verified on a live mongod over {a:5}, {a:null}, {} — both targets select only the third for `=== undefined`, and `=== null` still selects only the second, so null is not collapsed into missing."
    },
    "expr\u0000$.x === undefined ? 1 : 2": {
      "entry": "expr",
      "src": "$.x === undefined ? 1 : 2",
      "kind": "now-accepted",
      "was": "'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a '$match' stage.",
      "now": "{\"$cond\":{\"if\":{\"$eq\":[{\"$type\":\"$x\"},\"missing\"]},\"then\":1,\"else\":2}}",
      "reason": "Intended, a NEW CAPABILITY the developer asked for: `x === undefined` is an EXISTENCE test in both targets. It already lowered to `$exists` in query position; expression position THREW, on the stated belief that the aggregation language cannot distinguish a missing field from a present-but-null one. It can: `$type` answers \"missing\" for an absent field and \"null\" for an explicit null, which is exactly the line `$exists` draws. The `Exists` node in src/predicate-ir.ts now owns both cells. Verified on a live mongod over {a:5}, {a:null}, {} — both targets select only the third for `=== undefined`, and `=== null` still selects only the second, so null is not collapsed into missing."
    },
    "filter\u0000$.x === undefined ? 1 : 2": {
      "entry": "filter",
      "src": "$.x === undefined ? 1 : 2",
      "kind": "now-accepted",
      "was": "'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a '$match' stage.",
      "now": "{\"$expr\":{\"$cond\":{\"if\":{\"$eq\":[{\"$type\":\"$x\"},\"missing\"]},\"then\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[1,null]},null]},{\"$ne\":[1,false]},{\"$ne\":[1,\"\"]},{\"$ne\":[1,0]}]},\"else\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[2,null]},null]},{\"$ne\":[",
      "reason": "Intended, a NEW CAPABILITY the developer asked for: `x === undefined` is an EXISTENCE test in both targets. It already lowered to `$exists` in query position; expression position THREW, on the stated belief that the aggregation language cannot distinguish a missing field from a present-but-null one. It can: `$type` answers \"missing\" for an absent field and \"null\" for an explicit null, which is exactly the line `$exists` draws. The `Exists` node in src/predicate-ir.ts now owns both cells. Verified on a live mongod over {a:5}, {a:null}, {} — both targets select only the third for `=== undefined`, and `=== null` still selects only the second, so null is not collapsed into missing."
    },
    "validate\u0000$.x === undefined ? 1 : 2": {
      "entry": "validate",
      "src": "$.x === undefined ? 1 : 2",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a",
      "now": "{\"valid\":true,\"errors\":[]}",
      "reason": "Intended, a NEW CAPABILITY the developer asked for: `x === undefined` is an EXISTENCE test in both targets. It already lowered to `$exists` in query position; expression position THREW, on the stated belief that the aggregation language cannot distinguish a missing field from a present-but-null one. It can: `$type` answers \"missing\" for an absent field and \"null\" for an explicit null, which is exactly the line `$exists` draws. The `Exists` node in src/predicate-ir.ts now owns both cells. Verified on a live mongod over {a:5}, {a:null}, {} — both targets select only the third for `=== undefined`, and `=== null` still selects only the second, so null is not collapsed into missing."
    },
    "jsmql\u0000$.a + undefined": {
      "entry": "jsmql",
      "src": "$.a + undefined",
      "kind": "message",
      "was": "'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a '$match' stage.",
      "now": "'undefined' is only meaningful in a comparison — 'x === undefined' / 'x !== undefined' test whether a field is present. As a value it has no MongoDB equivalent: use 'null' for the present-but-null case, or 'delete $.fiel",
      "reason": "Intended, message follow-through. `undefined` used as a VALUE (not compared) still has no MongoDB equivalent and still throws, but the old text said it was \"only meaningful in '$match' position\" — no longer true now that a comparison works in expression position too. The message names the two comparison forms and the two alternatives (`null`, `delete $.field`)."
    },
    "expr\u0000$.a + undefined": {
      "entry": "expr",
      "src": "$.a + undefined",
      "kind": "message",
      "was": "'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a '$match' stage.",
      "now": "'undefined' is only meaningful in a comparison — 'x === undefined' / 'x !== undefined' test whether a field is present. As a value it has no MongoDB equivalent: use 'null' for the present-but-null case, or 'delete $.fiel",
      "reason": "Intended, message follow-through. `undefined` used as a VALUE (not compared) still has no MongoDB equivalent and still throws, but the old text said it was \"only meaningful in '$match' position\" — no longer true now that a comparison works in expression position too. The message names the two comparison forms and the two alternatives (`null`, `delete $.field`)."
    },
    "filter\u0000$.a + undefined": {
      "entry": "filter",
      "src": "$.a + undefined",
      "kind": "message",
      "was": "'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a '$match' stage.",
      "now": "'undefined' is only meaningful in a comparison — 'x === undefined' / 'x !== undefined' test whether a field is present. As a value it has no MongoDB equivalent: use 'null' for the present-but-null case, or 'delete $.fiel",
      "reason": "Intended, message follow-through. `undefined` used as a VALUE (not compared) still has no MongoDB equivalent and still throws, but the old text said it was \"only meaningful in '$match' position\" — no longer true now that a comparison works in expression position too. The message names the two comparison forms and the two alternatives (`null`, `delete $.field`)."
    },
    "validate\u0000$.a + undefined": {
      "entry": "validate",
      "src": "$.a + undefined",
      "kind": "output",
      "was": "{\"valid\":false,\"errors\":[{\"message\":\"'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a",
      "now": "{\"valid\":false,\"errors\":[{\"message\":\"'undefined' is only meaningful in a comparison — 'x === undefined' / 'x !== undefined' test whether a field is present. As a value it has no MongoDB equivalent: use 'null' for the pre",
      "reason": "Intended, message follow-through. `undefined` used as a VALUE (not compared) still has no MongoDB equivalent and still throws, but the old text said it was \"only meaningful in '$match' position\" — no longer true now that a comparison works in expression position too. The message names the two comparison forms and the two alternatives (`null`, `delete $.field`)."
    },
    "expr\u0000$.x !== undefined": {
      "entry": "expr",
      "src": "$.x !== undefined",
      "kind": "now-accepted",
      "was": "'undefined' is only meaningful in '$match' position (where it lowers to '$exists'). In aggregation expressions, use 'null' for the present-but-null case, or move the comparison into a '$match' stage.",
      "now": "{\"$ne\":[{\"$type\":\"$x\"},\"missing\"]}",
      "reason": "Intended, a NEW CAPABILITY the developer asked for: `x === undefined` is an EXISTENCE test in both targets. It already lowered to `$exists` in query position; expression position THREW, on the stated belief that the aggregation language cannot distinguish a missing field from a present-but-null one. It can: `$type` answers \"missing\" for an absent field and \"null\" for an explicit null, which is exactly the line `$exists` draws. The `Exists` node in src/predicate-ir.ts now owns both cells. Verified on a live mongod over {a:5}, {a:null}, {} — both targets select only the third for `=== undefined`, and `=== null` still selects only the second, so null is not collapsed into missing."
    },
    "jsmql\u0000$.email.startsWith(\"admin\")": {
      "entry": "jsmql",
      "src": "$.email.startsWith(\"admin\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$eq\":[{\"$indexOfCP\":[\"$email\",\"admin\"]},0]}}",
      "now": "{\"email\":\"«regexp:^admin/»\"}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "filter\u0000$.email.startsWith(\"admin\")": {
      "entry": "filter",
      "src": "$.email.startsWith(\"admin\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$eq\":[{\"$indexOfCP\":[\"$email\",\"admin\"]},0]}}",
      "now": "{\"email\":{\"$regex\":\"«regexp:^admin/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "jsmql\u0000$.file.endsWith(\".pdf\")": {
      "entry": "jsmql",
      "src": "$.file.endsWith(\".pdf\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"$file\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},4]}]},4]},\".pdf\"]}}}}",
      "now": "{\"file\":\"«regexp:\\\\.pdf$/»\"}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "filter\u0000$.file.endsWith(\".pdf\")": {
      "entry": "filter",
      "src": "$.file.endsWith(\".pdf\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"$file\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},4]}]},4]},\".pdf\"]}}}}",
      "now": "{\"file\":{\"$regex\":\"«regexp:\\\\.pdf$/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "jsmql\u0000$.file.endsWith(\"a👍b\")": {
      "entry": "jsmql",
      "src": "$.file.endsWith(\"a👍b\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"$file\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},3]}]},3]},\"a👍b\"]}}}}",
      "now": "{\"file\":\"«regexp:a👍b$/»\"}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "filter\u0000$.file.endsWith(\"a👍b\")": {
      "entry": "filter",
      "src": "$.file.endsWith(\"a👍b\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"$file\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},3]}]},3]},\"a👍b\"]}}}}",
      "now": "{\"file\":{\"$regex\":\"«regexp:a👍b$/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "jsmql\u0000$.arr.startsWith('a')": {
      "entry": "jsmql",
      "src": "$.arr.startsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$eq\":[{\"$indexOfCP\":[\"$arr\",\"a\"]},0]}}",
      "now": "{\"arr\":\"«regexp:^a/»\"}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "filter\u0000$.arr.startsWith('a')": {
      "entry": "filter",
      "src": "$.arr.startsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$eq\":[{\"$indexOfCP\":[\"$arr\",\"a\"]},0]}}",
      "now": "{\"arr\":{\"$regex\":\"«regexp:^a/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "jsmql\u0000$.arr.endsWith('a')": {
      "entry": "jsmql",
      "src": "$.arr.endsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"$arr\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},1]}]},1]},\"a\"]}}}}",
      "now": "{\"arr\":\"«regexp:a$/»\"}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "filter\u0000$.arr.endsWith('a')": {
      "entry": "filter",
      "src": "$.arr.endsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"$arr\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},1]}]},1]},\"a\"]}}}}",
      "now": "{\"arr\":{\"$regex\":\"«regexp:a$/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "jsmql\u0000$.s.startsWith('a')": {
      "entry": "jsmql",
      "src": "$.s.startsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$eq\":[{\"$indexOfCP\":[\"$s\",\"a\"]},0]}}",
      "now": "{\"s\":\"«regexp:^a/»\"}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "filter\u0000$.s.startsWith('a')": {
      "entry": "filter",
      "src": "$.s.startsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$eq\":[{\"$indexOfCP\":[\"$s\",\"a\"]},0]}}",
      "now": "{\"s\":{\"$regex\":\"«regexp:^a/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "jsmql\u0000$.s.endsWith('a')": {
      "entry": "jsmql",
      "src": "$.s.endsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},1]}]},1]},\"a\"]}}}}",
      "now": "{\"s\":\"«regexp:a$/»\"}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "filter\u0000$.s.endsWith('a')": {
      "entry": "filter",
      "src": "$.s.endsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"$s\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},1]}]},1]},\"a\"]}}}}",
      "now": "{\"s\":{\"$regex\":\"«regexp:a$/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "jsmql\u0000$.n.startsWith('a')": {
      "entry": "jsmql",
      "src": "$.n.startsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$eq\":[{\"$indexOfCP\":[\"$n\",\"a\"]},0]}}",
      "now": "{\"n\":\"«regexp:^a/»\"}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "filter\u0000$.n.startsWith('a')": {
      "entry": "filter",
      "src": "$.n.startsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$eq\":[{\"$indexOfCP\":[\"$n\",\"a\"]},0]}}",
      "now": "{\"n\":{\"$regex\":\"«regexp:^a/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "jsmql\u0000$.n.endsWith('a')": {
      "entry": "jsmql",
      "src": "$.n.endsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"$n\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},1]}]},1]},\"a\"]}}}}",
      "now": "{\"n\":\"«regexp:a$/»\"}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "filter\u0000$.n.endsWith('a')": {
      "entry": "filter",
      "src": "$.n.endsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"$n\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},1]}]},1]},\"a\"]}}}}",
      "now": "{\"n\":{\"$regex\":\"«regexp:a$/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "jsmql\u0000$.obj.startsWith('a')": {
      "entry": "jsmql",
      "src": "$.obj.startsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$eq\":[{\"$indexOfCP\":[\"$obj\",\"a\"]},0]}}",
      "now": "{\"obj\":\"«regexp:^a/»\"}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "filter\u0000$.obj.startsWith('a')": {
      "entry": "filter",
      "src": "$.obj.startsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$eq\":[{\"$indexOfCP\":[\"$obj\",\"a\"]},0]}}",
      "now": "{\"obj\":{\"$regex\":\"«regexp:^a/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "jsmql\u0000$.obj.endsWith('a')": {
      "entry": "jsmql",
      "src": "$.obj.endsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"$obj\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},1]}]},1]},\"a\"]}}}}",
      "now": "{\"obj\":\"«regexp:a$/»\"}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "filter\u0000$.obj.endsWith('a')": {
      "entry": "filter",
      "src": "$.obj.endsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"$obj\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},1]}]},1]},\"a\"]}}}}",
      "now": "{\"obj\":{\"$regex\":\"«regexp:a$/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "Intended, and a PERFORMANCE + ROBUSTNESS fix. `.startsWith(lit)` / `.endsWith(lit)` had no query form and fell through to `{ $expr: … }`, which is two problems. `$expr` CANNOT USE AN INDEX — measured on a live mongod with an index on the field: `{ s: /^he/ }` plans an IXSCAN, the `$expr` form plans a COLLSCAN. And the expression form calls `$indexOfCP`, which ERRORS on a non-string, so a single numeric value anywhere in the collection ABORTED THE WHOLE QUERY; the regex simply does not match those documents. The anchored Query cell of the `Contains` IR node now emits a real BSON regex (a RegExp instance, not a `$regex` document, so the driver serialises what the index reads). Gated on a LITERAL needle and a static field path — a runtime needle or a computed receiver keeps the expression fallback, and an HR1 `\"$x\"` needle is a field reference, not a literal. Regex metacharacters in the needle are escaped with a replacer FUNCTION, so a needle containing `$` cannot corrupt itself through String.replace's substitution patterns. Verified: query and expr select the same ids for `he`, `lo` and the metacharacter case `a.b`."
    },
    "jsmql\u0000$.items.length": {
      "entry": "jsmql",
      "src": "$.items.length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$s",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\"",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000$.items.length": {
      "entry": "expr",
      "src": "$.items.length",
      "kind": "output",
      "was": "{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}}",
      "now": "{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"else\":\"$$REMOVE\"}}}}",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "filter\u0000$.items.length": {
      "entry": "filter",
      "src": "$.items.length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$s",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"array\"]]},\"then\":{\"$size\":\"$items\"}},{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\"",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000$.matrix.find(r => r.length > 0).map(x => x)": {
      "entry": "jsmql",
      "src": "$.matrix.find(r => r.length > 0).map(x => x)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$map\":{\"input\":{\"$arrayElemAt\":[{\"$filter\":{\"input\":\"$matrix\",\"as\":\"r\",\"cond\":{\"$gt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$$r\"},\"then\":{\"$size\":\"$$r\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$$r\"",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$map\":{\"input\":{\"$arrayElemAt\":[{\"$filter\":{\"input\":\"$matrix\",\"as\":\"r\",\"cond\":{\"$gt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$$r\"},\"then\":{\"$size\":\"$$r\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000$.matrix.find(r => r.length > 0).map(x => x)": {
      "entry": "expr",
      "src": "$.matrix.find(r => r.length > 0).map(x => x)",
      "kind": "output",
      "was": "{\"$map\":{\"input\":{\"$arrayElemAt\":[{\"$filter\":{\"input\":\"$matrix\",\"as\":\"r\",\"cond\":{\"$gt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$$r\"},\"then\":{\"$size\":\"$$r\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$$r\",\"\"]}}}},0]}}},0]},\"as\":\"x\",\"in\":\"$$x\"",
      "now": "{\"$map\":{\"input\":{\"$arrayElemAt\":[{\"$filter\":{\"input\":\"$matrix\",\"as\":\"r\",\"cond\":{\"$gt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$$r\"},\"then\":{\"$size\":\"$$r\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$$r\"},[\"string\",\"missing\",\"null\"]]}",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000$.s.length.clamp(0, 10)": {
      "entry": "jsmql",
      "src": "$.s.length.clamp(0, 10)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$min\":[{\"$max\":[{\"$cond\":{\"if\":{\"$isArray\":\"$s\"},\"then\":{\"$size\":\"$s\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$s\",\"\"]}}}},0]},10]},null]},null]},{\"$ne\":[{\"$min\":[{\"$max\":[{\"$cond\":{\"if\":",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$min\":[{\"$max\":[{\"$cond\":{\"if\":{\"$isArray\":\"$s\"},\"then\":{\"$size\":\"$s\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$s\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000$.s.length.clamp(0, 10)": {
      "entry": "expr",
      "src": "$.s.length.clamp(0, 10)",
      "kind": "output",
      "was": "{\"$min\":[{\"$max\":[{\"$cond\":{\"if\":{\"$isArray\":\"$s\"},\"then\":{\"$size\":\"$s\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$s\",\"\"]}}}},0]},10]}",
      "now": "{\"$min\":[{\"$max\":[{\"$cond\":{\"if\":{\"$isArray\":\"$s\"},\"then\":{\"$size\":\"$s\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$s\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$s\",\"\"]}},\"else\":\"$$REMOVE\"}}}},0]},10]",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000$.user?.tags.length": {
      "entry": "jsmql",
      "src": "$.user?.tags.length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":{\"$ifNull\":[\"$user.tags\",[]]}},\"then\":{\"$size\":{\"$ifNull\":[\"$user.tags\",[]]}},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$user.tags\",[]]}}}},null]},null]},{\"$ne\":[{\"",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":{\"$ifNull\":[\"$user.tags\",[]]}},\"then\":{\"$size\":{\"$ifNull\":[\"$user.tags\",[]]}},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":{\"$ifNull\":[\"$user.tags\",[]]}},[\"strin",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000$.user?.tags.length": {
      "entry": "expr",
      "src": "$.user?.tags.length",
      "kind": "output",
      "was": "{\"$cond\":{\"if\":{\"$isArray\":{\"$ifNull\":[\"$user.tags\",[]]}},\"then\":{\"$size\":{\"$ifNull\":[\"$user.tags\",[]]}},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$user.tags\",[]]}}}}",
      "now": "{\"$cond\":{\"if\":{\"$isArray\":{\"$ifNull\":[\"$user.tags\",[]]}},\"then\":{\"$size\":{\"$ifNull\":[\"$user.tags\",[]]}},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":{\"$ifNull\":[\"$user.tags\",[]]}},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLe",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "filter\u0000$.user?.tags.length": {
      "entry": "filter",
      "src": "$.user?.tags.length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":{\"$ifNull\":[\"$user.tags\",[]]}},\"then\":{\"$size\":{\"$ifNull\":[\"$user.tags\",[]]}},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$user.tags\",[]]}}}},null]},null]},{\"$ne\":[{\"",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$user.tags\"},[\"array\"]]},\"then\":{\"$size\":\"$user.tags\"}},{\"case\":{\"$in\":[{\"$type\":\"$user.tags\"},[\"string\",\"null\",\"missing\"]]},\"then\":",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000[$match($.items.length === 3)]": {
      "entry": "jsmql",
      "src": "[$match($.items.length === 3)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},3]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"el",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000[$match($.items.length === 3)]": {
      "entry": "expr",
      "src": "[$match($.items.length === 3)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},3]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"el",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "pipeline\u0000[$match($.items.length === 3)]": {
      "entry": "pipeline",
      "src": "[$match($.items.length === 3)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},3]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"array\"]]},\"then\":{\"$size\":\"$items\"}},{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$ifNull",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000[$match($.items.length !== 0)]": {
      "entry": "jsmql",
      "src": "[$match($.items.length !== 0)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},0]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"el",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000[$match($.items.length !== 0)]": {
      "entry": "expr",
      "src": "[$match($.items.length !== 0)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},0]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"el",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "pipeline\u0000[$match($.items.length !== 0)]": {
      "entry": "pipeline",
      "src": "[$match($.items.length !== 0)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},0]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$ne\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"array\"]]},\"then\":{\"$size\":\"$items\"}},{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$ifNull",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000[$match(3 === $.items.length)]": {
      "entry": "jsmql",
      "src": "[$match(3 === $.items.length)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[3,{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}}]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[3,{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000[$match(3 === $.items.length)]": {
      "entry": "expr",
      "src": "[$match(3 === $.items.length)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[3,{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}}]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[3,{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "pipeline\u0000[$match(3 === $.items.length)]": {
      "entry": "pipeline",
      "src": "[$match(3 === $.items.length)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[3,{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}}]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[3,{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"array\"]]},\"then\":{\"$size\":\"$items\"}},{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$ifNu",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000[$match($.order.items.length === 1)]": {
      "entry": "jsmql",
      "src": "[$match($.order.items.length === 1)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$order.items\"},\"then\":{\"$size\":\"$order.items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$order.items\",\"\"]}}}},1]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$order.items\"},\"then\":{\"$size\":\"$order.items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$order.items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000[$match($.order.items.length === 1)]": {
      "entry": "expr",
      "src": "[$match($.order.items.length === 1)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$order.items\"},\"then\":{\"$size\":\"$order.items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$order.items\",\"\"]}}}},1]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$order.items\"},\"then\":{\"$size\":\"$order.items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$order.items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "pipeline\u0000[$match($.order.items.length === 1)]": {
      "entry": "pipeline",
      "src": "[$match($.order.items.length === 1)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$order.items\"},\"then\":{\"$size\":\"$order.items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$order.items\",\"\"]}}}},1]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$order.items\"},[\"array\"]]},\"then\":{\"$size\":\"$order.items\"}},{\"case\":{\"$in\":[{\"$type\":\"$order.items\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$s",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000[$match($.items.length < 20)]": {
      "entry": "jsmql",
      "src": "[$match($.items.length < 20)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$lt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},20]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$lt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"el",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000[$match($.items.length < 20)]": {
      "entry": "expr",
      "src": "[$match($.items.length < 20)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$lt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},20]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$lt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"el",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "pipeline\u0000[$match($.items.length < 20)]": {
      "entry": "pipeline",
      "src": "[$match($.items.length < 20)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$lt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},20]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$lt\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"array\"]]},\"then\":{\"$size\":\"$items\"}},{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$ifNull",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000[$match($.items.length >= 2)]": {
      "entry": "jsmql",
      "src": "[$match($.items.length >= 2)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$gte\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},2]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$gte\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"e",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000[$match($.items.length >= 2)]": {
      "entry": "expr",
      "src": "[$match($.items.length >= 2)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$gte\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},2]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$gte\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"e",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "pipeline\u0000[$match($.items.length >= 2)]": {
      "entry": "pipeline",
      "src": "[$match($.items.length >= 2)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$gte\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},2]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$gte\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"array\"]]},\"then\":{\"$size\":\"$items\"}},{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$ifNul",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000[$match(0 < $.items.length)]": {
      "entry": "jsmql",
      "src": "[$match(0 < $.items.length)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$lt\":[0,{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}}]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$lt\":[0,{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000[$match(0 < $.items.length)]": {
      "entry": "expr",
      "src": "[$match(0 < $.items.length)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$lt\":[0,{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}}]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$lt\":[0,{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "pipeline\u0000[$match(0 < $.items.length)]": {
      "entry": "pipeline",
      "src": "[$match(0 < $.items.length)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$lt\":[0,{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}}]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$lt\":[0,{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"array\"]]},\"then\":{\"$size\":\"$items\"}},{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$ifNu",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000[$match($.items.length === -1)]": {
      "entry": "jsmql",
      "src": "[$match($.items.length === -1)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},-1]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"el",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000[$match($.items.length === -1)]": {
      "entry": "expr",
      "src": "[$match($.items.length === -1)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},-1]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}},\"el",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "pipeline\u0000[$match($.items.length === -1)]": {
      "entry": "pipeline",
      "src": "[$match($.items.length === -1)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$cond\":{\"if\":{\"$isArray\":\"$items\"},\"then\":{\"$size\":\"$items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$items\",\"\"]}}}},-1]}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"array\"]]},\"then\":{\"$size\":\"$items\"}},{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$ifNull",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000\n$.cart.total >= 50 &&\n$.customer.status in [\"premium\", \"gold\", \"platinum\"] &&\n$.cart.items.length < 20 &&\n$.customer.region.trim().toLowerCase() === \"us\"\n      ": {
      "entry": "jsmql",
      "src": "\n$.cart.total >= 50 &&\n$.customer.status in [\"premium\", \"gold\", \"platinum\"] &&\n$.cart.items.length < 20 &&\n$.customer.region.trim().toLowerCase() === \"us\"\n      ",
      "kind": "output",
      "was": "{\"cart.total\":{\"$gte\":50},\"$expr\":{\"$and\":[{\"$in\":[\"$customer.status\",[\"premium\",\"gold\",\"platinum\"]]},{\"$lt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$cart.items\"},\"then\":{\"$size\":\"$cart.items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$car",
      "now": "{\"cart.total\":{\"$gte\":50},\"$expr\":{\"$and\":[{\"$in\":[\"$customer.status\",[\"premium\",\"gold\",\"platinum\"]]},{\"$lt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$cart.items\"},\"then\":{\"$size\":\"$cart.items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000\n$.cart.total >= 50 &&\n$.customer.status in [\"premium\", \"gold\", \"platinum\"] &&\n$.cart.items.length < 20 &&\n$.customer.region.trim().toLowerCase() === \"us\"\n      ": {
      "entry": "expr",
      "src": "\n$.cart.total >= 50 &&\n$.customer.status in [\"premium\", \"gold\", \"platinum\"] &&\n$.cart.items.length < 20 &&\n$.customer.region.trim().toLowerCase() === \"us\"\n      ",
      "kind": "output",
      "was": "{\"$and\":[{\"$gte\":[\"$cart.total\",50]},{\"$in\":[\"$customer.status\",[\"premium\",\"gold\",\"platinum\"]]},{\"$lt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$cart.items\"},\"then\":{\"$size\":\"$cart.items\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$cart.item",
      "now": "{\"$and\":[{\"$gte\":[\"$cart.total\",50]},{\"$in\":[\"$customer.status\",[\"premium\",\"gold\",\"platinum\"]]},{\"$lt\":[{\"$cond\":{\"if\":{\"$isArray\":\"$cart.items\"},\"then\":{\"$size\":\"$cart.items\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$ca",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000$.arr.length": {
      "entry": "jsmql",
      "src": "$.arr.length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$arr\"},\"then\":{\"$size\":\"$arr\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$arr\",\"\"]}}}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$arr\"},\"then\":{\"$size\":\"$a",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$arr\"},\"then\":{\"$size\":\"$arr\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$arr\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$arr\",\"\"]}},\"",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000$.arr.length": {
      "entry": "expr",
      "src": "$.arr.length",
      "kind": "output",
      "was": "{\"$cond\":{\"if\":{\"$isArray\":\"$arr\"},\"then\":{\"$size\":\"$arr\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$arr\",\"\"]}}}}",
      "now": "{\"$cond\":{\"if\":{\"$isArray\":\"$arr\"},\"then\":{\"$size\":\"$arr\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$arr\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$arr\",\"\"]}},\"else\":\"$$REMOVE\"}}}}",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "filter\u0000$.arr.length": {
      "entry": "filter",
      "src": "$.arr.length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$arr\"},\"then\":{\"$size\":\"$arr\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$arr\",\"\"]}}}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$arr\"},\"then\":{\"$size\":\"$a",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$arr\"},[\"array\"]]},\"then\":{\"$size\":\"$arr\"}},{\"case\":{\"$in\":[{\"$type\":\"$arr\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$if",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000$.s.length": {
      "entry": "jsmql",
      "src": "$.s.length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$s\"},\"then\":{\"$size\":\"$s\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$s\",\"\"]}}}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$s\"},\"then\":{\"$size\":\"$s\"},\"else",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$s\"},\"then\":{\"$size\":\"$s\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$s\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$s\",\"\"]}},\"else\":\"$",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000$.s.length": {
      "entry": "expr",
      "src": "$.s.length",
      "kind": "output",
      "was": "{\"$cond\":{\"if\":{\"$isArray\":\"$s\"},\"then\":{\"$size\":\"$s\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$s\",\"\"]}}}}",
      "now": "{\"$cond\":{\"if\":{\"$isArray\":\"$s\"},\"then\":{\"$size\":\"$s\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$s\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$s\",\"\"]}},\"else\":\"$$REMOVE\"}}}}",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "filter\u0000$.s.length": {
      "entry": "filter",
      "src": "$.s.length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$s\"},\"then\":{\"$size\":\"$s\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$s\",\"\"]}}}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$s\"},\"then\":{\"$size\":\"$s\"},\"else",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$s\"},[\"array\"]]},\"then\":{\"$size\":\"$s\"}},{\"case\":{\"$in\":[{\"$type\":\"$s\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000$.n.length": {
      "entry": "jsmql",
      "src": "$.n.length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$n\"},\"then\":{\"$size\":\"$n\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$n\",\"\"]}}}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$n\"},\"then\":{\"$size\":\"$n\"},\"else",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$n\"},\"then\":{\"$size\":\"$n\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$n\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$n\",\"\"]}},\"else\":\"$",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000$.n.length": {
      "entry": "expr",
      "src": "$.n.length",
      "kind": "output",
      "was": "{\"$cond\":{\"if\":{\"$isArray\":\"$n\"},\"then\":{\"$size\":\"$n\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$n\",\"\"]}}}}",
      "now": "{\"$cond\":{\"if\":{\"$isArray\":\"$n\"},\"then\":{\"$size\":\"$n\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$n\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$n\",\"\"]}},\"else\":\"$$REMOVE\"}}}}",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "filter\u0000$.n.length": {
      "entry": "filter",
      "src": "$.n.length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$n\"},\"then\":{\"$size\":\"$n\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$n\",\"\"]}}}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$n\"},\"then\":{\"$size\":\"$n\"},\"else",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$n\"},[\"array\"]]},\"then\":{\"$size\":\"$n\"}},{\"case\":{\"$in\":[{\"$type\":\"$n\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000$.obj.length": {
      "entry": "jsmql",
      "src": "$.obj.length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$obj\"},\"then\":{\"$size\":\"$obj\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$obj\",\"\"]}}}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$obj\"},\"then\":{\"$size\":\"$o",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$obj\"},\"then\":{\"$size\":\"$obj\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$obj\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$obj\",\"\"]}},\"",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "expr\u0000$.obj.length": {
      "entry": "expr",
      "src": "$.obj.length",
      "kind": "output",
      "was": "{\"$cond\":{\"if\":{\"$isArray\":\"$obj\"},\"then\":{\"$size\":\"$obj\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$obj\",\"\"]}}}}",
      "now": "{\"$cond\":{\"if\":{\"$isArray\":\"$obj\"},\"then\":{\"$size\":\"$obj\"},\"else\":{\"$cond\":{\"if\":{\"$in\":[{\"$type\":\"$obj\"},[\"string\",\"missing\",\"null\"]]},\"then\":{\"$strLenCP\":{\"$ifNull\":[\"$obj\",\"\"]}},\"else\":\"$$REMOVE\"}}}}",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "filter\u0000$.obj.length": {
      "entry": "filter",
      "src": "$.obj.length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$isArray\":\"$obj\"},\"then\":{\"$size\":\"$obj\"},\"else\":{\"$strLenCP\":{\"$ifNull\":[\"$obj\",\"\"]}}}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$isArray\":\"$obj\"},\"then\":{\"$size\":\"$o",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$obj\"},[\"array\"]]},\"then\":{\"$size\":\"$obj\"}},{\"case\":{\"$in\":[{\"$type\":\"$obj\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$if",
      "reason": "Intended, an HR3 fix: `.length` on a receiver of unknown type ABORTED THE QUERY. Its runtime dispatch was two-way — `$isArray` ? `$size` : `$strLenCP` — so it read \"not an array\" as \"string\", and `$strLenCP` ERRORS on an int. One numerically-typed value anywhere in that field killed the whole command, in filter and expression position alike. That is the same flawed reasoning `.at()` and numeric index access already avoid, and both already use the three-way form; `.length` was the odd one out. It now tests for a string EXPLICITLY and falls to `$$REMOVE` otherwise, which is what JavaScript's `(7).length` is. Crucially, MISSING and null still take the string branch, where `strLenOf`'s `$ifNull` makes them 0 — that 0 is a deliberate documented answer (`undefined?.length` is undefined; jsmql surfaces 0) and this fix preserves it. Verified on a live mongod across array/string/missing/null/int/object: 3, 4, 0, 0, missing, missing — and `$.v.length > 1` now returns [1,2] where it was rejected outright. Costs one `$cond` in the unknown-receiver case only."
    },
    "jsmql\u0000$.items.some(x => x > 0)": {
      "entry": "jsmql",
      "src": "$.items.some(x => x > 0)",
      "kind": "output",
      "was": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$items\",\"as\":\"x\",\"in\":{\"$gt\":[\"$$x\",0]}}}}}",
      "now": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$items\",[]]},\"as\":\"x\",\"in\":{\"$gt\":[\"$$x\",0]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.items.some(x => x > 0)": {
      "entry": "expr",
      "src": "$.items.some(x => x > 0)",
      "kind": "output",
      "was": "{\"$anyElementTrue\":{\"$map\":{\"input\":\"$items\",\"as\":\"x\",\"in\":{\"$gt\":[\"$$x\",0]}}}}",
      "now": "{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$items\",[]]},\"as\":\"x\",\"in\":{\"$gt\":[\"$$x\",0]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.items.every(x => x > 0)": {
      "entry": "jsmql",
      "src": "$.items.every(x => x > 0)",
      "kind": "output",
      "was": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":\"$items\",\"as\":\"x\",\"in\":{\"$gt\":[\"$$x\",0]}}}}}",
      "now": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$items\",[]]},\"as\":\"x\",\"in\":{\"$gt\":[\"$$x\",0]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.items.every(x => x > 0)": {
      "entry": "expr",
      "src": "$.items.every(x => x > 0)",
      "kind": "output",
      "was": "{\"$allElementsTrue\":{\"$map\":{\"input\":\"$items\",\"as\":\"x\",\"in\":{\"$gt\":[\"$$x\",0]}}}}",
      "now": "{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$items\",[]]},\"as\":\"x\",\"in\":{\"$gt\":[\"$$x\",0]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.xs.some(Boolean)": {
      "entry": "jsmql",
      "src": "$.xs.some(Boolean)",
      "kind": "output",
      "was": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$xs\",\"as\":\"v\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$v\",null]},null]},{\"$ne\":[\"$$v\",false]},{\"$ne\":[\"$$v\",\"\"]},{\"$ne\":[\"$$v\",0]}]}}}}}",
      "now": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$xs\",[]]},\"as\":\"v\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$v\",null]},null]},{\"$ne\":[\"$$v\",false]},{\"$ne\":[\"$$v\",\"\"]},{\"$ne\":[\"$$v\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.xs.some(Boolean)": {
      "entry": "expr",
      "src": "$.xs.some(Boolean)",
      "kind": "output",
      "was": "{\"$anyElementTrue\":{\"$map\":{\"input\":\"$xs\",\"as\":\"v\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$v\",null]},null]},{\"$ne\":[\"$$v\",false]},{\"$ne\":[\"$$v\",\"\"]},{\"$ne\":[\"$$v\",0]}]}}}}",
      "now": "{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$xs\",[]]},\"as\":\"v\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$v\",null]},null]},{\"$ne\":[\"$$v\",false]},{\"$ne\":[\"$$v\",\"\"]},{\"$ne\":[\"$$v\",0]}]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.xs.every(Boolean)": {
      "entry": "jsmql",
      "src": "$.xs.every(Boolean)",
      "kind": "output",
      "was": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":\"$xs\",\"as\":\"v\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$v\",null]},null]},{\"$ne\":[\"$$v\",false]},{\"$ne\":[\"$$v\",\"\"]},{\"$ne\":[\"$$v\",0]}]}}}}}",
      "now": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$xs\",[]]},\"as\":\"v\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$v\",null]},null]},{\"$ne\":[\"$$v\",false]},{\"$ne\":[\"$$v\",\"\"]},{\"$ne\":[\"$$v\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.xs.every(Boolean)": {
      "entry": "expr",
      "src": "$.xs.every(Boolean)",
      "kind": "output",
      "was": "{\"$allElementsTrue\":{\"$map\":{\"input\":\"$xs\",\"as\":\"v\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$v\",null]},null]},{\"$ne\":[\"$$v\",false]},{\"$ne\":[\"$$v\",\"\"]},{\"$ne\":[\"$$v\",0]}]}}}}",
      "now": "{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$xs\",[]]},\"as\":\"v\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$v\",null]},null]},{\"$ne\":[\"$$v\",false]},{\"$ne\":[\"$$v\",\"\"]},{\"$ne\":[\"$$v\",0]}]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.items.every(x => x.ok).toString()": {
      "entry": "jsmql",
      "src": "$.items.every(x => x.ok).toString()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$toString\":{\"$allElementsTrue\":{\"$map\":{\"input\":\"$items\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x.ok\",null]},null]},{\"$ne\":[\"$$x.ok\",false]},{\"$ne\":[\"$$x.ok\",\"\"]},{\"$ne\":[\"$",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$toString\":{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$items\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x.ok\",null]},null]},{\"$ne\":[\"$$x.ok\",false]},{\"$ne\":[\"$$x.ok",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.items.every(x => x.ok).toString()": {
      "entry": "expr",
      "src": "$.items.every(x => x.ok).toString()",
      "kind": "output",
      "was": "{\"$toString\":{\"$allElementsTrue\":{\"$map\":{\"input\":\"$items\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x.ok\",null]},null]},{\"$ne\":[\"$$x.ok\",false]},{\"$ne\":[\"$$x.ok\",\"\"]},{\"$ne\":[\"$$x.ok\",0]}]}}}}}",
      "now": "{\"$toString\":{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$items\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x.ok\",null]},null]},{\"$ne\":[\"$$x.ok\",false]},{\"$ne\":[\"$$x.ok\",\"\"]},{\"$ne\":[\"$$x.ok\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.a.some({ ok: true })": {
      "entry": "jsmql",
      "src": "$.a.some({ ok: true })",
      "kind": "output",
      "was": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$a\",\"as\":\"jsmqlItem\",\"in\":{\"$eq\":[\"$$jsmqlItem.ok\",true]}}}}}",
      "now": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$a\",[]]},\"as\":\"jsmqlItem\",\"in\":{\"$eq\":[\"$$jsmqlItem.ok\",true]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.a.some({ ok: true })": {
      "entry": "expr",
      "src": "$.a.some({ ok: true })",
      "kind": "output",
      "was": "{\"$anyElementTrue\":{\"$map\":{\"input\":\"$a\",\"as\":\"jsmqlItem\",\"in\":{\"$eq\":[\"$$jsmqlItem.ok\",true]}}}}",
      "now": "{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$a\",[]]},\"as\":\"jsmqlItem\",\"in\":{\"$eq\":[\"$$jsmqlItem.ok\",true]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "filter\u0000$.a.some({ ok: true })": {
      "entry": "filter",
      "src": "$.a.some({ ok: true })",
      "kind": "output",
      "was": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$a\",\"as\":\"jsmqlItem\",\"in\":{\"$eq\":[\"$$jsmqlItem.ok\",true]}}}}}",
      "now": "{\"a\":{\"$elemMatch\":{\"ok\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000[$match($.items.some(it => it.tag.toLowerCase() === 'vip'))]": {
      "entry": "jsmql",
      "src": "[$match($.items.some(it => it.tag.toLowerCase() === 'vip'))]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$items\",\"as\":\"it\",\"in\":{\"$eq\":[{\"$toLower\":\"$$it.tag\"},\"vip\"]}}}}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$items\",[]]},\"as\":\"it\",\"in\":{\"$eq\":[{\"$toLower\":\"$$it.tag\"},\"vip\"]}}}}}}]",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000[$match($.items.some(it => it.tag.toLowerCase() === 'vip'))]": {
      "entry": "expr",
      "src": "[$match($.items.some(it => it.tag.toLowerCase() === 'vip'))]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$items\",\"as\":\"it\",\"in\":{\"$eq\":[{\"$toLower\":\"$$it.tag\"},\"vip\"]}}}}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$items\",[]]},\"as\":\"it\",\"in\":{\"$eq\":[{\"$toLower\":\"$$it.tag\"},\"vip\"]}}}}}}]",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000[$match($.items.some(it => it))]": {
      "entry": "jsmql",
      "src": "[$match($.items.some(it => it))]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$items\",\"as\":\"it\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$it\",null]},null]},{\"$ne\":[\"$$it\",false]},{\"$ne\":[\"$$it\",\"\"]},{\"$ne\":[\"$$it\",0]}]}}}}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$items\",[]]},\"as\":\"it\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$it\",null]},null]},{\"$ne\":[\"$$it\",false]},{\"$ne\":[\"$$it\",\"\"]},{\"$ne\":[\"$$it\",0]}]}}}}}}]",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000[$match($.items.some(it => it))]": {
      "entry": "expr",
      "src": "[$match($.items.some(it => it))]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$items\",\"as\":\"it\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$it\",null]},null]},{\"$ne\":[\"$$it\",false]},{\"$ne\":[\"$$it\",\"\"]},{\"$ne\":[\"$$it\",0]}]}}}}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$items\",[]]},\"as\":\"it\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$it\",null]},null]},{\"$ne\":[\"$$it\",false]},{\"$ne\":[\"$$it\",\"\"]},{\"$ne\":[\"$$it\",0]}]}}}}}}]",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.arr.some(x => x)": {
      "entry": "jsmql",
      "src": "$.arr.some(x => x)",
      "kind": "output",
      "was": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$arr\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "now": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$arr\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.arr.some(x => x)": {
      "entry": "expr",
      "src": "$.arr.some(x => x)",
      "kind": "output",
      "was": "{\"$anyElementTrue\":{\"$map\":{\"input\":\"$arr\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "now": "{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$arr\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.arr.every(x => x)": {
      "entry": "jsmql",
      "src": "$.arr.every(x => x)",
      "kind": "output",
      "was": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":\"$arr\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "now": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$arr\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.arr.every(x => x)": {
      "entry": "expr",
      "src": "$.arr.every(x => x)",
      "kind": "output",
      "was": "{\"$allElementsTrue\":{\"$map\":{\"input\":\"$arr\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "now": "{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$arr\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.s.some(x => x)": {
      "entry": "jsmql",
      "src": "$.s.some(x => x)",
      "kind": "output",
      "was": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$s\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "now": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$s\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.s.some(x => x)": {
      "entry": "expr",
      "src": "$.s.some(x => x)",
      "kind": "output",
      "was": "{\"$anyElementTrue\":{\"$map\":{\"input\":\"$s\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "now": "{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$s\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.s.every(x => x)": {
      "entry": "jsmql",
      "src": "$.s.every(x => x)",
      "kind": "output",
      "was": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":\"$s\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "now": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$s\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.s.every(x => x)": {
      "entry": "expr",
      "src": "$.s.every(x => x)",
      "kind": "output",
      "was": "{\"$allElementsTrue\":{\"$map\":{\"input\":\"$s\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "now": "{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$s\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.n.some(x => x)": {
      "entry": "jsmql",
      "src": "$.n.some(x => x)",
      "kind": "output",
      "was": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$n\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "now": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$n\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.n.some(x => x)": {
      "entry": "expr",
      "src": "$.n.some(x => x)",
      "kind": "output",
      "was": "{\"$anyElementTrue\":{\"$map\":{\"input\":\"$n\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "now": "{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$n\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.n.every(x => x)": {
      "entry": "jsmql",
      "src": "$.n.every(x => x)",
      "kind": "output",
      "was": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":\"$n\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "now": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$n\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.n.every(x => x)": {
      "entry": "expr",
      "src": "$.n.every(x => x)",
      "kind": "output",
      "was": "{\"$allElementsTrue\":{\"$map\":{\"input\":\"$n\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "now": "{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$n\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.obj.some(x => x)": {
      "entry": "jsmql",
      "src": "$.obj.some(x => x)",
      "kind": "output",
      "was": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":\"$obj\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "now": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$obj\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.obj.some(x => x)": {
      "entry": "expr",
      "src": "$.obj.some(x => x)",
      "kind": "output",
      "was": "{\"$anyElementTrue\":{\"$map\":{\"input\":\"$obj\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "now": "{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$obj\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000$.obj.every(x => x)": {
      "entry": "jsmql",
      "src": "$.obj.every(x => x)",
      "kind": "output",
      "was": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":\"$obj\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "now": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$obj\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.obj.every(x => x)": {
      "entry": "expr",
      "src": "$.obj.every(x => x)",
      "kind": "output",
      "was": "{\"$allElementsTrue\":{\"$map\":{\"input\":\"$obj\",\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "now": "{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$obj\",[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000[1,2,3].some(x => x)": {
      "entry": "jsmql",
      "src": "[1,2,3].some(x => x)",
      "kind": "output",
      "was": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":[1,2,3],\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "now": "{\"$expr\":{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[[1,2,3],[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000[1,2,3].some(x => x)": {
      "entry": "expr",
      "src": "[1,2,3].some(x => x)",
      "kind": "output",
      "was": "{\"$anyElementTrue\":{\"$map\":{\"input\":[1,2,3],\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "now": "{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[[1,2,3],[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "jsmql\u0000[1,2,3].every(x => x)": {
      "entry": "jsmql",
      "src": "[1,2,3].every(x => x)",
      "kind": "output",
      "was": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":[1,2,3],\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "now": "{\"$expr\":{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[[1,2,3],[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000[1,2,3].every(x => x)": {
      "entry": "expr",
      "src": "[1,2,3].every(x => x)",
      "kind": "output",
      "was": "{\"$allElementsTrue\":{\"$map\":{\"input\":[1,2,3],\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "now": "{\"$allElementsTrue\":{\"$map\":{\"input\":{\"$ifNull\":[[1,2,3],[]]},\"as\":\"x\",\"in\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$x\",null]},null]},{\"$ne\":[\"$$x\",false]},{\"$ne\":[\"$$x\",\"\"]},{\"$ne\":[\"$$x\",0]}]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.items.some(i => i.q > 2)": {
      "entry": "expr",
      "src": "$.items.some(i => i.q > 2)",
      "kind": "output",
      "was": "{\"$anyElementTrue\":{\"$map\":{\"input\":\"$items\",\"as\":\"i\",\"in\":{\"$gt\":[\"$$i.q\",2]}}}}",
      "now": "{\"$anyElementTrue\":{\"$map\":{\"input\":{\"$ifNull\":[\"$items\",[]]},\"as\":\"i\",\"in\":{\"$gt\":[\"$$i.q\",2]}}}}",
      "reason": "Intended, an HR3 fix that also makes the two targets AGREE. `.some()` / `.every()` mapped the predicate over the raw receiver, and `$anyElementTrue` / `$allElementsTrue` abort the whole command on a non-array — so ONE document missing the field killed the query. The same predicate in Filter position lowers to `$elemMatch`, which simply does not match. The two targets answered differently and one of the answers was 'your query is dead'. The `$map` input is now `$ifNull[recv, []]`, so a missing receiver gives `false` for `.some` and `true` for `.every` — matching `$elemMatch` AND JavaScript's own `[].some` / `[].every`. Verified on a live mongod over docs with a match, a non-match, an empty array and a missing field: both targets select [1] and [1,3,4] respectively. Only `$ifNull` — a receiver that is PRESENT but wrongly typed still aborts, which is the line the rest of the array surface draws, and changing that is a separate decision."
    },
    "expr\u0000$.r = $$$.orders.map(o => { $sort({ x: -1 }); return o.total; });": {
      "entry": "expr",
      "src": "$.r = $$$.orders.map(o => { $sort({ x: -1 }); return o.total; });",
      "kind": "message",
      "was": "jsmql.expr() does not allow lookup syntax ('$$$.<coll>.<method>(...)') — joins are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() for cross-collection queries.",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "expr\u0000$.r = $$$.orders.filter(o => { const t = o.total; });": {
      "entry": "expr",
      "src": "$.r = $$$.orders.filter(o => { const t = o.total; });",
      "kind": "message",
      "was": "jsmql.expr() does not allow lookup syntax ('$$$.<coll>.<method>(...)') — joins are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() for cross-collection queries.",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "expr\u0000$dateDiff({ startDate: $.a, endDate: $.b, unit: \"day\", startOfWeek: \"funday\" })": {
      "entry": "expr",
      "src": "$dateDiff({ startDate: $.a, endDate: $.b, unit: \"day\", startOfWeek: \"funday\" })",
      "kind": "message",
      "was": "'$dateDiff' startOfWeek must be a weekday (monday, tuesday, wednesday, thursday, friday, saturday, sunday) — got 'funday'. Did you mean 'sunday'?",
      "now": "'$dateDiff' startOfWeek must be one of: monday, tuesday, wednesday, thursday, friday, saturday, sunday, mon, tue, wed, thu, fri, sat, sun — got 'funday'. Did you mean 'sunday'?",
      "reason": "message: one wording for every closed set, from the row's enum; the weekday list now also names the three-letter forms the server accepts."
    },
    "expr\u0000$convert({ input: $.s, to: \"intt\" })": {
      "entry": "expr",
      "src": "$convert({ input: $.s, to: \"intt\" })",
      "kind": "message",
      "was": "'$convert' to must be one of: double, string, object, array, binData, objectId, bool, date, null, regex, dbPointer, javascript, symbol, javascriptWithScope, int, timestamp, long, decimal, minKey, maxKey — got 'intt'. Did",
      "now": "'$convert' to must be one of: double, string, object, array, binData, objectId, bool, date, null, regex, dbPointer, javascript, symbol, javascriptWithScope, int, timestamp, long, decimal, minKey, maxKey, undefined — got ",
      "reason": "message: one wording for every closed set, from the row's enum; the weekday list now also names the three-letter forms the server accepts."
    },
    "expr\u0000$regexMatch({ input: $.s, regex: \"a\", options: \"gi\" })": {
      "entry": "expr",
      "src": "$regexMatch({ input: $.s, regex: \"a\", options: \"gi\" })",
      "kind": "message",
      "was": "'$regexMatch' options has an invalid regex flag 'g'. MongoDB allows only i, m, x, s — a JavaScript 'g' or 'y' flag is not supported.",
      "now": "'$regexMatch' options has an invalid flag 'g'. MongoDB allows only i, m, x, s, u — a JavaScript 'g' or 'y' flag is not supported.",
      "reason": "shippedBug: the server accepts the 'u' regex option (measured); the row states imxsu and the message lists it."
    },
    "expr\u00002 ** 3 ** 2": {
      "entry": "expr",
      "src": "2 ** 3 ** 2",
      "kind": "output",
      "was": "{\"$pow\":[2,{\"$pow\":[3,2]}]}",
      "now": "512",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000($.a + $.b) && $.c": {
      "entry": "expr",
      "src": "($.a + $.b) && $.c",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"v\":{\"$add\":[\"$a\",\"$b\"]}},\"in\":{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$v\",null]},null]},{\"$ne\":[\"$$v\",false]},{\"$ne\":[\"$$v\",\"\"]},{\"$ne\":[\"$$v\",0]}]},\"then\":\"$c\",\"else\":\"$$v\"}}}}",
      "now": "{\"$let\":{\"vars\":{\"jsmqlV\":{\"$add\":[\"$a\",\"$b\"]}},\"in\":{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$jsmqlV\",null]},null]},{\"$ne\":[\"$$jsmqlV\",false]},{\"$ne\":[\"$$jsmqlV\",\"\"]},{\"$ne\":[\"$$jsmqlV\",0]}]},\"then\":\"$c\",\"else\":\"$$",
      "reason": "intended: a compiler mint carries the jsmql prefix (src/namespace.ts), so it can never collide with a developer's parameter; the shipped bare `v` could."
    },
    "expr\u0000({ 0x507f1f77bcf86cd799439011: 1 })": {
      "entry": "expr",
      "src": "({ 0x507f1f77bcf86cd799439011: 1 })",
      "kind": "message",
      "was": "An ObjectId literal can't be an object key at position 3 — a field name is a string. Quote it (`{ \"507f1f77bcf86cd799439011\": … }`) to use it as a field name.",
      "now": "An ObjectId literal can't be an object key at position 3 — a field name is a string. Quote it (`{ \"507f1f77bcf86cd799439011\": … }`) to use it as a field name. at position 3",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000$.items.0": {
      "entry": "expr",
      "src": "$.items.0",
      "kind": "message",
      "was": "Expected property name after '.' at position 8",
      "now": "Expected a name but got '0' at position 8",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000[1,2,3,4,5].slice(1, 3)": {
      "entry": "expr",
      "src": "[1,2,3,4,5].slice(1, 3)",
      "kind": "output",
      "was": "{\"$slice\":[[1,2,3,4,5],1,2]}",
      "now": "[2,3]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3,4,5].slice(-2)": {
      "entry": "expr",
      "src": "[1,2,3,4,5].slice(-2)",
      "kind": "output",
      "was": "{\"$slice\":[[1,2,3,4,5],-2]}",
      "now": "[4,5]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3,4,5].slice(0, -1)": {
      "entry": "expr",
      "src": "[1,2,3,4,5].slice(0, -1)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlArr\":[1,2,3,4,5]},\"in\":{\"$slice\":[\"$$jsmqlArr\",{\"$max\":[{\"$subtract\":[{\"$size\":\"$$jsmqlArr\"},1]},0]}]}}}",
      "now": "[1,2,3,4]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3,4,5].slice(1, -1)": {
      "entry": "expr",
      "src": "[1,2,3,4,5].slice(1, -1)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlArr\":[1,2,3,4,5]},\"in\":{\"$let\":{\"vars\":{\"jsmqlK\":{\"$min\":[1,{\"$size\":\"$$jsmqlArr\"}]},\"jsmqlF\":{\"$max\":[{\"$subtract\":[{\"$size\":\"$$jsmqlArr\"},1]},0]}},\"in\":{\"$cond\":[{\"$gt\":[{\"$subtract\":[\"$$jsmqlF\",",
      "now": "[2,3,4]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000Boolean + 5": {
      "entry": "expr",
      "src": "Boolean + 5",
      "kind": "message",
      "was": "'Boolean' used as a value is only valid as a callback to a higher-order array method (e.g. $.items.filter(Boolean)). To coerce a single value, write Boolean(value).",
      "now": "'Boolean' used as a value is only valid as a callback to a higher-order array method (e.g. $.items.map(Boolean)). To apply it to one value, write Boolean(value).",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000new Date(\"not-a-date\")": {
      "entry": "expr",
      "src": "new Date(\"not-a-date\")",
      "kind": "message",
      "was": "new Date(\"not-a-date\") — \"not-a-date\" is not a valid date string. Use an ISO 8601 date like \"2026-01-01\" or \"2026-01-01T00:00:00.000Z\".",
      "now": "new Date(<string>) — the string is not a valid date. Use an ISO 8601 date like \"2026-01-01\" or \"2026-01-01T00:00:00.000Z\".",
      "reason": "message: the row states one text for the class of constant; the shipped interpolated the offending constant."
    },
    "expr\u0000new Date(1, 2, 3, 4, 5, 6, 7, 8)": {
      "entry": "expr",
      "src": "new Date(1, 2, 3, 4, 5, 6, 7, 8)",
      "kind": "message",
      "was": "'new Date(year, month, day, hour, minute, second, ms)' takes at most 7 arguments, got 8 at position 0",
      "now": "Date(year, month, day, hour, minute, second, ms) requires 2, 3, 4, 5, 6, or 7 arguments, got 8",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000Date.UTC(2024, 1, 15)": {
      "entry": "expr",
      "src": "Date.UTC(2024, 1, 15)",
      "kind": "output",
      "was": "{\"$toLong\":{\"$dateFromParts\":{\"year\":2024,\"month\":1,\"day\":15,\"timezone\":\"UTC\"}}}",
      "now": "1705276800000",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000Date.UTC(1970)": {
      "entry": "expr",
      "src": "Date.UTC(1970)",
      "kind": "output",
      "was": "{\"$toLong\":{\"$dateFromParts\":{\"year\":1970,\"timezone\":\"UTC\"}}}",
      "now": "0",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000$.a | $.b && $.c": {
      "entry": "expr",
      "src": "$.a | $.b && $.c",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"v\":{\"$bitOr\":[\"$a\",\"$b\"]}},\"in\":{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$v\",null]},null]},{\"$ne\":[\"$$v\",false]},{\"$ne\":[\"$$v\",\"\"]},{\"$ne\":[\"$$v\",0]}]},\"then\":\"$c\",\"else\":\"$$v\"}}}}",
      "now": "{\"$let\":{\"vars\":{\"jsmqlV\":{\"$bitOr\":[\"$a\",\"$b\"]}},\"in\":{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$jsmqlV\",null]},null]},{\"$ne\":[\"$$jsmqlV\",false]},{\"$ne\":[\"$$jsmqlV\",\"\"]},{\"$ne\":[\"$$jsmqlV\",0]}]},\"then\":\"$c\",\"else\":\"",
      "reason": "intended: a compiler mint carries the jsmql prefix (src/namespace.ts), so it can never collide with a developer's parameter; the shipped bare `v` could."
    },
    "expr\u0000$.items.map(x => { const a = 1; })": {
      "entry": "expr",
      "src": "$.items.map(x => { const a = 1; })",
      "kind": "message",
      "was": "A block body must end with a `return <expr>` statement at position 32, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "now": "A block body must end with a `return <expr>` statement at position 32. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directly at posi",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000[1, 2].map(a => a)": {
      "entry": "expr",
      "src": "[1, 2].map(a => a)",
      "kind": "output",
      "was": "{\"$map\":{\"input\":[1,2],\"as\":\"a\",\"in\":\"$$a\"}}",
      "now": "[1,2]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1, 2].join(\",\")": {
      "entry": "expr",
      "src": "[1, 2].join(\",\")",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":[1,2],\"initialValue\":\"\",\"in\":{\"$cond\":{\"if\":{\"$eq\":[\"$$value\",\"\"]},\"then\":{\"$toString\":\"$$this\"},\"else\":{\"$concat\":[\"$$value\",\",\",{\"$toString\":\"$$this\"}]}}}}}",
      "now": "\"1,2\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000$.r = $$$.orders.sortBy((o) => { $limit(2); });": {
      "entry": "expr",
      "src": "$.r = $$$.orders.sortBy((o) => { $limit(2); });",
      "kind": "message",
      "was": "Unexpected stage call at position 33: a `=> { $stage(...); ... }` block is a sub-pipeline, but '.sortBy(...)' doesn't take one. Pipeline stages belong to `.aggregate(pipeline)` alone; every other callback's block body is",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "expr\u0000$.r = $$$.orders.aggregat((o) => { $limit(2); });": {
      "entry": "expr",
      "src": "$.r = $$$.orders.aggregat((o) => { $limit(2); });",
      "kind": "message",
      "was": "Unexpected stage call at position 35: a `=> { $stage(...); ... }` block is a sub-pipeline, but '.aggregat(...)' doesn't take one. Did you mean '.aggregate'? Pipeline stages belong to `.aggregate(pipeline)` alone; every o",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "expr\u0000$.items.map((d) => { const a = d.v; })": {
      "entry": "expr",
      "src": "$.items.map((d) => { const a = d.v; })",
      "kind": "message",
      "was": "A block body must end with a `return <expr>` statement at position 36, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "now": "A block body must end with a `return <expr>` statement at position 36. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directly at posi",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000$.items.map((d) => { d.v; })": {
      "entry": "expr",
      "src": "$.items.map((d) => { d.v; })",
      "kind": "message",
      "was": "A block body must end with a `return <expr>` statement at position 21, got an identifier 'd'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expr",
      "now": "A block body must end with a `return <expr>` statement at position 26. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directly at posi",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000$.items.map(x => { k: x })": {
      "entry": "expr",
      "src": "$.items.map(x => { k: x })",
      "kind": "message",
      "was": "A block body must end with a `return <expr>` statement at position 19, got an identifier 'k'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expr",
      "now": "Expected ';' but got ':' at position 20",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000\"-\".repeat(5)": {
      "entry": "expr",
      "src": "\"-\".repeat(5)",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":{\"$range\":[0,5]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\"-\"]}}}",
      "now": "\"-----\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000Array.from($.iter)": {
      "entry": "expr",
      "src": "Array.from($.iter)",
      "kind": "message",
      "was": "Array.from() only supports the {length: n} form: Array.from({length: n}, (_, i) => …). For other inputs use $op($range, …) or .map().",
      "now": "Only 'Array.from({ length: n })' is supported. To turn an iterable into an array, write the array literal or '.map(...)' on it.",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u00001.5n": {
      "entry": "expr",
      "src": "1.5n",
      "kind": "message",
      "was": "Invalid BigInt literal at position 0: 'n' suffix requires an integer (no fraction or exponent)",
      "now": "Invalid BigInt literal: the 'n' suffix requires an integer at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u00001e2n": {
      "entry": "expr",
      "src": "1e2n",
      "kind": "message",
      "was": "Invalid BigInt literal at position 0: 'n' suffix requires an integer (no fraction or exponent)",
      "now": "Invalid BigInt literal: the 'n' suffix requires an integer at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000((x) => x + 1)(5)": {
      "entry": "expr",
      "src": "((x) => x + 1)(5)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"x\":5},\"in\":{\"$add\":[\"$$x\",1]}}}",
      "now": "6",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000$.name.frobulate()": {
      "entry": "expr",
      "src": "$.name.frobulate()",
      "kind": "message",
      "was": "Unknown method '.frobulate()'.",
      "now": "Unknown method '.frobulate()' at position 6.",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "expr\u0000$.name.toLowerCse()": {
      "entry": "expr",
      "src": "$.name.toLowerCse()",
      "kind": "message",
      "was": "Unknown method '.toLowerCse()'. Did you mean '.toLowerCase()'?",
      "now": "Unknown method '.toLowerCse()' at position 6. Did you mean '.toLowerCase()'?",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "expr\u0000$.items.fliter(x => x)": {
      "entry": "expr",
      "src": "$.items.fliter(x => x)",
      "kind": "message",
      "was": "Unknown method '.fliter()'. Did you mean '.filter()'?",
      "now": "Unknown method '.fliter()' at position 7. Did you mean '.filter()'?",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "expr\u0000$.name.substing(1)": {
      "entry": "expr",
      "src": "$.name.substing(1)",
      "kind": "message",
      "was": "Unknown method '.substing()'. Did you mean '.substring()'?",
      "now": "Unknown method '.substing()' at position 6. Did you mean '.substring()'?",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "expr\u0000Math.flor($.x)": {
      "entry": "expr",
      "src": "Math.flor($.x)",
      "kind": "message",
      "was": "Unknown Math member 'flor' at position 5. Did you mean 'Math.floor'? See docs/LANGUAGE.md for the full list of supported Math methods and constants.",
      "now": "Unknown method 'Math.flor()' at position 4. Did you mean 'Math.floor'?",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "expr\u0000Number.isItneger($.x)": {
      "entry": "expr",
      "src": "Number.isItneger($.x)",
      "kind": "message",
      "was": "Unknown Number static method 'isItneger' at position 7. Did you mean 'Number.isInteger'? Supported: Number.isInteger, Number.isNaN, Number.isFinite.",
      "now": "Unknown method 'Number.isItneger()' at position 6. Did you mean 'Number.isInteger'?",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "expr\u0000Object.keyz($.o)": {
      "entry": "expr",
      "src": "Object.keyz($.o)",
      "kind": "message",
      "was": "Unknown Object method 'keyz' at position 7. Did you mean 'Object.keys'? Supported: keys, values, entries, assign, fromEntries, groupBy.",
      "now": "Unknown method 'Object.keyz()' at position 6. Did you mean 'Object.keys'?",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "expr\u0000$.s.trim() = 1": {
      "entry": "expr",
      "src": "$.s.trim() = 1",
      "kind": "message",
      "was": "Cannot assign to a method-call result ('.trim()') at position 11 — only field paths like '$.x' or '$.x.y' are assignable.",
      "now": "Unexpected token '=' at position 11",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "expr\u000042 = 1": {
      "entry": "expr",
      "src": "42 = 1",
      "kind": "message",
      "was": "Cannot assign to a literal value at position 3 — only field paths like '$.x' or '$.x.y' are assignable.",
      "now": "Unexpected token '=' at position 3",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "expr\u0000\"hello\".slice(1, 3)": {
      "entry": "expr",
      "src": "\"hello\".slice(1, 3)",
      "kind": "output",
      "was": "{\"$substrCP\":[\"hello\",1,2]}",
      "now": "\"el\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000\"hello\".slice(2)": {
      "entry": "expr",
      "src": "\"hello\".slice(2)",
      "kind": "output",
      "was": "{\"$substrCP\":[\"hello\",2,3]}",
      "now": "\"llo\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000\"hello\".slice(-3)": {
      "entry": "expr",
      "src": "\"hello\".slice(-3)",
      "kind": "output",
      "was": "{\"$substrCP\":[\"hello\",2,3]}",
      "now": "\"llo\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000\"hello\".slice(1, -1)": {
      "entry": "expr",
      "src": "\"hello\".slice(1, -1)",
      "kind": "output",
      "was": "{\"$substrCP\":[\"hello\",1,3]}",
      "now": "\"ell\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000$.a &&": {
      "entry": "expr",
      "src": "$.a &&",
      "kind": "message",
      "was": "Unexpected end of expression",
      "now": "Unexpected end of input at position 6",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000$.a ? $.b": {
      "entry": "expr",
      "src": "$.a ? $.b",
      "kind": "message",
      "was": "Expected ':' in ternary expression at position 9",
      "now": "Expected ':' but got end of input at position 9",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000Object.fromEntries([[\"a\", 1], [\"b\", 2]])": {
      "entry": "expr",
      "src": "Object.fromEntries([[\"a\", 1], [\"b\", 2]])",
      "kind": "output",
      "was": "{\"$arrayToObject\":[[\"a\",1],[\"b\",2]]}",
      "now": "{\"a\":1,\"b\":2}",
      "reason": "shippedBug: the shipped emitted {$arrayToObject:[[\"a\",1],[\"b\",2]]}, which the server reads as two arguments and refuses (measured: 'Expression $arrayToObject takes exactly 1 arguments. 2 were passed in'); the fold settles the constant to {a:1,b:2}."
    },
    "expr\u0000[1, 2].concat([3, 4])": {
      "entry": "expr",
      "src": "[1, 2].concat([3, 4])",
      "kind": "output",
      "was": "{\"$concatArrays\":[[1,2],[3,4]]}",
      "now": "[1,2,3,4]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u00001_": {
      "entry": "expr",
      "src": "1_",
      "kind": "message",
      "was": "Numeric separator '_' must be between two digits (at position 1)",
      "now": "Numeric separator '_' must be between two digits at position 1",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u00001__0": {
      "entry": "expr",
      "src": "1__0",
      "kind": "message",
      "was": "Numeric separator '_' must be between two digits (at position 1)",
      "now": "Numeric separator '_' must be between two digits at position 1",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u000010 /* skip */ / 2": {
      "entry": "expr",
      "src": "10 /* skip */ / 2",
      "kind": "output",
      "was": "{\"$divide\":[10,2]}",
      "now": "5",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000$.a /* unclosed": {
      "entry": "expr",
      "src": "$.a /* unclosed",
      "kind": "message",
      "was": "Unclosed block comment starting at position 4",
      "now": "Unterminated block comment at position 4",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000$min()": {
      "entry": "expr",
      "src": "$min()",
      "kind": "message",
      "was": "Operator $min expects at least 1 argument",
      "now": "$min(operands) requires at least 1 argument, got 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000$median($.scores, \"approximate\")": {
      "entry": "expr",
      "src": "$median($.scores, \"approximate\")",
      "kind": "now-accepted",
      "was": "$median is an accumulator operator — only valid inside '$group' field-value slots or '$setWindowFields' output slots. Use $group({ _id: ..., <key>: $median(...) }) to compute it per-group, or $setWindowFields(...) for th",
      "now": "{\"$median\":{\"input\":\"$scores\",\"method\":\"approximate\"}}",
      "reason": "shippedBug: $median is an aggregation expression since MongoDB 7.0 — measured: {$addFields:{m:{$median:{input:\"$a\",method:\"approximate\"}}}} → 2 on mongod."
    },
    "expr\u0000$$foo": {
      "entry": "expr",
      "src": "$$foo",
      "kind": "message",
      "was": "Expected '.<name>' or '[<expr>]' after '$$' at position 0",
      "now": "Unexpected token 'foo' at position 2",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000Math.max(1, 2,)": {
      "entry": "expr",
      "src": "Math.max(1, 2,)",
      "kind": "output",
      "was": "{\"$max\":[1,2]}",
      "now": "2",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000Math.max(1, 2)": {
      "entry": "expr",
      "src": "Math.max(1, 2)",
      "kind": "output",
      "was": "{\"$max\":[1,2]}",
      "now": "2",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000Number($.x, $.y)": {
      "entry": "expr",
      "src": "Number($.x, $.y)",
      "kind": "message",
      "was": "Type cast 'Number()' takes exactly 1 argument at position 0",
      "now": "'Number(x)' takes exactly one value.",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000Date.UTC(2020, 1,)": {
      "entry": "expr",
      "src": "Date.UTC(2020, 1,)",
      "kind": "output",
      "was": "{\"$toLong\":{\"$dateFromParts\":{\"year\":2020,\"month\":1,\"timezone\":\"UTC\"}}}",
      "now": "1577836800000",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000Date.UTC(2020, 1)": {
      "entry": "expr",
      "src": "Date.UTC(2020, 1)",
      "kind": "output",
      "was": "{\"$toLong\":{\"$dateFromParts\":{\"year\":2020,\"month\":1,\"timezone\":\"UTC\"}}}",
      "now": "1577836800000",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000$._id === ObjectId(\"507f1f77bcf86cd79943901\")": {
      "entry": "expr",
      "src": "$._id === ObjectId(\"507f1f77bcf86cd79943901\")",
      "kind": "message",
      "was": "ObjectId(\"507f1f77bcf86cd79943901\") is not a valid ObjectId: expected exactly 24 hexadecimal characters, got 23 characters, at position 19.",
      "now": "'ObjectId(<constant>)' — this constant is not 24 hex characters. Write '0x507f1f77bcf86cd799439011' (or 'ObjectId(\"507f1f77bcf86cd799439011\")').",
      "reason": "message: the row states one text for the class of constant; the shipped interpolated the offending constant."
    },
    "expr\u0000$._id === ObjectId(\"zzzf1f77bcf86cd799439011\")": {
      "entry": "expr",
      "src": "$._id === ObjectId(\"zzzf1f77bcf86cd799439011\")",
      "kind": "message",
      "was": "ObjectId(\"zzzf1f77bcf86cd799439011\") is not a valid ObjectId: expected exactly 24 hexadecimal characters, it contains non-hexadecimal characters, at position 19.",
      "now": "'ObjectId(<constant>)' — this constant is not 24 hex characters. Write '0x507f1f77bcf86cd799439011' (or 'ObjectId(\"507f1f77bcf86cd799439011\")').",
      "reason": "message: the row states one text for the class of constant; the shipped interpolated the offending constant."
    },
    "expr\u0000$._id === ObjectId(\"nothex\")": {
      "entry": "expr",
      "src": "$._id === ObjectId(\"nothex\")",
      "kind": "message",
      "was": "ObjectId(\"nothex\") is not a valid ObjectId: expected exactly 24 hexadecimal characters, got 6 characters, at position 19.",
      "now": "'ObjectId(<constant>)' — this constant is not 24 hex characters. Write '0x507f1f77bcf86cd799439011' (or 'ObjectId(\"507f1f77bcf86cd799439011\")').",
      "reason": "message: the row states one text for the class of constant; the shipped interpolated the offending constant."
    },
    "expr\u0000$.x === 0xFFFFFFFFFFFFFFFF": {
      "entry": "expr",
      "src": "$.x === 0xFFFFFFFFFFFFFFFF",
      "kind": "message",
      "was": "Hex literal '0xFFFFFFFFFFFFFFFF' at position 8 has 16 digits — neither a 24-character ObjectId nor an integer that fits Number.MAX_SAFE_INTEGER. Paste a 24-character hex string for an ObjectId, or use a decimal literal.",
      "now": "Hex literal '0xFFFFFFFFFFFFFFFF' at position 8 has 16 digits — neither a 24-character ObjectId nor an integer that fits Number.MAX_SAFE_INTEGER. Paste a 24-character hex string for an ObjectId, or use a decimal literal. ",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u00000x20000000000000": {
      "entry": "expr",
      "src": "0x20000000000000",
      "kind": "message",
      "was": "Hex literal '0x20000000000000' at position 0 has 14 digits — neither a 24-character ObjectId nor an integer that fits Number.MAX_SAFE_INTEGER. Paste a 24-character hex string for an ObjectId, or use a decimal literal.",
      "now": "Hex literal '0x20000000000000' at position 0 has 14 digits — neither a 24-character ObjectId nor an integer that fits Number.MAX_SAFE_INTEGER. Paste a 24-character hex string for an ObjectId, or use a decimal literal. at",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000$._id === 0x507f1f77bcf86cd79943901": {
      "entry": "expr",
      "src": "$._id === 0x507f1f77bcf86cd79943901",
      "kind": "message",
      "was": "Hex literal '0x507f1f77bcf86cd79943901' at position 10 has 23 digits — neither a 24-character ObjectId nor an integer that fits Number.MAX_SAFE_INTEGER. Paste a 24-character hex string for an ObjectId, or use a decimal l",
      "now": "Hex literal '0x507f1f77bcf86cd79943901' at position 10 has 23 digits — neither a 24-character ObjectId nor an integer that fits Number.MAX_SAFE_INTEGER. Paste a 24-character hex string for an ObjectId, or use a decimal l",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u00000x1234567812345678123456789": {
      "entry": "expr",
      "src": "0x1234567812345678123456789",
      "kind": "message",
      "was": "Hex literal '0x1234567812345678123456789' at position 0 has 25 digits — neither a 24-character ObjectId nor an integer that fits Number.MAX_SAFE_INTEGER. Paste a 24-character hex string for an ObjectId, or use a decimal ",
      "now": "Hex literal '0x1234567812345678123456789' at position 0 has 25 digits — neither a 24-character ObjectId nor an integer that fits Number.MAX_SAFE_INTEGER. Paste a 24-character hex string for an ObjectId, or use a decimal ",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u00000x000000000000000000000001": {
      "entry": "expr",
      "src": "0x000000000000000000000001",
      "kind": "message",
      "was": "ObjectId 000000000000000000000001 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 000000000000000000000001 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000{ _id: 0x123456781234567812345678 }": {
      "entry": "expr",
      "src": "{ _id: 0x123456781234567812345678 }",
      "kind": "message",
      "was": "ObjectId 123456781234567812345678 looks like a typo: its embedded timestamp decodes to 1979-09-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 123456781234567812345678 looks like a typo: its embedded timestamp decodes to 1979-09-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u00000x000000000000000000000000": {
      "entry": "expr",
      "src": "0x000000000000000000000000",
      "kind": "message",
      "was": "ObjectId 000000000000000000000000 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 000000000000000000000000 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u00000x49ffffffffffffffffffffff": {
      "entry": "expr",
      "src": "0x49ffffffffffffffffffffff",
      "kind": "message",
      "was": "ObjectId 49ffffffffffffffffffffff looks like a typo: its embedded timestamp decodes to 2009-05-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 49ffffffffffffffffffffff looks like a typo: its embedded timestamp decodes to 2009-05-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000$._id === 0x000000000000000000000001": {
      "entry": "expr",
      "src": "$._id === 0x000000000000000000000001",
      "kind": "message",
      "was": "ObjectId 000000000000000000000001 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 000000000000000000000001 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000((_) => 1)(2)": {
      "entry": "expr",
      "src": "((_) => 1)(2)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"v_\":2},\"in\":1}}",
      "now": "1",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000$ = { a: $.xs.map(x => { const g = z => z + 1; return g(x); }) };": {
      "entry": "expr",
      "src": "$ = { a: $.xs.map(x => { const g = z => z + 1; return g(x); }) };",
      "kind": "message",
      "was": "Reusable functions must be declared at the top level of a pipeline, not inside an arrow body. Move `const g = (…) => …` out of the `=> { … }` block.",
      "now": "Unexpected token '=' at position 2",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000((x) => x * 2)(5)": {
      "entry": "expr",
      "src": "((x) => x * 2)(5)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"x\":5},\"in\":{\"$multiply\":[\"$$x\",2]}}}",
      "now": "10",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000(function (x) { return x * 2 })(5)": {
      "entry": "expr",
      "src": "(function (x) { return x * 2 })(5)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"x\":5},\"in\":{\"$multiply\":[\"$$x\",2]}}}",
      "now": "10",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000function* f(x) { return x } $ = { a: f($.n) }": {
      "entry": "expr",
      "src": "function* f(x) { return x } $ = { a: f($.n) }",
      "kind": "message",
      "was": "jsmql does not support generator functions (`function*`). Use a plain `function (…) { return … }` or an arrow.",
      "now": "Expected '(' but got '*' at position 8",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000[1, let x = 5]": {
      "entry": "expr",
      "src": "[1, let x = 5]",
      "kind": "message",
      "was": "`let` is a pipeline statement, not a value, and is only valid as a pipeline-array element. If this array is meant to be a pipeline, ensure its first element is a stage like `$match(...)`.",
      "now": "`let` is a statement, not a value. It is only valid at the top level or as a pipeline-array element.",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000let = 5;": {
      "entry": "expr",
      "src": "let = 5;",
      "kind": "message",
      "was": "Expected an identifier after `let` at position 4, got '='",
      "now": "Unexpected token 'let' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000let x 5;": {
      "entry": "expr",
      "src": "let x 5;",
      "kind": "message",
      "was": "Expected '=' after `let x` at position 6, got '5'. `let` requires an initialiser — write `let x = <expr>`.",
      "now": "Unexpected token 'let' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000let x = ; $match($.a > 0)": {
      "entry": "expr",
      "src": "let x = ; $match($.a > 0)",
      "kind": "message",
      "was": "Unexpected token ';' at position 8",
      "now": "Unexpected token 'let' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000let in = 5;": {
      "entry": "expr",
      "src": "let in = 5;",
      "kind": "message",
      "was": "Expected an identifier after `let` at position 4, got 'in'",
      "now": "Unexpected token 'let' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000let $foo = 5;": {
      "entry": "expr",
      "src": "let $foo = 5;",
      "kind": "message",
      "was": "Expected an identifier after `let` at position 4, got '$'",
      "now": "Unexpected token 'let' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000const = 5;": {
      "entry": "expr",
      "src": "const = 5;",
      "kind": "message",
      "was": "Expected an identifier after `const` at position 6, got '='",
      "now": "Unexpected token 'const' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000const x 5;": {
      "entry": "expr",
      "src": "const x 5;",
      "kind": "message",
      "was": "Expected '=' after `const x` at position 8, got '5'. `const` requires an initialiser — write `const x = <expr>`.",
      "now": "Unexpected token 'const' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000($.a + $.b) || $.c": {
      "entry": "expr",
      "src": "($.a + $.b) || $.c",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"v\":{\"$add\":[\"$a\",\"$b\"]}},\"in\":{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$v\",null]},null]},{\"$ne\":[\"$$v\",false]},{\"$ne\":[\"$$v\",\"\"]},{\"$ne\":[\"$$v\",0]}]},\"then\":\"$$v\",\"else\":\"$c\"}}}}",
      "now": "{\"$let\":{\"vars\":{\"jsmqlV\":{\"$add\":[\"$a\",\"$b\"]}},\"in\":{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$jsmqlV\",null]},null]},{\"$ne\":[\"$$jsmqlV\",false]},{\"$ne\":[\"$$jsmqlV\",\"\"]},{\"$ne\":[\"$$jsmqlV\",0]}]},\"then\":\"$$jsmqlV\",\"els",
      "reason": "intended: a compiler mint carries the jsmql prefix (src/namespace.ts), so it can never collide with a developer's parameter; the shipped bare `v` could."
    },
    "expr\u0000$.x = $$$.orders.map(o => { $sort({ x: -1 }); return o.total; });": {
      "entry": "expr",
      "src": "$.x = $$$.orders.map(o => { $sort({ x: -1 }); return o.total; });",
      "kind": "message",
      "was": "jsmql.expr() does not allow lookup syntax ('$$$.<coll>.<method>(...)') — joins are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() for cross-collection queries.",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "expr\u0000$.x = $$$.orders.filter(o => o.uid === $._id).find(o => { $match(o.c); });": {
      "entry": "expr",
      "src": "$.x = $$$.orders.filter(o => o.uid === $._id).find(o => { $match(o.c); });",
      "kind": "message",
      "was": "jsmql.expr() does not allow lookup syntax ('$$$.<coll>.<method>(...)') — joins are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() for cross-collection queries.",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "expr\u0000$.x = $$$.c.aggregate((o) => { $sort({ a: 1 }); return o.v; });": {
      "entry": "expr",
      "src": "$.x = $$$.c.aggregate((o) => { $sort({ a: 1 }); return o.v; });",
      "kind": "message",
      "was": "jsmql.expr() does not allow lookup syntax ('$$$.<coll>.<method>(...)') — joins are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() for cross-collection queries.",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "expr\u0000[1, 2].head().round()": {
      "entry": "expr",
      "src": "[1, 2].head().round()",
      "kind": "output",
      "was": "{\"$round\":[{\"$first\":[[1,2]]},0]}",
      "now": "1",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[[1, 2], [3]].head().toReversed()": {
      "entry": "expr",
      "src": "[[1, 2], [3]].head().toReversed()",
      "kind": "output",
      "was": "{\"$reverseArray\":{\"$first\":[[[1,2],[3]]]}}",
      "now": "[2,1]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[{ $macth: $.age > 18 }]": {
      "entry": "expr",
      "src": "[{ $macth: $.age > 18 }]",
      "kind": "now-accepted",
      "was": "Element 0 of pipeline: '$macth' is not a known aggregation stage. Did you mean '$match'?",
      "now": "[{\"$macth\":{\"$gt\":[\"$age\",18]}}]",
      "reason": "statement slice: detecting a bracketed stage list is the statement target's job; the value target reads an array of documents, which HR2 passes through."
    },
    "expr\u0000[$prject({ name: 1 })]": {
      "entry": "expr",
      "src": "[$prject({ name: 1 })]",
      "kind": "now-accepted",
      "was": "Element 0 of pipeline: '$prject' is not a known aggregation stage. Did you mean '$project'?",
      "now": "[{\"$prject\":{\"name\":1}}]",
      "reason": "statement slice: detecting a bracketed stage list is the statement target's job; the value target reads an array of documents, which HR2 passes through."
    },
    "expr\u0000[{ $match: { age: 1 }, $sort: { age: 1 } }]": {
      "entry": "expr",
      "src": "[{ $match: { age: 1 }, $sort: { age: 1 } }]",
      "kind": "now-accepted",
      "was": "Element 0 of pipeline must be a single-key stage object (e.g. `{ $match: ... }`), but found an object with 2 keys.",
      "now": "[{\"$match\":{\"age\":1},\"$sort\":{\"age\":1}}]",
      "reason": "statement slice: detecting a bracketed stage list is the statement target's job; the value target reads an array of documents, which HR2 passes through."
    },
    "expr\u0000$ = { topByScore: $$.filter(o => { $sort({ score: -1 }); $limit(10); }) };": {
      "entry": "expr",
      "src": "$ = { topByScore: $$.filter(o => { $sort({ score: -1 }); $limit(10); }) };",
      "kind": "message",
      "was": "`.filter(o => { … })` takes a JavaScript callback, so its block body holds `const`/`let` bindings and one `return <expr>`. `$sort(...)` is a pipeline stage, not part of a callback — chain them as stage calls instead — `$",
      "now": "Unexpected token '=' at position 2",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000$$ = $$$.orders.filter(o => o.userId === $._id).map(o => {\n          assert(o.total > 0, \"bad order\");\n          return { id: o._id, t: o.total };\n        });": {
      "entry": "expr",
      "src": "$$ = $$$.orders.filter(o => o.userId === $._id).map(o => {\n          assert(o.total > 0, \"bad order\");\n          return { id: o._id, t: o.total };\n        });",
      "kind": "message",
      "was": "jsmql.expr() does not allow lookup syntax ('$$$.<coll>.<method>(...)') — joins are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() for cross-collection queries.",
      "now": "Unexpected token '=' at position 3",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "expr\u0000$$ = $$.map(d => { $match(d.active === true); return { id: d._id }; });": {
      "entry": "expr",
      "src": "$$ = $$.map(d => { $match(d.active === true); return { id: d._id }; });",
      "kind": "message",
      "was": "`.map(d => { … })` takes a JavaScript callback, so its block body holds `const`/`let` bindings and one `return <expr>`. `$match(...)` is a pipeline stage, not part of a callback — chain them as stage calls instead — `$$.",
      "now": "Unexpected token '=' at position 3",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000$$ = $$.map((d, _i, coll) => { assert(coll.length > 0, \"empty\"); return { id: d._id }; });": {
      "entry": "expr",
      "src": "$$ = $$.map((d, _i, coll) => { assert(coll.length > 0, \"empty\"); return { id: d._id }; });",
      "kind": "message",
      "was": "`.map(d => { … })` takes a JavaScript callback, so its block body holds `const`/`let` bindings and one `return <expr>`. `assert(...)` is a pipeline stage, not part of a callback — chain them as stage calls instead — `$$.",
      "now": "Unexpected token '=' at position 3",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000$$ = $$.map(d => { const t = d.total; });": {
      "entry": "expr",
      "src": "$$ = $$.map(d => { const t = d.total; });",
      "kind": "message",
      "was": ".map(d => { … }) must end with 'return <expr>' — the returned value becomes each output document.",
      "now": "Unexpected token '=' at position 3",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u00001 + $.x++": {
      "entry": "expr",
      "src": "1 + $.x++",
      "kind": "message",
      "was": "Cannot assign to a '+' expression at position 7 — only field paths like '$.x' or '$.x.y' are assignable.",
      "now": "Unexpected token '++' at position 7",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u00005 - -3": {
      "entry": "expr",
      "src": "5 - -3",
      "kind": "output",
      "was": "{\"$subtract\":[5,-3]}",
      "now": "8",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000$.a += $.b += 1": {
      "entry": "expr",
      "src": "$.a += $.b += 1",
      "kind": "message",
      "was": "Compound assignment cannot be chained at position 7 — split into separate statements",
      "now": "Unexpected token '+=' at position 4",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000$.a =": {
      "entry": "expr",
      "src": "$.a =",
      "kind": "message",
      "was": "Unexpected end of expression",
      "now": "Unexpected token '=' at position 4",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000delete": {
      "entry": "expr",
      "src": "delete",
      "kind": "message",
      "was": "Unexpected end of expression",
      "now": "Unexpected token 'delete' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000($.a = 1) + 2": {
      "entry": "expr",
      "src": "($.a = 1) + 2",
      "kind": "message",
      "was": "Assignment is a statement, not a value. It is only valid at the top level or as a pipeline-array element.",
      "now": "Expected ')' but got '=' at position 5",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000[1,2,3].take(2)": {
      "entry": "expr",
      "src": "[1,2,3].take(2)",
      "kind": "output",
      "was": "{\"$slice\":[[1,2,3],2]}",
      "now": "[1,2]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3].drop(2)": {
      "entry": "expr",
      "src": "[1,2,3].drop(2)",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlArr\":[1,2,3]},\"in\":{\"$slice\":[\"$$jsmqlArr\",2,{\"$max\":[1,{\"$size\":\"$$jsmqlArr\"}]}]}}}",
      "now": "[3]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3].slice(1,2)": {
      "entry": "expr",
      "src": "[1,2,3].slice(1,2)",
      "kind": "output",
      "was": "{\"$slice\":[[1,2,3],1,1]}",
      "now": "[2]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3].slice(-2)": {
      "entry": "expr",
      "src": "[1,2,3].slice(-2)",
      "kind": "output",
      "was": "{\"$slice\":[[1,2,3],-2]}",
      "now": "[2,3]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3].map(x => x)": {
      "entry": "expr",
      "src": "[1,2,3].map(x => x)",
      "kind": "output",
      "was": "{\"$map\":{\"input\":[1,2,3],\"as\":\"x\",\"in\":\"$$x\"}}",
      "now": "[1,2,3]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3].sortBy('t')": {
      "entry": "expr",
      "src": "[1,2,3].sortBy('t')",
      "kind": "output",
      "was": "{\"$sortArray\":{\"input\":[1,2,3],\"sortBy\":{\"t\":1}}}",
      "now": "[1,2,3]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3].reduce((a,b) => a + b, 0)": {
      "entry": "expr",
      "src": "[1,2,3].reduce((a,b) => a + b, 0)",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":[1,2,3],\"initialValue\":0,\"in\":{\"$add\":[\"$$value\",\"$$this\"]}}}",
      "now": "6",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3].includes(1)": {
      "entry": "expr",
      "src": "[1,2,3].includes(1)",
      "kind": "output",
      "was": "{\"$in\":[1,[1,2,3]]}",
      "now": "true",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3].join(',')": {
      "entry": "expr",
      "src": "[1,2,3].join(',')",
      "kind": "output",
      "was": "{\"$reduce\":{\"input\":[1,2,3],\"initialValue\":\"\",\"in\":{\"$cond\":{\"if\":{\"$eq\":[\"$$value\",\"\"]},\"then\":{\"$toString\":\"$$this\"},\"else\":{\"$concat\":[\"$$value\",\",\",{\"$toString\":\"$$this\"}]}}}}}",
      "now": "\"1,2,3\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3].at(-1)": {
      "entry": "expr",
      "src": "[1,2,3].at(-1)",
      "kind": "output",
      "was": "{\"$arrayElemAt\":[[1,2,3],-1]}",
      "now": "3",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3].concat([1])": {
      "entry": "expr",
      "src": "[1,2,3].concat([1])",
      "kind": "output",
      "was": "{\"$concatArrays\":[[1,2,3],[1]]}",
      "now": "[1,2,3,1]",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000[1,2,3].length": {
      "entry": "expr",
      "src": "[1,2,3].length",
      "kind": "output",
      "was": "{\"$size\":[[1,2,3]]}",
      "now": "3",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000\"abc\".slice(1,2)": {
      "entry": "expr",
      "src": "\"abc\".slice(1,2)",
      "kind": "output",
      "was": "{\"$substrCP\":[\"abc\",1,1]}",
      "now": "\"b\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000\"abc\".slice(-2)": {
      "entry": "expr",
      "src": "\"abc\".slice(-2)",
      "kind": "output",
      "was": "{\"$substrCP\":[\"abc\",1,2]}",
      "now": "\"bc\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000\"abc\".groupBy('t')": {
      "entry": "expr",
      "src": "\"abc\".groupBy('t')",
      "kind": "message",
      "was": "'.groupBy(...)' expects an array receiver, but the value before it returns a string. Call '.groupBy(...)' on an array value instead.",
      "now": "'.groupBy()' is not available on a 'string' — it is defined on 'array', 'stream', 'Object'.",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "expr\u0000\"abc\".trim()": {
      "entry": "expr",
      "src": "\"abc\".trim()",
      "kind": "output",
      "was": "{\"$trim\":{\"input\":\"abc\"}}",
      "now": "\"abc\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000\"abc\".toUpperCase()": {
      "entry": "expr",
      "src": "\"abc\".toUpperCase()",
      "kind": "output",
      "was": "{\"$toUpper\":\"abc\"}",
      "now": "\"ABC\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000\"abc\".startsWith('a')": {
      "entry": "expr",
      "src": "\"abc\".startsWith('a')",
      "kind": "output",
      "was": "{\"$eq\":[{\"$indexOfCP\":[\"abc\",\"a\"]},0]}",
      "now": "true",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000\"abc\".endsWith('a')": {
      "entry": "expr",
      "src": "\"abc\".endsWith('a')",
      "kind": "output",
      "was": "{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"abc\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},1]}]},1]},\"a\"]}}}",
      "now": "false",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000\"abc\".charAt(1)": {
      "entry": "expr",
      "src": "\"abc\".charAt(1)",
      "kind": "output",
      "was": "{\"$substrCP\":[\"abc\",1,1]}",
      "now": "\"b\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000\"abc\".substring(1,2)": {
      "entry": "expr",
      "src": "\"abc\".substring(1,2)",
      "kind": "output",
      "was": "{\"$substrCP\":[\"abc\",1,1]}",
      "now": "\"b\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000\"abc\".at(-1)": {
      "entry": "expr",
      "src": "\"abc\".at(-1)",
      "kind": "output",
      "was": "{\"$substrCP\":[\"abc\",2,1]}",
      "now": "\"c\"",
      "reason": "equivalent: the fold settles a constant expression to its value; the shipped compiler lowered the same constants to an operator the server evaluates to that value."
    },
    "expr\u0000$accumulator({ init: \"function() {}\", accumulate: \"function() {}\", merge: \"function() {}\", lang: \"js\" })": {
      "entry": "expr",
      "src": "$accumulator({ init: \"function() {}\", accumulate: \"function() {}\", merge: \"function() {}\", lang: \"js\" })",
      "kind": "message",
      "was": "$accumulator is an accumulator operator — only valid inside '$group' field-value slots or '$setWindowFields' output slots. Use $group({ _id: ..., <key>: $accumulator(...) }) to compute it per-group, or $setWindowFields(.",
      "now": "$accumulator is a '$group'-only accumulator — MongoDB has no expression or window form for it. Use $group({ _id: ..., <key>: $accumulator({ init, accumulate, accumulateArgs, merge, lang }) }).",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$.r = $$$.orders.map(o => { $sort({ x: -1 }); return o.total; });": {
      "entry": "filter",
      "src": "$.r = $$$.orders.map(o => { $sort({ x: -1 }); return o.total; });",
      "kind": "message",
      "was": "jsmql.filter() does not allow lookup syntax ('$$$.<coll>.<method>(...)') — joins are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() for cross-collection queries.",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u0000$.r = $$$.orders.filter(o => { const t = o.total; });": {
      "entry": "filter",
      "src": "$.r = $$$.orders.filter(o => { const t = o.total; });",
      "kind": "message",
      "was": "jsmql.filter() does not allow lookup syntax ('$$$.<coll>.<method>(...)') — joins are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() for cross-collection queries.",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u0000$gt($.x)": {
      "entry": "filter",
      "src": "$gt($.x)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$gt\":\"$x\"}}",
      "now": "$gt(expr1, expr2) requires exactly 2 arguments, got 1",
      "reason": "shippedBug: a comparison operator with one operand at the top of a filter became {$expr:{$gt:\"$x\"}}, which the server refuses ($gt takes two operands); inside a raw query document the one-operand spelling still passes through as the query operator."
    },
    "filter\u0000$eq(5)": {
      "entry": "filter",
      "src": "$eq(5)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$eq\":5}}",
      "now": "$eq(expr1, expr2) requires exactly 2 arguments, got 1",
      "reason": "shippedBug: a comparison operator with one operand at the top of a filter became {$expr:{$gt:\"$x\"}}, which the server refuses ($gt takes two operands); inside a raw query document the one-operand spelling still passes through as the query operator."
    },
    "filter\u0000$lte($.score)": {
      "entry": "filter",
      "src": "$lte($.score)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$lte\":\"$score\"}}",
      "now": "$lte(expr1, expr2) requires exactly 2 arguments, got 1",
      "reason": "shippedBug: a comparison operator with one operand at the top of a filter became {$expr:{$gt:\"$x\"}}, which the server refuses ($gt takes two operands); inside a raw query document the one-operand spelling still passes through as the query operator."
    },
    "filter\u0000$gt($.a)": {
      "entry": "filter",
      "src": "$gt($.a)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$gt\":\"$a\"}}",
      "now": "$gt(expr1, expr2) requires exactly 2 arguments, got 1",
      "reason": "shippedBug: a comparison operator with one operand at the top of a filter became {$expr:{$gt:\"$x\"}}, which the server refuses ($gt takes two operands); inside a raw query document the one-operand spelling still passes through as the query operator."
    },
    "filter\u0000$gt($.a, $.b, $.c)": {
      "entry": "filter",
      "src": "$gt($.a, $.b, $.c)",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$gt\":[\"$a\",\"$b\",\"$c\"]}}",
      "now": "$gt(expr1, expr2) requires exactly 2 arguments, got 3",
      "reason": "shippedBug: a comparison operator with one operand at the top of a filter became {$expr:{$gt:\"$x\"}}, which the server refuses ($gt takes two operands); inside a raw query document the one-operand spelling still passes through as the query operator."
    },
    "filter\u0000$dateDiff({ startDate: $.a, endDate: $.b, unit: \"day\", startOfWeek: \"funday\" })": {
      "entry": "filter",
      "src": "$dateDiff({ startDate: $.a, endDate: $.b, unit: \"day\", startOfWeek: \"funday\" })",
      "kind": "message",
      "was": "'$dateDiff' startOfWeek must be a weekday (monday, tuesday, wednesday, thursday, friday, saturday, sunday) — got 'funday'. Did you mean 'sunday'?",
      "now": "'$dateDiff' startOfWeek must be one of: monday, tuesday, wednesday, thursday, friday, saturday, sunday, mon, tue, wed, thu, fri, sat, sun — got 'funday'. Did you mean 'sunday'?",
      "reason": "message: one wording for every closed set, from the row's enum; the weekday list now also names the three-letter forms the server accepts."
    },
    "filter\u0000$convert({ input: $.s, to: \"intt\" })": {
      "entry": "filter",
      "src": "$convert({ input: $.s, to: \"intt\" })",
      "kind": "message",
      "was": "'$convert' to must be one of: double, string, object, array, binData, objectId, bool, date, null, regex, dbPointer, javascript, symbol, javascriptWithScope, int, timestamp, long, decimal, minKey, maxKey — got 'intt'. Did",
      "now": "'$convert' to must be one of: double, string, object, array, binData, objectId, bool, date, null, regex, dbPointer, javascript, symbol, javascriptWithScope, int, timestamp, long, decimal, minKey, maxKey, undefined — got ",
      "reason": "message: one wording for every closed set, from the row's enum; the weekday list now also names the three-letter forms the server accepts."
    },
    "filter\u0000$regexMatch({ input: $.s, regex: \"a\", options: \"gi\" })": {
      "entry": "filter",
      "src": "$regexMatch({ input: $.s, regex: \"a\", options: \"gi\" })",
      "kind": "message",
      "was": "'$regexMatch' options has an invalid regex flag 'g'. MongoDB allows only i, m, x, s — a JavaScript 'g' or 'y' flag is not supported.",
      "now": "'$regexMatch' options has an invalid flag 'g'. MongoDB allows only i, m, x, s, u — a JavaScript 'g' or 'y' flag is not supported.",
      "reason": "shippedBug: the server accepts the 'u' regex option (measured); the row states imxsu and the message lists it."
    },
    "filter\u00002 ** 3 ** 2": {
      "entry": "filter",
      "src": "2 ** 3 ** 2",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$pow\":[2,{\"$pow\":[3,2]}]},null]},null]},{\"$ne\":[{\"$pow\":[2,{\"$pow\":[3,2]}]},false]},{\"$ne\":[{\"$pow\":[2,{\"$pow\":[3,2]}]},\"\"]},{\"$ne\":[{\"$pow\":[2,{\"$pow\":[3,2]}]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[512,null]},null]},{\"$ne\":[512,false]},{\"$ne\":[512,\"\"]},{\"$ne\":[512,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000$.a ? $.b : $.c": {
      "entry": "filter",
      "src": "$.a ? $.b : $.c",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$a\",null]},null]},{\"$ne\":[\"$a\",false]},{\"$ne\":[\"$a\",\"\"]},{\"$ne\":[\"$a\",0]}]},\"then\":\"$b\",\"else\":\"$c\"}},null]},null]},{\"$ne\":[{\"$cond\":{\"if",
      "now": "{\"$expr\":{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$a\",null]},null]},{\"$ne\":[\"$a\",false]},{\"$ne\":[\"$a\",\"\"]},{\"$ne\":[\"$a\",0]}]},\"then\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$b\",null]},null]},{\"$ne\":[\"$b\",false]},{\"$ne\":[\"$b\",\"",
      "reason": "equivalent: a ternary read for truth pushes the truth into its branches (cond(test, truth(a), truth(b))) where the shipped wrapped the whole $cond; the same documents on every input."
    },
    "filter\u0000$.age >= 18 ? 'adult' : 'minor'": {
      "entry": "filter",
      "src": "$.age >= 18 ? 'adult' : 'minor'",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$gte\":[\"$age\",18]},\"then\":\"adult\",\"else\":\"minor\"}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$gte\":[\"$age\",18]},\"then\":\"adult\",\"else\":\"minor\"}},false]},{\"$ne\":[{\"$cond\"",
      "now": "{\"$expr\":{\"$cond\":{\"if\":{\"$gte\":[\"$age\",18]},\"then\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"adult\",null]},null]},{\"$ne\":[\"adult\",false]},{\"$ne\":[\"adult\",\"\"]},{\"$ne\":[\"adult\",0]}]},\"else\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"minor\",null]}",
      "reason": "equivalent: a ternary read for truth pushes the truth into its branches (cond(test, truth(a), truth(b))) where the shipped wrapped the whole $cond; the same documents on every input."
    },
    "filter\u0000$.name ? \"yes\" : \"no\"": {
      "entry": "filter",
      "src": "$.name ? \"yes\" : \"no\"",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$name\",null]},null]},{\"$ne\":[\"$name\",false]},{\"$ne\":[\"$name\",\"\"]},{\"$ne\":[\"$name\",0]}]},\"then\":\"yes\",\"else\":\"no\"}},null]},null]},{\"$ne\":[",
      "now": "{\"$expr\":{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$name\",null]},null]},{\"$ne\":[\"$name\",false]},{\"$ne\":[\"$name\",\"\"]},{\"$ne\":[\"$name\",0]}]},\"then\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"yes\",null]},null]},{\"$ne\":[\"yes\",false]},",
      "reason": "equivalent: a ternary read for truth pushes the truth into its branches (cond(test, truth(a), truth(b))) where the shipped wrapped the whole $cond; the same documents on every input."
    },
    "filter\u0000$.a ? 'x' : $.b ? 'y' : 'z'": {
      "entry": "filter",
      "src": "$.a ? 'x' : $.b ? 'y' : 'z'",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$a\",null]},null]},{\"$ne\":[\"$a\",false]},{\"$ne\":[\"$a\",\"\"]},{\"$ne\":[\"$a\",0]}]},\"then\":\"x\",\"else\":{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":",
      "now": "{\"$expr\":{\"$cond\":{\"if\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$a\",null]},null]},{\"$ne\":[\"$a\",false]},{\"$ne\":[\"$a\",\"\"]},{\"$ne\":[\"$a\",0]}]},\"then\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"x\",null]},null]},{\"$ne\":[\"x\",false]},{\"$ne\":[\"x\",\"\"]}",
      "reason": "equivalent: a ternary read for truth pushes the truth into its branches (cond(test, truth(a), truth(b))) where the shipped wrapped the whole $cond; the same documents on every input."
    },
    "filter\u0000$.a > 0 || $.b > 0 || $.c > 0": {
      "entry": "filter",
      "src": "$.a > 0 || $.b > 0 || $.c > 0",
      "kind": "output",
      "was": "{\"$or\":[{\"$or\":[{\"a\":{\"$gt\":0}},{\"b\":{\"$gt\":0}}]},{\"c\":{\"$gt\":0}}]}",
      "now": "{\"$or\":[{\"a\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}},{\"b\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}},{\"c\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}]}",
      "reason": "intended: a left-nested `||` chain lowers as one flat $or; the shipped nested one $or in another — the same documents."
    },
    "filter\u0000$.items.0": {
      "entry": "filter",
      "src": "$.items.0",
      "kind": "message",
      "was": "Expected property name after '.' at position 8",
      "now": "Expected a name but got '0' at position 8",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000[1,2,3,4,5].slice(1, 3)": {
      "entry": "filter",
      "src": "[1,2,3,4,5].slice(1, 3)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$slice\":[[1,2,3,4,5],1,2]},null]},null]},{\"$ne\":[{\"$slice\":[[1,2,3,4,5],1,2]},false]},{\"$ne\":[{\"$slice\":[[1,2,3,4,5],1,2]},\"\"]},{\"$ne\":[{\"$slice\":[[1,2,3,4,5],1,2]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[2,3],null]},null]},{\"$ne\":[[2,3],false]},{\"$ne\":[[2,3],\"\"]},{\"$ne\":[[2,3],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3,4,5].slice(-2)": {
      "entry": "filter",
      "src": "[1,2,3,4,5].slice(-2)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$slice\":[[1,2,3,4,5],-2]},null]},null]},{\"$ne\":[{\"$slice\":[[1,2,3,4,5],-2]},false]},{\"$ne\":[{\"$slice\":[[1,2,3,4,5],-2]},\"\"]},{\"$ne\":[{\"$slice\":[[1,2,3,4,5],-2]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[4,5],null]},null]},{\"$ne\":[[4,5],false]},{\"$ne\":[[4,5],\"\"]},{\"$ne\":[[4,5],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3,4,5].slice(0, -1)": {
      "entry": "filter",
      "src": "[1,2,3,4,5].slice(0, -1)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlArr\":[1,2,3,4,5]},\"in\":{\"$slice\":[\"$$jsmqlArr\",{\"$max\":[{\"$subtract\":[{\"$size\":\"$$jsmqlArr\"},1]},0]}]}}},null]},null]},{\"$ne\":[{\"$let\":{\"vars\":{\"jsmqlArr\":[1,2",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[1,2,3,4],null]},null]},{\"$ne\":[[1,2,3,4],false]},{\"$ne\":[[1,2,3,4],\"\"]},{\"$ne\":[[1,2,3,4],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3,4,5].slice(1, -1)": {
      "entry": "filter",
      "src": "[1,2,3,4,5].slice(1, -1)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlArr\":[1,2,3,4,5]},\"in\":{\"$let\":{\"vars\":{\"jsmqlK\":{\"$min\":[1,{\"$size\":\"$$jsmqlArr\"}]},\"jsmqlF\":{\"$max\":[{\"$subtract\":[{\"$size\":\"$$jsmqlArr\"},1]},0]}},\"in\":{\"$co",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[2,3,4],null]},null]},{\"$ne\":[[2,3,4],false]},{\"$ne\":[[2,3,4],\"\"]},{\"$ne\":[[2,3,4],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000Boolean + 5": {
      "entry": "filter",
      "src": "Boolean + 5",
      "kind": "message",
      "was": "'Boolean' used as a value is only valid as a callback to a higher-order array method (e.g. $.items.filter(Boolean)). To coerce a single value, write Boolean(value).",
      "now": "'Boolean' used as a value is only valid as a callback to a higher-order array method (e.g. $.items.map(Boolean)). To apply it to one value, write Boolean(value).",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000new Date(\"not-a-date\")": {
      "entry": "filter",
      "src": "new Date(\"not-a-date\")",
      "kind": "message",
      "was": "new Date(\"not-a-date\") — \"not-a-date\" is not a valid date string. Use an ISO 8601 date like \"2026-01-01\" or \"2026-01-01T00:00:00.000Z\".",
      "now": "new Date(<constant>) — only an ISO 8601 string or a millisecond count is a date constant, and this one is neither a valid date string nor a number. Write new Date(\"2026-01-01\") or new Date(0).",
      "reason": "message: the row states one text for the class of constant; the shipped interpolated the offending constant."
    },
    "filter\u0000new Date(1, 2, 3, 4, 5, 6, 7, 8)": {
      "entry": "filter",
      "src": "new Date(1, 2, 3, 4, 5, 6, 7, 8)",
      "kind": "message",
      "was": "'new Date(year, month, day, hour, minute, second, ms)' takes at most 7 arguments, got 8 at position 0",
      "now": "Date(year, month, day, hour, minute, second, ms) requires 2, 3, 4, 5, 6, or 7 arguments, got 8",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000Date.UTC(2024, 1, 15)": {
      "entry": "filter",
      "src": "Date.UTC(2024, 1, 15)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$toLong\":{\"$dateFromParts\":{\"year\":2024,\"month\":1,\"day\":15,\"timezone\":\"UTC\"}}},null]},null]},{\"$ne\":[{\"$toLong\":{\"$dateFromParts\":{\"year\":2024,\"month\":1,\"day\":15,\"timezone\":\"UTC\"}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[1705276800000,null]},null]},{\"$ne\":[1705276800000,false]},{\"$ne\":[1705276800000,\"\"]},{\"$ne\":[1705276800000,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000Date.UTC(1970)": {
      "entry": "filter",
      "src": "Date.UTC(1970)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$toLong\":{\"$dateFromParts\":{\"year\":1970,\"timezone\":\"UTC\"}}},null]},null]},{\"$ne\":[{\"$toLong\":{\"$dateFromParts\":{\"year\":1970,\"timezone\":\"UTC\"}}},false]},{\"$ne\":[{\"$toLong\":{\"$dateFr",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[0,null]},null]},{\"$ne\":[0,false]},{\"$ne\":[0,\"\"]},{\"$ne\":[0,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000$.items.map(x => { const a = 1; })": {
      "entry": "filter",
      "src": "$.items.map(x => { const a = 1; })",
      "kind": "message",
      "was": "A block body must end with a `return <expr>` statement at position 32, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "now": "A block body must end with a `return <expr>` statement at position 32, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000[1, 2].map(a => a)": {
      "entry": "filter",
      "src": "[1, 2].map(a => a)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$map\":{\"input\":[1,2],\"as\":\"a\",\"in\":\"$$a\"}},null]},null]},{\"$ne\":[{\"$map\":{\"input\":[1,2],\"as\":\"a\",\"in\":\"$$a\"}},false]},{\"$ne\":[{\"$map\":{\"input\":[1,2],\"as\":\"a\",\"in\":\"$$a\"}},\"\"]},{\"$n",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[1,2],null]},null]},{\"$ne\":[[1,2],false]},{\"$ne\":[[1,2],\"\"]},{\"$ne\":[[1,2],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1, 2].join(\",\")": {
      "entry": "filter",
      "src": "[1, 2].join(\",\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":[1,2],\"initialValue\":\"\",\"in\":{\"$cond\":{\"if\":{\"$eq\":[\"$$value\",\"\"]},\"then\":{\"$toString\":\"$$this\"},\"else\":{\"$concat\":[\"$$value\",\",\",{\"$toString\":\"$$this\"}]}}}}},nul",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"1,2\",null]},null]},{\"$ne\":[\"1,2\",false]},{\"$ne\":[\"1,2\",\"\"]},{\"$ne\":[\"1,2\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000$.r = $$$.orders.sortBy((o) => { $limit(2); });": {
      "entry": "filter",
      "src": "$.r = $$$.orders.sortBy((o) => { $limit(2); });",
      "kind": "message",
      "was": "Unexpected stage call at position 33: a `=> { $stage(...); ... }` block is a sub-pipeline, but '.sortBy(...)' doesn't take one. Pipeline stages belong to `.aggregate(pipeline)` alone; every other callback's block body is",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u0000$.r = $$$.orders.aggregat((o) => { $limit(2); });": {
      "entry": "filter",
      "src": "$.r = $$$.orders.aggregat((o) => { $limit(2); });",
      "kind": "message",
      "was": "Unexpected stage call at position 35: a `=> { $stage(...); ... }` block is a sub-pipeline, but '.aggregat(...)' doesn't take one. Did you mean '.aggregate'? Pipeline stages belong to `.aggregate(pipeline)` alone; every o",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u0000$.items.map((d) => { const a = d.v; })": {
      "entry": "filter",
      "src": "$.items.map((d) => { const a = d.v; })",
      "kind": "message",
      "was": "A block body must end with a `return <expr>` statement at position 36, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "now": "A block body must end with a `return <expr>` statement at position 36, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$.items.map((d) => { d.v; })": {
      "entry": "filter",
      "src": "$.items.map((d) => { d.v; })",
      "kind": "message",
      "was": "A block body must end with a `return <expr>` statement at position 21, got an identifier 'd'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expr",
      "now": "A block body must end with a `return <expr>` statement at position 26, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$.items.map(x => { k: x })": {
      "entry": "filter",
      "src": "$.items.map(x => { k: x })",
      "kind": "message",
      "was": "A block body must end with a `return <expr>` statement at position 19, got an identifier 'k'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expr",
      "now": "A block body must end with a `return <expr>` statement at position 19, got an identifier 'k'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expr",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$toBool($.x) ? 1 : 2": {
      "entry": "filter",
      "src": "$toBool($.x) ? 1 : 2",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$toBool\":\"$x\"},\"then\":1,\"else\":2}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":{\"$toBool\":\"$x\"},\"then\":1,\"else\":2}},false]},{\"$ne\":[{\"$cond\":{\"if\":{\"$toBool\":\"$x\"},\"then\":1",
      "now": "{\"$expr\":{\"$cond\":{\"if\":{\"$toBool\":\"$x\"},\"then\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[1,null]},null]},{\"$ne\":[1,false]},{\"$ne\":[1,\"\"]},{\"$ne\":[1,0]}]},\"else\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[2,null]},null]},{\"$ne\":[2,false]},{\"$ne\":[",
      "reason": "equivalent: a ternary read for truth pushes the truth into its branches (cond(test, truth(a), truth(b))) where the shipped wrapped the whole $cond; the same documents on every input."
    },
    "filter\u0000$.s.trim().unzipWith((a, b) => a)": {
      "entry": "filter",
      "src": "$.s.trim().unzipWith((a, b) => a)",
      "kind": "message",
      "was": "'.unzipWith(...)' expects an array receiver, but '.trim(...)' returns a string. Call '.unzipWith(...)' on an array value instead.",
      "now": ".unzipWith(fn) isn't supported — its iteratee's argument count depends on the array's length at runtime. Write '.unzip().map(group => …)' instead, where 'group' is one unzipped column.",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$.s.trim().forEach(x => x)": {
      "entry": "filter",
      "src": "$.s.trim().forEach(x => x)",
      "kind": "message",
      "was": "'.forEach(...)' expects an array receiver, but '.trim(...)' returns a string. Call '.forEach(...)' on an array value instead.",
      "now": ".forEach() returns undefined in JavaScript; jsmql expressions must produce a value. Use '.map(...)' to transform, or move side-effecting work outside the query.",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000\"-\".repeat(5)": {
      "entry": "filter",
      "src": "\"-\".repeat(5)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":{\"$range\":[0,5]},\"initialValue\":\"\",\"in\":{\"$concat\":[\"$$value\",\"-\"]}}},null]},null]},{\"$ne\":[{\"$reduce\":{\"input\":{\"$range\":[0,5]},\"initialValue\":\"\",\"in\":{\"$concat\"",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"-----\",null]},null]},{\"$ne\":[\"-----\",false]},{\"$ne\":[\"-----\",\"\"]},{\"$ne\":[\"-----\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000Array.from($.iter)": {
      "entry": "filter",
      "src": "Array.from($.iter)",
      "kind": "message",
      "was": "Array.from() only supports the {length: n} form: Array.from({length: n}, (_, i) => …). For other inputs use $op($range, …) or .map().",
      "now": "Only 'Array.from({ length: n })' is supported. To turn an iterable into an array, write the array literal or '.map(...)' on it.",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u00001.5n": {
      "entry": "filter",
      "src": "1.5n",
      "kind": "message",
      "was": "Invalid BigInt literal at position 0: 'n' suffix requires an integer (no fraction or exponent)",
      "now": "Invalid BigInt literal: the 'n' suffix requires an integer at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u00001e2n": {
      "entry": "filter",
      "src": "1e2n",
      "kind": "message",
      "was": "Invalid BigInt literal at position 0: 'n' suffix requires an integer (no fraction or exponent)",
      "now": "Invalid BigInt literal: the 'n' suffix requires an integer at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000((x) => x + 1)(5)": {
      "entry": "filter",
      "src": "((x) => x + 1)(5)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"x\":5},\"in\":{\"$add\":[\"$$x\",1]}}},null]},null]},{\"$ne\":[{\"$let\":{\"vars\":{\"x\":5},\"in\":{\"$add\":[\"$$x\",1]}}},false]},{\"$ne\":[{\"$let\":{\"vars\":{\"x\":5},\"in\":{\"$add\":[\"$$x\",",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[6,null]},null]},{\"$ne\":[6,false]},{\"$ne\":[6,\"\"]},{\"$ne\":[6,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000$.name.frobulate()": {
      "entry": "filter",
      "src": "$.name.frobulate()",
      "kind": "message",
      "was": "Unknown method '.frobulate()'.",
      "now": "Unknown method '.frobulate()' at position 6.",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "filter\u0000$.name.toLowerCse()": {
      "entry": "filter",
      "src": "$.name.toLowerCse()",
      "kind": "message",
      "was": "Unknown method '.toLowerCse()'. Did you mean '.toLowerCase()'?",
      "now": "Unknown method '.toLowerCse()' at position 6. Did you mean '.toLowerCase()'?",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "filter\u0000$.items.fliter(x => x)": {
      "entry": "filter",
      "src": "$.items.fliter(x => x)",
      "kind": "message",
      "was": "Unknown method '.fliter()'. Did you mean '.filter()'?",
      "now": "Unknown method '.fliter()' at position 7. Did you mean '.filter()'?",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "filter\u0000$.name.substing(1)": {
      "entry": "filter",
      "src": "$.name.substing(1)",
      "kind": "message",
      "was": "Unknown method '.substing()'. Did you mean '.substring()'?",
      "now": "Unknown method '.substing()' at position 6. Did you mean '.substring()'?",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "filter\u0000Math.flor($.x)": {
      "entry": "filter",
      "src": "Math.flor($.x)",
      "kind": "message",
      "was": "Unknown Math member 'flor' at position 5. Did you mean 'Math.floor'? See docs/LANGUAGE.md for the full list of supported Math methods and constants.",
      "now": "Unknown method 'Math.flor()' at position 4. Did you mean 'Math.floor'?",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "filter\u0000Number.isItneger($.x)": {
      "entry": "filter",
      "src": "Number.isItneger($.x)",
      "kind": "message",
      "was": "Unknown Number static method 'isItneger' at position 7. Did you mean 'Number.isInteger'? Supported: Number.isInteger, Number.isNaN, Number.isFinite.",
      "now": "Unknown method 'Number.isItneger()' at position 6. Did you mean 'Number.isInteger'?",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "filter\u0000Object.keyz($.o)": {
      "entry": "filter",
      "src": "Object.keyz($.o)",
      "kind": "message",
      "was": "Unknown Object method 'keyz' at position 7. Did you mean 'Object.keys'? Supported: keys, values, entries, assign, fromEntries, groupBy.",
      "now": "Unknown method 'Object.keyz()' at position 6. Did you mean 'Object.keys'?",
      "reason": "message: a position-bearing error says `at position N` (CLAUDE.md), and a namespace suggestion is spelled as the namespace; the suggestion itself is the same."
    },
    "filter\u0000$.s.trim() = 1": {
      "entry": "filter",
      "src": "$.s.trim() = 1",
      "kind": "message",
      "was": "Cannot assign to a method-call result ('.trim()') at position 11 — only field paths like '$.x' or '$.x.y' are assignable.",
      "now": "Unexpected token '=' at position 11",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u000042 = 1": {
      "entry": "filter",
      "src": "42 = 1",
      "kind": "message",
      "was": "Cannot assign to a literal value at position 3 — only field paths like '$.x' or '$.x.y' are assignable.",
      "now": "Unexpected token '=' at position 3",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u0000\"hello\".slice(1, 3)": {
      "entry": "filter",
      "src": "\"hello\".slice(1, 3)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$substrCP\":[\"hello\",1,2]},null]},null]},{\"$ne\":[{\"$substrCP\":[\"hello\",1,2]},false]},{\"$ne\":[{\"$substrCP\":[\"hello\",1,2]},\"\"]},{\"$ne\":[{\"$substrCP\":[\"hello\",1,2]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"el\",null]},null]},{\"$ne\":[\"el\",false]},{\"$ne\":[\"el\",\"\"]},{\"$ne\":[\"el\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000\"hello\".slice(2)": {
      "entry": "filter",
      "src": "\"hello\".slice(2)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$substrCP\":[\"hello\",2,3]},null]},null]},{\"$ne\":[{\"$substrCP\":[\"hello\",2,3]},false]},{\"$ne\":[{\"$substrCP\":[\"hello\",2,3]},\"\"]},{\"$ne\":[{\"$substrCP\":[\"hello\",2,3]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"llo\",null]},null]},{\"$ne\":[\"llo\",false]},{\"$ne\":[\"llo\",\"\"]},{\"$ne\":[\"llo\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000\"hello\".slice(-3)": {
      "entry": "filter",
      "src": "\"hello\".slice(-3)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$substrCP\":[\"hello\",2,3]},null]},null]},{\"$ne\":[{\"$substrCP\":[\"hello\",2,3]},false]},{\"$ne\":[{\"$substrCP\":[\"hello\",2,3]},\"\"]},{\"$ne\":[{\"$substrCP\":[\"hello\",2,3]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"llo\",null]},null]},{\"$ne\":[\"llo\",false]},{\"$ne\":[\"llo\",\"\"]},{\"$ne\":[\"llo\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000\"hello\".slice(1, -1)": {
      "entry": "filter",
      "src": "\"hello\".slice(1, -1)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$substrCP\":[\"hello\",1,3]},null]},null]},{\"$ne\":[{\"$substrCP\":[\"hello\",1,3]},false]},{\"$ne\":[{\"$substrCP\":[\"hello\",1,3]},\"\"]},{\"$ne\":[{\"$substrCP\":[\"hello\",1,3]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"ell\",null]},null]},{\"$ne\":[\"ell\",false]},{\"$ne\":[\"ell\",\"\"]},{\"$ne\":[\"ell\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000$.a &&": {
      "entry": "filter",
      "src": "$.a &&",
      "kind": "message",
      "was": "Unexpected end of expression",
      "now": "Unexpected end of input at position 6",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$.a ? $.b": {
      "entry": "filter",
      "src": "$.a ? $.b",
      "kind": "message",
      "was": "Expected ':' in ternary expression at position 9",
      "now": "Expected ':' but got end of input at position 9",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000Object.fromEntries([[\"a\", 1], [\"b\", 2]])": {
      "entry": "filter",
      "src": "Object.fromEntries([[\"a\", 1], [\"b\", 2]])",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$arrayToObject\":[[\"a\",1],[\"b\",2]]},null]},null]},{\"$ne\":[{\"$arrayToObject\":[[\"a\",1],[\"b\",2]]},false]},{\"$ne\":[{\"$arrayToObject\":[[\"a\",1],[\"b\",2]]},\"\"]},{\"$ne\":[{\"$arrayToObject\":[[",
      "now": "{\"a\":1,\"b\":2}",
      "reason": "shippedBug: the shipped emitted {$arrayToObject:[[\"a\",1],[\"b\",2]]} inside its truthiness check, which the server refuses as two arguments (measured); the fold settles the constant to the raw document {a:1,b:2}."
    },
    "filter\u0000({...$.user?.profile, name: 'x'})": {
      "entry": "filter",
      "src": "({...$.user?.profile, name: 'x'})",
      "kind": "now-rejected",
      "was": "{\"$mergeObjects\":[\"$user.profile\",{\"name\":\"x\"}]}",
      "now": "Spread elements in objects are not supported in MQL output",
      "reason": "shippedBug: a spread in a filter's raw document emitted a top-level {$mergeObjects:[…]}, which is not a query document the server accepts; the spread is refused as it is in an operator body."
    },
    "filter\u0000[1, 2].concat([3, 4])": {
      "entry": "filter",
      "src": "[1, 2].concat([3, 4])",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$concatArrays\":[[1,2],[3,4]]},null]},null]},{\"$ne\":[{\"$concatArrays\":[[1,2],[3,4]]},false]},{\"$ne\":[{\"$concatArrays\":[[1,2],[3,4]]},\"\"]},{\"$ne\":[{\"$concatArrays\":[[1,2],[3,4]]},0]}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[1,2,3,4],null]},null]},{\"$ne\":[[1,2,3,4],false]},{\"$ne\":[[1,2,3,4],\"\"]},{\"$ne\":[[1,2,3,4],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u00001_": {
      "entry": "filter",
      "src": "1_",
      "kind": "message",
      "was": "Numeric separator '_' must be between two digits (at position 1)",
      "now": "Numeric separator '_' must be between two digits at position 1",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u00001__0": {
      "entry": "filter",
      "src": "1__0",
      "kind": "message",
      "was": "Numeric separator '_' must be between two digits (at position 1)",
      "now": "Numeric separator '_' must be between two digits at position 1",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u000010 /* skip */ / 2": {
      "entry": "filter",
      "src": "10 /* skip */ / 2",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$divide\":[10,2]},null]},null]},{\"$ne\":[{\"$divide\":[10,2]},false]},{\"$ne\":[{\"$divide\":[10,2]},\"\"]},{\"$ne\":[{\"$divide\":[10,2]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[5,null]},null]},{\"$ne\":[5,false]},{\"$ne\":[5,\"\"]},{\"$ne\":[5,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000$.a /* unclosed": {
      "entry": "filter",
      "src": "$.a /* unclosed",
      "kind": "message",
      "was": "Unclosed block comment starting at position 4",
      "now": "Unterminated block comment at position 4",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$min()": {
      "entry": "filter",
      "src": "$min()",
      "kind": "message",
      "was": "Operator $min expects at least 1 argument",
      "now": "$min(operands) requires at least 1 argument, got 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$accumulator({ init: \"function() {}\", accumulate: \"function() {}\", merge: \"function() {}\", lang: \"js\" })": {
      "entry": "filter",
      "src": "$accumulator({ init: \"function() {}\", accumulate: \"function() {}\", merge: \"function() {}\", lang: \"js\" })",
      "kind": "message",
      "was": "$accumulator is an accumulator operator — only valid inside '$group' field-value slots or '$setWindowFields' output slots. Use $group({ _id: ..., <key>: $accumulator(...) }) to compute it per-group, or $setWindowFields(.",
      "now": "$accumulator is an accumulator operator, not a filter predicate — use it inside '$group' field-value slots or '$setWindowFields' output slots.",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$median($.scores, \"approximate\")": {
      "entry": "filter",
      "src": "$median($.scores, \"approximate\")",
      "kind": "now-accepted",
      "was": "$median is an accumulator operator — only valid inside '$group' field-value slots or '$setWindowFields' output slots. Use $group({ _id: ..., <key>: $median(...) }) to compute it per-group, or $setWindowFields(...) for th",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$median\":{\"input\":\"$scores\",\"method\":\"approximate\"}},null]},null]},{\"$ne\":[{\"$median\":{\"input\":\"$scores\",\"method\":\"approximate\"}},false]},{\"$ne\":[{\"$median\":{\"input\":\"$scores\",\"met",
      "reason": "shippedBug: $median is an aggregation expression since MongoDB 7.0 (measured), so as a predicate it takes the $expr fallback."
    },
    "filter\u0000$encStrContains($.encField, \"secret\")": {
      "entry": "filter",
      "src": "$encStrContains($.encField, \"secret\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$encStrContains\":{\"input\":\"$encField\",\"substring\":\"secret\"}},null]},null]},{\"$ne\":[{\"$encStrContains\":{\"input\":\"$encField\",\"substring\":\"secret\"}},false]},{\"$ne\":[{\"$encStrContains\"",
      "now": "{\"$expr\":{\"$encStrContains\":{\"input\":\"$encField\",\"substring\":\"secret\"}}}",
      "reason": "intended: the operator's row states `returns: \"bool\"` (measured on mongod), so the filter's $expr takes it as it is; the shipped wrapped it in the truthiness check."
    },
    "filter\u0000$encStrStartsWith({ input: $.encField, prefix: \"abc\" })": {
      "entry": "filter",
      "src": "$encStrStartsWith({ input: $.encField, prefix: \"abc\" })",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$encStrStartsWith\":{\"input\":\"$encField\",\"prefix\":\"abc\"}},null]},null]},{\"$ne\":[{\"$encStrStartsWith\":{\"input\":\"$encField\",\"prefix\":\"abc\"}},false]},{\"$ne\":[{\"$encStrStartsWith\":{\"inp",
      "now": "{\"$expr\":{\"$encStrStartsWith\":{\"input\":\"$encField\",\"prefix\":\"abc\"}}}",
      "reason": "intended: the operator's row states `returns: \"bool\"` (measured on mongod), so the filter's $expr takes it as it is; the shipped wrapped it in the truthiness check."
    },
    "filter\u0000$encStrEndsWith($.encField, \"xyz\")": {
      "entry": "filter",
      "src": "$encStrEndsWith($.encField, \"xyz\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$encStrEndsWith\":{\"input\":\"$encField\",\"suffix\":\"xyz\"}},null]},null]},{\"$ne\":[{\"$encStrEndsWith\":{\"input\":\"$encField\",\"suffix\":\"xyz\"}},false]},{\"$ne\":[{\"$encStrEndsWith\":{\"input\":\"$",
      "now": "{\"$expr\":{\"$encStrEndsWith\":{\"input\":\"$encField\",\"suffix\":\"xyz\"}}}",
      "reason": "intended: the operator's row states `returns: \"bool\"` (measured on mongod), so the filter's $expr takes it as it is; the shipped wrapped it in the truthiness check."
    },
    "filter\u0000$encStrNormalizedEq($.encField, \"compare\")": {
      "entry": "filter",
      "src": "$encStrNormalizedEq($.encField, \"compare\")",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$encStrNormalizedEq\":{\"input\":\"$encField\",\"string\":\"compare\"}},null]},null]},{\"$ne\":[{\"$encStrNormalizedEq\":{\"input\":\"$encField\",\"string\":\"compare\"}},false]},{\"$ne\":[{\"$encStrNorma",
      "now": "{\"$expr\":{\"$encStrNormalizedEq\":{\"input\":\"$encField\",\"string\":\"compare\"}}}",
      "reason": "intended: the operator's row states `returns: \"bool\"` (measured on mongod), so the filter's $expr takes it as it is; the shipped wrapped it in the truthiness check."
    },
    "filter\u0000$rank()": {
      "entry": "filter",
      "src": "$rank()",
      "kind": "message",
      "was": "$rank is a window operator — only valid inside '$setWindowFields' output slots. Use $setWindowFields({ partitionBy: ..., sortBy: ..., output: { <key>: $rank(...) } }) to compute it per-document over a window.",
      "now": "$rank is a window operator, not a filter predicate — use it inside '$setWindowFields' output slots: $setWindowFields({ partitionBy: ..., sortBy: ..., output: { <key>: $rank(...) } }).",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$$foo": {
      "entry": "filter",
      "src": "$$foo",
      "kind": "message",
      "was": "Expected '.<name>' or '[<expr>]' after '$$' at position 0",
      "now": "Unexpected token 'foo' at position 2",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000Math.max(1, 2,)": {
      "entry": "filter",
      "src": "Math.max(1, 2,)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$max\":[1,2]},null]},null]},{\"$ne\":[{\"$max\":[1,2]},false]},{\"$ne\":[{\"$max\":[1,2]},\"\"]},{\"$ne\":[{\"$max\":[1,2]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[2,null]},null]},{\"$ne\":[2,false]},{\"$ne\":[2,\"\"]},{\"$ne\":[2,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000Math.max(1, 2)": {
      "entry": "filter",
      "src": "Math.max(1, 2)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$max\":[1,2]},null]},null]},{\"$ne\":[{\"$max\":[1,2]},false]},{\"$ne\":[{\"$max\":[1,2]},\"\"]},{\"$ne\":[{\"$max\":[1,2]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[2,null]},null]},{\"$ne\":[2,false]},{\"$ne\":[2,\"\"]},{\"$ne\":[2,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000Number($.x, $.y)": {
      "entry": "filter",
      "src": "Number($.x, $.y)",
      "kind": "message",
      "was": "Type cast 'Number()' takes exactly 1 argument at position 0",
      "now": "'Number(x)' takes exactly one value.",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000Date.UTC(2020, 1,)": {
      "entry": "filter",
      "src": "Date.UTC(2020, 1,)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$toLong\":{\"$dateFromParts\":{\"year\":2020,\"month\":1,\"timezone\":\"UTC\"}}},null]},null]},{\"$ne\":[{\"$toLong\":{\"$dateFromParts\":{\"year\":2020,\"month\":1,\"timezone\":\"UTC\"}}},false]},{\"$ne\":[",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[1577836800000,null]},null]},{\"$ne\":[1577836800000,false]},{\"$ne\":[1577836800000,\"\"]},{\"$ne\":[1577836800000,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000Date.UTC(2020, 1)": {
      "entry": "filter",
      "src": "Date.UTC(2020, 1)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$toLong\":{\"$dateFromParts\":{\"year\":2020,\"month\":1,\"timezone\":\"UTC\"}}},null]},null]},{\"$ne\":[{\"$toLong\":{\"$dateFromParts\":{\"year\":2020,\"month\":1,\"timezone\":\"UTC\"}}},false]},{\"$ne\":[",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[1577836800000,null]},null]},{\"$ne\":[1577836800000,false]},{\"$ne\":[1577836800000,\"\"]},{\"$ne\":[1577836800000,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000$._id === ObjectId(\"507f1f77bcf86cd79943901\")": {
      "entry": "filter",
      "src": "$._id === ObjectId(\"507f1f77bcf86cd79943901\")",
      "kind": "message",
      "was": "ObjectId(\"507f1f77bcf86cd79943901\") is not a valid ObjectId: expected exactly 24 hexadecimal characters, got 23 characters, at position 19.",
      "now": "'ObjectId(<constant>)' — this constant is not 24 hex characters. Write '0x507f1f77bcf86cd799439011' (or 'ObjectId(\"507f1f77bcf86cd799439011\")').",
      "reason": "message: the row states one text for the class of constant; the shipped interpolated the offending constant."
    },
    "filter\u0000$._id === ObjectId(\"zzzf1f77bcf86cd799439011\")": {
      "entry": "filter",
      "src": "$._id === ObjectId(\"zzzf1f77bcf86cd799439011\")",
      "kind": "message",
      "was": "ObjectId(\"zzzf1f77bcf86cd799439011\") is not a valid ObjectId: expected exactly 24 hexadecimal characters, it contains non-hexadecimal characters, at position 19.",
      "now": "'ObjectId(<constant>)' — this constant is not 24 hex characters. Write '0x507f1f77bcf86cd799439011' (or 'ObjectId(\"507f1f77bcf86cd799439011\")').",
      "reason": "message: the row states one text for the class of constant; the shipped interpolated the offending constant."
    },
    "filter\u0000$._id === ObjectId(\"nothex\")": {
      "entry": "filter",
      "src": "$._id === ObjectId(\"nothex\")",
      "kind": "message",
      "was": "ObjectId(\"nothex\") is not a valid ObjectId: expected exactly 24 hexadecimal characters, got 6 characters, at position 19.",
      "now": "'ObjectId(<constant>)' — this constant is not 24 hex characters. Write '0x507f1f77bcf86cd799439011' (or 'ObjectId(\"507f1f77bcf86cd799439011\")').",
      "reason": "message: the row states one text for the class of constant; the shipped interpolated the offending constant."
    },
    "filter\u00000x000000000000000000000001": {
      "entry": "filter",
      "src": "0x000000000000000000000001",
      "kind": "message",
      "was": "ObjectId 000000000000000000000001 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 000000000000000000000001 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000{ _id: 0x123456781234567812345678 }": {
      "entry": "filter",
      "src": "{ _id: 0x123456781234567812345678 }",
      "kind": "message",
      "was": "ObjectId 123456781234567812345678 looks like a typo: its embedded timestamp decodes to 1979-09-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 123456781234567812345678 looks like a typo: its embedded timestamp decodes to 1979-09-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u00000x000000000000000000000000": {
      "entry": "filter",
      "src": "0x000000000000000000000000",
      "kind": "message",
      "was": "ObjectId 000000000000000000000000 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 000000000000000000000000 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u00000x49ffffffffffffffffffffff": {
      "entry": "filter",
      "src": "0x49ffffffffffffffffffffff",
      "kind": "message",
      "was": "ObjectId 49ffffffffffffffffffffff looks like a typo: its embedded timestamp decodes to 2009-05-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 49ffffffffffffffffffffff looks like a typo: its embedded timestamp decodes to 2009-05-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$._id === 0x000000000000000000000001": {
      "entry": "filter",
      "src": "$._id === 0x000000000000000000000001",
      "kind": "message",
      "was": "ObjectId 000000000000000000000001 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 000000000000000000000001 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000((_) => 1)(2)": {
      "entry": "filter",
      "src": "((_) => 1)(2)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"v_\":2},\"in\":1}},null]},null]},{\"$ne\":[{\"$let\":{\"vars\":{\"v_\":2},\"in\":1}},false]},{\"$ne\":[{\"$let\":{\"vars\":{\"v_\":2},\"in\":1}},\"\"]},{\"$ne\":[{\"$let\":{\"vars\":{\"v_\":2},\"in\"",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[1,null]},null]},{\"$ne\":[1,false]},{\"$ne\":[1,\"\"]},{\"$ne\":[1,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000$ = { a: $.xs.map(x => { const g = z => z + 1; return g(x); }) };": {
      "entry": "filter",
      "src": "$ = { a: $.xs.map(x => { const g = z => z + 1; return g(x); }) };",
      "kind": "message",
      "was": "Reusable functions must be declared at the top level of a pipeline, not inside an arrow body. Move `const g = (…) => …` out of the `=> { … }` block.",
      "now": "Unexpected token '=' at position 2",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000((x) => x * 2)(5)": {
      "entry": "filter",
      "src": "((x) => x * 2)(5)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"x\":5},\"in\":{\"$multiply\":[\"$$x\",2]}}},null]},null]},{\"$ne\":[{\"$let\":{\"vars\":{\"x\":5},\"in\":{\"$multiply\":[\"$$x\",2]}}},false]},{\"$ne\":[{\"$let\":{\"vars\":{\"x\":5},\"in\":{\"$mu",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[10,null]},null]},{\"$ne\":[10,false]},{\"$ne\":[10,\"\"]},{\"$ne\":[10,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000(function (x) { return x * 2 })(5)": {
      "entry": "filter",
      "src": "(function (x) { return x * 2 })(5)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"x\":5},\"in\":{\"$multiply\":[\"$$x\",2]}}},null]},null]},{\"$ne\":[{\"$let\":{\"vars\":{\"x\":5},\"in\":{\"$multiply\":[\"$$x\",2]}}},false]},{\"$ne\":[{\"$let\":{\"vars\":{\"x\":5},\"in\":{\"$mu",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[10,null]},null]},{\"$ne\":[10,false]},{\"$ne\":[10,\"\"]},{\"$ne\":[10,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000function* f(x) { return x } $ = { a: f($.n) }": {
      "entry": "filter",
      "src": "function* f(x) { return x } $ = { a: f($.n) }",
      "kind": "message",
      "was": "jsmql does not support generator functions (`function*`). Use a plain `function (…) { return … }` or an arrow.",
      "now": "Expected '(' but got '*' at position 8",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000[1, let x = 5]": {
      "entry": "filter",
      "src": "[1, let x = 5]",
      "kind": "message",
      "was": "`let` is a pipeline statement, not a value, and is only valid as a pipeline-array element. If this array is meant to be a pipeline, ensure its first element is a stage like `$match(...)`.",
      "now": "`let` is a statement, not a value. It is only valid at the top level or as a pipeline-array element.",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000let = 5;": {
      "entry": "filter",
      "src": "let = 5;",
      "kind": "message",
      "was": "Expected an identifier after `let` at position 4, got '='",
      "now": "Unexpected token 'let' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000let x 5;": {
      "entry": "filter",
      "src": "let x 5;",
      "kind": "message",
      "was": "Expected '=' after `let x` at position 6, got '5'. `let` requires an initialiser — write `let x = <expr>`.",
      "now": "Unexpected token 'let' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000let x = ; $match($.a > 0)": {
      "entry": "filter",
      "src": "let x = ; $match($.a > 0)",
      "kind": "message",
      "was": "Unexpected token ';' at position 8",
      "now": "Unexpected token 'let' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000let in = 5;": {
      "entry": "filter",
      "src": "let in = 5;",
      "kind": "message",
      "was": "Expected an identifier after `let` at position 4, got 'in'",
      "now": "Unexpected token 'let' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000let $foo = 5;": {
      "entry": "filter",
      "src": "let $foo = 5;",
      "kind": "message",
      "was": "Expected an identifier after `let` at position 4, got '$'",
      "now": "Unexpected token 'let' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000const = 5;": {
      "entry": "filter",
      "src": "const = 5;",
      "kind": "message",
      "was": "Expected an identifier after `const` at position 6, got '='",
      "now": "Unexpected token 'const' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000const x 5;": {
      "entry": "filter",
      "src": "const x 5;",
      "kind": "message",
      "was": "Expected '=' after `const x` at position 8, got '5'. `const` requires an initialiser — write `const x = <expr>`.",
      "now": "Unexpected token 'const' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$.x = $$$.orders.map(o => { $sort({ x: -1 }); return o.total; });": {
      "entry": "filter",
      "src": "$.x = $$$.orders.map(o => { $sort({ x: -1 }); return o.total; });",
      "kind": "message",
      "was": "jsmql.filter() does not allow lookup syntax ('$$$.<coll>.<method>(...)') — joins are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() for cross-collection queries.",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u0000$.x = $$$.orders.filter(o => o.uid === $._id).find(o => { $match(o.c); });": {
      "entry": "filter",
      "src": "$.x = $$$.orders.filter(o => o.uid === $._id).find(o => { $match(o.c); });",
      "kind": "message",
      "was": "jsmql.filter() does not allow lookup syntax ('$$$.<coll>.<method>(...)') — joins are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() for cross-collection queries.",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u0000$.x = $$$.c.aggregate((o) => { $sort({ a: 1 }); return o.v; });": {
      "entry": "filter",
      "src": "$.x = $$$.c.aggregate((o) => { $sort({ a: 1 }); return o.v; });",
      "kind": "message",
      "was": "jsmql.filter() does not allow lookup syntax ('$$$.<coll>.<method>(...)') — joins are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() for cross-collection queries.",
      "now": "Unexpected token '=' at position 4",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u0000[1, 2].head().round()": {
      "entry": "filter",
      "src": "[1, 2].head().round()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$round\":[{\"$first\":[[1,2]]},0]},null]},null]},{\"$ne\":[{\"$round\":[{\"$first\":[[1,2]]},0]},false]},{\"$ne\":[{\"$round\":[{\"$first\":[[1,2]]},0]},\"\"]},{\"$ne\":[{\"$round\":[{\"$first\":[[1,2]]}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[1,null]},null]},{\"$ne\":[1,false]},{\"$ne\":[1,\"\"]},{\"$ne\":[1,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[[1, 2], [3]].head().toReversed()": {
      "entry": "filter",
      "src": "[[1, 2], [3]].head().toReversed()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reverseArray\":{\"$first\":[[[1,2],[3]]]}},null]},null]},{\"$ne\":[{\"$reverseArray\":{\"$first\":[[[1,2],[3]]]}},false]},{\"$ne\":[{\"$reverseArray\":{\"$first\":[[[1,2],[3]]]}},\"\"]},{\"$ne\":[{\"",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[2,1],null]},null]},{\"$ne\":[[2,1],false]},{\"$ne\":[[2,1],\"\"]},{\"$ne\":[[2,1],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[{ $macth: $.age > 18 }]": {
      "entry": "filter",
      "src": "[{ $macth: $.age > 18 }]",
      "kind": "message",
      "was": "jsmql.filter() expects a Filter, but received a Pipeline array (`[{ $stage: ... }, ...]`). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "now": "Element 0 of pipeline: '$macth' is not a known aggregation stage. Did you mean '$match'?",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u0000[$prject({ name: 1 })]": {
      "entry": "filter",
      "src": "[$prject({ name: 1 })]",
      "kind": "message",
      "was": "jsmql.filter() expects a Filter, but received a Pipeline array (`[{ $stage: ... }, ...]`). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "now": "Element 0 of pipeline: '$prject' is not a known aggregation stage. Did you mean '$project'?",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u0000[{ $match: { age: 1 }, $sort: { age: 1 } }]": {
      "entry": "filter",
      "src": "[{ $match: { age: 1 }, $sort: { age: 1 } }]",
      "kind": "message",
      "was": "jsmql.filter() expects a Filter, but received a Pipeline array (`[{ $stage: ... }, ...]`). Call jsmql.pipeline() or jsmql() for Pipeline output.",
      "now": "Element 0 of pipeline must be a single-key stage object (e.g. `{ $match: ... }`), but found an object with 2 keys.",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u0000$ = { topByScore: $$.filter(o => { $sort({ score: -1 }); $limit(10); }) };": {
      "entry": "filter",
      "src": "$ = { topByScore: $$.filter(o => { $sort({ score: -1 }); $limit(10); }) };",
      "kind": "message",
      "was": "jsmql.filter() expects a Filter (the document `db.coll.find(filter)` takes), but received a `;`-separated Pipeline. Drop the `;` to compose a Filter, or call jsmql.pipeline() / jsmql() for Pipeline output.",
      "now": "Unexpected token '=' at position 2",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$.stock >= $.reorderPoint ? \"ok\" : $.stock > 0 ? \"low\" : \"out-of-stock\"": {
      "entry": "filter",
      "src": "$.stock >= $.reorderPoint ? \"ok\" : $.stock > 0 ? \"low\" : \"out-of-stock\"",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$gte\":[\"$stock\",\"$reorderPoint\"]},\"then\":\"ok\",\"else\":{\"$cond\":{\"if\":{\"$gt\":[\"$stock\",0]},\"then\":\"low\",\"else\":\"out-of-stock\"}}}},null]},null]},{\"$ne\":[{\"$cond\":{\"if\":",
      "now": "{\"$expr\":{\"$cond\":{\"if\":{\"$gte\":[\"$stock\",\"$reorderPoint\"]},\"then\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"ok\",null]},null]},{\"$ne\":[\"ok\",false]},{\"$ne\":[\"ok\",\"\"]},{\"$ne\":[\"ok\",0]}]},\"else\":{\"$cond\":{\"if\":{\"$gt\":[\"$stock\",0]},\"the",
      "reason": "equivalent: a ternary read for truth pushes the truth into its branches (cond(test, truth(a), truth(b))) where the shipped wrapped the whole $cond; the same documents on every input."
    },
    "filter\u0000\n$.quantity > 1 && $.price >= 10 && $.category in [\"sale\", \"clearance\"]\n  ? $.price * 0.8\n  : $.price\n      ": {
      "entry": "filter",
      "src": "\n$.quantity > 1 && $.price >= 10 && $.category in [\"sale\", \"clearance\"]\n  ? $.price * 0.8\n  : $.price\n      ",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$cond\":{\"if\":{\"$and\":[{\"$gt\":[\"$quantity\",1]},{\"$gte\":[\"$price\",10]},{\"$in\":[\"$category\",[\"sale\",\"clearance\"]]}]},\"then\":{\"$multiply\":[\"$price\",0.8]},\"else\":\"$price\"}},null]},null]",
      "now": "{\"$expr\":{\"$cond\":{\"if\":{\"$and\":[{\"$gt\":[\"$quantity\",1]},{\"$gte\":[\"$price\",10]},{\"$in\":[\"$category\",[\"sale\",\"clearance\"]]}]},\"then\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$multiply\":[\"$price\",0.8]},null]},null]},{\"$ne\":[{\"$multi",
      "reason": "equivalent: a ternary read for truth pushes the truth into its branches (cond(test, truth(a), truth(b))) where the shipped wrapped the whole $cond; the same documents on every input."
    },
    "filter\u0000$$ = $$$.orders.filter(o => o.userId === $._id).map(o => {\n          assert(o.total > 0, \"bad order\");\n          return { id: o._id, t: o.total };\n        });": {
      "entry": "filter",
      "src": "$$ = $$$.orders.filter(o => o.userId === $._id).map(o => {\n          assert(o.total > 0, \"bad order\");\n          return { id: o._id, t: o.total };\n        });",
      "kind": "message",
      "was": "jsmql.filter() does not allow lookup syntax ('$$$.<coll>.<method>(...)') — joins are Pipeline-only. Use jsmql() (in Pipeline mode) or jsmql.pipeline() for cross-collection queries.",
      "now": "Unexpected token '=' at position 3",
      "reason": "statement slice: a statement program is refused by the expression parser; the statement target words these."
    },
    "filter\u0000$$ = $$.map(d => { $match(d.active === true); return { id: d._id }; });": {
      "entry": "filter",
      "src": "$$ = $$.map(d => { $match(d.active === true); return { id: d._id }; });",
      "kind": "message",
      "was": "jsmql.filter() expects a Filter (the document `db.coll.find(filter)` takes), but received a `;`-separated Pipeline. Drop the `;` to compose a Filter, or call jsmql.pipeline() / jsmql() for Pipeline output.",
      "now": "Unexpected token '=' at position 3",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$$ = $$.map((d, _i, coll) => { assert(coll.length > 0, \"empty\"); return { id: d._id }; });": {
      "entry": "filter",
      "src": "$$ = $$.map((d, _i, coll) => { assert(coll.length > 0, \"empty\"); return { id: d._id }; });",
      "kind": "message",
      "was": "jsmql.filter() expects a Filter (the document `db.coll.find(filter)` takes), but received a `;`-separated Pipeline. Drop the `;` to compose a Filter, or call jsmql.pipeline() / jsmql() for Pipeline output.",
      "now": "Unexpected token '=' at position 3",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$$ = $$.map(d => { const t = d.total; });": {
      "entry": "filter",
      "src": "$$ = $$.map(d => { const t = d.total; });",
      "kind": "message",
      "was": "jsmql.filter() expects a Filter (the document `db.coll.find(filter)` takes), but received a `;`-separated Pipeline. Drop the `;` to compose a Filter, or call jsmql.pipeline() / jsmql() for Pipeline output.",
      "now": "Unexpected token '=' at position 3",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u00001 + $.x++": {
      "entry": "filter",
      "src": "1 + $.x++",
      "kind": "message",
      "was": "Cannot assign to a '+' expression at position 7 — only field paths like '$.x' or '$.x.y' are assignable.",
      "now": "Unexpected token '++' at position 7",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u00005 - -3": {
      "entry": "filter",
      "src": "5 - -3",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$subtract\":[5,-3]},null]},null]},{\"$ne\":[{\"$subtract\":[5,-3]},false]},{\"$ne\":[{\"$subtract\":[5,-3]},\"\"]},{\"$ne\":[{\"$subtract\":[5,-3]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[8,null]},null]},{\"$ne\":[8,false]},{\"$ne\":[8,\"\"]},{\"$ne\":[8,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000$.a += $.b += 1": {
      "entry": "filter",
      "src": "$.a += $.b += 1",
      "kind": "message",
      "was": "Compound assignment cannot be chained at position 7 — split into separate statements",
      "now": "Unexpected token '+=' at position 4",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000$.a =": {
      "entry": "filter",
      "src": "$.a =",
      "kind": "message",
      "was": "Unexpected end of expression",
      "now": "Unexpected token '=' at position 4",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000delete": {
      "entry": "filter",
      "src": "delete",
      "kind": "message",
      "was": "Unexpected end of expression",
      "now": "Unexpected token 'delete' at position 0",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000($.a = 1) + 2": {
      "entry": "filter",
      "src": "($.a = 1) + 2",
      "kind": "message",
      "was": "Assignment is a statement, not a value. It is only valid at the top level or as a pipeline-array element.",
      "now": "Expected ')' but got '=' at position 5",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000[1,2,3].take(2)": {
      "entry": "filter",
      "src": "[1,2,3].take(2)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$slice\":[[1,2,3],2]},null]},null]},{\"$ne\":[{\"$slice\":[[1,2,3],2]},false]},{\"$ne\":[{\"$slice\":[[1,2,3],2]},\"\"]},{\"$ne\":[{\"$slice\":[[1,2,3],2]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[1,2],null]},null]},{\"$ne\":[[1,2],false]},{\"$ne\":[[1,2],\"\"]},{\"$ne\":[[1,2],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3].drop(2)": {
      "entry": "filter",
      "src": "[1,2,3].drop(2)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$let\":{\"vars\":{\"jsmqlArr\":[1,2,3]},\"in\":{\"$slice\":[\"$$jsmqlArr\",2,{\"$max\":[1,{\"$size\":\"$$jsmqlArr\"}]}]}}},null]},null]},{\"$ne\":[{\"$let\":{\"vars\":{\"jsmqlArr\":[1,2,3]},\"in\":{\"$slice\":",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[3],null]},null]},{\"$ne\":[[3],false]},{\"$ne\":[[3],\"\"]},{\"$ne\":[[3],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3].slice(1,2)": {
      "entry": "filter",
      "src": "[1,2,3].slice(1,2)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$slice\":[[1,2,3],1,1]},null]},null]},{\"$ne\":[{\"$slice\":[[1,2,3],1,1]},false]},{\"$ne\":[{\"$slice\":[[1,2,3],1,1]},\"\"]},{\"$ne\":[{\"$slice\":[[1,2,3],1,1]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[2],null]},null]},{\"$ne\":[[2],false]},{\"$ne\":[[2],\"\"]},{\"$ne\":[[2],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3].slice(-2)": {
      "entry": "filter",
      "src": "[1,2,3].slice(-2)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$slice\":[[1,2,3],-2]},null]},null]},{\"$ne\":[{\"$slice\":[[1,2,3],-2]},false]},{\"$ne\":[{\"$slice\":[[1,2,3],-2]},\"\"]},{\"$ne\":[{\"$slice\":[[1,2,3],-2]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[2,3],null]},null]},{\"$ne\":[[2,3],false]},{\"$ne\":[[2,3],\"\"]},{\"$ne\":[[2,3],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3].map(x => x)": {
      "entry": "filter",
      "src": "[1,2,3].map(x => x)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$map\":{\"input\":[1,2,3],\"as\":\"x\",\"in\":\"$$x\"}},null]},null]},{\"$ne\":[{\"$map\":{\"input\":[1,2,3],\"as\":\"x\",\"in\":\"$$x\"}},false]},{\"$ne\":[{\"$map\":{\"input\":[1,2,3],\"as\":\"x\",\"in\":\"$$x\"}},\"\"]",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[1,2,3],null]},null]},{\"$ne\":[[1,2,3],false]},{\"$ne\":[[1,2,3],\"\"]},{\"$ne\":[[1,2,3],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3].sortBy('t')": {
      "entry": "filter",
      "src": "[1,2,3].sortBy('t')",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$sortArray\":{\"input\":[1,2,3],\"sortBy\":{\"t\":1}}},null]},null]},{\"$ne\":[{\"$sortArray\":{\"input\":[1,2,3],\"sortBy\":{\"t\":1}}},false]},{\"$ne\":[{\"$sortArray\":{\"input\":[1,2,3],\"sortBy\":{\"t\"",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[1,2,3],null]},null]},{\"$ne\":[[1,2,3],false]},{\"$ne\":[[1,2,3],\"\"]},{\"$ne\":[[1,2,3],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3].reduce((a,b) => a + b, 0)": {
      "entry": "filter",
      "src": "[1,2,3].reduce((a,b) => a + b, 0)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":[1,2,3],\"initialValue\":0,\"in\":{\"$add\":[\"$$value\",\"$$this\"]}}},null]},null]},{\"$ne\":[{\"$reduce\":{\"input\":[1,2,3],\"initialValue\":0,\"in\":{\"$add\":[\"$$value\",\"$$this\"]",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[6,null]},null]},{\"$ne\":[6,false]},{\"$ne\":[6,\"\"]},{\"$ne\":[6,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3].includes(1)": {
      "entry": "filter",
      "src": "[1,2,3].includes(1)",
      "kind": "output",
      "was": "{\"$expr\":{\"$in\":[1,[1,2,3]]}}",
      "now": "{\"$expr\":true}",
      "reason": "equivalent: the fold settles a constant predicate to its value, which the filter carries as `{ $expr: <value> }`; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3].join(',')": {
      "entry": "filter",
      "src": "[1,2,3].join(',')",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$reduce\":{\"input\":[1,2,3],\"initialValue\":\"\",\"in\":{\"$cond\":{\"if\":{\"$eq\":[\"$$value\",\"\"]},\"then\":{\"$toString\":\"$$this\"},\"else\":{\"$concat\":[\"$$value\",\",\",{\"$toString\":\"$$this\"}]}}}}},n",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"1,2,3\",null]},null]},{\"$ne\":[\"1,2,3\",false]},{\"$ne\":[\"1,2,3\",\"\"]},{\"$ne\":[\"1,2,3\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3].at(-1)": {
      "entry": "filter",
      "src": "[1,2,3].at(-1)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$arrayElemAt\":[[1,2,3],-1]},null]},null]},{\"$ne\":[{\"$arrayElemAt\":[[1,2,3],-1]},false]},{\"$ne\":[{\"$arrayElemAt\":[[1,2,3],-1]},\"\"]},{\"$ne\":[{\"$arrayElemAt\":[[1,2,3],-1]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[3,null]},null]},{\"$ne\":[3,false]},{\"$ne\":[3,\"\"]},{\"$ne\":[3,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3].concat([1])": {
      "entry": "filter",
      "src": "[1,2,3].concat([1])",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$concatArrays\":[[1,2,3],[1]]},null]},null]},{\"$ne\":[{\"$concatArrays\":[[1,2,3],[1]]},false]},{\"$ne\":[{\"$concatArrays\":[[1,2,3],[1]]},\"\"]},{\"$ne\":[{\"$concatArrays\":[[1,2,3],[1]]},0]}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[[1,2,3,1],null]},null]},{\"$ne\":[[1,2,3,1],false]},{\"$ne\":[[1,2,3,1],\"\"]},{\"$ne\":[[1,2,3,1],0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000[1,2,3].length": {
      "entry": "filter",
      "src": "[1,2,3].length",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$size\":[[1,2,3]]},null]},null]},{\"$ne\":[{\"$size\":[[1,2,3]]},false]},{\"$ne\":[{\"$size\":[[1,2,3]]},\"\"]},{\"$ne\":[{\"$size\":[[1,2,3]]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[3,null]},null]},{\"$ne\":[3,false]},{\"$ne\":[3,\"\"]},{\"$ne\":[3,0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000\"abc\".slice(1,2)": {
      "entry": "filter",
      "src": "\"abc\".slice(1,2)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$substrCP\":[\"abc\",1,1]},null]},null]},{\"$ne\":[{\"$substrCP\":[\"abc\",1,1]},false]},{\"$ne\":[{\"$substrCP\":[\"abc\",1,1]},\"\"]},{\"$ne\":[{\"$substrCP\":[\"abc\",1,1]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"b\",null]},null]},{\"$ne\":[\"b\",false]},{\"$ne\":[\"b\",\"\"]},{\"$ne\":[\"b\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000\"abc\".slice(-2)": {
      "entry": "filter",
      "src": "\"abc\".slice(-2)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$substrCP\":[\"abc\",1,2]},null]},null]},{\"$ne\":[{\"$substrCP\":[\"abc\",1,2]},false]},{\"$ne\":[{\"$substrCP\":[\"abc\",1,2]},\"\"]},{\"$ne\":[{\"$substrCP\":[\"abc\",1,2]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"bc\",null]},null]},{\"$ne\":[\"bc\",false]},{\"$ne\":[\"bc\",\"\"]},{\"$ne\":[\"bc\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000\"abc\".groupBy('t')": {
      "entry": "filter",
      "src": "\"abc\".groupBy('t')",
      "kind": "message",
      "was": "'.groupBy(...)' expects an array receiver, but the value before it returns a string. Call '.groupBy(...)' on an array value instead.",
      "now": "'.groupBy()' is not available on a 'string' — it is defined on 'array', 'stream', 'Object'.",
      "reason": "message: reworded — both compilers refuse the source; the new text is the row's own or the generic wording for its class."
    },
    "filter\u0000\"abc\".trim()": {
      "entry": "filter",
      "src": "\"abc\".trim()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$trim\":{\"input\":\"abc\"}},null]},null]},{\"$ne\":[{\"$trim\":{\"input\":\"abc\"}},false]},{\"$ne\":[{\"$trim\":{\"input\":\"abc\"}},\"\"]},{\"$ne\":[{\"$trim\":{\"input\":\"abc\"}},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"abc\",null]},null]},{\"$ne\":[\"abc\",false]},{\"$ne\":[\"abc\",\"\"]},{\"$ne\":[\"abc\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000\"abc\".toUpperCase()": {
      "entry": "filter",
      "src": "\"abc\".toUpperCase()",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$toUpper\":\"abc\"},null]},null]},{\"$ne\":[{\"$toUpper\":\"abc\"},false]},{\"$ne\":[{\"$toUpper\":\"abc\"},\"\"]},{\"$ne\":[{\"$toUpper\":\"abc\"},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"ABC\",null]},null]},{\"$ne\":[\"ABC\",false]},{\"$ne\":[\"ABC\",\"\"]},{\"$ne\":[\"ABC\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000\"abc\".startsWith('a')": {
      "entry": "filter",
      "src": "\"abc\".startsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$eq\":[{\"$indexOfCP\":[\"abc\",\"a\"]},0]}}",
      "now": "{\"$expr\":true}",
      "reason": "equivalent: the fold settles a constant predicate to its value, which the filter carries as `{ $expr: <value> }`; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000\"abc\".endsWith('a')": {
      "entry": "filter",
      "src": "\"abc\".endsWith('a')",
      "kind": "output",
      "was": "{\"$expr\":{\"$let\":{\"vars\":{\"jsmqlStr\":{\"$ifNull\":[\"abc\",\"\"]}},\"in\":{\"$eq\":[{\"$substrCP\":[\"$$jsmqlStr\",{\"$max\":[0,{\"$subtract\":[{\"$strLenCP\":\"$$jsmqlStr\"},1]}]},1]},\"a\"]}}}}",
      "now": "{\"$expr\":false}",
      "reason": "equivalent: the fold settles a constant predicate to its value, which the filter carries as `{ $expr: <value> }`; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000\"abc\".charAt(1)": {
      "entry": "filter",
      "src": "\"abc\".charAt(1)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$substrCP\":[\"abc\",1,1]},null]},null]},{\"$ne\":[{\"$substrCP\":[\"abc\",1,1]},false]},{\"$ne\":[{\"$substrCP\":[\"abc\",1,1]},\"\"]},{\"$ne\":[{\"$substrCP\":[\"abc\",1,1]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"b\",null]},null]},{\"$ne\":[\"b\",false]},{\"$ne\":[\"b\",\"\"]},{\"$ne\":[\"b\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000\"abc\".substring(1,2)": {
      "entry": "filter",
      "src": "\"abc\".substring(1,2)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$substrCP\":[\"abc\",1,1]},null]},null]},{\"$ne\":[{\"$substrCP\":[\"abc\",1,1]},false]},{\"$ne\":[{\"$substrCP\":[\"abc\",1,1]},\"\"]},{\"$ne\":[{\"$substrCP\":[\"abc\",1,1]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"b\",null]},null]},{\"$ne\":[\"b\",false]},{\"$ne\":[\"b\",\"\"]},{\"$ne\":[\"b\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000\"abc\".at(-1)": {
      "entry": "filter",
      "src": "\"abc\".at(-1)",
      "kind": "output",
      "was": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[{\"$substrCP\":[\"abc\",2,1]},null]},null]},{\"$ne\":[{\"$substrCP\":[\"abc\",2,1]},false]},{\"$ne\":[{\"$substrCP\":[\"abc\",2,1]},\"\"]},{\"$ne\":[{\"$substrCP\":[\"abc\",2,1]},0]}]}}",
      "now": "{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"c\",null]},null]},{\"$ne\":[\"c\",false]},{\"$ne\":[\"c\",\"\"]},{\"$ne\":[\"c\",0]}]}}",
      "reason": "equivalent: the fold settles a constant expression to its value inside the filter's $expr truthiness check; the shipped lowered the same constants to an operator the server evaluates to that value."
    },
    "filter\u0000$.status == null": {
      "entry": "filter",
      "src": "$.status == null",
      "kind": "output",
      "was": "{\"status\":null}",
      "now": "{\"status\":{\"$eq\":null,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.status === 'active'": {
      "entry": "filter",
      "src": "$.status === 'active'",
      "kind": "output",
      "was": "{\"status\":\"active\"}",
      "now": "{\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.status === null": {
      "entry": "filter",
      "src": "$.status === null",
      "kind": "output",
      "was": "{\"status\":{\"$type\":\"null\"}}",
      "now": "{\"status\":{\"$type\":\"null\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.status != null": {
      "entry": "filter",
      "src": "$.status != null",
      "kind": "output",
      "was": "{\"status\":{\"$ne\":null}}",
      "now": "{\"$or\":[{\"status\":{\"$ne\":null}},{\"status\":{\"$type\":\"array\"}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.status !== null": {
      "entry": "filter",
      "src": "$.status !== null",
      "kind": "output",
      "was": "{\"status\":{\"$not\":{\"$type\":\"null\"}}}",
      "now": "{\"$or\":[{\"status\":{\"$not\":{\"$type\":\"null\"}}},{\"status\":{\"$type\":\"array\"}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000null == $.status": {
      "entry": "filter",
      "src": "null == $.status",
      "kind": "output",
      "was": "{\"status\":null}",
      "now": "{\"status\":{\"$eq\":null,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.age > 18": {
      "entry": "filter",
      "src": "$.age > 18",
      "kind": "output",
      "was": "{\"age\":{\"$gt\":18}}",
      "now": "{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.age >= 21": {
      "entry": "filter",
      "src": "$.age >= 21",
      "kind": "output",
      "was": "{\"age\":{\"$gte\":21}}",
      "now": "{\"age\":{\"$gte\":21,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.score < 50": {
      "entry": "filter",
      "src": "$.score < 50",
      "kind": "output",
      "was": "{\"score\":{\"$lt\":50}}",
      "now": "{\"score\":{\"$lt\":50,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.score <= 100": {
      "entry": "filter",
      "src": "$.score <= 100",
      "kind": "output",
      "was": "{\"score\":{\"$lte\":100}}",
      "now": "{\"score\":{\"$lte\":100,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a > 0 && $.b > 0": {
      "entry": "filter",
      "src": "$.a > 0 && $.b > 0",
      "kind": "output",
      "was": "{\"a\":{\"$gt\":0},\"b\":{\"$gt\":0}}",
      "now": "{\"a\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}},\"b\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a > 0 || $.b > 0": {
      "entry": "filter",
      "src": "$.a > 0 || $.b > 0",
      "kind": "output",
      "was": "{\"$or\":[{\"a\":{\"$gt\":0}},{\"b\":{\"$gt\":0}}]}",
      "now": "{\"$or\":[{\"a\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}},{\"b\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a > 0 && $.b > 0 && $.c > 0": {
      "entry": "filter",
      "src": "$.a > 0 && $.b > 0 && $.c > 0",
      "kind": "output",
      "was": "{\"a\":{\"$gt\":0},\"b\":{\"$gt\":0},\"c\":{\"$gt\":0}}",
      "now": "{\"a\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}},\"b\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}},\"c\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.age > 18 && $.active": {
      "entry": "filter",
      "src": "$.age > 18 && $.active",
      "kind": "output",
      "was": "{\"age\":{\"$gt\":18},\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}",
      "now": "{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}},\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.in === 'test'": {
      "entry": "filter",
      "src": "$.in === 'test'",
      "kind": "output",
      "was": "{\"in\":\"test\"}",
      "now": "{\"in\":{\"$eq\":\"test\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.loyalty.years >= 2": {
      "entry": "filter",
      "src": "$.loyalty.years >= 2",
      "kind": "output",
      "was": "{\"loyalty.years\":{\"$gte\":2}}",
      "now": "{\"loyalty.years\":{\"$gte\":2,\"$not\":{\"$type\":\"array\"}},\"loyalty\":{\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.email.includes(\"@\")": {
      "entry": "filter",
      "src": "$.email.includes(\"@\")",
      "kind": "output",
      "was": "{\"email\":\"@\"}",
      "now": "{\"$or\":[{\"email\":{\"$eq\":\"@\",\"$type\":\"array\"}},{\"email\":{\"$regex\":\"@\",\"$not\":{\"$type\":\"array\"}}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.str.match(/^[A-Z]/)": {
      "entry": "filter",
      "src": "$.str.match(/^[A-Z]/)",
      "kind": "output",
      "was": "{\"str\":\"«regexp:^[A-Z]/»\"}",
      "now": "{\"str\":{\"$regex\":\"«regexp:^[A-Z]/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.str.match(/^[a-z]/i)": {
      "entry": "filter",
      "src": "$.str.match(/^[a-z]/i)",
      "kind": "output",
      "was": "{\"str\":\"«regexp:^[a-z]/i»\"}",
      "now": "{\"str\":{\"$regex\":\"«regexp:^[a-z]/i»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a % 5 === 0": {
      "entry": "filter",
      "src": "$.a % 5 === 0",
      "kind": "output",
      "was": "{\"a\":{\"$mod\":[5,0]}}",
      "now": "{\"a\":{\"$mod\":[5,0],\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a % 5 !== 0": {
      "entry": "filter",
      "src": "$.a % 5 !== 0",
      "kind": "output",
      "was": "{\"a\":{\"$not\":{\"$mod\":[5,0]}}}",
      "now": "{\"$or\":[{\"a\":{\"$not\":{\"$mod\":[5,0]}}},{\"a\":{\"$type\":\"array\"}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a === null": {
      "entry": "filter",
      "src": "$.a === null",
      "kind": "output",
      "was": "{\"a\":{\"$type\":\"null\"}}",
      "now": "{\"a\":{\"$type\":\"null\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a.b.c === undefined": {
      "entry": "filter",
      "src": "$.a.b.c === undefined",
      "kind": "output",
      "was": "{\"a.b.c\":{\"$exists\":false}}",
      "now": "{\"$or\":[{\"a.b.c\":{\"$exists\":false}},{\"a\":{\"$type\":\"array\"}},{\"a.b\":{\"$type\":\"array\"}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000typeof $.a === \"boolean\"": {
      "entry": "filter",
      "src": "typeof $.a === \"boolean\"",
      "kind": "now-rejected",
      "was": "{\"a\":{\"$type\":\"bool\"}}",
      "now": "'typeof' compares against one of MongoDB's type names, and \"boolean\" is not one. Did you mean 'bool'? For absence, write 'x === undefined'.",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000typeof $.a === \"number\"": {
      "entry": "filter",
      "src": "typeof $.a === \"number\"",
      "kind": "output",
      "was": "{\"a\":{\"$type\":\"number\"}}",
      "now": "{\"a\":{\"$type\":\"number\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000typeof $.a !== \"boolean\"": {
      "entry": "filter",
      "src": "typeof $.a !== \"boolean\"",
      "kind": "now-rejected",
      "was": "{\"a\":{\"$not\":{\"$type\":\"bool\"}}}",
      "now": "'typeof' compares against one of MongoDB's type names, and \"boolean\" is not one. Did you mean 'bool'? For absence, write 'x === undefined'.",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000typeof $.a !== \"number\"": {
      "entry": "filter",
      "src": "typeof $.a !== \"number\"",
      "kind": "output",
      "was": "{\"a\":{\"$not\":{\"$type\":\"number\"}}}",
      "now": "{\"$or\":[{\"a\":{\"$not\":{\"$type\":\"number\"}}},{\"a\":{\"$type\":\"array\"}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000typeof $.a.b.c === \"boolean\"": {
      "entry": "filter",
      "src": "typeof $.a.b.c === \"boolean\"",
      "kind": "now-rejected",
      "was": "{\"a.b.c\":{\"$type\":\"bool\"}}",
      "now": "'typeof' compares against one of MongoDB's type names, and \"boolean\" is not one. Did you mean 'bool'? For absence, write 'x === undefined'.",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000typeof $.x === \"string\"": {
      "entry": "filter",
      "src": "typeof $.x === \"string\"",
      "kind": "output",
      "was": "{\"x\":{\"$type\":\"string\"}}",
      "now": "{\"x\":{\"$type\":\"string\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.str.match(/[a-z]+/)": {
      "entry": "filter",
      "src": "$.str.match(/[a-z]+/)",
      "kind": "output",
      "was": "{\"str\":\"«regexp:[a-z]+/»\"}",
      "now": "{\"str\":{\"$regex\":\"«regexp:[a-z]+/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.str.match(/pattern/gi)": {
      "entry": "filter",
      "src": "$.str.match(/pattern/gi)",
      "kind": "output",
      "was": "{\"str\":\"«regexp:pattern/gi»\"}",
      "now": "{\"str\":{\"$regex\":\"«regexp:pattern/gi»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.x < 5": {
      "entry": "filter",
      "src": "$.x < 5",
      "kind": "output",
      "was": "{\"x\":{\"$lt\":5}}",
      "now": "{\"x\":{\"$lt\":5,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.x === 5": {
      "entry": "filter",
      "src": "$.x === 5",
      "kind": "output",
      "was": "{\"x\":5}",
      "now": "{\"x\":{\"$eq\":5,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000[\"a\", \"b\"].includes($.x)": {
      "entry": "filter",
      "src": "[\"a\", \"b\"].includes($.x)",
      "kind": "output",
      "was": "{\"x\":{\"$in\":[\"a\",\"b\"]}}",
      "now": "{\"x\":{\"$in\":[\"a\",\"b\"],\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.tags?.includes('vip')": {
      "entry": "filter",
      "src": "$.tags?.includes('vip')",
      "kind": "output",
      "was": "{\"tags\":\"vip\"}",
      "now": "{\"$or\":[{\"tags\":{\"$eq\":\"vip\",\"$type\":\"array\"}},{\"tags\":{\"$regex\":\"vip\",\"$not\":{\"$type\":\"array\"}}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.user?.role === 'admin'": {
      "entry": "filter",
      "src": "$.user?.role === 'admin'",
      "kind": "output",
      "was": "{\"user.role\":\"admin\"}",
      "now": "{\"user.role\":{\"$eq\":\"admin\",\"$not\":{\"$type\":\"array\"}},\"user\":{\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.user?.role == null": {
      "entry": "filter",
      "src": "$.user?.role == null",
      "kind": "output",
      "was": "{\"user.role\":null}",
      "now": "{\"$or\":[{\"user.role\":{\"$eq\":null,\"$not\":{\"$type\":\"array\"}}},{\"user\":{\"$type\":\"array\"}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.path.match(/[/\\\\]/)": {
      "entry": "filter",
      "src": "$.path.match(/[/\\\\]/)",
      "kind": "output",
      "was": "{\"path\":\"«regexp:[/\\\\\\\\]/»\"}",
      "now": "{\"path\":{\"$regex\":\"«regexp:[/\\\\\\\\]/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.path.match(/* skip */ /foo/i)": {
      "entry": "filter",
      "src": "$.path.match(/* skip */ /foo/i)",
      "kind": "output",
      "was": "{\"path\":\"«regexp:foo/i»\"}",
      "now": "{\"path\":{\"$regex\":\"«regexp:foo/i»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.status === \"active\"": {
      "entry": "filter",
      "src": "$.status === \"active\"",
      "kind": "output",
      "was": "{\"status\":\"active\"}",
      "now": "{\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.status === 'shipped'": {
      "entry": "filter",
      "src": "$.status === 'shipped'",
      "kind": "output",
      "was": "{\"status\":\"shipped\"}",
      "now": "{\"status\":{\"$eq\":\"shipped\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.status === 'active' && $.age >= 18": {
      "entry": "filter",
      "src": "$.status === 'active' && $.age >= 18",
      "kind": "output",
      "was": "{\"status\":\"active\",\"age\":{\"$gte\":18}}",
      "now": "{\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}},\"age\":{\"$gte\":18,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.address.country === 'AU'": {
      "entry": "filter",
      "src": "$.address.country === 'AU'",
      "kind": "output",
      "was": "{\"address.country\":\"AU\"}",
      "now": "{\"address.country\":{\"$eq\":\"AU\",\"$not\":{\"$type\":\"array\"}},\"address\":{\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a === 0": {
      "entry": "filter",
      "src": "$.a === 0",
      "kind": "output",
      "was": "{\"a\":0}",
      "now": "{\"a\":{\"$eq\":0,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$._id === 0x507f_1f77_bcf8_6cd7_9943_9011": {
      "entry": "filter",
      "src": "$._id === 0x507f_1f77_bcf8_6cd7_9943_9011",
      "kind": "output",
      "was": "{\"_id\":\"«oid:507f1f77bcf86cd799439011»\"}",
      "now": "{\"_id\":{\"$eq\":\"«oid:507f1f77bcf86cd799439011»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.let > 5": {
      "entry": "filter",
      "src": "$.let > 5",
      "kind": "output",
      "was": "{\"let\":{\"$gt\":5}}",
      "now": "{\"let\":{\"$gt\":5,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.const > 5": {
      "entry": "filter",
      "src": "$.const > 5",
      "kind": "output",
      "was": "{\"const\":{\"$gt\":5}}",
      "now": "{\"const\":{\"$gt\":5,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.s.match(/word/g)": {
      "entry": "filter",
      "src": "$.s.match(/word/g)",
      "kind": "output",
      "was": "{\"s\":\"«regexp:word/g»\"}",
      "now": "{\"s\":{\"$regex\":\"«regexp:word/g»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.method === \"postalDelivery\" && $.createdAt >= new Date(\"2026-01-01\")": {
      "entry": "filter",
      "src": "$.method === \"postalDelivery\" && $.createdAt >= new Date(\"2026-01-01\")",
      "kind": "output",
      "was": "{\"method\":\"postalDelivery\",\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"}}",
      "now": "{\"method\":{\"$eq\":\"postalDelivery\",\"$not\":{\"$type\":\"array\"}},\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.subscription.tier === \"premium\" && $.status === \"active\" && $.expiresAt > new Date(\"2026-05-01\")": {
      "entry": "filter",
      "src": "$.subscription.tier === \"premium\" && $.status === \"active\" && $.expiresAt > new Date(\"2026-05-01\")",
      "kind": "output",
      "was": "{\"subscription.tier\":\"premium\",\"status\":\"active\",\"expiresAt\":{\"$gt\":\"«date:2026-05-01T00:00:00.000Z»\"}}",
      "now": "{\"subscription.tier\":{\"$eq\":\"premium\",\"$not\":{\"$type\":\"array\"}},\"subscription\":{\"$not\":{\"$type\":\"array\"}},\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}},\"expiresAt\":{\"$gt\":\"«date:2026-05-01T00:00:00.000Z»\",\"$not\":{\"$",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.pinned === true || $.author === \"ada\"": {
      "entry": "filter",
      "src": "$.pinned === true || $.author === \"ada\"",
      "kind": "output",
      "was": "{\"$or\":[{\"pinned\":true},{\"author\":\"ada\"}]}",
      "now": "{\"$or\":[{\"pinned\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}},{\"author\":{\"$eq\":\"ada\",\"$not\":{\"$type\":\"array\"}}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.pinned !== true && $.lastModifiedAt < new Date(\"2025-01-01\")": {
      "entry": "filter",
      "src": "$.pinned !== true && $.lastModifiedAt < new Date(\"2025-01-01\")",
      "kind": "output",
      "was": "{\"pinned\":{\"$ne\":true},\"lastModifiedAt\":{\"$lt\":\"«date:2025-01-01T00:00:00.000Z»\"}}",
      "now": "{\"$or\":[{\"pinned\":{\"$ne\":true}},{\"pinned\":{\"$type\":\"array\"}}],\"lastModifiedAt\":{\"$lt\":\"«date:2025-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.inStock === true && $.price >= 50 && $.price <= 200": {
      "entry": "filter",
      "src": "$.inStock === true && $.price >= 50 && $.price <= 200",
      "kind": "output",
      "was": "{\"inStock\":true,\"$and\":[{\"price\":{\"$gte\":50}},{\"price\":{\"$lte\":200}}]}",
      "now": "{\"inStock\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}},\"price\":{\"$gte\":50,\"$not\":{\"$type\":\"array\"},\"$lte\":200}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.email != null && $.status === \"active\"": {
      "entry": "filter",
      "src": "$.email != null && $.status === \"active\"",
      "kind": "output",
      "was": "{\"email\":{\"$ne\":null},\"status\":\"active\"}",
      "now": "{\"$or\":[{\"email\":{\"$ne\":null}},{\"email\":{\"$type\":\"array\"}}],\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000typeof $.profile === \"object\" && $.status === \"active\"": {
      "entry": "filter",
      "src": "typeof $.profile === \"object\" && $.status === \"active\"",
      "kind": "output",
      "was": "{\"profile\":{\"$type\":\"object\"},\"status\":\"active\"}",
      "now": "{\"profile\":{\"$type\":\"object\",\"$not\":{\"$type\":\"array\"}},\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$._id === \"doc_42\" && $.tenantId === \"acme\"": {
      "entry": "filter",
      "src": "$._id === \"doc_42\" && $.tenantId === \"acme\"",
      "kind": "output",
      "was": "{\"_id\":\"doc_42\",\"tenantId\":\"acme\"}",
      "now": "{\"_id\":{\"$eq\":\"doc_42\",\"$not\":{\"$type\":\"array\"}},\"tenantId\":{\"$eq\":\"acme\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$._id === 0x507f1f77bcf86cd799439011 && $.tenantId === \"acme\"": {
      "entry": "filter",
      "src": "$._id === 0x507f1f77bcf86cd799439011 && $.tenantId === \"acme\"",
      "kind": "output",
      "was": "{\"_id\":\"«oid:507f1f77bcf86cd799439011»\",\"tenantId\":\"acme\"}",
      "now": "{\"_id\":{\"$eq\":\"«oid:507f1f77bcf86cd799439011»\",\"$not\":{\"$type\":\"array\"}},\"tenantId\":{\"$eq\":\"acme\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.deleted !== true && $.archived !== true": {
      "entry": "filter",
      "src": "$.deleted !== true && $.archived !== true",
      "kind": "output",
      "was": "{\"deleted\":{\"$ne\":true},\"archived\":{\"$ne\":true}}",
      "now": "{\"$and\":[{\"$or\":[{\"deleted\":{\"$ne\":true}},{\"deleted\":{\"$type\":\"array\"}}]},{\"$or\":[{\"archived\":{\"$ne\":true}},{\"archived\":{\"$type\":\"array\"}}]}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.parent === null && $.published === true": {
      "entry": "filter",
      "src": "$.parent === null && $.published === true",
      "kind": "output",
      "was": "{\"parent\":{\"$type\":\"null\"},\"published\":true}",
      "now": "{\"parent\":{\"$type\":\"null\",\"$not\":{\"$type\":\"array\"}},\"published\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.tier === \"gold\" && $.country === \"AU\"": {
      "entry": "filter",
      "src": "$.tier === \"gold\" && $.country === \"AU\"",
      "kind": "output",
      "was": "{\"tier\":\"gold\",\"country\":\"AU\"}",
      "now": "{\"tier\":{\"$eq\":\"gold\",\"$not\":{\"$type\":\"array\"}},\"country\":{\"$eq\":\"AU\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000\n$.customerId === \"cust_42\" &&\n$.placedAt >= new Date(\"2026-01-01\") && $.placedAt < new Date(\"2026-02-01\") &&\n$.status === \"shipped\"\n      ": {
      "entry": "filter",
      "src": "\n$.customerId === \"cust_42\" &&\n$.placedAt >= new Date(\"2026-01-01\") && $.placedAt < new Date(\"2026-02-01\") &&\n$.status === \"shipped\"\n      ",
      "kind": "output",
      "was": "{\"customerId\":\"cust_42\",\"$and\":[{\"placedAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"}},{\"placedAt\":{\"$lt\":\"«date:2026-02-01T00:00:00.000Z»\"}}],\"status\":\"shipped\"}",
      "now": "{\"customerId\":{\"$eq\":\"cust_42\",\"$not\":{\"$type\":\"array\"}},\"placedAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"},\"$lt\":\"«date:2026-02-01T00:00:00.000Z»\"},\"status\":{\"$eq\":\"shipped\",\"$not\":{\"$type\":\"a",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000\n// age window: 25 to 70 inclusive\n$.driver.age >= 25 && $.driver.age <= 70 &&\n\n/* annual mileage cap — 30k km/year keeps us out of commercial-fleet pricing */\n$.policy.kmPerYear <= 30_000 &&\n\n// hard-list occupations that bump the applicant to the high-risk tier\n!($.driver.occupation in [\"stunt-double\", \"test-pilot\", \"demolition-engineer\"])\n      ": {
      "entry": "filter",
      "src": "\n// age window: 25 to 70 inclusive\n$.driver.age >= 25 && $.driver.age <= 70 &&\n\n/* annual mileage cap — 30k km/year keeps us out of commercial-fleet pricing */\n$.policy.kmPerYear <= 30_000 &&\n\n// hard-list occupations that bump the applicant to the high-risk tier\n!($.driver.occupation in [\"stunt-double\", \"test-pilot\", \"demolition-engineer\"])\n      ",
      "kind": "output",
      "was": "{\"$and\":[{\"driver.age\":{\"$gte\":25}},{\"driver.age\":{\"$lte\":70}}],\"policy.kmPerYear\":{\"$lte\":30000},\"$expr\":{\"$not\":{\"$in\":[\"$driver.occupation\",[\"stunt-double\",\"test-pilot\",\"demolition-engineer\"]]}}}",
      "now": "{\"driver.age\":{\"$gte\":25,\"$not\":{\"$type\":\"array\"},\"$lte\":70},\"driver\":{\"$not\":{\"$type\":\"array\"}},\"policy.kmPerYear\":{\"$lte\":30000,\"$not\":{\"$type\":\"array\"}},\"policy\":{\"$not\":{\"$type\":\"array\"}},\"$expr\":{\"$not\":{\"$in\":[\"$dr",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000\n$.score >= 75 &&\n$.grade in [\"A\", \"B\"] &&\n$.submitted === true\n      ": {
      "entry": "filter",
      "src": "\n$.score >= 75 &&\n$.grade in [\"A\", \"B\"] &&\n$.submitted === true\n      ",
      "kind": "output",
      "was": "{\"score\":{\"$gte\":75},\"submitted\":true,\"$expr\":{\"$in\":[\"$grade\",[\"A\",\"B\"]]}}",
      "now": "{\"score\":{\"$gte\":75,\"$not\":{\"$type\":\"array\"}},\"submitted\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}},\"$expr\":{\"$in\":[\"$grade\",[\"A\",\"B\"]]}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.age > 18 && $.status === 'active'": {
      "entry": "filter",
      "src": "$.age > 18 && $.status === 'active'",
      "kind": "output",
      "was": "{\"age\":{\"$gt\":18},\"status\":\"active\"}",
      "now": "{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}},\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a == null": {
      "entry": "filter",
      "src": "$.a == null",
      "kind": "output",
      "was": "{\"a\":null}",
      "now": "{\"a\":{\"$eq\":null,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a === 1": {
      "entry": "filter",
      "src": "$.a === 1",
      "kind": "output",
      "was": "{\"a\":1}",
      "now": "{\"a\":{\"$eq\":1,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a >= 1": {
      "entry": "filter",
      "src": "$.a >= 1",
      "kind": "output",
      "was": "{\"a\":{\"$gte\":1}}",
      "now": "{\"a\":{\"$gte\":1,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a <= 1": {
      "entry": "filter",
      "src": "$.a <= 1",
      "kind": "output",
      "was": "{\"a\":{\"$lte\":1}}",
      "now": "{\"a\":{\"$lte\":1,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a != null": {
      "entry": "filter",
      "src": "$.a != null",
      "kind": "output",
      "was": "{\"a\":{\"$ne\":null}}",
      "now": "{\"$or\":[{\"a\":{\"$ne\":null}},{\"a\":{\"$type\":\"array\"}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a !== 1": {
      "entry": "filter",
      "src": "$.a !== 1",
      "kind": "output",
      "was": "{\"a\":{\"$ne\":1}}",
      "now": "{\"$or\":[{\"a\":{\"$ne\":1}},{\"a\":{\"$type\":\"array\"}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.arr.includes(1)": {
      "entry": "filter",
      "src": "$.arr.includes(1)",
      "kind": "output",
      "was": "{\"arr\":1}",
      "now": "{\"$or\":[{\"arr\":{\"$eq\":1,\"$type\":\"array\"}},{\"arr\":{\"$regex\":\"1\",\"$not\":{\"$type\":\"array\"}}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.s.includes(1)": {
      "entry": "filter",
      "src": "$.s.includes(1)",
      "kind": "output",
      "was": "{\"s\":1}",
      "now": "{\"$or\":[{\"s\":{\"$eq\":1,\"$type\":\"array\"}},{\"s\":{\"$regex\":\"1\",\"$not\":{\"$type\":\"array\"}}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.n.includes(1)": {
      "entry": "filter",
      "src": "$.n.includes(1)",
      "kind": "output",
      "was": "{\"n\":1}",
      "now": "{\"$or\":[{\"n\":{\"$eq\":1,\"$type\":\"array\"}},{\"n\":{\"$regex\":\"1\",\"$not\":{\"$type\":\"array\"}}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.obj.includes(1)": {
      "entry": "filter",
      "src": "$.obj.includes(1)",
      "kind": "output",
      "was": "{\"obj\":1}",
      "now": "{\"$or\":[{\"obj\":{\"$eq\":1,\"$type\":\"array\"}},{\"obj\":{\"$regex\":\"1\",\"$not\":{\"$type\":\"array\"}}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000typeof $.a === \"string\"": {
      "entry": "filter",
      "src": "typeof $.a === \"string\"",
      "kind": "output",
      "was": "{\"a\":{\"$type\":\"string\"}}",
      "now": "{\"a\":{\"$type\":\"string\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000typeof $.a.b.c === \"number\"": {
      "entry": "filter",
      "src": "typeof $.a.b.c === \"number\"",
      "kind": "output",
      "was": "{\"a.b.c\":{\"$type\":\"number\"}}",
      "now": "{\"a.b.c\":{\"$type\":\"number\",\"$not\":{\"$type\":\"array\"}},\"a\":{\"$not\":{\"$type\":\"array\"}},\"a.b\":{\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.a === 1 && $.b < 2": {
      "entry": "filter",
      "src": "$.a === 1 && $.b < 2",
      "kind": "output",
      "was": "{\"a\":1,\"b\":{\"$lt\":2}}",
      "now": "{\"a\":{\"$eq\":1,\"$not\":{\"$type\":\"array\"}},\"b\":{\"$lt\":2,\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.tags.includes('x')": {
      "entry": "filter",
      "src": "$.tags.includes('x')",
      "kind": "output",
      "was": "{\"tags\":\"x\"}",
      "now": "{\"$or\":[{\"tags\":{\"$eq\":\"x\",\"$type\":\"array\"}},{\"tags\":{\"$regex\":\"x\",\"$not\":{\"$type\":\"array\"}}}]}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.s.match(/^a/)": {
      "entry": "filter",
      "src": "$.s.match(/^a/)",
      "kind": "output",
      "was": "{\"s\":\"«regexp:^a/»\"}",
      "now": "{\"s\":{\"$regex\":\"«regexp:^a/»\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.items.some(i => i.q > 2)": {
      "entry": "filter",
      "src": "$.items.some(i => i.q > 2)",
      "kind": "output",
      "was": "{\"items\":{\"$elemMatch\":{\"q\":{\"$gt\":2}}}}",
      "now": "{\"items\":{\"$elemMatch\":{\"q\":{\"$gt\":2,\"$not\":{\"$type\":\"array\"}}}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000typeof $.x === 'string'": {
      "entry": "filter",
      "src": "typeof $.x === 'string'",
      "kind": "output",
      "was": "{\"x\":{\"$type\":\"string\"}}",
      "now": "{\"x\":{\"$type\":\"string\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.x % 4 === 1": {
      "entry": "filter",
      "src": "$.x % 4 === 1",
      "kind": "output",
      "was": "{\"x\":{\"$mod\":[4,1]}}",
      "now": "{\"x\":{\"$mod\":[4,1],\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000$.x === null": {
      "entry": "filter",
      "src": "$.x === null",
      "kind": "output",
      "was": "{\"x\":{\"$type\":\"null\"}}",
      "now": "{\"x\":{\"$type\":\"null\",\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000['a','b'].includes($.s)": {
      "entry": "filter",
      "src": "['a','b'].includes($.s)",
      "kind": "output",
      "was": "{\"s\":{\"$in\":[\"a\",\"b\"]}}",
      "now": "{\"s\":{\"$in\":[\"a\",\"b\"],\"$not\":{\"$type\":\"array\"}}}",
      "reason": "intended: a JavaScript spelling reads the field's OWN value. The shipped shape also selects a document whose field is an ARRAY holding the value, which JavaScript's `===` (and `typeof`, and an ordered comparison) never does, and it TRAVERSES an array in the middle of a path, which JavaScript reads as undefined. The exclusion is the developer's ruling of 2026-09-04, measured against a JavaScript oracle in test/compiler-js-agreement.test.ts, and it keeps the index bounds (measured: IXSCAN over [1, 1], the same as the shipped shape)."
    },
    "filter\u0000!($.a > 0)": {
      "entry": "filter",
      "src": "!($.a > 0)",
      "kind": "output",
      "was": "{\"$expr\":{\"$not\":{\"$gt\":[\"$a\",0]}}}",
      "now": "{\"$nor\":[{\"a\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}]}",
      "reason": "intended: `!p` is the COMPLEMENT of p's own query clause. The shipped `$expr` negation orders across BSON types, so `{ $not: { $gt: [\"$a\", 0] } }` is false for `a: [0, 20]` and for a string, where JavaScript answers true for both — measured, and `p || !p` stopped being a tautology. `$nor` over the native clause restores it. Only a clause with no `$expr` inside is complemented; anything else keeps the truth road."
    },
    "pipeline\u0000$.x > 0 ? $.x : assert($.x, 'no')": {
      "entry": "pipeline",
      "src": "$.x > 0 ? $.x : assert($.x, 'no')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.r = $$$.orders.map(o => { $sort({ x: -1 }); return o.total; });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.map(o => { $sort({ x: -1 }); return o.total; });",
      "kind": "message",
      "was": "`.map(o => { … })` takes a JavaScript callback, so its block body holds `const`/`let` bindings and one `return <expr>`. `$sort(...)` is a pipeline stage, not part of a callback — the chain is consumed as a value here, so",
      "now": "A callback block with a 'return' may only declare values before it at position 46",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$.r = $$$.orders.filter(o => { const t = o.total; });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.filter(o => { const t = o.total; });",
      "kind": "message",
      "was": "`.filter(o => { … })` must end with `return <expr>` — a block body that returns nothing has no value to use. Write `.filter(o => <expr>)`, or `{ const a = …; return <expr>; }` to bind first.",
      "now": "A block body must end with a `return <expr>` statement at position 50, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000 bin. These spawn ": {
      "entry": "pipeline",
      "src": " bin. These spawn ",
      "kind": "message",
      "was": "Unexpected token 'spawn' at position 12",
      "now": "Expected ';' but got 'spawn' at position 12",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$abs(42)": {
      "entry": "pipeline",
      "src": "$abs(42)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$toLower(\"Hello\")": {
      "entry": "pipeline",
      "src": "$toLower(\"Hello\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$not(true)": {
      "entry": "pipeline",
      "src": "$not(true)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$not(null)": {
      "entry": "pipeline",
      "src": "$not(null)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$abs($.delta)": {
      "entry": "pipeline",
      "src": "$abs($.delta)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$year($.createdAt)": {
      "entry": "pipeline",
      "src": "$year($.createdAt)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$abs($.address.city)": {
      "entry": "pipeline",
      "src": "$abs($.address.city)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$not($.active)": {
      "entry": "pipeline",
      "src": "$not($.active)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$size($.items)": {
      "entry": "pipeline",
      "src": "$size($.items)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$toLower($.name)": {
      "entry": "pipeline",
      "src": "$toLower($.name)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$eq($.age, 18)": {
      "entry": "pipeline",
      "src": "$eq($.age, 18)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$gt($.age, 18)": {
      "entry": "pipeline",
      "src": "$gt($.age, 18)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$gt($.x)": {
      "entry": "pipeline",
      "src": "$gt($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$eq(5)": {
      "entry": "pipeline",
      "src": "$eq(5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$lte($.score)": {
      "entry": "pipeline",
      "src": "$lte($.score)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000{ x: $gt($.y) }": {
      "entry": "pipeline",
      "src": "{ x: $gt($.y) }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000{ score: $lte(80) }": {
      "entry": "pipeline",
      "src": "{ score: $lte(80) }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$in([1, 2, 3])": {
      "entry": "pipeline",
      "src": "$in([1, 2, 3])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$in($.x, $.arr)": {
      "entry": "pipeline",
      "src": "$in($.x, $.arr)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$add($.a, $.b, $.c)": {
      "entry": "pipeline",
      "src": "$add($.a, $.b, $.c)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$setUnion([$.a, $.b])": {
      "entry": "pipeline",
      "src": "$setUnion([$.a, $.b])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$setUnion($.a, $.b)": {
      "entry": "pipeline",
      "src": "$setUnion($.a, $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$divide([10, 2])": {
      "entry": "pipeline",
      "src": "$divide([10, 2])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$setUnion($.a)": {
      "entry": "pipeline",
      "src": "$setUnion($.a)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$divide(10)": {
      "entry": "pipeline",
      "src": "$divide(10)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$and(true)": {
      "entry": "pipeline",
      "src": "$and(true)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000({ $setUnion: $.x })": {
      "entry": "pipeline",
      "src": "({ $setUnion: $.x })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000({ $add: 5 })": {
      "entry": "pipeline",
      "src": "({ $add: 5 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000({ $setUnion: [$.a, $.b] })": {
      "entry": "pipeline",
      "src": "({ $setUnion: [$.a, $.b] })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$and($gt($.age, 18), $eq($.status, \"active\"))": {
      "entry": "pipeline",
      "src": "$and($gt($.age, 18), $eq($.status, \"active\"))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$or($eq($.a, 1), $eq($.b, 2))": {
      "entry": "pipeline",
      "src": "$or($eq($.a, 1), $eq($.b, 2))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$in($.status, [\"active\", \"pending\"])": {
      "entry": "pipeline",
      "src": "$in($.status, [\"active\", \"pending\"])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$ifNull($.nickname, $.firstName, \"Unknown\")": {
      "entry": "pipeline",
      "src": "$ifNull($.nickname, $.firstName, \"Unknown\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$multiply($add($.a, $.b), 2)": {
      "entry": "pipeline",
      "src": "$multiply($add($.a, $.b), 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$trim({ input: $.name, chars: ' ' })": {
      "entry": "pipeline",
      "src": "$trim({ input: $.name, chars: ' ' })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$replaceOne({ input: $.text, find: \"old\", replacement: \"new\" })": {
      "entry": "pipeline",
      "src": "$replaceOne({ input: $.text, find: \"old\", replacement: \"new\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateAdd({ startDate: $.date, unit: \"day\", amount: 7 })": {
      "entry": "pipeline",
      "src": "$dateAdd({ startDate: $.date, unit: \"day\", amount: 7 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$trim($.name, ' ')": {
      "entry": "pipeline",
      "src": "$trim($.name, ' ')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$trim($.name)": {
      "entry": "pipeline",
      "src": "$trim($.name)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$replaceOne($.text, \"old\", \"new\")": {
      "entry": "pipeline",
      "src": "$replaceOne($.text, \"old\", \"new\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$getField(\"fieldName\", $.doc)": {
      "entry": "pipeline",
      "src": "$getField(\"fieldName\", $.doc)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$switch({ branches: [{ case: $eq($.tier, \"gold\"), then: 0.2 }, { case: $eq($.tier, \"silver\"), then: 0.1 }], default: 0 })": {
      "entry": "pipeline",
      "src": "$switch({ branches: [{ case: $eq($.tier, \"gold\"), then: 0.2 }, { case: $eq($.tier, \"silver\"), then: 0.1 }], default: 0 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateTrunc($.createdAt, \"day\")": {
      "entry": "pipeline",
      "src": "$dateTrunc($.createdAt, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateFromString($.dateString)": {
      "entry": "pipeline",
      "src": "$dateFromString($.dateString)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateAdd({ startDate: $.t, amount: 5 })": {
      "entry": "pipeline",
      "src": "$dateAdd({ startDate: $.t, amount: 5 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$cond({ if: $.a, then: 1 })": {
      "entry": "pipeline",
      "src": "$cond({ if: $.a, then: 1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$filter({ input: $.a })": {
      "entry": "pipeline",
      "src": "$filter({ input: $.a })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$convert({ input: $.v })": {
      "entry": "pipeline",
      "src": "$convert({ input: $.v })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$cond({ iff: $.a, then: 1, else: 2 })": {
      "entry": "pipeline",
      "src": "$cond({ iff: $.a, then: 1, else: 2 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateAdd({ startdate: $.t, unit: \"day\", amount: 1 })": {
      "entry": "pipeline",
      "src": "$dateAdd({ startdate: $.t, unit: \"day\", amount: 1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$filter({ input: $.a, conds: $.x })": {
      "entry": "pipeline",
      "src": "$filter({ input: $.a, conds: $.x })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateAdd({ startDate: $.t, unit: \"day\", amount: 1, timezone: \"UTC\" })": {
      "entry": "pipeline",
      "src": "$dateAdd({ startDate: $.t, unit: \"day\", amount: 1, timezone: \"UTC\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$filter({ input: $.a, cond: $.keep, as: 'i', limit: 3 })": {
      "entry": "pipeline",
      "src": "$filter({ input: $.a, cond: $.keep, as: 'i', limit: 3 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateTrunc($.t, \"day\")": {
      "entry": "pipeline",
      "src": "$dateTrunc($.t, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$mergeObjects({ a: 1, bogus: 2 })": {
      "entry": "pipeline",
      "src": "$mergeObjects({ a: 1, bogus: 2 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$divide(6, 2, 1)": {
      "entry": "pipeline",
      "src": "$divide(6, 2, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$cmp([1, 2, 3])": {
      "entry": "pipeline",
      "src": "$cmp([1, 2, 3])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$substrCP($.s, 0, 2, 3)": {
      "entry": "pipeline",
      "src": "$substrCP($.s, 0, 2, 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$arrayElemAt($.a, 0, 1)": {
      "entry": "pipeline",
      "src": "$arrayElemAt($.a, 0, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$slice([$.a])": {
      "entry": "pipeline",
      "src": "$slice([$.a])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$ifNull([$.x])": {
      "entry": "pipeline",
      "src": "$ifNull([$.x])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$add(1, 2, 3, 4)": {
      "entry": "pipeline",
      "src": "$add(1, 2, 3, 4)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$setUnion($.a, $.b, $.c)": {
      "entry": "pipeline",
      "src": "$setUnion($.a, $.b, $.c)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$divide(6, 2)": {
      "entry": "pipeline",
      "src": "$divide(6, 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$divide([6, 2])": {
      "entry": "pipeline",
      "src": "$divide([6, 2])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$round($.x)": {
      "entry": "pipeline",
      "src": "$round($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$round($.x, 2)": {
      "entry": "pipeline",
      "src": "$round($.x, 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$slice($.a, 0, 3)": {
      "entry": "pipeline",
      "src": "$slice($.a, 0, 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$gt($.a)": {
      "entry": "pipeline",
      "src": "$gt($.a)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$gt($.a, $.b, $.c)": {
      "entry": "pipeline",
      "src": "$gt($.a, $.b, $.c)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000{ age: $gt($.x) }": {
      "entry": "pipeline",
      "src": "{ age: $gt($.x) }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000{ tier: $eq(5) }": {
      "entry": "pipeline",
      "src": "{ tier: $eq(5) }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$gt($.a, $.b)": {
      "entry": "pipeline",
      "src": "$gt($.a, $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$max($.scores)": {
      "entry": "pipeline",
      "src": "$max($.scores)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$sum($.amount)": {
      "entry": "pipeline",
      "src": "$sum($.amount)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateAdd({ startDate: $.t, unit: \"fortnight\", amount: 5 })": {
      "entry": "pipeline",
      "src": "$dateAdd({ startDate: $.t, unit: \"fortnight\", amount: 5 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateTrunc({ date: $.t, unit: \"Day\" })": {
      "entry": "pipeline",
      "src": "$dateTrunc({ date: $.t, unit: \"Day\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateDiff({ startDate: $.a, endDate: $.b, unit: \"day\", startOfWeek: \"funday\" })": {
      "entry": "pipeline",
      "src": "$dateDiff({ startDate: $.a, endDate: $.b, unit: \"day\", startOfWeek: \"funday\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateTrunc({ date: $.t, unit: \"week\", startOfWeek: \"Monday\" })": {
      "entry": "pipeline",
      "src": "$dateTrunc({ date: $.t, unit: \"week\", startOfWeek: \"Monday\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$convert({ input: $.s, to: \"intt\" })": {
      "entry": "pipeline",
      "src": "$convert({ input: $.s, to: \"intt\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$convert({ input: $.s, to: \"int\" })": {
      "entry": "pipeline",
      "src": "$convert({ input: $.s, to: \"int\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$convert({ input: $.s, to: 16 })": {
      "entry": "pipeline",
      "src": "$convert({ input: $.s, to: 16 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$regexMatch({ input: $.s, regex: \"a\", options: \"gi\" })": {
      "entry": "pipeline",
      "src": "$regexMatch({ input: $.s, regex: \"a\", options: \"gi\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$regexMatch({ input: $.s, regex: \"a\", options: \"im\" })": {
      "entry": "pipeline",
      "src": "$regexMatch({ input: $.s, regex: \"a\", options: \"im\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$function({ body: \"function(){}\", args: [], lang: \"python\" })": {
      "entry": "pipeline",
      "src": "$function({ body: \"function(){}\", args: [], lang: \"python\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateAdd({ startDate: $.t, unit: $.u, amount: 5 })": {
      "entry": "pipeline",
      "src": "$dateAdd({ startDate: $.t, unit: $.u, amount: 5 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$year(\"2020-01-01\")": {
      "entry": "pipeline",
      "src": "$year(\"2020-01-01\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$hour(5)": {
      "entry": "pipeline",
      "src": "$hour(5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateAdd({ startDate: \"2020-01-01\", unit: \"day\", amount: 1 })": {
      "entry": "pipeline",
      "src": "$dateAdd({ startDate: \"2020-01-01\", unit: \"day\", amount: 1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateAdd({ startDate: $.t, unit: \"day\", amount: \"3\" })": {
      "entry": "pipeline",
      "src": "$dateAdd({ startDate: $.t, unit: \"day\", amount: \"3\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateAdd({ startDate: $.t, unit: \"day\", amount: 3.5 })": {
      "entry": "pipeline",
      "src": "$dateAdd({ startDate: $.t, unit: \"day\", amount: 3.5 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateAdd({ startDate: $.t, unit: \"day\", amount: 1, timezone: 5 })": {
      "entry": "pipeline",
      "src": "$dateAdd({ startDate: $.t, unit: \"day\", amount: 1, timezone: 5 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateDiff({ startDate: \"2020\", endDate: $.b, unit: \"day\" })": {
      "entry": "pipeline",
      "src": "$dateDiff({ startDate: \"2020\", endDate: $.b, unit: \"day\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateTrunc({ date: \"2020\", unit: \"day\" })": {
      "entry": "pipeline",
      "src": "$dateTrunc({ date: \"2020\", unit: \"day\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$year(\"$createdAt\")": {
      "entry": "pipeline",
      "src": "$year(\"$createdAt\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateAdd({ startDate: new Date(\"2020-01-01\"), unit: \"day\", amount: 1 })": {
      "entry": "pipeline",
      "src": "$dateAdd({ startDate: new Date(\"2020-01-01\"), unit: \"day\", amount: 1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateAdd({ startDate: $.t, unit: 'day', amount: -3 })": {
      "entry": "pipeline",
      "src": "$dateAdd({ startDate: $.t, unit: 'day', amount: -3 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$abs(\"x\")": {
      "entry": "pipeline",
      "src": "$abs(\"x\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$sqrt(true)": {
      "entry": "pipeline",
      "src": "$sqrt(true)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$multiply($.a, \"x\")": {
      "entry": "pipeline",
      "src": "$multiply($.a, \"x\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$add($.price, \"x\")": {
      "entry": "pipeline",
      "src": "$add($.price, \"x\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$bitNot(5.5)": {
      "entry": "pipeline",
      "src": "$bitNot(5.5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$bitAnd($.a, 2.5)": {
      "entry": "pipeline",
      "src": "$bitAnd($.a, 2.5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$bitNot(\"x\")": {
      "entry": "pipeline",
      "src": "$bitNot(\"x\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$mergeObjects(\"hello\")": {
      "entry": "pipeline",
      "src": "$mergeObjects(\"hello\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$objectToArray(5)": {
      "entry": "pipeline",
      "src": "$objectToArray(5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$size(\"hello\")": {
      "entry": "pipeline",
      "src": "$size(\"hello\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$reverseArray(5)": {
      "entry": "pipeline",
      "src": "$reverseArray(5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$group({ _id: 1, t: $tsSecond('x') });": {
      "entry": "pipeline",
      "src": "$group({ _id: 1, t: $tsSecond('x') });",
      "kind": "message",
      "was": "'$tsSecond' expects a timestamp, but got a string. Use a field path (a timestamp has no literal form).",
      "now": "'$tsSecond' is not valid in a $group output position — see its 'where'.",
      "reason": "message: both refuse the name in that position; the new text is the row's own."
    },
    "pipeline\u0000$abs(\"$delta\")": {
      "entry": "pipeline",
      "src": "$abs(\"$delta\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$add($.price, 10)": {
      "entry": "pipeline",
      "src": "$add($.price, 10)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$bitNot($.flags)": {
      "entry": "pipeline",
      "src": "$bitNot($.flags)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$mergeObjects($.a, { x: 1 })": {
      "entry": "pipeline",
      "src": "$mergeObjects($.a, { x: 1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$and([])": {
      "entry": "pipeline",
      "src": "$and([])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$or([])": {
      "entry": "pipeline",
      "src": "$or([])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$concat([])": {
      "entry": "pipeline",
      "src": "$concat([])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$setUnion([$.a])": {
      "entry": "pipeline",
      "src": "$setUnion([$.a])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$setIntersection([$.a])": {
      "entry": "pipeline",
      "src": "$setIntersection([$.a])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateFromParts({ year: 2020, month: 13 })": {
      "entry": "pipeline",
      "src": "$dateFromParts({ year: 2020, month: 13 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$sampleRate(0.1)": {
      "entry": "pipeline",
      "src": "$sampleRate(0.1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.age > 18 && $sampleRate(0.1)": {
      "entry": "pipeline",
      "src": "$.age > 18 && $sampleRate(0.1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x === /foo/": {
      "entry": "pipeline",
      "src": "$.x === /foo/",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$rand()": {
      "entry": "pipeline",
      "src": "$rand()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$createObjectId()": {
      "entry": "pipeline",
      "src": "$createObjectId()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$count()": {
      "entry": "pipeline",
      "src": "$count()",
      "kind": "message",
      "was": "Element 0 of pipeline is not a recognised stage. Expected `{ $stage: ... }` or `$stage(...)` where $stage is one of: $addFields, $bucket, $bucketAuto, $changeStream, $changeStreamSplitLargeEvent, $collStats, $count, $cur",
      "now": "'$count'(body) requires exactly 1 argument, got 0",
      "reason": "message: both refuse the argument count; the new wording names the parameter from the row's own signature."
    },
    "pipeline\u0000$rand(1, 2)": {
      "entry": "pipeline",
      "src": "$rand(1, 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$createObjectId($.x)": {
      "entry": "pipeline",
      "src": "$createObjectId($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$count(5)": {
      "entry": "pipeline",
      "src": "$count(5)",
      "kind": "message",
      "was": "'$count' expects a field-name string, but got a number.",
      "now": "'$count' names a field to WRITE, and a number is not a name. Pass a plain field name, e.g. 'total'.",
      "reason": "message: both refuse a non-name in a field-name slot; the new wording says what a name is."
    },
    "pipeline\u0000$rand({ x: 1 })": {
      "entry": "pipeline",
      "src": "$rand({ x: 1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$setWindowFields({ sortBy: { t: 1 }, output: { r: $rank($.x) } });": {
      "entry": "pipeline",
      "src": "$setWindowFields({ sortBy: { t: 1 }, output: { r: $rank($.x) } });",
      "kind": "message",
      "was": "$rank() takes no arguments, got 1. Its value is computed from the '$setWindowFields' sortBy ordering — set sortBy on the stage, don't pass a field.",
      "now": "$rank() takes no arguments, got 1",
      "reason": "message: both refuse the argument count; the new wording names the parameter from the row's own signature."
    },
    "pipeline\u0000$someNewOp()": {
      "entry": "pipeline",
      "src": "$someNewOp()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$someOp($.a)": {
      "entry": "pipeline",
      "src": "$someOp($.a)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$someOp({ key: \"val\" })": {
      "entry": "pipeline",
      "src": "$someOp({ key: \"val\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$someNewOp($.a, $.b)": {
      "entry": "pipeline",
      "src": "$someNewOp($.a, $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$in($.x, [1, 2, 3])": {
      "entry": "pipeline",
      "src": "$in($.x, [1, 2, 3])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo([1, [2, 3]])": {
      "entry": "pipeline",
      "src": "$foo([1, [2, 3]])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo([...$.arr])": {
      "entry": "pipeline",
      "src": "$foo([...$.arr])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo([...$.a, ...$.b])": {
      "entry": "pipeline",
      "src": "$foo([...$.a, ...$.b])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo([1, 2, ...$.rest])": {
      "entry": "pipeline",
      "src": "$foo([1, 2, ...$.rest])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo([...$.base, 1, 2])": {
      "entry": "pipeline",
      "src": "$foo([...$.base, 1, 2])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo([1, ...$.mid, 2])": {
      "entry": "pipeline",
      "src": "$foo([1, ...$.mid, 2])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo([1, ...$.a, 2, ...$.b, 3])": {
      "entry": "pipeline",
      "src": "$foo([1, ...$.a, 2, ...$.b, 3])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo([...[1, 2], 3])": {
      "entry": "pipeline",
      "src": "$foo([...[1, 2], 3])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.map(x => [...$.prefix, x])": {
      "entry": "pipeline",
      "src": "$.xs.map(x => [...$.prefix, x])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo([])": {
      "entry": "pipeline",
      "src": "$foo([])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo([1, 2, 3])": {
      "entry": "pipeline",
      "src": "$foo([1, 2, 3])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo([[...$.a]])": {
      "entry": "pipeline",
      "src": "$foo([[...$.a]])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo({ a: 1 }, $.b)": {
      "entry": "pipeline",
      "src": "$foo({ a: 1 }, $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo({ ...$.base })": {
      "entry": "pipeline",
      "src": "$foo({ ...$.base })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo({ ...$.a, ...$.b })": {
      "entry": "pipeline",
      "src": "$foo({ ...$.a, ...$.b })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo({ x: 1, y: 2, ...$.rest })": {
      "entry": "pipeline",
      "src": "$foo({ x: 1, y: 2, ...$.rest })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo({ ...$.base, x: 1 })": {
      "entry": "pipeline",
      "src": "$foo({ ...$.base, x: 1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo({ x: 1, ...$.mid, y: 2 })": {
      "entry": "pipeline",
      "src": "$foo({ x: 1, ...$.mid, y: 2 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo({ ...$.base, [$.k]: $.v })": {
      "entry": "pipeline",
      "src": "$foo({ ...$.base, [$.k]: $.v })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.statuses.reduce((acc, s) => ({ ...acc, [s]: (acc[s] ?? 0) + 1 }), {})": {
      "entry": "pipeline",
      "src": "$.statuses.reduce((acc, s) => ({ ...acc, [s]: (acc[s] ?? 0) + 1 }), {})",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$replaceOne({ ...$.opts, input: $.s })": {
      "entry": "pipeline",
      "src": "$replaceOne({ ...$.opts, input: $.s })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$cond($.age, \"adult\", \"minor\")": {
      "entry": "pipeline",
      "src": "$cond($.age, \"adult\", \"minor\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$cond({ if: $.active, then: \"yes\", else: \"no\" })": {
      "entry": "pipeline",
      "src": "$cond({ if: $.active, then: \"yes\", else: \"no\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$eq(age, 18)": {
      "entry": "pipeline",
      "src": "$eq(age, 18)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$eq($.age, 18) garbage": {
      "entry": "pipeline",
      "src": "$eq($.age, 18) garbage",
      "kind": "message",
      "was": "Unexpected token 'garbage' at position 15",
      "now": "Expected ';' but got 'garbage' at position 15",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$abs(-5)": {
      "entry": "pipeline",
      "src": "$abs(-5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a + $.b": {
      "entry": "pipeline",
      "src": "$.a + $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a - $.b": {
      "entry": "pipeline",
      "src": "$.a - $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a * 1.1": {
      "entry": "pipeline",
      "src": "$.a * 1.1",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a / $.b": {
      "entry": "pipeline",
      "src": "$.a / $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a % 2": {
      "entry": "pipeline",
      "src": "$.a % 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.base ** 2": {
      "entry": "pipeline",
      "src": "$.base ** 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u00002 ** 3 ** 2": {
      "entry": "pipeline",
      "src": "2 ** 3 ** 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.status == null": {
      "entry": "pipeline",
      "src": "$.status == null",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.status === 'active'": {
      "entry": "pipeline",
      "src": "$.status === 'active'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.status === null": {
      "entry": "pipeline",
      "src": "$.status === null",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.status != null": {
      "entry": "pipeline",
      "src": "$.status != null",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.status !== null": {
      "entry": "pipeline",
      "src": "$.status !== null",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.status == 'active'": {
      "entry": "pipeline",
      "src": "$.status == 'active'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.status != 'active'": {
      "entry": "pipeline",
      "src": "$.status != 'active'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x == 5": {
      "entry": "pipeline",
      "src": "$.x == 5",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000null == $.status": {
      "entry": "pipeline",
      "src": "null == $.status",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.age > 18": {
      "entry": "pipeline",
      "src": "$.age > 18",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.age >= 21": {
      "entry": "pipeline",
      "src": "$.age >= 21",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.score < 50": {
      "entry": "pipeline",
      "src": "$.score < 50",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.score <= 100": {
      "entry": "pipeline",
      "src": "$.score <= 100",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.status in [\"active\", \"pending\"]": {
      "entry": "pipeline",
      "src": "$.status in [\"active\", \"pending\"]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.filter(x => x.name)": {
      "entry": "pipeline",
      "src": "$.items.filter(x => x.name)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a ? $.b : $.c": {
      "entry": "pipeline",
      "src": "$.a ? $.b : $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000!$.a": {
      "entry": "pipeline",
      "src": "!$.a",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a && $.b": {
      "entry": "pipeline",
      "src": "$.a && $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a || $.b": {
      "entry": "pipeline",
      "src": "$.a || $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a > 0 && $.b > 0": {
      "entry": "pipeline",
      "src": "$.a > 0 && $.b > 0",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a > 0 || $.b > 0": {
      "entry": "pipeline",
      "src": "$.a > 0 || $.b > 0",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000!$.active": {
      "entry": "pipeline",
      "src": "!$.active",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000!!$.active": {
      "entry": "pipeline",
      "src": "!!$.active",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000!($.a > 0)": {
      "entry": "pipeline",
      "src": "!($.a > 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000($.a + $.b) && $.c": {
      "entry": "pipeline",
      "src": "($.a + $.b) && $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.nickname || \"anonymous\"": {
      "entry": "pipeline",
      "src": "$.nickname || \"anonymous\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.age >= 18 ? 'adult' : 'minor'": {
      "entry": "pipeline",
      "src": "$.age >= 18 ? 'adult' : 'minor'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name ? \"yes\" : \"no\"": {
      "entry": "pipeline",
      "src": "$.name ? \"yes\" : \"no\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a ? 'x' : $.b ? 'y' : 'z'": {
      "entry": "pipeline",
      "src": "$.a ? 'x' : $.b ? 'y' : 'z'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.nickname ?? $.name": {
      "entry": "pipeline",
      "src": "$.nickname ?? $.name",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a ?? $.b ?? 'unknown'": {
      "entry": "pipeline",
      "src": "$.a ?? $.b ?? 'unknown'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000-$.amount": {
      "entry": "pipeline",
      "src": "-$.amount",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000-5": {
      "entry": "pipeline",
      "src": "-5",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000-($.a + $.b)": {
      "entry": "pipeline",
      "src": "-($.a + $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a + $.b + $.c": {
      "entry": "pipeline",
      "src": "$.a + $.b + $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a * $.b * $.c": {
      "entry": "pipeline",
      "src": "$.a * $.b * $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a > 0 && $.b > 0 && $.c > 0": {
      "entry": "pipeline",
      "src": "$.a > 0 && $.b > 0 && $.c > 0",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a > 0 || $.b > 0 || $.c > 0": {
      "entry": "pipeline",
      "src": "$.a > 0 || $.b > 0 || $.c > 0",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a && $.b && $.c": {
      "entry": "pipeline",
      "src": "$.a && $.b && $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a || $.b || $.c": {
      "entry": "pipeline",
      "src": "$.a || $.b || $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a ?? $.b ?? $.c ?? 0": {
      "entry": "pipeline",
      "src": "$.a ?? $.b ?? $.c ?? 0",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a - $.b - $.c": {
      "entry": "pipeline",
      "src": "$.a - $.b - $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.first + \" \" + $.last": {
      "entry": "pipeline",
      "src": "$.first + \" \" + $.last",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a + \"\"": {
      "entry": "pipeline",
      "src": "$.a + \"\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$toString($.n) + $.s": {
      "entry": "pipeline",
      "src": "$toString($.n) + $.s",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.prefix + $toLower($.name)": {
      "entry": "pipeline",
      "src": "$.prefix + $toLower($.name)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.count + \" items\"": {
      "entry": "pipeline",
      "src": "$.count + \" items\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items[0]": {
      "entry": "pipeline",
      "src": "$.items[0]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items[-1]": {
      "entry": "pipeline",
      "src": "$.items[-1]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.tags.uniq()[-2]": {
      "entry": "pipeline",
      "src": "$.tags.uniq()[-2]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.doc[$.k]": {
      "entry": "pipeline",
      "src": "$.doc[$.k]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.doc[\"host\"]": {
      "entry": "pipeline",
      "src": "$.doc[\"host\"]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.doc[$.k.toLowerCase()]": {
      "entry": "pipeline",
      "src": "$.doc[$.k.toLowerCase()]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000({ 0: 1, 1.5: 2, 0x10: 3 })": {
      "entry": "pipeline",
      "src": "({ 0: 1, 1.5: 2, 0x10: 3 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$[0]": {
      "entry": "pipeline",
      "src": "$[0]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.doc[\"a\"][0]": {
      "entry": "pipeline",
      "src": "$.doc[\"a\"][0]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items[$.idx]": {
      "entry": "pipeline",
      "src": "$.items[$.idx]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.config[\"host\"]": {
      "entry": "pipeline",
      "src": "$.config[\"host\"]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.map[$.key.toLowerCase()]": {
      "entry": "pipeline",
      "src": "$.map[$.key.toLowerCase()]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$[\"field.length\"]": {
      "entry": "pipeline",
      "src": "$[\"field.length\"]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$[\"weird-name\"]": {
      "entry": "pipeline",
      "src": "$[\"weird-name\"]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$[\"field.length\"] * $.field.width": {
      "entry": "pipeline",
      "src": "$[\"field.length\"] * $.field.width",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$[$.fieldName]": {
      "entry": "pipeline",
      "src": "$[$.fieldName]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000({ a: 1, b: 2 })[$.k]": {
      "entry": "pipeline",
      "src": "({ a: 1, b: 2 })[$.k]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.m[$.r][$.c]": {
      "entry": "pipeline",
      "src": "$.m[$.r][$.c]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$reverseArray($.items)[0]": {
      "entry": "pipeline",
      "src": "$reverseArray($.items)[0]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => x.id)[0]": {
      "entry": "pipeline",
      "src": "$.items.map(x => x.id)[0]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[\"sender\", \"recipient\"].map(party => $.cre.result[party])": {
      "entry": "pipeline",
      "src": "[\"sender\", \"recipient\"].map(party => $.cre.result[party])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[\"a\", \"b\"].filter(k => $.m[k])": {
      "entry": "pipeline",
      "src": "[\"a\", \"b\"].filter(k => $.m[k])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[\"a\", \"b\"].reduce((acc, k) => acc + $.m[k], \"\")": {
      "entry": "pipeline",
      "src": "[\"a\", \"b\"].reduce((acc, k) => acc + $.m[k], \"\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.csv.split(\",\").map(k => $.m[k])": {
      "entry": "pipeline",
      "src": "$.csv.split(\",\").map(k => $.m[k])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[{ a: 1 }, { b: 2 }].map(o => o[$.k])": {
      "entry": "pipeline",
      "src": "[{ a: 1 }, { b: 2 }].map(o => o[$.k])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1, 2, 3].map(i => $.m[i])": {
      "entry": "pipeline",
      "src": "[1, 2, 3].map(i => $.m[i])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.tags.map(t => $.m[t])": {
      "entry": "pipeline",
      "src": "$.tags.map(t => $.m[t])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[\"a\", \"b\"].map((k, i) => $.m[i])": {
      "entry": "pipeline",
      "src": "[\"a\", \"b\"].map((k, i) => $.m[i])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000($.a + $.b) * 2": {
      "entry": "pipeline",
      "src": "($.a + $.b) * 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a + $.b * 2": {
      "entry": "pipeline",
      "src": "$.a + $.b * 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a + $.b * $.c": {
      "entry": "pipeline",
      "src": "$.a + $.b * $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.age > 18 && $.active": {
      "entry": "pipeline",
      "src": "$.age > 18 && $.active",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a || $.b && $.c": {
      "entry": "pipeline",
      "src": "$.a || $.b && $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000!$.a && $.b": {
      "entry": "pipeline",
      "src": "!$.a && $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$and($.age > 18, $.status === 'active')": {
      "entry": "pipeline",
      "src": "$and($.age > 18, $.status === 'active')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$abs($.a - $.b)": {
      "entry": "pipeline",
      "src": "$abs($.a - $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.in === 'test'": {
      "entry": "pipeline",
      "src": "$.in === 'test'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$size($.in)": {
      "entry": "pipeline",
      "src": "$size($.in)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.b.c": {
      "entry": "pipeline",
      "src": "$.a.b.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items[0].name": {
      "entry": "pipeline",
      "src": "$.items[0].name",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.0": {
      "entry": "pipeline",
      "src": "$.items.0",
      "kind": "message",
      "was": "Expected property name after '.' at position 8",
      "now": "Expected a name but got '0' at position 8",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$abs($.a.b.c)": {
      "entry": "pipeline",
      "src": "$abs($.a.b.c)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.loyalty.years >= 2": {
      "entry": "pipeline",
      "src": "$.loyalty.years >= 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.trim()": {
      "entry": "pipeline",
      "src": "$.name.trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.trimStart()": {
      "entry": "pipeline",
      "src": "$.name.trimStart()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.trimLeft()": {
      "entry": "pipeline",
      "src": "$.name.trimLeft()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.trimEnd()": {
      "entry": "pipeline",
      "src": "$.name.trimEnd()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.trimRight()": {
      "entry": "pipeline",
      "src": "$.name.trimRight()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.toLowerCase()": {
      "entry": "pipeline",
      "src": "$.name.toLowerCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.toUpperCase()": {
      "entry": "pipeline",
      "src": "$.name.toUpperCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.substr(0, 5)": {
      "entry": "pipeline",
      "src": "$.name.substr(0, 5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.csv.split(\",\")": {
      "entry": "pipeline",
      "src": "$.csv.split(\",\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.indexOf(\"@\")": {
      "entry": "pipeline",
      "src": "$.name.indexOf(\"@\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.toLowerCase().indexOf(\"@\")": {
      "entry": "pipeline",
      "src": "$.name.toLowerCase().indexOf(\"@\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.replace(\"a\", \"b\")": {
      "entry": "pipeline",
      "src": "$.name.replace(\"a\", \"b\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.slug.replaceAll(\" \", \"-\")": {
      "entry": "pipeline",
      "src": "$.slug.replaceAll(\" \", \"-\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.email.includes(\"@\")": {
      "entry": "pipeline",
      "src": "$.email.includes(\"@\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.email.toLowerCase().includes(\"@\")": {
      "entry": "pipeline",
      "src": "$.email.toLowerCase().includes(\"@\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.str.match(/^[A-Z]/)": {
      "entry": "pipeline",
      "src": "$.str.match(/^[A-Z]/)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.str.match(/^[a-z]/i)": {
      "entry": "pipeline",
      "src": "$.str.match(/^[a-z]/i)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.str.match(\"^[a-z]\")": {
      "entry": "pipeline",
      "src": "$.str.match(\"^[a-z]\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.trim().length": {
      "entry": "pipeline",
      "src": "$.name.trim().length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.csv.split(\",\").length": {
      "entry": "pipeline",
      "src": "$.csv.split(\",\").length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => x).length": {
      "entry": "pipeline",
      "src": "$.items.map(x => x).length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.length": {
      "entry": "pipeline",
      "src": "$.items.length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items[\"length\"]": {
      "entry": "pipeline",
      "src": "$.items[\"length\"]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.csv.split(\",\")[\"length\"]": {
      "entry": "pipeline",
      "src": "$.csv.split(\",\")[\"length\"]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.trim().toLowerCase()": {
      "entry": "pipeline",
      "src": "$.name.trim().toLowerCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.toLowerCase().trim()": {
      "entry": "pipeline",
      "src": "$.name.toLowerCase().trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a % 5 === 0": {
      "entry": "pipeline",
      "src": "$.a % 5 === 0",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a % 5 !== 0": {
      "entry": "pipeline",
      "src": "$.a % 5 !== 0",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a === undefined": {
      "entry": "pipeline",
      "src": "$.a === undefined",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a !== undefined": {
      "entry": "pipeline",
      "src": "$.a !== undefined",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000undefined === $.a": {
      "entry": "pipeline",
      "src": "undefined === $.a",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a === null": {
      "entry": "pipeline",
      "src": "$.a === null",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.b.c === undefined": {
      "entry": "pipeline",
      "src": "$.a.b.c === undefined",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateTrunc({ date: $.d, unit: $.u })": {
      "entry": "pipeline",
      "src": "$dateTrunc({ date: $.d, unit: $.u })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateTrunc({ date: $.d, unit: \"$u\" })": {
      "entry": "pipeline",
      "src": "$dateTrunc({ date: $.d, unit: \"$u\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$merge({ into: \"x\", whenMatched: \"$g\" });": {
      "entry": "pipeline",
      "src": "$merge({ into: \"x\", whenMatched: \"$g\" });",
      "kind": "message",
      "was": "'$merge' whenMatched must be one of: replace, keepExisting, merge, fail — got '$g'.",
      "now": "This stage's body is a sub-pipeline: write it as a bracketed list of stages, '[$match(…), $sort(…)]'.",
      "reason": "message: both refuse a sub-pipeline body that is not a bracketed stage list; the new wording shows the spelling."
    },
    "pipeline\u0000typeof $.a === \"boolean\"": {
      "entry": "pipeline",
      "src": "typeof $.a === \"boolean\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.a === \"number\"": {
      "entry": "pipeline",
      "src": "typeof $.a === \"number\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.a !== \"boolean\"": {
      "entry": "pipeline",
      "src": "typeof $.a !== \"boolean\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.a !== \"number\"": {
      "entry": "pipeline",
      "src": "typeof $.a !== \"number\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.a === \"function\"": {
      "entry": "pipeline",
      "src": "typeof $.a === \"function\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.a.b.c === \"boolean\"": {
      "entry": "pipeline",
      "src": "typeof $.a.b.c === \"boolean\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.charAt()": {
      "entry": "pipeline",
      "src": "$.s.charAt()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.split(\"a\", \"b\")": {
      "entry": "pipeline",
      "src": "$.s.split(\"a\", \"b\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.replace(\"a\")": {
      "entry": "pipeline",
      "src": "$.s.replace(\"a\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.with(1)": {
      "entry": "pipeline",
      "src": "$.arr.with(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.substr()": {
      "entry": "pipeline",
      "src": "$.s.substr()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.slice(1, 2, 3)": {
      "entry": "pipeline",
      "src": "$.arr.slice(1, 2, 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.padStart()": {
      "entry": "pipeline",
      "src": "$.s.padStart()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.concat()": {
      "entry": "pipeline",
      "src": "$.arr.concat()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.toSpliced()": {
      "entry": "pipeline",
      "src": "$.arr.toSpliced()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.toReversed(1)": {
      "entry": "pipeline",
      "src": "$.arr.toReversed(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.map(5)": {
      "entry": "pipeline",
      "src": "$.a.map(5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.reduce((acc, x) => acc + x, 0)": {
      "entry": "pipeline",
      "src": "$.a.reduce((acc, x) => acc + x, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.pow(2)": {
      "entry": "pipeline",
      "src": "Math.pow(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.hypot()": {
      "entry": "pipeline",
      "src": "Math.hypot()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.random(1)": {
      "entry": "pipeline",
      "src": "Math.random(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.floor()": {
      "entry": "pipeline",
      "src": "Math.floor()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.keys()": {
      "entry": "pipeline",
      "src": "Object.keys()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.assign()": {
      "entry": "pipeline",
      "src": "Object.assign()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000/x/.test()": {
      "entry": "pipeline",
      "src": "/x/.test()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.at(0)": {
      "entry": "pipeline",
      "src": "$.items.at(0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.at(-1)": {
      "entry": "pipeline",
      "src": "$.items.at(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.tags.uniq().at(-1)": {
      "entry": "pipeline",
      "src": "$.tags.uniq().at(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.aliases.at(0) ?? \"anonymous\"": {
      "entry": "pipeline",
      "src": "$.aliases.at(0) ?? \"anonymous\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.slice(2)": {
      "entry": "pipeline",
      "src": "$.items.slice(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.slice(0, 3)": {
      "entry": "pipeline",
      "src": "$.items.slice(0, 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3,4,5].slice(1, 3)": {
      "entry": "pipeline",
      "src": "[1,2,3,4,5].slice(1, 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3,4,5].slice(3, 2)": {
      "entry": "pipeline",
      "src": "[1,2,3,4,5].slice(3, 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3,4,5].slice(2, 2)": {
      "entry": "pipeline",
      "src": "[1,2,3,4,5].slice(2, 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3,4,5].slice(-2)": {
      "entry": "pipeline",
      "src": "[1,2,3,4,5].slice(-2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].slice(0)": {
      "entry": "pipeline",
      "src": "[1,2,3].slice(0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3,4,5].slice(0, -1)": {
      "entry": "pipeline",
      "src": "[1,2,3,4,5].slice(0, -1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3,4,5].slice(1, -1)": {
      "entry": "pipeline",
      "src": "[1,2,3,4,5].slice(1, -1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.toReversed()": {
      "entry": "pipeline",
      "src": "$.items.toReversed()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.prices.map(p => p * 1.1)": {
      "entry": "pipeline",
      "src": "$.prices.map(p => p * 1.1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.prices.map((p) => p * 1.1)": {
      "entry": "pipeline",
      "src": "$.prices.map((p) => p * 1.1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.filter(x => x > 0)": {
      "entry": "pipeline",
      "src": "$.items.filter(x => x > 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.find(x => x > 0)": {
      "entry": "pipeline",
      "src": "$.items.find(x => x > 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.some(x => x > 0)": {
      "entry": "pipeline",
      "src": "$.items.some(x => x > 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.every(x => x > 0)": {
      "entry": "pipeline",
      "src": "$.items.every(x => x > 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ns.reduce((acc, x) => acc + x, 0)": {
      "entry": "pipeline",
      "src": "$.ns.reduce((acc, x) => acc + x, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => x * $.taxRate)": {
      "entry": "pipeline",
      "src": "$.items.map(x => x * $.taxRate)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.orders.filter(o => o.status === \"active\")": {
      "entry": "pipeline",
      "src": "$.orders.filter(o => o.status === \"active\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.orders.reduce((sum, o) => sum + o.price, 0)": {
      "entry": "pipeline",
      "src": "$.orders.reduce((sum, o) => sum + o.price, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce((a, k) => ({ ...a, [k]: 1 }), {})": {
      "entry": "pipeline",
      "src": "$.xs.reduce((a, k) => ({ ...a, [k]: 1 }), {})",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce((a, k) => ({ ...a, [k]: a[k] }), {})": {
      "entry": "pipeline",
      "src": "$.xs.reduce((a, k) => ({ ...a, [k]: a[k] }), {})",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce((a, x) => [...a, a[0]], [])": {
      "entry": "pipeline",
      "src": "$.xs.reduce((a, x) => [...a, a[0]], [])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce((a, x) => x.foo, {})": {
      "entry": "pipeline",
      "src": "$.xs.reduce((a, x) => x.foo, {})",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce((a, x) => a[0], {})": {
      "entry": "pipeline",
      "src": "$.xs.reduce((a, x) => a[0], {})",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce((a, k) => ({ ...a, k: a[0] }), $.seed)": {
      "entry": "pipeline",
      "src": "$.xs.reduce((a, k) => ({ ...a, k: a[0] }), $.seed)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce((a, x) => ({ ...a, k: x[0] }), {})": {
      "entry": "pipeline",
      "src": "$.xs.reduce((a, x) => ({ ...a, k: x[0] }), {})",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce((acc, x) => ({ ...acc, k: x.ys.reduce((acc, y) => [...acc, acc[0]], []) }), {})": {
      "entry": "pipeline",
      "src": "$.xs.reduce((acc, x) => ({ ...acc, k: x.ys.reduce((acc, y) => [...acc, acc[0]], []) }), {})",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce((a, k) => ({ ...a, [k]: a?.[k] }), {})": {
      "entry": "pipeline",
      "src": "$.xs.reduce((a, k) => ({ ...a, [k]: a?.[k] }), {})",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ids.map(ObjectId)": {
      "entry": "pipeline",
      "src": "$.ids.map(ObjectId)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ids.map(v => ObjectId(v))": {
      "entry": "pipeline",
      "src": "$.ids.map(v => ObjectId(v))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.sumBy(Number)": {
      "entry": "pipeline",
      "src": "$.xs.sumBy(Number)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.keyBy(ObjectId)": {
      "entry": "pipeline",
      "src": "$.xs.keyBy(ObjectId)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.map(Date)": {
      "entry": "pipeline",
      "src": "$.xs.map(Date)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.map(parseInt)": {
      "entry": "pipeline",
      "src": "$.xs.map(parseInt)",
      "kind": "message",
      "was": "Expected '(' but got ')' at position 17",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a = ObjectId": {
      "entry": "pipeline",
      "src": "$.a = ObjectId",
      "kind": "message",
      "was": "'ObjectId' used as a value is only valid as a callback to a higher-order array method (e.g. $.ids.map(ObjectId)). To convert a single value, write ObjectId(value); for a constant id, write ObjectId(\"<24 hex>\") or the 0x<",
      "now": "'ObjectId' used as a value is only valid as a callback to a higher-order array method (e.g. $.items.map(ObjectId)). To apply it to one value, write ObjectId(value).",
      "reason": "message: both refuse the name in that position; the new text is the row's own."
    },
    "pipeline\u0000$.items.filter(Boolean)": {
      "entry": "pipeline",
      "src": "$.items.filter(Boolean)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.nums.map(Number)": {
      "entry": "pipeline",
      "src": "$.nums.map(Number)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.map(String)": {
      "entry": "pipeline",
      "src": "$.xs.map(String)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.find(Boolean)": {
      "entry": "pipeline",
      "src": "$.xs.find(Boolean)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.some(Boolean)": {
      "entry": "pipeline",
      "src": "$.xs.some(Boolean)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.every(Boolean)": {
      "entry": "pipeline",
      "src": "$.xs.every(Boolean)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.flatMap(Number)": {
      "entry": "pipeline",
      "src": "$.xs.flatMap(Number)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.parts.filter(Boolean).join(\" \")": {
      "entry": "pipeline",
      "src": "$.parts.filter(Boolean).join(\" \")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Boolean + 5": {
      "entry": "pipeline",
      "src": "Boolean + 5",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce(Boolean, 0)": {
      "entry": "pipeline",
      "src": "$.xs.reduce(Boolean, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.filter(parseInt)": {
      "entry": "pipeline",
      "src": "$.xs.filter(parseInt)",
      "kind": "message",
      "was": "Expected '(' but got ')' at position 20",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.filter(parseFloat)": {
      "entry": "pipeline",
      "src": "$.xs.filter(parseFloat)",
      "kind": "message",
      "was": "Expected '(' but got ')' at position 22",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getFullYear()": {
      "entry": "pipeline",
      "src": "$.ts.getFullYear()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getMonth()": {
      "entry": "pipeline",
      "src": "$.ts.getMonth()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getDate()": {
      "entry": "pipeline",
      "src": "$.ts.getDate()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getDay()": {
      "entry": "pipeline",
      "src": "$.ts.getDay()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getHours()": {
      "entry": "pipeline",
      "src": "$.ts.getHours()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getMinutes()": {
      "entry": "pipeline",
      "src": "$.ts.getMinutes()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getSeconds()": {
      "entry": "pipeline",
      "src": "$.ts.getSeconds()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getMilliseconds()": {
      "entry": "pipeline",
      "src": "$.ts.getMilliseconds()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getFullYear(\"UTC\")": {
      "entry": "pipeline",
      "src": "$.ts.getFullYear(\"UTC\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getMonth(1, 2, 3)": {
      "entry": "pipeline",
      "src": "$.ts.getMonth(1, 2, 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getUTCHours(\"UTC\")": {
      "entry": "pipeline",
      "src": "$.ts.getUTCHours(\"UTC\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getTime(1)": {
      "entry": "pipeline",
      "src": "$.ts.getTime(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.toISOString(\"UTC\")": {
      "entry": "pipeline",
      "src": "$.ts.toISOString(\"UTC\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.week(\"UTC\")": {
      "entry": "pipeline",
      "src": "$.ts.week(\"UTC\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.week(\"America/New_York\")": {
      "entry": "pipeline",
      "src": "$.ts.week(\"America/New_York\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getUTCHours()": {
      "entry": "pipeline",
      "src": "$.ts.getUTCHours()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.subscribedAt.plus(30, \"day\")": {
      "entry": "pipeline",
      "src": "$.subscribedAt.plus(30, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.expiresAt.minus(1, \"month\")": {
      "entry": "pipeline",
      "src": "$.expiresAt.minus(1, \"month\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(2, \"hour\", \"America/New_York\")": {
      "entry": "pipeline",
      "src": "$.t.plus(2, \"hour\", \"America/New_York\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.order.createdAt.plus($.slaDays, \"day\")": {
      "entry": "pipeline",
      "src": "$.order.createdAt.plus($.slaDays, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(1, $.unit)": {
      "entry": "pipeline",
      "src": "$.t.plus(1, $.unit)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(30, \"days\")": {
      "entry": "pipeline",
      "src": "$.t.plus(30, \"days\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(30)": {
      "entry": "pipeline",
      "src": "$.t.plus(30)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(1.5, \"day\")": {
      "entry": "pipeline",
      "src": "$.t.plus(1.5, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.minus(\"30\", \"day\")": {
      "entry": "pipeline",
      "src": "$.t.minus(\"30\", \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(1, \"day\", 5)": {
      "entry": "pipeline",
      "src": "$.t.plus(1, \"day\", 5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(-5, \"day\")": {
      "entry": "pipeline",
      "src": "$.t.plus(-5, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus($.n, \"day\", $.tz)": {
      "entry": "pipeline",
      "src": "$.t.plus($.n, \"day\", $.tz)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set({ year: 2030 })": {
      "entry": "pipeline",
      "src": "$.t.set({ year: 2030 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set({ month: 1, day: 1 })": {
      "entry": "pipeline",
      "src": "$.t.set({ month: 1, day: 1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set({ year: 2030, month: 1, day: 1, hour: 0, minute: 0, second: 0, millisecond: 0 })": {
      "entry": "pipeline",
      "src": "$.t.set({ year: 2030, month: 1, day: 1, hour: 0, minute: 0, second: 0, millisecond: 0 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set({ isoWeek: 5 })": {
      "entry": "pipeline",
      "src": "$.t.set({ isoWeek: 5 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set({ year: 2030 }, \"America/New_York\")": {
      "entry": "pipeline",
      "src": "$.t.set({ year: 2030 }, \"America/New_York\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(jsmqlParts => jsmqlParts.t.set({ year: 2030 }))": {
      "entry": "pipeline",
      "src": "$.items.map(jsmqlParts => jsmqlParts.t.set({ year: 2030 }))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set({ year: 2030, isoWeek: 5 })": {
      "entry": "pipeline",
      "src": "$.t.set({ year: 2030, isoWeek: 5 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set({ yaer: 2030 })": {
      "entry": "pipeline",
      "src": "$.t.set({ yaer: 2030 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set({ timezone: \"UTC\" })": {
      "entry": "pipeline",
      "src": "$.t.set({ timezone: \"UTC\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set($.parts)": {
      "entry": "pipeline",
      "src": "$.t.set($.parts)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set({ ...$.p })": {
      "entry": "pipeline",
      "src": "$.t.set({ ...$.p })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set({ year: 2030.5 })": {
      "entry": "pipeline",
      "src": "$.t.set({ year: 2030.5 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set({ year: \"2030\" })": {
      "entry": "pipeline",
      "src": "$.t.set({ year: \"2030\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateFromParts({ year: \"2030\" })": {
      "entry": "pipeline",
      "src": "$dateFromParts({ year: \"2030\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.isSame($.b, \"day\")": {
      "entry": "pipeline",
      "src": "$.a.isSame($.b, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.isBefore($.b, \"month\")": {
      "entry": "pipeline",
      "src": "$.a.isBefore($.b, \"month\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.isAfter($.b, \"month\")": {
      "entry": "pipeline",
      "src": "$.a.isAfter($.b, \"month\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.isSame($.b, \"week\", { startOfWeek: \"monday\" })": {
      "entry": "pipeline",
      "src": "$.a.isSame($.b, \"week\", { startOfWeek: \"monday\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.isSame($.b)": {
      "entry": "pipeline",
      "src": "$.a.isSame($.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.isBefore($.b)": {
      "entry": "pipeline",
      "src": "$.a.isBefore($.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.isAfter($.b)": {
      "entry": "pipeline",
      "src": "$.a.isAfter($.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.isSame($.b, \"days\")": {
      "entry": "pipeline",
      "src": "$.a.isSame($.b, \"days\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.isSame(\"2020-01-01\", \"day\")": {
      "entry": "pipeline",
      "src": "$.a.isSame(\"2020-01-01\", \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.week()": {
      "entry": "pipeline",
      "src": "$.t.week()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.isoWeek()": {
      "entry": "pipeline",
      "src": "$.t.isoWeek()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.isoWeekYear()": {
      "entry": "pipeline",
      "src": "$.t.isoWeekYear()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.isoWeekday()": {
      "entry": "pipeline",
      "src": "$.t.isoWeekday()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.dayOfYear()": {
      "entry": "pipeline",
      "src": "$.t.dayOfYear()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.quarter()": {
      "entry": "pipeline",
      "src": "$.t.quarter()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.week(\"America/New_York\")": {
      "entry": "pipeline",
      "src": "$.t.week(\"America/New_York\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.quarter({ timezone: \"UTC\" })": {
      "entry": "pipeline",
      "src": "$.t.quarter({ timezone: \"UTC\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.week(\"UTC\", 1)": {
      "entry": "pipeline",
      "src": "$.t.week(\"UTC\", 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.quarter({ binSize: 2 })": {
      "entry": "pipeline",
      "src": "$.t.quarter({ binSize: 2 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"x\".week()": {
      "entry": "pipeline",
      "src": "\"x\".week()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.format(\"%Y-%m-%d\")": {
      "entry": "pipeline",
      "src": "$.createdAt.format(\"%Y-%m-%d\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.startOf(\"month\").format(\"%Y-%m\")": {
      "entry": "pipeline",
      "src": "$.createdAt.startOf(\"month\").format(\"%Y-%m\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.format(\"%H:%M\", \"America/New_York\")": {
      "entry": "pipeline",
      "src": "$.createdAt.format(\"%H:%M\", \"America/New_York\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.format(\"%Y\") + \"-x\"": {
      "entry": "pipeline",
      "src": "$.t.format(\"%Y\") + \"-x\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.format(\"%Q\")": {
      "entry": "pipeline",
      "src": "$.createdAt.format(\"%Q\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.format(\"%y-%m\")": {
      "entry": "pipeline",
      "src": "$.createdAt.format(\"%y-%m\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.format(\"YYYY-MM-DD HH:mm:ss\")": {
      "entry": "pipeline",
      "src": "$.createdAt.format(\"YYYY-MM-DD HH:mm:ss\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.format(\"dddd, DD MMM YYYY\")": {
      "entry": "pipeline",
      "src": "$.createdAt.format(\"dddd, DD MMM YYYY\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.format(\"hh:mm A\")": {
      "entry": "pipeline",
      "src": "$.createdAt.format(\"hh:mm A\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.format(\"Report\")": {
      "entry": "pipeline",
      "src": "$.createdAt.format(\"Report\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.format($.fmt)": {
      "entry": "pipeline",
      "src": "$.createdAt.format($.fmt)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.format(5)": {
      "entry": "pipeline",
      "src": "$.createdAt.format(5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.format()": {
      "entry": "pipeline",
      "src": "$.createdAt.format()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.format(\"%Y\", { onNull: \"\" })": {
      "entry": "pipeline",
      "src": "$.createdAt.format(\"%Y\", { onNull: \"\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.startOf(\"month\")": {
      "entry": "pipeline",
      "src": "$.createdAt.startOf(\"month\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.startOf(\"quarter\")": {
      "entry": "pipeline",
      "src": "$.createdAt.startOf(\"quarter\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.startOf(\"day\", \"America/New_York\")": {
      "entry": "pipeline",
      "src": "$.createdAt.startOf(\"day\", \"America/New_York\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.startOf(\"minute\", { binSize: 15 })": {
      "entry": "pipeline",
      "src": "$.createdAt.startOf(\"minute\", { binSize: 15 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.startOf(\"week\", { startOfWeek: \"monday\" })": {
      "entry": "pipeline",
      "src": "$.createdAt.startOf(\"week\", { startOfWeek: \"monday\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.startOf(\"month\").plus(1, \"month\")": {
      "entry": "pipeline",
      "src": "$.createdAt.startOf(\"month\").plus(1, \"month\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.startOf()": {
      "entry": "pipeline",
      "src": "$.createdAt.startOf()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.startOf(\"months\")": {
      "entry": "pipeline",
      "src": "$.createdAt.startOf(\"months\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"2020-01-01\".startOf(\"day\")": {
      "entry": "pipeline",
      "src": "\"2020-01-01\".startOf(\"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.endOf(\"month\")": {
      "entry": "pipeline",
      "src": "$.createdAt.endOf(\"month\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.endOf(\"day\", \"America/New_York\")": {
      "entry": "pipeline",
      "src": "$.createdAt.endOf(\"day\", \"America/New_York\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.endOf(\"minute\", { binSize: 15 })": {
      "entry": "pipeline",
      "src": "$.createdAt.endOf(\"minute\", { binSize: 15 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.endOf(\"week\", { startOfWeek: \"monday\" })": {
      "entry": "pipeline",
      "src": "$.createdAt.endOf(\"week\", { startOfWeek: \"monday\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.endOf()": {
      "entry": "pipeline",
      "src": "$.createdAt.endOf()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.createdAt.endOf(\"months\")": {
      "entry": "pipeline",
      "src": "$.createdAt.endOf(\"months\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.end.diff($.start, \"day\")": {
      "entry": "pipeline",
      "src": "$.end.diff($.start, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.end.diff($.start, \"week\", \"UTC\")": {
      "entry": "pipeline",
      "src": "$.end.diff($.start, \"week\", \"UTC\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.end.diff($.start, \"week\", { startOfWeek: \"monday\", timezone: \"Europe/Kyiv\" })": {
      "entry": "pipeline",
      "src": "$.end.diff($.start, \"week\", { startOfWeek: \"monday\", timezone: \"Europe/Kyiv\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date().diff($._id, \"day\")": {
      "entry": "pipeline",
      "src": "new Date().diff($._id, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.end.diff($.start)": {
      "entry": "pipeline",
      "src": "$.end.diff($.start)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.end.diff($.start, \"days\")": {
      "entry": "pipeline",
      "src": "$.end.diff($.start, \"days\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.end.diff(\"2020-01-01\", \"day\")": {
      "entry": "pipeline",
      "src": "$.end.diff(\"2020-01-01\", \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.end.diff($.start, $.unit)": {
      "entry": "pipeline",
      "src": "$.end.diff($.start, $.unit)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.end.diff($.start, \"day\", { startofweek: \"monday\" })": {
      "entry": "pipeline",
      "src": "$.end.diff($.start, \"day\", { startofweek: \"monday\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(1, \"day\", { binSize: 2 })": {
      "entry": "pipeline",
      "src": "$.t.plus(1, \"day\", { binSize: 2 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.end.diff($.start, \"day\", { ...$.o })": {
      "entry": "pipeline",
      "src": "$.end.diff($.start, \"day\", { ...$.o })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.end.diff($.start, \"day\", { timezone: 5 })": {
      "entry": "pipeline",
      "src": "$.end.diff($.start, \"day\", { timezone: 5 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.end.diff($.start, \"week\", { startOfWeek: \"moonday\" })": {
      "entry": "pipeline",
      "src": "$.end.diff($.start, \"week\", { startOfWeek: \"moonday\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.end.diff($.start, \"week\", { timezone: $.tz, startOfWeek: $.wk })": {
      "entry": "pipeline",
      "src": "$.end.diff($.start, \"week\", { timezone: $.tz, startOfWeek: $.wk })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(2, \"hour\", { timezone: \"America/New_York\" })": {
      "entry": "pipeline",
      "src": "$.t.plus(2, \"hour\", { timezone: \"America/New_York\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"2020-01-01\".getFullYear()": {
      "entry": "pipeline",
      "src": "\"2020-01-01\".getFullYear()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"2020\".toISOString()": {
      "entry": "pipeline",
      "src": "\"2020\".toISOString()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"2020\".getUTCHours()": {
      "entry": "pipeline",
      "src": "\"2020\".getUTCHours()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"2020-01-01\".plus(1, \"day\")": {
      "entry": "pipeline",
      "src": "\"2020-01-01\".plus(1, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"2020\".getTime()": {
      "entry": "pipeline",
      "src": "\"2020\".getTime()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date($.x).getMonth()": {
      "entry": "pipeline",
      "src": "new Date($.x).getMonth()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"$ts\".getHours()": {
      "entry": "pipeline",
      "src": "\"$ts\".getHours()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getUTCFullYear()": {
      "entry": "pipeline",
      "src": "$.ts.getUTCFullYear()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getUTCMonth()": {
      "entry": "pipeline",
      "src": "$.ts.getUTCMonth()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getUTCDate()": {
      "entry": "pipeline",
      "src": "$.ts.getUTCDate()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getUTCDay()": {
      "entry": "pipeline",
      "src": "$.ts.getUTCDay()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getUTCMinutes()": {
      "entry": "pipeline",
      "src": "$.ts.getUTCMinutes()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getUTCSeconds()": {
      "entry": "pipeline",
      "src": "$.ts.getUTCSeconds()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getUTCMilliseconds()": {
      "entry": "pipeline",
      "src": "$.ts.getUTCMilliseconds()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.x": {
      "entry": "pipeline",
      "src": "typeof $.x",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.x === \"string\"": {
      "entry": "pipeline",
      "src": "typeof $.x === \"string\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date()": {
      "entry": "pipeline",
      "src": "new Date()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date($.ts)": {
      "entry": "pipeline",
      "src": "new Date($.ts)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date(\"2024-01-01\")": {
      "entry": "pipeline",
      "src": "new Date(\"2024-01-01\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date(2024, 1)": {
      "entry": "pipeline",
      "src": "new Date(2024, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date(2024, 1, 15)": {
      "entry": "pipeline",
      "src": "new Date(2024, 1, 15)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date(2024, 12, 31, 23, 59, 58, 999)": {
      "entry": "pipeline",
      "src": "new Date(2024, 12, 31, 23, 59, 58, 999)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date(2024, 13, 1)": {
      "entry": "pipeline",
      "src": "new Date(2024, 13, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date($.y, $.m, 1)": {
      "entry": "pipeline",
      "src": "new Date($.y, $.m, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date(2024, 0, 15)": {
      "entry": "pipeline",
      "src": "new Date(2024, 0, 15)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date(2024, -1, 15)": {
      "entry": "pipeline",
      "src": "new Date(2024, -1, 15)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Date.UTC(2024, 0, 15)": {
      "entry": "pipeline",
      "src": "Date.UTC(2024, 0, 15)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date(\"not-a-date\")": {
      "entry": "pipeline",
      "src": "new Date(\"not-a-date\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000{ createdAt: { $gte: new Date(\"2026-05-17T02:57:59.714Z\") } }": {
      "entry": "pipeline",
      "src": "{ createdAt: { $gte: new Date(\"2026-05-17T02:57:59.714Z\") } }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date(1, 2, 3, 4, 5, 6, 7, 8)": {
      "entry": "pipeline",
      "src": "new Date(1, 2, 3, 4, 5, 6, 7, 8)",
      "kind": "message",
      "was": "'new Date(year, month, day, hour, minute, second, ms)' takes at most 7 arguments, got 8 at position 0",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Date.UTC(2024, 1, 15)": {
      "entry": "pipeline",
      "src": "Date.UTC(2024, 1, 15)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Date.UTC(1970)": {
      "entry": "pipeline",
      "src": "Date.UTC(1970)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date(Date.UTC(2024, 1, 15))": {
      "entry": "pipeline",
      "src": "new Date(Date.UTC(2024, 1, 15))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Date.UTC()": {
      "entry": "pipeline",
      "src": "Date.UTC()",
      "kind": "message",
      "was": "Date.UTC(year[, month, day, hour, minute, second, ms]) takes 1 to 7 arguments, got 0 at position 0",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Date.UTC(1,2,3,4,5,6,7,8)": {
      "entry": "pipeline",
      "src": "Date.UTC(1,2,3,4,5,6,7,8)",
      "kind": "message",
      "was": "Date.UTC(year[, month, day, hour, minute, second, ms]) takes 1 to 7 arguments, got 8 at position 0",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Number($.str)": {
      "entry": "pipeline",
      "src": "Number($.str)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000String($.n)": {
      "entry": "pipeline",
      "src": "String($.n)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Boolean($.x)": {
      "entry": "pipeline",
      "src": "Boolean($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Boolean($.x > 0)": {
      "entry": "pipeline",
      "src": "Boolean($.x > 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$toBool($.x)": {
      "entry": "pipeline",
      "src": "$toBool($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000parseInt($.s)": {
      "entry": "pipeline",
      "src": "parseInt($.s)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000parseFloat($.s)": {
      "entry": "pipeline",
      "src": "parseFloat($.s)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.abs($.x)": {
      "entry": "pipeline",
      "src": "Math.abs($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.ceil($.x)": {
      "entry": "pipeline",
      "src": "Math.ceil($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.floor($.x)": {
      "entry": "pipeline",
      "src": "Math.floor($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.round($.x)": {
      "entry": "pipeline",
      "src": "Math.round($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.pow(2, $.n)": {
      "entry": "pipeline",
      "src": "Math.pow(2, $.n)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.sqrt($.x)": {
      "entry": "pipeline",
      "src": "Math.sqrt($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.exp($.x)": {
      "entry": "pipeline",
      "src": "Math.exp($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.log($.x)": {
      "entry": "pipeline",
      "src": "Math.log($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.trunc($.x)": {
      "entry": "pipeline",
      "src": "Math.trunc($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.scores.map(Math.floor)": {
      "entry": "pipeline",
      "src": "$.scores.map(Math.floor)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.scores.map(Math.round)": {
      "entry": "pipeline",
      "src": "$.scores.map(Math.round)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.scores.map(Math.abs)": {
      "entry": "pipeline",
      "src": "$.scores.map(Math.abs)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.scores.map(Math.ceil)": {
      "entry": "pipeline",
      "src": "$.scores.map(Math.ceil)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.scores.map(Math.sqrt)": {
      "entry": "pipeline",
      "src": "$.scores.map(Math.sqrt)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.scores.map(Math.trunc)": {
      "entry": "pipeline",
      "src": "$.scores.map(Math.trunc)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.scores.map(Math.pow)": {
      "entry": "pipeline",
      "src": "$.scores.map(Math.pow)",
      "kind": "message",
      "was": "Math.pow requires '(...)'. Only the unary Math methods (Math.floor / .ceil / .round / .abs / …) can be passed as bare callbacks (e.g. `arr.map(Math.floor)`).",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.scores.map(Math.min)": {
      "entry": "pipeline",
      "src": "$.scores.map(Math.min)",
      "kind": "message",
      "was": "Math.min requires '(...)'. Only the unary Math methods (Math.floor / .ceil / .round / .abs / …) can be passed as bare callbacks (e.g. `arr.map(Math.floor)`).",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x = Math.floor": {
      "entry": "pipeline",
      "src": "$.x = Math.floor",
      "kind": "message",
      "was": "'Math.floor' used as a value is only valid as a callback to a higher-order array method (e.g. $.items.map(Math.floor)). To compute on a single value, write Math.floor(value).",
      "now": "Math.floor(value) requires exactly 1 argument, got 0",
      "reason": "message: both refuse the argument count; the new wording names the parameter from the row's own signature."
    },
    "pipeline\u0000Math.sin($.angle)": {
      "entry": "pipeline",
      "src": "Math.sin($.angle)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.cos($.angle)": {
      "entry": "pipeline",
      "src": "Math.cos($.angle)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.tan($.angle)": {
      "entry": "pipeline",
      "src": "Math.tan($.angle)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.asin($.x)": {
      "entry": "pipeline",
      "src": "Math.asin($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.acos($.x)": {
      "entry": "pipeline",
      "src": "Math.acos($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.atan($.x)": {
      "entry": "pipeline",
      "src": "Math.atan($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.atan2($.y, $.x)": {
      "entry": "pipeline",
      "src": "Math.atan2($.y, $.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.atan2($.x)": {
      "entry": "pipeline",
      "src": "Math.atan2($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.sinh($.x)": {
      "entry": "pipeline",
      "src": "Math.sinh($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.cosh($.x)": {
      "entry": "pipeline",
      "src": "Math.cosh($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.tanh($.x)": {
      "entry": "pipeline",
      "src": "Math.tanh($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.asinh($.x)": {
      "entry": "pipeline",
      "src": "Math.asinh($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.acosh($.x)": {
      "entry": "pipeline",
      "src": "Math.acosh($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.atanh($.x)": {
      "entry": "pipeline",
      "src": "Math.atanh($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a & $.b": {
      "entry": "pipeline",
      "src": "$.a & $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a | $.b": {
      "entry": "pipeline",
      "src": "$.a | $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a ^ $.b": {
      "entry": "pipeline",
      "src": "$.a ^ $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000~$.a": {
      "entry": "pipeline",
      "src": "~$.a",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a & $.b & $.c": {
      "entry": "pipeline",
      "src": "$.a & $.b & $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a | $.b | $.c": {
      "entry": "pipeline",
      "src": "$.a | $.b | $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a ^ $.b ^ $.c": {
      "entry": "pipeline",
      "src": "$.a ^ $.b ^ $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a & $.b | $.c": {
      "entry": "pipeline",
      "src": "$.a & $.b | $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a ^ $.b | $.c": {
      "entry": "pipeline",
      "src": "$.a ^ $.b | $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a & $.b ^ $.c": {
      "entry": "pipeline",
      "src": "$.a & $.b ^ $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a | $.b && $.c": {
      "entry": "pipeline",
      "src": "$.a | $.b && $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a === $.b & $.c": {
      "entry": "pipeline",
      "src": "$.a === $.b & $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000~$.flags & 255": {
      "entry": "pipeline",
      "src": "~$.flags & 255",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.keys($.doc)": {
      "entry": "pipeline",
      "src": "Object.keys($.doc)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.values($.doc)": {
      "entry": "pipeline",
      "src": "Object.values($.doc)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.entries($.doc)": {
      "entry": "pipeline",
      "src": "Object.entries($.doc)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.assign($.a, $.b)": {
      "entry": "pipeline",
      "src": "Object.assign($.a, $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.assign($.a, $.b, $.c)": {
      "entry": "pipeline",
      "src": "Object.assign($.a, $.b, $.c)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$let({ d: $.price * 0.1 }, (d) => $.price - d)": {
      "entry": "pipeline",
      "src": "$let({ d: $.price * 0.1 }, (d) => $.price - d)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$let({ p: $.price }, (p) => { const tax = p * 0.1; return p + tax; })": {
      "entry": "pipeline",
      "src": "$let({ p: $.price }, (p) => { const tax = p * 0.1; return p + tax; })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => { const y = x * 2; return y; })": {
      "entry": "pipeline",
      "src": "$.items.map(x => { const y = x * 2; return y; })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => { const a = x + 1; const b = a * 2; return b; })": {
      "entry": "pipeline",
      "src": "$.items.map(x => { const a = x + 1; const b = a * 2; return b; })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.filter(x => { const ok = x.active; return ok; })": {
      "entry": "pipeline",
      "src": "$.items.filter(x => { const ok = x.active; return ok; })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.nums.reduce((acc, n) => { const step = n + 1; return acc + step; }, 0)": {
      "entry": "pipeline",
      "src": "$.nums.reduce((acc, n) => { const step = n + 1; return acc + step; }, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => { return x.name; })": {
      "entry": "pipeline",
      "src": "$.items.map(x => { return x.name; })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => { const _tmp = x * 2; return _tmp; })": {
      "entry": "pipeline",
      "src": "$.items.map(x => { const _tmp = x * 2; return _tmp; })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.rows.map(r => { const xs = r.vals.map(v => { const d = v * 2; return d; }); return xs; })": {
      "entry": "pipeline",
      "src": "$.rows.map(r => { const xs = r.vals.map(v => { const d = v * 2; return d; }); return xs; })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map((x, i) => { const tagged = i; return tagged; })": {
      "entry": "pipeline",
      "src": "$.items.map((x, i) => { const tagged = i; return tagged; })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => { const a = 1; const a = 2; return a; })": {
      "entry": "pipeline",
      "src": "$.items.map(x => { const a = 1; const a = 2; return a; })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => { const a = 1; })": {
      "entry": "pipeline",
      "src": "$.items.map(x => { const a = 1; })",
      "kind": "message",
      "was": "A block body must end with a `return <expr>` statement at position 32, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "now": "A block body must end with a `return <expr>` statement at position 32, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$.items.map(a => a).toReversed()": {
      "entry": "pipeline",
      "src": "$.items.map(a => a).toReversed()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1, 2].map(a => a)": {
      "entry": "pipeline",
      "src": "[1, 2].map(a => a)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1, 2].filter(a => a)": {
      "entry": "pipeline",
      "src": "[1, 2].filter(a => a)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[[1, 2], [3]].join(\",\")": {
      "entry": "pipeline",
      "src": "[[1, 2], [3]].join(\",\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[[1, 2], [3]].toString()": {
      "entry": "pipeline",
      "src": "[[1, 2], [3]].toString()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1, 2].join(\",\")": {
      "entry": "pipeline",
      "src": "[1, 2].join(\",\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.join(\",\")": {
      "entry": "pipeline",
      "src": "$.items.join(\",\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[[1, 2], [3]].flat().join(\",\")": {
      "entry": "pipeline",
      "src": "[[1, 2], [3]].flat().join(\",\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.r = $$$.orders.sortBy((o) => { $limit(2); });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.sortBy((o) => { $limit(2); });",
      "kind": "message",
      "was": "Unexpected stage call at position 33: a `=> { $stage(...); ... }` block is a sub-pipeline, but '.sortBy(...)' doesn't take one. Pipeline stages belong to `.aggregate(pipeline)` alone; every other callback's block body is",
      "now": "A block body must end with a `return <expr>` statement at position 44, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$.r = $$$.orders.aggregat((o) => { $limit(2); });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.aggregat((o) => { $limit(2); });",
      "kind": "message",
      "was": "Unexpected stage call at position 35: a `=> { $stage(...); ... }` block is a sub-pipeline, but '.aggregat(...)' doesn't take one. Did you mean '.aggregate'? Pipeline stages belong to `.aggregate(pipeline)` alone; every o",
      "now": "A block body must end with a `return <expr>` statement at position 46, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$.items.map((d) => { const a = d.v; })": {
      "entry": "pipeline",
      "src": "$.items.map((d) => { const a = d.v; })",
      "kind": "message",
      "was": "A block body must end with a `return <expr>` statement at position 36, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "now": "A block body must end with a `return <expr>` statement at position 36, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$.items.map((d) => { d.v; })": {
      "entry": "pipeline",
      "src": "$.items.map((d) => { d.v; })",
      "kind": "message",
      "was": "A block body must end with a `return <expr>` statement at position 21, got an identifier 'd'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expr",
      "now": "A block body must end with a `return <expr>` statement at position 26, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$.items.map((d) => { const a = d.v; return a; })": {
      "entry": "pipeline",
      "src": "$.items.map((d) => { const a = d.v; return a; })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => { k: x })": {
      "entry": "pipeline",
      "src": "$.items.map(x => { k: x })",
      "kind": "message",
      "was": "A block body must end with a `return <expr>` statement at position 19, got an identifier 'k'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expr",
      "now": "A block body must end with a `return <expr>` statement at position 19, got an identifier 'k'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expr",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$.items.map(x => ({ k: x }))": {
      "entry": "pipeline",
      "src": "$.items.map(x => ({ k: x }))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.scores.toSorted()": {
      "entry": "pipeline",
      "src": "$.scores.toSorted()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.scores.toSorted((a, b) => a - b)": {
      "entry": "pipeline",
      "src": "$.scores.toSorted((a, b) => a - b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.toReversed().map(x => x.name)": {
      "entry": "pipeline",
      "src": "$.items.toReversed().map(x => x.name)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.findLast(x => x.active)": {
      "entry": "pipeline",
      "src": "$.items.findLast(x => x.active)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.findLastIndex(x => x.active)": {
      "entry": "pipeline",
      "src": "$.items.findLastIndex(x => x.active)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.findIndex(x => x.active)": {
      "entry": "pipeline",
      "src": "$.items.findIndex(x => x.active)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.lastIndexOf(42)": {
      "entry": "pipeline",
      "src": "$.items.lastIndexOf(42)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.toLowerCase().lastIndexOf(\"x\")": {
      "entry": "pipeline",
      "src": "$.s.toLowerCase().lastIndexOf(\"x\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduceRight((acc, x) => acc + x, 0)": {
      "entry": "pipeline",
      "src": "$.xs.reduceRight((acc, x) => acc + x, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.toSpliced(1, 2, \"a\", \"b\")": {
      "entry": "pipeline",
      "src": "$.xs.toSpliced(1, 2, \"a\", \"b\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.toSpliced(2)": {
      "entry": "pipeline",
      "src": "$.xs.toSpliced(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.toSpliced(-1, 1)": {
      "entry": "pipeline",
      "src": "$.xs.toSpliced(-1, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.with(1, 99)": {
      "entry": "pipeline",
      "src": "$.xs.with(1, 99)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.with(-1, 9)": {
      "entry": "pipeline",
      "src": "$.xs.with(-1, 9)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.with(0)": {
      "entry": "pipeline",
      "src": "$.xs.with(0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.map(x => x + 1).toString()": {
      "entry": "pipeline",
      "src": "$.xs.map(x => x + 1).toString()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.toLowerCase().toString()": {
      "entry": "pipeline",
      "src": "$.name.toLowerCase().toString()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.toString()": {
      "entry": "pipeline",
      "src": "$.n.toString()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.map((x, i) => x + i)": {
      "entry": "pipeline",
      "src": "$.xs.map((x, i) => x + i)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.filter((x, i) => i > 0)": {
      "entry": "pipeline",
      "src": "$.xs.filter((x, i) => i > 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.map(v => $.b.map(() => v))": {
      "entry": "pipeline",
      "src": "$.a.map(v => $.b.map(() => v))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.find((x, i) => i === 2)": {
      "entry": "pipeline",
      "src": "$.xs.find((x, i) => i === 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.some((x, i) => i > 5)": {
      "entry": "pipeline",
      "src": "$.xs.some((x, i) => i > 5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.findIndex((x, i) => x === i)": {
      "entry": "pipeline",
      "src": "$.xs.findIndex((x, i) => x === i)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.findIndex((_, i) => i > 2)": {
      "entry": "pipeline",
      "src": "$.xs.findIndex((_, i) => i > 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.findLastIndex((_, i) => i > 2)": {
      "entry": "pipeline",
      "src": "$.xs.findLastIndex((_, i) => i > 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce((acc, x, i) => acc + x * i, 0)": {
      "entry": "pipeline",
      "src": "$.xs.reduce((acc, x, i) => acc + x * i, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduceRight((acc, x, i) => acc + i, 0)": {
      "entry": "pipeline",
      "src": "$.xs.reduceRight((acc, x, i) => acc + i, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.map((x, i, arr) => arr.length)": {
      "entry": "pipeline",
      "src": "$.xs.map((x, i, arr) => arr.length)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.filter((x, i, arr) => arr.length > 0)": {
      "entry": "pipeline",
      "src": "$.xs.filter((x, i, arr) => arr.length > 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.map((x, i) => x)": {
      "entry": "pipeline",
      "src": "$.xs.map((x, i) => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.map((x, i, arr, extra) => x)": {
      "entry": "pipeline",
      "src": "$.xs.map((x, i, arr, extra) => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.findIndex((x, i, arr) => true)": {
      "entry": "pipeline",
      "src": "$.xs.findIndex((x, i, arr) => true)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce((acc, x, i, arr) => acc, 0)": {
      "entry": "pipeline",
      "src": "$.xs.reduce((acc, x, i, arr) => acc, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.push(1)": {
      "entry": "pipeline",
      "src": "$.xs.push(1)",
      "kind": "now-accepted",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "[{\"$set\":{\"xs\":{\"$concatArrays\":[\"$xs\",[1]]}}}]",
      "reason": "intended: a mutator method IS a statement — the row's statement cell renders it as the write it means, where the shipped compiler answered from the pipeline assembler and refused a lone one."
    },
    "pipeline\u0000$.xs.unshift(1)": {
      "entry": "pipeline",
      "src": "$.xs.unshift(1)",
      "kind": "now-accepted",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "[{\"$set\":{\"xs\":{\"$concatArrays\":[[1],\"$xs\"]}}}]",
      "reason": "intended: a mutator method IS a statement — the row's statement cell renders it as the write it means, where the shipped compiler answered from the pipeline assembler and refused a lone one."
    },
    "pipeline\u0000$.events.toSorted(e => e.distance)": {
      "entry": "pipeline",
      "src": "$.events.toSorted(e => e.distance)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.toSorted(e => -e.distance)": {
      "entry": "pipeline",
      "src": "$.events.toSorted(e => -e.distance)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.toSorted(e => e.user.name)": {
      "entry": "pipeline",
      "src": "$.events.toSorted(e => e.user.name)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.toSorted(e => e.distance).slice(-10)": {
      "entry": "pipeline",
      "src": "$.events.toSorted(e => e.distance).slice(-10)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.toSorted((a, b) => a.x - b.x)": {
      "entry": "pipeline",
      "src": "$.events.toSorted((a, b) => a.x - b.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.toSorted(e => e.x + e.y)": {
      "entry": "pipeline",
      "src": "$.events.toSorted(e => e.x + e.y)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.toSorted(e => e)": {
      "entry": "pipeline",
      "src": "$.events.toSorted(e => e)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.toSorted(\"distance\")": {
      "entry": "pipeline",
      "src": "$.events.toSorted(\"distance\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.toSorted({ distance: -1 })": {
      "entry": "pipeline",
      "src": "$.events.toSorted({ distance: -1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.toSorted({ a: \"asc\", b: \"desc\" })": {
      "entry": "pipeline",
      "src": "$.events.toSorted({ a: \"asc\", b: \"desc\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.toSorted([\"a\", \"b\"])": {
      "entry": "pipeline",
      "src": "$.events.toSorted([\"a\", \"b\"])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.toSorted({ a: 3 })": {
      "entry": "pipeline",
      "src": "$.events.toSorted({ a: 3 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.toSorted(\"$x\")": {
      "entry": "pipeline",
      "src": "$.events.toSorted(\"$x\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.o.mapValues(v => v * 2)": {
      "entry": "pipeline",
      "src": "$.o.mapValues(v => v * 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.o.pick([\"a\", \"b\"])": {
      "entry": "pipeline",
      "src": "$.o.pick([\"a\", \"b\"])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.o.pick(\"a\")": {
      "entry": "pipeline",
      "src": "$.o.pick(\"a\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.o.omit([\"a\"])": {
      "entry": "pipeline",
      "src": "$.o.omit([\"a\"])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.o.toPairs()": {
      "entry": "pipeline",
      "src": "$.o.toPairs()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.p.fromPairs()": {
      "entry": "pipeline",
      "src": "$.p.fromPairs()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.o.mapKeys((v, k) => k)": {
      "entry": "pipeline",
      "src": "$.o.mapKeys((v, k) => k)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.o.pickBy(v => v)": {
      "entry": "pipeline",
      "src": "$.o.pickBy(v => v)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.o.omitBy(v => v)": {
      "entry": "pipeline",
      "src": "$.o.omitBy(v => v)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.o.invert()": {
      "entry": "pipeline",
      "src": "$.o.invert()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sum()": {
      "entry": "pipeline",
      "src": "$.a.sum()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.mean()": {
      "entry": "pipeline",
      "src": "$.a.mean()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.max()": {
      "entry": "pipeline",
      "src": "$.a.max()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.min()": {
      "entry": "pipeline",
      "src": "$.a.min()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sumBy(\"x\")": {
      "entry": "pipeline",
      "src": "$.a.sumBy(\"x\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sumBy(o => o.x)": {
      "entry": "pipeline",
      "src": "$.a.sumBy(o => o.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.tags.uniq()": {
      "entry": "pipeline",
      "src": "$.tags.uniq()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.compact()": {
      "entry": "pipeline",
      "src": "$.a.compact()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.filter(jsmqlItem => jsmqlItem)": {
      "entry": "pipeline",
      "src": "$.a.filter(jsmqlItem => jsmqlItem)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.chunk(2)": {
      "entry": "pipeline",
      "src": "$.a.chunk(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.chunk(0)": {
      "entry": "pipeline",
      "src": "$.a.chunk(0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.intersection($.b)": {
      "entry": "pipeline",
      "src": "$.a.intersection($.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.difference($.b)": {
      "entry": "pipeline",
      "src": "$.a.difference($.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.keyBy(\"id\")": {
      "entry": "pipeline",
      "src": "$.a.keyBy(\"id\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.partition(o => o.ok)": {
      "entry": "pipeline",
      "src": "$.a.partition(o => o.ok)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.filter(o => o.ok)": {
      "entry": "pipeline",
      "src": "$.a.filter(o => o.ok)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.reject(o => o.ok)": {
      "entry": "pipeline",
      "src": "$.a.reject(o => o.ok)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.reject(o => o.n > 1)": {
      "entry": "pipeline",
      "src": "$.a.reject(o => o.n > 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.reject({ ok: true })": {
      "entry": "pipeline",
      "src": "$.a.reject({ ok: true })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.groupBy(\"t\")": {
      "entry": "pipeline",
      "src": "$.a.groupBy(\"t\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.countBy(\"t\")": {
      "entry": "pipeline",
      "src": "$.a.countBy(\"t\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.uniqBy(\"id\")": {
      "entry": "pipeline",
      "src": "$.a.uniqBy(\"id\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.maxBy(\"x\")": {
      "entry": "pipeline",
      "src": "$.a.maxBy(\"x\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.union($.b)": {
      "entry": "pipeline",
      "src": "$.a.union($.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.zipObject($.b)": {
      "entry": "pipeline",
      "src": "$.a.zipObject($.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.keyBy(\"t\")": {
      "entry": "pipeline",
      "src": "$.a.keyBy(\"t\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.keyBy()": {
      "entry": "pipeline",
      "src": "$.a.keyBy()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.countBy()": {
      "entry": "pipeline",
      "src": "$.a.countBy()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.groupBy()": {
      "entry": "pipeline",
      "src": "$.a.groupBy()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.countBy(\"x\", \"y\")": {
      "entry": "pipeline",
      "src": "$.a.countBy(\"x\", \"y\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.groupBy(\"x\", \"y\")": {
      "entry": "pipeline",
      "src": "$.a.groupBy(\"x\", \"y\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.keyBy(\"x\", \"y\")": {
      "entry": "pipeline",
      "src": "$.a.keyBy(\"x\", \"y\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$trunc($.n, 1) + 1": {
      "entry": "pipeline",
      "src": "$trunc($.n, 1) + 1",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$strcasecmp($.a, $.b) + 1": {
      "entry": "pipeline",
      "src": "$strcasecmp($.a, $.b) + 1",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$toBool($.x) ? 1 : 2": {
      "entry": "pipeline",
      "src": "$toBool($.x) ? 1 : 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$regexFindAll($.s, \"a\").length": {
      "entry": "pipeline",
      "src": "$regexFindAll($.s, \"a\").length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.every(x => x.ok).map(y => y)": {
      "entry": "pipeline",
      "src": "$.items.every(x => x.ok).map(y => y)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.some(x => x.ok).filter(y => y)": {
      "entry": "pipeline",
      "src": "$.items.some(x => x.ok).filter(y => y)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.startsWith(\"A\").trim()": {
      "entry": "pipeline",
      "src": "$.name.startsWith(\"A\").trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.every(x => x.ok).toString()": {
      "entry": "pipeline",
      "src": "$.items.every(x => x.ok).toString()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.split(',').toUpperCase()": {
      "entry": "pipeline",
      "src": "$.name.split(',').toUpperCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => x).round(2)": {
      "entry": "pipeline",
      "src": "$.items.map(x => x).round(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => x).mapValues(v => v)": {
      "entry": "pipeline",
      "src": "$.items.map(x => x).mapValues(v => v)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.toUpperCase().map(x => x)": {
      "entry": "pipeline",
      "src": "$.name.toUpperCase().map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.size().map(x => x)": {
      "entry": "pipeline",
      "src": "$.items.size().map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.countBy(\"t\").take(3)": {
      "entry": "pipeline",
      "src": "$.items.countBy(\"t\").take(3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.countBy(\"t\").map(v => v)": {
      "entry": "pipeline",
      "src": "$.items.countBy(\"t\").map(v => v)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.matrix.find(r => r.length > 0).map(x => x)": {
      "entry": "pipeline",
      "src": "$.matrix.find(r => r.length > 0).map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.at(0).toLowerCase()": {
      "entry": "pipeline",
      "src": "$.items.at(0).toLowerCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.toUpperCase().trim()": {
      "entry": "pipeline",
      "src": "$.name.toUpperCase().trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => x).filter(y => y)": {
      "entry": "pipeline",
      "src": "$.items.map(x => x).filter(y => y)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.countBy(\"t\").mapValues(v => v)": {
      "entry": "pipeline",
      "src": "$.items.countBy(\"t\").mapValues(v => v)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.toUpperCase().includes(\"A\")": {
      "entry": "pipeline",
      "src": "$.name.toUpperCase().includes(\"A\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.d.clamp($.a, $.b).getFullYear()": {
      "entry": "pipeline",
      "src": "$.d.clamp($.a, $.b).getFullYear()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.startOf(\"month\").map(x => x)": {
      "entry": "pipeline",
      "src": "$.t.startOf(\"month\").map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.endOf(\"month\").mapValues(v => v)": {
      "entry": "pipeline",
      "src": "$.t.endOf(\"month\").mapValues(v => v)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.set({ year: 2030 }).round(2)": {
      "entry": "pipeline",
      "src": "$.t.set({ year: 2030 }).round(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.minus(1, \"day\").split(\",\")": {
      "entry": "pipeline",
      "src": "$.t.minus(1, \"day\").split(\",\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date($.x).map(y => y)": {
      "entry": "pipeline",
      "src": "new Date($.x).map(y => y)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(1, \"day\").toUpperCase()": {
      "entry": "pipeline",
      "src": "$.t.plus(1, \"day\").toUpperCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(1, \"day\").round(2)": {
      "entry": "pipeline",
      "src": "$.t.plus(1, \"day\").round(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.startOf(\"month\").plus(1, \"month\")": {
      "entry": "pipeline",
      "src": "$.t.startOf(\"month\").plus(1, \"month\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(1, \"day\").format(\"%Y\")": {
      "entry": "pipeline",
      "src": "$.t.plus(1, \"day\").format(\"%Y\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.startOf(\"month\").isSame($.b, \"day\")": {
      "entry": "pipeline",
      "src": "$.t.startOf(\"month\").isSame($.b, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(1, \"day\").getTime()": {
      "entry": "pipeline",
      "src": "$.t.plus(1, \"day\").getTime()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(1, \"day\").toString()": {
      "entry": "pipeline",
      "src": "$.t.plus(1, \"day\").toString()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.plus(1, \"day\").clamp($.a, $.b)": {
      "entry": "pipeline",
      "src": "$.t.plus(1, \"day\").clamp($.a, $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".map(x => x)": {
      "entry": "pipeline",
      "src": "\"abc\".map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".mapValues(v => v)": {
      "entry": "pipeline",
      "src": "\"abc\".mapValues(v => v)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000(5).trim()": {
      "entry": "pipeline",
      "src": "(5).trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000(5).map(x => x)": {
      "entry": "pipeline",
      "src": "(5).map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`a${$.x}b`.plus(1, 'day')": {
      "entry": "pipeline",
      "src": "`a${$.x}b`.plus(1, 'day')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000(typeof $.x).map(c => c)": {
      "entry": "pipeline",
      "src": "(typeof $.x).map(c => c)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.length.map(x => x)": {
      "entry": "pipeline",
      "src": "$.s.length.map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.length.trim()": {
      "entry": "pipeline",
      "src": "$.items.length.trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.length.clamp(0, 10)": {
      "entry": "pipeline",
      "src": "$.s.length.clamp(0, 10)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.o.pick([\"a\"]).trim()": {
      "entry": "pipeline",
      "src": "$.o.pick([\"a\"]).trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.o.pick([\"a\"]).mapValues(v => v)": {
      "entry": "pipeline",
      "src": "$.o.pick([\"a\"]).mapValues(v => v)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"$items\".map(x => x)": {
      "entry": "pipeline",
      "src": "\"$items\".map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"$t\".plus(1, \"day\")": {
      "entry": "pipeline",
      "src": "\"$t\".plus(1, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$concat($.a, $.b).map(x => x)": {
      "entry": "pipeline",
      "src": "$concat($.a, $.b).map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$abs($.n).trim()": {
      "entry": "pipeline",
      "src": "$abs($.n).trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateTrunc($.t, \"month\").toUpperCase()": {
      "entry": "pipeline",
      "src": "$dateTrunc($.t, \"month\").toUpperCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateToParts($.t).map(x => x)": {
      "entry": "pipeline",
      "src": "$dateToParts($.t).map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$add($.a, $.b).plus(1, \"day\")": {
      "entry": "pipeline",
      "src": "$add($.a, $.b).plus(1, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$subtract($.a, $.b).plus(1, \"day\")": {
      "entry": "pipeline",
      "src": "$subtract($.a, $.b).plus(1, \"day\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$ifNull($.a, $.b).trim()": {
      "entry": "pipeline",
      "src": "$ifNull($.a, $.b).trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$max($.a).map(x => x)": {
      "entry": "pipeline",
      "src": "$max($.a).map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$first($.a).trim()": {
      "entry": "pipeline",
      "src": "$first($.a).trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$cond($.c, $.a, $.b).map(x => x)": {
      "entry": "pipeline",
      "src": "$cond($.c, $.a, $.b).map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateTrunc($.t, \"month\").plus(1, \"month\")": {
      "entry": "pipeline",
      "src": "$dateTrunc($.t, \"month\").plus(1, \"month\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$concat($.a, $.b).trim()": {
      "entry": "pipeline",
      "src": "$concat($.a, $.b).trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$split($.s, \",\").map(x => x)": {
      "entry": "pipeline",
      "src": "$split($.s, \",\").map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$year($.t).clamp(0, 5)": {
      "entry": "pipeline",
      "src": "$year($.t).clamp(0, 5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateToParts($.t).mapValues(v => v)": {
      "entry": "pipeline",
      "src": "$dateToParts($.t).mapValues(v => v)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.trim().findIndex(x => x)": {
      "entry": "pipeline",
      "src": "$.s.trim().findIndex(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.trim().toSpliced(0, 1)": {
      "entry": "pipeline",
      "src": "$.s.trim().toSpliced(0, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.trim().with(0, 1)": {
      "entry": "pipeline",
      "src": "$.s.trim().with(0, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.round(2).reduceRight((a, b) => a, 0)": {
      "entry": "pipeline",
      "src": "$.n.round(2).reduceRight((a, b) => a, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.startOf(\"month\").findIndex(x => x)": {
      "entry": "pipeline",
      "src": "$.t.startOf(\"month\").findIndex(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.size().difference($.b)": {
      "entry": "pipeline",
      "src": "$.a.size().difference($.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.trim().union($.b)": {
      "entry": "pipeline",
      "src": "$.s.trim().union($.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.trim().unzipWith((a, b) => a)": {
      "entry": "pipeline",
      "src": "$.s.trim().unzipWith((a, b) => a)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.trim().forEach(x => x)": {
      "entry": "pipeline",
      "src": "$.s.trim().forEach(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.forEach(x => x)": {
      "entry": "pipeline",
      "src": "$.a.forEach(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.map(\"name\")": {
      "entry": "pipeline",
      "src": "$.a.map(\"name\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.filter(\"active\")": {
      "entry": "pipeline",
      "src": "$.a.filter(\"active\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.map(\"addr.city\")": {
      "entry": "pipeline",
      "src": "$.a.map(\"addr.city\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.filter({ ok: true })": {
      "entry": "pipeline",
      "src": "$.a.filter({ ok: true })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.filter({ role: \"admin\", active: true })": {
      "entry": "pipeline",
      "src": "$.a.filter({ role: \"admin\", active: true })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.find([\"status.code\", 200])": {
      "entry": "pipeline",
      "src": "$.a.find([\"status.code\", 200])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sumBy(\"price\")": {
      "entry": "pipeline",
      "src": "$.a.sumBy(\"price\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.some({ ok: true })": {
      "entry": "pipeline",
      "src": "$.a.some({ ok: true })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.filter([1, 2])": {
      "entry": "pipeline",
      "src": "$.a.filter([1, 2])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.take(2)": {
      "entry": "pipeline",
      "src": "$.a.take(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.take()": {
      "entry": "pipeline",
      "src": "$.a.take()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.takeRight(2)": {
      "entry": "pipeline",
      "src": "$.a.takeRight(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.takeRight()": {
      "entry": "pipeline",
      "src": "$.a.takeRight()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.drop(2)": {
      "entry": "pipeline",
      "src": "$.a.drop(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.dropRight(2)": {
      "entry": "pipeline",
      "src": "$.a.dropRight(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.head()": {
      "entry": "pipeline",
      "src": "$.a.head()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.first()": {
      "entry": "pipeline",
      "src": "$.a.first()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.last()": {
      "entry": "pipeline",
      "src": "$.a.last()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.nth(2)": {
      "entry": "pipeline",
      "src": "$.a.nth(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.nth(-1)": {
      "entry": "pipeline",
      "src": "$.a.nth(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.nth()": {
      "entry": "pipeline",
      "src": "$.a.nth()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.uniq().nth(-1)": {
      "entry": "pipeline",
      "src": "$.a.uniq().nth(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.tail()": {
      "entry": "pipeline",
      "src": "$.a.tail()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.initial()": {
      "entry": "pipeline",
      "src": "$.a.initial()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.size()": {
      "entry": "pipeline",
      "src": "$.a.size()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.take(-1)": {
      "entry": "pipeline",
      "src": "$.a.take(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.takeRight(-1)": {
      "entry": "pipeline",
      "src": "$.a.takeRight(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.drop(-1)": {
      "entry": "pipeline",
      "src": "$.a.drop(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.without(2, 4)": {
      "entry": "pipeline",
      "src": "$.a.without(2, 4)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.xor($.b)": {
      "entry": "pipeline",
      "src": "$.a.xor($.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.differenceBy($.b, \"id\")": {
      "entry": "pipeline",
      "src": "$.a.differenceBy($.b, \"id\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.intersectionBy($.b, \"id\")": {
      "entry": "pipeline",
      "src": "$.a.intersectionBy($.b, \"id\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.unionBy($.b, \"id\")": {
      "entry": "pipeline",
      "src": "$.a.unionBy($.b, \"id\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.xorBy($.b, \"id\")": {
      "entry": "pipeline",
      "src": "$.a.xorBy($.b, \"id\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sortedUniq()": {
      "entry": "pipeline",
      "src": "$.a.sortedUniq()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.uniq()": {
      "entry": "pipeline",
      "src": "$.a.uniq()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sortedUniqBy(\"id\")": {
      "entry": "pipeline",
      "src": "$.a.sortedUniqBy(\"id\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.uniqBy(value => value.id)": {
      "entry": "pipeline",
      "src": "$.a.uniqBy(value => value.id)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.uniqBy(x => x)": {
      "entry": "pipeline",
      "src": "$.a.uniqBy(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.zip($.b)": {
      "entry": "pipeline",
      "src": "$.a.zip($.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.zip($.b, $.c)": {
      "entry": "pipeline",
      "src": "$.a.zip($.b, $.c)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.zipWith($.b, (x, y) => x + y)": {
      "entry": "pipeline",
      "src": "$.a.zipWith($.b, (x, y) => x + y)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.zipWith($.b, x => x)": {
      "entry": "pipeline",
      "src": "$.a.zipWith($.b, x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.unzipWith(f)": {
      "entry": "pipeline",
      "src": "$.a.unzipWith(f)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.t.unzip()": {
      "entry": "pipeline",
      "src": "$.t.unzip()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.takeWhile(x => x < 3)": {
      "entry": "pipeline",
      "src": "$.a.takeWhile(x => x < 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.dropWhile(x => x < 3)": {
      "entry": "pipeline",
      "src": "$.a.dropWhile(x => x < 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.takeRightWhile(x => x < 3)": {
      "entry": "pipeline",
      "src": "$.a.takeRightWhile(x => x < 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.dropRightWhile(x => x < 3)": {
      "entry": "pipeline",
      "src": "$.a.dropRightWhile(x => x < 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.objs.takeWhile({ ok: true })": {
      "entry": "pipeline",
      "src": "$.objs.takeWhile({ ok: true })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sortBy(\"age\")": {
      "entry": "pipeline",
      "src": "$.a.sortBy(\"age\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sortBy()": {
      "entry": "pipeline",
      "src": "$.a.sortBy()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sortBy([\"x\", \"y\"])": {
      "entry": "pipeline",
      "src": "$.a.sortBy([\"x\", \"y\"])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sortBy(u => u.age)": {
      "entry": "pipeline",
      "src": "$.a.sortBy(u => u.age)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sortBy({ age: -1 })": {
      "entry": "pipeline",
      "src": "$.a.sortBy({ age: -1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.orderBy(\"age\", \"desc\")": {
      "entry": "pipeline",
      "src": "$.a.orderBy(\"age\", \"desc\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.orderBy([\"a\", \"b\"], [\"asc\", \"desc\"])": {
      "entry": "pipeline",
      "src": "$.a.orderBy([\"a\", \"b\"], [\"asc\", \"desc\"])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.orderBy([\"a\", \"b\"])": {
      "entry": "pipeline",
      "src": "$.a.orderBy([\"a\", \"b\"])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.orderBy({ score: -1 })": {
      "entry": "pipeline",
      "src": "$.a.orderBy({ score: -1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.orderBy({ score: -1, name: \"asc\" })": {
      "entry": "pipeline",
      "src": "$.a.orderBy({ score: -1, name: \"asc\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.orderBy({ score: -1 }, [\"asc\"])": {
      "entry": "pipeline",
      "src": "$.a.orderBy({ score: -1 }, [\"asc\"])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sample()": {
      "entry": "pipeline",
      "src": "$.a.sample()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sampleSize(3)": {
      "entry": "pipeline",
      "src": "$.a.sampleSize(3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sampleSize()": {
      "entry": "pipeline",
      "src": "$.a.sampleSize()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sampleSize(-1)": {
      "entry": "pipeline",
      "src": "$.a.sampleSize(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.capitalize()": {
      "entry": "pipeline",
      "src": "$.s.capitalize()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.upperFirst()": {
      "entry": "pipeline",
      "src": "$.s.upperFirst()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.lowerFirst()": {
      "entry": "pipeline",
      "src": "$.s.lowerFirst()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.words()": {
      "entry": "pipeline",
      "src": "$.s.words()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.truncate()": {
      "entry": "pipeline",
      "src": "$.s.truncate()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.truncate({ length: 10 })": {
      "entry": "pipeline",
      "src": "$.s.truncate({ length: 10 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.truncate({ separator: \" \" })": {
      "entry": "pipeline",
      "src": "$.s.truncate({ separator: \" \" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.kebabCase()": {
      "entry": "pipeline",
      "src": "$.s.kebabCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.camelCase()": {
      "entry": "pipeline",
      "src": "$.s.camelCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.escape()": {
      "entry": "pipeline",
      "src": "$.s.escape()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.capitalize(1)": {
      "entry": "pipeline",
      "src": "$.s.capitalize(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.clamp(0, 100)": {
      "entry": "pipeline",
      "src": "$.n.clamp(0, 100)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.clamp(0)": {
      "entry": "pipeline",
      "src": "$.n.clamp(0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.inRange(10)": {
      "entry": "pipeline",
      "src": "$.n.inRange(10)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.inRange(5, 10)": {
      "entry": "pipeline",
      "src": "$.n.inRange(5, 10)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.round()": {
      "entry": "pipeline",
      "src": "$.n.round()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.round(2)": {
      "entry": "pipeline",
      "src": "$.n.round(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.ceil()": {
      "entry": "pipeline",
      "src": "$.n.ceil()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.floor()": {
      "entry": "pipeline",
      "src": "$.n.floor()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.ceil(2)": {
      "entry": "pipeline",
      "src": "$.n.ceil(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.push($.x)": {
      "entry": "pipeline",
      "src": "$.events.push($.x)",
      "kind": "now-accepted",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "[{\"$set\":{\"events\":{\"$concatArrays\":[\"$events\",[\"$x\"]]}}}]",
      "reason": "intended: a mutator method IS a statement — the row's statement cell renders it as the write it means, where the shipped compiler answered from the pipeline assembler and refused a lone one."
    },
    "pipeline\u0000$.events.sort().slice(-10)": {
      "entry": "pipeline",
      "src": "$.events.sort().slice(-10)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.push(x)": {
      "entry": "pipeline",
      "src": "$.events.push(x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "Unknown identifier 'x'. Did you mean '$.x'?",
      "reason": "intended: a mutator method IS a statement — the row's statement cell renders it as the write it means, where the shipped compiler answered from the pipeline assembler and refused a lone one."
    },
    "pipeline\u0000Object.assign({}, { a: 1 });": {
      "entry": "pipeline",
      "src": "Object.assign({}, { a: 1 });",
      "kind": "message",
      "was": "'Object.assign(...)' at statement position mutates its first argument, but the first argument isn't a writable target. Pass a document field ('$.profile') or an in-scope 'let'/'const' binding — e.g. 'Object.assign($.prof",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000$.xs.forEach(x => x)": {
      "entry": "pipeline",
      "src": "$.xs.forEach(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.entries()": {
      "entry": "pipeline",
      "src": "$.xs.entries()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.keys()": {
      "entry": "pipeline",
      "src": "$.xs.keys()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.values()": {
      "entry": "pipeline",
      "src": "$.xs.values()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.toLocaleString()": {
      "entry": "pipeline",
      "src": "$.xs.toLocaleString()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Set($.a).intersection(new Set($.b))": {
      "entry": "pipeline",
      "src": "new Set($.a).intersection(new Set($.b))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Set($.a).union(new Set($.b))": {
      "entry": "pipeline",
      "src": "new Set($.a).union(new Set($.b))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Set($.a).difference(new Set($.b))": {
      "entry": "pipeline",
      "src": "new Set($.a).difference(new Set($.b))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Set($.a).isSubsetOf(new Set($.b))": {
      "entry": "pipeline",
      "src": "new Set($.a).isSubsetOf(new Set($.b))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Set($.a).isSupersetOf(new Set($.b))": {
      "entry": "pipeline",
      "src": "new Set($.a).isSupersetOf(new Set($.b))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Set([1, 2, 3]).intersection(new Set([2, 3, 4]))": {
      "entry": "pipeline",
      "src": "new Set([1, 2, 3]).intersection(new Set([2, 3, 4]))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Set($.a).symmetricDifference(new Set($.b))": {
      "entry": "pipeline",
      "src": "new Set($.a).symmetricDifference(new Set($.b))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Set($.a).intersection($.b)": {
      "entry": "pipeline",
      "src": "new Set($.a).intersection($.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000/[a-z]+/.test($.s)": {
      "entry": "pipeline",
      "src": "/[a-z]+/.test($.s)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000/PAT/i.test($.s)": {
      "entry": "pipeline",
      "src": "/PAT/i.test($.s)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000/word/.exec($.s)": {
      "entry": "pipeline",
      "src": "/word/.exec($.s)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.matchAll(/word/g)": {
      "entry": "pipeline",
      "src": "$.s.matchAll(/word/g)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.matchAll(/word/)": {
      "entry": "pipeline",
      "src": "$.s.matchAll(/word/)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.search(/foo/)": {
      "entry": "pipeline",
      "src": "$.s.search(/foo/)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Number.isInteger($.n)": {
      "entry": "pipeline",
      "src": "Number.isInteger($.n)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Number.isNaN($.x)": {
      "entry": "pipeline",
      "src": "Number.isNaN($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Number.isFinite($.x)": {
      "entry": "pipeline",
      "src": "Number.isFinite($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.code.padStart(5, \"0\")": {
      "entry": "pipeline",
      "src": "$.code.padStart(5, \"0\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.code.padStart(5, \"US\")": {
      "entry": "pipeline",
      "src": "$.code.padStart(5, \"US\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.code.padStart(5, \"$sep\")": {
      "entry": "pipeline",
      "src": "$.code.padStart(5, \"$sep\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.code.padStart(5, $.sep)": {
      "entry": "pipeline",
      "src": "$.code.padStart(5, $.sep)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.padStart(10)": {
      "entry": "pipeline",
      "src": "$.s.padStart(10)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.padEnd(10, \"-\")": {
      "entry": "pipeline",
      "src": "$.s.padEnd(10, \"-\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(s => s.code.padStart(s.width, s.pad))": {
      "entry": "pipeline",
      "src": "$.items.map(s => s.code.padStart(s.width, s.pad))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(jsmqlPad => jsmqlPad.code.padStart(jsmqlPad.width))": {
      "entry": "pipeline",
      "src": "$.items.map(jsmqlPad => jsmqlPad.code.padStart(jsmqlPad.width))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"-\".repeat(5)": {
      "entry": "pipeline",
      "src": "\"-\".repeat(5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(s => s.code.padStart(3, s.pad))": {
      "entry": "pipeline",
      "src": "$.items.map(s => s.code.padStart(3, s.pad))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(jsmqlArr => jsmqlArr.list.lastIndexOf(jsmqlArr.needle))": {
      "entry": "pipeline",
      "src": "$.items.map(jsmqlArr => jsmqlArr.list.lastIndexOf(jsmqlArr.needle))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(jsmqlStr => jsmqlStr.name.endsWith(jsmqlStr.ext))": {
      "entry": "pipeline",
      "src": "$.items.map(jsmqlStr => jsmqlStr.name.endsWith(jsmqlStr.ext))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => x.list.lastIndexOf(x.needle))": {
      "entry": "pipeline",
      "src": "$.items.map(x => x.list.lastIndexOf(x.needle))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Array.from({ length: 5 })": {
      "entry": "pipeline",
      "src": "Array.from({ length: 5 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Array.from({ length: 3 }, (_, i) => i * 2)": {
      "entry": "pipeline",
      "src": "Array.from({ length: 3 }, (_, i) => i * 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Array.from({ length: $.n }, (_, i) => i)": {
      "entry": "pipeline",
      "src": "Array.from({ length: $.n }, (_, i) => i)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Array.from($.iter)": {
      "entry": "pipeline",
      "src": "Array.from($.iter)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Array.from({ length: 3 }, x => x)": {
      "entry": "pipeline",
      "src": "Array.from({ length: 3 }, x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000123n": {
      "entry": "pipeline",
      "src": "123n",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u00000n": {
      "entry": "pipeline",
      "src": "0n",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u00001.5n": {
      "entry": "pipeline",
      "src": "1.5n",
      "kind": "message",
      "was": "Invalid BigInt literal at position 0: 'n' suffix requires an integer (no fraction or exponent)",
      "now": "Invalid BigInt literal: the 'n' suffix requires an integer at position 0",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u00001e2n": {
      "entry": "pipeline",
      "src": "1e2n",
      "kind": "message",
      "was": "Invalid BigInt literal at position 0: 'n' suffix requires an integer (no fraction or exponent)",
      "now": "Invalid BigInt literal: the 'n' suffix requires an integer at position 0",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$.timestamp - 1000n": {
      "entry": "pipeline",
      "src": "$.timestamp - 1000n",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.groupBy($.items, x => x.category)": {
      "entry": "pipeline",
      "src": "Object.groupBy($.items, x => x.category)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.groupBy($.items, $.f)": {
      "entry": "pipeline",
      "src": "Object.groupBy($.items, $.f)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.groupBy($.items, (a, b) => a)": {
      "entry": "pipeline",
      "src": "Object.groupBy($.items, (a, b) => a)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000((x) => x + 1)(5)": {
      "entry": "pipeline",
      "src": "((x) => x + 1)(5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000(x => x * 2)($.n)": {
      "entry": "pipeline",
      "src": "(x => x * 2)($.n)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000((maxAge, minAge) => $.age >= minAge && $.age <= maxAge)(65, 18)": {
      "entry": "pipeline",
      "src": "((maxAge, minAge) => $.age >= minAge && $.age <= maxAge)(65, 18)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000(() => $.x + $.y)()": {
      "entry": "pipeline",
      "src": "(() => $.x + $.y)()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000((d) => $.price - d)($.price * 0.1)": {
      "entry": "pipeline",
      "src": "((d) => $.price - d)($.price * 0.1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000((x, y) => x + y)(1)": {
      "entry": "pipeline",
      "src": "((x, y) => x + y)(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.func(1, 2)": {
      "entry": "pipeline",
      "src": "$.func(1, 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000((x) => x)(...$.arr)": {
      "entry": "pipeline",
      "src": "((x) => x)(...$.arr)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.first.trim() + \" \" + $.last": {
      "entry": "pipeline",
      "src": "$.first.trim() + \" \" + $.last",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000String($.n) + \" items\"": {
      "entry": "pipeline",
      "src": "String($.n) + \" items\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.x + \" type\"": {
      "entry": "pipeline",
      "src": "typeof $.x + \" type\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.str.match(/[a-z]+/)": {
      "entry": "pipeline",
      "src": "$.str.match(/[a-z]+/)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x / 2": {
      "entry": "pipeline",
      "src": "$.x / 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.str.match(/pattern/gi)": {
      "entry": "pipeline",
      "src": "$.str.match(/pattern/gi)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000x > 0": {
      "entry": "pipeline",
      "src": "x > 0",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.frobulate()": {
      "entry": "pipeline",
      "src": "$.name.frobulate()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.toLowerCse()": {
      "entry": "pipeline",
      "src": "$.name.toLowerCse()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.fliter(x => x)": {
      "entry": "pipeline",
      "src": "$.items.fliter(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.substing(1)": {
      "entry": "pipeline",
      "src": "$.name.substing(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.flor($.x)": {
      "entry": "pipeline",
      "src": "Math.flor($.x)",
      "kind": "message",
      "was": "Unknown Math member 'flor' at position 5. Did you mean 'Math.floor'? See docs/LANGUAGE.md for the full list of supported Math methods and constants.",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Number.isItneger($.x)": {
      "entry": "pipeline",
      "src": "Number.isItneger($.x)",
      "kind": "message",
      "was": "Unknown Number static method 'isItneger' at position 7. Did you mean 'Number.isInteger'? Supported: Number.isInteger, Number.isNaN, Number.isFinite.",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.keyz($.o)": {
      "entry": "pipeline",
      "src": "Object.keyz($.o)",
      "kind": "message",
      "was": "Unknown Object method 'keyz' at position 7. Did you mean 'Object.keys'? Supported: keys, values, entries, assign, fromEntries, groupBy.",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$abs(x => x)": {
      "entry": "pipeline",
      "src": "$abs(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.trim() = 1": {
      "entry": "pipeline",
      "src": "$.s.trim() = 1",
      "kind": "message",
      "was": "Cannot assign to a method-call result ('.trim()') at position 11 — only field paths like '$.x' or '$.x.y' are assignable.",
      "now": "Cannot apply '=' to a '.field' expression — only a field, a binding, '$', '$$' or a collection can be written at position 11",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u000042 = 1": {
      "entry": "pipeline",
      "src": "42 = 1",
      "kind": "message",
      "was": "Cannot assign to a literal value at position 3 — only field paths like '$.x' or '$.x.y' are assignable.",
      "now": "Cannot apply '=' to a NumberLiteral — only a field, a binding, '$', '$$' or a collection can be written at position 3",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$.email.substr(1)": {
      "entry": "pipeline",
      "src": "$.email.substr(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.substr(0, 3)": {
      "entry": "pipeline",
      "src": "$.name.substr(0, 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.email.substr($.headerLength + 1)": {
      "entry": "pipeline",
      "src": "$.email.substr($.headerLength + 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.email.substr(-3)": {
      "entry": "pipeline",
      "src": "$.email.substr(-3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.email.substr(0, -1)": {
      "entry": "pipeline",
      "src": "$.email.substr(0, -1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"hello\".slice(1, 3)": {
      "entry": "pipeline",
      "src": "\"hello\".slice(1, 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.toLowerCase().slice(0, 3)": {
      "entry": "pipeline",
      "src": "$.name.toLowerCase().slice(0, 3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"hello\".slice(2)": {
      "entry": "pipeline",
      "src": "\"hello\".slice(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000String($.s).slice(2)": {
      "entry": "pipeline",
      "src": "String($.s).slice(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"hello\".slice(-3)": {
      "entry": "pipeline",
      "src": "\"hello\".slice(-3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000String($.s).slice(-3)": {
      "entry": "pipeline",
      "src": "String($.s).slice(-3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"hello\".slice(1, -1)": {
      "entry": "pipeline",
      "src": "\"hello\".slice(1, -1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000String($.s).slice(1, -1)": {
      "entry": "pipeline",
      "src": "String($.s).slice(1, -1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000String($.s).slice($.i)": {
      "entry": "pipeline",
      "src": "String($.s).slice($.i)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"hello\".slice()": {
      "entry": "pipeline",
      "src": "\"hello\".slice()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.substring(2, 7)": {
      "entry": "pipeline",
      "src": "$.name.substring(2, 7)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.email.substring(1)": {
      "entry": "pipeline",
      "src": "$.email.substring(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.substring()": {
      "entry": "pipeline",
      "src": "$.name.substring()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.substring($.i, 10)": {
      "entry": "pipeline",
      "src": "$.s.substring($.i, 10)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.substring(-3, 4)": {
      "entry": "pipeline",
      "src": "$.name.substring(-3, 4)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a < $.b === true": {
      "entry": "pipeline",
      "src": "$.a < $.b === true",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a > 0 === $.b > 0": {
      "entry": "pipeline",
      "src": "$.a > 0 === $.b > 0",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x < 5": {
      "entry": "pipeline",
      "src": "$.x < 5",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x === 5": {
      "entry": "pipeline",
      "src": "$.x === 5",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x in \"abc\"": {
      "entry": "pipeline",
      "src": "$.x in \"abc\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x in 42": {
      "entry": "pipeline",
      "src": "$.x in 42",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x in true": {
      "entry": "pipeline",
      "src": "$.x in true",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x in null": {
      "entry": "pipeline",
      "src": "$.x in null",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x in [\"a\", \"b\"]": {
      "entry": "pipeline",
      "src": "$.x in [\"a\", \"b\"]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x in { a: 1, b: 2 }": {
      "entry": "pipeline",
      "src": "$.x in { a: 1, b: 2 }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000'a' in { a: 1, b: 2 }": {
      "entry": "pipeline",
      "src": "'a' in { a: 1, b: 2 }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x in { a: 1, [$.dynKey]: 2 }": {
      "entry": "pipeline",
      "src": "$.x in { a: 1, [$.dynKey]: 2 }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x in { ...$.base, a: 1 }": {
      "entry": "pipeline",
      "src": "$.x in { ...$.base, a: 1 }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x in { ...$.other }": {
      "entry": "pipeline",
      "src": "$.x in { ...$.other }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x in $.list": {
      "entry": "pipeline",
      "src": "$.x in $.list",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a &&": {
      "entry": "pipeline",
      "src": "$.a &&",
      "kind": "message",
      "was": "Unexpected end of expression",
      "now": "Unexpected end of input at position 6",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$.a ? $.b": {
      "entry": "pipeline",
      "src": "$.a ? $.b",
      "kind": "message",
      "was": "Expected ':' in ternary expression at position 9",
      "now": "Expected ':' but got end of input at position 9",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000`hello`": {
      "entry": "pipeline",
      "src": "`hello`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`hello, ${$.name}!`": {
      "entry": "pipeline",
      "src": "`hello, ${$.name}!`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`${$.first} ${$.last}`": {
      "entry": "pipeline",
      "src": "`${$.first} ${$.last}`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`${$.x} px`": {
      "entry": "pipeline",
      "src": "`${$.x} px`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`prefix-${$.id}`": {
      "entry": "pipeline",
      "src": "`prefix-${$.id}`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`total: ${$.a + $.b}`": {
      "entry": "pipeline",
      "src": "`total: ${$.a + $.b}`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`v=${$let({ x: 1 }, x => x)}`": {
      "entry": "pipeline",
      "src": "`v=${$let({ x: 1 }, x => x)}`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`outer ${`inner ${$.x}`}`": {
      "entry": "pipeline",
      "src": "`outer ${`inner ${$.x}`}`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`a\\nb`": {
      "entry": "pipeline",
      "src": "`a\\nb`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`a\\`b\\${c}`": {
      "entry": "pipeline",
      "src": "`a\\`b\\${c}`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`x=${$.x}` + ' done'": {
      "entry": "pipeline",
      "src": "`x=${$.x}` + ' done'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`name=${$.name.toLowerCase()}`": {
      "entry": "pipeline",
      "src": "`name=${$.name.toLowerCase()}`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`n=${42}`": {
      "entry": "pipeline",
      "src": "`n=${42}`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[\"a\", \"b\"].includes($.x)": {
      "entry": "pipeline",
      "src": "[\"a\", \"b\"].includes($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.csv.split(\",\").includes(\"active\")": {
      "entry": "pipeline",
      "src": "$.csv.split(\",\").includes(\"active\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.field.includes($.x)": {
      "entry": "pipeline",
      "src": "$.field.includes($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.senderChain.includes(sc => sc.tier === 2)": {
      "entry": "pipeline",
      "src": "$.senderChain.includes(sc => sc.tier === 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.indexOf(it => it.qty > 5)": {
      "entry": "pipeline",
      "src": "$.items.indexOf(it => it.qty > 5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.min($.a, $.b, $.c)": {
      "entry": "pipeline",
      "src": "Math.min($.a, $.b, $.c)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.max($.a, $.b)": {
      "entry": "pipeline",
      "src": "Math.max($.a, $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.max($.scores)": {
      "entry": "pipeline",
      "src": "Math.max($.scores)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.max(...$.scores)": {
      "entry": "pipeline",
      "src": "Math.max(...$.scores)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.min($.a, ...$.others)": {
      "entry": "pipeline",
      "src": "Math.min($.a, ...$.others)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Date.now()": {
      "entry": "pipeline",
      "src": "Date.now()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.fromEntries(Object.entries($.doc))": {
      "entry": "pipeline",
      "src": "Object.fromEntries(Object.entries($.doc))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.fromEntries([[\"a\", 1], [\"b\", 2]])": {
      "entry": "pipeline",
      "src": "Object.fromEntries([[\"a\", 1], [\"b\", 2]])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Array.isArray($.items)": {
      "entry": "pipeline",
      "src": "Array.isArray($.items)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a?.b": {
      "entry": "pipeline",
      "src": "$.a?.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a?.b?.c": {
      "entry": "pipeline",
      "src": "$.a?.b?.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[...$.a?.b, 'x']": {
      "entry": "pipeline",
      "src": "[...$.a?.b, 'x']",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[...$.a?.b]": {
      "entry": "pipeline",
      "src": "[...$.a?.b]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[...$.moderators, ...$.room?.mods, 'root'].includes($.userId)": {
      "entry": "pipeline",
      "src": "[...$.moderators, ...$.room?.mods, 'root'].includes($.userId)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[...$.a, 'x']": {
      "entry": "pipeline",
      "src": "[...$.a, 'x']",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user?.posts.map(p => p.id)": {
      "entry": "pipeline",
      "src": "$.user?.posts.map(p => p.id)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user?.posts.at(0)": {
      "entry": "pipeline",
      "src": "$.user?.posts.at(0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user?.posts.toReversed()": {
      "entry": "pipeline",
      "src": "$.user?.posts.toReversed()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user?.posts.slice(0, 5)": {
      "entry": "pipeline",
      "src": "$.user?.posts.slice(0, 5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user?.posts.toReversed().includes('hello')": {
      "entry": "pipeline",
      "src": "$.user?.posts.toReversed().includes('hello')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.tags?.includes('vip')": {
      "entry": "pipeline",
      "src": "$.tags?.includes('vip')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name?.trim()": {
      "entry": "pipeline",
      "src": "$.name?.trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user?.name?.trim()": {
      "entry": "pipeline",
      "src": "$.user?.name?.trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user?.name.toUpperCase()": {
      "entry": "pipeline",
      "src": "$.user?.name.toUpperCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user?.csv.split(',')": {
      "entry": "pipeline",
      "src": "$.user?.csv.split(',')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user?.tags.length": {
      "entry": "pipeline",
      "src": "$.user?.tags.length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.firstName + ' ' + $.user?.lastName": {
      "entry": "pipeline",
      "src": "$.firstName + ' ' + $.user?.lastName",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`hello ${$.user?.name}`": {
      "entry": "pipeline",
      "src": "`hello ${$.user?.name}`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.keys($.user?.profile)": {
      "entry": "pipeline",
      "src": "Object.keys($.user?.profile)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.entries($.user?.profile)": {
      "entry": "pipeline",
      "src": "Object.entries($.user?.profile)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.scoresByLevel?.[$.level]": {
      "entry": "pipeline",
      "src": "$.scoresByLevel?.[$.level]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.toReversed()?.[0]": {
      "entry": "pipeline",
      "src": "$.items.toReversed()?.[0]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000({...$.user?.profile, name: 'x'})": {
      "entry": "pipeline",
      "src": "({...$.user?.profile, name: 'x'})",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user?.role === 'admin'": {
      "entry": "pipeline",
      "src": "$.user?.role === 'admin'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user?.role == null": {
      "entry": "pipeline",
      "src": "$.user?.role == null",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.base + $.user?.bonus": {
      "entry": "pipeline",
      "src": "$.base + $.user?.bonus",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => x?.tags)": {
      "entry": "pipeline",
      "src": "$.items.map(x => x?.tags)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.email.startsWith(\"admin\")": {
      "entry": "pipeline",
      "src": "$.email.startsWith(\"admin\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.file.endsWith(\".pdf\")": {
      "entry": "pipeline",
      "src": "$.file.endsWith(\".pdf\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.file.endsWith(\"a👍b\")": {
      "entry": "pipeline",
      "src": "$.file.endsWith(\"a👍b\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.file.endsWith(\"$ext\")": {
      "entry": "pipeline",
      "src": "$.file.endsWith(\"$ext\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.charAt(2)": {
      "entry": "pipeline",
      "src": "$.name.charAt(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.charAt(-1)": {
      "entry": "pipeline",
      "src": "$.name.charAt(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.charAt($.i)": {
      "entry": "pipeline",
      "src": "$.name.charAt($.i)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[\"a\", \"b\", \"c\"].indexOf($.x)": {
      "entry": "pipeline",
      "src": "[\"a\", \"b\", \"c\"].indexOf($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.email.toLowerCase().indexOf(\"@\")": {
      "entry": "pipeline",
      "src": "$.email.toLowerCase().indexOf(\"@\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.email.indexOf(\"@\")": {
      "entry": "pipeline",
      "src": "$.email.indexOf(\"@\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1, 2].concat([3, 4])": {
      "entry": "pipeline",
      "src": "[1, 2].concat([3, 4])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.first.trim().concat($.last)": {
      "entry": "pipeline",
      "src": "$.first.trim().concat($.last)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.parts.concat($.tail)": {
      "entry": "pipeline",
      "src": "$.parts.concat($.tail)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.tags.join()": {
      "entry": "pipeline",
      "src": "$.tags.join()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.tags.join(\" | \")": {
      "entry": "pipeline",
      "src": "$.tags.join(\" | \")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.nested.flat()": {
      "entry": "pipeline",
      "src": "$.nested.flat()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.nested.flat(1)": {
      "entry": "pipeline",
      "src": "$.nested.flat(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.nested.flat(2)": {
      "entry": "pipeline",
      "src": "$.nested.flat(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.docs.flatMap(d => d.tags)": {
      "entry": "pipeline",
      "src": "$.docs.flatMap(d => d.tags)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.getTime()": {
      "entry": "pipeline",
      "src": "$.ts.getTime()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.ts.toISOString()": {
      "entry": "pipeline",
      "src": "$.ts.toISOString()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.sign($.x)": {
      "entry": "pipeline",
      "src": "Math.sign($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.log2($.x)": {
      "entry": "pipeline",
      "src": "Math.log2($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.log10($.x)": {
      "entry": "pipeline",
      "src": "Math.log10($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.cbrt($.x)": {
      "entry": "pipeline",
      "src": "Math.cbrt($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.hypot($.a, $.b)": {
      "entry": "pipeline",
      "src": "Math.hypot($.a, $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.random()": {
      "entry": "pipeline",
      "src": "Math.random()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.PI": {
      "entry": "pipeline",
      "src": "Math.PI",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.E": {
      "entry": "pipeline",
      "src": "Math.E",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$abs(1_000_000)": {
      "entry": "pipeline",
      "src": "$abs(1_000_000)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$abs(1_234.567_89)": {
      "entry": "pipeline",
      "src": "$abs(1_234.567_89)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$abs(1_2e3)": {
      "entry": "pipeline",
      "src": "$abs(1_2e3)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u00001_": {
      "entry": "pipeline",
      "src": "1_",
      "kind": "message",
      "was": "Numeric separator '_' must be between two digits (at position 1)",
      "now": "Numeric separator '_' must be between two digits at position 1",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u00001__0": {
      "entry": "pipeline",
      "src": "1__0",
      "kind": "message",
      "was": "Numeric separator '_' must be between two digits (at position 1)",
      "now": "Numeric separator '_' must be between two digits at position 1",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$.a // tail\n+ $.b": {
      "entry": "pipeline",
      "src": "$.a // tail\n+ $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$abs($.x) // trailing comment": {
      "entry": "pipeline",
      "src": "$abs($.x) // trailing comment",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a // x\r+ $.b": {
      "entry": "pipeline",
      "src": "$.a // x\r+ $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a // x\r\n+ $.b": {
      "entry": "pipeline",
      "src": "$.a // x\r\n+ $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a /* mid */ + $.b": {
      "entry": "pipeline",
      "src": "$.a /* mid */ + $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a /*\n  spans\n  lines\n*/ + $.b": {
      "entry": "pipeline",
      "src": "$.a /*\n  spans\n  lines\n*/ + $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a /**/ + $.b": {
      "entry": "pipeline",
      "src": "$.a /**/ + $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a // one\n  /* two */ \n // three\n + $.b": {
      "entry": "pipeline",
      "src": "$.a // one\n  /* two */ \n // three\n + $.b",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000`hi ${ $.name /* user */ }`": {
      "entry": "pipeline",
      "src": "`hi ${ $.name /* user */ }`",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$eq($.url, \"https://example.com\")": {
      "entry": "pipeline",
      "src": "$eq($.url, \"https://example.com\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.path.match(/[/\\\\]/)": {
      "entry": "pipeline",
      "src": "$.path.match(/[/\\\\]/)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.path.match(/* skip */ /foo/i)": {
      "entry": "pipeline",
      "src": "$.path.match(/* skip */ /foo/i)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u000010 /* skip */ / 2": {
      "entry": "pipeline",
      "src": "10 /* skip */ / 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a /* unclosed": {
      "entry": "pipeline",
      "src": "$.a /* unclosed",
      "kind": "message",
      "was": "Unclosed block comment starting at position 4",
      "now": "Unterminated block comment at position 4",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$foo({ [$.k]: 1 })": {
      "entry": "pipeline",
      "src": "$foo({ [$.k]: 1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$arrayToObject($.pairs)": {
      "entry": "pipeline",
      "src": "$arrayToObject($.pairs)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$foo({ a: 1, [$.k]: 2 })": {
      "entry": "pipeline",
      "src": "$foo({ a: 1, [$.k]: 2 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.assign(...$.docs)": {
      "entry": "pipeline",
      "src": "Object.assign(...$.docs)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$concatArrays(...$.arrs)": {
      "entry": "pipeline",
      "src": "$concatArrays(...$.arrs)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$mergeObjects(...$.docs)": {
      "entry": "pipeline",
      "src": "$mergeObjects(...$.docs)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => ({ x }))": {
      "entry": "pipeline",
      "src": "$.items.map(x => ({ x }))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => ({ x, x }))": {
      "entry": "pipeline",
      "src": "$.items.map(x => ({ x, x }))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000({ foo })": {
      "entry": "pipeline",
      "src": "({ foo })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$round($.price)": {
      "entry": "pipeline",
      "src": "$round($.price)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$round($.price, 2)": {
      "entry": "pipeline",
      "src": "$round($.price, 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$trunc($.value)": {
      "entry": "pipeline",
      "src": "$trunc($.value)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$trunc($.value, 1)": {
      "entry": "pipeline",
      "src": "$trunc($.value, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$min($.scores)": {
      "entry": "pipeline",
      "src": "$min($.scores)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$min($.a, $.b, $.c)": {
      "entry": "pipeline",
      "src": "$min($.a, $.b, $.c)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$max($.a, $.b)": {
      "entry": "pipeline",
      "src": "$max($.a, $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$avg($.values)": {
      "entry": "pipeline",
      "src": "$avg($.values)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$avg($.a, $.b, $.c)": {
      "entry": "pipeline",
      "src": "$avg($.a, $.b, $.c)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$sum($.amounts)": {
      "entry": "pipeline",
      "src": "$sum($.amounts)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$sum($.a, $.b)": {
      "entry": "pipeline",
      "src": "$sum($.a, $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$stdDevPop($.measurements)": {
      "entry": "pipeline",
      "src": "$stdDevPop($.measurements)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$stdDevSamp($.a, $.b, $.c)": {
      "entry": "pipeline",
      "src": "$stdDevSamp($.a, $.b, $.c)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$mergeObjects($.docs)": {
      "entry": "pipeline",
      "src": "$mergeObjects($.docs)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$mergeObjects($.a, $.b)": {
      "entry": "pipeline",
      "src": "$mergeObjects($.a, $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$min(...$.scores)": {
      "entry": "pipeline",
      "src": "$min(...$.scores)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$max($.first, ...$.rest)": {
      "entry": "pipeline",
      "src": "$max($.first, ...$.rest)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$min()": {
      "entry": "pipeline",
      "src": "$min()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$mergeObjects({ a: 1, b: $.x })": {
      "entry": "pipeline",
      "src": "$mergeObjects({ a: 1, b: $.x })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$round($.price * 1.1, 2)": {
      "entry": "pipeline",
      "src": "$round($.price * 1.1, 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.status === \"active\"": {
      "entry": "pipeline",
      "src": "$.status === \"active\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateDiff({ startDate: $.a, endDate: $.b, unit: \"day\" })": {
      "entry": "pipeline",
      "src": "$dateDiff({ startDate: $.a, endDate: $.b, unit: \"day\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"$foo\"": {
      "entry": "pipeline",
      "src": "\"$foo\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"$$NOW\"": {
      "entry": "pipeline",
      "src": "\"$$NOW\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"hello\"": {
      "entry": "pipeline",
      "src": "\"hello\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"\"": {
      "entry": "pipeline",
      "src": "\"\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1, \"$foo\", \"bar\"]": {
      "entry": "pipeline",
      "src": "[1, \"$foo\", \"bar\"]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000({ x: \"$foo\", y: \"bar\" })": {
      "entry": "pipeline",
      "src": "({ x: \"$foo\", y: \"bar\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000({ \"$foo\": 1 })": {
      "entry": "pipeline",
      "src": "({ \"$foo\": 1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$concat(\"$first\", \" \", \"$last\")": {
      "entry": "pipeline",
      "src": "$concat(\"$first\", \" \", \"$last\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$concat($.first, $.last)": {
      "entry": "pipeline",
      "src": "$concat($.first, $.last)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$literal(\"$foo\")": {
      "entry": "pipeline",
      "src": "$literal(\"$foo\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$literal({ x: \"$foo\" })": {
      "entry": "pipeline",
      "src": "$literal({ x: \"$foo\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$literal([\"$a\", \"$b\"])": {
      "entry": "pipeline",
      "src": "$literal([\"$a\", \"$b\"])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$hash($.password, \"sha256\")": {
      "entry": "pipeline",
      "src": "$hash($.password, \"sha256\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$hexHash({ input: $.token, algorithm: \"sha512\" })": {
      "entry": "pipeline",
      "src": "$hexHash({ input: $.token, algorithm: \"sha512\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$function({ body: \"function(x) { return x * 2; }\", args: [$.value], lang: \"js\" })": {
      "entry": "pipeline",
      "src": "$function({ body: \"function(x) { return x * 2; }\", args: [$.value], lang: \"js\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$accumulator({ init: \"function() {}\", accumulate: \"function() {}\", merge: \"function() {}\", lang: \"js\" })": {
      "entry": "pipeline",
      "src": "$accumulator({ init: \"function() {}\", accumulate: \"function() {}\", merge: \"function() {}\", lang: \"js\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$median($.scores, \"approximate\")": {
      "entry": "pipeline",
      "src": "$median($.scores, \"approximate\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$encStrContains($.encField, \"secret\")": {
      "entry": "pipeline",
      "src": "$encStrContains($.encField, \"secret\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$encStrStartsWith({ input: $.encField, prefix: \"abc\" })": {
      "entry": "pipeline",
      "src": "$encStrStartsWith({ input: $.encField, prefix: \"abc\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$encStrEndsWith($.encField, \"xyz\")": {
      "entry": "pipeline",
      "src": "$encStrEndsWith($.encField, \"xyz\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$encStrNormalizedEq($.encField, \"compare\")": {
      "entry": "pipeline",
      "src": "$encStrNormalizedEq($.encField, \"compare\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$rank()": {
      "entry": "pipeline",
      "src": "$rank()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.status === 'shipped'": {
      "entry": "pipeline",
      "src": "$.status === 'shipped'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.status === 'active' && $.age >= 18": {
      "entry": "pipeline",
      "src": "$.status === 'active' && $.age >= 18",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.address.country === 'AU'": {
      "entry": "pipeline",
      "src": "$.address.country === 'AU'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$add($.a, $.b)": {
      "entry": "pipeline",
      "src": "$add($.a, $.b)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.name.trim() === 'alice'": {
      "entry": "pipeline",
      "src": "$.name.trim() === 'alice'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000{ age: { $gt: 18 } }": {
      "entry": "pipeline",
      "src": "{ age: { $gt: 18 } }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000{ a: 1 }": {
      "entry": "pipeline",
      "src": "{ a: 1 }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000{ a: 1, b: 'x' }": {
      "entry": "pipeline",
      "src": "{ a: 1, b: 'x' }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$match($.age > 18)": {
      "entry": "pipeline",
      "src": "$match($.age > 18)",
      "kind": "output",
      "was": "[{\"$match\":{\"age\":{\"$gt\":18}}}]",
      "now": "[{\"$match\":{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000{ $match: $.age > 18 }": {
      "entry": "pipeline",
      "src": "{ $match: $.age > 18 }",
      "kind": "output",
      "was": "[{\"$match\":{\"age\":{\"$gt\":18}}}]",
      "now": "[{\"$match\":{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$match($.age > 18);": {
      "entry": "pipeline",
      "src": "$match($.age > 18);",
      "kind": "output",
      "was": "[{\"$match\":{\"age\":{\"$gt\":18}}}]",
      "now": "[{\"$match\":{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.status === 'active' && $.name.trim() === 'alice'": {
      "entry": "pipeline",
      "src": "$.status === 'active' && $.name.trim() === 'alice'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$match($.age > 18); $sort({ age: 1 })": {
      "entry": "pipeline",
      "src": "$match($.age > 18); $sort({ age: 1 })",
      "kind": "output",
      "was": "[{\"$match\":{\"age\":{\"$gt\":18}}},{\"$sort\":{\"age\":1}}]",
      "now": "[{\"$match\":{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}},{\"$sort\":{\"age\":1}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.age > 18;": {
      "entry": "pipeline",
      "src": "$.age > 18;",
      "kind": "message",
      "was": "Element 0 of pipeline is not a stage call. To filter documents on a predicate, wrap it as `$match(...)` — e.g. `$match($.age > 18)`. Pipeline statements must be stage calls; available stages: $addFields, $bucket, $bucket",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000   $.age > 18;": {
      "entry": "pipeline",
      "src": "   $.age > 18;",
      "kind": "message",
      "was": "Element 0 of pipeline is not a stage call. To filter documents on a predicate, wrap it as `$match(...)` — e.g. `$match($.age > 18)`. Pipeline statements must be stage calls; available stages: $addFields, $bucket, $bucket",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000$match($.age > 18); $sort({ age: 1 });": {
      "entry": "pipeline",
      "src": "$match($.age > 18); $sort({ age: 1 });",
      "kind": "output",
      "was": "[{\"$match\":{\"age\":{\"$gt\":18}}},{\"$sort\":{\"age\":1}}]",
      "now": "[{\"$match\":{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}},{\"$sort\":{\"age\":1}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$match($.a === 0)": {
      "entry": "pipeline",
      "src": "$match($.a === 0)",
      "kind": "output",
      "was": "[{\"$match\":{\"a\":0}}]",
      "now": "[{\"$match\":{\"a\":{\"$eq\":0,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.a === 0": {
      "entry": "pipeline",
      "src": "$.a === 0",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$$.foo": {
      "entry": "pipeline",
      "src": "$$.foo",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000$$[\"foo\"]": {
      "entry": "pipeline",
      "src": "$$[\"foo\"]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000$$$.myColl": {
      "entry": "pipeline",
      "src": "$$$.myColl",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000$$$[\"coll\"]": {
      "entry": "pipeline",
      "src": "$$$[\"coll\"]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000$$$$.myDb.myColl": {
      "entry": "pipeline",
      "src": "$$$$.myDb.myColl",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000  $$$$.db.coll": {
      "entry": "pipeline",
      "src": "  $$$$.db.coll",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000$$": {
      "entry": "pipeline",
      "src": "$$",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000$$foo": {
      "entry": "pipeline",
      "src": "$$foo",
      "kind": "message",
      "was": "Expected '.<name>' or '[<expr>]' after '$$' at position 0",
      "now": "Expected ';' but got 'foo' at position 2",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$$$": {
      "entry": "pipeline",
      "src": "$$$",
      "kind": "message",
      "was": "Expected '.<name>' or '[<expr>]' after '$$$' at position 0",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000$$$$": {
      "entry": "pipeline",
      "src": "$$$$",
      "kind": "message",
      "was": "Expected '.<name>' or '[<expr>]' after '$$$$' at position 0",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000Math.max(1, 2,)": {
      "entry": "pipeline",
      "src": "Math.max(1, 2,)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.max(1, 2)": {
      "entry": "pipeline",
      "src": "Math.max(1, 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => x * 1.1,)": {
      "entry": "pipeline",
      "src": "$.items.map(x => x * 1.1,)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => x * 1.1)": {
      "entry": "pipeline",
      "src": "$.items.map(x => x * 1.1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$add(1, 2,)": {
      "entry": "pipeline",
      "src": "$add(1, 2,)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$add(1, 2)": {
      "entry": "pipeline",
      "src": "$add(1, 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateTrunc({ date: $.t, unit: \"week\", },)": {
      "entry": "pipeline",
      "src": "$dateTrunc({ date: $.t, unit: \"week\", },)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateTrunc({ date: $.t, unit: \"week\" })": {
      "entry": "pipeline",
      "src": "$dateTrunc({ date: $.t, unit: \"week\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Number($.x,)": {
      "entry": "pipeline",
      "src": "Number($.x,)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Number($.x)": {
      "entry": "pipeline",
      "src": "Number($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Number($.x, $.y)": {
      "entry": "pipeline",
      "src": "Number($.x, $.y)",
      "kind": "message",
      "was": "Type cast 'Number()' takes exactly 1 argument at position 0",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Number.isInteger($.x,)": {
      "entry": "pipeline",
      "src": "Number.isInteger($.x,)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Number.isInteger($.x)": {
      "entry": "pipeline",
      "src": "Number.isInteger($.x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.keys($.a,)": {
      "entry": "pipeline",
      "src": "Object.keys($.a,)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.keys($.a)": {
      "entry": "pipeline",
      "src": "Object.keys($.a)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date(2020, 1, 1,)": {
      "entry": "pipeline",
      "src": "new Date(2020, 1, 1,)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Date(2020, 1, 1)": {
      "entry": "pipeline",
      "src": "new Date(2020, 1, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Set([1, 2],)": {
      "entry": "pipeline",
      "src": "new Set([1, 2],)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new Set([1, 2])": {
      "entry": "pipeline",
      "src": "new Set([1, 2])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Date.UTC(2020, 1,)": {
      "entry": "pipeline",
      "src": "Date.UTC(2020, 1,)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Date.UTC(2020, 1)": {
      "entry": "pipeline",
      "src": "Date.UTC(2020, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Array.from({ length: 5 },)": {
      "entry": "pipeline",
      "src": "Array.from({ length: 5 },)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Array.from({ length: 5 }, (_, i) => i,)": {
      "entry": "pipeline",
      "src": "Array.from({ length: 5 }, (_, i) => i,)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Array.from({ length: 5 }, (_, i) => i)": {
      "entry": "pipeline",
      "src": "Array.from({ length: 5 }, (_, i) => i)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1, 2, 3,]": {
      "entry": "pipeline",
      "src": "[1, 2, 3,]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1, 2, 3]": {
      "entry": "pipeline",
      "src": "[1, 2, 3]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$add([$.a, $.b,])": {
      "entry": "pipeline",
      "src": "$add([$.a, $.b,])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$add([$.a, $.b])": {
      "entry": "pipeline",
      "src": "$add([$.a, $.b])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.reduce((acc, x,) => acc + x, 0)": {
      "entry": "pipeline",
      "src": "$.a.reduce((acc, x,) => acc + x, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$match($.amount > 100 && ($.currency === \"USD\" || $.currency === \"EUR\") && $.status === \"active\",)": {
      "entry": "pipeline",
      "src": "$match($.amount > 100 && ($.currency === \"USD\" || $.currency === \"EUR\") && $.status === \"active\",)",
      "kind": "output",
      "was": "[{\"$match\":{\"amount\":{\"$gt\":100},\"$or\":[{\"currency\":\"USD\"},{\"currency\":\"EUR\"}],\"status\":\"active\"}}]",
      "now": "[{\"$match\":{\"amount\":{\"$gt\":100,\"$not\":{\"$type\":\"array\"}},\"$or\":[{\"currency\":{\"$eq\":\"USD\",\"$not\":{\"$type\":\"array\"}}},{\"currency\":{\"$eq\":\"EUR\",\"$not\":{\"$type\":\"array\"}}}],\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}}",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$match($.amount > 100 && ($.currency === \"USD\" || $.currency === \"EUR\") && $.status === \"active\")": {
      "entry": "pipeline",
      "src": "$match($.amount > 100 && ($.currency === \"USD\" || $.currency === \"EUR\") && $.status === \"active\")",
      "kind": "output",
      "was": "[{\"$match\":{\"amount\":{\"$gt\":100},\"$or\":[{\"currency\":\"USD\"},{\"currency\":\"EUR\"}],\"status\":\"active\"}}]",
      "now": "[{\"$match\":{\"amount\":{\"$gt\":100,\"$not\":{\"$type\":\"array\"}},\"$or\":[{\"currency\":{\"$eq\":\"USD\",\"$not\":{\"$type\":\"array\"}}},{\"currency\":{\"$eq\":\"EUR\",\"$not\":{\"$type\":\"array\"}}}],\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}}",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.r.map(d => d.l.slice(d.i))": {
      "entry": "pipeline",
      "src": "$.r.map(d => d.l.slice(d.i))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.take($.n)": {
      "entry": "pipeline",
      "src": "$.a.take($.n)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$year(5)": {
      "entry": "pipeline",
      "src": "$year(5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$year($.d)": {
      "entry": "pipeline",
      "src": "$year($.d)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$._id === ObjectId(\"507f1f77bcf86cd79943901\")": {
      "entry": "pipeline",
      "src": "$._id === ObjectId(\"507f1f77bcf86cd79943901\")",
      "kind": "message",
      "was": "ObjectId(\"507f1f77bcf86cd79943901\") is not a valid ObjectId: expected exactly 24 hexadecimal characters, got 23 characters, at position 19.",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$._id === ObjectId(\"zzzf1f77bcf86cd799439011\")": {
      "entry": "pipeline",
      "src": "$._id === ObjectId(\"zzzf1f77bcf86cd799439011\")",
      "kind": "message",
      "was": "ObjectId(\"zzzf1f77bcf86cd799439011\") is not a valid ObjectId: expected exactly 24 hexadecimal characters, it contains non-hexadecimal characters, at position 19.",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000ObjectId()": {
      "entry": "pipeline",
      "src": "ObjectId()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new ObjectId()": {
      "entry": "pipeline",
      "src": "new ObjectId()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000ObjectId($.idStr)": {
      "entry": "pipeline",
      "src": "ObjectId($.idStr)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$._id === ObjectId($.idStr)": {
      "entry": "pipeline",
      "src": "$._id === ObjectId($.idStr)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$._id === ObjectId(\"nothex\")": {
      "entry": "pipeline",
      "src": "$._id === ObjectId(\"nothex\")",
      "kind": "message",
      "was": "ObjectId(\"nothex\") is not a valid ObjectId: expected exactly 24 hexadecimal characters, got 6 characters, at position 19.",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$._id === 0x507f_1f77_bcf8_6cd7_9943_9011": {
      "entry": "pipeline",
      "src": "$._id === 0x507f_1f77_bcf8_6cd7_9943_9011",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u00000XABCDEF781234567812345678": {
      "entry": "pipeline",
      "src": "0XABCDEF781234567812345678",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u00000xff": {
      "entry": "pipeline",
      "src": "0xff",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u00000x1FFFFFFFFFFFFF": {
      "entry": "pipeline",
      "src": "0x1FFFFFFFFFFFFF",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u00000x000000000000000000000001": {
      "entry": "pipeline",
      "src": "0x000000000000000000000001",
      "kind": "message",
      "was": "ObjectId 000000000000000000000001 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 000000000000000000000001 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000{ _id: 0x123456781234567812345678 }": {
      "entry": "pipeline",
      "src": "{ _id: 0x123456781234567812345678 }",
      "kind": "message",
      "was": "ObjectId 123456781234567812345678 looks like a typo: its embedded timestamp decodes to 1979-09-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 123456781234567812345678 looks like a typo: its embedded timestamp decodes to 1979-09-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000ObjectId(\"123456781234567812345678\")": {
      "entry": "pipeline",
      "src": "ObjectId(\"123456781234567812345678\")",
      "kind": "message",
      "was": "ObjectId 123456781234567812345678 looks like a typo: its embedded timestamp decodes to 1979-09-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000new ObjectId(\"123456781234567812345678\")": {
      "entry": "pipeline",
      "src": "new ObjectId(\"123456781234567812345678\")",
      "kind": "message",
      "was": "ObjectId 123456781234567812345678 looks like a typo: its embedded timestamp decodes to 1979-09-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u00000x000000000000000000000000": {
      "entry": "pipeline",
      "src": "0x000000000000000000000000",
      "kind": "message",
      "was": "ObjectId 000000000000000000000000 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 000000000000000000000000 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u00000x4a0000000000000000000000": {
      "entry": "pipeline",
      "src": "0x4a0000000000000000000000",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u00000x4a0000000000000000000001": {
      "entry": "pipeline",
      "src": "0x4a0000000000000000000001",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u00000x49ffffffffffffffffffffff": {
      "entry": "pipeline",
      "src": "0x49ffffffffffffffffffffff",
      "kind": "message",
      "was": "ObjectId 49ffffffffffffffffffffff looks like a typo: its embedded timestamp decodes to 2009-05-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 49ffffffffffffffffffffff looks like a typo: its embedded timestamp decodes to 2009-05-05, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$._id === 0x000000000000000000000001": {
      "entry": "pipeline",
      "src": "$._id === 0x000000000000000000000001",
      "kind": "message",
      "was": "ObjectId 000000000000000000000001 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release).",
      "now": "ObjectId 000000000000000000000001 looks like a typo: its embedded timestamp decodes to 1970-01-01, older than the smallest valid ObjectId 4a0000000000000000000000 (2009-05-05, around MongoDB's first release). at position",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$arrayToObject([[\"a\", 1], [\"b\", 2]])": {
      "entry": "pipeline",
      "src": "$arrayToObject([[\"a\", 1], [\"b\", 2]])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000 instead, and the expression from ": {
      "entry": "pipeline",
      "src": " instead, and the expression from ",
      "kind": "message",
      "was": "Unexpected token ',' at position 8",
      "now": "Expected ';' but got ',' at position 8",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000 on the predicate.\n    expect(() => jsmql.expr(\"$match($.a === 0)\")).toThrow(\n      /'\\$match' is a pipeline stage, not an expression.*For the value-position equivalent, use '\\$filter\\(…\\)'/s,\n    );\n    expect(jsmql.pipeline(\"$match($.a === 0)\")).toEqual([{ $match: { a: 0 } }]);\n    expect(jsmql.expr(\"$.a === 0\")).toEqual({ $eq: [\"$a\", 0] });\n  });\n\n  it(\"rejects wrong-typed input with a TypeError naming the entry point\", () => {\n    expect(() => (jsmql.expr as (x: unknown) => unknown)(42)).toThrow(/jsmql\\.expr\\(\\) expects a string/);\n  });\n});\n\ndescribe(\"context-reference prefixes ($$, $$$, $$$$)\", () => {\n  // Three new prefix levels. Lex + parse succeed; codegen throws a reserved-syntax\n  // error (semantics deferred — see docs/specs/context-references.md). Both dot-ident\n  // (": {
      "entry": "pipeline",
      "src": " on the predicate.\n    expect(() => jsmql.expr(\"$match($.a === 0)\")).toThrow(\n      /'\\$match' is a pipeline stage, not an expression.*For the value-position equivalent, use '\\$filter\\(…\\)'/s,\n    );\n    expect(jsmql.pipeline(\"$match($.a === 0)\")).toEqual([{ $match: { a: 0 } }]);\n    expect(jsmql.expr(\"$.a === 0\")).toEqual({ $eq: [\"$a\", 0] });\n  });\n\n  it(\"rejects wrong-typed input with a TypeError naming the entry point\", () => {\n    expect(() => (jsmql.expr as (x: unknown) => unknown)(42)).toThrow(/jsmql\\.expr\\(\\) expects a string/);\n  });\n});\n\ndescribe(\"context-reference prefixes ($$, $$$, $$$$)\", () => {\n  // Three new prefix levels. Lex + parse succeed; codegen throws a reserved-syntax\n  // error (semantics deferred — see docs/specs/context-references.md). Both dot-ident\n  // (",
      "kind": "message",
      "was": "Unexpected token 'the' at position 4",
      "now": "Expected ';' but got 'the' at position 4",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000 prefix on a compiler-emitted ": {
      "entry": "pipeline",
      "src": " prefix on a compiler-emitted ",
      "kind": "message",
      "was": "Unexpected token 'on' at position 8",
      "now": "Expected ';' but got 'on' at position 8",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000const userId = 0x507f1f77bcf86cd799439011; $.userId === userId": {
      "entry": "pipeline",
      "src": "const userId = 0x507f1f77bcf86cd799439011; $.userId === userId",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const msInDay = 24*60*60*1000; $.elapsedMs > msInDay": {
      "entry": "pipeline",
      "src": "const msInDay = 24*60*60*1000; $.elapsedMs > msInDay",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const limit = 2**32; $.n < limit": {
      "entry": "pipeline",
      "src": "const limit = 2**32; $.n < limit",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const status = \"active\"; $.status === status": {
      "entry": "pipeline",
      "src": "const status = \"active\"; $.status === status",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const base = 10; const doubled = base * 2; $.n < doubled": {
      "entry": "pipeline",
      "src": "const base = 10; const doubled = base * 2; $.n < doubled",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const bad = [\"cancelled\", \"rejected\"]; $.status in bad": {
      "entry": "pipeline",
      "src": "const bad = [\"cancelled\", \"rejected\"]; $.status in bad",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const base = { a: 1 }; const cfg = { ...base, b: 2 }; $.cfg === cfg": {
      "entry": "pipeline",
      "src": "const base = { a: 1 }; const cfg = { ...base, b: 2 }; $.cfg === cfg",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const region = `us-east`; const key = `region:${region}`; $.key === key": {
      "entry": "pipeline",
      "src": "const region = `us-east`; const key = `region:${region}`; $.key === key",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const n = [10, 20, 30].length; $.count === n": {
      "entry": "pipeline",
      "src": "const n = [10, 20, 30].length; $.count === n",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const first = [10, 20, 30][0]; $.first === first": {
      "entry": "pipeline",
      "src": "const first = [10, 20, 30][0]; $.first === first",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const minAge = 18; $match($.age > minAge); $sort({ age: -1 })": {
      "entry": "pipeline",
      "src": "const minAge = 18; $match($.age > minAge); $sort({ age: -1 })",
      "kind": "output",
      "was": "[{\"$match\":{\"age\":{\"$gt\":18}}},{\"$sort\":{\"age\":-1}}]",
      "now": "[{\"$match\":{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}},{\"$sort\":{\"age\":-1}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000const big = 123n; $match($.n === big)": {
      "entry": "pipeline",
      "src": "const big = 123n; $match($.n === big)",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.big\":{\"$toLong\":\"123\"}}},{\"$match\":{\"$expr\":{\"$eq\":[\"$n\",\"$__jsmql.var.big\"]}}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[\"$n\",{\"$toLong\":\"123\"}]}}}]",
      "reason": "intended: a BigInt constant keeps its long-ness through the fold: the value target emits `$toLong` of the digits, which is the value the developer wrote. Measured equal on the server."
    },
    "pipeline\u0000const x = 5;": {
      "entry": "pipeline",
      "src": "const x = 5;",
      "kind": "message",
      "was": "A `const`/`let` declaration on its own produces no query — nothing reads the constant. Add a statement that uses it (a predicate, or a stage like `$match(...)`), or remove the declaration.",
      "now": "This program produces no stages, so it would leave the documents untouched. Write at least one statement that reads or changes them.",
      "reason": "message: both refuse a program that emits nothing; the new wording says what the program would do rather than naming the entry point."
    },
    "pipeline\u0000const x = 1/0; $.n < x": {
      "entry": "pipeline",
      "src": "const x = 1/0; $.n < x",
      "kind": "message",
      "was": "This constant expression evaluates to Infinity, which has no MongoDB literal. Check the arithmetic (e.g. division by zero, or an out-of-range exponent).",
      "now": "This constant expression evaluates to Infinity, which has no MongoDB literal. Check the arithmetic — a division by zero, or an exponent out of range. at position 0",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000const x = 0/0; $.n < x": {
      "entry": "pipeline",
      "src": "const x = 0/0; $.n < x",
      "kind": "message",
      "was": "This constant expression evaluates to NaN, which has no MongoDB literal. Check the arithmetic (e.g. division by zero, or an out-of-range exponent).",
      "now": "This constant expression evaluates to NaN, which has no MongoDB literal. Check the arithmetic — a division by zero, or an exponent out of range. at position 0",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000const bad = [\"cancelled\", \"rejected\", \"returned\"].map(s => s.toUpperCase()); $.status in bad": {
      "entry": "pipeline",
      "src": "const bad = [\"cancelled\", \"rejected\", \"returned\"].map(s => s.toUpperCase()); $.status in bad",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const evens = [1, 2, 3, 4, 5, 6].filter(n => n % 2 === 0); $.k in evens": {
      "entry": "pipeline",
      "src": "const evens = [1, 2, 3, 4, 5, 6].filter(n => n % 2 === 0); $.k in evens",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const total = [1, 2, 3, 4].reduce((a, b) => a + b, 0); $.n === total": {
      "entry": "pipeline",
      "src": "const total = [1, 2, 3, 4].reduce((a, b) => a + b, 0); $.n === total",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const xs = [1, 2, 3, 4].map(x => x * 10).filter(x => x > 15); $.v in xs": {
      "entry": "pipeline",
      "src": "const xs = [1, 2, 3, 4].map(x => x * 10).filter(x => x > 15); $.v in xs",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const up = \"active\".toUpperCase(); $.s === up": {
      "entry": "pipeline",
      "src": "const up = \"active\".toUpperCase(); $.s === up",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const parts = \"a,b,c\".split(\",\"); $.x in parts": {
      "entry": "pipeline",
      "src": "const parts = \"a,b,c\".split(\",\"); $.x in parts",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const recent = [1, 2, 3, 4, 5].slice(-3); $.k in recent": {
      "entry": "pipeline",
      "src": "const recent = [1, 2, 3, 4, 5].slice(-3); $.k in recent",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const page = [1, 2, 3, 4, 5, 6].slice(1, 3); $.k in page": {
      "entry": "pipeline",
      "src": "const page = [1, 2, 3, 4, 5, 6].slice(1, 3); $.k in page",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const empty = [1, 2, 3].slice(2, 1); $.k in empty": {
      "entry": "pipeline",
      "src": "const empty = [1, 2, 3].slice(2, 1); $.k in empty",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000let webhookMessage = \"time elapsed\"; let webhookType = webhookMessage.snakeCase(); $.type === webhookType": {
      "entry": "pipeline",
      "src": "let webhookMessage = \"time elapsed\"; let webhookType = webhookMessage.snakeCase(); $.type === webhookType",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const k = \"order_total\".camelCase(); $.f === k": {
      "entry": "pipeline",
      "src": "const k = \"order_total\".camelCase(); $.f === k",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const k = \"orderTotal\".kebabCase(); $.f === k": {
      "entry": "pipeline",
      "src": "const k = \"orderTotal\".kebabCase(); $.f === k",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const k = \"hello world\".startCase(); $.f === k": {
      "entry": "pipeline",
      "src": "const k = \"hello world\".startCase(); $.f === k",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const k = \"hELLO\".capitalize(); $.f === k": {
      "entry": "pipeline",
      "src": "const k = \"hELLO\".capitalize(); $.f === k",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const k = '<a href=\"x\">'.escape(); $.f === k": {
      "entry": "pipeline",
      "src": "const k = '<a href=\"x\">'.escape(); $.f === k",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const k = \"the quick brown fox\".truncate({ length: 12 }); $.f === k": {
      "entry": "pipeline",
      "src": "const k = \"the quick brown fox\".truncate({ length: 12 }); $.f === k",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const w = \"fooBar-baz 9\".words(); $.tags === w": {
      "entry": "pipeline",
      "src": "const w = \"fooBar-baz 9\".words(); $.tags === w",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000const first = \"fooBar-baz 9\".words()[0]; $.f === first": {
      "entry": "pipeline",
      "src": "const first = \"fooBar-baz 9\".words()[0]; $.f === first",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => { const factor = 2; return x * factor })": {
      "entry": "pipeline",
      "src": "$.items.map(x => { const factor = 2; return x * factor })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => { const dbl = x * 2; return dbl + 1 })": {
      "entry": "pipeline",
      "src": "$.items.map(x => { const dbl = x * 2; return dbl + 1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => { const x = 99; return x })": {
      "entry": "pipeline",
      "src": "$.items.map(x => { const x = 99; return x })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(x => { const bump = 10; const y = x + bump; return y })": {
      "entry": "pipeline",
      "src": "$.items.map(x => { const bump = 10; const y = x + bump; return y })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$match($.x > 0); $sort({ x: 1 })": {
      "entry": "pipeline",
      "src": "$match($.x > 0); $sort({ x: 1 })",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":{\"$gt\":0}}},{\"$sort\":{\"x\":1}}]",
      "now": "[{\"$match\":{\"x\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}},{\"$sort\":{\"x\":1}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000((_) => 1)(2)": {
      "entry": "pipeline",
      "src": "((_) => 1)(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$ = { a: $.xs.map(x => { const g = z => z + 1; return g(x); }) };": {
      "entry": "pipeline",
      "src": "$ = { a: $.xs.map(x => { const g = z => z + 1; return g(x); }) };",
      "kind": "message",
      "was": "Reusable functions must be declared at the top level of a pipeline, not inside an arrow body. Move `const g = (…) => …` out of the `=> { … }` block.",
      "now": "A callback block with a 'return' may only declare values before it at position 47",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$.items.map((x) => x * 2)": {
      "entry": "pipeline",
      "src": "$.items.map((x) => x * 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(function (x) { return x * 2 })": {
      "entry": "pipeline",
      "src": "$.items.map(function (x) { return x * 2 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(function scale(x) { return x * 2 })": {
      "entry": "pipeline",
      "src": "$.items.map(function scale(x) { return x * 2 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map((x) => { const y = x + 1; return y * 2 })": {
      "entry": "pipeline",
      "src": "$.items.map((x) => { const y = x + 1; return y * 2 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.map(function (x) { const y = x + 1; return y * 2 })": {
      "entry": "pipeline",
      "src": "$.items.map(function (x) { const y = x + 1; return y * 2 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000((x) => x * 2)(5)": {
      "entry": "pipeline",
      "src": "((x) => x * 2)(5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000(function (x) { return x * 2 })(5)": {
      "entry": "pipeline",
      "src": "(function (x) { return x * 2 })(5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000function* f(x) { return x } $ = { a: f($.n) }": {
      "entry": "pipeline",
      "src": "function* f(x) { return x } $ = { a: f($.n) }",
      "kind": "message",
      "was": "jsmql does not support generator functions (`function*`). Use a plain `function (…) { return … }` or an arrow.",
      "now": "Expected '(' but got '*' at position 8",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000{ function: $.x }": {
      "entry": "pipeline",
      "src": "{ function: $.x }",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.function": {
      "entry": "pipeline",
      "src": "$.function",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$match($.a === 0);": {
      "entry": "pipeline",
      "src": "$match($.a === 0);",
      "kind": "output",
      "was": "[{\"$match\":{\"a\":0}}]",
      "now": "[{\"$match\":{\"a\":{\"$eq\":0,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$match($.a === 0); $.b = 1": {
      "entry": "pipeline",
      "src": "$match($.a === 0); $.b = 1",
      "kind": "output",
      "was": "[{\"$match\":{\"a\":0}},{\"$set\":{\"b\":1}}]",
      "now": "[{\"$match\":{\"a\":{\"$eq\":0,\"$not\":{\"$type\":\"array\"}}}},{\"$set\":{\"b\":1}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000{ $match: $.a === 0 }": {
      "entry": "pipeline",
      "src": "{ $match: $.a === 0 }",
      "kind": "output",
      "was": "[{\"$match\":{\"a\":0}}]",
      "now": "[{\"$match\":{\"a\":{\"$eq\":0,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u00001 + 1; $.a = 2": {
      "entry": "pipeline",
      "src": "1 + 1; $.a = 2",
      "kind": "message",
      "was": "Element 0 of pipeline is not a recognised stage. Expected `{ $stage: ... }` or `$stage(...)` where $stage is one of: $addFields, $bucket, $bucketAuto, $changeStream, $changeStreamSplitLargeEvent, $collStats, $count, $cur",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000$macth($.a); $.b = 1": {
      "entry": "pipeline",
      "src": "$macth($.a); $.b = 1",
      "kind": "message",
      "was": "Element 0 of pipeline: '$macth' is not a known aggregation stage. Did you mean '$match'?",
      "now": "Unknown name ''$macth'' at position 0.",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$.items.map(i => i.price).reduce((acc, p) => acc + p, 0)": {
      "entry": "pipeline",
      "src": "$.items.map(i => i.price).reduce((acc, p) => acc + p, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.events.map(e => e.at.getTime()).reduce((max, t) => Math.max(max, t), 0)": {
      "entry": "pipeline",
      "src": "$.events.map(e => e.at.getTime()).reduce((max, t) => Math.max(max, t), 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000let x = 5": {
      "entry": "pipeline",
      "src": "let x = 5",
      "kind": "message",
      "was": "`let x = …` is only valid inside a pipeline. Add at least one more statement separated by `;` to flip into pipeline mode (e.g. `let x = …; { $project: … }`). The bracketed pipeline form `[ let x = …, … ]` works too.",
      "now": "This program produces no stages, so it would leave the documents untouched. Write at least one statement that reads or changes them.",
      "reason": "message: both refuse a program that emits nothing; the new wording says what the program would do rather than naming the entry point."
    },
    "pipeline\u0000[1, let x = 5]": {
      "entry": "pipeline",
      "src": "[1, let x = 5]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000let = 5;": {
      "entry": "pipeline",
      "src": "let = 5;",
      "kind": "message",
      "was": "Expected an identifier after `let` at position 4, got '='",
      "now": "Expected identifier but got '=' at position 4",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000let x 5;": {
      "entry": "pipeline",
      "src": "let x 5;",
      "kind": "message",
      "was": "Expected '=' after `let x` at position 6, got '5'. `let` requires an initialiser — write `let x = <expr>`.",
      "now": "Expected '=' but got '5' at position 6",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000let in = 5;": {
      "entry": "pipeline",
      "src": "let in = 5;",
      "kind": "message",
      "was": "Expected an identifier after `let` at position 4, got 'in'",
      "now": "Expected identifier but got 'in' at position 4",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000let $foo = 5;": {
      "entry": "pipeline",
      "src": "let $foo = 5;",
      "kind": "message",
      "was": "Expected an identifier after `let` at position 4, got '$'",
      "now": "Expected identifier but got '$' at position 4",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000let big = 123n; $project({ big })": {
      "entry": "pipeline",
      "src": "let big = 123n; $project({ big })",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.big\":{\"$toLong\":\"123\"}}},{\"$project\":{\"big\":\"$__jsmql.var.big\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$project\":{\"big\":{\"$toLong\":\"123\"}}}]",
      "reason": "intended: a BigInt constant keeps its long-ness through the fold: the value target emits `$toLong` of the digits, which is the value the developer wrote. Measured equal on the server."
    },
    "pipeline\u0000$.let > 5": {
      "entry": "pipeline",
      "src": "$.let > 5",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user.let": {
      "entry": "pipeline",
      "src": "$.user.let",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$let({ x: 10 }, x => x * 2)": {
      "entry": "pipeline",
      "src": "$let({ x: 10 }, x => x * 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$match($.a > 0); zzz = 5; $project({ a: 1 });": {
      "entry": "pipeline",
      "src": "$match($.a > 0); zzz = 5; $project({ a: 1 });",
      "kind": "message",
      "was": "Cannot assign to bare identifier 'zzz' — it isn't a `let` binding in scope. Declare it first with `let zzz = …`, or write `$.zzz = …` to set a document field.",
      "now": "A write names a field: '$.total = …', '$.a.b = …', or the document itself, '$ = { … }'. A computed destination ('$[expr] = …') has no field name at compile time — use '$setField({ field: <expr>, input: $, value: … })' wh",
      "reason": "message: both refuse a destination that is not a field path; the new wording names the three spellings that are, and `$setField` for a computed name."
    },
    "pipeline\u0000x = 5": {
      "entry": "pipeline",
      "src": "x = 5",
      "kind": "message",
      "was": "Cannot assign to bare identifier 'x'. Reassignable `let` bindings exist only inside a pipeline — add a `;` to enter pipeline mode and declare it first (`let x = …`). To write a document field, use a field path: `$.x = …`",
      "now": "A write names a field: '$.total = …', '$.a.b = …', or the document itself, '$ = { … }'. A computed destination ('$[expr] = …') has no field name at compile time — use '$setField({ field: <expr>, input: $, value: … })' wh",
      "reason": "message: both refuse a destination that is not a field path; the new wording names the three spellings that are, and `$setField` for a computed name."
    },
    "pipeline\u0000const = 5;": {
      "entry": "pipeline",
      "src": "const = 5;",
      "kind": "message",
      "was": "Expected an identifier after `const` at position 6, got '='",
      "now": "Expected identifier but got '=' at position 6",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000const x 5;": {
      "entry": "pipeline",
      "src": "const x 5;",
      "kind": "message",
      "was": "Expected '=' after `const x` at position 8, got '5'. `const` requires an initialiser — write `const x = <expr>`.",
      "now": "Expected '=' but got '5' at position 8",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$.const > 5": {
      "entry": "pipeline",
      "src": "$.const > 5",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.user.const": {
      "entry": "pipeline",
      "src": "$.user.const",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$project({ const: 1 }); $match($.x > 1);": {
      "entry": "pipeline",
      "src": "$project({ const: 1 }); $match($.x > 1);",
      "kind": "output",
      "was": "[{\"$project\":{\"const\":1}},{\"$match\":{\"x\":{\"$gt\":1}}}]",
      "now": "[{\"$project\":{\"const\":1}},{\"$match\":{\"x\":{\"$gt\":1,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$replaceWith(\"hello\");": {
      "entry": "pipeline",
      "src": "$replaceWith(\"hello\");",
      "kind": "message",
      "was": "'$replaceWith' must resolve to a document, but got a string. Wrap it, e.g. '{ value: … }'.",
      "now": "'$replaceWith' expects a document, but got a string.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entries for the stage bodies."
    },
    "pipeline\u0000$literal(\"$y\")": {
      "entry": "pipeline",
      "src": "$literal(\"$y\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000($.a + $.b) || $.c": {
      "entry": "pipeline",
      "src": "($.a + $.b) || $.c",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.xs.reduce((acc, _, i) => acc + i, 0)": {
      "entry": "pipeline",
      "src": "$.xs.reduce((acc, _, i) => acc + i, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.match(/word/g)": {
      "entry": "pipeline",
      "src": "$.s.match(/word/g)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000/pattern/gi.test($.str)": {
      "entry": "pipeline",
      "src": "/pattern/gi.test($.str)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000 alike, at any depth\n//   including inside nested operator calls. It IS the MQL field ref ": {
      "entry": "pipeline",
      "src": " alike, at any depth\n//   including inside nested operator calls. It IS the MQL field ref ",
      "kind": "message",
      "was": "Unexpected token ',' at position 6",
      "now": "Expected ';' but got ',' at position 6",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000 position with a ": {
      "entry": "pipeline",
      "src": " position with a ",
      "kind": "message",
      "was": "Unexpected token 'with' at position 10",
      "now": "Expected ';' but got 'with' at position 10",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$.x = $$$.orders.map(o => { $sort({ x: -1 }); return o.total; });": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.map(o => { $sort({ x: -1 }); return o.total; });",
      "kind": "message",
      "was": "`.map(o => { … })` takes a JavaScript callback, so its block body holds `const`/`let` bindings and one `return <expr>`. `$sort(...)` is a pipeline stage, not part of a callback — the chain is consumed as a value here, so",
      "now": "A callback block with a 'return' may only declare values before it at position 46",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$.x = $$$.orders.filter(o => o.uid === $._id).find(o => { $match(o.c); });": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter(o => o.uid === $._id).find(o => { $match(o.c); });",
      "kind": "message",
      "was": "`.find(o => { … })` takes a JavaScript callback, so its block body holds `const`/`let` bindings and one `return <expr>`. `$match(...)` is a pipeline stage, not part of a callback — the chain is consumed as a value here, ",
      "now": "A block body must end with a `return <expr>` statement at position 71, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$.x = $$$.c.aggregate((o) => { $sort({ a: 1 }); return o.v; });": {
      "entry": "pipeline",
      "src": "$.x = $$$.c.aggregate((o) => { $sort({ a: 1 }); return o.v; });",
      "kind": "message",
      "was": ".aggregate((o) => { … }) doesn't take a `return` — each statement is a pipeline stage, not a per-document reshape. To shape the output add a `$project`/`$replaceWith` stage; for a per-document transform use `.map(o => …)",
      "now": "A callback block with a 'return' may only declare values before it at position 48",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000[1, 2].head().round()": {
      "entry": "pipeline",
      "src": "[1, 2].head().round()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[[1, 2], [3]].head().toReversed()": {
      "entry": "pipeline",
      "src": "[[1, 2], [3]].head().toReversed()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[$match($.email === \"alice@example.com\")]": {
      "entry": "pipeline",
      "src": "[$match($.email === \"alice@example.com\")]",
      "kind": "output",
      "was": "[{\"$match\":{\"email\":\"alice@example.com\"}}]",
      "now": "[{\"$match\":{\"email\":{\"$eq\":\"alice@example.com\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.userId === 42)]": {
      "entry": "pipeline",
      "src": "[$match($.userId === 42)]",
      "kind": "output",
      "was": "[{\"$match\":{\"userId\":42}}]",
      "now": "[{\"$match\":{\"userId\":{\"$eq\":42,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.active === true)]": {
      "entry": "pipeline",
      "src": "[$match($.active === true)]",
      "kind": "output",
      "was": "[{\"$match\":{\"active\":true}}]",
      "now": "[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.status !== \"archived\")]": {
      "entry": "pipeline",
      "src": "[$match($.status !== \"archived\")]",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":{\"$ne\":\"archived\"}}}]",
      "now": "[{\"$match\":{\"$or\":[{\"status\":{\"$ne\":\"archived\"}},{\"status\":{\"$type\":\"array\"}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(\"alice\" === $.name)]": {
      "entry": "pipeline",
      "src": "[$match(\"alice\" === $.name)]",
      "kind": "output",
      "was": "[{\"$match\":{\"name\":\"alice\"}}]",
      "now": "[{\"$match\":{\"name\":{\"$eq\":\"alice\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.user.role === \"admin\")]": {
      "entry": "pipeline",
      "src": "[$match($.user.role === \"admin\")]",
      "kind": "output",
      "was": "[{\"$match\":{\"user.role\":\"admin\"}}]",
      "now": "[{\"$match\":{\"user.role\":{\"$eq\":\"admin\",\"$not\":{\"$type\":\"array\"}},\"user\":{\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.age > 18)]": {
      "entry": "pipeline",
      "src": "[$match($.age > 18)]",
      "kind": "output",
      "was": "[{\"$match\":{\"age\":{\"$gt\":18}}}]",
      "now": "[{\"$match\":{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.score >= 80)]": {
      "entry": "pipeline",
      "src": "[$match($.score >= 80)]",
      "kind": "output",
      "was": "[{\"$match\":{\"score\":{\"$gte\":80}}}]",
      "now": "[{\"$match\":{\"score\":{\"$gte\":80,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.year < 2020)]": {
      "entry": "pipeline",
      "src": "[$match($.year < 2020)]",
      "kind": "output",
      "was": "[{\"$match\":{\"year\":{\"$lt\":2020}}}]",
      "now": "[{\"$match\":{\"year\":{\"$lt\":2020,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.qty <= 5)]": {
      "entry": "pipeline",
      "src": "[$match($.qty <= 5)]",
      "kind": "output",
      "was": "[{\"$match\":{\"qty\":{\"$lte\":5}}}]",
      "now": "[{\"$match\":{\"qty\":{\"$lte\":5,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.placedAt >= \"2026-01-01\")]": {
      "entry": "pipeline",
      "src": "[$match($.placedAt >= \"2026-01-01\")]",
      "kind": "output",
      "was": "[{\"$match\":{\"placedAt\":{\"$gte\":\"2026-01-01\"}}}]",
      "now": "[{\"$match\":{\"placedAt\":{\"$gte\":\"2026-01-01\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(18 < $.age)]": {
      "entry": "pipeline",
      "src": "[$match(18 < $.age)]",
      "kind": "output",
      "was": "[{\"$match\":{\"age\":{\"$gt\":18}}}]",
      "now": "[{\"$match\":{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(5 >= $.qty)]": {
      "entry": "pipeline",
      "src": "[$match(5 >= $.qty)]",
      "kind": "output",
      "was": "[{\"$match\":{\"qty\":{\"$lte\":5}}}]",
      "now": "[{\"$match\":{\"qty\":{\"$lte\":5,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.deletedAt === null)]": {
      "entry": "pipeline",
      "src": "[$match($.deletedAt === null)]",
      "kind": "output",
      "was": "[{\"$match\":{\"deletedAt\":{\"$type\":\"null\"}}}]",
      "now": "[{\"$match\":{\"deletedAt\":{\"$type\":\"null\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.paidAt !== null)]": {
      "entry": "pipeline",
      "src": "[$match($.paidAt !== null)]",
      "kind": "output",
      "was": "[{\"$match\":{\"paidAt\":{\"$not\":{\"$type\":\"null\"}}}}]",
      "now": "[{\"$match\":{\"$or\":[{\"paidAt\":{\"$not\":{\"$type\":\"null\"}}},{\"paidAt\":{\"$type\":\"array\"}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.deletedAt == null)]": {
      "entry": "pipeline",
      "src": "[$match($.deletedAt == null)]",
      "kind": "output",
      "was": "[{\"$match\":{\"deletedAt\":null}}]",
      "now": "[{\"$match\":{\"deletedAt\":{\"$eq\":null,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.paidAt != null)]": {
      "entry": "pipeline",
      "src": "[$match($.paidAt != null)]",
      "kind": "output",
      "was": "[{\"$match\":{\"paidAt\":{\"$ne\":null}}}]",
      "now": "[{\"$match\":{\"$or\":[{\"paidAt\":{\"$ne\":null}},{\"paidAt\":{\"$type\":\"array\"}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(null == $.x)]": {
      "entry": "pipeline",
      "src": "[$match(null == $.x)]",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":null}}]",
      "now": "[{\"$match\":{\"x\":{\"$eq\":null,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.status === \"active\" && $.age > 18)]": {
      "entry": "pipeline",
      "src": "[$match($.status === \"active\" && $.age > 18)]",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"active\",\"age\":{\"$gt\":18}}}]",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}},\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.age > 18 && $.age < 65)]": {
      "entry": "pipeline",
      "src": "[$match($.age > 18 && $.age < 65)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$and\":[{\"age\":{\"$gt\":18}},{\"age\":{\"$lt\":65}}]}}]",
      "now": "[{\"$match\":{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"},\"$lt\":65}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.role === \"admin\" || $.role === \"owner\")]": {
      "entry": "pipeline",
      "src": "[$match($.role === \"admin\" || $.role === \"owner\")]",
      "kind": "output",
      "was": "[{\"$match\":{\"$or\":[{\"role\":\"admin\"},{\"role\":\"owner\"}]}}]",
      "now": "[{\"$match\":{\"$or\":[{\"role\":{\"$eq\":\"admin\",\"$not\":{\"$type\":\"array\"}}},{\"role\":{\"$eq\":\"owner\",\"$not\":{\"$type\":\"array\"}}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(($.role === \"admin\" || $.role === \"owner\") && $.active === true)]": {
      "entry": "pipeline",
      "src": "[$match(($.role === \"admin\" || $.role === \"owner\") && $.active === true)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$or\":[{\"role\":\"admin\"},{\"role\":\"owner\"}],\"active\":true}}]",
      "now": "[{\"$match\":{\"$or\":[{\"role\":{\"$eq\":\"admin\",\"$not\":{\"$type\":\"array\"}}},{\"role\":{\"$eq\":\"owner\",\"$not\":{\"$type\":\"array\"}}}],\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.status === \"active\" && $.score > $.threshold)]": {
      "entry": "pipeline",
      "src": "[$match($.status === \"active\" && $.score > $.threshold)]",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"active\",\"$expr\":{\"$gt\":[\"$score\",\"$threshold\"]}}}]",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}},\"$expr\":{\"$gt\":[\"$score\",\"$threshold\"]}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.status === \"active\" && $.a > $.b && $.c < $.d)]": {
      "entry": "pipeline",
      "src": "[$match($.status === \"active\" && $.a > $.b && $.c < $.d)]",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"active\",\"$expr\":{\"$and\":[{\"$gt\":[\"$a\",\"$b\"]},{\"$lt\":[\"$c\",\"$d\"]}]}}}]",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}},\"$expr\":{\"$and\":[{\"$gt\":[\"$a\",\"$b\"]},{\"$lt\":[\"$c\",\"$d\"]}]}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.status === \"active\" || $.score > $.threshold)]": {
      "entry": "pipeline",
      "src": "[$match($.status === \"active\" || $.score > $.threshold)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$or\":[{\"$eq\":[\"$status\",\"active\"]},{\"$gt\":[\"$score\",\"$threshold\"]}]}}}]",
      "now": "[{\"$match\":{\"$or\":[{\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}}},{\"$expr\":{\"$gt\":[\"$score\",\"$threshold\"]}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(typeof $.email === \"string\")]": {
      "entry": "pipeline",
      "src": "[$match(typeof $.email === \"string\")]",
      "kind": "output",
      "was": "[{\"$match\":{\"email\":{\"$type\":\"string\"}}}]",
      "now": "[{\"$match\":{\"email\":{\"$type\":\"string\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(\"int\" === typeof $.count)]": {
      "entry": "pipeline",
      "src": "[$match(\"int\" === typeof $.count)]",
      "kind": "output",
      "was": "[{\"$match\":{\"count\":{\"$type\":\"int\"}}}]",
      "now": "[{\"$match\":{\"count\":{\"$type\":\"int\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(typeof $.x !== \"null\")]": {
      "entry": "pipeline",
      "src": "[$match(typeof $.x !== \"null\")]",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":{\"$not\":{\"$type\":\"null\"}}}}]",
      "now": "[{\"$match\":{\"$or\":[{\"x\":{\"$not\":{\"$type\":\"null\"}}},{\"x\":{\"$type\":\"array\"}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(typeof $.user.role === \"string\")]": {
      "entry": "pipeline",
      "src": "[$match(typeof $.user.role === \"string\")]",
      "kind": "output",
      "was": "[{\"$match\":{\"user.role\":{\"$type\":\"string\"}}}]",
      "now": "[{\"$match\":{\"user.role\":{\"$type\":\"string\",\"$not\":{\"$type\":\"array\"}},\"user\":{\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(typeof $.age === \"int\" && $.age > 18)]": {
      "entry": "pipeline",
      "src": "[$match(typeof $.age === \"int\" && $.age > 18)]",
      "kind": "output",
      "was": "[{\"$match\":{\"$and\":[{\"age\":{\"$type\":\"int\"}},{\"age\":{\"$gt\":18}}]}}]",
      "now": "[{\"$match\":{\"age\":{\"$type\":\"int\",\"$not\":{\"$type\":\"array\"},\"$gt\":18}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.createdAt >= new Date(\"2026-01-01\"))]": {
      "entry": "pipeline",
      "src": "[$match($.createdAt >= new Date(\"2026-01-01\"))]",
      "kind": "output",
      "was": "[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"}}}]",
      "now": "[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.method === \"postalDelivery\" && $.createdAt >= new Date(\"2026-01-01\")": {
      "entry": "pipeline",
      "src": "$.method === \"postalDelivery\" && $.createdAt >= new Date(\"2026-01-01\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[$match($.createdAt > new Date(\"2026-01-01\"))]": {
      "entry": "pipeline",
      "src": "[$match($.createdAt > new Date(\"2026-01-01\"))]",
      "kind": "output",
      "was": "[{\"$match\":{\"createdAt\":{\"$gt\":\"«date:2026-01-01T00:00:00.000Z»\"}}}]",
      "now": "[{\"$match\":{\"createdAt\":{\"$gt\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.createdAt < new Date(\"2026-01-01\"))]": {
      "entry": "pipeline",
      "src": "[$match($.createdAt < new Date(\"2026-01-01\"))]",
      "kind": "output",
      "was": "[{\"$match\":{\"createdAt\":{\"$lt\":\"«date:2026-01-01T00:00:00.000Z»\"}}}]",
      "now": "[{\"$match\":{\"createdAt\":{\"$lt\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.createdAt <= new Date(\"2026-01-01\"))]": {
      "entry": "pipeline",
      "src": "[$match($.createdAt <= new Date(\"2026-01-01\"))]",
      "kind": "output",
      "was": "[{\"$match\":{\"createdAt\":{\"$lte\":\"«date:2026-01-01T00:00:00.000Z»\"}}}]",
      "now": "[{\"$match\":{\"createdAt\":{\"$lte\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.startedAt === new Date(\"2026-01-01\"))]": {
      "entry": "pipeline",
      "src": "[$match($.startedAt === new Date(\"2026-01-01\"))]",
      "kind": "output",
      "was": "[{\"$match\":{\"startedAt\":\"«date:2026-01-01T00:00:00.000Z»\"}}]",
      "now": "[{\"$match\":{\"startedAt\":{\"$eq\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.startedAt !== new Date(\"2026-01-01\"))]": {
      "entry": "pipeline",
      "src": "[$match($.startedAt !== new Date(\"2026-01-01\"))]",
      "kind": "output",
      "was": "[{\"$match\":{\"startedAt\":{\"$ne\":\"«date:2026-01-01T00:00:00.000Z»\"}}}]",
      "now": "[{\"$match\":{\"$or\":[{\"startedAt\":{\"$ne\":\"«date:2026-01-01T00:00:00.000Z»\"}},{\"startedAt\":{\"$type\":\"array\"}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(new Date(\"2026-01-01\") <= $.createdAt)]": {
      "entry": "pipeline",
      "src": "[$match(new Date(\"2026-01-01\") <= $.createdAt)]",
      "kind": "output",
      "was": "[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"}}}]",
      "now": "[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.createdAt >= new Date(2026, 1, 1))]": {
      "entry": "pipeline",
      "src": "[$match($.createdAt >= new Date(2026, 1, 1))]",
      "kind": "output",
      "was": "[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"}}}]",
      "now": "[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.createdAt >= new Date(Date.UTC(2026, 1, 1)))]": {
      "entry": "pipeline",
      "src": "[$match($.createdAt >= new Date(Date.UTC(2026, 1, 1)))]",
      "kind": "output",
      "was": "[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"}}}]",
      "now": "[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.createdAt >= new Date(\"not-a-date\"))]": {
      "entry": "pipeline",
      "src": "[$match($.createdAt >= new Date(\"not-a-date\"))]",
      "kind": "message",
      "was": "new Date(\"not-a-date\") — \"not-a-date\" is not a valid date string. Use an ISO 8601 date like \"2026-01-01\" or \"2026-01-01T00:00:00.000Z\".",
      "now": "new Date(<constant>) — only an ISO 8601 string or a millisecond count is a date constant, and this one is neither a valid date string nor a number. Write new Date(\"2026-01-01\") or new Date(0).",
      "reason": "intended: a constant the compiler can evaluate is checked while it is still a constant — the shipped compiler emitted an invalid date and an implausible ObjectId unchecked."
    },
    "pipeline\u0000[$match($.createdAt >= new Date(\"2026-01-01\") && $.createdAt < new Date(\"2026-02-01\"))]": {
      "entry": "pipeline",
      "src": "[$match($.createdAt >= new Date(\"2026-01-01\") && $.createdAt < new Date(\"2026-02-01\"))]",
      "kind": "output",
      "was": "[{\"$match\":{\"$and\":[{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"}},{\"createdAt\":{\"$lt\":\"«date:2026-02-01T00:00:00.000Z»\"}}]}}]",
      "now": "[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"},\"$lt\":\"«date:2026-02-01T00:00:00.000Z»\"}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.tags.includes(\"vip\"))]": {
      "entry": "pipeline",
      "src": "[$match($.tags.includes(\"vip\"))]",
      "kind": "output",
      "was": "[{\"$match\":{\"tags\":\"vip\"}}]",
      "now": "[{\"$match\":{\"$or\":[{\"tags\":{\"$eq\":\"vip\",\"$type\":\"array\"}},{\"tags\":{\"$regex\":\"vip\",\"$not\":{\"$type\":\"array\"}}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match([\"active\", \"trial\"].includes($.status))]": {
      "entry": "pipeline",
      "src": "[$match([\"active\", \"trial\"].includes($.status))]",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":{\"$in\":[\"active\",\"trial\"]}}}]",
      "now": "[{\"$match\":{\"status\":{\"$in\":[\"active\",\"trial\"],\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.user.roles.includes(\"admin\"))]": {
      "entry": "pipeline",
      "src": "[$match($.user.roles.includes(\"admin\"))]",
      "kind": "output",
      "was": "[{\"$match\":{\"user.roles\":\"admin\"}}]",
      "now": "[{\"$match\":{\"$or\":[{\"user.roles\":{\"$eq\":\"admin\",\"$type\":\"array\"}},{\"user.roles\":{\"$regex\":\"admin\",\"$not\":{\"$type\":\"array\"}},\"user\":{\"$not\":{\"$type\":\"array\"}}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.name.match(/^a/i))]": {
      "entry": "pipeline",
      "src": "[$match($.name.match(/^a/i))]",
      "kind": "output",
      "was": "[{\"$match\":{\"name\":\"«regexp:^a/i»\"}}]",
      "now": "[{\"$match\":{\"name\":{\"$regex\":\"«regexp:^a/i»\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.user.email.match(/@example\\.com$/))]": {
      "entry": "pipeline",
      "src": "[$match($.user.email.match(/@example\\.com$/))]",
      "kind": "output",
      "was": "[{\"$match\":{\"user.email\":\"«regexp:@example\\\\.com$/»\"}}]",
      "now": "[{\"$match\":{\"user.email\":{\"$regex\":\"«regexp:@example\\\\.com$/»\",\"$not\":{\"$type\":\"array\"}},\"user\":{\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.items.some(item => item.tag === 'vip'))]": {
      "entry": "pipeline",
      "src": "[$match($.items.some(item => item.tag === 'vip'))]",
      "kind": "output",
      "was": "[{\"$match\":{\"items\":{\"$elemMatch\":{\"tag\":\"vip\"}}}}]",
      "now": "[{\"$match\":{\"items\":{\"$elemMatch\":{\"tag\":{\"$eq\":\"vip\",\"$not\":{\"$type\":\"array\"}}}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.items.some(i => i.qty > 5 && i.tag === 'vip'))]": {
      "entry": "pipeline",
      "src": "[$match($.items.some(i => i.qty > 5 && i.tag === 'vip'))]",
      "kind": "output",
      "was": "[{\"$match\":{\"items\":{\"$elemMatch\":{\"qty\":{\"$gt\":5},\"tag\":\"vip\"}}}}]",
      "now": "[{\"$match\":{\"items\":{\"$elemMatch\":{\"qty\":{\"$gt\":5,\"$not\":{\"$type\":\"array\"}},\"tag\":{\"$eq\":\"vip\",\"$not\":{\"$type\":\"array\"}}}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.line.some(it => it.product.price > 100))]": {
      "entry": "pipeline",
      "src": "[$match($.line.some(it => it.product.price > 100))]",
      "kind": "output",
      "was": "[{\"$match\":{\"line\":{\"$elemMatch\":{\"product.price\":{\"$gt\":100}}}}}]",
      "now": "[{\"$match\":{\"line\":{\"$elemMatch\":{\"product.price\":{\"$gt\":100,\"$not\":{\"$type\":\"array\"}},\"product\":{\"$not\":{\"$type\":\"array\"}}}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.user.deletedAt === undefined)]": {
      "entry": "pipeline",
      "src": "[$match($.user.deletedAt === undefined)]",
      "kind": "output",
      "was": "[{\"$match\":{\"user.deletedAt\":{\"$exists\":false}}}]",
      "now": "[{\"$match\":{\"$or\":[{\"user.deletedAt\":{\"$exists\":false}},{\"user\":{\"$type\":\"array\"}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.x === undefined ? 1 : 2": {
      "entry": "pipeline",
      "src": "$.x === undefined ? 1 : 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a + undefined": {
      "entry": "pipeline",
      "src": "$.a + undefined",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[$match(typeof $.flag === 'boolean')]": {
      "entry": "pipeline",
      "src": "[$match(typeof $.flag === 'boolean')]",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"flag\":{\"$type\":\"bool\"}}}]",
      "now": "'typeof' compares against one of MongoDB's type names, and \"boolean\" is not one. Did you mean 'bool'? For absence, write 'x === undefined'.",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(typeof $.flag === 'bool')]": {
      "entry": "pipeline",
      "src": "[$match(typeof $.flag === 'bool')]",
      "kind": "output",
      "was": "[{\"$match\":{\"flag\":{\"$type\":\"bool\"}}}]",
      "now": "[{\"$match\":{\"flag\":{\"$type\":\"bool\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.items.length === 3.5)]": {
      "entry": "pipeline",
      "src": "[$match($.items.length === 3.5)]",
      "kind": "output",
      "was": "[{\"$match\":{\"items.length\":3.5}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"array\"]]},\"then\":{\"$size\":\"$items\"}},{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$ifNull",
      "reason": "intended: `.length` dispatches on the receiver's own `$type` through one `$switch` rather than a nest of `$cond`s — the value target's shape, unchanged here."
    },
    "pipeline\u0000[$match($.items.length < 3.5)]": {
      "entry": "pipeline",
      "src": "[$match($.items.length < 3.5)]",
      "kind": "output",
      "was": "[{\"$match\":{\"items.length\":{\"$lt\":3.5}}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$lt\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"array\"]]},\"then\":{\"$size\":\"$items\"}},{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$ifNull",
      "reason": "intended: `.length` dispatches on the receiver's own `$type` through one `$switch` rather than a nest of `$cond`s — the value target's shape, unchanged here."
    },
    "pipeline\u0000[$match($.items.length === \"x\")]": {
      "entry": "pipeline",
      "src": "[$match($.items.length === \"x\")]",
      "kind": "output",
      "was": "[{\"$match\":{\"items.length\":\"x\"}}]",
      "now": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$switch\":{\"branches\":[{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"array\"]]},\"then\":{\"$size\":\"$items\"}},{\"case\":{\"$in\":[{\"$type\":\"$items\"},[\"string\",\"null\",\"missing\"]]},\"then\":{\"$strLenCP\":{\"$ifNull",
      "reason": "intended: `.length` dispatches on the receiver's own `$type` through one `$switch` rather than a nest of `$cond`s — the value target's shape, unchanged here."
    },
    "pipeline\u0000[$match($.x % 5 === 0)]": {
      "entry": "pipeline",
      "src": "[$match($.x % 5 === 0)]",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":{\"$mod\":[5,0]}}}]",
      "now": "[{\"$match\":{\"x\":{\"$mod\":[5,0],\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.x % 7 !== 3)]": {
      "entry": "pipeline",
      "src": "[$match($.x % 7 !== 3)]",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":{\"$not\":{\"$mod\":[7,3]}}}}]",
      "now": "[{\"$match\":{\"$or\":[{\"x\":{\"$not\":{\"$mod\":[7,3]}}},{\"x\":{\"$type\":\"array\"}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match(0 === $.x % 5)]": {
      "entry": "pipeline",
      "src": "[$match(0 === $.x % 5)]",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":{\"$mod\":[5,0]}}}]",
      "now": "[{\"$match\":{\"x\":{\"$mod\":[5,0],\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.user.score % 10 === 0)]": {
      "entry": "pipeline",
      "src": "[$match($.user.score % 10 === 0)]",
      "kind": "output",
      "was": "[{\"$match\":{\"user.score\":{\"$mod\":[10,0]}}}]",
      "now": "[{\"$match\":{\"user.score\":{\"$mod\":[10,0],\"$not\":{\"$type\":\"array\"}},\"user\":{\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.tags.includes(\"a\") && $.tags.includes(\"b\"))]": {
      "entry": "pipeline",
      "src": "[$match($.tags.includes(\"a\") && $.tags.includes(\"b\"))]",
      "kind": "output",
      "was": "[{\"$match\":{\"tags\":{\"$all\":[\"a\",\"b\"]}}}]",
      "now": "[{\"$match\":{\"$or\":[{\"tags\":{\"$all\":[\"a\",\"b\"],\"$type\":\"array\"}},{\"$and\":[{\"tags\":{\"$regex\":\"a\",\"$not\":{\"$type\":\"array\"}}},{\"tags\":{\"$regex\":\"b\"}}]}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.tags.includes(\"a\") && $.tags.includes(\"b\") && $.tags.includes(\"c\"))]": {
      "entry": "pipeline",
      "src": "[$match($.tags.includes(\"a\") && $.tags.includes(\"b\") && $.tags.includes(\"c\"))]",
      "kind": "output",
      "was": "[{\"$match\":{\"tags\":{\"$all\":[\"a\",\"b\",\"c\"]}}}]",
      "now": "[{\"$match\":{\"$or\":[{\"tags\":{\"$all\":[\"a\",\"b\",\"c\"],\"$type\":\"array\"}},{\"$and\":[{\"tags\":{\"$regex\":\"a\",\"$not\":{\"$type\":\"array\"}}},{\"tags\":{\"$regex\":\"b\"}},{\"tags\":{\"$regex\":\"c\"}}]}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.tags.includes(\"a\") && $.colors.includes(\"red\"))]": {
      "entry": "pipeline",
      "src": "[$match($.tags.includes(\"a\") && $.colors.includes(\"red\"))]",
      "kind": "output",
      "was": "[{\"$match\":{\"tags\":\"a\",\"colors\":\"red\"}}]",
      "now": "[{\"$match\":{\"$and\":[{\"$or\":[{\"tags\":{\"$eq\":\"a\",\"$type\":\"array\"}},{\"tags\":{\"$regex\":\"a\",\"$not\":{\"$type\":\"array\"}}}]},{\"$or\":[{\"colors\":{\"$eq\":\"red\",\"$type\":\"array\"}},{\"colors\":{\"$regex\":\"red\",\"$not\":{\"$type\":\"array\"}}}]}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[$match($.tags.includes(\"a\") && $.age > 18)]": {
      "entry": "pipeline",
      "src": "[$match($.tags.includes(\"a\") && $.age > 18)]",
      "kind": "output",
      "was": "[{\"$match\":{\"tags\":\"a\",\"age\":{\"$gt\":18}}}]",
      "now": "[{\"$match\":{\"$or\":[{\"tags\":{\"$eq\":\"a\",\"$type\":\"array\"}},{\"tags\":{\"$regex\":\"a\",\"$not\":{\"$type\":\"array\"}}}],\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.d.getUTCHours(\"UTC\")": {
      "entry": "pipeline",
      "src": "$.d.getUTCHours(\"UTC\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.trim(\"x\")": {
      "entry": "pipeline",
      "src": "$.s.trim(\"x\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.toLowerCase(1)": {
      "entry": "pipeline",
      "src": "$.s.toLowerCase(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1, { $limit: 10 }]": {
      "entry": "pipeline",
      "src": "[1, { $limit: 10 }]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[]": {
      "entry": "pipeline",
      "src": "[]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[{ $match: $.age > 18 }]": {
      "entry": "pipeline",
      "src": "[{ $match: $.age > 18 }]",
      "kind": "output",
      "was": "[{\"$match\":{\"age\":{\"$gt\":18}}}]",
      "now": "[{\"$match\":{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[{ $lookup: { from: \"x\", pipeline: $.someVar, as: \"y\" } }]": {
      "entry": "pipeline",
      "src": "[{ $lookup: { from: \"x\", pipeline: $.someVar, as: \"y\" } }]",
      "kind": "message",
      "was": "'$lookup pipeline' must be a constant array — got a field reference, which the server can't accept here. Use a literal array.",
      "now": "This stage's body is a sub-pipeline: write it as a bracketed list of stages, '[$match(…), $sort(…)]'.",
      "reason": "message: both refuse a sub-pipeline body that is not a bracketed stage list; the new wording shows the spelling."
    },
    "pipeline\u0000[{ $macth: $.age > 18 }]": {
      "entry": "pipeline",
      "src": "[{ $macth: $.age > 18 }]",
      "kind": "message",
      "was": "Element 0 of pipeline: '$macth' is not a known aggregation stage. Did you mean '$match'?",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[$prject({ name: 1 })]": {
      "entry": "pipeline",
      "src": "[$prject({ name: 1 })]",
      "kind": "message",
      "was": "Element 0 of pipeline: '$prject' is not a known aggregation stage. Did you mean '$project'?",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[{ $match: $.a > 1 }, 42]": {
      "entry": "pipeline",
      "src": "[{ $match: $.a > 1 }, 42]",
      "kind": "message",
      "was": "Element 1 of pipeline is not a recognised stage. Expected `{ $stage: ... }` or `$stage(...)` where $stage is one of: $addFields, $bucket, $bucketAuto, $changeStream, $changeStreamSplitLargeEvent, $collStats, $count, $cur",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000[{ $match: { age: 1 }, $sort: { age: 1 } }]": {
      "entry": "pipeline",
      "src": "[{ $match: { age: 1 }, $sort: { age: 1 } }]",
      "kind": "message",
      "was": "Element 0 of pipeline must be a single-key stage object (e.g. `{ $match: ... }`), but found an object with 2 keys.",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$ = 5": {
      "entry": "pipeline",
      "src": "$ = 5",
      "kind": "message",
      "was": "Cannot replace root with a number — the new root must be a document. Did you mean to wrap it: '$ = { value: ... }'?",
      "now": "'$ = …' replaces the document, so the value has to BE a document — a number is not one. Put it under a field ('$ = { value: … };'), or write to a field instead ('$.value = …;').",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entries for the stage bodies."
    },
    "pipeline\u0000$ = [1, 2]": {
      "entry": "pipeline",
      "src": "$ = [1, 2]",
      "kind": "message",
      "was": "Cannot fan out an array of number — each array element becomes a document root, so elements must be documents. Did you mean to wrap them: '$ = [{ value: ... }]'?",
      "now": "'$ = …' replaces the document, so the value has to BE a document — an array is not one. Put it under a field ('$ = { value: … };'), or write to a field instead ('$.value = …;').",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entries for the stage bodies."
    },
    "pipeline\u0000$mergeObjects($, { x: 1 })": {
      "entry": "pipeline",
      "src": "$mergeObjects($, { x: 1 })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[ $ = [{ a: 1 }, { b: 2 }] ]": {
      "entry": "pipeline",
      "src": "[ $ = [{ a: 1 }, { b: 2 }] ]",
      "kind": "now-rejected",
      "was": "[{\"$set\":{\"__jsmql.tmp.1\":[{\"a\":1},{\"b\":2}]}},{\"$unwind\":\"$__jsmql.tmp.1\"},{\"$replaceWith\":\"$__jsmql.tmp.1\"}]",
      "now": "'$ = …' replaces the document, so the value has to BE a document — an array is not one. Put it under a field ('$ = { value: … };'), or write to a field instead ('$.value = …;').",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entries for the stage bodies."
    },
    "pipeline\u0000[ $ = [...$.items] ]": {
      "entry": "pipeline",
      "src": "[ $ = [...$.items] ]",
      "kind": "now-rejected",
      "was": "[{\"$set\":{\"__jsmql.tmp.1\":\"$items\"}},{\"$unwind\":\"$__jsmql.tmp.1\"},{\"$replaceWith\":\"$__jsmql.tmp.1\"}]",
      "now": "'$ = …' replaces the document, so the value has to BE a document — an array is not one. Put it under a field ('$ = { value: … };'), or write to a field instead ('$.value = …;').",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entries for the stage bodies."
    },
    "pipeline\u0000[ $ = [] ]": {
      "entry": "pipeline",
      "src": "[ $ = [] ]",
      "kind": "message",
      "was": "Cannot fan out an empty array — '$ = []' would discard every document. To drop documents conditionally, fan out a data-dependent array (e.g. '$ = $.items.filter(...)'); to empty the stream, use '$$ = []'.",
      "now": "'$ = …' replaces the document, so the value has to BE a document — an array is not one. Put it under a field ('$ = { value: … };'), or write to a field instead ('$.value = …;').",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entries for the stage bodies."
    },
    "pipeline\u0000[ $ = [1, 2] ]": {
      "entry": "pipeline",
      "src": "[ $ = [1, 2] ]",
      "kind": "message",
      "was": "Cannot fan out an array of number — each array element becomes a document root, so elements must be documents. Did you mean to wrap them: '$ = [{ value: ... }]'?",
      "now": "'$ = …' replaces the document, so the value has to BE a document — an array is not one. Put it under a field ('$ = { value: … };'), or write to a field instead ('$.value = …;').",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entries for the stage bodies."
    },
    "pipeline\u0000[ $ = 5 ]": {
      "entry": "pipeline",
      "src": "[ $ = 5 ]",
      "kind": "message",
      "was": "Cannot replace root with a number — the new root must be a document. Did you mean to wrap it: '$ = { value: ... }'?",
      "now": "'$ = …' replaces the document, so the value has to BE a document — a number is not one. Put it under a field ('$ = { value: … };'), or write to a field instead ('$.value = …;').",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entries for the stage bodies."
    },
    "pipeline\u0000[ $ = \"foo\" ]": {
      "entry": "pipeline",
      "src": "[ $ = \"foo\" ]",
      "kind": "message",
      "was": "Cannot replace root with a string — the new root must be a document. Did you mean to wrap it: '$ = { value: ... }'?",
      "now": "'$ = …' replaces the document, so the value has to BE a document — a string is not one. Put it under a field ('$ = { value: … };'), or write to a field instead ('$.value = …;').",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entries for the stage bodies."
    },
    "pipeline\u0000delete $;": {
      "entry": "pipeline",
      "src": "delete $;",
      "kind": "message",
      "was": "Cannot 'delete $' — bare '$' is the whole document. Use '$ = <newDoc>' to replace it, or 'delete $.<field>' to drop a single field.",
      "now": "'delete $' would delete the document itself. To replace it, write '$ = { … };'; to drop every field but one, write '$ = { keep: $.keep };'.",
      "reason": "message: both refuse the same source; the wording differs, and the new one comes from the row or names the way out."
    },
    "pipeline\u0000$++;": {
      "entry": "pipeline",
      "src": "$++;",
      "kind": "message",
      "was": "Cannot use compound assignment / increment on bare '$' — '$' is the whole document, not a scalar. Use '$ = { ...$, ...overrides }' to merge fields into the root or '$ = <newRoot>' to replace it outright.",
      "now": "Cannot use '++' on bare '$' — it is the whole document, not a scalar. Write the field: '$.<field> ++ …' at position 0",
      "reason": "message: both refuse a scalar operation on the whole document; the wording differs."
    },
    "pipeline\u0000$ += 5;": {
      "entry": "pipeline",
      "src": "$ += 5;",
      "kind": "message",
      "was": "Cannot use compound assignment / increment on bare '$' — '$' is the whole document, not a scalar. Use '$ = { ...$, ...overrides }' to merge fields into the root or '$ = <newRoot>' to replace it outright.",
      "now": "Cannot use '+=' on bare '$' — it is the whole document, not a scalar. Write the field: '$.<field> += …' at position 0",
      "reason": "message: both refuse a scalar operation on the whole document; the wording differs."
    },
    "pipeline\u0000[ $match($.x > 1), { $collStats: {} } ]": {
      "entry": "pipeline",
      "src": "[ $match($.x > 1), { $collStats: {} } ]",
      "kind": "message",
      "was": "'$collStats' must be the first stage in a pipeline — it produces the pipeline's source documents, so nothing can run before it. Move it to the front, or remove the stage(s) that precede it.",
      "now": "'$collStats' produces the pipeline's source documents, so it has to be the FIRST stage — the server refuses it anywhere else. Move it to the top of the program.",
      "reason": "intended: the row's stated placement is applied now — measured, the server refuses a stage after `$out` and a source stage anywhere but first, and the shipped compiler emitted both."
    },
    "pipeline\u0000[ $sort({ x: 1 }), { $geoNear: { near: [0, 0], distanceField: 'd' } } ]": {
      "entry": "pipeline",
      "src": "[ $sort({ x: 1 }), { $geoNear: { near: [0, 0], distanceField: 'd' } } ]",
      "kind": "message",
      "was": "'$geoNear' must be the first stage in a pipeline — it produces the pipeline's source documents, so nothing can run before it. Move it to the front, or remove the stage(s) that precede it.",
      "now": "'$geoNear' produces the pipeline's source documents, so it has to be the FIRST stage — the server refuses it anywhere else. Move it to the top of the program.",
      "reason": "intended: the row's stated placement is applied now — measured, the server refuses a stage after `$out` and a source stage anywhere but first, and the shipped compiler emitted both."
    },
    "pipeline\u0000$match($.x > 1); { $changeStream: {} }": {
      "entry": "pipeline",
      "src": "$match($.x > 1); { $changeStream: {} }",
      "kind": "message",
      "was": "'$changeStream' must be the first stage in a pipeline — it produces the pipeline's source documents, so nothing can run before it. Move it to the front, or remove the stage(s) that precede it.",
      "now": "'$changeStream' produces the pipeline's source documents, so it has to be the FIRST stage — the server refuses it anywhere else. Move it to the top of the program.",
      "reason": "intended: the row's stated placement is applied now — measured, the server refuses a stage after `$out` and a source stage anywhere but first, and the shipped compiler emitted both."
    },
    "pipeline\u0000[ { $merge: 'archive' }, $sort({ x: 1 }) ]": {
      "entry": "pipeline",
      "src": "[ { $merge: 'archive' }, $sort({ x: 1 }) ]",
      "kind": "message",
      "was": "'$merge' must be the last stage in a pipeline — nothing can run after it. Move it to the end of the pipeline, or remove the stage(s) that follow it.",
      "now": "Nothing can follow '$merge': it writes the pipeline's output and the server requires it last. Move this statement above it.",
      "reason": "intended: the row's stated placement is applied now — measured, the server refuses a stage after `$out` and a source stage anywhere but first, and the shipped compiler emitted both."
    },
    "pipeline\u0000[ { $out: 'c' }, $count('n') ]": {
      "entry": "pipeline",
      "src": "[ { $out: 'c' }, $count('n') ]",
      "kind": "message",
      "was": "'$out' must be the last stage in a pipeline — nothing can run after it. Move it to the end of the pipeline, or remove the stage(s) that follow it.",
      "now": "Nothing can follow '$out': it writes the pipeline's output and the server requires it last. Move this statement above it.",
      "reason": "intended: the row's stated placement is applied now — measured, the server refuses a stage after `$out` and a source stage anywhere but first, and the shipped compiler emitted both."
    },
    "pipeline\u0000[ { $changeStreamSplitLargeEvent: {} }, $sort({ x: 1 }) ]": {
      "entry": "pipeline",
      "src": "[ { $changeStreamSplitLargeEvent: {} }, $sort({ x: 1 }) ]",
      "kind": "message",
      "was": "'$changeStreamSplitLargeEvent' must be the last stage in a pipeline — nothing can run after it. Move it to the end of the pipeline, or remove the stage(s) that follow it.",
      "now": "Nothing can follow '$changeStreamSplitLargeEvent': it writes the pipeline's output and the server requires it last. Move this statement above it.",
      "reason": "intended: the row's stated placement is applied now — measured, the server refuses a stage after `$out` and a source stage anywhere but first, and the shipped compiler emitted both."
    },
    "pipeline\u0000[ { $out: 'a' }, { $merge: 'b' } ]": {
      "entry": "pipeline",
      "src": "[ { $out: 'a' }, { $merge: 'b' } ]",
      "kind": "message",
      "was": "'$out' must be the last stage in a pipeline — nothing can run after it. Move it to the end of the pipeline, or remove the stage(s) that follow it.",
      "now": "Nothing can follow '$out': it writes the pipeline's output and the server requires it last. Move this statement above it.",
      "reason": "intended: the row's stated placement is applied now — measured, the server refuses a stage after `$out` and a source stage anywhere but first, and the shipped compiler emitted both."
    },
    "pipeline\u0000[ { $collStats: {} }, { $indexStats: {} } ]": {
      "entry": "pipeline",
      "src": "[ { $collStats: {} }, { $indexStats: {} } ]",
      "kind": "message",
      "was": "'$indexStats' must be the first stage in a pipeline — it produces the pipeline's source documents, so nothing can run before it. Move it to the front, or remove the stage(s) that precede it.",
      "now": "'$indexStats' produces the pipeline's source documents, so it has to be the FIRST stage — the server refuses it anywhere else. Move it to the top of the program.",
      "reason": "intended: the row's stated placement is applied now — measured, the server refuses a stage after `$out` and a source stage anywhere but first, and the shipped compiler emitted both."
    },
    "pipeline\u0000[ { $facet: { a: [ { $out: 'x' } ] } } ]": {
      "entry": "pipeline",
      "src": "[ { $facet: { a: [ { $out: 'x' } ] } } ]",
      "kind": "message",
      "was": "'$out' is not allowed inside a '$facet' sub-pipeline. Move it to the outer (top-level) pipeline.",
      "now": "'$out' cannot stand inside '$facet' — the server refuses this stage in that body. Run it as a stage of the outer pipeline instead.",
      "reason": "intended: the row's stated `forbiddenIn` is applied now — the server refuses a write stage inside a sub-pipeline, and the shipped compiler emitted it."
    },
    "pipeline\u0000[ { $lookup: { from: 'c', as: 'r', pipeline: [ { $merge: 'x' } ] } } ]": {
      "entry": "pipeline",
      "src": "[ { $lookup: { from: 'c', as: 'r', pipeline: [ { $merge: 'x' } ] } } ]",
      "kind": "message",
      "was": "'$merge' is not allowed inside a '$lookup' sub-pipeline. Move it to the outer (top-level) pipeline.",
      "now": "'$merge' cannot stand inside '$lookup' — the server refuses this stage in that body. Run it as a stage of the outer pipeline instead.",
      "reason": "intended: the row's stated `forbiddenIn` is applied now — the server refuses a write stage inside a sub-pipeline, and the shipped compiler emitted it."
    },
    "pipeline\u0000[ { $unionWith: { coll: 'c', pipeline: [ { $merge: 'x' } ] } } ]": {
      "entry": "pipeline",
      "src": "[ { $unionWith: { coll: 'c', pipeline: [ { $merge: 'x' } ] } } ]",
      "kind": "message",
      "was": "'$merge' is not allowed inside a '$unionWith' sub-pipeline. Move it to the outer (top-level) pipeline.",
      "now": "'$merge' cannot stand inside '$unionWith' — the server refuses this stage in that body. Run it as a stage of the outer pipeline instead.",
      "reason": "intended: the row's stated `forbiddenIn` is applied now — the server refuses a write stage inside a sub-pipeline, and the shipped compiler emitted it."
    },
    "pipeline\u0000[ { $facet: { a: [ { $facet: { b: [ $count('n') ] } } ] } } ]": {
      "entry": "pipeline",
      "src": "[ { $facet: { a: [ { $facet: { b: [ $count('n') ] } } ] } } ]",
      "kind": "message",
      "was": "'$facet' is not allowed inside a '$facet' sub-pipeline. Move it to the outer (top-level) pipeline.",
      "now": "'$facet' cannot stand inside '$facet' — the server refuses this stage in that body. Run it as a stage of the outer pipeline instead.",
      "reason": "intended: the row's stated `forbiddenIn` is applied now — the server refuses a write stage inside a sub-pipeline, and the shipped compiler emitted it."
    },
    "pipeline\u0000[ { $facet: { a: [ { $geoNear: { near: [0, 0], distanceField: 'd' } } ] } } ]": {
      "entry": "pipeline",
      "src": "[ { $facet: { a: [ { $geoNear: { near: [0, 0], distanceField: 'd' } } ] } } ]",
      "kind": "message",
      "was": "'$geoNear' is not allowed inside a '$facet' sub-pipeline. Move it to the outer (top-level) pipeline.",
      "now": "'$geoNear' cannot stand inside '$facet' — the server refuses this stage in that body. Run it as a stage of the outer pipeline instead.",
      "reason": "intended: the row's stated `forbiddenIn` is applied now — the server refuses a write stage inside a sub-pipeline, and the shipped compiler emitted it."
    },
    "pipeline\u0000$$.$match": {
      "entry": "pipeline",
      "src": "$$.$match",
      "kind": "message",
      "was": "'$match' is a pipeline stage — call it with its body: '$match(<body>)' at position 3.",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000$.out = $.items.$match({ a: 1 });": {
      "entry": "pipeline",
      "src": "$.out = $.items.$match({ a: 1 });",
      "kind": "message",
      "was": "'.$match(...)' is a pipeline stage, but its receiver here is a value, not a stream. Stages chain on '$$' or '$$$.<coll>', and only while the chain is still a stream — a value-producing link ('.map(\"<field>\")', '.uniq()',",
      "now": "'$match' is a pipeline stage, not an expression — MongoDB has no '$match' expression operator, so '{ $match: … }' in a value position is rejected by the server. Write it as a pipeline statement ('$match(…);') or as a cha",
      "reason": "intended: a stage named where a VALUE belongs is refused by its own row's text, which names the pipeline and chain spellings that work. The shipped compiler answered from the pipeline assembler instead, so the message did not name the stage."
    },
    "pipeline\u0000$lookup({ from: \"c\", pipeline: [ $out(\"a\") ], as: \"x\" });": {
      "entry": "pipeline",
      "src": "$lookup({ from: \"c\", pipeline: [ $out(\"a\") ], as: \"x\" });",
      "kind": "message",
      "was": "'$out' is not allowed inside a '$lookup' sub-pipeline. Move it to the outer (top-level) pipeline.",
      "now": "'$out' cannot stand inside '$lookup' — the server refuses this stage in that body. Run it as a stage of the outer pipeline instead.",
      "reason": "intended: the row's stated `forbiddenIn` is applied now — the server refuses a write stage inside a sub-pipeline, and the shipped compiler emitted it."
    },
    "pipeline\u0000$ = { hi: $$.$match({ s: \"a\" }).$limit(2), lo: $$.filter(d => d.n < 5) };": {
      "entry": "pipeline",
      "src": "$ = { hi: $$.$match({ s: \"a\" }).$limit(2), lo: $$.filter(d => d.n < 5) };",
      "kind": "now-rejected",
      "was": "[{\"$facet\":{\"hi\":[{\"$match\":{\"s\":\"a\"}},{\"$limit\":2}],\"lo\":[{\"$match\":{\"n\":{\"$lt\":5}}}]}}]",
      "now": "'$limit' is a pipeline stage, not an expression — MongoDB has no '$limit' expression operator, so '{ $limit: … }' in a value position is rejected by the server. Write it as a pipeline statement ('$limit(…);') or as a cha",
      "reason": "intended: a stage named where a VALUE belongs is refused by its own row's text, which names the pipeline and chain spellings that work. The shipped compiler answered from the pipeline assembler instead, so the message did not name the stage."
    },
    "pipeline\u0000$project({ o: $$$.orders.find(o => o.uid === 1) });": {
      "entry": "pipeline",
      "src": "$project({ o: $$$.orders.find(o => o.uid === 1) });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$project\":{\"o\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":{\"$eq\":1,\"$not\":{\"$type\":\"array\"}}}},{\"$limit\":1}],\"as\":\"__jsmql.tmp.0\"}},{\"$set\":{\"__jsmql.tmp.0\":{\"$first\":\"$__jsmql.tmp.0\"}}},{\"$project\":{\"o\":\"$__jsmql.tmp.0\"",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[\n        { $match: $.active === true },\n        $sort({ created: -1 }),\n        { $limit: 25 }\n      ]": {
      "entry": "pipeline",
      "src": "[\n        { $match: $.active === true },\n        $sort({ created: -1 }),\n        { $limit: 25 }\n      ]",
      "kind": "output",
      "was": "[{\"$match\":{\"active\":true}},{\"$sort\":{\"created\":-1}},{\"$limit\":25}]",
      "now": "[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}},{\"$sort\":{\"created\":-1}},{\"$limit\":25}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$ = { topByScore: $$.$sort({ score: -1 }).$limit(10) };": {
      "entry": "pipeline",
      "src": "$ = { topByScore: $$.$sort({ score: -1 }).$limit(10) };",
      "kind": "now-rejected",
      "was": "[{\"$facet\":{\"topByScore\":[{\"$sort\":{\"score\":-1}},{\"$limit\":10}]}}]",
      "now": "'$limit' is a pipeline stage, not an expression — MongoDB has no '$limit' expression operator, so '{ $limit: … }' in a value position is rejected by the server. Write it as a pipeline statement ('$limit(…);') or as a cha",
      "reason": "intended: a stage named where a VALUE belongs is refused by its own row's text, which names the pipeline and chain spellings that work. The shipped compiler answered from the pipeline assembler instead, so the message did not name the stage."
    },
    "pipeline\u0000$ = { topByScore: $$.filter(o => { $sort({ score: -1 }); $limit(10); }) };": {
      "entry": "pipeline",
      "src": "$ = { topByScore: $$.filter(o => { $sort({ score: -1 }); $limit(10); }) };",
      "kind": "message",
      "was": "`.filter(o => { … })` takes a JavaScript callback, so its block body holds `const`/`let` bindings and one `return <expr>`. `$sort(...)` is a pipeline stage, not part of a callback — chain them as stage calls instead — `$",
      "now": "A block body must end with a `return <expr>` statement at position 69, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$ = {\n        topByScore: $$.$sort({ score: -1 }).$limit(10),\n        recent:     $$.filter(o => o.createdAt >= \"2026-01-01\"),\n        byStatus:   $$.$group({ _id: $.status, n: $sum(1) })\n      };": {
      "entry": "pipeline",
      "src": "$ = {\n        topByScore: $$.$sort({ score: -1 }).$limit(10),\n        recent:     $$.filter(o => o.createdAt >= \"2026-01-01\"),\n        byStatus:   $$.$group({ _id: $.status, n: $sum(1) })\n      };",
      "kind": "now-rejected",
      "was": "[{\"$facet\":{\"topByScore\":[{\"$sort\":{\"score\":-1}},{\"$limit\":10}],\"recent\":[{\"$match\":{\"createdAt\":{\"$gte\":\"2026-01-01\"}}}],\"byStatus\":[{\"$group\":{\"_id\":\"$status\",\"n\":{\"$sum\":1}}}]}}]",
      "now": "'$limit' is a pipeline stage, not an expression — MongoDB has no '$limit' expression operator, so '{ $limit: … }' in a value position is rejected by the server. Write it as a pipeline statement ('$limit(…);') or as a cha",
      "reason": "intended: a stage named where a VALUE belongs is refused by its own row's text, which names the pipeline and chain spellings that work. The shipped compiler answered from the pipeline assembler instead, so the message did not name the stage."
    },
    "pipeline\u0000$.ratings.countBy()": {
      "entry": "pipeline",
      "src": "$.ratings.countBy()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.subscription.tier === \"premium\" && $.status === \"active\" && $.expiresAt > new Date(\"2026-05-01\")": {
      "entry": "pipeline",
      "src": "$.subscription.tier === \"premium\" && $.status === \"active\" && $.expiresAt > new Date(\"2026-05-01\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.pinned === true || $.author === \"ada\"": {
      "entry": "pipeline",
      "src": "$.pinned === true || $.author === \"ada\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.pinned !== true && $.lastModifiedAt < new Date(\"2025-01-01\")": {
      "entry": "pipeline",
      "src": "$.pinned !== true && $.lastModifiedAt < new Date(\"2025-01-01\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.inStock === true && $.price >= 50 && $.price <= 200": {
      "entry": "pipeline",
      "src": "$.inStock === true && $.price >= 50 && $.price <= 200",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.email != null && $.status === \"active\"": {
      "entry": "pipeline",
      "src": "$.email != null && $.status === \"active\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.profile === \"object\" && $.status === \"active\"": {
      "entry": "pipeline",
      "src": "typeof $.profile === \"object\" && $.status === \"active\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$._id === \"doc_42\" && $.tenantId === \"acme\"": {
      "entry": "pipeline",
      "src": "$._id === \"doc_42\" && $.tenantId === \"acme\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$._id === 0x507f1f77bcf86cd799439011 && $.tenantId === \"acme\"": {
      "entry": "pipeline",
      "src": "$._id === 0x507f1f77bcf86cd799439011 && $.tenantId === \"acme\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.deleted !== true && $.archived !== true": {
      "entry": "pipeline",
      "src": "$.deleted !== true && $.archived !== true",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.parent === null && $.published === true": {
      "entry": "pipeline",
      "src": "$.parent === null && $.published === true",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.tier === \"gold\" && $.country === \"AU\"": {
      "entry": "pipeline",
      "src": "$.tier === \"gold\" && $.country === \"AU\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$[\"cart.field.length\"] * $.cart.field.width": {
      "entry": "pipeline",
      "src": "$[\"cart.field.length\"] * $.cart.field.width",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.cart.field[$.mainSide]": {
      "entry": "pipeline",
      "src": "$.cart.field[$.mainSide]",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.active && $.role.toLowerCase().includes(\"admin\") && $.name.trim().length > 0": {
      "entry": "pipeline",
      "src": "$.active && $.role.toLowerCase().includes(\"admin\") && $.name.trim().length > 0",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.stock >= $.reorderPoint ? \"ok\" : $.stock > 0 ? \"low\" : \"out-of-stock\"": {
      "entry": "pipeline",
      "src": "$.stock >= $.reorderPoint ? \"ok\" : $.stock > 0 ? \"low\" : \"out-of-stock\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000!$.discontinued && $.stock < $.baseReorder * 2 ** $.urgencyLevel": {
      "entry": "pipeline",
      "src": "!$.discontinued && $.stock < $.baseReorder * 2 ** $.urgencyLevel",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000($.score - $.minScore) / ($.maxScore - $.minScore) * 100": {
      "entry": "pipeline",
      "src": "($.score - $.minScore) / ($.maxScore - $.minScore) * 100",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.floor($.age / 10) * 10": {
      "entry": "pipeline",
      "src": "Math.floor($.age / 10) * 10",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000($.quantity * ($.unitPrice + $.unitPrice * $.taxRate)).round(2)": {
      "entry": "pipeline",
      "src": "($.quantity * ($.unitPrice + $.unitPrice * $.taxRate)).round(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000String($.articleId) + \"-\" + $.title.toLowerCase().trim().replaceAll(\" \", \"-\")": {
      "entry": "pipeline",
      "src": "String($.articleId) + \"-\" + $.title.toLowerCase().trim().replaceAll(\" \", \"-\")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.email.split(\"@\").at(1).toLowerCase()": {
      "entry": "pipeline",
      "src": "$.email.split(\"@\").at(1).toLowerCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.tags.split(\",\").length": {
      "entry": "pipeline",
      "src": "$.tags.split(\",\").length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.sumBy(item => item.qty * item.price)": {
      "entry": "pipeline",
      "src": "$.items.sumBy(item => item.qty * item.price)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[$.firstName, $.middleName, $.lastName].filter(Boolean).join(\" \")": {
      "entry": "pipeline",
      "src": "[$.firstName, $.middleName, $.lastName].filter(Boolean).join(\" \")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.posts.map(\"tags\").flat().join(\", \")": {
      "entry": "pipeline",
      "src": "$.posts.map(\"tags\").flat().join(\", \")",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[...$.moderators, ...$.room?.mods, \"root\"].includes($.userId)": {
      "entry": "pipeline",
      "src": "[...$.moderators, ...$.room?.mods, \"root\"].includes($.userId)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.firstName ?? $.aliases.at(0) ?? \"anonymous\"": {
      "entry": "pipeline",
      "src": "$.firstName ?? $.aliases.at(0) ?? \"anonymous\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[...$.moderators, ...$.room.mods, \"root\"].includes($.userId)": {
      "entry": "pipeline",
      "src": "[...$.moderators, ...$.room.mods, \"root\"].includes($.userId)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Array.isArray($.scores) ? Math.max(...$.scores) - Math.min(...$.scores) : 0": {
      "entry": "pipeline",
      "src": "Array.isArray($.scores) ? Math.max(...$.scores) - Math.min(...$.scores) : 0",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.products.map(p => ({ [p.category]: p.price, p }))": {
      "entry": "pipeline",
      "src": "$.products.map(p => ({ [p.category]: p.price, p }))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Object.fromEntries($.metrics.map(m => [m.name, m.value]))": {
      "entry": "pipeline",
      "src": "Object.fromEntries($.metrics.map(m => [m.name, m.value]))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.min(10_000, $.lines.sumBy(l => l.qty * l.price))": {
      "entry": "pipeline",
      "src": "Math.min(10_000, $.lines.sumBy(l => l.qty * l.price))",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.value === \"string\" ? $.value.trim() : String($.value)": {
      "entry": "pipeline",
      "src": "typeof $.value === \"string\" ? $.value.trim() : String($.value)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.abs($dateDiff({ startDate: $.lastLoginAt, endDate: new Date(), unit: 'day' }) ?? -1)": {
      "entry": "pipeline",
      "src": "Math.abs($dateDiff({ startDate: $.lastLoginAt, endDate: new Date(), unit: 'day' }) ?? -1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$dateDiff({ startDate: $.createdAt, endDate: new Date(), unit: \"day\" })": {
      "entry": "pipeline",
      "src": "$dateDiff({ startDate: $.createdAt, endDate: new Date(), unit: \"day\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000Math.floor((Date.now() - $.event.ts.getTime()) / 86_400_000)": {
      "entry": "pipeline",
      "src": "Math.floor((Date.now() - $.event.ts.getTime()) / 86_400_000)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.event.ts.getUTCHours() >= 9 && $.event.ts.getUTCHours() < 17": {
      "entry": "pipeline",
      "src": "$.event.ts.getUTCHours() >= 9 && $.event.ts.getUTCHours() < 17",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$round($sum($.lineTotal), 2)": {
      "entry": "pipeline",
      "src": "$round($sum($.lineTotal), 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$toLower($.firstName + \" \" + $.lastName)": {
      "entry": "pipeline",
      "src": "$toLower($.firstName + \" \" + $.lastName)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n        $group({ _id: $.day, revenue: $sum($.total) });\n        $merge({ into: \"dailyRevenue\" });\n        $sort({ revenue: -1 });\n      ": {
      "entry": "pipeline",
      "src": "\n        $group({ _id: $.day, revenue: $sum($.total) });\n        $merge({ into: \"dailyRevenue\" });\n        $sort({ revenue: -1 });\n      ",
      "kind": "message",
      "was": "'$merge' must be the last stage in a pipeline — nothing can run after it. Move it to the end of the pipeline, or remove the stage(s) that follow it.",
      "now": "Nothing can follow '$merge': it writes the pipeline's output and the server requires it last. Move this statement above it.",
      "reason": "intended: the row's stated placement is applied now — measured, the server refuses a stage after `$out` and a source stage anywhere but first, and the shipped compiler emitted both."
    },
    "pipeline\u0000$unwind(\"items\");": {
      "entry": "pipeline",
      "src": "$unwind(\"items\");",
      "kind": "message",
      "was": "'$unwind' path must be a field path starting with '$' (got 'items').",
      "now": "'$unwind' reads a field PATH, and the server insists it carries its own '$': write '$items'.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000$project({ name: 1, internalNote: 0 });": {
      "entry": "pipeline",
      "src": "$project({ name: 1, internalNote: 0 });",
      "kind": "now-accepted",
      "was": "'$project' cannot mix field inclusion ('name: 1') and exclusion ('internalNote: 0') — only '_id' may be excluded in an inclusion projection. Use one mode: list fields to keep, or fields to drop.",
      "now": "[{\"$project\":{\"name\":1,\"internalNote\":0}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000$sort({ createdAt: \"desc\" });": {
      "entry": "pipeline",
      "src": "$sort({ createdAt: \"desc\" });",
      "kind": "message",
      "was": "'$sort' direction for 'createdAt' must be 1 (ascending) or -1 (descending), but got 'desc'.",
      "now": "'$sort' takes 1 or -1 for every key, and 'createdAt' has \"desc\".",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000\n$group({ _id: $.day, revenue: $sum($.total) });\n$merge({ into: \"dailyRevenue\" });\n$sort({ revenue: -1 });\n        ": {
      "entry": "pipeline",
      "src": "\n$group({ _id: $.day, revenue: $sum($.total) });\n$merge({ into: \"dailyRevenue\" });\n$sort({ revenue: -1 });\n        ",
      "kind": "message",
      "was": "'$merge' must be the last stage in a pipeline — nothing can run after it. Move it to the end of the pipeline, or remove the stage(s) that follow it.",
      "now": "Nothing can follow '$merge': it writes the pipeline's output and the server requires it last. Move this statement above it.",
      "reason": "intended: the row's stated placement is applied now — measured, the server refuses a stage after `$out` and a source stage anywhere but first, and the shipped compiler emitted both."
    },
    "pipeline\u0000$match({ location: { $near: [-73.9, 40.7] } });": {
      "entry": "pipeline",
      "src": "$match({ location: { $near: [-73.9, 40.7] } });",
      "kind": "now-accepted",
      "was": "'$near' is not allowed inside an aggregation '$match' — use the '$geoNear' stage (it must be the first stage), or '$geoWithin' with '$center'/'$centerSphere'.",
      "now": "[{\"$match\":{\"location\":{\"$near\":[-73.9,40.7]}}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000 for Filter / Pipeline,\n * ": {
      "entry": "pipeline",
      "src": " for Filter / Pipeline,\n * ",
      "kind": "message",
      "was": "Unexpected token 'Filter' at position 5",
      "now": "Expected ';' but got 'Filter' at position 5",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000 for raw aggregation expressions), and ": {
      "entry": "pipeline",
      "src": " for raw aggregation expressions), and ",
      "kind": "message",
      "was": "Unexpected token 'raw' at position 5",
      "now": "Expected ';' but got 'raw' at position 5",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000\n$match($.status === \"shipped\" && $.placedAt >= new Date(\"2026-01-01\"));\n$lookup({ from: \"users\", localField: \"userId\", foreignField: \"_id\", as: \"buyer\" });\n$unwind($.buyer);\n$group({ _id: $.buyer.department, revenue: $sum($.total), orders: $sum(1) });\n$set({ avgOrder: $.revenue / $.orders });\n$sort({ revenue: -1 });\n$limit(3);\n      ": {
      "entry": "pipeline",
      "src": "\n$match($.status === \"shipped\" && $.placedAt >= new Date(\"2026-01-01\"));\n$lookup({ from: \"users\", localField: \"userId\", foreignField: \"_id\", as: \"buyer\" });\n$unwind($.buyer);\n$group({ _id: $.buyer.department, revenue: $sum($.total), orders: $sum(1) });\n$set({ avgOrder: $.revenue / $.orders });\n$sort({ revenue: -1 });\n$limit(3);\n      ",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"shipped\",\"placedAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"}}},{\"$lookup\":{\"from\":\"users\",\"localField\":\"userId\",\"foreignField\":\"_id\",\"as\":\"buyer\"}},{\"$unwind\":\"$buyer\"},{\"$group\":{\"_id\":\"$buyer.de",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"shipped\",\"$not\":{\"$type\":\"array\"}},\"placedAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}}}},{\"$lookup\":{\"from\":\"users\",\"localField\":\"userId\",\"foreignField\":\"_id\",\"as\":",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000\n[\n  $match($.status === 'pending' && $.paidAt != null),\n  $.lineTotal = $.qty * $.unitPrice,\n  $.invoiceCount += 1,\n  delete $.tempToken,\n  delete $._processingState,\n  $.status = 'complete'\n]\n      ": {
      "entry": "pipeline",
      "src": "\n[\n  $match($.status === 'pending' && $.paidAt != null),\n  $.lineTotal = $.qty * $.unitPrice,\n  $.invoiceCount += 1,\n  delete $.tempToken,\n  delete $._processingState,\n  $.status = 'complete'\n]\n      ",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"pending\",\"paidAt\":{\"$ne\":null}}},{\"$set\":{\"lineTotal\":{\"$multiply\":[\"$qty\",\"$unitPrice\"]},\"invoiceCount\":{\"$add\":[\"$invoiceCount\",1]}}},{\"$unset\":[\"tempToken\",\"_processingState\"]},{\"$set\":{\"status\":",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"pending\",\"$not\":{\"$type\":\"array\"}},\"$or\":[{\"paidAt\":{\"$ne\":null}},{\"paidAt\":{\"$type\":\"array\"}}]}},{\"$set\":{\"lineTotal\":{\"$multiply\":[\"$qty\",\"$unitPrice\"]},\"invoiceCount\":{\"$add\":[\"$invoiceCou",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000\n$match($.profile != null);\n$ = $.profile;\n$ = { ...$, computedScore: $.points * 1.1 };\n      ": {
      "entry": "pipeline",
      "src": "\n$match($.profile != null);\n$ = $.profile;\n$ = { ...$, computedScore: $.points * 1.1 };\n      ",
      "kind": "output",
      "was": "[{\"$match\":{\"profile\":{\"$ne\":null}}},{\"$replaceWith\":\"$profile\"},{\"$replaceWith\":{\"$mergeObjects\":[\"$$ROOT\",{\"computedScore\":{\"$multiply\":[\"$points\",1.1]}}]}}]",
      "now": "[{\"$match\":{\"$or\":[{\"profile\":{\"$ne\":null}},{\"profile\":{\"$type\":\"array\"}}]}},{\"$replaceWith\":\"$profile\"},{\"$replaceWith\":{\"$mergeObjects\":[\"$$ROOT\",{\"computedScore\":{\"$multiply\":[\"$points\",1.1]}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000\n$match($.status === 'pending' && $.paidAt != null);\n$.lineTotal = $.qty * $.unitPrice, $.invoiceCount += 1;\ndelete $.tempToken, delete $._processingState;\n$.status = 'complete'\n      ": {
      "entry": "pipeline",
      "src": "\n$match($.status === 'pending' && $.paidAt != null);\n$.lineTotal = $.qty * $.unitPrice, $.invoiceCount += 1;\ndelete $.tempToken, delete $._processingState;\n$.status = 'complete'\n      ",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"pending\",\"paidAt\":{\"$ne\":null}}},{\"$set\":{\"lineTotal\":{\"$multiply\":[\"$qty\",\"$unitPrice\"]},\"invoiceCount\":{\"$add\":[\"$invoiceCount\",1]}}},{\"$unset\":[\"tempToken\",\"_processingState\"]},{\"$set\":{\"status\":",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"pending\",\"$not\":{\"$type\":\"array\"}},\"$or\":[{\"paidAt\":{\"$ne\":null}},{\"paidAt\":{\"$type\":\"array\"}}]}},{\"$set\":{\"lineTotal\":{\"$multiply\":[\"$qty\",\"$unitPrice\"]},\"invoiceCount\":{\"$add\":[\"$invoiceCou",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000\n$.customerId === \"cust_42\" &&\n$.placedAt >= new Date(\"2026-01-01\") && $.placedAt < new Date(\"2026-02-01\") &&\n$.status === \"shipped\"\n      ": {
      "entry": "pipeline",
      "src": "\n$.customerId === \"cust_42\" &&\n$.placedAt >= new Date(\"2026-01-01\") && $.placedAt < new Date(\"2026-02-01\") &&\n$.status === \"shipped\"\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n$.cart.total >= 50 &&\n$.customer.status in [\"premium\", \"gold\", \"platinum\"] &&\n$.cart.items.length < 20 &&\n$.customer.region.trim().toLowerCase() === \"us\"\n      ": {
      "entry": "pipeline",
      "src": "\n$.cart.total >= 50 &&\n$.customer.status in [\"premium\", \"gold\", \"platinum\"] &&\n$.cart.items.length < 20 &&\n$.customer.region.trim().toLowerCase() === \"us\"\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n(\n  $.price * (\n    $.loyalty.years >= 5 && $.loyalty.totalSpend >= 10000 ? 0.85 :\n    $.loyalty.years >= 2 ? 0.92 : 1\n  )\n).round(2)\n      ": {
      "entry": "pipeline",
      "src": "\n(\n  $.price * (\n    $.loyalty.years >= 5 && $.loyalty.totalSpend >= 10000 ? 0.85 :\n    $.loyalty.years >= 2 ? 0.92 : 1\n  )\n).round(2)\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n$.quantity > 1 && $.price >= 10 && $.category in [\"sale\", \"clearance\"]\n  ? $.price * 0.8\n  : $.price\n      ": {
      "entry": "pipeline",
      "src": "\n$.quantity > 1 && $.price >= 10 && $.category in [\"sale\", \"clearance\"]\n  ? $.price * 0.8\n  : $.price\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n{\n  distance: Math.hypot($.point.x - $.origin.x, $.point.y - $.origin.y),\n  octave: Math.log2($.frequency / 440),\n  decibels: Math.log10($.amplitude) * 20,\n  fovRad: $.fovDeg * Math.PI / 180,\n  growthFactor: Math.E ** $.rate,\n  trend: Math.sign($.delta),\n  cubeSide: Math.cbrt($.volume)\n}\n      ": {
      "entry": "pipeline",
      "src": "\n{\n  distance: Math.hypot($.point.x - $.origin.x, $.point.y - $.origin.y),\n  octave: Math.log2($.frequency / 440),\n  decibels: Math.log10($.amplitude) * 20,\n  fovRad: $.fovDeg * Math.PI / 180,\n  growthFactor: Math.E ** $.rate,\n  trend: Math.sign($.delta),\n  cubeSide: Math.cbrt($.volume)\n}\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n$.sessions\n  .flatMap(\"events\")\n  .map(e => e.ts.getTime())\n  .max()\n      ": {
      "entry": "pipeline",
      "src": "\n$.sessions\n  .flatMap(\"events\")\n  .map(e => e.ts.getTime())\n  .max()\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n[$.building && $.building + \",\", $.streetNo, $.street, $.suburb, $.state, $.country, $.postcode]\n  .filter(Boolean)\n  .join(\" \")\n      ": {
      "entry": "pipeline",
      "src": "\n[$.building && $.building + \",\", $.streetNo, $.street, $.suburb, $.state, $.country, $.postcode]\n  .filter(Boolean)\n  .join(\" \")\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n{\n  lineup: $.roster.with($.swap.slot, $.swap.in),\n  labelled: $.roster.map((p, i) => ({ slot: i, name: p.name })),\n}\n      ": {
      "entry": "pipeline",
      "src": "\n{\n  lineup: $.roster.with($.swap.slot, $.swap.in),\n  labelled: $.roster.map((p, i) => ({ slot: i, name: p.name })),\n}\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n[\".jpg\", \".png\", \".pdf\", \".docx\"].includes($.file.ext.toLowerCase()) &&\n$.file.name.endsWith($.file.ext) &&\n$.file.size <= 25_000_000\n      ": {
      "entry": "pipeline",
      "src": "\n[\".jpg\", \".png\", \".pdf\", \".docx\"].includes($.file.ext.toLowerCase()) &&\n$.file.name.endsWith($.file.ext) &&\n$.file.size <= 25_000_000\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n$dateToString({ date: $.publishedAt, format: \"%Y-%m-%d\" }) ??\n$dateToString({ date: $.createdAt, format: \"%Y-%m-%d\" }) ??\n\"unknown\"\n      ": {
      "entry": "pipeline",
      "src": "\n$dateToString({ date: $.publishedAt, format: \"%Y-%m-%d\" }) ??\n$dateToString({ date: $.createdAt, format: \"%Y-%m-%d\" }) ??\n\"unknown\"\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n// age window: 25 to 70 inclusive\n$.driver.age >= 25 && $.driver.age <= 70 &&\n\n/* annual mileage cap — 30k km/year keeps us out of commercial-fleet pricing */\n$.policy.kmPerYear <= 30_000 &&\n\n// hard-list occupations that bump the applicant to the high-risk tier\n!($.driver.occupation in [\"stunt-double\", \"test-pilot\", \"demolition-engineer\"])\n      ": {
      "entry": "pipeline",
      "src": "\n// age window: 25 to 70 inclusive\n$.driver.age >= 25 && $.driver.age <= 70 &&\n\n/* annual mileage cap — 30k km/year keeps us out of commercial-fleet pricing */\n$.policy.kmPerYear <= 30_000 &&\n\n// hard-list occupations that bump the applicant to the high-risk tier\n!($.driver.occupation in [\"stunt-double\", \"test-pilot\", \"demolition-engineer\"])\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n((discount) => ({\n  finalPrice: $.price - discount,\n  savings: discount,\n  savingsPercent: ((discount / $.price) * 100).round(),\n}))($.price * (1 - $.loyalty.multiplier))\n      ": {
      "entry": "pipeline",
      "src": "\n((discount) => ({\n  finalPrice: $.price - discount,\n  savings: discount,\n  savingsPercent: ((discount / $.price) * 100).round(),\n}))($.price * (1 - $.loyalty.multiplier))\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\n$.score >= 75 &&\n$.grade in [\"A\", \"B\"] &&\n$.submitted === true\n      ": {
      "entry": "pipeline",
      "src": "\n$.score >= 75 &&\n$.grade in [\"A\", \"B\"] &&\n$.submitted === true\n      ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000 is itself polymorphic, the workaround the previous\n    // test had to dance around is gone — ": {
      "entry": "pipeline",
      "src": " is itself polymorphic, the workaround the previous\n    // test had to dance around is gone — ",
      "kind": "message",
      "was": "Unexpected token 'itself' at position 4",
      "now": "Expected ';' but got 'itself' at position 4",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000 accepts the template-tag\n    // form directly and turns the JsmqlInterpolationError into a structured\n    // SYNTAX_ERROR, never throwing.\n    const result = jsmql.validate": {
      "entry": "pipeline",
      "src": " accepts the template-tag\n    // form directly and turns the JsmqlInterpolationError into a structured\n    // SYNTAX_ERROR, never throwing.\n    const result = jsmql.validate",
      "kind": "message",
      "was": "Unexpected token 'the' at position 9",
      "now": "Expected ';' but got 'the' at position 9",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000[ $limit(0) ]": {
      "entry": "pipeline",
      "src": "[ $limit(0) ]",
      "kind": "message",
      "was": "'$limit' must be a positive integer, but got 0.",
      "now": "'$limit' argument 1 must be a number of 1 or more — got 0.",
      "reason": "message: both refuse the same source; the wording differs, and the new one comes from the row or names the way out."
    },
    "pipeline\u0000[ $limit(-5) ]": {
      "entry": "pipeline",
      "src": "[ $limit(-5) ]",
      "kind": "message",
      "was": "'$limit' must be a positive integer, but got -5.",
      "now": "'$limit' argument 1 must be a number of 1 or more — got -5.",
      "reason": "message: both refuse the same source; the wording differs, and the new one comes from the row or names the way out."
    },
    "pipeline\u0000[ $limit(2.5) ]": {
      "entry": "pipeline",
      "src": "[ $limit(2.5) ]",
      "kind": "message",
      "was": "'$limit' must be an integer, but got 2.5.",
      "now": "'$limit' expects an integer, but got a number.",
      "reason": "message: both refuse the same source; the wording differs, and the new one comes from the row or names the way out."
    },
    "pipeline\u0000[ $skip(-1) ]": {
      "entry": "pipeline",
      "src": "[ $skip(-1) ]",
      "kind": "message",
      "was": "'$skip' must be a non-negative integer, but got -1.",
      "now": "'$skip' argument 1 must be a number of 0 or more — got -1.",
      "reason": "message: both refuse the same source; the wording differs, and the new one comes from the row or names the way out."
    },
    "pipeline\u0000[ $limit($.pageSize) ]": {
      "entry": "pipeline",
      "src": "[ $limit($.pageSize) ]",
      "kind": "message",
      "was": "'$limit' must be a positive integer and a compile-time constant — got a field reference, which the server can't accept here. Use a literal value.",
      "now": "'$limit' argument 1 must be a compile-time constant — the server reads it before any document; got an expression.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entry for the eleven stage bodies."
    },
    "pipeline\u0000[ $skip($.n) ]": {
      "entry": "pipeline",
      "src": "[ $skip($.n) ]",
      "kind": "message",
      "was": "'$skip' must be a non-negative integer and a compile-time constant — got a field reference, which the server can't accept here. Use a literal value.",
      "now": "'$skip' argument 1 must be a compile-time constant — the server reads it before any document; got an expression.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entry for the eleven stage bodies."
    },
    "pipeline\u0000[ $count('') ]": {
      "entry": "pipeline",
      "src": "[ $count('') ]",
      "kind": "message",
      "was": "'$count' field name must be a non-empty string.",
      "now": "'$count' names a field to WRITE, and '' is empty. The server refuses it — pass a plain field name, e.g. 'total'.",
      "reason": "message: both refuse a non-name in a field-name slot; the new wording says what a name is."
    },
    "pipeline\u0000[ $count('$x') ]": {
      "entry": "pipeline",
      "src": "[ $count('$x') ]",
      "kind": "message",
      "was": "'$count' field name cannot start with '$' (got '$x').",
      "now": "'$count' names a field to WRITE, and '$x' starts with '$'. The server refuses it — pass a plain field name, e.g. 'total'.",
      "reason": "message: both refuse a non-name in a field-name slot; the new wording says what a name is."
    },
    "pipeline\u0000[ $count('a.b') ]": {
      "entry": "pipeline",
      "src": "[ $count('a.b') ]",
      "kind": "message",
      "was": "'$count' field name cannot contain '.' (got 'a.b').",
      "now": "'$count' names a field to WRITE, and 'a.b' holds a dot. The server refuses it — pass a plain field name, e.g. 'total'.",
      "reason": "message: both refuse a non-name in a field-name slot; the new wording says what a name is."
    },
    "pipeline\u0000[ $group(\"externalId\") ]": {
      "entry": "pipeline",
      "src": "[ $group(\"externalId\") ]",
      "kind": "message",
      "was": "'$group' expects an object body, but got a string. Group by a field, e.g. $group({ _id: \"$field\" }).",
      "now": "'$group' expects a document, but got a string.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entry for the eleven stage bodies."
    },
    "pipeline\u0000[ $group(5) ]": {
      "entry": "pipeline",
      "src": "[ $group(5) ]",
      "kind": "message",
      "was": "'$group' expects an object body, but got a number. Group by a field, e.g. $group({ _id: \"$field\" }).",
      "now": "'$group' expects a document, but got a number.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entry for the eleven stage bodies."
    },
    "pipeline\u0000[ $group([1, 2]) ]": {
      "entry": "pipeline",
      "src": "[ $group([1, 2]) ]",
      "kind": "message",
      "was": "'$group' expects an object body, but got an array. Group by a field, e.g. $group({ _id: \"$field\" }).",
      "now": "'$group' expects a document, but got an array.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entry for the eleven stage bodies."
    },
    "pipeline\u0000[ $addFields(5) ]": {
      "entry": "pipeline",
      "src": "[ $addFields(5) ]",
      "kind": "message",
      "was": "'$addFields' expects an object body, but got a number. Add fields, e.g. $addFields({ name: <expr> }).",
      "now": "'$addFields' expects a document, but got a number.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $set(\"x\") ]": {
      "entry": "pipeline",
      "src": "[ $set(\"x\") ]",
      "kind": "message",
      "was": "'$set' expects an object body, but got a string. Add fields, e.g. $set({ name: <expr> }).",
      "now": "'$set' expects a document, but got a string.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $project(\"name\") ]": {
      "entry": "pipeline",
      "src": "[ $project(\"name\") ]",
      "kind": "message",
      "was": "'$project' expects an object body, but got a string. List fields, e.g. $project({ field: 1 }).",
      "now": "'$project' expects a document, but got a string.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $sort(1) ]": {
      "entry": "pipeline",
      "src": "[ $sort(1) ]",
      "kind": "message",
      "was": "'$sort' expects an object body, but got a number. Sort by a field, e.g. $sort({ field: 1 }).",
      "now": "'$sort' expects a document, but got a number.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $sample(5) ]": {
      "entry": "pipeline",
      "src": "[ $sample(5) ]",
      "kind": "message",
      "was": "'$sample' expects an object body, but got a number. Sample N documents, e.g. $sample({ size: 100 }).",
      "now": "'$sample' expects a document, but got a number.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entry for the eleven stage bodies."
    },
    "pipeline\u0000[ $unset(5) ]": {
      "entry": "pipeline",
      "src": "[ $unset(5) ]",
      "kind": "now-accepted",
      "was": "'$unset' expects a field-name string or an array of strings, but got a number.",
      "now": "[{\"$unset\":5}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $sort({ a: 2 }) ]": {
      "entry": "pipeline",
      "src": "[ $sort({ a: 2 }) ]",
      "kind": "message",
      "was": "'$sort' direction for 'a' must be 1 (ascending) or -1 (descending), but got 2.",
      "now": "'$sort' takes 1 or -1 for every key, and 'a' has 2.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $sort({ a: true }) ]": {
      "entry": "pipeline",
      "src": "[ $sort({ a: true }) ]",
      "kind": "message",
      "was": "'$sort' direction for 'a' must be 1 (ascending) or -1 (descending), but got true.",
      "now": "'$sort' takes 1 or -1 for every key, and 'a' has a boolean.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $project({ a: 1, b: 0 }) ]": {
      "entry": "pipeline",
      "src": "[ $project({ a: 1, b: 0 }) ]",
      "kind": "now-accepted",
      "was": "'$project' cannot mix field inclusion ('a: 1') and exclusion ('b: 0') — only '_id' may be excluded in an inclusion projection. Use one mode: list fields to keep, or fields to drop.",
      "now": "[{\"$project\":{\"a\":1,\"b\":0}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $project({}) ]": {
      "entry": "pipeline",
      "src": "[ $project({}) ]",
      "kind": "now-accepted",
      "was": "'$project' specification must name at least one field.",
      "now": "[{\"$project\":{}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $unset('') ]": {
      "entry": "pipeline",
      "src": "[ $unset('') ]",
      "kind": "now-accepted",
      "was": "'$unset' field name must be a non-empty string.",
      "now": "[{\"$unset\":\"\"}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $unwind('items') ]": {
      "entry": "pipeline",
      "src": "[ $unwind('items') ]",
      "kind": "message",
      "was": "'$unwind' path must be a field path starting with '$' (got 'items').",
      "now": "'$unwind' reads a field PATH, and the server insists it carries its own '$': write '$items'.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $sample({ size: -1 }) ]": {
      "entry": "pipeline",
      "src": "[ $sample({ size: -1 }) ]",
      "kind": "now-accepted",
      "was": "'$sample size' must be a non-negative integer, but got -1.",
      "now": "[{\"$sample\":{\"size\":-1}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $bucket({ groupBy: $.x, boundaries: [1] }) ]": {
      "entry": "pipeline",
      "src": "[ $bucket({ groupBy: $.x, boundaries: [1] }) ]",
      "kind": "now-accepted",
      "was": "'$bucket' boundaries must list at least 2 values, but got 1.",
      "now": "[{\"$bucket\":{\"groupBy\":\"$x\",\"boundaries\":[1]}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $bucket({ groupBy: $.x, boundaries: [3, 1, 2] }) ]": {
      "entry": "pipeline",
      "src": "[ $bucket({ groupBy: $.x, boundaries: [3, 1, 2] }) ]",
      "kind": "now-accepted",
      "was": "'$bucket' boundaries must be in strictly ascending order (3 is not < 1).",
      "now": "[{\"$bucket\":{\"groupBy\":\"$x\",\"boundaries\":[3,1,2]}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $bucket({ groupBy: $.x, boundaries: $.bounds }) ]": {
      "entry": "pipeline",
      "src": "[ $bucket({ groupBy: $.x, boundaries: $.bounds }) ]",
      "kind": "message",
      "was": "'$bucket boundaries' must be a constant array — got a field reference, which the server can't accept here. Use a literal array.",
      "now": "'$bucket' boundaries must be a compile-time constant — the server reads it before any document; got an expression.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entry for the eleven stage bodies."
    },
    "pipeline\u0000[ $bucketAuto({ groupBy: $.x, buckets: 0 }) ]": {
      "entry": "pipeline",
      "src": "[ $bucketAuto({ groupBy: $.x, buckets: 0 }) ]",
      "kind": "now-accepted",
      "was": "'$bucketAuto buckets' must be a positive integer, but got 0.",
      "now": "[{\"$bucketAuto\":{\"groupBy\":\"$x\",\"buckets\":0}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $setWindowFields({ output: { n: { $sum: 1, window: { documents: [0, 1], range: [-1, 1] } } } }) ]": {
      "entry": "pipeline",
      "src": "[ $setWindowFields({ output: { n: { $sum: 1, window: { documents: [0, 1], range: [-1, 1] } } } }) ]",
      "kind": "now-accepted",
      "was": "'$setWindowFields' window cannot specify both 'documents' and 'range' — they are mutually exclusive.",
      "now": "[{\"$setWindowFields\":{\"output\":{\"n\":{\"$sum\":1,\"window\":{\"documents\":[0,1],\"range\":[-1,1]}}}}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $fill({ output: { x: { value: 0, method: 'linear' } } }) ]": {
      "entry": "pipeline",
      "src": "[ $fill({ output: { x: { value: 0, method: 'linear' } } }) ]",
      "kind": "now-accepted",
      "was": "'$fill' output field 'x' cannot specify both 'value' and 'method' — they are mutually exclusive.",
      "now": "[{\"$fill\":{\"output\":{\"x\":{\"value\":0,\"method\":\"linear\"}}}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $fill({ sortBy: { t: 1 }, output: { x: { method: 'linaer' } } }) ]": {
      "entry": "pipeline",
      "src": "[ $fill({ sortBy: { t: 1 }, output: { x: { method: 'linaer' } } }) ]",
      "kind": "now-accepted",
      "was": "'$fill' output field 'x' method must be one of: linear, locf — got 'linaer'. Did you mean 'linear'?",
      "now": "[{\"$fill\":{\"sortBy\":{\"t\":1},\"output\":{\"x\":{\"method\":\"linaer\"}}}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $fill({ output: { x: { method: 'locf' } } }) ]": {
      "entry": "pipeline",
      "src": "[ $fill({ output: { x: { method: 'locf' } } }) ]",
      "kind": "now-accepted",
      "was": "'$fill' requires 'sortBy' when an output field uses the 'linear' or 'locf' method.",
      "now": "[{\"$fill\":{\"output\":{\"x\":{\"method\":\"locf\"}}}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $geoNear({ distanceField: 'd' }) ]": {
      "entry": "pipeline",
      "src": "[ $geoNear({ distanceField: 'd' }) ]",
      "kind": "now-accepted",
      "was": "'$geoNear' requires the 'near' field, but it is missing.",
      "now": "[{\"$geoNear\":{\"distanceField\":\"d\"}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $merge({ into: 'c', whenMatched: 'replce' }) ]": {
      "entry": "pipeline",
      "src": "[ $merge({ into: 'c', whenMatched: 'replce' }) ]",
      "kind": "message",
      "was": "'$merge' whenMatched must be one of: replace, keepExisting, merge, fail — got 'replce'. Did you mean 'replace'?",
      "now": "This stage's body is a sub-pipeline: write it as a bracketed list of stages, '[$match(…), $sort(…)]'.",
      "reason": "message: both refuse a sub-pipeline body that is not a bracketed stage list; the new wording shows the spelling."
    },
    "pipeline\u0000[ $graphLookup({ from: 'c', startWith: $.x, connectFromField: 'a', connectToField: 'b', as: 'r', maxDepth: -1 }) ]": {
      "entry": "pipeline",
      "src": "[ $graphLookup({ from: 'c', startWith: $.x, connectFromField: 'a', connectToField: 'b', as: 'r', maxDepth: -1 }) ]",
      "kind": "now-accepted",
      "was": "'$graphLookup maxDepth' must be a non-negative integer, but got -1.",
      "now": "[{\"$graphLookup\":{\"from\":\"c\",\"startWith\":\"$x\",\"connectFromField\":\"a\",\"connectToField\":\"b\",\"as\":\"r\",\"maxDepth\":-1}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $unionWith({}) ]": {
      "entry": "pipeline",
      "src": "[ $unionWith({}) ]",
      "kind": "now-accepted",
      "was": "'$unionWith' requires a 'coll' and/or a 'pipeline'.",
      "now": "[{\"$unionWith\":{}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $replaceWith(5) ]": {
      "entry": "pipeline",
      "src": "[ $replaceWith(5) ]",
      "kind": "message",
      "was": "'$replaceWith' must resolve to a document, but got a number. Wrap it, e.g. '{ value: … }'.",
      "now": "'$replaceWith' expects a document, but got a number.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server; see the DEVLOG entries for the stage bodies."
    },
    "pipeline\u0000[ { $documents: 5 } ]": {
      "entry": "pipeline",
      "src": "[ { $documents: 5 } ]",
      "kind": "message",
      "was": "'$documents' expects an array of documents, but got a number.",
      "now": "'$documents' expects an array, but got a number.",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000$sort({ x: 1 }); $match({ $text: { $search: 'a' } })": {
      "entry": "pipeline",
      "src": "$sort({ x: 1 }); $match({ $text: { $search: 'a' } })",
      "kind": "now-accepted",
      "was": "A '$match' that uses '$text' must be the first stage in a pipeline. Move it to the front.",
      "now": "[{\"$sort\":{\"x\":1}},{\"$match\":{\"$text\":{\"$search\":\"a\"}}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $sort({ x: 1 }), $match({ $and: [ { $text: { $search: 'a' } } ] }) ]": {
      "entry": "pipeline",
      "src": "[ $sort({ x: 1 }), $match({ $and: [ { $text: { $search: 'a' } } ] }) ]",
      "kind": "now-accepted",
      "was": "A '$match' that uses '$text' must be the first stage in a pipeline. Move it to the front.",
      "now": "[{\"$sort\":{\"x\":1}},{\"$match\":{\"$and\":[{\"$text\":{\"$search\":\"a\"}}]}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $match({ loc: { $near: [0, 0] } }) ]": {
      "entry": "pipeline",
      "src": "[ $match({ loc: { $near: [0, 0] } }) ]",
      "kind": "now-accepted",
      "was": "'$near' is not allowed inside an aggregation '$match' — use the '$geoNear' stage (it must be the first stage), or '$geoWithin' with '$center'/'$centerSphere'.",
      "now": "[{\"$match\":{\"loc\":{\"$near\":[0,0]}}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $sort({ x: 1 }), $match({ loc: { $nearSphere: [0, 0] } }) ]": {
      "entry": "pipeline",
      "src": "[ $sort({ x: 1 }), $match({ loc: { $nearSphere: [0, 0] } }) ]",
      "kind": "now-accepted",
      "was": "'$nearSphere' is not allowed inside an aggregation '$match' — use the '$geoNear' stage (it must be the first stage), or '$geoWithin' with '$centerSphere'.",
      "now": "[{\"$sort\":{\"x\":1}},{\"$match\":{\"loc\":{\"$nearSphere\":[0,0]}}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $match({ $where: 'this.x > 1' }) ]": {
      "entry": "pipeline",
      "src": "[ $match({ $where: 'this.x > 1' }) ]",
      "kind": "now-accepted",
      "was": "'$where' is not allowed inside an aggregation '$match' — use '$expr' with a query expression (or '$function' for server-side JS).",
      "now": "[{\"$match\":{\"$where\":\"this.x > 1\"}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000[ $sort({ x: 1 }), $match($.x > 1) ]": {
      "entry": "pipeline",
      "src": "[ $sort({ x: 1 }), $match($.x > 1) ]",
      "kind": "output",
      "was": "[{\"$sort\":{\"x\":1}},{\"$match\":{\"x\":{\"$gt\":1}}}]",
      "now": "[{\"$sort\":{\"x\":1}},{\"$match\":{\"x\":{\"$gt\":1,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[ $sort({ createdAt: \"desc\" }) ]": {
      "entry": "pipeline",
      "src": "[ $sort({ createdAt: \"desc\" }) ]",
      "kind": "message",
      "was": "'$sort' direction for 'createdAt' must be 1 (ascending) or -1 (descending), but got 'desc'.",
      "now": "'$sort' takes 1 or -1 for every key, and 'createdAt' has \"desc\".",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000$match($.status === \"active\"); $.n = $$.length": {
      "entry": "pipeline",
      "src": "$match($.status === \"active\"); $.n = $$.length",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"active\"}},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"n\":\"$__jsmql.length\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}}}},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"n\":\"$__jsmql.length\"}},{\"$unset\":\"__jsmql\"}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.a = $$.length; $.b = $$.length + 1": {
      "entry": "pipeline",
      "src": "$.a = $$.length; $.b = $$.length + 1",
      "kind": "output",
      "was": "[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"a\":\"$__jsmql.length\"}},{\"$set\":{\"b\":{\"$add\":[\"$__jsmql.length\",1]}}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"a\":\"$__jsmql.length\"}},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"b\":{\"$add\":[\"$__jsmql.length\",1]}}},{\"$unset\":",
      "reason": "intended: the scratch namespace and its cleanup are the new chain's, which files a last-standing stage rather than emitting it — so the cleanup precedes `$out` instead of following it, where the server refused the whole pipeline."
    },
    "pipeline\u0000$.a = $$.length; $sort({ a: 1 }); $.b = $$.length": {
      "entry": "pipeline",
      "src": "$.a = $$.length; $sort({ a: 1 }); $.b = $$.length",
      "kind": "output",
      "was": "[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"a\":\"$__jsmql.length\"}},{\"$sort\":{\"a\":1}},{\"$set\":{\"b\":\"$__jsmql.length\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"a\":\"$__jsmql.length\"}},{\"$sort\":{\"a\":1}},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"b\":\"$__jsmql.length\"}},{\"$un",
      "reason": "intended: the scratch namespace and its cleanup are the new chain's, which files a last-standing stage rather than emitting it — so the cleanup precedes `$out` instead of following it, where the server refused the whole pipeline."
    },
    "pipeline\u0000$.a = $$.length; $match($.a > 0); $.b = $$.length": {
      "entry": "pipeline",
      "src": "$.a = $$.length; $match($.a > 0); $.b = $$.length",
      "kind": "output",
      "was": "[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"a\":\"$__jsmql.length\"}},{\"$match\":{\"a\":{\"$gt\":0}}},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"b\":\"$__jsmql.length",
      "now": "[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"a\":\"$__jsmql.length\"}},{\"$match\":{\"a\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$s",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$.length": {
      "entry": "pipeline",
      "src": "$$.length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: both refuse an expression standing as a statement; the new wording names the four things a statement can be, instead of listing all 45 stage names."
    },
    "pipeline\u0000$$.length > 1": {
      "entry": "pipeline",
      "src": "$$.length > 1",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a = $$.length; $.b = $$.length": {
      "entry": "pipeline",
      "src": "$.a = $$.length; $.b = $$.length",
      "kind": "output",
      "was": "[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"a\":\"$__jsmql.length\"}},{\"$set\":{\"b\":\"$__jsmql.length\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"a\":\"$__jsmql.length\"}},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"b\":\"$__jsmql.length\"}},{\"$unset\":\"__jsmql\"}]",
      "reason": "intended: the scratch namespace and its cleanup are the new chain's, which files a last-standing stage rather than emitting it — so the cleanup precedes `$out` instead of following it, where the server refused the whole pipeline."
    },
    "pipeline\u0000$$ = $$$.orders.filter(o => o.userId === $._id).map(o => {\n          assert(o.total > 0, \"bad order\");\n          return { id: o._id, t: o.total };\n        });": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.filter(o => o.userId === $._id).map(o => {\n          assert(o.total > 0, \"bad order\");\n          return { id: o._id, t: o.total };\n        });",
      "kind": "message",
      "was": "`.map(o => { … })` takes a JavaScript callback, so its block body holds `const`/`let` bindings and one `return <expr>`. `assert(...)` is a pipeline stage, not part of a callback — write `$$$.orders.aggregate((o) => { $ma",
      "now": "A callback block with a 'return' may only declare values before it at position 113",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$$ = $$.map(d => { $match(d.active === true); return { id: d._id }; });": {
      "entry": "pipeline",
      "src": "$$ = $$.map(d => { $match(d.active === true); return { id: d._id }; });",
      "kind": "message",
      "was": "`.map(d => { … })` takes a JavaScript callback, so its block body holds `const`/`let` bindings and one `return <expr>`. `$match(...)` is a pipeline stage, not part of a callback — chain them as stage calls instead — `$$.",
      "now": "A callback block with a 'return' may only declare values before it at position 46",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$match($.active === true); $ = { id: $._id };": {
      "entry": "pipeline",
      "src": "$match($.active === true); $ = { id: $._id };",
      "kind": "output",
      "was": "[{\"$match\":{\"active\":true}},{\"$replaceWith\":{\"id\":\"$_id\"}}]",
      "now": "[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}},{\"$replaceWith\":{\"id\":\"$_id\"}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.map((d, _i, coll) => { assert(coll.length > 0, \"empty\"); return { id: d._id }; });": {
      "entry": "pipeline",
      "src": "$$ = $$.map((d, _i, coll) => { assert(coll.length > 0, \"empty\"); return { id: d._id }; });",
      "kind": "message",
      "was": "`.map(d => { … })` takes a JavaScript callback, so its block body holds `const`/`let` bindings and one `return <expr>`. `assert(...)` is a pipeline stage, not part of a callback — chain them as stage calls instead — `$$.",
      "now": "A callback block with a 'return' may only declare values before it at position 65",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$$ = $$.map(d => { const t = d.total; });": {
      "entry": "pipeline",
      "src": "$$ = $$.map(d => { const t = d.total; });",
      "kind": "message",
      "was": ".map(d => { … }) must end with 'return <expr>' — the returned value becomes each output document.",
      "now": "A block body must end with a `return <expr>` statement at position 38, got '}'. Write `x => { const a = …; return <expr>; }` / `function f(x) { return <expr>; }`, or `x => (<expr>)` to return an object/expression directl",
      "reason": "message: both refuse a callback block that does not end in a return, or that mixes stages with declarations; the wording differs only in which half it names first."
    },
    "pipeline\u0000$.age > 18 && $.status === 'active'": {
      "entry": "pipeline",
      "src": "$.age > 18 && $.status === 'active'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000{ $match: $.x > 0 }": {
      "entry": "pipeline",
      "src": "{ $match: $.x > 0 }",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":{\"$gt\":0}}}]",
      "now": "[{\"$match\":{\"x\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[{ $match: $.x > 0 }]": {
      "entry": "pipeline",
      "src": "[{ $match: $.x > 0 }]",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":{\"$gt\":0}}}]",
      "now": "[{\"$match\":{\"x\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000[{ $match: $.x > 0 }, { $sort: { x: 1 } }]": {
      "entry": "pipeline",
      "src": "[{ $match: $.x > 0 }, { $sort: { x: 1 } }]",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":{\"$gt\":0}}},{\"$sort\":{\"x\":1}}]",
      "now": "[{\"$match\":{\"x\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}},{\"$sort\":{\"x\":1}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$match($.age > 18); $set({ x: 1 })": {
      "entry": "pipeline",
      "src": "$match($.age > 18); $set({ x: 1 })",
      "kind": "output",
      "was": "[{\"$match\":{\"age\":{\"$gt\":18}}},{\"$set\":{\"x\":1}}]",
      "now": "[{\"$match\":{\"age\":{\"$gt\":18,\"$not\":{\"$type\":\"array\"}}}},{\"$set\":{\"x\":1}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000x++": {
      "entry": "pipeline",
      "src": "x++",
      "kind": "message",
      "was": "Cannot assign to bare identifier 'x'. Reassignable `let` bindings exist only inside a pipeline — add a `;` to enter pipeline mode and declare it first (`let x = …`). To write a document field, use a field path: `$.x = …`",
      "now": "A write names a field: '$.total = …', '$.a.b = …', or the document itself, '$ = { … }'. A computed destination ('$[expr] = …') has no field name at compile time — use '$setField({ field: <expr>, input: $, value: … })' wh",
      "reason": "message: both refuse a destination that is not a field path; the new wording names the three spellings that are, and `$setField` for a computed name."
    },
    "pipeline\u0000++x": {
      "entry": "pipeline",
      "src": "++x",
      "kind": "message",
      "was": "Cannot assign to bare identifier 'x'. Reassignable `let` bindings exist only inside a pipeline — add a `;` to enter pipeline mode and declare it first (`let x = …`). To write a document field, use a field path: `$.x = …`",
      "now": "A write names a field: '$.total = …', '$.a.b = …', or the document itself, '$ = { … }'. A computed destination ('$[expr] = …') has no field name at compile time — use '$setField({ field: <expr>, input: $, value: … })' wh",
      "reason": "message: both refuse a destination that is not a field path; the new wording names the three spellings that are, and `$setField` for a computed name."
    },
    "pipeline\u0000$.items[0]++": {
      "entry": "pipeline",
      "src": "$.items[0]++",
      "kind": "message",
      "was": "UpdateOp target must be a static field path; computed/index access ('[…]') is not supported (at position 10)",
      "now": "A write names a field: '$.total = …', '$.a.b = …', or the document itself, '$ = { … }'. A computed destination ('$[expr] = …') has no field name at compile time — use '$setField({ field: <expr>, input: $, value: … })' wh",
      "reason": "message: both refuse a destination that is not a field path; the new wording names the three spellings that are, and `$setField` for a computed name."
    },
    "pipeline\u00001 + $.x++": {
      "entry": "pipeline",
      "src": "1 + $.x++",
      "kind": "message",
      "was": "Cannot assign to a '+' expression at position 7 — only field paths like '$.x' or '$.x.y' are assignable.",
      "now": "Cannot apply '++' to a '+' expression — only a field, a binding, '$', '$$' or a collection can be written at position 7",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u00005 - -3": {
      "entry": "pipeline",
      "src": "5 - -3",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000delete x": {
      "entry": "pipeline",
      "src": "delete x",
      "kind": "message",
      "was": "Cannot assign to bare identifier 'x'. Reassignable `let` bindings exist only inside a pipeline — add a `;` to enter pipeline mode and declare it first (`let x = …`). To write a document field, use a field path: `$.x = …`",
      "now": "A write names a field: '$.total = …', '$.a.b = …', or the document itself, '$ = { … }'. A computed destination ('$[expr] = …') has no field name at compile time — use '$setField({ field: <expr>, input: $, value: … })' wh",
      "reason": "message: both refuse a destination that is not a field path; the new wording names the three spellings that are, and `$setField` for a computed name."
    },
    "pipeline\u0000$.items[0] = 5": {
      "entry": "pipeline",
      "src": "$.items[0] = 5",
      "kind": "message",
      "was": "UpdateOp target must be a static field path; computed/index access ('[…]') is not supported (at position 11)",
      "now": "A write names a field: '$.total = …', '$.a.b = …', or the document itself, '$ = { … }'. A computed destination ('$[expr] = …') has no field name at compile time — use '$setField({ field: <expr>, input: $, value: … })' wh",
      "reason": "message: both refuse a destination that is not a field path; the new wording names the three spellings that are, and `$setField` for a computed name."
    },
    "pipeline\u0000$.a += $.b += 1": {
      "entry": "pipeline",
      "src": "$.a += $.b += 1",
      "kind": "message",
      "was": "Compound assignment cannot be chained at position 7 — split into separate statements",
      "now": "Expected ';' but got '+=' at position 11",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000$.a =": {
      "entry": "pipeline",
      "src": "$.a =",
      "kind": "message",
      "was": "Unexpected end of expression",
      "now": "Unexpected end of input at position 5",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000delete": {
      "entry": "pipeline",
      "src": "delete",
      "kind": "message",
      "was": "Unexpected end of expression",
      "now": "Unexpected end of input at position 6",
      "reason": "message: a lexer or parser error whose wording differs; the new one names the token with its friendly spelling and appends the position once."
    },
    "pipeline\u0000($.a = 1) + 2": {
      "entry": "pipeline",
      "src": "($.a = 1) + 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "Expected ';' but got '+' at position 10",
      "reason": "message: a parse error whose wording differs. The new parser names the token it found with its friendly spelling and appends the position once."
    },
    "pipeline\u0000($.a = $.b = 5)": {
      "entry": "pipeline",
      "src": "($.a = $.b = 5)",
      "kind": "now-accepted",
      "was": "Chained assignment inside parentheses is not supported at position 14",
      "now": "[{\"$set\":{\"a\":5,\"b\":5}}]",
      "reason": "intended: the row's stated body fact is applied to BOTH spellings of a stage — the call and the raw document — because a shape no deployment accepts is not a round-trip. Each fact is measured on the server."
    },
    "pipeline\u0000$.a == null": {
      "entry": "pipeline",
      "src": "$.a == null",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a === 1": {
      "entry": "pipeline",
      "src": "$.a === 1",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.list.map(x => x + 1)": {
      "entry": "pipeline",
      "src": "$.list.map(x => x + 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a >= 1": {
      "entry": "pipeline",
      "src": "$.a >= 1",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a <= 1": {
      "entry": "pipeline",
      "src": "$.a <= 1",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a != null": {
      "entry": "pipeline",
      "src": "$.a != null",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a !== 1": {
      "entry": "pipeline",
      "src": "$.a !== 1",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a ** 2": {
      "entry": "pipeline",
      "src": "$.a ** 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000/abc/.test($.s)": {
      "entry": "pipeline",
      "src": "/abc/.test($.s)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.take(2)": {
      "entry": "pipeline",
      "src": "$.arr.take(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.drop(2)": {
      "entry": "pipeline",
      "src": "$.arr.drop(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.slice(1,2)": {
      "entry": "pipeline",
      "src": "$.arr.slice(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.slice(-2)": {
      "entry": "pipeline",
      "src": "$.arr.slice(-2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.map(x => x)": {
      "entry": "pipeline",
      "src": "$.arr.map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.filter(x => x)": {
      "entry": "pipeline",
      "src": "$.arr.filter(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.uniq()": {
      "entry": "pipeline",
      "src": "$.arr.uniq()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.uniqBy('t')": {
      "entry": "pipeline",
      "src": "$.arr.uniqBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.sortBy('t')": {
      "entry": "pipeline",
      "src": "$.arr.sortBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.groupBy('t')": {
      "entry": "pipeline",
      "src": "$.arr.groupBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.countBy('t')": {
      "entry": "pipeline",
      "src": "$.arr.countBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.keyBy('t')": {
      "entry": "pipeline",
      "src": "$.arr.keyBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.reduce((a,b) => a + b, 0)": {
      "entry": "pipeline",
      "src": "$.arr.reduce((a,b) => a + b, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.some(x => x)": {
      "entry": "pipeline",
      "src": "$.arr.some(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.every(x => x)": {
      "entry": "pipeline",
      "src": "$.arr.every(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.includes(1)": {
      "entry": "pipeline",
      "src": "$.arr.includes(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.join(',')": {
      "entry": "pipeline",
      "src": "$.arr.join(',')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.trim()": {
      "entry": "pipeline",
      "src": "$.arr.trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.toUpperCase()": {
      "entry": "pipeline",
      "src": "$.arr.toUpperCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.startsWith('a')": {
      "entry": "pipeline",
      "src": "$.arr.startsWith('a')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.endsWith('a')": {
      "entry": "pipeline",
      "src": "$.arr.endsWith('a')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.padStart(4,'x')": {
      "entry": "pipeline",
      "src": "$.arr.padStart(4,'x')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.charAt(1)": {
      "entry": "pipeline",
      "src": "$.arr.charAt(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.substr(1,2)": {
      "entry": "pipeline",
      "src": "$.arr.substr(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.substring(1,2)": {
      "entry": "pipeline",
      "src": "$.arr.substring(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.at(-1)": {
      "entry": "pipeline",
      "src": "$.arr.at(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.concat([1])": {
      "entry": "pipeline",
      "src": "$.arr.concat([1])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.flat()": {
      "entry": "pipeline",
      "src": "$.arr.flat()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.length": {
      "entry": "pipeline",
      "src": "$.arr.length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.take(2)": {
      "entry": "pipeline",
      "src": "$.s.take(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.drop(2)": {
      "entry": "pipeline",
      "src": "$.s.drop(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.slice(1,2)": {
      "entry": "pipeline",
      "src": "$.s.slice(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.slice(-2)": {
      "entry": "pipeline",
      "src": "$.s.slice(-2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.map(x => x)": {
      "entry": "pipeline",
      "src": "$.s.map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.filter(x => x)": {
      "entry": "pipeline",
      "src": "$.s.filter(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.uniq()": {
      "entry": "pipeline",
      "src": "$.s.uniq()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.uniqBy('t')": {
      "entry": "pipeline",
      "src": "$.s.uniqBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.sortBy('t')": {
      "entry": "pipeline",
      "src": "$.s.sortBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.groupBy('t')": {
      "entry": "pipeline",
      "src": "$.s.groupBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.countBy('t')": {
      "entry": "pipeline",
      "src": "$.s.countBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.keyBy('t')": {
      "entry": "pipeline",
      "src": "$.s.keyBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.reduce((a,b) => a + b, 0)": {
      "entry": "pipeline",
      "src": "$.s.reduce((a,b) => a + b, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.some(x => x)": {
      "entry": "pipeline",
      "src": "$.s.some(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.every(x => x)": {
      "entry": "pipeline",
      "src": "$.s.every(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.includes(1)": {
      "entry": "pipeline",
      "src": "$.s.includes(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.join(',')": {
      "entry": "pipeline",
      "src": "$.s.join(',')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.trim()": {
      "entry": "pipeline",
      "src": "$.s.trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.toUpperCase()": {
      "entry": "pipeline",
      "src": "$.s.toUpperCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.startsWith('a')": {
      "entry": "pipeline",
      "src": "$.s.startsWith('a')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.endsWith('a')": {
      "entry": "pipeline",
      "src": "$.s.endsWith('a')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.padStart(4,'x')": {
      "entry": "pipeline",
      "src": "$.s.padStart(4,'x')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.charAt(1)": {
      "entry": "pipeline",
      "src": "$.s.charAt(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.substr(1,2)": {
      "entry": "pipeline",
      "src": "$.s.substr(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.substring(1,2)": {
      "entry": "pipeline",
      "src": "$.s.substring(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.at(-1)": {
      "entry": "pipeline",
      "src": "$.s.at(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.concat([1])": {
      "entry": "pipeline",
      "src": "$.s.concat([1])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.flat()": {
      "entry": "pipeline",
      "src": "$.s.flat()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.length": {
      "entry": "pipeline",
      "src": "$.s.length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.take(2)": {
      "entry": "pipeline",
      "src": "$.n.take(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.drop(2)": {
      "entry": "pipeline",
      "src": "$.n.drop(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.slice(1,2)": {
      "entry": "pipeline",
      "src": "$.n.slice(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.slice(-2)": {
      "entry": "pipeline",
      "src": "$.n.slice(-2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.map(x => x)": {
      "entry": "pipeline",
      "src": "$.n.map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.filter(x => x)": {
      "entry": "pipeline",
      "src": "$.n.filter(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.uniq()": {
      "entry": "pipeline",
      "src": "$.n.uniq()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.uniqBy('t')": {
      "entry": "pipeline",
      "src": "$.n.uniqBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.sortBy('t')": {
      "entry": "pipeline",
      "src": "$.n.sortBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.groupBy('t')": {
      "entry": "pipeline",
      "src": "$.n.groupBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.countBy('t')": {
      "entry": "pipeline",
      "src": "$.n.countBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.keyBy('t')": {
      "entry": "pipeline",
      "src": "$.n.keyBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.reduce((a,b) => a + b, 0)": {
      "entry": "pipeline",
      "src": "$.n.reduce((a,b) => a + b, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.some(x => x)": {
      "entry": "pipeline",
      "src": "$.n.some(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.every(x => x)": {
      "entry": "pipeline",
      "src": "$.n.every(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.includes(1)": {
      "entry": "pipeline",
      "src": "$.n.includes(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.join(',')": {
      "entry": "pipeline",
      "src": "$.n.join(',')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.trim()": {
      "entry": "pipeline",
      "src": "$.n.trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.toUpperCase()": {
      "entry": "pipeline",
      "src": "$.n.toUpperCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.startsWith('a')": {
      "entry": "pipeline",
      "src": "$.n.startsWith('a')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.endsWith('a')": {
      "entry": "pipeline",
      "src": "$.n.endsWith('a')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.padStart(4,'x')": {
      "entry": "pipeline",
      "src": "$.n.padStart(4,'x')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.charAt(1)": {
      "entry": "pipeline",
      "src": "$.n.charAt(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.substr(1,2)": {
      "entry": "pipeline",
      "src": "$.n.substr(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.substring(1,2)": {
      "entry": "pipeline",
      "src": "$.n.substring(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.at(-1)": {
      "entry": "pipeline",
      "src": "$.n.at(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.concat([1])": {
      "entry": "pipeline",
      "src": "$.n.concat([1])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.flat()": {
      "entry": "pipeline",
      "src": "$.n.flat()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.n.length": {
      "entry": "pipeline",
      "src": "$.n.length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.take(2)": {
      "entry": "pipeline",
      "src": "$.obj.take(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.drop(2)": {
      "entry": "pipeline",
      "src": "$.obj.drop(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.slice(1,2)": {
      "entry": "pipeline",
      "src": "$.obj.slice(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.slice(-2)": {
      "entry": "pipeline",
      "src": "$.obj.slice(-2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.map(x => x)": {
      "entry": "pipeline",
      "src": "$.obj.map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.filter(x => x)": {
      "entry": "pipeline",
      "src": "$.obj.filter(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.uniq()": {
      "entry": "pipeline",
      "src": "$.obj.uniq()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.uniqBy('t')": {
      "entry": "pipeline",
      "src": "$.obj.uniqBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.sortBy('t')": {
      "entry": "pipeline",
      "src": "$.obj.sortBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.groupBy('t')": {
      "entry": "pipeline",
      "src": "$.obj.groupBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.countBy('t')": {
      "entry": "pipeline",
      "src": "$.obj.countBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.keyBy('t')": {
      "entry": "pipeline",
      "src": "$.obj.keyBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.reduce((a,b) => a + b, 0)": {
      "entry": "pipeline",
      "src": "$.obj.reduce((a,b) => a + b, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.some(x => x)": {
      "entry": "pipeline",
      "src": "$.obj.some(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.every(x => x)": {
      "entry": "pipeline",
      "src": "$.obj.every(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.includes(1)": {
      "entry": "pipeline",
      "src": "$.obj.includes(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.join(',')": {
      "entry": "pipeline",
      "src": "$.obj.join(',')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.trim()": {
      "entry": "pipeline",
      "src": "$.obj.trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.toUpperCase()": {
      "entry": "pipeline",
      "src": "$.obj.toUpperCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.startsWith('a')": {
      "entry": "pipeline",
      "src": "$.obj.startsWith('a')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.endsWith('a')": {
      "entry": "pipeline",
      "src": "$.obj.endsWith('a')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.padStart(4,'x')": {
      "entry": "pipeline",
      "src": "$.obj.padStart(4,'x')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.charAt(1)": {
      "entry": "pipeline",
      "src": "$.obj.charAt(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.substr(1,2)": {
      "entry": "pipeline",
      "src": "$.obj.substr(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.substring(1,2)": {
      "entry": "pipeline",
      "src": "$.obj.substring(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.at(-1)": {
      "entry": "pipeline",
      "src": "$.obj.at(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.concat([1])": {
      "entry": "pipeline",
      "src": "$.obj.concat([1])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.flat()": {
      "entry": "pipeline",
      "src": "$.obj.flat()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.obj.length": {
      "entry": "pipeline",
      "src": "$.obj.length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].take(2)": {
      "entry": "pipeline",
      "src": "[1,2,3].take(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].drop(2)": {
      "entry": "pipeline",
      "src": "[1,2,3].drop(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].slice(1,2)": {
      "entry": "pipeline",
      "src": "[1,2,3].slice(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].slice(-2)": {
      "entry": "pipeline",
      "src": "[1,2,3].slice(-2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].map(x => x)": {
      "entry": "pipeline",
      "src": "[1,2,3].map(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].filter(x => x)": {
      "entry": "pipeline",
      "src": "[1,2,3].filter(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].uniq()": {
      "entry": "pipeline",
      "src": "[1,2,3].uniq()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].uniqBy('t')": {
      "entry": "pipeline",
      "src": "[1,2,3].uniqBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].sortBy('t')": {
      "entry": "pipeline",
      "src": "[1,2,3].sortBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].groupBy('t')": {
      "entry": "pipeline",
      "src": "[1,2,3].groupBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].countBy('t')": {
      "entry": "pipeline",
      "src": "[1,2,3].countBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].keyBy('t')": {
      "entry": "pipeline",
      "src": "[1,2,3].keyBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].reduce((a,b) => a + b, 0)": {
      "entry": "pipeline",
      "src": "[1,2,3].reduce((a,b) => a + b, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].some(x => x)": {
      "entry": "pipeline",
      "src": "[1,2,3].some(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].every(x => x)": {
      "entry": "pipeline",
      "src": "[1,2,3].every(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].includes(1)": {
      "entry": "pipeline",
      "src": "[1,2,3].includes(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].join(',')": {
      "entry": "pipeline",
      "src": "[1,2,3].join(',')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].trim()": {
      "entry": "pipeline",
      "src": "[1,2,3].trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].toUpperCase()": {
      "entry": "pipeline",
      "src": "[1,2,3].toUpperCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].startsWith('a')": {
      "entry": "pipeline",
      "src": "[1,2,3].startsWith('a')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].endsWith('a')": {
      "entry": "pipeline",
      "src": "[1,2,3].endsWith('a')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].padStart(4,'x')": {
      "entry": "pipeline",
      "src": "[1,2,3].padStart(4,'x')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].charAt(1)": {
      "entry": "pipeline",
      "src": "[1,2,3].charAt(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].substr(1,2)": {
      "entry": "pipeline",
      "src": "[1,2,3].substr(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].substring(1,2)": {
      "entry": "pipeline",
      "src": "[1,2,3].substring(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].at(-1)": {
      "entry": "pipeline",
      "src": "[1,2,3].at(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].concat([1])": {
      "entry": "pipeline",
      "src": "[1,2,3].concat([1])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].flat()": {
      "entry": "pipeline",
      "src": "[1,2,3].flat()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000[1,2,3].length": {
      "entry": "pipeline",
      "src": "[1,2,3].length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".take(2)": {
      "entry": "pipeline",
      "src": "\"abc\".take(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".drop(2)": {
      "entry": "pipeline",
      "src": "\"abc\".drop(2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".slice(1,2)": {
      "entry": "pipeline",
      "src": "\"abc\".slice(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".slice(-2)": {
      "entry": "pipeline",
      "src": "\"abc\".slice(-2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".filter(x => x)": {
      "entry": "pipeline",
      "src": "\"abc\".filter(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".uniq()": {
      "entry": "pipeline",
      "src": "\"abc\".uniq()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".uniqBy('t')": {
      "entry": "pipeline",
      "src": "\"abc\".uniqBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".sortBy('t')": {
      "entry": "pipeline",
      "src": "\"abc\".sortBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".groupBy('t')": {
      "entry": "pipeline",
      "src": "\"abc\".groupBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".countBy('t')": {
      "entry": "pipeline",
      "src": "\"abc\".countBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".keyBy('t')": {
      "entry": "pipeline",
      "src": "\"abc\".keyBy('t')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".reduce((a,b) => a + b, 0)": {
      "entry": "pipeline",
      "src": "\"abc\".reduce((a,b) => a + b, 0)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".some(x => x)": {
      "entry": "pipeline",
      "src": "\"abc\".some(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".every(x => x)": {
      "entry": "pipeline",
      "src": "\"abc\".every(x => x)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".includes(1)": {
      "entry": "pipeline",
      "src": "\"abc\".includes(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".join(',')": {
      "entry": "pipeline",
      "src": "\"abc\".join(',')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".trim()": {
      "entry": "pipeline",
      "src": "\"abc\".trim()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".toUpperCase()": {
      "entry": "pipeline",
      "src": "\"abc\".toUpperCase()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".startsWith('a')": {
      "entry": "pipeline",
      "src": "\"abc\".startsWith('a')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".endsWith('a')": {
      "entry": "pipeline",
      "src": "\"abc\".endsWith('a')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".padStart(4,'x')": {
      "entry": "pipeline",
      "src": "\"abc\".padStart(4,'x')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".charAt(1)": {
      "entry": "pipeline",
      "src": "\"abc\".charAt(1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".substr(1,2)": {
      "entry": "pipeline",
      "src": "\"abc\".substr(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".substring(1,2)": {
      "entry": "pipeline",
      "src": "\"abc\".substring(1,2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".at(-1)": {
      "entry": "pipeline",
      "src": "\"abc\".at(-1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".concat([1])": {
      "entry": "pipeline",
      "src": "\"abc\".concat([1])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".flat()": {
      "entry": "pipeline",
      "src": "\"abc\".flat()",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000\"abc\".length": {
      "entry": "pipeline",
      "src": "\"abc\".length",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$match($.a > 1);": {
      "entry": "pipeline",
      "src": "$match($.a > 1);",
      "kind": "output",
      "was": "[{\"$match\":{\"a\":{\"$gt\":1}}}]",
      "now": "[{\"$match\":{\"a\":{\"$gt\":1,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.a.take(1.5)": {
      "entry": "pipeline",
      "src": "$.a.take(1.5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.drop(1.5)": {
      "entry": "pipeline",
      "src": "$.a.drop(1.5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.slice(0.5)": {
      "entry": "pipeline",
      "src": "$.a.slice(0.5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.slice(0, 2.5)": {
      "entry": "pipeline",
      "src": "$.a.slice(0, 2.5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.sampleSize(1.5)": {
      "entry": "pipeline",
      "src": "$.a.sampleSize(1.5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.takeRight(2.5)": {
      "entry": "pipeline",
      "src": "$.a.takeRight(2.5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.chunk(1.5)": {
      "entry": "pipeline",
      "src": "$.a.chunk(1.5)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$year({ date: $.d, timezone: \"UTC\" })": {
      "entry": "pipeline",
      "src": "$year({ date: $.d, timezone: \"UTC\" })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$hour({ date: $.d })": {
      "entry": "pipeline",
      "src": "$hour({ date: $.d })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.pick([\"id\",\"val\"])": {
      "entry": "pipeline",
      "src": "$.arr.pick([\"id\",\"val\"])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.arr.omit([\"type\"])": {
      "entry": "pipeline",
      "src": "$.arr.omit([\"type\"])",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.map(x => x, 1)": {
      "entry": "pipeline",
      "src": "$.a.map(x => x, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.filter(x => x, 1)": {
      "entry": "pipeline",
      "src": "$.a.filter(x => x, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a.findIndex(x => x, 1)": {
      "entry": "pipeline",
      "src": "$.a.findIndex(x => x, 1)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.a === \"string\"": {
      "entry": "pipeline",
      "src": "typeof $.a === \"string\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.a.b.c === \"number\"": {
      "entry": "pipeline",
      "src": "typeof $.a.b.c === \"number\"",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.a === 1 && $.b < 2": {
      "entry": "pipeline",
      "src": "$.a === 1 && $.b < 2",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.tags.includes('x')": {
      "entry": "pipeline",
      "src": "$.tags.includes('x')",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.s.match(/^a/)": {
      "entry": "pipeline",
      "src": "$.s.match(/^a/)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.items.some(i => i.q > 2)": {
      "entry": "pipeline",
      "src": "$.items.some(i => i.q > 2)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000typeof $.x === 'string'": {
      "entry": "pipeline",
      "src": "typeof $.x === 'string'",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x % 4 === 1": {
      "entry": "pipeline",
      "src": "$.x % 4 === 1",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x === null": {
      "entry": "pipeline",
      "src": "$.x === null",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000$.x !== undefined": {
      "entry": "pipeline",
      "src": "$.x !== undefined",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "pipeline\u0000['a','b'].includes($.s)": {
      "entry": "pipeline",
      "src": "['a','b'].includes($.s)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline is one or more statements: `;`-separated ('$match(…); $sort({…});') or a bracketed list of stages ('[$match(…), $sort({…})]'). This source is a single expression — pass it to jsmql() for a filter, or jsmql.exp",
      "reason": "message: both refuse a source that is not a pipeline; the new wording says what a pipeline IS and how to spell one, instead of naming the entry point that was called."
    },
    "expr\u0000typeof $.a === \"function\"": {
      "entry": "expr",
      "src": "typeof $.a === \"function\"",
      "kind": "now-rejected",
      "was": "{\"$eq\":[{\"$type\":\"$a\"},\"function\"]}",
      "now": "'typeof' compares against one of MongoDB's type names, and \"function\" is not one. Did you mean 'javascript'? For absence, write 'x === undefined'.",
      "reason": "intended: by the developer's ruling, `typeof` compares against MongoDB's type names and nothing else. \"function\" is not one, and the shipped compiler lowered it to a test that quietly matched nothing; it is refused now with the nearest MongoDB name."
    },
    "filter\u0000typeof $.a === \"function\"": {
      "entry": "filter",
      "src": "typeof $.a === \"function\"",
      "kind": "now-rejected",
      "was": "{\"$expr\":{\"$eq\":[{\"$type\":\"$a\"},\"function\"]}}",
      "now": "'typeof' compares against one of MongoDB's type names, and \"function\" is not one. Did you mean 'javascript'? For absence, write 'x === undefined'.",
      "reason": "intended: by the developer's ruling, `typeof` compares against MongoDB's type names and nothing else. \"function\" is not one, and the shipped compiler lowered it to a test that quietly matched nothing; it is refused now with the nearest MongoDB name."
    },
    "pipeline\u0000[$match(typeof $.fn === \"function\")]": {
      "entry": "pipeline",
      "src": "[$match(typeof $.fn === \"function\")]",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"$expr\":{\"$eq\":[{\"$type\":\"$fn\"},\"function\"]}}}]",
      "now": "'typeof' compares against one of MongoDB's type names, and \"function\" is not one. Did you mean 'javascript'? For absence, write 'x === undefined'.",
      "reason": "intended: by the developer's ruling, `typeof` compares against MongoDB's type names and nothing else. \"function\" is not one, and the shipped compiler lowered it to a test that quietly matched nothing; it is refused now with the nearest MongoDB name."
    },
    "pipeline\u0000$$ = $$.reject(d => { const t = d.total; return t > 5; });": {
      "entry": "pipeline",
      "src": "$$ = $$.reject(d => { const t = d.total; return t > 5; });",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$let\":{\"vars\":{\"t\":\"$total\"},\"in\":{\"$not\":{\"$gt\":[\"$$t\",5]}}}}}}]",
      "now": "[{\"$match\":{\"$nor\":[{\"$expr\":{\"$let\":{\"vars\":{\"t\":\"$total\"},\"in\":{\"$gt\":[\"$$t\",5]}}}}]}}]",
      "reason": "intended: `.reject` is the COMPLEMENT of the predicate's own clause — `$nor` of the JavaScript-faithful query, as `!p` is — where the shipped compiler emitted an `$expr` negation that orders across BSON types."
    },
    "pipeline\u0000$$ = $$.countBy(String)": {
      "entry": "pipeline",
      "src": "$$ = $$.countBy(String)",
      "kind": "message",
      "was": ".countBy(key) takes a field name ('.countBy(\"status\")'), a bare-path arrow ('.countBy(d => d.status)'), a computed iteratee ('.countBy(d => d.status.toLowerCase())'), or a lodash matches shorthand ('{ active: true }' / '",
      "now": "'.countBy()' takes a reshape as a one-parameter arrow here — 'd => …' — and got the name 'String'.",
      "reason": "message: both refuse an argument that is neither an arrow nor a shorthand the row accepts; the new wording says what arrived and what the method takes."
    },
    "pipeline\u0000$$ = $$.aggregate((o) => { $match(o.a === 1); $limit(3); });": {
      "entry": "pipeline",
      "src": "$$ = $$.aggregate((o) => { $match(o.a === 1); $limit(3); });",
      "kind": "output",
      "was": "[{\"$match\":{\"a\":1}},{\"$limit\":3}]",
      "now": "[{\"$match\":{\"a\":{\"$eq\":1,\"$not\":{\"$type\":\"array\"}}}},{\"$limit\":3}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.reject({ archived: true });": {
      "entry": "pipeline",
      "src": "$$ = $$.reject({ archived: true });",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$not\":{\"$eq\":[\"$archived\",true]}}}}]",
      "now": "[{\"$match\":{\"$nor\":[{\"archived\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.filter(d => d.x === 1)": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(d => d.x === 1)",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":1}}]",
      "now": "[{\"$match\":{\"x\":{\"$eq\":1,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$.filter(d => d.a).$out('archive').$limit(1);": {
      "entry": "pipeline",
      "src": "$$.filter(d => d.a).$out('archive').$limit(1);",
      "kind": "message",
      "was": "'$out' must be the last stage in a pipeline — nothing can run after it. Move it to the end of the pipeline, or remove the stage(s) that follow it.",
      "now": "Nothing can follow '$out': it writes the pipeline's output and the server requires it last. Move this statement above it.",
      "reason": "intended: a link after the terminal stage is refused as a statement after it is — measured, the server refuses both."
    },
    "pipeline\u0000$$.filter(d => d.a).$merge({ into: 't' }).take(1);": {
      "entry": "pipeline",
      "src": "$$.filter(d => d.a).$merge({ into: 't' }).take(1);",
      "kind": "message",
      "was": "'$merge' must be the last stage in a pipeline — nothing can run after it. Move it to the end of the pipeline, or remove the stage(s) that follow it.",
      "now": "Nothing can follow '$merge': it writes the pipeline's output and the server requires it last. Move this statement above it.",
      "reason": "intended: a link after the terminal stage is refused as a statement after it is — measured, the server refuses both."
    },
    "pipeline\u0000$$.filter(p => p.a > 1).$sort({ b: -1 }).take(2);": {
      "entry": "pipeline",
      "src": "$$.filter(p => p.a > 1).$sort({ b: -1 }).take(2);",
      "kind": "output",
      "was": "[{\"$match\":{\"a\":{\"$gt\":1}}},{\"$sort\":{\"b\":-1}},{\"$limit\":2}]",
      "now": "[{\"$match\":{\"a\":{\"$gt\":1,\"$not\":{\"$type\":\"array\"}}}},{\"$sort\":{\"b\":-1}},{\"$limit\":2}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$.$sort({ a: 1 }).toReversed();": {
      "entry": "pipeline",
      "src": "$$.$sort({ a: 1 }).toReversed();",
      "kind": "message",
      "was": "'.toReversed(...)' isn't available on a stream — it reverses the stream, and a MongoDB stream has no order to reverse (there is no stage that reverses one; '$reverseArray' is an expression, for an array inside a document",
      "now": "'.toReversed()' isn't available on '$$' — reverses the stream, and a stream has no defined order to reverse until it is sorted. Use '.sort(<key>)' with the direction you want.",
      "reason": "message: both refuse the method on the stream; the new text is the row's own."
    },
    "pipeline\u0000$$.$prject({ a: 1 });": {
      "entry": "pipeline",
      "src": "$$.$prject({ a: 1 });",
      "kind": "message",
      "was": "'$prject' is not a known aggregation stage. Did you mean '$project'?",
      "now": "'.$prject()' is not a method of the stream '$$'. Did you mean '.$project()'? A stage is a link too: '$$.$match(…)'.",
      "reason": "message: both refuse an unknown link; the new text suggests the nearest method or stage in the chain's own spelling."
    },
    "pipeline\u0000$$.$abs(1);": {
      "entry": "pipeline",
      "src": "$$.$abs(1);",
      "kind": "message",
      "was": "'$abs' is an expression operator, not an aggregation stage — only stages chain as '.$abs(...)'. To use '$abs' as an expression, write it inside a stage body (e.g. '.$set({ x: $abs(...) })').",
      "now": "'$abs' is an expression operator, not a stage. A chain link is a stage ('$$.$match(…)') or a method ('.filter(…)'); to use its value, assign it to a field: '$.<field> = $abs(…);'",
      "reason": "message: both refuse an expression operator as a chain link; the row's text now names the way out."
    },
    "pipeline\u0000$$.$limit();": {
      "entry": "pipeline",
      "src": "$$.$limit();",
      "kind": "message",
      "was": "'.$limit(<body>)' takes exactly one argument — the stage body, got 0.",
      "now": "'$limit'(body) requires exactly 1 argument, got 0",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$.$limit(5, 6);": {
      "entry": "pipeline",
      "src": "$$.$limit(5, 6);",
      "kind": "message",
      "was": "'.$limit(<body>)' takes exactly one argument — the stage body, got 2.",
      "now": "'$limit'(body) requires exactly 1 argument, got 2",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$?.$match({ a: 1 });": {
      "entry": "pipeline",
      "src": "$$?.$match({ a: 1 });",
      "kind": "message",
      "was": "Optional chaining ('?.') is not meaningful on the pipeline stage '$match' — a stage always runs. Write '.$match(...)' at position 4.",
      "now": "'$$' is the stream of documents and is never null, so '?.' has nothing to guard. Write '$$.' instead.",
      "reason": "intended: `$$?.` has nothing to guard, and the shipped compiler said so too; the new text names the plain spelling."
    },
    "pipeline\u0000$ = { k: $$.filter(d => d.a === 1).$limit(3) };": {
      "entry": "pipeline",
      "src": "$ = { k: $$.filter(d => d.a === 1).$limit(3) };",
      "kind": "now-rejected",
      "was": "[{\"$facet\":{\"k\":[{\"$match\":{\"a\":1}},{\"$limit\":3}]}}]",
      "now": "'$limit' is a pipeline stage, not an expression — MongoDB has no '$limit' expression operator, so '{ $limit: … }' in a value position is rejected by the server. Write it as a pipeline statement ('$limit(…);') or as a cha",
      "reason": "intended: a chain on the stream where a value belongs is the `$facet` road, a stated pending; the compiler says so instead of running a stream cell on a value record."
    },
    "pipeline\u0000$$.filter(o => o.x > 0);": {
      "entry": "pipeline",
      "src": "$$.filter(o => o.x > 0);",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":{\"$gt\":0}}}]",
      "now": "[{\"$match\":{\"x\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.filter(t => t.client === 156 && t.createdAt >= \"2026-01-01\");": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(t => t.client === 156 && t.createdAt >= \"2026-01-01\");",
      "kind": "output",
      "was": "[{\"$match\":{\"client\":156,\"createdAt\":{\"$gte\":\"2026-01-01\"}}}]",
      "now": "[{\"$match\":{\"client\":{\"$eq\":156,\"$not\":{\"$type\":\"array\"}},\"createdAt\":{\"$gte\":\"2026-01-01\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000[ $$ = $$.filter(t => t.x > 0) ]": {
      "entry": "pipeline",
      "src": "[ $$ = $$.filter(t => t.x > 0) ]",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":{\"$gt\":0}}}]",
      "now": "[{\"$match\":{\"x\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.map(o => ({ v: $.x }));": {
      "entry": "pipeline",
      "src": "$$ = $$.map(o => ({ v: $.x }));",
      "kind": "now-accepted",
      "was": "'$.<field>' inside '.map(o => …)' isn't supported — use the lambda parameter (e.g. 'o.x') to reference each input document. Inside this callback, the lambda parameter IS the current document.",
      "now": "[{\"$replaceWith\":{\"v\":\"$x\"}}]",
      "reason": "intended: `$.x` inside a stream callback names the ROOT document, which the callback's parameter also is — HR4 at every depth. The shipped compiler refused the spelling."
    },
    "pipeline\u0000$$ = true ? $$.filter(o => o.x) : $$.filter(o => o.y);": {
      "entry": "pipeline",
      "src": "$$ = true ? $$.filter(o => o.x) : $$.filter(o => o.y);",
      "kind": "message",
      "was": "'$$ = <ternary>' (conditional stream branching) is not a supported form — a stream has no single condition that swaps the whole stream for A or B. The RHS of '$$ = …' must be '$$.filter(<predicate>)' (narrow the current ",
      "now": "'$$ = …' replaces the stream with a chain on it: '$$ = $$.filter(d => d.x > 1).take(10);'. Write the right side as a chain that starts from '$$'.",
      "reason": "message: both refuse a right side that is not a chain on the stream; the new wording shows the chain form."
    },
    "pipeline\u0000$$ = $$.filter(t => $.x > 5);": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(t => $.x > 5);",
      "kind": "now-accepted",
      "was": "'$.<field>' inside '$$.filter(<predicate>)' on the RHS of '$$ = …' is not supported — use the lambda parameter (e.g. 't.x') to reference the current document. Inside this predicate, the lambda's parameter `t` IS the curr",
      "now": "[{\"$match\":{\"x\":{\"$gt\":5,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: `$.x` inside a stream callback names the ROOT document, which the parameter also is — HR4 at every depth. The shipped compiler refused the spelling."
    },
    "pipeline\u0000$$ = $$.filter({ a: 1 })": {
      "entry": "pipeline",
      "src": "$$ = $$.filter({ a: 1 })",
      "kind": "output",
      "was": "[{\"$match\":{\"a\":1}}]",
      "now": "[{\"$match\":{\"a\":{\"$eq\":1,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.filter({ a: 1 });": {
      "entry": "pipeline",
      "src": "$$ = $$.filter({ a: 1 });",
      "kind": "output",
      "was": "[{\"$match\":{\"a\":1}}]",
      "now": "[{\"$match\":{\"a\":{\"$eq\":1,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.a = 1, $$ = $$.filter({ b: 2 })": {
      "entry": "pipeline",
      "src": "$.a = 1, $$ = $$.filter({ b: 2 })",
      "kind": "output",
      "was": "[{\"$set\":{\"a\":1}},{\"$match\":{\"b\":2}}]",
      "now": "[{\"$set\":{\"a\":1}},{\"$match\":{\"b\":{\"$eq\":2,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.filter({ b: 2 }), $.a = 1": {
      "entry": "pipeline",
      "src": "$$ = $$.filter({ b: 2 }), $.a = 1",
      "kind": "output",
      "was": "[{\"$match\":{\"b\":2}},{\"$set\":{\"a\":1}}]",
      "now": "[{\"$match\":{\"b\":{\"$eq\":2,\"$not\":{\"$type\":\"array\"}}}},{\"$set\":{\"a\":1}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = 5": {
      "entry": "pipeline",
      "src": "$$ = 5",
      "kind": "message",
      "was": "'$$ = …' RHS must be '$$.<streamMethod>…' (narrow/transform the current stream) or '$$$.<coll>.<streamMethod>…' (switch source to another collection). Any lodash stream method may head the chain (e.g. '.filter', '.toSort",
      "now": "'$$ = …' replaces the stream with a chain on it: '$$ = $$.filter(d => d.x > 1).take(10);'. Write the right side as a chain that starts from '$$'.",
      "reason": "message: both refuse a right side that is not a chain on the stream; the new wording shows the chain form."
    },
    "pipeline\u0000$$ = 5;": {
      "entry": "pipeline",
      "src": "$$ = 5;",
      "kind": "message",
      "was": "'$$ = …' RHS must be '$$.<streamMethod>…' (narrow/transform the current stream) or '$$$.<coll>.<streamMethod>…' (switch source to another collection). Any lodash stream method may head the chain (e.g. '.filter', '.toSort",
      "now": "'$$ = …' replaces the stream with a chain on it: '$$ = $$.filter(d => d.x > 1).take(10);'. Write the right side as a chain that starts from '$$'.",
      "reason": "message: both refuse a right side that is not a chain on the stream; the new wording shows the chain form."
    },
    "pipeline\u0000$$.filter({ status: \"shipped\" });\n$ = {\n  topByScore: $$.toSorted({ score: -1 }).take(10),\n  recent:     $$.filter(o => o.createdAt >= new Date(\"2026-01-01\")),\n  byStatus:   $$.$group({ _id: $.status, n: $sum(1) }),\n};": {
      "entry": "pipeline",
      "src": "$$.filter({ status: \"shipped\" });\n$ = {\n  topByScore: $$.toSorted({ score: -1 }).take(10),\n  recent:     $$.filter(o => o.createdAt >= new Date(\"2026-01-01\")),\n  byStatus:   $$.$group({ _id: $.status, n: $sum(1) }),\n};",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"status\":\"shipped\"}},{\"$facet\":{\"topByScore\":[{\"$sort\":{\"score\":-1}},{\"$limit\":10}],\"recent\":[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"}}}],\"byStatus\":[{\"$group\":{\"_id\":\"$status\",\"n\":{\"",
      "now": "sortSpec is not a function",
      "reason": "intended: a chain on the stream where a value belongs is the `$facet` road, a stated pending; the compiler says so instead of running a stream cell on a value record."
    },
    "pipeline\u0000\n$$.filter(p => p.season === \"2026\" && p.score > 0)\n  .slice(40, 60)\n  .map(p => ({ player: p.name, score: p.score, rank: p.rank }));\n      ": {
      "entry": "pipeline",
      "src": "\n$$.filter(p => p.season === \"2026\" && p.score > 0)\n  .slice(40, 60)\n  .map(p => ({ player: p.name, score: p.score, rank: p.rank }));\n      ",
      "kind": "output",
      "was": "[{\"$match\":{\"season\":\"2026\",\"score\":{\"$gt\":0}}},{\"$skip\":40},{\"$limit\":20},{\"$replaceWith\":{\"player\":\"$name\",\"score\":\"$score\",\"rank\":\"$rank\"}}]",
      "now": "[{\"$match\":{\"season\":{\"$eq\":\"2026\",\"$not\":{\"$type\":\"array\"}},\"score\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}},{\"$skip\":40},{\"$limit\":20},{\"$replaceWith\":{\"player\":\"$name\",\"score\":\"$score\",\"rank\":\"$rank\"}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000\n$$ = $$.reduce(\n  (acc, d) => (d.active && d.contactDetails.email ? acc.concat(d.contactDetails) : acc),\n  []\n);\n$$$$.exports.email_contacts = $$;\n        ": {
      "entry": "pipeline",
      "src": "\n$$ = $$.reduce(\n  (acc, d) => (d.active && d.contactDetails.email ? acc.concat(d.contactDetails) : acc),\n  []\n);\n$$$$.exports.email_contacts = $$;\n        ",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]},{\"$ne\":[{\"$ifNull\":[\"$contactDetails.email\",null]},null]},{\"$ne\":[\"$contactD",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$ = $$.slice(0);": {
      "entry": "pipeline",
      "src": "$$ = $$.slice(0);",
      "kind": "now-rejected",
      "was": "[]",
      "now": "This program produces no stages, so it would leave the documents untouched. Write at least one statement that reads or changes them.",
      "reason": "intended: a program that emits nothing is refused as a program, whichever construct emptied it — `.drop(0)` and `.slice(0)` are identities, and an identity alone is no pipeline."
    },
    "pipeline\u0000$$ = $$.filter(o => o.tier === 'gold').slice(0, 5);": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(o => o.tier === 'gold').slice(0, 5);",
      "kind": "output",
      "was": "[{\"$match\":{\"tier\":\"gold\"}},{\"$limit\":5}]",
      "now": "[{\"$match\":{\"tier\":{\"$eq\":\"gold\",\"$not\":{\"$type\":\"array\"}}}},{\"$limit\":5}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.filter(o => o.tier === 'gold');": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(o => o.tier === 'gold');",
      "kind": "output",
      "was": "[{\"$match\":{\"tier\":\"gold\"}}]",
      "now": "[{\"$match\":{\"tier\":{\"$eq\":\"gold\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.slice();": {
      "entry": "pipeline",
      "src": "$$ = $$.slice();",
      "kind": "message",
      "was": ".slice(start[, end]) takes 1 or 2 arguments, got 0.",
      "now": "'.slice()'(start[, end]) requires 1 or 2 arguments, got 0",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.slice(0, 5, 10);": {
      "entry": "pipeline",
      "src": "$$ = $$.slice(0, 5, 10);",
      "kind": "message",
      "was": ".slice(start[, end]) takes 1 or 2 arguments, got 3.",
      "now": "'.slice()'(start[, end]) requires 1 or 2 arguments, got 3",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.slice(-1, 5);": {
      "entry": "pipeline",
      "src": "$$ = $$.slice(-1, 5);",
      "kind": "message",
      "was": ".slice(start[, end]) requires non-negative integer literals; got 'UnaryExpr'. Computed or dynamic arguments aren't supported on streams — write the literal in source.",
      "now": "'slice' argument 1 must be a number of 0 or more — got -1.",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.slice(0, -3);": {
      "entry": "pipeline",
      "src": "$$ = $$.slice(0, -3);",
      "kind": "message",
      "was": ".slice(start[, end]) requires non-negative integer literals; got 'UnaryExpr'. Computed or dynamic arguments aren't supported on streams — write the literal in source.",
      "now": "'slice' argument 2 must be a number of 0 or more — got -3.",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.slice(1.5, 5);": {
      "entry": "pipeline",
      "src": "$$ = $$.slice(1.5, 5);",
      "kind": "message",
      "was": ".slice(start[, end]) requires non-negative integer literals; got 1.5. Negative indices and fractional values aren't supported on streams.",
      "now": "'slice' expects an integer, but got a number.",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.slice(10, 5);": {
      "entry": "pipeline",
      "src": "$$ = $$.slice(10, 5);",
      "kind": "now-accepted",
      "was": ".slice(start, end) requires end >= start (got start=10, end=5).",
      "now": "[{\"$skip\":10},{\"$match\":{\"$expr\":false}}]",
      "reason": "intended: `.slice(10, 5)` is empty in JavaScript, and an always-false `$match` is that; the shipped compiler refused the spelling."
    },
    "pipeline\u0000$$ = $$.slice($.offset, 5);": {
      "entry": "pipeline",
      "src": "$$ = $$.slice($.offset, 5);",
      "kind": "message",
      "was": ".slice(start[, end]) requires non-negative integer literals; got 'FieldRef'. Computed or dynamic arguments aren't supported on streams — write the literal in source.",
      "now": "'slice' argument 1 must be a compile-time constant — the server reads it before any document; got an expression.",
      "reason": "message: both refuse a computed count; the new wording is the one every constant slot shares."
    },
    "pipeline\u0000$$ = $$.slice(...[0, 5]);": {
      "entry": "pipeline",
      "src": "$$ = $$.slice(...[0, 5]);",
      "kind": "message",
      "was": ".slice(start[, end]) does not accept spread arguments.",
      "now": "'slice' argument 1 must be a compile-time constant — the server reads it before any document; got an expression.",
      "reason": "message: both refuse a computed count; the new wording is the one every constant slot shares."
    },
    "pipeline\u0000$$ = $$.take(1.5);": {
      "entry": "pipeline",
      "src": "$$ = $$.take(1.5);",
      "kind": "message",
      "was": ".take(n) requires an integer >= 0; got 1.5.",
      "now": "'take' expects an integer, but got a number.",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.take(-1);": {
      "entry": "pipeline",
      "src": "$$ = $$.take(-1);",
      "kind": "message",
      "was": ".take(n) requires an integer literal; got 'UnaryExpr'. Computed or dynamic arguments aren't supported on streams — write the literal in source.",
      "now": "'take' argument 1 must be a number of 0 or more — got -1.",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.take(\"x\");": {
      "entry": "pipeline",
      "src": "$$ = $$.take(\"x\");",
      "kind": "message",
      "was": ".take(n) requires an integer literal; got 'StringLiteral'. Computed or dynamic arguments aren't supported on streams — write the literal in source.",
      "now": "'take' expects an integer, but got a string.",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.take($.n);": {
      "entry": "pipeline",
      "src": "$$ = $$.take($.n);",
      "kind": "message",
      "was": ".take(n) requires an integer literal; got 'FieldRef'. Computed or dynamic arguments aren't supported on streams — write the literal in source.",
      "now": "'take' argument 1 must be a compile-time constant — the server reads it before any document; got an expression.",
      "reason": "message: both refuse a computed count; the new wording is the one every constant slot shares."
    },
    "pipeline\u0000$$ = $$.take(1, 2);": {
      "entry": "pipeline",
      "src": "$$ = $$.take(1, 2);",
      "kind": "message",
      "was": ".take(n) takes exactly 1 argument, got 2.",
      "now": "'.take()'([n=1]) requires 0 or 1 arguments, got 2",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.take(...[1]);": {
      "entry": "pipeline",
      "src": "$$ = $$.take(...[1]);",
      "kind": "message",
      "was": ".take(n) does not accept a spread argument.",
      "now": "'take' argument 1 must be a compile-time constant — the server reads it before any document; got an expression.",
      "reason": "message: both refuse a computed count; the new wording is the one every constant slot shares."
    },
    "pipeline\u0000$$ = $$.drop(0);": {
      "entry": "pipeline",
      "src": "$$ = $$.drop(0);",
      "kind": "now-rejected",
      "was": "[]",
      "now": "This program produces no stages, so it would leave the documents untouched. Write at least one statement that reads or changes them.",
      "reason": "intended: a program that emits nothing is refused as a program, whichever construct emptied it — `.drop(0)` and `.slice(0)` are identities, and an identity alone is no pipeline."
    },
    "pipeline\u0000$$ = $$.tail(2);": {
      "entry": "pipeline",
      "src": "$$ = $$.tail(2);",
      "kind": "message",
      "was": ".tail() takes no arguments, got 1.",
      "now": "'.tail()'() takes no arguments, got 1",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.takeRight(3);": {
      "entry": "pipeline",
      "src": "$$ = $$.takeRight(3);",
      "kind": "message",
      "was": "'.takeRight(...)' isn't available on a stream — it counts from the END of the stream, and a MongoDB stream has no end to count back from (there is no stage that reverses one; '$reverseArray' is an expression, for an arra",
      "now": "'.takeRight()' isn't available on '$$' — counts from the END, which needs the whole stream buffered. Sort by the opposite key and use '.take(n)'.",
      "reason": "message: both refuse the method on the stream; the new text is the row's own."
    },
    "pipeline\u0000$$ = $$.toReversed();": {
      "entry": "pipeline",
      "src": "$$ = $$.toReversed();",
      "kind": "message",
      "was": "'.toReversed(...)' isn't available on a stream — it reverses the stream, and a MongoDB stream has no order to reverse (there is no stage that reverses one; '$reverseArray' is an expression, for an array inside a document",
      "now": "'.toReversed()' isn't available on '$$' — reverses the stream, and a stream has no defined order to reverse until it is sorted. Use '.sort(<key>)' with the direction you want.",
      "reason": "message: both refuse the method on the stream; the new text is the row's own."
    },
    "pipeline\u0000$$.shuffle();": {
      "entry": "pipeline",
      "src": "$$.shuffle();",
      "kind": "output",
      "was": "[{\"$addFields\":{\"__jsmql.tmp.1\":{\"$rand\":{}}}},{\"$sort\":{\"__jsmql.tmp.1\":1}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$addFields\":{\"__jsmql.tmp.0\":{\"$rand\":{}}}},{\"$sort\":{\"__jsmql.tmp.0\":1}},{\"$unset\":\"__jsmql\"}]",
      "reason": "intended: a shape difference inside a stream method's stage that the value or filter target already accounts for (the `.length` dispatch, the scratch namespace, or the array ruling); the stream cell only places the stage."
    },
    "pipeline\u0000$$ = $$.shuffle().take(3);": {
      "entry": "pipeline",
      "src": "$$ = $$.shuffle().take(3);",
      "kind": "output",
      "was": "[{\"$addFields\":{\"__jsmql.tmp.1\":{\"$rand\":{}}}},{\"$sort\":{\"__jsmql.tmp.1\":1}},{\"$limit\":3},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$addFields\":{\"__jsmql.tmp.0\":{\"$rand\":{}}}},{\"$sort\":{\"__jsmql.tmp.0\":1}},{\"$limit\":3},{\"$unset\":\"__jsmql\"}]",
      "reason": "intended: a shape difference inside a stream method's stage that the value or filter target already accounts for (the `.length` dispatch, the scratch namespace, or the array ruling); the stream cell only places the stage."
    },
    "pipeline\u0000$$ = $$.sampleSize(0);": {
      "entry": "pipeline",
      "src": "$$ = $$.sampleSize(0);",
      "kind": "message",
      "was": ".sampleSize(n) requires an integer >= 1; got 0.",
      "now": "'sampleSize' argument 1 must be a number of 1 or more — got 0.",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.filter({ tier: \"gold\" }).sample();": {
      "entry": "pipeline",
      "src": "$$ = $$.filter({ tier: \"gold\" }).sample();",
      "kind": "output",
      "was": "[{\"$match\":{\"tier\":\"gold\"}},{\"$sample\":{\"size\":1}}]",
      "now": "[{\"$match\":{\"tier\":{\"$eq\":\"gold\",\"$not\":{\"$type\":\"array\"}}}},{\"$sample\":{\"size\":1}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.sample(3);": {
      "entry": "pipeline",
      "src": "$$ = $$.sample(3);",
      "kind": "message",
      "was": ".sample() takes no arguments, got 1. For n random documents use '.sampleSize(n)'.",
      "now": "'.sample()'() takes no arguments, got 1",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.sortBy({ age: -1 });": {
      "entry": "pipeline",
      "src": "$$ = $$.sortBy({ age: -1 });",
      "kind": "message",
      "was": ".sortBy({ … }) isn't supported — an object here is a lodash matches-shorthand, not a direction. Use '.orderBy({ field: -1 })' or '.sort({ field: -1 })' for directions.",
      "now": ".sortBy({ … }) reads an object as a lodash matcher, not as directions. For directions write '.orderBy({ field: -1 })' or '.sort({ field: -1 })'.",
      "reason": "message: both refuse a malformed sort argument; the new text comes from the one sort reader."
    },
    "pipeline\u0000$$ = $$.orderBy(\"age\", \"up\");": {
      "entry": "pipeline",
      "src": "$$ = $$.orderBy(\"age\", \"up\");",
      "kind": "message",
      "was": ".orderBy(keys, orders) directions must be 1 / -1 / \"asc\" / \"desc\".",
      "now": ".orderBy(keys, orders) takes 1, -1, \"asc\" or \"desc\" for each order.",
      "reason": "message: both refuse a malformed sort argument; the new text comes from the one sort reader."
    },
    "pipeline\u0000$$ = $$.orderBy({ score: -1 }, [\"asc\"]);": {
      "entry": "pipeline",
      "src": "$$ = $$.orderBy({ score: -1 }, [\"asc\"]);",
      "kind": "message",
      "was": ".orderBy({ … }) already carries a direction per field — drop the second 'orders' argument.",
      "now": ".orderBy({ field: dir }) carries its directions inline; a second argument has nothing to say.",
      "reason": "message: both refuse a malformed sort argument; the new text comes from the one sort reader."
    },
    "pipeline\u0000$$ = $$.orderBy();": {
      "entry": "pipeline",
      "src": "$$ = $$.orderBy();",
      "kind": "message",
      "was": ".orderBy(keys[, orders] | { field: dir }) takes one or two arguments, got 0.",
      "now": "'.orderBy()'(keys[, orders] | { field: dir }) requires 1 or 2 arguments, got 0",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.sort({ a: 2 });": {
      "entry": "pipeline",
      "src": "$$ = $$.sort({ a: 2 });",
      "kind": "message",
      "was": ".sort(...) direction for 'a' must be 1 / -1 / \"asc\" / \"desc\".",
      "now": ".sort({ a: … }) takes a direction: 1, -1, \"asc\" or \"desc\".",
      "reason": "message: both refuse a malformed sort argument; the new text comes from the one sort reader."
    },
    "pipeline\u0000$$ = $$.sort(\"$x\");": {
      "entry": "pipeline",
      "src": "$$ = $$.sort(\"$x\");",
      "kind": "message",
      "was": ".sort(...) requires plain field names (no leading '$'), got \"$x\".",
      "now": ".sort() sorts by a field NAME — a plain string like \"age\", with no leading '$'.",
      "reason": "message: both refuse a malformed sort argument; the new text comes from the one sort reader."
    },
    "pipeline\u0000$$ = $$.sort();": {
      "entry": "pipeline",
      "src": "$$ = $$.sort();",
      "kind": "message",
      "was": ".sort(<sort>) needs a sort key — MongoDB streams have no natural document ordering. Pass a field name ('.sort(\"createdAt\")'), a '{ field: 1 | -1 | \"asc\" | \"desc\" }' spec, or a comparator '(a, b) => a.x - b.x'.",
      "now": "'.sort()'(\"field\" | [fields] | { field: dir } | comparator) requires exactly 1 argument, got 0",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$.reject(o => o.archived === true);": {
      "entry": "pipeline",
      "src": "$$.reject(o => o.archived === true);",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$not\":{\"$eq\":[\"$archived\",true]}}}}]",
      "now": "[{\"$match\":{\"$nor\":[{\"archived\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$.reject({ archived: true });": {
      "entry": "pipeline",
      "src": "$$.reject({ archived: true });",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":{\"$not\":{\"$eq\":[\"$archived\",true]}}}}]",
      "now": "[{\"$match\":{\"$nor\":[{\"archived\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$.filter(o => o.active === true).reject(o => o.hidden === true).take(5);": {
      "entry": "pipeline",
      "src": "$$.filter(o => o.active === true).reject(o => o.hidden === true).take(5);",
      "kind": "output",
      "was": "[{\"$match\":{\"active\":true}},{\"$match\":{\"$expr\":{\"$not\":{\"$eq\":[\"$hidden\",true]}}}},{\"$limit\":5}]",
      "now": "[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}},{\"$match\":{\"$nor\":[{\"hidden\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}]}},{\"$limit\":5}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$.reject(o => o.a, o => o.b);": {
      "entry": "pipeline",
      "src": "$$.reject(o => o.a, o => o.b);",
      "kind": "message",
      "was": "'$$.reject(<predicate>)' takes exactly one predicate argument, got 2.",
      "now": "'.reject()'(predicate) requires exactly 1 argument, got 2",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$.reject(...preds);": {
      "entry": "pipeline",
      "src": "$$.reject(...preds);",
      "kind": "message",
      "was": "'$$.reject(<predicate>)' on the RHS of '$$ = …' takes a single arrow predicate ('o => …'), a matches-object ('{ active: true }'), a field name ('\"active\"'), or a [\"field\", value] pair.",
      "now": "'.reject()' takes a predicate as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: both refuse an argument that is neither an arrow nor a shorthand the row accepts; the new wording says what arrived and what the method takes."
    },
    "pipeline\u0000$$.pick([\"_id\", \"name\"]);": {
      "entry": "pipeline",
      "src": "$$.pick([\"_id\", \"name\"]);",
      "kind": "output",
      "was": "[{\"$project\":{\"_id\":1,\"name\":1}}]",
      "now": "[{\"$project\":{\"_id\":0,\"name\":1}}]",
      "reason": "intended: a shape difference inside a stream method's stage that the value or filter target already accounts for (the `.length` dispatch, the scratch namespace, or the array ruling); the stream cell only places the stage."
    },
    "pipeline\u0000$$.pick(\"name\");": {
      "entry": "pipeline",
      "src": "$$.pick(\"name\");",
      "kind": "message",
      "was": ".pick([fields]) takes an array of field-name strings, e.g. '.pick([\"name\", \"email\"])'.",
      "now": "'pick' expects an array, but got a string.",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$.omit([1, 2]);": {
      "entry": "pipeline",
      "src": "$$.omit([1, 2]);",
      "kind": "message",
      "was": ".omit([fields]) entries must be plain field-name strings (no leading '$').",
      "now": "'omit' element 1 names a field to WRITE, and a number is not a name. Pass a plain field name, e.g. 'total'.",
      "reason": "intended: `.omit`/`.pick` list field NAMES, stated as `arrayOf: fieldName`; a number is refused with what a name is, where the shipped shape projected fields called \"1\" and \"2\"."
    },
    "pipeline\u0000$$ = $$.groupBy({ _id: \"$dept\", n: $sum(1), total: $sum(\"$amount\") });": {
      "entry": "pipeline",
      "src": "$$ = $$.groupBy({ _id: \"$dept\", n: $sum(1), total: $sum(\"$amount\") });",
      "kind": "now-rejected",
      "was": "[{\"$group\":{\"_id\":\"$dept\",\"n\":{\"$sum\":1},\"total\":{\"$sum\":\"$amount\"}}}]",
      "now": "'.groupBy()' takes a reshape as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: both refuse an argument that is neither an arrow nor a shorthand the row accepts; the new wording says what arrived and what the method takes."
    },
    "pipeline\u0000$$ = $$.groupBy({ _id: null, ids: $addToSet(\"$productId\") });": {
      "entry": "pipeline",
      "src": "$$ = $$.groupBy({ _id: null, ids: $addToSet(\"$productId\") });",
      "kind": "now-rejected",
      "was": "[{\"$group\":{\"_id\":null,\"ids\":{\"$addToSet\":\"$productId\"}}}]",
      "now": "'.groupBy()' takes a reshape as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: both refuse an argument that is neither an arrow nor a shorthand the row accepts; the new wording says what arrived and what the method takes."
    },
    "pipeline\u0000$$ = $$.groupBy({ n: $sum(1) });": {
      "entry": "pipeline",
      "src": "$$ = $$.groupBy({ n: $sum(1) });",
      "kind": "message",
      "was": ".groupBy({ … }) requires an '_id' key (the group key). Use '.groupBy(\"field\")' to group by a single field.",
      "now": "'.groupBy()' takes a reshape as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: both refuse an argument that is neither an arrow nor a shorthand the row accepts; the new wording says what arrived and what the method takes."
    },
    "pipeline\u0000$$ = $$.groupBy(5);": {
      "entry": "pipeline",
      "src": "$$ = $$.groupBy(5);",
      "kind": "message",
      "was": ".groupBy(key) takes a field name ('.groupBy(\"status\")'), a bare-path arrow ('.groupBy(d => d.status)'), a computed iteratee ('.groupBy(d => d.status.toLowerCase())') or a '$group' body ('{ _id: \"$dept\", n: $sum(1) }'), o",
      "now": "'.groupBy()' takes a reshape as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: both refuse an argument that is neither an arrow nor a shorthand the row accepts; the new wording says what arrived and what the method takes."
    },
    "pipeline\u0000$$ = $$.map(d => 5);": {
      "entry": "pipeline",
      "src": "$$ = $$.map(d => 5);",
      "kind": "message",
      "was": ".map(d => …) must return a document, but this maps each document to a number — MongoDB's '$replaceWith' requires an object root. To reshape into a new document write '.map(d => ({ … }))'; to keep a single value under a k",
      "now": "'.map(d => …)' replaces each document with what the arrow returns, so it has to return a document — a number is not one. Return '({ value: … })' to keep it under a field.",
      "reason": "intended: `.map` replaces each document with what the arrow returns, so it has to return a document — measured, the server refuses every non-document root; a body the registry can prove is not one is refused at compile time."
    },
    "pipeline\u0000$$ = $$.map(d => \"x\");": {
      "entry": "pipeline",
      "src": "$$ = $$.map(d => \"x\");",
      "kind": "message",
      "was": ".map(d => …) must return a document, but this maps each document to a string — MongoDB's '$replaceWith' requires an object root. To reshape into a new document write '.map(d => ({ … }))'; to keep a single value under a k",
      "now": "'.map(d => …)' replaces each document with what the arrow returns, so it has to return a document — a string is not one. Return '({ value: … })' to keep it under a field.",
      "reason": "intended: `.map` replaces each document with what the arrow returns, so it has to return a document — measured, the server refuses every non-document root; a body the registry can prove is not one is refused at compile time."
    },
    "pipeline\u0000$$ = $$.map(d => true);": {
      "entry": "pipeline",
      "src": "$$ = $$.map(d => true);",
      "kind": "message",
      "was": ".map(d => …) must return a document, but this maps each document to a boolean — MongoDB's '$replaceWith' requires an object root. To reshape into a new document write '.map(d => ({ … }))'; to keep a single value under a ",
      "now": "'.map(d => …)' replaces each document with what the arrow returns, so it has to return a document — a bool is not one. Return '({ value: … })' to keep it under a field.",
      "reason": "intended: `.map` replaces each document with what the arrow returns, so it has to return a document — measured, the server refuses every non-document root; a body the registry can prove is not one is refused at compile time."
    },
    "pipeline\u0000$$ = $$.map(d => [1, 2]);": {
      "entry": "pipeline",
      "src": "$$ = $$.map(d => [1, 2]);",
      "kind": "message",
      "was": ".map(d => …) must return a document, but this maps each document to an array — MongoDB's '$replaceWith' requires an object root. To reshape into a new document write '.map(d => ({ … }))'; to keep a single value under a k",
      "now": "'.map(d => …)' replaces each document with what the arrow returns, so it has to return a document — a array is not one. Return '({ value: … })' to keep it under a field.",
      "reason": "intended: `.map` replaces each document with what the arrow returns, so it has to return a document — measured, the server refuses every non-document root; a body the registry can prove is not one is refused at compile time."
    },
    "pipeline\u0000$$ = $$.filter({ status: \"CLOSED\", tier: \"gold\" });": {
      "entry": "pipeline",
      "src": "$$ = $$.filter({ status: \"CLOSED\", tier: \"gold\" });",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"CLOSED\",\"tier\":\"gold\"}}]",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"CLOSED\",\"$not\":{\"$type\":\"array\"}},\"tier\":{\"$eq\":\"gold\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.filter({ status: \"CLOSED\" }).sort({ createdAt: -1 }).take(10)": {
      "entry": "pipeline",
      "src": "$$ = $$.filter({ status: \"CLOSED\" }).sort({ createdAt: -1 }).take(10)",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"CLOSED\"}},{\"$sort\":{\"createdAt\":-1}},{\"$limit\":10}]",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"CLOSED\",\"$not\":{\"$type\":\"array\"}}}},{\"$sort\":{\"createdAt\":-1}},{\"$limit\":10}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.filter(o => o.tier === 'gold').map(d => ({ id: d._id }));": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(o => o.tier === 'gold').map(d => ({ id: d._id }));",
      "kind": "output",
      "was": "[{\"$match\":{\"tier\":\"gold\"}},{\"$replaceWith\":{\"id\":\"$_id\"}}]",
      "now": "[{\"$match\":{\"tier\":{\"$eq\":\"gold\",\"$not\":{\"$type\":\"array\"}}}},{\"$replaceWith\":{\"id\":\"$_id\"}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.map();": {
      "entry": "pipeline",
      "src": "$$ = $$.map();",
      "kind": "message",
      "was": ".map(d => <expr>) takes exactly one argument (a single-parameter arrow), got 0.",
      "now": "'.map()'(callback) requires exactly 1 argument, got 0",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.map((d, i) => ({ id: d._id, idx: i }));": {
      "entry": "pipeline",
      "src": "$$ = $$.map((d, i) => ({ id: d._id, idx: i }));",
      "kind": "message",
      "was": ".map((d, i) => …) can't use the index parameter 'i' — MongoDB streams have no per-doc index. Drop it, or keep it unused (e.g. '(d, _i, coll)') only to reach the 3rd 'collection' parameter.",
      "now": "`i` has no value inside `.map()` — a stream has no per-document index; leave the parameter unused.",
      "reason": "message: a read of the index or collection parameter is refused on both sides; the new text says what to write instead."
    },
    "pipeline\u0000$$ = $$.map((d, _i, coll) => ({ id: d._id, n: coll.length }));": {
      "entry": "pipeline",
      "src": "$$ = $$.map((d, _i, coll) => ({ id: d._id, n: coll.length }));",
      "kind": "now-rejected",
      "was": "[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$replaceWith\":{\"id\":\"$_id\",\"n\":\"$__jsmql.length\"}}]",
      "now": "`coll` has no value inside `.map()` — the collection is the stream itself; write '$$.length' for its size.",
      "reason": "message: a read of the index or collection parameter is refused on both sides; the new text says what to write instead."
    },
    "pipeline\u0000$$ = $$.map((o, _i, coll) => ({ share: o.total / coll.length }));": {
      "entry": "pipeline",
      "src": "$$ = $$.map((o, _i, coll) => ({ share: o.total / coll.length }));",
      "kind": "now-rejected",
      "was": "[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$replaceWith\":{\"share\":{\"$divide\":[\"$total\",\"$__jsmql.length\"]}}}]",
      "now": "`coll` has no value inside `.map()` — the collection is the stream itself; write '$$.length' for its size.",
      "reason": "message: a read of the index or collection parameter is refused on both sides; the new text says what to write instead."
    },
    "pipeline\u0000$$ = $$.map((o, _i, coll) => ({ first: coll[0] }));": {
      "entry": "pipeline",
      "src": "$$ = $$.map((o, _i, coll) => ({ first: coll[0] }));",
      "kind": "message",
      "was": "In '.map((o, _i, coll) => …)' over a '$$$.<coll>' stream, only 'coll.length' (the sub-stream's document count) is available — there's no materialised array to index or iterate. To work with the array itself, use the mate",
      "now": "`coll` has no value inside `.map()` — the collection is the stream itself; write '$$.length' for its size.",
      "reason": "message: a read of the index or collection parameter is refused on both sides; the new text says what to write instead."
    },
    "pipeline\u0000$$ = $$.map((o, _i, coll) => ({ all: coll }));": {
      "entry": "pipeline",
      "src": "$$ = $$.map((o, _i, coll) => ({ all: coll }));",
      "kind": "message",
      "was": "In '.map((o, _i, coll) => …)' over a '$$$.<coll>' stream, only 'coll.length' (the sub-stream's document count) is available — there's no materialised array to index or iterate. To work with the array itself, use the mate",
      "now": "`coll` has no value inside `.map()` — the collection is the stream itself; write '$$.length' for its size.",
      "reason": "message: a read of the index or collection parameter is refused on both sides; the new text says what to write instead."
    },
    "pipeline\u0000$$ = $$.map((o, i, coll) => ({ x: i, n: coll.length }));": {
      "entry": "pipeline",
      "src": "$$ = $$.map((o, i, coll) => ({ x: i, n: coll.length }));",
      "kind": "message",
      "was": ".map((o, i) => …) can't use the index parameter 'i' — MongoDB streams have no per-doc index. Drop it, or keep it unused (e.g. '(o, _i, coll)') only to reach the 3rd 'collection' parameter.",
      "now": "`i` has no value inside `.map()` — a stream has no per-document index; leave the parameter unused.",
      "reason": "message: a read of the index or collection parameter is refused on both sides; the new text says what to write instead."
    },
    "pipeline\u0000$$ = $$.map(d => ({ n: $.name }));": {
      "entry": "pipeline",
      "src": "$$ = $$.map(d => ({ n: $.name }));",
      "kind": "now-accepted",
      "was": "'$.<field>' inside '.map(d => …)' isn't supported — use the lambda parameter (e.g. 'd.name') to reference each input document. Inside this callback, the lambda parameter IS the current document.",
      "now": "[{\"$replaceWith\":{\"n\":\"$name\"}}]",
      "reason": "intended: `$.x` inside a stream callback names the ROOT document, which the parameter also is — HR4 at every depth. The shipped compiler refused the spelling."
    },
    "pipeline\u0000$$ = $$.map(d => ({ a: $$$.archive.find(x => x._id === d._id) }));": {
      "entry": "pipeline",
      "src": "$$ = $$.map(d => ({ a: $$$.archive.find(x => x._id === d._id) }));",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"archive\",\"localField\":\"_id\",\"foreignField\":\"_id\",\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$replaceWith\":{\"a\":\"$__jsmql.tmp.1\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"archive\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_f0__id\"]}}},{\"$limit\":1}],\"as\":\"__jsmql.tmp.0\"}},{\"$set\":{\"__jsmql.tmp.0\":{\"$first\":\"$__jsmql.tmp.0\"}}},{",
      "reason": "message: a read from another collection inside a stream callback is the join road, not built yet; both compilers refuse the spelling here, with different words."
    },
    "pipeline\u0000$$ = $$.filter(o => o.active === true).map(d => ({ id: d._id, archived: $$$.archive.find(x => x._id === d._id) }));": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(o => o.active === true).map(d => ({ id: d._id, archived: $$$.archive.find(x => x._id === d._id) }));",
      "kind": "output",
      "was": "[{\"$match\":{\"active\":true}},{\"$lookup\":{\"from\":\"archive\",\"localField\":\"_id\",\"foreignField\":\"_id\",\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$replaceWith\":{\"id\":\"$_id\",\"archived\":\"$__j",
      "now": "[{\"$lookup\":{\"from\":\"archive\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_f0__id\"]}}},{\"$limit\":1}],\"as\":\"__jsmql.tmp.0\"}},{\"$set\":{\"__jsmql.tmp.0\":{\"$first\":\"$__jsmql.tmp.0\"}}},{",
      "reason": "message: a read from another collection inside a stream callback is the join road, not built yet; both compilers refuse the spelling here, with different words."
    },
    "pipeline\u0000$$ = $$.map(d => ({ id: d._id, items: $$$.archive.filter(x => x.userId === d._id) }));": {
      "entry": "pipeline",
      "src": "$$ = $$.map(d => ({ id: d._id, items: $$$.archive.filter(x => x.userId === d._id) }));",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"archive\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"__jsmql.tmp.1\"}},{\"$replaceWith\":{\"id\":\"$_id\",\"items\":\"$__jsmql.tmp.1\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"archive\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$replaceWith\":{\"id\":\"$_id\",\"items\":\"$__jsmql.tmp.0\"}}]",
      "reason": "message: a read from another collection inside a stream callback is the join road, not built yet; both compilers refuse the spelling here, with different words."
    },
    "pipeline\u0000$$ = $$.filter(o => o.active === true).toSorted((a, b) => a.age - b.age);": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(o => o.active === true).toSorted((a, b) => a.age - b.age);",
      "kind": "output",
      "was": "[{\"$match\":{\"active\":true}},{\"$sort\":{\"age\":1}}]",
      "now": "[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}},{\"$sort\":{\"age\":1}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.toSorted();": {
      "entry": "pipeline",
      "src": "$$ = $$.toSorted();",
      "kind": "message",
      "was": ".toSorted(<sort>) needs a sort key — MongoDB streams have no natural document ordering. Pass a field name ('.toSorted(\"createdAt\")'), a '{ field: 1 | -1 | \"asc\" | \"desc\" }' spec, or a comparator '(a, b) => a.x - b.x'.",
      "now": "'.toSorted()'(\"field\" | [fields] | { field: dir } | comparator) requires exactly 1 argument, got 0",
      "reason": "message: both refuse the argument count or a literal of the wrong type or range; the new wording is the one every slot check shares."
    },
    "pipeline\u0000$$ = $$.toSorted(x => x.age);": {
      "entry": "pipeline",
      "src": "$$ = $$.toSorted(x => x.age);",
      "kind": "now-accepted",
      "was": ".toSorted((a, b) => …) comparator requires a two-parameter arrow (got 1 params).",
      "now": "[{\"$sort\":{\"age\":1}}]",
      "reason": "intended: a key function, a block body with a binding, and lodash's unused index and collection parameters are all accepted now; the shipped compiler refused each, and the reader or the binding says what to write if one is READ."
    },
    "pipeline\u0000$$ = $$.toSorted((a, b) => a.x + b.x);": {
      "entry": "pipeline",
      "src": "$$ = $$.toSorted((a, b) => a.x + b.x);",
      "kind": "message",
      "was": ".toSorted((a, b) => …) accepts only 'a.<field> - b.<field>' (ascending) or 'b.<field> - a.<field>' (descending) terms, combined with '||' for compound sorts. Other comparator shapes aren't supported on streams.",
      "now": ".toSorted((a, b) => …) compares one field of each: 'a.age - b.age', or 'b.age - a.age' for descending. Join keys with '||'.",
      "reason": "message: both refuse a malformed sort argument; the new text comes from the one sort reader."
    },
    "pipeline\u0000$$ = $$.toSorted((a, b) => a.x - b.y);": {
      "entry": "pipeline",
      "src": "$$ = $$.toSorted((a, b) => a.x - b.y);",
      "kind": "message",
      "was": ".toSorted((a, b) => …) accepts only 'a.<field> - b.<field>' (ascending) or 'b.<field> - a.<field>' (descending) terms, combined with '||' for compound sorts. Other comparator shapes aren't supported on streams.",
      "now": ".toSorted((a, b) => …) subtracts the SAME field of both parameters: 'a.age - b.age'.",
      "reason": "message: both refuse a malformed sort argument; the new text comes from the one sort reader."
    },
    "pipeline\u0000$$ = $$.filter(o => o.active === true).flatMap(d => d.items).map(d => ({ item: d.items }));": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(o => o.active === true).flatMap(d => d.items).map(d => ({ item: d.items }));",
      "kind": "output",
      "was": "[{\"$match\":{\"active\":true}},{\"$unwind\":\"$items\"},{\"$replaceWith\":{\"item\":\"$items\"}}]",
      "now": "[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}},{\"$unwind\":\"$items\"},{\"$replaceWith\":{\"item\":\"$items\"}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.flatMap(d => 5);": {
      "entry": "pipeline",
      "src": "$$ = $$.flatMap(d => 5);",
      "kind": "message",
      "was": ".flatMap(d => …) needs a field path — it lowers to '$unwind', which returns each element to a NAMED field, so a computed body (e.g. '.flatMap(d => d.items.map(...))') has nothing to unwind into. Build the array into a fi",
      "now": "'.flatMap(d => …)' names the ARRAY FIELD to flatten: 'd => d.items'. It lowers to '$unwind', which takes a field path and nothing else.",
      "reason": "intended: `.flatMap` names the array field to unwind; a body that is not a field of the document is refused, where the shipped compiler said the same in other words."
    },
    "pipeline\u0000$$ = $$.flatMap((d, i) => d.items);": {
      "entry": "pipeline",
      "src": "$$ = $$.flatMap((d, i) => d.items);",
      "kind": "now-accepted",
      "was": ".flatMap(d => d.<path>) requires a single-parameter arrow (got 2 params).",
      "now": "[{\"$unwind\":\"$items\"}]",
      "reason": "intended: a key function, a block body with a binding, and lodash's unused index and collection parameters are all accepted now; the shipped compiler refused each, and the reader or the binding says what to write if one is READ."
    },
    "pipeline\u0000$$ = $$.reduce((acc, d) => (d.active && d.contactDetails.email ? acc.concat(d.contactDetails) : acc), []);": {
      "entry": "pipeline",
      "src": "$$ = $$.reduce((acc, d) => (d.active && d.contactDetails.email ? acc.concat(d.contactDetails) : acc), []);",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]},{\"$ne\":[{\"$ifNull\":[\"$contactDetails.email\",null]},null]},{\"$ne\":[\"$contactD",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$ = $$.reduce((acc, d) => (d.tier === 'gold' ? acc.concat(d.profile) : acc), []);": {
      "entry": "pipeline",
      "src": "$$ = $$.reduce((acc, d) => (d.tier === 'gold' ? acc.concat(d.profile) : acc), []);",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"tier\":\"gold\"}},{\"$replaceWith\":\"$profile\"}]",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$ = $$.reduce((acc, d) => acc.concat(d.contactDetails), []);": {
      "entry": "pipeline",
      "src": "$$ = $$.reduce((acc, d) => acc.concat(d.contactDetails), []);",
      "kind": "now-rejected",
      "was": "[{\"$replaceWith\":\"$contactDetails\"}]",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$ = $$.reduce((acc, d) => (d.active === true ? acc.concat(d) : acc), []);": {
      "entry": "pipeline",
      "src": "$$ = $$.reduce((acc, d) => (d.active === true ? acc.concat(d) : acc), []);",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"active\":true}}]",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$ = $$.reduce((acc, d) => acc.concat(d), []);": {
      "entry": "pipeline",
      "src": "$$ = $$.reduce((acc, d) => acc.concat(d), []);",
      "kind": "now-rejected",
      "was": "[]",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$ = $$.reduce((acc, d) => acc.concat(d.contactDetails), [1, 2]);": {
      "entry": "pipeline",
      "src": "$$ = $$.reduce((acc, d) => acc.concat(d.contactDetails), [1, 2]);",
      "kind": "message",
      "was": "'$$ = $$.reduce(<reducer>, <init>)' with an array-returning reducer requires the init to be '[]' — a non-empty seed array isn't supported (no MQL accumulator preserves the JS-faithful \"start with these elements\" semantic",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$ = $$.reduce((acc, d) => (d.active ? acc.concat(d.contactDetails) : []), []);": {
      "entry": "pipeline",
      "src": "$$ = $$.reduce((acc, d) => (d.active ? acc.concat(d.contactDetails) : []), []);",
      "kind": "message",
      "was": "Array-returning reducer body — supported shapes: • Unconditional map: '(acc, d) => acc.concat(d.<field>)' → '$replaceWith: \"$<field>\"' • Filter + map: '(acc, d) => (<cond> ? acc.concat(d.<field>) : acc)' → '$match(<cond>",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$ = $$.reduce((acc, d) => acc.push(d.contactDetails), []);": {
      "entry": "pipeline",
      "src": "$$ = $$.reduce((acc, d) => acc.push(d.contactDetails), []);",
      "kind": "message",
      "was": "Array-returning reducer body — supported shapes: • Unconditional map: '(acc, d) => acc.concat(d.<field>)' → '$replaceWith: \"$<field>\"' • Filter + map: '(acc, d) => (<cond> ? acc.concat(d.<field>) : acc)' → '$match(<cond>",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$ = $$.reduce((acc, d) => acc.concat([d.x, d.y]), []);": {
      "entry": "pipeline",
      "src": "$$ = $$.reduce((acc, d) => acc.concat([d.x, d.y]), []);",
      "kind": "message",
      "was": "Array-returning reducer body — supported shapes: • Unconditional map: '(acc, d) => acc.concat(d.<field>)' → '$replaceWith: \"$<field>\"' • Filter + map: '(acc, d) => (<cond> ? acc.concat(d.<field>) : acc)' → '$match(<cond>",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$ = $$.reduce((acc, d) => ($.active ? acc.concat(d.contactDetails) : acc), []);": {
      "entry": "pipeline",
      "src": "$$ = $$.reduce((acc, d) => ($.active ? acc.concat(d.contactDetails) : acc), []);",
      "kind": "message",
      "was": "'$.<field>' inside '$$.filter(<predicate>)' on the RHS of '$$ = …' is not supported — use the lambda parameter (e.g. 'd.active') to reference the current document. Inside this predicate, the lambda's parameter `d` IS the",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$ = $$.reduce((acc, d) => acc + d.amount, 0);": {
      "entry": "pipeline",
      "src": "$$ = $$.reduce((acc, d) => acc + d.amount, 0);",
      "kind": "message",
      "was": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Wrap the reduce result into a stream-shaped RHS: • Scalar reducer: '$$ = [{ <",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$ = $$.filter(o => o.tier === 'gold').reduce((acc, d) => acc + d.amount, 0);": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(o => o.tier === 'gold').reduce((acc, d) => acc + d.amount, 0);",
      "kind": "message",
      "was": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Wrap the reduce result into a stream-shaped RHS: • Scalar reducer: '$$ = [{ <",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: both refuse `.reduce` on the stream and point at the wrap form; the new text is the row's own, with the receiver spelled '$$'."
    },
    "pipeline\u0000$$.filter(o => o.tier === 'gold');": {
      "entry": "pipeline",
      "src": "$$.filter(o => o.tier === 'gold');",
      "kind": "output",
      "was": "[{\"$match\":{\"tier\":\"gold\"}}]",
      "now": "[{\"$match\":{\"tier\":{\"$eq\":\"gold\",\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$.filter(o => o.tier === 'gold').map(d => ({ id: d._id }));": {
      "entry": "pipeline",
      "src": "$$.filter(o => o.tier === 'gold').map(d => ({ id: d._id }));",
      "kind": "output",
      "was": "[{\"$match\":{\"tier\":\"gold\"}},{\"$replaceWith\":{\"id\":\"$_id\"}}]",
      "now": "[{\"$match\":{\"tier\":{\"$eq\":\"gold\",\"$not\":{\"$type\":\"array\"}}}},{\"$replaceWith\":{\"id\":\"$_id\"}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$.filter(o => o.tier === 'gold'); $$.map(d => ({ id: d._id }));": {
      "entry": "pipeline",
      "src": "$$.filter(o => o.tier === 'gold'); $$.map(d => ({ id: d._id }));",
      "kind": "output",
      "was": "[{\"$match\":{\"tier\":\"gold\"}},{\"$replaceWith\":{\"id\":\"$_id\"}}]",
      "now": "[{\"$match\":{\"tier\":{\"$eq\":\"gold\",\"$not\":{\"$type\":\"array\"}}}},{\"$replaceWith\":{\"id\":\"$_id\"}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.map(d => { return { n: $.name }; });": {
      "entry": "pipeline",
      "src": "$$ = $$.map(d => { return { n: $.name }; });",
      "kind": "now-accepted",
      "was": "'$.<field>' inside '.map(d => …)' isn't supported — use the lambda parameter (e.g. 'd.name') to reference each input document. Inside this callback, the lambda parameter IS the current document.",
      "now": "[{\"$replaceWith\":{\"n\":\"$name\"}}]",
      "reason": "intended: `$.x` inside a stream callback names the ROOT document, which the parameter also is — HR4 at every depth. The shipped compiler refused the spelling."
    },
    "pipeline\u0000$$ = $$.groupBy({ _id: \"$dept\", n: $sum(1) });": {
      "entry": "pipeline",
      "src": "$$ = $$.groupBy({ _id: \"$dept\", n: $sum(1) });",
      "kind": "now-rejected",
      "was": "[{\"$group\":{\"_id\":\"$dept\",\"n\":{\"$sum\":1}}}]",
      "now": "'.groupBy()' takes a reshape as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: both refuse an argument that is neither an arrow nor a shorthand the row accepts; the new wording says what arrived and what the method takes."
    },
    "pipeline\u0000$$ = $$.groupBy({ _id: \"$cat\" });": {
      "entry": "pipeline",
      "src": "$$ = $$.groupBy({ _id: \"$cat\" });",
      "kind": "now-rejected",
      "was": "[{\"$group\":{\"_id\":\"$cat\"}}]",
      "now": "'.groupBy()' takes a reshape as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: both refuse an argument that is neither an arrow nor a shorthand the row accepts; the new wording says what arrived and what the method takes."
    },
    "pipeline\u0000$$ = $$.countBy(d => $.other);": {
      "entry": "pipeline",
      "src": "$$ = $$.countBy(d => $.other);",
      "kind": "now-accepted",
      "was": "'$.<field>' inside '.countBy(d => …)' isn't supported — use the lambda parameter (e.g. 'd.other') to reference each input document. Inside this callback, the lambda parameter IS the current document.",
      "now": "[{\"$group\":{\"_id\":\"$other\",\"__jsmqlTmp\":{\"$sum\":1}}},{\"$group\":{\"_id\":null,\"__jsmqlTmp\":{\"$push\":{\"k\":{\"$ifNull\":[{\"$toString\":\"$_id\"},\"null\"]},\"v\":\"$__jsmqlTmp\"}}}},{\"$replaceWith\":{\"$arrayToObject\":\"$__jsmqlTmp\"}}]",
      "reason": "intended: `$.x` inside a stream callback names the ROOT document, which the parameter also is — HR4 at every depth. The shipped compiler refused the spelling."
    },
    "pipeline\u0000$$ = $$.uniqBy(function (d) { const y = d.x; return y; });": {
      "entry": "pipeline",
      "src": "$$ = $$.uniqBy(function (d) { const y = d.x; return y; });",
      "kind": "now-accepted",
      "was": ".uniqBy(d => { … }) with 'let'/'const' bindings isn't supported — use a single 'return <expr>' (e.g. '.uniqBy(d => d.field)'), or hoist the bindings to a top-level 'let' before the chain.",
      "now": "[{\"$group\":{\"_id\":{\"$let\":{\"vars\":{\"y\":\"$x\"},\"in\":\"$$y\"}},\"__jsmqlTmp\":{\"$first\":\"$$ROOT\"}}},{\"$replaceWith\":\"$__jsmqlTmp\"}]",
      "reason": "intended: a key function, a block body with a binding, and lodash's unused index and collection parameters are all accepted now; the shipped compiler refused each, and the reader or the binding says what to write if one is READ."
    },
    "pipeline\u0000$$ = $$.countBy((d, i) => d.x);": {
      "entry": "pipeline",
      "src": "$$ = $$.countBy((d, i) => d.x);",
      "kind": "now-accepted",
      "was": ".countBy(key) takes a single-parameter iteratee '(d) => <key expr>', got 2 parameters.",
      "now": "[{\"$group\":{\"_id\":\"$x\",\"__jsmqlTmp\":{\"$sum\":1}}},{\"$group\":{\"_id\":null,\"__jsmqlTmp\":{\"$push\":{\"k\":{\"$ifNull\":[{\"$toString\":\"$_id\"},\"null\"]},\"v\":\"$__jsmqlTmp\"}}}},{\"$replaceWith\":{\"$arrayToObject\":\"$__jsmqlTmp\"}}]",
      "reason": "intended: a key function, a block body with a binding, and lodash's unused index and collection parameters are all accepted now; the shipped compiler refused each, and the reader or the binding says what to write if one is READ."
    },
    "pipeline\u0000$$ = $$.filter(t => t.a === 1)": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(t => t.a === 1)",
      "kind": "output",
      "was": "[{\"$match\":{\"a\":1}}]",
      "now": "[{\"$match\":{\"a\":{\"$eq\":1,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$.indexStat()": {
      "entry": "pipeline",
      "src": "$$.indexStat()",
      "kind": "message",
      "was": "'$$.indexStat(...)' is not a known diagnostic stage. '$$' (collection reference) supports the collection-scoped system stages: .collStats(), .indexStats(), .listSearchIndexes(), .planCacheStats(). Did you mean '$$.indexS",
      "now": "'.indexStat()' is not a method of the stream '$$'. Did you mean '.$indexStats()'? A stage is a link too: '$$.$match(…)'.",
      "reason": "message: both refuse an unknown link; the new text suggests the nearest method or stage in the chain's own spelling."
    },
    "pipeline\u0000$$ = $$.push({ a: 1 });": {
      "entry": "pipeline",
      "src": "$$ = $$.push({ a: 1 });",
      "kind": "message",
      "was": "'.push(...)' isn't a chain method. To append documents mid-chain use '.concat(...)', which emits the same '$unionWith' — e.g. '$$.filter(<pred>).concat({ … })'. ('$$.push(...)' does work as a top-level statement of its o",
      "now": "'.push()' isn't available on '$$' — appends by mutating. Use '.concat(...)' mid-chain, which emits the same '$unionWith'.",
      "reason": "message: both refuse the method on the stream; the new text is the row's own."
    },
    "pipeline\u0000$$ = $$.filter(d => d.a > 1);": {
      "entry": "pipeline",
      "src": "$$ = $$.filter(d => d.a > 1);",
      "kind": "output",
      "was": "[{\"$match\":{\"a\":{\"$gt\":1}}}]",
      "now": "[{\"$match\":{\"a\":{\"$gt\":1,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$.map(d => \"$sub\");": {
      "entry": "pipeline",
      "src": "$$ = $$.map(d => \"$sub\");",
      "kind": "now-rejected",
      "was": "[{\"$replaceWith\":\"$sub\"}]",
      "now": "'.map(d => …)' replaces each document with what the arrow returns, so it has to return a document — a string is not one. Return '({ value: … })' to keep it under a field.",
      "reason": "message: both refuse; the new wording says what arrived and what the method takes."
    },
    "pipeline\u0000let date = new Date(\"2020-01-01\"); $.createdAt < date": {
      "entry": "pipeline",
      "src": "let date = new Date(\"2020-01-01\"); $.createdAt < date",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: same refusal, reworded — a pipeline statement writes something; this expression only computes a value."
    },
    "pipeline\u0000const x = 5; const t = new Date(); $match($.a === x && $.b >= t)": {
      "entry": "pipeline",
      "src": "const x = 5; const t = new Date(); $match($.a === x && $.b >= t)",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.t\":{\"$toDate\":\"$$NOW\"}}},{\"$match\":{\"a\":5,\"$expr\":{\"$gte\":[\"$b\",\"$__jsmql.var.t\"]}}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$set\":{\"__jsmql.var.t\":{\"$toDate\":\"$$NOW\"}}},{\"$match\":{\"a\":{\"$eq\":5,\"$not\":{\"$type\":\"array\"}},\"$expr\":{\"$gte\":[\"$b\",\"$__jsmql.var.t\"]}}},{\"$unset\":\"__jsmql\"}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000const d = new Date(\"nope\"); $.t < d": {
      "entry": "pipeline",
      "src": "const d = new Date(\"nope\"); $.t < d",
      "kind": "message",
      "was": "new Date(\"nope\") — \"nope\" is not a valid date string. Use an ISO 8601 date like \"2026-01-01\" or \"2026-01-01T00:00:00.000Z\".",
      "now": "new Date(<constant>) — only an ISO 8601 string or a millisecond count is a date constant, and this one is neither a valid date string nor a number. Write new Date(\"2026-01-01\") or new Date(0).",
      "reason": "message: same refusal, reworded."
    },
    "pipeline\u0000let big = $.x > 100; $match(big)": {
      "entry": "pipeline",
      "src": "let big = $.x > 100; $match(big)",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.big\":{\"$gt\":[\"$x\",100]}}},{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$__jsmql.var.big\",null]},null]},{\"$ne\":[\"$__jsmql.var.big\",false]},{\"$ne\":[\"$__jsmql.var.big\",\"\"]},{\"$ne\":[\"$__jsmql.var.b",
      "now": "[{\"$set\":{\"__jsmql.var.big\":{\"$gt\":[\"$x\",100]}}},{\"$match\":{\"$expr\":\"$__jsmql.var.big\"}},{\"$unset\":\"__jsmql\"}]",
      "reason": "intended: `big` holds a comparison, a provable boolean, and the truth of a boolean is the value itself; the four-clause truthiness test is owed only to a value of unknown type."
    },
    "pipeline\u0000[$match($.x > 0), let y = $.x * 2, $.flag = true, $sort({ y: 1 })]": {
      "entry": "pipeline",
      "src": "[$match($.x > 0), let y = $.x * 2, $.flag = true, $sort({ y: 1 })]",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":{\"$gt\":0}}},{\"$set\":{\"__jsmql.var.y\":{\"$multiply\":[\"$x\",2]}}},{\"$set\":{\"flag\":true}},{\"$sort\":{\"y\":1}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$match\":{\"x\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}},{\"$set\":{\"__jsmql.var.y\":{\"$multiply\":[\"$x\",2]}}},{\"$set\":{\"flag\":true}},{\"$sort\":{\"y\":1}},{\"$unset\":\"__jsmql\"}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000let total = $.price * $.qty; $group({ _id: $.cat }); $match(total > 0)": {
      "entry": "pipeline",
      "src": "let total = $.price * $.qty; $group({ _id: $.cat }); $match(total > 0)",
      "kind": "message",
      "was": "`total` is a `let` binding and can't be read after `$group` — the stage replaces the document. Inline the expression into the `$group` body, or rebind after the stage with another `let`.",
      "now": "`total` is a `let` binding and can't be read after `$group` — that stage replaced the document that carried it. Assign it again after the stage (`total = …`), or carry the value as a field of the new document.",
      "reason": "message: same refusal, reworded — the way back is `x = …` (a `let` a document-replacing stage dropped is carried again by an assignment) or a field of the new document; the shipped hint, `rebind with let`, is a second declaration in one block, which JavaScript refuses."
    },
    "pipeline\u0000let total = $.x; $replaceRoot({ newRoot: $.sub }); $match(total > 0)": {
      "entry": "pipeline",
      "src": "let total = $.x; $replaceRoot({ newRoot: $.sub }); $match(total > 0)",
      "kind": "message",
      "was": "`total` is a `let` binding and can't be read after `$replaceRoot` — the stage replaces the document. Inline the expression into the `$replaceRoot` body, or rebind after the stage with another `let`.",
      "now": "`total` is a `let` binding and can't be read after `$replaceRoot` — that stage replaced the document that carried it. Assign it again after the stage (`total = …`), or carry the value as a field of the new document.",
      "reason": "message: same refusal, reworded — the way back is `x = …` (a `let` a document-replacing stage dropped is carried again by an assignment) or a field of the new document; the shipped hint, `rebind with let`, is a second declaration in one block, which JavaScript refuses."
    },
    "pipeline\u0000let total = $.x; $replaceWith($.sub); $match(total > 0)": {
      "entry": "pipeline",
      "src": "let total = $.x; $replaceWith($.sub); $match(total > 0)",
      "kind": "message",
      "was": "`total` is a `let` binding and can't be read after `$replaceWith` — the stage replaces the document. Inline the expression into the `$replaceWith` body, or rebind after the stage with another `let`.",
      "now": "`total` is a `let` binding and can't be read after `$replaceWith` — that stage replaced the document that carried it. Assign it again after the stage (`total = …`), or carry the value as a field of the new document.",
      "reason": "message: same refusal, reworded — the way back is `x = …` (a `let` a document-replacing stage dropped is carried again by an assignment) or a field of the new document; the shipped hint, `rebind with let`, is a second declaration in one block, which JavaScript refuses."
    },
    "pipeline\u0000let v = $.x; $bucket({ groupBy: $.y, boundaries: [0, 10, 20] }); $match(v > 0)": {
      "entry": "pipeline",
      "src": "let v = $.x; $bucket({ groupBy: $.y, boundaries: [0, 10, 20] }); $match(v > 0)",
      "kind": "message",
      "was": "`v` is a `let` binding and can't be read after `$bucket` — the stage replaces the document. Inline the expression into the `$bucket` body, or rebind after the stage with another `let`.",
      "now": "`v` is a `let` binding and can't be read after `$bucket` — that stage replaced the document that carried it. Assign it again after the stage (`v = …`), or carry the value as a field of the new document.",
      "reason": "message: same refusal, reworded — the way back is `x = …` (a `let` a document-replacing stage dropped is carried again by an assignment) or a field of the new document; the shipped hint, `rebind with let`, is a second declaration in one block, which JavaScript refuses."
    },
    "pipeline\u0000let x = 1; let x = 2; $project({ x })": {
      "entry": "pipeline",
      "src": "let x = 1; let x = 2; $project({ x })",
      "kind": "message",
      "was": "`let x` is already declared earlier in this pipeline. Re-declaration in the same scope is not allowed — reassign it (`x = …`), pick a different name, or rebind after a reshape stage (`$group`, `$replaceRoot`, …).",
      "now": "`let x` is already declared earlier in this block — re-declaration in the same scope is not allowed; pick a different name.",
      "reason": "message: same refusal, reworded; the shipped hint to rebind after a reshape stage with another `let` is a second declaration in one block, which JavaScript refuses."
    },
    "pipeline\u0000let total = $.price; $group({ _id: $.cat }); $match(total > 0)": {
      "entry": "pipeline",
      "src": "let total = $.price; $group({ _id: $.cat }); $match(total > 0)",
      "kind": "message",
      "was": "`total` is a `let` binding and can't be read after `$group` — the stage replaces the document. Inline the expression into the `$group` body, or rebind after the stage with another `let`.",
      "now": "`total` is a `let` binding and can't be read after `$group` — that stage replaced the document that carried it. Assign it again after the stage (`total = …`), or carry the value as a field of the new document.",
      "reason": "message: same refusal, reworded — the way back is `x = …` (a `let` a document-replacing stage dropped is carried again by an assignment) or a field of the new document; the shipped hint, `rebind with let`, is a second declaration in one block, which JavaScript refuses."
    },
    "pipeline\u0000let v = $.x; $sort({ x: 1 }); let v = $.y;": {
      "entry": "pipeline",
      "src": "let v = $.x; $sort({ x: 1 }); let v = $.y;",
      "kind": "message",
      "was": "`let v` is already declared earlier in this pipeline. Re-declaration in the same scope is not allowed — reassign it (`v = …`), pick a different name, or rebind after a reshape stage (`$group`, `$replaceRoot`, …).",
      "now": "`let v` is already declared earlier in this block — re-declaration in the same scope is not allowed; pick a different name.",
      "reason": "message: same refusal, reworded; the shipped hint to rebind after a reshape stage with another `let` is a second declaration in one block, which JavaScript refuses."
    },
    "pipeline\u0000$match($.x > 0); let s = $.a + $.b; $sort({ x: 1 }); $project({ s })": {
      "entry": "pipeline",
      "src": "$match($.x > 0); let s = $.a + $.b; $sort({ x: 1 }); $project({ s })",
      "kind": "output",
      "was": "[{\"$match\":{\"x\":{\"$gt\":0}}},{\"$set\":{\"__jsmql.var.s\":{\"$add\":[\"$a\",\"$b\"]}}},{\"$sort\":{\"x\":1}},{\"$project\":{\"s\":\"$__jsmql.var.s\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$match\":{\"x\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}},{\"$set\":{\"__jsmql.var.s\":{\"$add\":[\"$a\",\"$b\"]}}},{\"$sort\":{\"x\":1}},{\"$project\":{\"s\":\"$__jsmql.var.s\"}},{\"$unset\":\"__jsmql\"}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000let v = $.x; $bucketAuto({ groupBy: $.y, buckets: 4 }); $match(v > 0)": {
      "entry": "pipeline",
      "src": "let v = $.x; $bucketAuto({ groupBy: $.y, buckets: 4 }); $match(v > 0)",
      "kind": "message",
      "was": "`v` is a `let` binding and can't be read after `$bucketAuto` — the stage replaces the document. Inline the expression into the `$bucketAuto` body, or rebind after the stage with another `let`.",
      "now": "`v` is a `let` binding and can't be read after `$bucketAuto` — that stage replaced the document that carried it. Assign it again after the stage (`v = …`), or carry the value as a field of the new document.",
      "reason": "message: same refusal, reworded — the way back is `x = …` (a `let` a document-replacing stage dropped is carried again by an assignment) or a field of the new document; the shipped hint, `rebind with let`, is a second declaration in one block, which JavaScript refuses."
    },
    "pipeline\u0000let x = $.a; $project({ x: 1 }); $match(x > 0)": {
      "entry": "pipeline",
      "src": "let x = $.a; $project({ x: 1 }); $match(x > 0)",
      "kind": "now-rejected",
      "was": "[{\"$set\":{\"__jsmql.var.x\":\"$a\"}},{\"$project\":{\"x\":1}},{\"$match\":{\"$expr\":{\"$gt\":[\"$__jsmql.var.x\",0]}}},{\"$unset\":\"__jsmql\"}]",
      "now": "`x` is a `let` binding and can't be read after `$project` — that stage replaced the document that carried it. Assign it again after the stage (`x = …`), or carry the value as a field of the new document.",
      "reason": "shippedBug: an inclusion `$project` drops `__jsmql.var.x` (measured), and the shipped compiler emitted a read of a field that was no longer there."
    },
    "pipeline\u0000const x = $.foo; x = 5; $project({ x });": {
      "entry": "pipeline",
      "src": "const x = $.foo; x = 5; $project({ x });",
      "kind": "message",
      "was": "Cannot reassign `x` — it is a `const` binding. Declare it with `let x = …` instead if its value needs to change.",
      "now": "'x' is a 'const' and cannot be assigned again. Declare it with 'let' to write it more than once.",
      "reason": "message: same refusal, reworded."
    },
    "pipeline\u0000let v = $.x; $group({ _id: $.cat }); v = 5; $match(v > 0)": {
      "entry": "pipeline",
      "src": "let v = $.x; $group({ _id: $.cat }); v = 5; $match(v > 0)",
      "kind": "now-accepted",
      "was": "`v` is a `let` binding and can't be reassigned after `$group` — the stage replaces the document. Rebind it after the stage with `let v = …`.",
      "now": "[{\"$set\":{\"__jsmql.var.v\":\"$x\"}},{\"$group\":{\"_id\":\"$cat\"}},{\"$set\":{\"__jsmql.var.v\":5}},{\"$match\":{\"$expr\":{\"$gt\":[\"$__jsmql.var.v\",0]}}},{\"$unset\":\"__jsmql\"}]",
      "reason": "intended: an assignment to a `let` a document-replacing stage dropped carries it again in its slot — the JavaScript-valid way to rebind; the shipped compiler refused it and pointed at a re-declaration JavaScript refuses."
    },
    "pipeline\u0000let x = $.a; $ = $.profile; $.b = x;": {
      "entry": "pipeline",
      "src": "let x = $.a; $ = $.profile; $.b = x;",
      "kind": "message",
      "was": "`x` is a `let` binding and can't be read after `$replaceWith` — the stage replaces the document. Inline the expression into the `$replaceWith` body, or rebind after the stage with another `let`.",
      "now": "`x` is a `let` binding and can't be read after `$replaceWith` — that stage replaced the document that carried it. Assign it again after the stage (`x = …`), or carry the value as a field of the new document.",
      "reason": "message: same refusal, reworded — the way back is `x = …` (a `let` a document-replacing stage dropped is carried again by an assignment) or a field of the new document; the shipped hint, `rebind with let`, is a second declaration in one block, which JavaScript refuses."
    },
    "pipeline\u0000let n = $.qty * 2; $$.$group({ _id: '$a' }); $set({ x: n });": {
      "entry": "pipeline",
      "src": "let n = $.qty * 2; $$.$group({ _id: '$a' }); $set({ x: n });",
      "kind": "message",
      "was": "`n` is a `let` binding and can't be read after `$group` — the stage replaces the document. Inline the expression into the `$group` body, or rebind after the stage with another `let`.",
      "now": "`n` is a `let` binding and can't be read after `$group` — that stage replaced the document that carried it. Assign it again after the stage (`n = …`), or carry the value as a field of the new document.",
      "reason": "message: same refusal, reworded — the way back is `x = …` (a `let` a document-replacing stage dropped is carried again by an assignment) or a field of the new document; the shipped hint, `rebind with let`, is a second declaration in one block, which JavaScript refuses."
    },
    "pipeline\u0000$match($.active === true); $$ = [{ _id: 1 }];": {
      "entry": "pipeline",
      "src": "$match($.active === true); $$ = [{ _id: 1 }];",
      "kind": "message",
      "was": "'$$ = [<docs>]' is only valid as the first stage of a pipeline ('$documents' must be at the head per MongoDB). To append documents to an existing stream, use '$$.push({...}, {...}, …)' instead, which lowers to '$unionWit",
      "now": "'$documents' produces the pipeline's source documents, so it has to be the FIRST stage — the server refuses it anywhere else. Move it to the top of the program.",
      "reason": "message: same refusal, reworded — `$documents` is a source stage and stands first."
    },
    "pipeline\u0000\nconst basePrice = $.price * $.qty;\nbasePrice = basePrice * 0.9;\n$project({ total: basePrice });\n        ": {
      "entry": "pipeline",
      "src": "\nconst basePrice = $.price * $.qty;\nbasePrice = basePrice * 0.9;\n$project({ total: basePrice });\n        ",
      "kind": "message",
      "was": "Cannot reassign `basePrice` — it is a `const` binding. Declare it with `let basePrice = …` instead if its value needs to change.",
      "now": "'basePrice' is a 'const' and cannot be assigned again. Declare it with 'let' to write it more than once.",
      "reason": "message: same refusal, reworded."
    },
    "pipeline\u0000$$.reduce((acc, o) => acc + o.total, 0);": {
      "entry": "pipeline",
      "src": "$$.reduce((acc, o) => acc + o.total, 0);",
      "kind": "message",
      "was": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Wrap the reduce result into a stream-shaped RHS: • Scalar reducer: '$$ = [{ <",
      "now": "'.reduce(...)' is not a chain method on '$$' — in JS '.reduce' collapses an array to a single value, but '$$' must stay a stream of documents. Use the '$ = [{ k: $.reduce(...) }]' wrap form.",
      "reason": "message: same refusal; the hint names the one wrap form instead of a menu."
    },
    "pipeline\u0000\n          const CLOSED = [\"cancelled\", \"rejected\", \"refunded\"].map(s => s.toUpperCase());\n          const CUTOFF = new Date(\"2024-01-01\");\n          const MAX_RETRIES = 2 ** 4;\n          $.status in CLOSED && $.createdAt >= CUTOFF && $.retries <= MAX_RETRIES\n        ": {
      "entry": "pipeline",
      "src": "\n          const CLOSED = [\"cancelled\", \"rejected\", \"refunded\"].map(s => s.toUpperCase());\n          const CUTOFF = new Date(\"2024-01-01\");\n          const MAX_RETRIES = 2 ** 4;\n          $.status in CLOSED && $.createdAt >= CUTOFF && $.retries <= MAX_RETRIES\n        ",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "A pipeline statement writes something: a field ('$.total = …;'), the document ('$ = { … };'), a deletion ('delete $.x;'), or a stage ('$match(…);'). This expression only computes a value — assign it to a field, or wrap a",
      "reason": "message: same refusal, reworded — a pipeline statement writes something; this expression only computes a value."
    },
    "pipeline\u0000$$.filter({ status: \"shipped\" }).countBy(\"paymentMethod\");": {
      "entry": "pipeline",
      "src": "$$.filter({ status: \"shipped\" }).countBy(\"paymentMethod\");",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"shipped\"}},{\"$group\":{\"_id\":\"$paymentMethod\",\"__jsmqlTmp\":{\"$sum\":1}}},{\"$group\":{\"_id\":null,\"__jsmqlTmp\":{\"$push\":{\"k\":{\"$ifNull\":[{\"$toString\":\"$_id\"},\"null\"]},\"v\":\"$__jsmqlTmp\"}}}},{\"$replaceWith",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"shipped\",\"$not\":{\"$type\":\"array\"}}}},{\"$group\":{\"_id\":\"$paymentMethod\",\"__jsmqlTmp\":{\"$sum\":1}}},{\"$group\":{\"_id\":null,\"__jsmqlTmp\":{\"$push\":{\"k\":{\"$ifNull\":[{\"$toString\":\"$_id\"},\"null\"]},\"v\"",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$.filter(t => t.createdAt >= new Date(\"2026-01-01\") && t.client === 156);": {
      "entry": "pipeline",
      "src": "$$.filter(t => t.createdAt >= new Date(\"2026-01-01\") && t.client === 156);",
      "kind": "output",
      "was": "[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"},\"client\":156}}]",
      "now": "[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}},\"client\":{\"$eq\":156,\"$not\":{\"$type\":\"array\"}}}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000\n$$.filter({ status: \"shipped\" });\n$$.toSorted((a, b) => b.placedAt - a.placedAt).slice(25, 50);\n        ": {
      "entry": "pipeline",
      "src": "\n$$.filter({ status: \"shipped\" });\n$$.toSorted((a, b) => b.placedAt - a.placedAt).slice(25, 50);\n        ",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"shipped\"}},{\"$sort\":{\"placedAt\":-1}},{\"$skip\":25},{\"$limit\":25}]",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"shipped\",\"$not\":{\"$type\":\"array\"}}}},{\"$sort\":{\"placedAt\":-1}},{\"$skip\":25},{\"$limit\":25}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000\n$$.filter({ shipped: true });\n$$.map(o => ({\n  orderId:  o._id,\n  customer: o.userId,\n  total:    o.qty * o.unitPrice,\n  shippedAt: o.shippedAt\n}));\n        ": {
      "entry": "pipeline",
      "src": "\n$$.filter({ shipped: true });\n$$.map(o => ({\n  orderId:  o._id,\n  customer: o.userId,\n  total:    o.qty * o.unitPrice,\n  shippedAt: o.shippedAt\n}));\n        ",
      "kind": "output",
      "was": "[{\"$match\":{\"shipped\":true}},{\"$replaceWith\":{\"orderId\":\"$_id\",\"customer\":\"$userId\",\"total\":{\"$multiply\":[\"$qty\",\"$unitPrice\"]},\"shippedAt\":\"$shippedAt\"}}]",
      "now": "[{\"$match\":{\"shipped\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}},{\"$replaceWith\":{\"orderId\":\"$_id\",\"customer\":\"$userId\",\"total\":{\"$multiply\":[\"$qty\",\"$unitPrice\"]},\"shippedAt\":\"$shippedAt\"}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000\n$$.filter({ status: \"shipped\" }).flatMap(\"items\").map(\"items\");\n$sort({ price: -1 });\n$limit(50);\n        ": {
      "entry": "pipeline",
      "src": "\n$$.filter({ status: \"shipped\" }).flatMap(\"items\").map(\"items\");\n$sort({ price: -1 });\n$limit(50);\n        ",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"shipped\"}},{\"$unwind\":\"$items\"},{\"$replaceWith\":\"$items\"},{\"$sort\":{\"price\":-1}},{\"$limit\":50}]",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"shipped\",\"$not\":{\"$type\":\"array\"}}}},{\"$unwind\":\"$items\"},{\"$replaceWith\":\"$items\"},{\"$sort\":{\"price\":-1}},{\"$limit\":50}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$match($.active === true); $$ = [{ a: 1 }, { b: 2 }];": {
      "entry": "pipeline",
      "src": "$match($.active === true); $$ = [{ a: 1 }, { b: 2 }];",
      "kind": "message",
      "was": "'$$ = [<docs>]' is only valid as the first stage of a pipeline ('$documents' must be at the head per MongoDB). To append documents to an existing stream, use '$$.push({...}, {...}, …)' instead, which lowers to '$unionWit",
      "now": "'$documents' produces the pipeline's source documents, so it has to be the FIRST stage — the server refuses it anywhere else. Move it to the top of the program.",
      "reason": "message: same refusal, reworded — `$documents` is a source stage and stands first."
    },
    "pipeline\u0000let v = $.x; $group({ _id: $.c, sum: $sum($.a) }); let v = $.sum * 2; $project({ v })": {
      "entry": "pipeline",
      "src": "let v = $.x; $group({ _id: $.c, sum: $sum($.a) }); let v = $.sum * 2; $project({ v })",
      "kind": "now-rejected",
      "was": "[{\"$set\":{\"__jsmql.var.v\":\"$x\"}},{\"$group\":{\"_id\":\"$c\",\"sum\":{\"$sum\":\"$a\"}}},{\"$set\":{\"__jsmql.var.v\":{\"$multiply\":[\"$sum\",2]}}},{\"$project\":{\"v\":\"$__jsmql.var.v\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "`let v` is already declared earlier in this block — re-declaration in the same scope is not allowed; pick a different name.",
      "reason": "intended: a second `let v` in one block is a SyntaxError in JavaScript, and the language is a strict syntax subset; the shipped compiler accepted it after a document-replacing stage. `v = …` carries a dropped `let` again."
    },
    "pipeline\u0000$$ = $$$.orders.filter(o => { return o.uid === $._id; });": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.filter(o => { return o.uid === $._id; });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"uid\",\"as\":\"__jsmql.tmp.1\"}},{\"$unwind\":\"$__jsmql.tmp.1\"},{\"$replaceWith\":\"$__jsmql.tmp.1\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$uid\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"},{\"$replaceWith\":\"$__jsmql.tmp.0\"}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.filter(o => { return o.userId === $._id; });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.filter(o => { return o.userId === $._id; });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"r\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"r\"}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.filter(o => o.userId === $._id);": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.filter(o => o.userId === $._id);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"r\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"r\"}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.find(o => { return o.userId === $._id; });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.find(o => { return o.userId === $._id; });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"r\"}},{\"$set\":{\"r\":{\"$first\":\"$r\"}}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$limit\":1}],\"as\":\"r\"}},{\"$set\":{\"r\":{\"$first\":\"$r\"}}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.find(o => o.userId === $._id);": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.find(o => o.userId === $._id);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"r\"}},{\"$set\":{\"r\":{\"$first\":\"$r\"}}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$limit\":1}],\"as\":\"r\"}},{\"$set\":{\"r\":{\"$first\":\"$r\"}}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$$.myColl.find(o => o.x === $.y)": {
      "entry": "pipeline",
      "src": "$$$.myColl.find(o => o.x === $.y)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "Reading another collection produces a value, and this statement gives it no destination. Assign it to a field ('$.<field> = $$$.<coll>.…'), bind it ('let x = $$$.<coll>.…'), or make it the stream ('$$ = $$$.<coll>.…').",
      "reason": "message: same refusal — a read of another collection with no destination — reworded; a system stage on a context reference is a verified pending until its road lands."
    },
    "pipeline\u0000\n        const cutoff = \"2026-01-01\";\n        $lookup({ from: \"orders\", pipeline: [ $match($.createdAt > cutoff) ], as: \"recent\" });\n      ": {
      "entry": "pipeline",
      "src": "\n        const cutoff = \"2026-01-01\";\n        $lookup({ from: \"orders\", pipeline: [ $match($.createdAt > cutoff) ], as: \"recent\" });\n      ",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"createdAt\":{\"$gt\":\"2026-01-01\"}}}],\"as\":\"recent\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$gt\":[\"$$jsmql_f0_createdAt\",\"2026-01-01\"]}}}],\"as\":\"recent\",\"let\":{\"jsmql_f0_createdAt\":\"$createdAt\"}}}]",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000const r = $$$.tags.find(function (t) { const a = t.score; return a > 5 }); $ = { r };": {
      "entry": "pipeline",
      "src": "const r = $$$.tags.find(function (t) { const a = t.score; return a > 5 }); $ = { r };",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"tags\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$let\":{\"vars\":{\"a\":\"$score\"},\"in\":{\"$gt\":[\"$$a\",5]}}}}}],\"as\":\"__jsmql.var.r\"}},{\"$set\":{\"__jsmql.var.r\":{\"$first\":\"$__jsmql.var.r\"}}},{\"$replaceWith\":{\"r\":\"$__",
      "now": "[{\"$lookup\":{\"from\":\"tags\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$let\":{\"vars\":{\"a\":\"$score\"},\"in\":{\"$gt\":[\"$$a\",5]}}}}},{\"$limit\":1}],\"as\":\"__jsmql.var.r\"}},{\"$set\":{\"__jsmql.var.r\":{\"$first\":\"$__jsmql.var.r\"}}},{\"$replaceWi",
      "reason": "intended: `.find` bounds its body with `$limit: 1` — the first match is all it keeps, and the server examines one index key per document instead of every match. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000const tags = $$$.tags.find(function (t) { return t.active }); $ = { tags };": {
      "entry": "pipeline",
      "src": "const tags = $$$.tags.find(function (t) { return t.active }); $ = { tags };",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"tags\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}}],\"as\":\"__jsmql.var.tags\"}},{\"$set\"",
      "now": "[{\"$lookup\":{\"from\":\"tags\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}},{\"$limit\":1}],\"as\":\"__jsmql.var.ta",
      "reason": "intended: `.find` bounds its body with `$limit: 1` — the first match is all it keeps, and the server examines one index key per document instead of every match. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000const tags = $$$.tags.find((t) => t.active); $ = { tags };": {
      "entry": "pipeline",
      "src": "const tags = $$$.tags.find((t) => t.active); $ = { tags };",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"tags\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}}],\"as\":\"__jsmql.var.tags\"}},{\"$set\"",
      "now": "[{\"$lookup\":{\"from\":\"tags\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}},{\"$limit\":1}],\"as\":\"__jsmql.var.ta",
      "reason": "intended: `.find` bounds its body with `$limit: 1` — the first match is all it keeps, and the server examines one index key per document instead of every match. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000let k = $.x; $$ = $$$.orders.map(o => ({ t: o.total + k }));": {
      "entry": "pipeline",
      "src": "let k = $.x; $$ = $$$.orders.map(o => ({ t: o.total + k }));",
      "kind": "now-accepted",
      "was": "`k` is a `let`/`const` declared before `$$ = $$$.orders`, which replaces the stream with a different collection (a `$unionWith`, which can't correlate) — so `k` (along with the outer document and the root `$$.length`) is",
      "now": "[{\"$set\":{\"__jsmql.var.k\":\"$x\"}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_v0_k\":\"$__jsmql.var.k\"},\"pipeline\":[{\"$replaceWith\":{\"t\":{\"$add\":[\"$total\",\"$$jsmql_v0_k\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000[\n        $lookup({\n          from: \"orders\",\n          let: { uid: $._id },\n          pipeline: [\n            let recent = $.createdAt > \"2026-01-01\",\n            $match(recent)\n          ],\n          as: \"userOrders\"\n        })\n      ]": {
      "entry": "pipeline",
      "src": "[\n        $lookup({\n          from: \"orders\",\n          let: { uid: $._id },\n          pipeline: [\n            let recent = $.createdAt > \"2026-01-01\",\n            $match(recent)\n          ],\n          as: \"userOrders\"\n        })\n      ]",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"uid\":\"$_id\"},\"pipeline\":[{\"$set\":{\"__jsmql.var.recent\":{\"$gt\":[\"$createdAt\",\"2026-01-01\"]}}},{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$__jsmql.var.recent\",null]},null]},{\"$ne\"",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"uid\":\"$_id\",\"jsmql_f0_createdAt\":\"$createdAt\"},\"pipeline\":[{\"$set\":{\"__jsmql.var.recent\":{\"$gt\":[\"$$jsmql_f0_createdAt\",\"2026-01-01\"]}}},{\"$match\":{\"$expr\":\"$__jsmql.var.recent\"}},{\"$",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000\n        let cutoff = $.start;\n        $lookup({\n          from: \"orders\",\n          pipeline: [ $match($.createdAt > cutoff) ],\n          as: \"recent\"\n        });\n      ": {
      "entry": "pipeline",
      "src": "\n        let cutoff = $.start;\n        $lookup({\n          from: \"orders\",\n          pipeline: [ $match($.createdAt > cutoff) ],\n          as: \"recent\"\n        });\n      ",
      "kind": "now-accepted",
      "was": "Unknown identifier 'cutoff'. Did you mean '$.cutoff'?",
      "now": "[{\"$set\":{\"__jsmql.var.cutoff\":\"$start\"}},{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$gt\":[\"$$jsmql_f0_createdAt\",\"$$jsmql_v0_cutoff\"]}}}],\"as\":\"recent\",\"let\":{\"jsmql_f0_createdAt\":\"$createdAt\",\"jsmql_v",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000[\n        let x = $.a,\n        $lookup({\n          from: \"orders\",\n          pipeline: [\n            let x = $.b,\n            $match(x > 0)\n          ],\n          as: \"matched\"\n        }),\n        $match(x < 100)\n      ]": {
      "entry": "pipeline",
      "src": "[\n        let x = $.a,\n        $lookup({\n          from: \"orders\",\n          pipeline: [\n            let x = $.b,\n            $match(x > 0)\n          ],\n          as: \"matched\"\n        }),\n        $match(x < 100)\n      ]",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.x\":\"$a\"}},{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$set\":{\"__jsmql.var.x\":\"$b\"}},{\"$match\":{\"$expr\":{\"$gt\":[\"$__jsmql.var.x\",0]}}},{\"$unset\":\"__jsmql\"}],\"as\":\"matched\"}},{\"$match\":{\"$expr\":{\"$lt\":",
      "now": "[{\"$set\":{\"__jsmql.var.x\":\"$a\"}},{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$set\":{\"__jsmql.var.x\":\"$$jsmql_f0_b\"}},{\"$match\":{\"$expr\":{\"$gt\":[\"$__jsmql.var.x\",0]}}},{\"$unset\":\"__jsmql\"}],\"as\":\"matched\",\"let\":{\"jsmql_f0_b",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.u = $$$.users.find(u => u.refId === $._id && u.active);": {
      "entry": "pipeline",
      "src": "$.u = $$$.users.find(u => u.refId === $._id && u.active);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$eq\":[\"$refId\",\"$$jsmql_f0__id\"]},{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$eq\":[\"$refId\",\"$$jsmql_f0__id\"]},{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.o = $$$.orders.filter(o => o.userId === $._id);": {
      "entry": "pipeline",
      "src": "$.o = $$$.orders.filter(o => o.userId === $._id);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"o\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"o\"}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$ = $$$.transactions.filter(t => t.client === 156);": {
      "entry": "pipeline",
      "src": "$$ = $$$.transactions.filter(t => t.client === 156);",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"transactions\",\"pipeline\":[{\"$match\":{\"client\":156}}]}}]",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"transactions\",\"pipeline\":[{\"$match\":{\"client\":{\"$eq\":156,\"$not\":{\"$type\":\"array\"}}}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.orders = $$$.orders.filter(o => o.userId === $._id);": {
      "entry": "pipeline",
      "src": "$.orders = $$$.orders.filter(o => o.userId === $._id);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"orders\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"orders\"}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.order = $$$.orders.find(o => o.userId === $._id);": {
      "entry": "pipeline",
      "src": "$.order = $$$.orders.find(o => o.userId === $._id);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"order\"}},{\"$set\":{\"order\":{\"$first\":\"$order\"}}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$limit\":1}],\"as\":\"order\"}},{\"$set\":{\"order\":{\"$first\":\"$order\"}}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.user.profile = $$$.profiles.find(p => p.userId === $._id);": {
      "entry": "pipeline",
      "src": "$.user.profile = $$$.profiles.find(p => p.userId === $._id);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"profiles\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"user.profile\"}},{\"$set\":{\"user.profile\":{\"$first\":\"$user.profile\"}}}]",
      "now": "[{\"$lookup\":{\"from\":\"profiles\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$limit\":1}],\"as\":\"user.profile\"}},{\"$set\":{\"user.profile\":{\"$first\":\"$user.profile\"}}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.user = $$$.users.find(u => u._id === $.userId && u.active);": {
      "entry": "pipeline",
      "src": "$.user = $$$.users.find(u => u._id === $.userId && u.active);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0_userId\":\"$userId\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$eq\":[\"$_id\",\"$$jsmql_f0_userId\"]},{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0_userId\":\"$userId\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$eq\":[\"$_id\",\"$$jsmql_f0_userId\"]},{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.orders.filter(o => o.userId === $._id && o.status === \"shipped\");": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter(o => o.userId === $._id && o.status === \"shipped\");",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"status\":\"shipped\",\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"x\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"status\":{\"$eq\":\"shipped\",\"$not\":{\"$type\":\"array\"}},\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"x\"}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.x = $$$.orders.filter(o => o.ref === $.meta[\"sub-id\"] && o.qty > 0);": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter(o => o.ref === $.meta[\"sub-id\"] && o.qty > 0);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0_sub_id\":\"$meta.sub-id\"},\"pipeline\":[{\"$match\":{\"qty\":{\"$gt\":0},\"$expr\":{\"$eq\":[\"$ref\",\"$$jsmql_f0_sub_id\"]}}}],\"as\":\"x\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0_meta\":\"$meta\"},\"pipeline\":[{\"$match\":{\"qty\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}},\"$expr\":{\"$eq\":[\"$ref\",{\"$getField\":{\"field\":\"sub-id\",\"input\":\"$$jsmql_f0_meta\"}}]}}}],\"as\":\"x\"",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.x = $$$.orders.filter(o => o.a === $.meta[\"sub-id\"] && o.b === $.meta[\"sub_id\"]);": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter(o => o.a === $.meta[\"sub-id\"] && o.b === $.meta[\"sub_id\"]);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0_sub_id\":\"$meta.sub-id\",\"jsmql_f0_sub_id_2\":\"$meta.sub_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$eq\":[\"$a\",\"$$jsmql_f0_sub_id\"]},{\"$eq\":[\"$b\",\"$$jsmql_f0_sub_id_2\"]}]}}}",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0_meta\":\"$meta\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$eq\":[\"$a\",{\"$getField\":{\"field\":\"sub-id\",\"input\":\"$$jsmql_f0_meta\"}}]},{\"$eq\":[\"$b\",{\"$getField\":{\"field\":\"sub_id\",\"",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000let n = $$$.orders.filter(o => o.userId === $._id).length;": {
      "entry": "pipeline",
      "src": "let n = $$$.orders.filter(o => o.userId === $._id).length;",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$size\":\"$__jsmql.tmp.1\"}}},{\"$set\":{\"__jsmql.var.n\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$set\":{\"__jsmql.var.n\":{\"$size\":\"$__jsmql.tmp.0\"}}},{\"$unset\":\"__",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000let n = $$$.users.find(u => u._id === $._id).length;": {
      "entry": "pipeline",
      "src": "let n = $$$.users.find(u => u._id === $._id).length;",
      "kind": "message",
      "was": ".length on a .find() result is not meaningful — .find returns scalar-or-null. Use .filter(...).length to count matching documents, or chain a field access (.find(...).<field>) to read a property of the matched doc.",
      "now": "'.length()' is not available on a 'object' — it is defined on 'array', 'string', 'stream'.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000let name = $$$.users.find(u => u._id === $.userId).name;": {
      "entry": "pipeline",
      "src": "let name = $$$.users.find(u => u._id === $.userId).name;",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"localField\":\"userId\",\"foreignField\":\"_id\",\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$set\":{\"__jsmql.var.name\":\"$__jsmql.tmp.1.name\"}},{\"$unset\":\"__jsmql\"",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0_userId\":\"$userId\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_f0_userId\"]}}},{\"$limit\":1}],\"as\":\"__jsmql.tmp.0\"}},{\"$set\":{\"__jsmql.tmp.0\":{\"$first\":\"$__jsmql.tmp.",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.users.fnid(u => u._id === $._id);": {
      "entry": "pipeline",
      "src": "$.x = $$$.users.fnid(u => u._id === $._id);",
      "kind": "message",
      "was": "'$$$.users' supports .find(pred), .filter(pred), .aggregate(pipeline), and the lodash stream methods (e.g. .toSorted, .take, .map — see docs/specs/stream-methods.md), not .fnid(). Did you mean '.find'? For a full sub-pip",
      "now": "Unknown method '.fnid()' at position 15. Did you mean '.find()'?",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$.users.find();": {
      "entry": "pipeline",
      "src": "$.x = $$$.users.find();",
      "kind": "message",
      "was": ".find(predicate) takes exactly one argument (a single-parameter arrow), got 0.",
      "now": "'.find()'(predicate) requires exactly 1 argument, got 0",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$.users.filter(o => o.x === $.y, 0);": {
      "entry": "pipeline",
      "src": "$.x = $$$.users.filter(o => o.x === $.y, 0);",
      "kind": "message",
      "was": ".filter(predicate) takes exactly one argument (a single-parameter arrow), got 2.",
      "now": "'.filter()'(predicate) requires exactly 1 argument, got 2",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$.users.find(123);": {
      "entry": "pipeline",
      "src": "$.x = $$$.users.find(123);",
      "kind": "message",
      "was": ".find(predicate) requires an arrow predicate (`.find(o => o._id === $.userId)`), a matches-object (`{ userId: $._id }`), a field name (`\"active\"`), or a `[\"field\", value]` pair.",
      "now": "'.filter()' takes a predicate as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$.users.find((u, i) => u._id === $.userId);": {
      "entry": "pipeline",
      "src": "$.x = $$$.users.find((u, i) => u._id === $.userId);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"localField\":\"userId\",\"foreignField\":\"_id\",\"as\":\"x\"}},{\"$set\":{\"x\":{\"$first\":\"$x\"}}}]",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0_userId\":\"$userId\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_f0_userId\"]}}},{\"$limit\":1}],\"as\":\"x\"}},{\"$set\":{\"x\":{\"$first\":\"$x\"}}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.users.find((u, i) => i === 0);": {
      "entry": "pipeline",
      "src": "$.x = $$$.users.find((u, i) => i === 0);",
      "kind": "message",
      "was": "'i' (the index parameter) has no meaning on a '.find' predicate — the filtered sub-stream doesn't exist yet while the predicate runs. For its count, use `.aggregate` and its 3rd param, e.g. `$$$.users.aggregate((u, _i, c",
      "now": "`i` has no value inside `.filter()` — a stream has no per-document index; leave the parameter unused.",
      "reason": "message: the index and collection parameters of a callback have no value on a stream (`$$.length` is the stream's size); same refusals, reworded."
    },
    "pipeline\u0000$.x = $$$.users.find((u, i, c, d) => u.a);": {
      "entry": "pipeline",
      "src": "$.x = $$$.users.find((u, i, c, d) => u.a);",
      "kind": "message",
      "was": ".find(predicate) takes at most 3 parameters '(element, index, array)', got 4.",
      "now": "'.filter()' takes a predicate as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$.users.find(u => u._id === $._id)": {
      "entry": "pipeline",
      "src": "$.x = $$$.users.find(u => u._id === $._id)",
      "kind": "now-accepted",
      "was": "'$$$.<coll>' must be either followed by a stream chain and consumed as a value (a $lookup read — any lodash stream method may head the chain, e.g. '.filter(pred)' / '.toSorted(...)', and '.aggregate((o) => { ... })' runs",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_f0__id\"]}}},{\"$limit\":1}],\"as\":\"x\"}},{\"$set\":{\"x\":{\"$first\":\"$x\"}}}]",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.users.find(u => u._id === $._id);": {
      "entry": "pipeline",
      "src": "$.x = $$$.users.find(u => u._id === $._id);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"localField\":\"_id\",\"foreignField\":\"_id\",\"as\":\"x\"}},{\"$set\":{\"x\":{\"$first\":\"$x\"}}}]",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_f0__id\"]}}},{\"$limit\":1}],\"as\":\"x\"}},{\"$set\":{\"x\":{\"$first\":\"$x\"}}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$$.users.find(u => u._id === $._id)": {
      "entry": "pipeline",
      "src": "$$$.users.find(u => u._id === $._id)",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "Reading another collection produces a value, and this statement gives it no destination. Assign it to a field ('$.<field> = $$$.<coll>.…'), bind it ('let x = $$$.<coll>.…'), or make it the stream ('$$ = $$$.<coll>.…').",
      "reason": "message: same refusal — a read of another collection with no destination — reworded; a system stage on a context reference is a verified pending until its road lands."
    },
    "pipeline\u0000$.users = $$$.users.filter(o => o);": {
      "entry": "pipeline",
      "src": "$.users = $$$.users.filter(o => o);",
      "kind": "now-accepted",
      "was": "Bare lambda parameter 'o' in a $lookup predicate is not yet supported — use `o.<field>` to reference a foreign document field.",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$$ROOT\",null]},null]},{\"$ne\":[\"$$ROOT\",false]},{\"$ne\":[\"$$ROOT\",\"\"]},{\"$ne\":[\"$$ROOT\",0]}]}}}],\"as\":\"users\"}}]",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.orders.filter(o => o.ref === $);": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter(o => o.ref === $);",
      "kind": "now-accepted",
      "was": "Bare '$' (the whole outer document) can't be used as a value in a $lookup predicate — reference a specific field with '$.<field>' (e.g. '$.userId').",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0_root\":\"$$ROOT\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$ref\",\"$$jsmql_f0_root\"]}}}],\"as\":\"x\"}}]",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000    $.x = $$$.users.find();": {
      "entry": "pipeline",
      "src": "    $.x = $$$.users.find();",
      "kind": "message",
      "was": ".find(predicate) takes exactly one argument (a single-parameter arrow), got 0.",
      "now": "'.find()'(predicate) requires exactly 1 argument, got 0",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$.a.filter(a => $$$.b.filter(b => b.x === a.x).length > 0)": {
      "entry": "pipeline",
      "src": "$.x = $$$.a.filter(a => $$$.b.filter(b => b.x === a.x).length > 0)",
      "kind": "now-accepted",
      "was": "'$$$.<coll>' must be either followed by a stream chain and consumed as a value (a $lookup read — any lodash stream method may head the chain, e.g. '.filter(pred)' / '.toSorted(...)', and '.aggregate((o) => { ... })' runs",
      "now": "[{\"$lookup\":{\"from\":\"a\",\"pipeline\":[{\"$lookup\":{\"from\":\"b\",\"let\":{\"jsmql_f1_x\":\"$x\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$x\",\"$$jsmql_f1_x\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$match\":{\"$expr\":{\"$gt\":[{\"$size\":\"$__jsmql.tm",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.a.find(a => $$$.b.find(b => b.x === a.x))": {
      "entry": "pipeline",
      "src": "$.x = $$$.a.find(a => $$$.b.find(b => b.x === a.x))",
      "kind": "now-accepted",
      "was": "'$$$.<coll>' must be either followed by a stream chain and consumed as a value (a $lookup read — any lodash stream method may head the chain, e.g. '.filter(pred)' / '.toSorted(...)', and '.aggregate((o) => { ... })' runs",
      "now": "[{\"$lookup\":{\"from\":\"a\",\"pipeline\":[{\"$lookup\":{\"from\":\"b\",\"let\":{\"jsmql_f1_x\":\"$x\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$x\",\"$$jsmql_f1_x\"]}}},{\"$limit\":1}],\"as\":\"__jsmql.tmp.0\"}},{\"$set\":{\"__jsmql.tmp.0\":{\"$first\":",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.posts = $$$.posts.filter(p => p.userId === $._id && $$$.tags.filter(t => t.postId === p._id).length > 0)": {
      "entry": "pipeline",
      "src": "$.posts = $$$.posts.filter(p => p.userId === $._id && $$$.tags.filter(t => t.postId === p._id).length > 0)",
      "kind": "now-accepted",
      "was": "'$$$.<coll>' must be either followed by a stream chain and consumed as a value (a $lookup read — any lodash stream method may head the chain, e.g. '.filter(pred)' / '.toSorted(...)', and '.aggregate((o) => { ... })' runs",
      "now": "[{\"$lookup\":{\"from\":\"posts\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$lookup\":{\"from\":\"tags\",\"let\":{\"jsmql_f1__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$postId\",\"$$jsmql_f1__id\"]}}}],\"as\":\"__jsmql.tmp.0\"}},",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.a.filter(a => $$$.b.filter(b => $$$.c.filter(c => c.x === b.x).length > 0).length > 0)": {
      "entry": "pipeline",
      "src": "$.x = $$$.a.filter(a => $$$.b.filter(b => $$$.c.filter(c => c.x === b.x).length > 0).length > 0)",
      "kind": "now-accepted",
      "was": "'$$$.<coll>' must be either followed by a stream chain and consumed as a value (a $lookup read — any lodash stream method may head the chain, e.g. '.filter(pred)' / '.toSorted(...)', and '.aggregate((o) => { ... })' runs",
      "now": "[{\"$lookup\":{\"from\":\"a\",\"pipeline\":[{\"$lookup\":{\"from\":\"b\",\"pipeline\":[{\"$lookup\":{\"from\":\"c\",\"let\":{\"jsmql_f2_x\":\"$x\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$x\",\"$$jsmql_f2_x\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$match\":{\"$",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.a.filter(a => $$$.b.filter(b => b.x === a.x && b.active === true).length > 0)": {
      "entry": "pipeline",
      "src": "$.x = $$$.a.filter(a => $$$.b.filter(b => b.x === a.x && b.active === true).length > 0)",
      "kind": "now-accepted",
      "was": "'$$$.<coll>' must be either followed by a stream chain and consumed as a value (a $lookup read — any lodash stream method may head the chain, e.g. '.filter(pred)' / '.toSorted(...)', and '.aggregate((o) => { ... })' runs",
      "now": "[{\"$lookup\":{\"from\":\"a\",\"pipeline\":[{\"$lookup\":{\"from\":\"b\",\"let\":{\"jsmql_f1_x\":\"$x\"},\"pipeline\":[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}},\"$expr\":{\"$eq\":[\"$x\",\"$$jsmql_f1_x\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.a.filter(a => $$$.b.filter(b => b === a).length > 0)": {
      "entry": "pipeline",
      "src": "$.x = $$$.a.filter(a => $$$.b.filter(b => b === a).length > 0)",
      "kind": "now-accepted",
      "was": "'$$$.<coll>' must be either followed by a stream chain and consumed as a value (a $lookup read — any lodash stream method may head the chain, e.g. '.filter(pred)' / '.toSorted(...)', and '.aggregate((o) => { ... })' runs",
      "now": "[{\"$lookup\":{\"from\":\"a\",\"pipeline\":[{\"$lookup\":{\"from\":\"b\",\"let\":{\"jsmql_f1_a\":\"$$ROOT\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$$ROOT\",\"$$jsmql_f1_a\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$match\":{\"$expr\":{\"$gt\":[{\"$size\":\"$__",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.a.aggregate(a => { $match(a.active); $.bs = $$$.b.filter(b => b.aId === a._id); });": {
      "entry": "pipeline",
      "src": "$.x = $$$.a.aggregate(a => { $match(a.active); $.bs = $$$.b.filter(b => b.aId === a._id); });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"a\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}},{\"$lookup\":{\"from\":\"b\",\"let\":{\"jsmql_",
      "now": "The outer document can't be written from inside a body over another collection — only read. Write the body's own document through its callback parameter ('o.x = …', 'delete o.x', 'o = { … }'), or as a stage ('$set({ x: …",
      "reason": "intended: `$.` is the OUTER document at every depth (HR4) and cannot be written from a body over another collection; the body's own document is its callback parameter (`o.x = …`, `o = { … }`) or a stage (`$set(…)`). The shipped compiler wrote the FOREIGN document for `$.x =` there — the one exception to the ruling."
    },
    "pipeline\u0000$.x = $$$.a.aggregate(a => { $.bs = $$$.b.aggregate(b => { $match(b.aId === a._id); $sort({ _id: 1 }); }); });": {
      "entry": "pipeline",
      "src": "$.x = $$$.a.aggregate(a => { $.bs = $$$.b.aggregate(b => { $match(b.aId === a._id); $sort({ _id: 1 }); }); });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"a\",\"pipeline\":[{\"$lookup\":{\"from\":\"b\",\"let\":{\"jsmql_f1__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$aId\",\"$$jsmql_f1__id\"]}}},{\"$sort\":{\"_id\":1}}],\"as\":\"bs\"}}],\"as\":\"x\"}}]",
      "now": "The outer document can't be written from inside a body over another collection — only read. Write the body's own document through its callback parameter ('o.x = …', 'delete o.x', 'o = { … }'), or as a stage ('$set({ x: …",
      "reason": "intended: `$.` is the OUTER document at every depth (HR4) and cannot be written from a body over another collection; the body's own document is its callback parameter (`o.x = …`, `o = { … }`) or a stage (`$set(…)`). The shipped compiler wrote the FOREIGN document for `$.x =` there — the one exception to the ruling."
    },
    "pipeline\u0000$.x = $$$.users.aggregate(u => { $match($$$.orders.filter(o => o.uid === u._id).length > 0); $sort({ name: 1 }); });": {
      "entry": "pipeline",
      "src": "$.x = $$$.users.aggregate(u => { $match($$$.orders.filter(o => o.uid === u._id).length > 0); $sort({ name: 1 }); });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"pipeline\":[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f1__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$uid\",\"$$jsmql_f1__id\"]}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$siz",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"pipeline\":[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f1__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$uid\",\"$$jsmql_f1__id\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$match\":{\"$expr\":{\"$gt\":[{\"$s",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$$.analytics.orders.filter(o => o.userId === $._id)": {
      "entry": "pipeline",
      "src": "$.x = $$$$.analytics.orders.filter(o => o.userId === $._id)",
      "kind": "message",
      "was": "'$$$$.<db>.<coll>' is only usable as a cross-database $out destination ('$$$$.<db>.<coll> = $$'). Cross-database READS aren't supported (a $lookup/$unionWith with a '{ db, coll }' namespace is rejected by a standalone / ",
      "now": "A read of another DATABASE isn't supported: a '$lookup' joins a collection of the current database only (the '{ db, coll }' form is Atlas Data Federation's). Drop the '$$$$.<db>.' prefix — '$$$.<coll>.find(…)' — and run ",
      "reason": "message: same refusal — a cross-database read — reworded."
    },
    "pipeline\u0000let n = $$$$.analytics.orders.filter(o => o.userId === $._id).length;": {
      "entry": "pipeline",
      "src": "let n = $$$$.analytics.orders.filter(o => o.userId === $._id).length;",
      "kind": "message",
      "was": "Cross-database reads aren't supported: '$$$$.analytics.orders' would emit a $lookup/$unionWith with a '{ db, coll }' namespace, which a standalone / replica-set / sharded MongoDB rejects (that shape is Atlas Data Federat",
      "now": "A read of another DATABASE isn't supported: a '$lookup' joins a collection of the current database only (the '{ db, coll }' form is Atlas Data Federation's). Drop the '$$$$.<db>.' prefix — '$$$.<coll>.find(…)' — and run ",
      "reason": "message: same refusal — a cross-database read — reworded."
    },
    "pipeline\u0000$.stats = $$$.users.filter(u => u.active).map(u => ({ id: u._id, name: u.name }));": {
      "entry": "pipeline",
      "src": "$.stats = $$$.users.filter(u => u.active).map(u => ({ id: u._id, name: u.name }));",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}},{\"$replaceWith\":{\"id\":\"$_id\",\"nam",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.filter(o => o.userId === $._id).map(o => ({ t: o.total })).take(5);": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.filter(o => o.userId === $._id).map(o => ({ t: o.total })).take(5);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$replaceWith\":{\"t\":\"$total\"}},{\"$limit\":5}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$replaceWith\":{\"t\":\"$total\"}},{\"$limit\":5}],\"as\":\"r\"}}]",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.byScore = $$$.users.filter(u => u.active).toSorted((a, b) => b.score - a.score).slice(0, 5);": {
      "entry": "pipeline",
      "src": "$.byScore = $$$.users.filter(u => u.active).toSorted((a, b) => b.score - a.score).slice(0, 5);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}},{\"$sort\":{\"score\":-1}},{\"$limit\":",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}},{\"$sort\":{\"score\":-1}},{\"$limit\":",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.recent = $$$.events.filter(e => e.userId === $._id).toSorted((a, b) => b.createdAt - a.createdAt).slice(0, 10);": {
      "entry": "pipeline",
      "src": "$.recent = $$$.events.filter(e => e.userId === $._id).toSorted((a, b) => b.createdAt - a.createdAt).slice(0, 10);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"events\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"createdAt\":-1}},{\"$limit\":10}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"recent\":\"$__jsmq",
      "now": "[{\"$lookup\":{\"from\":\"events\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"createdAt\":-1}},{\"$limit\":10}],\"as\":\"recent\"}}]",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.items = $$$.orders.filter(o => o.userId === $._id).flatMap(o => o.items);": {
      "entry": "pipeline",
      "src": "$.items = $$$.orders.filter(o => o.userId === $._id).flatMap(o => o.items);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$unwind\":\"$items\"}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"items\":\"$__jsmql.tmp.1\"}},{\"$unset\":",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$unwind\":\"$items\"}],\"as\":\"items\"}}]",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.count = $$$.users.filter(u => u.active).length;": {
      "entry": "pipeline",
      "src": "$.count = $$$.users.filter(u => u.active).length;",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$active\",null]},null]},{\"$ne\":[\"$active\",false]},{\"$ne\":[\"$active\",\"\"]},{\"$ne\":[\"$active\",0]}]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$set\":{",
      "reason": "equivalent: the scratch slots count from 0 in this compiler; the shipped compiler counted from 1."
    },
    "pipeline\u0000$.firstName = $$$.users.find(u => u._id === $.userId).name;": {
      "entry": "pipeline",
      "src": "$.firstName = $$$.users.find(u => u._id === $.userId).name;",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"localField\":\"userId\",\"foreignField\":\"_id\",\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$set\":{\"firstName\":\"$__jsmql.tmp.1.name\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0_userId\":\"$userId\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_f0_userId\"]}}},{\"$limit\":1}],\"as\":\"__jsmql.tmp.0\"}},{\"$set\":{\"__jsmql.tmp.0\":{\"$first\":\"$__jsmql.tmp.",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.recent = $$$.orders.toSorted({ createdAt: -1 });": {
      "entry": "pipeline",
      "src": "$.recent = $$$.orders.toSorted({ createdAt: -1 });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"createdAt\":-1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"recent\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"createdAt\":-1}}],\"as\":\"recent\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.recent = $$$.orders.toSorted({ createdAt: -1 }).take(200).filter(o => o.qty > 1);": {
      "entry": "pipeline",
      "src": "$.recent = $$$.orders.toSorted({ createdAt: -1 }).take(200).filter(o => o.qty > 1);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"createdAt\":-1}},{\"$limit\":200},{\"$match\":{\"qty\":{\"$gt\":1}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"recent\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"createdAt\":-1}},{\"$limit\":200},{\"$match\":{\"qty\":{\"$gt\":1,\"$not\":{\"$type\":\"array\"}}}}],\"as\":\"recent\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.recent = $$$.orders.toSorted({ createdAt: -1 }).take(200).filter(o => o.userId === $._id && o.qty > 1);": {
      "entry": "pipeline",
      "src": "$.recent = $$$.orders.toSorted({ createdAt: -1 }).take(200).filter(o => o.userId === $._id && o.qty > 1);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$sort\":{\"createdAt\":-1}},{\"$limit\":200},{\"$match\":{\"qty\":{\"$gt\":1},\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$sort\":{\"createdAt\":-1}},{\"$limit\":200},{\"$match\":{\"qty\":{\"$gt\":1,\"$not\":{\"$type\":\"array\"}},\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"r",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.paid = $$$.orders.filter(o => o.qty > 1).filter(o => o.status === \"paid\");": {
      "entry": "pipeline",
      "src": "$.paid = $$$.orders.filter(o => o.qty > 1).filter(o => o.status === \"paid\");",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"qty\":{\"$gt\":1}}},{\"$match\":{\"status\":\"paid\"}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"paid\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"qty\":{\"$gt\":1,\"$not\":{\"$type\":\"array\"}}}},{\"$match\":{\"status\":{\"$eq\":\"paid\",\"$not\":{\"$type\":\"array\"}}}}],\"as\":\"paid\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.kept = $$$.orders.reject(o => o.cancelled).take(5);": {
      "entry": "pipeline",
      "src": "$.kept = $$$.orders.reject(o => o.cancelled).take(5);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"$expr\":{\"$not\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$cancelled\",null]},null]},{\"$ne\":[\"$cancelled\",false]},{\"$ne\":[\"$cancelled\",\"\"]},{\"$ne\":[\"$cancelled\",0]}]}}}},{\"$limit\":5",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"$nor\":[{\"$expr\":{\"$and\":[{\"$ne\":[{\"$ifNull\":[\"$cancelled\",null]},null]},{\"$ne\":[\"$cancelled\",false]},{\"$ne\":[\"$cancelled\",\"\"]},{\"$ne\":[\"$cancelled\",0]}]}}]}},{\"$limit\"",
      "reason": "intended: `!p` is `$nor` of p's native clause (the filter rule); the shipped compiler wrapped the whole predicate in `$expr: { $not: … }`."
    },
    "pipeline\u0000$.x = $$$.orders.toSrted({ createdAt: -1 });": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.toSrted({ createdAt: -1 });",
      "kind": "message",
      "was": "'$$$.orders' supports .find(pred), .filter(pred), .aggregate(pipeline), and the lodash stream methods (e.g. .toSorted, .take, .map — see docs/specs/stream-methods.md), not .toSrted(). Did you mean '.toSorted'? For a full",
      "now": "Unknown method '.toSrted()' at position 16. Did you mean '.toSorted()'?",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$$.other.orders.toSorted({ x: -1 });": {
      "entry": "pipeline",
      "src": "$.x = $$$$.other.orders.toSorted({ x: -1 });",
      "kind": "message",
      "was": "Cross-database reads aren't supported: '$$$$.other.orders' would emit a $lookup/$unionWith with a '{ db, coll }' namespace, which a standalone / replica-set / sharded MongoDB rejects (that shape is Atlas Data Federation ",
      "now": "A read of another DATABASE isn't supported: a '$lookup' joins a collection of the current database only (the '{ db, coll }' form is Atlas Data Federation's). Drop the '$$$$.<db>.' prefix — '$$$.<coll>.find(…)' — and run ",
      "reason": "message: same refusal — a cross-database read — reworded."
    },
    "pipeline\u0000$.x = $$$.orders.filter({ uid: 1 });": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter({ uid: 1 });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":1}}],\"as\":\"x\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":{\"$eq\":1,\"$not\":{\"$type\":\"array\"}}}}],\"as\":\"x\"}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.x = $$$.orders.filter(o => o.uid === 1);": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter(o => o.uid === 1);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":1}}],\"as\":\"x\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":{\"$eq\":1,\"$not\":{\"$type\":\"array\"}}}}],\"as\":\"x\"}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.x = $$$.orders.filter({});": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter({});",
      "kind": "message",
      "was": ".filter({ … }) needs at least one field to match.",
      "now": "'.filter()' takes a predicate as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$.orders.filter([1, 2]);": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter([1, 2]);",
      "kind": "message",
      "was": ".filter([\"field\", value]) matchesProperty shorthand needs a field-name string and a value.",
      "now": "'.filter()' takes a predicate as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.top = $$$.orders.slice(0, 0);": {
      "entry": "pipeline",
      "src": "$.top = $$$.orders.slice(0, 0);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"$expr\":false}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"top\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"$expr\":false}}],\"as\":\"top\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.orders.filter(o => o.uid === $._id).aggregate(o => { $sort({ x: -1 }); $ = { t: o.total }; });": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter(o => o.uid === $._id).aggregate(o => { $sort({ x: -1 }); $ = { t: o.total }; });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$uid\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"x\":-1}},{\"$replaceWith\":{\"t\":\"$total\"}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"x\":\"$__js",
      "now": "The outer document can't be written from inside a body over another collection — only read. Write the body's own document through its callback parameter ('o.x = …', 'delete o.x', 'o = { … }'), or as a stage ('$set({ x: …",
      "reason": "intended: `$.` is the OUTER document at every depth (HR4) and cannot be written from a body over another collection; the body's own document is its callback parameter (`o.x = …`, `o = { … }`) or a stage (`$set(…)`). The shipped compiler wrote the FOREIGN document for `$.x =` there — the one exception to the ruling."
    },
    "pipeline\u0000$.recentOrders = $$$.orders.filter(o => o.userId === $._id).aggregate((o) => { $sort({ placedAt: -1 }); $limit(5); });": {
      "entry": "pipeline",
      "src": "$.recentOrders = $$$.orders.filter(o => o.userId === $._id).aggregate((o) => { $sort({ placedAt: -1 }); $limit(5); });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"placedAt\":-1}},{\"$limit\":5}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"recentOrders\":\"$__",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"placedAt\":-1}},{\"$limit\":5}],\"as\":\"recentOrders\"}}]",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.n = $$$.orders.aggregate((o) => { $match(o.userId === $._id); $group({ _id: \"$productId\" }); }).length;": {
      "entry": "pipeline",
      "src": "$.n = $$$.orders.aggregate((o) => { $match(o.userId === $._id); $group({ _id: \"$productId\" }); }).length;",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$group\":{\"_id\":\"$productId\"}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$size\":\"",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$group\":{\"_id\":\"$productId\"}}],\"as\":\"__jsmql.tmp.0\"}},{\"$set\":{\"n\":{\"$size\":\"$__jsmql.tmp",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$ = $$$.products.aggregate((o, _i, coll) => { $set({ n: coll.length }); });": {
      "entry": "pipeline",
      "src": "$$ = $$$.products.aggregate((o, _i, coll) => { $set({ n: coll.length }); });",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"products\",\"pipeline\":[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$set\":{\"n\":\"$__jsmql.length\"}},{\"$unset\":\"__jsmql\"}]}}]",
      "now": "`coll` has no value inside `.aggregate()` — the collection is the stream itself; write '$$.length' for its size.",
      "reason": "message: the index and collection parameters of a callback have no value on a stream (`$$.length` is the stream's size); same refusals, reworded."
    },
    "pipeline\u0000$.top = $$$.orders.sort({ createdAt: -1 }).take(1000).aggregate((o) => { $group({ _id: o.status, n: $sum(1) }); });": {
      "entry": "pipeline",
      "src": "$.top = $$$.orders.sort({ createdAt: -1 }).take(1000).aggregate((o) => { $group({ _id: o.status, n: $sum(1) }); });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"createdAt\":-1}},{\"$limit\":1000},{\"$group\":{\"_id\":\"$status\",\"n\":{\"$sum\":1}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"top\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"createdAt\":-1}},{\"$limit\":1000},{\"$group\":{\"_id\":\"$status\",\"n\":{\"$sum\":1}}}],\"as\":\"top\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.toSorted((a, b) => a.total - b.total).aggregate((o) => { $group({ _id: o.status }); });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.toSorted((a, b) => a.total - b.total).aggregate((o) => { $group({ _id: o.status }); });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":1}},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":1}},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"r\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.sortBy(\"total\").drop(2).aggregate([{ $group: { _id: \"$status\" } }]);": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.sortBy(\"total\").drop(2).aggregate([{ $group: { _id: \"$status\" } }]);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":1}},{\"$skip\":2},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":1}},{\"$skip\":2},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"r\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.flatMap(o => o.items).aggregate((o) => { $group({ _id: null, n: $sum(1) }); });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.flatMap(o => o.items).aggregate((o) => { $group({ _id: null, n: $sum(1) }); });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$unwind\":\"$items\"},{\"$group\":{\"_id\":null,\"n\":{\"$sum\":1}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$unwind\":\"$items\"},{\"$group\":{\"_id\":null,\"n\":{\"$sum\":1}}}],\"as\":\"r\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.sort({ t: -1 }).aggregate((o) => { $group({ _id: o.status, n: $sum(1) }); }).sort({ n: -1 }).take(3);": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.sort({ t: -1 }).aggregate((o) => { $group({ _id: o.status, n: $sum(1) }); }).sort({ n: -1 }).take(3);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"t\":-1}},{\"$group\":{\"_id\":\"$status\",\"n\":{\"$sum\":1}}},{\"$sort\":{\"n\":-1}},{\"$limit\":3}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"t\":-1}},{\"$group\":{\"_id\":\"$status\",\"n\":{\"$sum\":1}}},{\"$sort\":{\"n\":-1}},{\"$limit\":3}],\"as\":\"r\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.$match({ total: { $gt: 0 } }).aggregate((o) => { $group({ _id: o.status }); });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.$match({ total: { $gt: 0 } }).aggregate((o) => { $group({ _id: o.status }); });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"total\":{\"$gt\":0}}},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"total\":{\"$gt\":0}}},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"r\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.filter(o => o.userId === $._id).sort({ t: -1 }).take(2).aggregate((o) => { $group({ _id: o.status }); });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.filter(o => o.userId === $._id).sort({ t: -1 }).take(2).aggregate((o) => { $group({ _id: o.status }); });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"t\":-1}},{\"$limit\":2},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"t\":-1}},{\"$limit\":2},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"r\"}}]",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.sort({ t: -1 }).take(5).aggregate((o) => { $match(o.userId === $._id); $group({ _id: o.status }); });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.sort({ t: -1 }).take(5).aggregate((o) => { $match(o.userId === $._id); $group({ _id: o.status }); });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$sort\":{\"t\":-1}},{\"$limit\":5},{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$sort\":{\"t\":-1}},{\"$limit\":5},{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"r\"}}]",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.sort({ t: -1 }).take(3).aggregate((o, _i, coll) => { $addFields({ n: coll.length }); });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.sort({ t: -1 }).take(3).aggregate((o, _i, coll) => { $addFields({ n: coll.length }); });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"t\":-1}},{\"$limit\":3},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$addFields\":{\"n\":\"$__jsmql.length\"}},{\"$unset\":\"__jsmql\"}],\"as\":\"__jsmql.tmp.1\"}",
      "now": "`coll` has no value inside `.aggregate()` — the collection is the stream itself; write '$$.length' for its size.",
      "reason": "message: the index and collection parameters of a callback have no value on a stream (`$$.length` is the stream's size); same refusals, reworded."
    },
    "pipeline\u0000$.n = $$$.orders.sort({ t: -1 }).aggregate((o) => { $group({ _id: o.status }); }).length;": {
      "entry": "pipeline",
      "src": "$.n = $$$.orders.sort({ t: -1 }).aggregate((o) => { $group({ _id: o.status }); }).length;",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"t\":-1}},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"n\":{\"$size\":\"$__jsmql.tmp.1\"}}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"t\":-1}},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"__jsmql.tmp.0\"}},{\"$set\":{\"n\":{\"$size\":\"$__jsmql.tmp.0\"}}},{\"$unset\":\"__jsmql\"}]",
      "reason": "equivalent: the scratch slots count from 0 in this compiler; the shipped compiler counted from 1."
    },
    "pipeline\u0000$.r = $$$.orders.aggregate((o) => { $sort({ total: -1 }); }).take(5);": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.aggregate((o) => { $sort({ total: -1 }); }).take(5);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":-1}},{\"$limit\":5}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":-1}},{\"$limit\":5}],\"as\":\"r\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.aggregate((o) => { $sort({ total: -1 }); }).pick([\"status\"]);": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.aggregate((o) => { $sort({ total: -1 }); }).pick([\"status\"]);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":-1}},{\"$project\":{\"status\":1,\"_id\":0}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":-1}},{\"$project\":{\"status\":1,\"_id\":0}}],\"as\":\"r\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.aggregate((o) => { $sort({ total: -1 }); }).omit([\"total\"]);": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.aggregate((o) => { $sort({ total: -1 }); }).omit([\"total\"]);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":-1}},{\"$project\":{\"total\":0}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":-1}},{\"$project\":{\"total\":0}}],\"as\":\"r\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.aggregate((o) => { $group({ _id: o.status, n: $sum(1) }); }).sort({ n: -1 });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.aggregate((o) => { $group({ _id: o.status, n: $sum(1) }); }).sort({ n: -1 });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$group\":{\"_id\":\"$status\",\"n\":{\"$sum\":1}}},{\"$sort\":{\"n\":-1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$group\":{\"_id\":\"$status\",\"n\":{\"$sum\":1}}},{\"$sort\":{\"n\":-1}}],\"as\":\"r\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.aggregate((o) => { $limit(3); }).aggregate((o) => { $group({ _id: o.status }); });": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.aggregate((o) => { $limit(3); }).aggregate((o) => { $group({ _id: o.status }); });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$limit\":3},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$limit\":3},{\"$group\":{\"_id\":\"$status\"}}],\"as\":\"r\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.aggregate((o) => { $sort({ total: -1 }); }).take(2).uniqBy(\"status\");": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.aggregate((o) => { $sort({ total: -1 }); }).take(2).uniqBy(\"status\");",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":-1}},{\"$limit\":2},{\"$group\":{\"_id\":\"$status\",\"__jsmqlTmp\":{\"$first\":\"$$ROOT\"}}},{\"$replaceWith\":\"$__jsmqlTmp\"}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql.tmp",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":-1}},{\"$limit\":2},{\"$group\":{\"_id\":\"$status\",\"__jsmqlTmp\":{\"$first\":\"$$ROOT\"}}},{\"$replaceWith\":\"$__jsmqlTmp\"}],\"as\":\"r\"}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.r = $$$.orders.sort({ total: -1 }).aggregate((o) => { $limit(2); }).uniqBy(\"status\");": {
      "entry": "pipeline",
      "src": "$.r = $$$.orders.sort({ total: -1 }).aggregate((o) => { $limit(2); }).uniqBy(\"status\");",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":-1}},{\"$limit\":2},{\"$group\":{\"_id\":\"$status\",\"__jsmqlTmp\":{\"$first\":\"$$ROOT\"}}},{\"$replaceWith\":\"$__jsmqlTmp\"}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"r\":\"$__jsmql.tmp",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"total\":-1}},{\"$limit\":2},{\"$group\":{\"_id\":\"$status\",\"__jsmqlTmp\":{\"$first\":\"$$ROOT\"}}},{\"$replaceWith\":\"$__jsmqlTmp\"}],\"as\":\"r\"}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.c.aggregate(o => o.v);": {
      "entry": "pipeline",
      "src": "$.x = $$$.c.aggregate(o => o.v);",
      "kind": "message",
      "was": ".aggregate(pipeline) needs a block body — write `$$$.c.aggregate((o) => { $match(...); $group(...); ... })` (each statement is a stage) — or pass a stage-array literal `[{ ... }, ...]`. An expression-body arrow isn't a p",
      "now": "'.aggregate()' takes an arrow whose body is a block of stages — 'o => { $match(…); $limit(1); }' — or a bracketed list of them.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$.c.aggregate((o, i) => { $match(o.n === i); });": {
      "entry": "pipeline",
      "src": "$.x = $$$.c.aggregate((o, i) => { $match(o.n === i); });",
      "kind": "message",
      "was": "'i' (the 2nd, index parameter) has no meaning inside '.aggregate((o, i, …) => …)' — MongoDB streams have no per-doc index. Keep it unused (e.g. '(o, _i, coll)') only to reach the 3rd 'collection' parameter.",
      "now": "`i` has no value inside `.aggregate()` — a stream has no per-document index; leave the parameter unused.",
      "reason": "message: the index and collection parameters of a callback have no value on a stream (`$$.length` is the stream's size); same refusals, reworded."
    },
    "pipeline\u0000$.x = $$$.c.aggregate((o, _i, coll) => { $match(o.n === coll[0]); });": {
      "entry": "pipeline",
      "src": "$.x = $$$.c.aggregate((o, _i, coll) => { $match(o.n === coll[0]); });",
      "kind": "message",
      "was": "In '.aggregate((o, _i, coll) => { … })', only 'coll.length' (the sub-stream count) is available — there's no materialised array to index or iterate inside the pipeline.",
      "now": "`coll` has no value inside `.aggregate()` — the collection is the stream itself; write '$$.length' for its size.",
      "reason": "message: the index and collection parameters of a callback have no value on a stream (`$$.length` is the stream's size); same refusals, reworded."
    },
    "pipeline\u0000$.x = $$$.c.aggregate([{ $sort: { a: 1 } }, ...$.more]);": {
      "entry": "pipeline",
      "src": "$.x = $$$.c.aggregate([{ $sort: { a: 1 } }, ...$.more]);",
      "kind": "message",
      "was": ".aggregate([...]) — a spread element isn't a pipeline stage. List the stages directly, e.g. `$$$.c.aggregate([{ $sort: { … } }, { $limit: 5 }])`.",
      "now": "A pipeline is written out stage by stage; '...' cannot spread stages into it. List each stage.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$.c.aggregate([]);": {
      "entry": "pipeline",
      "src": "$.x = $$$.c.aggregate([]);",
      "kind": "now-accepted",
      "was": ".aggregate([]) — an empty pipeline has nothing to run. List at least one stage, e.g. `$$$.c.aggregate([{ $sort: { … } }, { $limit: 5 }])`.",
      "now": "[{\"$lookup\":{\"from\":\"c\",\"pipeline\":[],\"as\":\"x\"}}]",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$ = $$$.c.aggregate((o) => { $group({ _id: \"$s\" }); });": {
      "entry": "pipeline",
      "src": "$ = $$$.c.aggregate((o) => { $group({ _id: \"$s\" }); });",
      "kind": "message",
      "was": "Cannot replace root with an array — '.aggregate(...)' returns an array. Use '.find(...)' for a single matching doc, or wrap: '$ = { items: $$$.<coll>.aggregate(...) }'.",
      "now": "The document can only become ONE document, and this chain gives an array. Write '$ = $$$.<coll>.find(pred)' for the first match, or keep the array in a field: '$.<field> = $$$.<coll>.…'.",
      "reason": "intended: `.find` gives ONE document and a stream is many; the root can only become ONE document (`$ = ….find(p)`, unwound — a document that found nothing leaves, where the shipped `$replaceWith: { $first }` failed on the server, measured). Same refusals, reworded, with the way out."
    },
    "pipeline\u0000$.x = $$$$.db.c.aggregate((o) => { $sort({ a: 1 }); });": {
      "entry": "pipeline",
      "src": "$.x = $$$$.db.c.aggregate((o) => { $sort({ a: 1 }); });",
      "kind": "message",
      "was": "Cross-database reads aren't supported: '$$$$.db.c' would emit a $lookup/$unionWith with a '{ db, coll }' namespace, which a standalone / replica-set / sharded MongoDB rejects (that shape is Atlas Data Federation only). R",
      "now": "A read of another DATABASE isn't supported: a '$lookup' joins a collection of the current database only (the '{ db, coll }' form is Atlas Data Federation's). Drop the '$$$$.<db>.' prefix — '$$$.<coll>.find(…)' — and run ",
      "reason": "message: same refusal — a cross-database read — reworded."
    },
    "pipeline\u0000$$$.c.aggregate((o) => { $limit(5); })": {
      "entry": "pipeline",
      "src": "$$$.c.aggregate((o) => { $limit(5); })",
      "kind": "message",
      "was": "jsmql.pipeline() expects a Pipeline (one or more aggregation stages — `;`-separated, a top-level stage call, or a stage-array literal), but received a bare expression that would lower to a Filter document. Call jsmql.fil",
      "now": "Reading another collection produces a value, and this statement gives it no destination. Assign it to a field ('$.<field> = $$$.<coll>.…'), bind it ('let x = $$$.<coll>.…'), or make it the stream ('$$ = $$$.<coll>.…').",
      "reason": "message: same refusal — a read of another collection with no destination — reworded; a system stage on a context reference is a verified pending until its road lands."
    },
    "pipeline\u0000$.x = $$$.c.find(o => o.x === 1).aggregate((o) => { $sort({ a: 1 }); });": {
      "entry": "pipeline",
      "src": "$.x = $$$.c.find(o => o.x === 1).aggregate((o) => { $sort({ a: 1 }); });",
      "kind": "message",
      "was": ".aggregate() on a .find() result is not meaningful — .find returns a scalar-or-null, not a collection to aggregate. Use .filter(pred).aggregate(...) to run a sub-pipeline over the matches.",
      "now": "'.aggregate()' produces pipeline stages, not a value.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$.orders.filter(o => o.v > 0).length.aggregate((o) => { $limit(1); });": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter(o => o.v > 0).length.aggregate((o) => { $limit(1); });",
      "kind": "message",
      "was": ".aggregate() on a .length result is not meaningful — .length reads a value off the result, not a collection to aggregate. Move .aggregate(...) ahead of it ('$$$.orders.aggregate((o) => { ... }).length'), or drop .length ",
      "now": "A read of another collection is a chain on '$$$.<coll>': '.find(pred)', '.filter(pred)', '.aggregate(o => { … })', a stream method or a stage link.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$.orders.filter(o => o.v > 0)[0].aggregate((o) => { $limit(1); });": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter(o => o.v > 0)[0].aggregate((o) => { $limit(1); });",
      "kind": "message",
      "was": ".aggregate() on a [0] result is not meaningful — [0] reads one element, not a collection to aggregate. Move .aggregate(...) ahead of it ('$$$.orders.aggregate((o) => { ... })[0]'), or drop [0] to aggregate the whole stre",
      "now": "A read of another collection is a chain on '$$$.<coll>': '.find(pred)', '.filter(pred)', '.aggregate(o => { … })', a stream method or a stage link.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$$.dw.orders.head().aggregate((o) => { $limit(1); });": {
      "entry": "pipeline",
      "src": "$.x = $$$$.dw.orders.head().aggregate((o) => { $limit(1); });",
      "kind": "message",
      "was": ".aggregate() on a .head() result is not meaningful — .head() returns a single value, not a collection to aggregate. Move .aggregate(...) ahead of it ('$$$$.dw.orders.aggregate((o) => { ... }).head()'), or drop .head() to",
      "now": "A read of another DATABASE isn't supported: a '$lookup' joins a collection of the current database only (the '{ db, coll }' form is Atlas Data Federation's). Drop the '$$$$.<db>.' prefix — '$$$.<coll>.find(…)' — and run ",
      "reason": "message: same refusal — a cross-database read — reworded."
    },
    "pipeline\u0000$.x = $$$.orders.filter(o => o.v > 0).sort({ t: 1 }).aggregate((o) => { $limit(1); });": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter(o => o.v > 0).sort({ t: 1 }).aggregate((o) => { $limit(1); });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"v\":{\"$gt\":0}}},{\"$sort\":{\"t\":1}},{\"$limit\":1}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"x\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"v\":{\"$gt\":0,\"$not\":{\"$type\":\"array\"}}}},{\"$sort\":{\"t\":1}},{\"$limit\":1}],\"as\":\"x\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$ = $$$.orders.aggregate((o) => { $match(o.userId === $._id); $group({ _id: \"$s\" }); });": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.aggregate((o) => { $match(o.userId === $._id); $group({ _id: \"$s\" }); });",
      "kind": "now-accepted",
      "was": "`$._id` (the outer document) isn't available inside `$$ = $$$.orders` — that source-switch replaces the stream with a different collection, so the original root document is gone (and `o._id` here would be the switched co",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$group\":{\"_id\":\"$s\"}}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"},{\"$replaceWith",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.c.filter(o => o.v > 1).aggregate(o => o.v);": {
      "entry": "pipeline",
      "src": "$.x = $$$.c.filter(o => o.v > 1).aggregate(o => o.v);",
      "kind": "message",
      "was": ".aggregate(pipeline) needs a block body — write `$$$.<coll>.aggregate((o) => { $match(...); $group(...); ... })` (each statement is a stage) — or pass a stage-array literal `[{ ... }, ...]`. An expression-body arrow isn'",
      "now": "'.aggregate()' takes an arrow whose body is a block of stages — 'o => { $match(…); $limit(1); }' — or a bracketed list of them.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.x = $$$.c.filter(o => o.v > 1).aggregate((o, i) => { $match(o.n === i); });": {
      "entry": "pipeline",
      "src": "$.x = $$$.c.filter(o => o.v > 1).aggregate((o, i) => { $match(o.n === i); });",
      "kind": "message",
      "was": "'i' (the 2nd, index parameter) has no meaning inside '.aggregate((o, i, …) => …)' — MongoDB streams have no per-doc index. Keep it unused (e.g. '(o, _i, coll)') only to reach the 3rd 'collection' parameter.",
      "now": "`i` has no value inside `.aggregate()` — a stream has no per-document index; leave the parameter unused.",
      "reason": "message: the index and collection parameters of a callback have no value on a stream (`$$.length` is the stream's size); same refusals, reworded."
    },
    "pipeline\u0000const top = $$$.orders.$match({ status: 'shipped' }).$sort({ total: -1 }).$limit(3);": {
      "entry": "pipeline",
      "src": "const top = $$$.orders.$match({ status: 'shipped' }).$sort({ total: -1 }).$limit(3);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"status\":\"shipped\"}},{\"$sort\":{\"total\":-1}},{\"$limit\":3}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.var.top\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"status\":\"shipped\"}},{\"$sort\":{\"total\":-1}},{\"$limit\":3}],\"as\":\"__jsmql.var.top\"}},{\"$unset\":\"__jsmql\"}]",
      "reason": "shippedBug: a nested join inside a predicate leaked `__jsmql.tmp` into the joined array (measured); the body's own chain now closes with its own `$unset`."
    },
    "pipeline\u0000$.t = $$$.orders.$set({ owner: $.tag });": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.$set({ owner: $.tag });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0_tag\":\"$tag\"},\"pipeline\":[{\"$set\":{\"owner\":\"$$jsmql_f0_tag\"}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"t\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0_tag\":\"$tag\"},\"pipeline\":[{\"$set\":{\"owner\":\"$$jsmql_f0_tag\"}}],\"as\":\"t\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.t = $$$.orders.$match({ userId: $._id });": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.$match({ userId: $._id });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"t\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"t\"}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.t = $$$.orders.filter({ userId: $._id });": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.filter({ userId: $._id });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"t\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"t\"}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.t = $$$.orders.filter({ userId: $._id, status: \"shipped\" });": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.filter({ userId: $._id, status: \"shipped\" });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"status\":\"shipped\",\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"t\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"status\":{\"$eq\":\"shipped\",\"$not\":{\"$type\":\"array\"}},\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"t\"}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.t = $$$.orders.$match({ qty: { $gt: 5 } });": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.$match({ qty: { $gt: 5 } });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"qty\":{\"$gt\":5}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"t\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"qty\":{\"$gt\":5}}}],\"as\":\"t\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.t = $$$.orders.$match({ createdAt: { $gte: $.since } });": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.$match({ createdAt: { $gte: $.since } });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0_since\":\"$since\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$gte\":[\"$createdAt\",\"$$jsmql_f0_since\"]}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"t\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0_since\":\"$since\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$gte\":[\"$createdAt\",\"$$jsmql_f0_since\"]}}}],\"as\":\"t\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.t = $$$.orders.filter({ status: \"shipped\" });": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.filter({ status: \"shipped\" });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"status\":\"shipped\"}}],\"as\":\"t\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"status\":{\"$eq\":\"shipped\",\"$not\":{\"$type\":\"array\"}}}}],\"as\":\"t\"}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.t = $$$.orders.$match({ $expr: { $eq: [\"$userId\", $._id] } });": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.$match({ $expr: { $eq: [\"$userId\", $._id] } });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"t\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"t\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.t = $$$.orders.$out('archive');": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.$out('archive');",
      "kind": "message",
      "was": "'$out' is not allowed inside a '$lookup' sub-pipeline. Move it to the outer (top-level) pipeline.",
      "now": "'$out' cannot stand inside '$lookup' — the server refuses this stage in that body. Run it as a stage of the outer pipeline instead.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.t = $$$.orders.$merge({ into: 'archive' });": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.$merge({ into: 'archive' });",
      "kind": "message",
      "was": "'$merge' is not allowed inside a '$lookup' sub-pipeline. Move it to the outer (top-level) pipeline.",
      "now": "'$merge' cannot stand inside '$lookup' — the server refuses this stage in that body. Run it as a stage of the outer pipeline instead.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.t = $$$.orders.$match({ a: 1 }).$documents([{ x: 1 }]);": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.$match({ a: 1 }).$documents([{ x: 1 }]);",
      "kind": "message",
      "was": "'$documents' must be the first stage in a pipeline — it produces the pipeline's source documents, so nothing can run before it. Move it to the front, or remove the stage(s) that precede it.",
      "now": "'$documents' cannot stand inside '$lookup' — the server refuses this stage in that body. Run it as a stage of the outer pipeline instead.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.t = $$$.orders.$sortt({ a: 1 });": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.$sortt({ a: 1 });",
      "kind": "message",
      "was": "'$sortt' is not a known aggregation stage. Did you mean '$sort'?",
      "now": "Unknown method '.$sortt()' at position 16. Did you mean '.sort()'?",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.orders = $$$[\"my-orders\"].filter(o => o.userId === $._id);": {
      "entry": "pipeline",
      "src": "$.orders = $$$[\"my-orders\"].filter(o => o.userId === $._id);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"my-orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"orders\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"my-orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"orders\"}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.x = $$$.orders.filter(o => o.ref === $[\"ext-code\"]);": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter(o => o.ref === $[\"ext-code\"]);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"ext-code\",\"foreignField\":\"ref\",\"as\":\"x\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0_ext_code\":\"$ext-code\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$ref\",\"$$jsmql_f0_ext_code\"]}}}],\"as\":\"x\"}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000\n        $.a = $$$.A.aggregate(a => {\n          $.c = $$$.C.aggregate(c => {\n            $match(c.aId === a._id);\n            assert(c.region === $.region, \"region mismatch\");\n          });\n        });\n      ": {
      "entry": "pipeline",
      "src": "\n        $.a = $$$.A.aggregate(a => {\n          $.c = $$$.C.aggregate(c => {\n            $match(c.aId === a._id);\n            assert(c.region === $.region, \"region mismatch\");\n          });\n        });\n      ",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"A\",\"let\":{\"jsmql_f0_region\":\"$region\"},\"pipeline\":[{\"$lookup\":{\"from\":\"C\",\"let\":{\"jsmql_f1__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$aId\",\"$$jsmql_f1__id\"]}}},{\"$match\":{\"$expr\":{\"$conver",
      "now": "The outer document can't be written from inside a body over another collection — only read. Write the body's own document through its callback parameter ('o.x = …', 'delete o.x', 'o = { … }'), or as a stage ('$set({ x: …",
      "reason": "intended: `$.` is the OUTER document at every depth (HR4) and cannot be written from a body over another collection; the body's own document is its callback parameter (`o.x = …`, `o = { … }`) or a stage (`$set(…)`). The shipped compiler wrote the FOREIGN document for `$.x =` there — the one exception to the ruling."
    },
    "pipeline\u0000$.x = $$$.orders.aggregate((o, i, c) => { $match(o.userId === $._id); $.first = c[0]; });": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.aggregate((o, i, c) => { $match(o.userId === $._id); $.first = c[0]; });",
      "kind": "message",
      "was": "In '.aggregate((o, _i, c) => { … })', only 'c.length' (the sub-stream count) is available — there's no materialised array to index or iterate inside the pipeline.",
      "now": "The outer document can't be written from inside a body over another collection — only read. Write the body's own document through its callback parameter ('o.x = …', 'delete o.x', 'o = { … }'), or as a stage ('$set({ x: …",
      "reason": "intended: `$.` is the OUTER document at every depth (HR4) and cannot be written from a body over another collection; the body's own document is its callback parameter (`o.x = …`, `o = { … }`) or a stage (`$set(…)`). The shipped compiler wrote the FOREIGN document for `$.x =` there — the one exception to the ruling."
    },
    "pipeline\u0000$.x = $$$.orders.filter((o, i, c) => c.length > 0);": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.filter((o, i, c) => c.length > 0);",
      "kind": "message",
      "was": "'c' (the array parameter) has no meaning on a '.filter' predicate — the filtered sub-stream doesn't exist yet while the predicate runs. For its count, use `.aggregate` and its 3rd param, e.g. `$$$.orders.aggregate((o, _i",
      "now": "`c` has no value inside `.filter()` — the collection is the stream itself; write '$$.length' for its size.",
      "reason": "message: the index and collection parameters of a callback have no value on a stream (`$$.length` is the stream's size); same refusals, reworded."
    },
    "pipeline\u0000\n      let nOrders = $$$.orders.filter(o => o.userId === $._id).length;\n      let nTx = $$$.tx.filter(t => t.userId === $._id).length;\n    ": {
      "entry": "pipeline",
      "src": "\n      let nOrders = $$$.orders.filter(o => o.userId === $._id).length;\n      let nTx = $$$.tx.filter(t => t.userId === $._id).length;\n    ",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$size\":\"$__jsmql.tmp.1\"}}},{\"$set\":{\"__jsmql.var.nOrders\":\"$__jsmql.tmp.1\"}},{\"$lookup\":{\"from\":\"t",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$set\":{\"__jsmql.var.nOrders\":{\"$size\":\"$__jsmql.tmp.0\"}}},{\"$look",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000\n      $.a = 1;\n      $.orders = $$$.orders.filter(o => o.userId === $._id);\n      $.b = 2;\n    ": {
      "entry": "pipeline",
      "src": "\n      $.a = 1;\n      $.orders = $$$.orders.filter(o => o.userId === $._id);\n      $.b = 2;\n    ",
      "kind": "output",
      "was": "[{\"$set\":{\"a\":1}},{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"orders\"}},{\"$set\":{\"b\":2}}]",
      "now": "[{\"$set\":{\"a\":1}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"orders\"}},{\"$set\":{\"b\":2}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000[ $ = $$$.users.find(u => u._id === $.userId) ]": {
      "entry": "pipeline",
      "src": "[ $ = $$$.users.find(u => u._id === $.userId) ]",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"localField\":\"userId\",\"foreignField\":\"_id\",\"as\":\"__jsmql.tmp.1\"}},{\"$replaceWith\":{\"$first\":\"$__jsmql.tmp.1\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0_userId\":\"$userId\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_f0_userId\"]}}},{\"$limit\":1}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"},{\"$replaceWith\":\"$__",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000[ $ = $$$$.analytics.users.find(u => u._id === $.userId) ]": {
      "entry": "pipeline",
      "src": "[ $ = $$$$.analytics.users.find(u => u._id === $.userId) ]",
      "kind": "message",
      "was": "Cross-database reads aren't supported: '$$$$.analytics.users' would emit a $lookup/$unionWith with a '{ db, coll }' namespace, which a standalone / replica-set / sharded MongoDB rejects (that shape is Atlas Data Federati",
      "now": "A read of another DATABASE isn't supported: a '$lookup' joins a collection of the current database only (the '{ db, coll }' form is Atlas Data Federation's). Drop the '$$$$.<db>.' prefix — '$$$.<coll>.find(…)' — and run ",
      "reason": "message: same refusal — a cross-database read — reworded."
    },
    "pipeline\u0000[ $ = $$$.users.filter(u => u.active) ]": {
      "entry": "pipeline",
      "src": "[ $ = $$$.users.filter(u => u.active) ]",
      "kind": "message",
      "was": "Cannot replace root with an array — '.filter(...)' returns an array. Use '.find(...)' for a single matching doc, or wrap: '$ = { items: $$$.<coll>.filter(...) }'.",
      "now": "The document can only become ONE document, and this chain gives an array. Write '$ = $$$.<coll>.find(pred)' for the first match, or keep the array in a field: '$.<field> = $$$.<coll>.…'.",
      "reason": "intended: `.find` gives ONE document and a stream is many; the root can only become ONE document (`$ = ….find(p)`, unwound — a document that found nothing leaves, where the shipped `$replaceWith: { $first }` failed on the server, measured). Same refusals, reworded, with the way out."
    },
    "pipeline\u0000$$ = $$$.orders.head();": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.head();",
      "kind": "message",
      "was": "'.head(...)' returns a single value, not a stream — it collapses '$$$.<coll>' to one value, so it's only valid in a VALUE position: 'const x = $$$.<coll>.head(...)' or '$.field = $$$.<coll>.head(...)', not as a '$$ = …' ",
      "now": "'.head()' makes a value, and the stream must stay documents. Assign the value to a field instead: '$.<field> = $$$.<coll>.….head()'.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$$$.orders.head();": {
      "entry": "pipeline",
      "src": "$$$.orders.head();",
      "kind": "message",
      "was": "Element 0 of pipeline is '.head(...)', which returns a single value, not a pipeline stage. Assign it to a field or a binding — '$.field = <expr>' or 'const x = <expr>'.",
      "now": "Reading another collection produces a value, and this statement gives it no destination. Assign it to a field ('$.<field> = $$$.<coll>.…'), bind it ('let x = $$$.<coll>.…'), or make it the stream ('$$ = $$$.<coll>.…').",
      "reason": "message: same refusal — a read of another collection with no destination — reworded; a system stage on a context reference is a verified pending until its road lands."
    },
    "pipeline\u0000$$ = $$$.orders.toSorted({ createdAt: -1 }).take(200).filter(o => o.qty > 1);": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.toSorted({ createdAt: -1 }).take(200).filter(o => o.qty > 1);",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"orders\",\"pipeline\":[{\"$sort\":{\"createdAt\":-1}},{\"$limit\":200},{\"$match\":{\"qty\":{\"$gt\":1}}}]}}]",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"orders\",\"pipeline\":[{\"$sort\":{\"createdAt\":-1}},{\"$limit\":200},{\"$match\":{\"qty\":{\"$gt\":1,\"$not\":{\"$type\":\"array\"}}}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$$.orders.toSorted({ createdAt: -1 }).filter(o => o.userId === $._id);": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.toSorted({ createdAt: -1 }).filter(o => o.userId === $._id);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$sort\":{\"createdAt\":-1}},{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$unwind\":\"$__jsmql.tmp.1\"},{\"$replaceW",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$sort\":{\"createdAt\":-1}},{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"},{\"$replaceW",
      "reason": "equivalent: the scratch slots count from 0 in this compiler; the shipped compiler counted from 1."
    },
    "pipeline\u0000$$ = $$$.orders.toSorted({ createdAt: -1 }).filter(o => o.userId === $._id).size();": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.toSorted({ createdAt: -1 }).filter(o => o.userId === $._id).size();",
      "kind": "message",
      "was": "'.size(...)' returns a single value, not a stream — it collapses '$$$.<coll>' to one value, so it's only valid in a VALUE position: 'const x = $$$.<coll>.size(...)' or '$.field = $$$.<coll>.size(...)', not as a '$$ = …' ",
      "now": "'.size()' makes a value, and the stream must stay documents. Assign the value to a field instead: '$.<field> = $$$.<coll>.….size()'.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$$ = $$$.orders.map(o => ({ v: $.length }));": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.map(o => ({ v: $.length }));",
      "kind": "now-accepted",
      "was": "`$.length` (the outer document) isn't available inside `$$ = $$$.orders` — that source-switch replaces the stream with a different collection, so the original root document is gone (and `o.length` here would be the switc",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0_length\":\"$length\"},\"pipeline\":[{\"$replaceWith\":{\"v\":\"$$jsmql_f0_length\"}}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"},{\"$replaceWith\":\"$__jsmql.tmp.0\"}]",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$ = $$$.orders.filter({ userId: $._id });": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.filter({ userId: $._id });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.1\"}},{\"$unwind\":\"$__jsmql.tmp.1\"},{\"$replaceWith\":\"$__jsmql.tmp.1\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"},{\"$replaceWith\":\"$__jsmql.tmp.0\"}]",
      "reason": "equivalent: the scratch slots count from 0 in this compiler; the shipped compiler counted from 1."
    },
    "pipeline\u0000$$ = $$$.orders.map(o => o.total);": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.map(o => o.total);",
      "kind": "message",
      "was": "'.map(...)' in a '$$ = $$$.<coll>.…' stream must return a DOCUMENT — the mapped result becomes the new document stream, which can't hold bare scalars/arrays. Reshape into a document with '.map(o => ({ … }))', or collect ",
      "now": "'.map()' makes a value, and the stream must stay documents. Assign the value to a field instead: '$.<field> = $$$.<coll>.….map()'.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000[ { $lookup: { from: 'c', as: 'r', pipeline: [ $match($.a > 0), { $geoNear: { near: [0, 0], distanceField: 'd' } } ] } } ]": {
      "entry": "pipeline",
      "src": "[ { $lookup: { from: 'c', as: 'r', pipeline: [ $match($.a > 0), { $geoNear: { near: [0, 0], distanceField: 'd' } } ] } } ]",
      "kind": "message",
      "was": "'$geoNear' must be the first stage in a pipeline — it produces the pipeline's source documents, so nothing can run before it. Move it to the front, or remove the stage(s) that precede it.",
      "now": "'$geoNear' produces the pipeline's source documents, so it has to be the FIRST stage — the server refuses it anywhere else. Move it to the top of the program.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.t = $$$.orders.aggregate(o => { $merge({ into: \"m\" }); });": {
      "entry": "pipeline",
      "src": "$.t = $$$.orders.aggregate(o => { $merge({ into: \"m\" }); });",
      "kind": "message",
      "was": "'$merge' is not allowed inside a '$lookup' sub-pipeline. Move it to the outer (top-level) pipeline.",
      "now": "'$merge' cannot stand inside '$lookup' — the server refuses this stage in that body. Run it as a stage of the outer pipeline instead.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$$$.orders.$match({ a: 1 });": {
      "entry": "pipeline",
      "src": "$$$.orders.$match({ a: 1 });",
      "kind": "message",
      "was": "A '$$$.orders.<chain>' statement has no destination — reading another collection produces a value, so it needs somewhere to go. Assign it to a field ('$.<field> = $$$.orders.…'), bind it ('const <name> = $$$.orders.…'), ",
      "now": "Reading another collection produces a value, and this statement gives it no destination. Assign it to a field ('$.<field> = $$$.<coll>.…'), bind it ('let x = $$$.<coll>.…'), or make it the stream ('$$ = $$$.<coll>.…').",
      "reason": "message: same refusal — a read of another collection with no destination — reworded; a system stage on a context reference is a verified pending until its road lands."
    },
    "pipeline\u0000$$$.orders.filter({ a: 1 }).take(2);": {
      "entry": "pipeline",
      "src": "$$$.orders.filter({ a: 1 }).take(2);",
      "kind": "message",
      "was": "A '$$$.orders.<chain>' statement has no destination — reading another collection produces a value, so it needs somewhere to go. Assign it to a field ('$.<field> = $$$.orders.…'), bind it ('const <name> = $$$.orders.…'), ",
      "now": "Reading another collection produces a value, and this statement gives it no destination. Assign it to a field ('$.<field> = $$$.<coll>.…'), bind it ('let x = $$$.<coll>.…'), or make it the stream ('$$ = $$$.<coll>.…').",
      "reason": "message: same refusal — a read of another collection with no destination — reworded; a system stage on a context reference is a verified pending until its road lands."
    },
    "pipeline\u0000$$$$.other.orders.$match({ a: 1 });": {
      "entry": "pipeline",
      "src": "$$$$.other.orders.$match({ a: 1 });",
      "kind": "message",
      "was": "A '$$$$.other.orders.<chain>' statement has no destination — reading another collection produces a value, so it needs somewhere to go. Assign it to a field ('$.<field> = $$$$.other.orders.…'), bind it ('const <name> = $$",
      "now": "Reading another collection produces a value, and this statement gives it no destination. Assign it to a field ('$.<field> = $$$.<coll>.…'), bind it ('let x = $$$.<coll>.…'), or make it the stream ('$$ = $$$.<coll>.…').",
      "reason": "message: same refusal — a read of another collection with no destination — reworded; a system stage on a context reference is a verified pending until its road lands."
    },
    "pipeline\u0000$$ = $$$.orders.take(2).filter(o => o.a, 2);": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.take(2).filter(o => o.a, 2);",
      "kind": "message",
      "was": "'$$$.orders.filter(<predicate>)' takes exactly one predicate argument, got 2.",
      "now": "'.filter()'(predicate) requires exactly 1 argument, got 2",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$unionWith({ coll: \"c\", pipeline: [$.o = $$$.orders.find(o => o.uid === 1)] });": {
      "entry": "pipeline",
      "src": "$unionWith({ coll: \"c\", pipeline: [$.o = $$$.orders.find(o => o.uid === 1)] });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$unionWith\":{\"coll\":\"c\",\"pipeline\":[{\"$set\":{\"o\":\"$__jsmql.tmp.1\"}}]}},{\"$uns",
      "now": "The outer document can't be written from inside a body over another collection — only read. Write the body's own document through its callback parameter ('o.x = …', 'delete o.x', 'o = { … }'), or as a stage ('$set({ x: …",
      "reason": "intended: `$.` is the OUTER document at every depth (HR4) and cannot be written from a body over another collection; the body's own document is its callback parameter (`o.x = …`, `o = { … }`) or a stage (`$set(…)`). The shipped compiler wrote the FOREIGN document for `$.x =` there — the one exception to the ruling."
    },
    "pipeline\u0000$lookup({ from: \"o\", pipeline: [$.x = $$$.items.find(i => i.k === 1)], as: \"o\" });": {
      "entry": "pipeline",
      "src": "$lookup({ from: \"o\", pipeline: [$.x = $$$.items.find(i => i.k === 1)], as: \"o\" });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"items\",\"pipeline\":[{\"$match\":{\"k\":1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$lookup\":{\"from\":\"o\",\"pipeline\":[{\"$set\":{\"x\":\"$__jsmql.tmp.1\"}}],\"as\":\"o\"}},{\"$",
      "now": "The outer document can't be written from inside a body over another collection — only read. Write the body's own document through its callback parameter ('o.x = …', 'delete o.x', 'o = { … }'), or as a stage ('$set({ x: …",
      "reason": "intended: `$.` is the OUTER document at every depth (HR4) and cannot be written from a body over another collection; the body's own document is its callback parameter (`o.x = …`, `o = { … }`) or a stage (`$set(…)`). The shipped compiler wrote the FOREIGN document for `$.x =` there — the one exception to the ruling."
    },
    "pipeline\u0000$facet({ a: [$.o = $$$.orders.find(o => o.uid === 1)] });": {
      "entry": "pipeline",
      "src": "$facet({ a: [$.o = $$$.orders.find(o => o.uid === 1)] });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$facet\":{\"a\":[{\"$set\":{\"o\":\"$__jsmql.tmp.1\"}}]}}]",
      "now": "[{\"$facet\":{\"a\":[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":{\"$eq\":1,\"$not\":{\"$type\":\"array\"}}}},{\"$limit\":1}],\"as\":\"o\"}},{\"$set\":{\"o\":{\"$first\":\"$o\"}}}]}}]",
      "reason": "intended: `.find` bounds its body with `$limit: 1` — the first match is all it keeps, and the server examines one index key per document instead of every match. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000[{\n        $lookup: {\n          from: \"orders\",\n          let: { uid: $._id },\n          pipeline: [\n            { $match: $.userId === 42 },\n            { $project: { total: 1 } }\n          ],\n          as: \"userOrders\"\n        }\n      }]": {
      "entry": "pipeline",
      "src": "[{\n        $lookup: {\n          from: \"orders\",\n          let: { uid: $._id },\n          pipeline: [\n            { $match: $.userId === 42 },\n            { $project: { total: 1 } }\n          ],\n          as: \"userOrders\"\n        }\n      }]",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"uid\":\"$_id\"},\"pipeline\":[{\"$match\":{\"userId\":42}},{\"$project\":{\"total\":1}}],\"as\":\"userOrders\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"uid\":\"$_id\",\"jsmql_f0_userId\":\"$userId\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$$jsmql_f0_userId\",42]}}},{\"$project\":{\"total\":1}}],\"as\":\"userOrders\"}}]",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000[{ $unionWith: { coll: \"archive\", pipeline: [{ $match: $.year < 2020 }] } }]": {
      "entry": "pipeline",
      "src": "[{ $unionWith: { coll: \"archive\", pipeline: [{ $match: $.year < 2020 }] } }]",
      "kind": "now-rejected",
      "was": "[{\"$unionWith\":{\"coll\":\"archive\",\"pipeline\":[{\"$match\":{\"year\":{\"$lt\":2020}}}]}}]",
      "now": "'$unionWith' has no 'let': its body cannot read the outer document or a binding declared outside it. Filter or reshape the outer stream in a statement before it, or read the other collection through a join ('$.<field> = ",
      "reason": "intended: `$unionWith` has no `let` (measured: IDLUnknownField), so a read of the outer document inside its body is refused (HR4); the shipped compiler read the foreign document there."
    },
    "pipeline\u0000$$ = $$$.transactions.filter(t => t.createdAt >= new Date(\"2026-01-01\"));": {
      "entry": "pipeline",
      "src": "$$ = $$$.transactions.filter(t => t.createdAt >= new Date(\"2026-01-01\"));",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"transactions\",\"pipeline\":[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"}}}]}}]",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"transactions\",\"pipeline\":[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}}}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$$.transactions.aggregate(t => { $match(t.amount > 100); $sort({ amount: -1 }); $limit(5); });": {
      "entry": "pipeline",
      "src": "$$ = $$$.transactions.aggregate(t => { $match(t.amount > 100); $sort({ amount: -1 }); $limit(5); });",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"transactions\",\"pipeline\":[{\"$match\":{\"amount\":{\"$gt\":100}}},{\"$sort\":{\"amount\":-1}},{\"$limit\":5}]}}]",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"transactions\",\"pipeline\":[{\"$match\":{\"amount\":{\"$gt\":100,\"$not\":{\"$type\":\"array\"}}}},{\"$sort\":{\"amount\":-1}},{\"$limit\":5}]}}]",
      "reason": "intended: the array ruling reaches a `$match` body, because a stage's predicate lowers through the same filter target. See the SR2 paragraph in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$$$.analytics.events.filter(e => e.type === \"purchase\");": {
      "entry": "pipeline",
      "src": "$$ = $$$$.analytics.events.filter(e => e.type === \"purchase\");",
      "kind": "message",
      "was": "Cross-database reads aren't supported: '$$$$.analytics.events' would emit a $lookup/$unionWith with a '{ db, coll }' namespace, which a standalone / replica-set / sharded MongoDB rejects (that shape is Atlas Data Federat",
      "now": "A read of another DATABASE isn't supported: a '$lookup' joins a collection of the current database only (the '{ db, coll }' form is Atlas Data Federation's). Drop the '$$$$.<db>.' prefix — '$$$.<coll>.find(…)' — and run ",
      "reason": "message: same refusal — a cross-database read — reworded."
    },
    "pipeline\u0000let cutoff = $.min; $$ = $$$.t.filter(o => true); $.flagged = cutoff;": {
      "entry": "pipeline",
      "src": "let cutoff = $.min; $$ = $$$.t.filter(o => true); $.flagged = cutoff;",
      "kind": "message",
      "was": "`cutoff` is a `let` binding and can't be read after `$unionWith` — the stage replaces the document. Inline the expression into the `$unionWith` body, or rebind after the stage with another `let`.",
      "now": "`cutoff` is a `let` binding and can't be read after `$unionWith` — that stage replaced the document that carried it. Assign it again after the stage (`cutoff = …`), or carry the value as a field of the new document.",
      "reason": "intended: `$unionWith` states `replacesDocument` — the stream after it is another collection's documents, or a mix, so a `let` carried in a field is not reliable there; the shipped compiler refused the same read with other words."
    },
    "pipeline\u0000const k = $.min + 1; $$ = $$$.orders.map(o => ({ v: k }));": {
      "entry": "pipeline",
      "src": "const k = $.min + 1; $$ = $$$.orders.map(o => ({ v: k }));",
      "kind": "now-accepted",
      "was": "`k` is a `let`/`const` declared before `$$ = $$$.orders`, which replaces the stream with a different collection (a `$unionWith`, which can't correlate) — so `k` (along with the outer document and the root `$$.length`) is",
      "now": "[{\"$set\":{\"__jsmql.var.k\":{\"$add\":[\"$min\",1]}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_v0_k\":\"$__jsmql.var.k\"},\"pipeline\":[{\"$replaceWith\":{\"v\":\"$$jsmql_v0_k\"}}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"},{\"$",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$ = $$$.users.find(u => u.active);": {
      "entry": "pipeline",
      "src": "$$ = $$$.users.find(u => u.active);",
      "kind": "message",
      "was": "'.find(...)' is not allowed in a chain on '$$$.<coll>' — '.find' returns a single element in JS, but pipelines are arrays. Use '$$$.<coll>.filter(<pred>).slice(0, 1)' for the equivalent \"first match\" / \"n-th\" shape. (For",
      "now": "'.find(…)' gives ONE document, and the stream is many. Write '$$ = $$$.<coll>.filter(pred).take(1)' for a stream of the first match, or '$ = $$$.<coll>.find(pred)' to make each document the one it finds.",
      "reason": "intended: `.find` gives ONE document and a stream is many; the root can only become ONE document (`$ = ….find(p)`, unwound — a document that found nothing leaves, where the shipped `$replaceWith: { $first }` failed on the server, measured). Same refusals, reworded, with the way out."
    },
    "pipeline\u0000$$ = $$$.transactions;": {
      "entry": "pipeline",
      "src": "$$ = $$$.transactions;",
      "kind": "now-accepted",
      "was": "'$$ = …' RHS must be '$$.<streamMethod>…' (narrow/transform the current stream) or '$$$.<coll>.<streamMethod>…' (switch source to another collection). Any lodash stream method may head the chain (e.g. '.filter', '.toSort",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":\"transactions\"}]",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$ = $$$.orders.filter(o => o.userId === $._id)": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.filter(o => o.userId === $._id)",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"__jsmql.tmp.1\"}},{\"$unwind\":\"$__jsmql.tmp.1\"},{\"$replaceWith\":\"$__jsmql.tmp.1\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"},{\"$replaceWith\":\"$__jsmql.tmp.0\"}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$ = $$$.users.filter(u => u._id === $.userId);": {
      "entry": "pipeline",
      "src": "$$ = $$$.users.filter(u => u._id === $.userId);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"localField\":\"userId\",\"foreignField\":\"_id\",\"as\":\"__jsmql.tmp.1\"}},{\"$unwind\":\"$__jsmql.tmp.1\"},{\"$replaceWith\":\"$__jsmql.tmp.1\"}]",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0_userId\":\"$userId\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_f0_userId\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"},{\"$replaceWith\":\"$__jsmql.tmp.0\"}",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$ = $$$.users.filter(u => u._id === $.userId).slice(0, 1);": {
      "entry": "pipeline",
      "src": "$$ = $$$.users.filter(u => u._id === $.userId).slice(0, 1);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0_userId\":\"$userId\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_f0_userId\"]}}},{\"$limit\":1}],\"as\":\"__jsmql.tmp.1\"}},{\"$unwind\":\"$__jsmql.tmp.1\"},{\"$replaceWith\":\"$__",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_f0_userId\":\"$userId\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_f0_userId\"]}}},{\"$limit\":1}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"},{\"$replaceWith\":\"$__",
      "reason": "equivalent: the scratch slots count from 0 in this compiler; the shipped compiler counted from 1."
    },
    "pipeline\u0000$$ = $$$.orders.filter(o => o.userId === $._id).toSorted((a, b) => b.placedAt - a.placedAt).slice(0, 5);": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.filter(o => o.userId === $._id).toSorted((a, b) => b.placedAt - a.placedAt).slice(0, 5);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"placedAt\":-1}},{\"$limit\":5}],\"as\":\"__jsmql.tmp.1\"}},{\"$unwind\":\"$__jsmql.tmp.1\"}",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"placedAt\":-1}},{\"$limit\":5}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"}",
      "reason": "equivalent: the scratch slots count from 0 in this compiler; the shipped compiler counted from 1."
    },
    "pipeline\u0000$$ = $$$.events.filter(e => e.userId === $._id && e.region === $.region);": {
      "entry": "pipeline",
      "src": "$$ = $$$.events.filter(e => e.userId === $._id && e.region === $.region);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"events\",\"let\":{\"jsmql_f0__id\":\"$_id\",\"jsmql_f0_region\":\"$region\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]},{\"$eq\":[\"$region\",\"$$jsmql_f0_region\"]}]}}}],\"as\":\"__jsm",
      "now": "[{\"$lookup\":{\"from\":\"events\",\"let\":{\"jsmql_f0__id\":\"$_id\",\"jsmql_f0_region\":\"$region\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]},{\"$eq\":[\"$region\",\"$$jsmql_f0_region\"]}]}}}],\"as\":\"__jsm",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$ = $$$.orders.filter(o => o.userId === $._id).map(o => ({ pid: o.productId }));": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.filter(o => o.userId === $._id).map(o => ({ pid: o.productId }));",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$replaceWith\":{\"pid\":\"$productId\"}}],\"as\":\"__jsmql.tmp.1\"}},{\"$unwind\":\"$__jsmql.tmp.1\"},",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$replaceWith\":{\"pid\":\"$productId\"}}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"},",
      "reason": "equivalent: the scratch slots count from 0 in this compiler; the shipped compiler counted from 1."
    },
    "pipeline\u0000$$ = $$$.orders.filter(o => o.userId === $._id).map(\"productId\");": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.filter(o => o.userId === $._id).map(\"productId\");",
      "kind": "message",
      "was": "'.map(...)' in a '$$ = $$$.<coll>.…' stream must return a DOCUMENT — the mapped result becomes the new document stream, which can't hold bare scalars/arrays. Reshape into a document with '.map(o => ({ … }))', or collect ",
      "now": "'.map()' makes a value, and the stream must stay documents. Assign the value to a field instead: '$.<field> = $$$.<coll>.….map()'.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$$ = $$$.orders.filter(o => o.userId === $._id).map(o => o.productId);": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.filter(o => o.userId === $._id).map(o => o.productId);",
      "kind": "message",
      "was": "'.map(...)' in a '$$ = $$$.<coll>.…' stream must return a DOCUMENT — the mapped result becomes the new document stream, which can't hold bare scalars/arrays. Reshape into a document with '.map(o => ({ … }))', or collect ",
      "now": "'.map()' makes a value, and the stream must stay documents. Assign the value to a field instead: '$.<field> = $$$.<coll>.….map()'.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$$ = $$$.orders.filter(o => o.userId === $._id).map(\"productId\").take(5);": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.filter(o => o.userId === $._id).map(\"productId\").take(5);",
      "kind": "message",
      "was": "'.map(...)' in a '$$ = $$$.<coll>.…' stream must return a DOCUMENT — the mapped result becomes the new document stream, which can't hold bare scalars/arrays. Reshape into a document with '.map(o => ({ … }))', or collect ",
      "now": "'.map()' makes a value, and the stream must stay documents. Assign the value to a field instead: '$.<field> = $$$.<coll>.….map()'.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$$ = $$$$.analytics.events.filter(e => e.userId === $._id);": {
      "entry": "pipeline",
      "src": "$$ = $$$$.analytics.events.filter(e => e.userId === $._id);",
      "kind": "message",
      "was": "Cross-database reads aren't supported: '$$$$.analytics.events' would emit a $lookup/$unionWith with a '{ db, coll }' namespace, which a standalone / replica-set / sharded MongoDB rejects (that shape is Atlas Data Federat",
      "now": "A read of another DATABASE isn't supported: a '$lookup' joins a collection of the current database only (the '{ db, coll }' form is Atlas Data Federation's). Drop the '$$$$.<db>.' prefix — '$$$.<coll>.find(…)' — and run ",
      "reason": "message: same refusal — a cross-database read — reworded."
    },
    "pipeline\u0000$.byStatus = $$$.orders.filter(o => o.userId === $._id).countBy(\"status\");": {
      "entry": "pipeline",
      "src": "$.byStatus = $$$.orders.filter(o => o.userId === $._id).countBy(\"status\");",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$group\":{\"_id\":\"$status\",\"__jsmqlTmp\":{\"$sum\":1}}},{\"$group\":{\"_id\":null,\"__jsmqlTmp\":{\"$",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$group\":{\"_id\":\"$status\",\"__jsmqlTmp\":{\"$sum\":1}}},{\"$group\":{\"_id\":null,\"__jsmqlTmp\":{\"$",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.out = $$$.orders.find(o => o.userId === $._id).total;": {
      "entry": "pipeline",
      "src": "$.out = $$$.orders.find(o => o.userId === $._id).total;",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$set\":{\"out\":\"$__jsmql.tmp.1.total\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$limit\":1}],\"as\":\"__jsmql.tmp.0\"}},{\"$set\":{\"__jsmql.tmp.0\":{\"$first\":\"$__jsmql.tmp.0\"}}}",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$ = $$$.users.filter(u => u.active === true);": {
      "entry": "pipeline",
      "src": "$$ = $$$.users.filter(u => u.active === true);",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"users\",\"pipeline\":[{\"$match\":{\"active\":true}}]}}]",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"users\",\"pipeline\":[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$$.users.filter(u => u.active === true).slice(0, 10);": {
      "entry": "pipeline",
      "src": "$$ = $$$.users.filter(u => u.active === true).slice(0, 10);",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"users\",\"pipeline\":[{\"$match\":{\"active\":true}},{\"$limit\":10}]}}]",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"users\",\"pipeline\":[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}},{\"$limit\":10}]}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000let uid = $.userId; $$ = $$$.users.filter(u => u._id === uid);": {
      "entry": "pipeline",
      "src": "let uid = $.userId; $$ = $$$.users.filter(u => u._id === uid);",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.uid\":\"$userId\"}},{\"$lookup\":{\"from\":\"users\",\"localField\":\"__jsmql.var.uid\",\"foreignField\":\"_id\",\"as\":\"__jsmql.tmp.1\"}},{\"$unwind\":\"$__jsmql.tmp.1\"},{\"$replaceWith\":\"$__jsmql.tmp.1\"}]",
      "now": "[{\"$set\":{\"__jsmql.var.uid\":\"$userId\"}},{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_v0_uid\":\"$__jsmql.var.uid\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_v0_uid\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000let uid = $.userId; $.matched = $$$.users.filter(u => u._id === uid);": {
      "entry": "pipeline",
      "src": "let uid = $.userId; $.matched = $$$.users.filter(u => u._id === uid);",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.uid\":\"$userId\"}},{\"$lookup\":{\"from\":\"users\",\"localField\":\"__jsmql.var.uid\",\"foreignField\":\"_id\",\"as\":\"matched\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$set\":{\"__jsmql.var.uid\":\"$userId\"}},{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_v0_uid\":\"$__jsmql.var.uid\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$_id\",\"$$jsmql_v0_uid\"]}}}],\"as\":\"matched\"}},{\"$unset\":\"__jsmql\"}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000let user = $.user; $$ = $$$.events.filter(e => e.userId === user._id);": {
      "entry": "pipeline",
      "src": "let user = $.user; $$ = $$$.events.filter(e => e.userId === user._id);",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.user\":\"$user\"}},{\"$lookup\":{\"from\":\"events\",\"localField\":\"__jsmql.var.user._id\",\"foreignField\":\"userId\",\"as\":\"__jsmql.tmp.1\"}},{\"$unwind\":\"$__jsmql.tmp.1\"},{\"$replaceWith\":\"$__jsmql.tmp.1\"}]",
      "now": "[{\"$set\":{\"__jsmql.var.user\":\"$user\"}},{\"$lookup\":{\"from\":\"events\",\"let\":{\"jsmql_v0__id\":\"$__jsmql.var.user._id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_v0__id\"]}}}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000let region = $.region; $$ = $$$.events.filter(e => e.userId === $._id && e.region === region);": {
      "entry": "pipeline",
      "src": "let region = $.region; $$ = $$$.events.filter(e => e.userId === $._id && e.region === region);",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.region\":\"$region\"}},{\"$lookup\":{\"from\":\"events\",\"let\":{\"jsmql_f0__id\":\"$_id\",\"jsmql_v0_region\":\"$__jsmql.var.region\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]},{\"",
      "now": "[{\"$set\":{\"__jsmql.var.region\":\"$region\"}},{\"$lookup\":{\"from\":\"events\",\"let\":{\"jsmql_f0__id\":\"$_id\",\"jsmql_v0_region\":\"$__jsmql.var.region\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]},{\"",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000let uid = $.userId; $$ = $$$.orders.filter(o => o.userId === uid).toSorted((a, b) => b.placedAt - a.placedAt).slice(0, 5);": {
      "entry": "pipeline",
      "src": "let uid = $.userId; $$ = $$$.orders.filter(o => o.userId === uid).toSorted((a, b) => b.placedAt - a.placedAt).slice(0, 5);",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.uid\":\"$userId\"}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_v0_uid\":\"$__jsmql.var.uid\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_v0_uid\"]}}},{\"$sort\":{\"placedAt\":-1}},{\"$limit\":5}],",
      "now": "[{\"$set\":{\"__jsmql.var.uid\":\"$userId\"}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_v0_uid\":\"$__jsmql.var.uid\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_v0_uid\"]}}},{\"$sort\":{\"placedAt\":-1}},{\"$limit\":5}],",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000\n$match($.createdAt >= new Date(2026, 1, 1));\n$$ = $$$.orders.filter({ userId: $._id }).map((o, i, ordersColl) => {\n  return {\n    totalShipments: $$$.shipments.filter({ orderId: o._id }).length,\n    totalOrders: ordersColl.length,\n    totalUsers: $$.length,\n  };\n});\n          ": {
      "entry": "pipeline",
      "src": "\n$match($.createdAt >= new Date(2026, 1, 1));\n$$ = $$$.orders.filter({ userId: $._id }).map((o, i, ordersColl) => {\n  return {\n    totalShipments: $$$.shipments.filter({ orderId: o._id }).length,\n    totalOrders: ordersColl.length,\n    totalUsers: $$.length,\n  };\n});\n          ",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"}}},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_s0_length\":\"$__jsmql.length\",\"jsmql_f0__id\":",
      "now": "`ordersColl` has no value inside `.map()` — the collection is the stream itself; write '$$.length' for its size.",
      "reason": "message: the index and collection parameters of a callback have no value on a stream (`$$.length` is the stream's size); same refusals, reworded."
    },
    "pipeline\u0000\n$$.filter({ status: \"active\" });\n$.monthlySpend = $$$.orders.aggregate((o) => {\n  $match(o.customerId === $._id);\n  $group({ _id: { $month: o.placedAt }, spent: $sum(o.total) });\n  $sort({ _id: 1 });\n});\n$.topProducts = $$$.products.aggregate([{ $sort: { sales: -1 } }, { $limit: 5 }, { $project: { name: 1 } }]);\n      ": {
      "entry": "pipeline",
      "src": "\n$$.filter({ status: \"active\" });\n$.monthlySpend = $$$.orders.aggregate((o) => {\n  $match(o.customerId === $._id);\n  $group({ _id: { $month: o.placedAt }, spent: $sum(o.total) });\n  $sort({ _id: 1 });\n});\n$.topProducts = $$$.products.aggregate([{ $sort: { sales: -1 } }, { $limit: 5 }, { $project: { name: 1 } }]);\n      ",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"active\"}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$customerId\",\"$$jsmql_f0__id\"]}}},{\"$group\":{\"_id\":{\"$month\":\"$placedAt\"},\"spent\":{\"$sum\":",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$customerId\",\"$$jsmql_f0__id\"]}}},{\"$group\":{\"_id\":{\"$mon",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000\n$$.filter({ status: \"active\" });\nconst cohortRevenue = $$$.orders\n  .$match({ status: \"shipped\" })\n  .$group({ _id: \"$region\", revenue: $sum(\"$total\"), orders: $sum(1) })\n  .$sort({ revenue: -1 })\n  .$limit(5)\n  .map(r => ({ region: r._id, revenue: r.revenue, avgOrder: r.revenue / r.orders }));\n$set({ topRegions: cohortRevenue });\n": {
      "entry": "pipeline",
      "src": "\n$$.filter({ status: \"active\" });\nconst cohortRevenue = $$$.orders\n  .$match({ status: \"shipped\" })\n  .$group({ _id: \"$region\", revenue: $sum(\"$total\"), orders: $sum(1) })\n  .$sort({ revenue: -1 })\n  .$limit(5)\n  .map(r => ({ region: r._id, revenue: r.revenue, avgOrder: r.revenue / r.orders }));\n$set({ topRegions: cohortRevenue });\n",
      "kind": "output",
      "was": "[{\"$match\":{\"status\":\"active\"}},{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"status\":\"shipped\"}},{\"$group\":{\"_id\":\"$region\",\"revenue\":{\"$sum\":\"$total\"},\"orders\":{\"$sum\":1}}},{\"$sort\":{\"revenue\":-1}},{\"$limit\":5}],",
      "now": "[{\"$match\":{\"status\":{\"$eq\":\"active\",\"$not\":{\"$type\":\"array\"}}}},{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"status\":\"shipped\"}},{\"$group\":{\"_id\":\"$region\",\"revenue\":{\"$sum\":\"$total\"},\"orders\":{\"$sum\":1}}},{\"$sor",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$$.transactions.filter(t => t.createdAt >= new Date(\"2026-01-01\") && t.client === 156);": {
      "entry": "pipeline",
      "src": "$$ = $$$.transactions.filter(t => t.createdAt >= new Date(\"2026-01-01\") && t.client === 156);",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"transactions\",\"pipeline\":[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\"},\"client\":156}}]}}]",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"transactions\",\"pipeline\":[{\"$match\":{\"createdAt\":{\"$gte\":\"«date:2026-01-01T00:00:00.000Z»\",\"$not\":{\"$type\":\"array\"}},\"client\":{\"$eq\":156,\"$not\":{\"$type\":\"array\"}}}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.statusBreakdown = $$$.orders.filter({ userId: $._id }).countBy(\"status\");": {
      "entry": "pipeline",
      "src": "$.statusBreakdown = $$$.orders.filter({ userId: $._id }).countBy(\"status\");",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$group\":{\"_id\":\"$status\",\"__jsmqlTmp\":{\"$sum\":1}}},{\"$group\":{\"_id\":null,\"__jsmqlTmp\":{\"$",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$group\":{\"_id\":\"$status\",\"__jsmqlTmp\":{\"$sum\":1}}},{\"$group\":{\"_id\":null,\"__jsmqlTmp\":{\"$",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000\n$$.filter({ active: true });\n$.recentOrders = $$$.orders.aggregate(o => {\n  $match(o.userId === $._id);\n  $sort({ createdAt: -1 });\n  $limit(5);\n});\nlet nOrders = $$$.orders.filter({ userId: $._id }).length;\n$project({ name: 1, recentOrders: 1, nOrders });\n      ": {
      "entry": "pipeline",
      "src": "\n$$.filter({ active: true });\n$.recentOrders = $$$.orders.aggregate(o => {\n  $match(o.userId === $._id);\n  $sort({ createdAt: -1 });\n  $limit(5);\n});\nlet nOrders = $$$.orders.filter({ userId: $._id }).length;\n$project({ name: 1, recentOrders: 1, nOrders });\n      ",
      "kind": "output",
      "was": "[{\"$match\":{\"active\":true}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"createdAt\":-1}},{\"$limit\":5}],\"as\":\"recentOrders\"}},{",
      "now": "[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"createdAt\":-1}},{\"$l",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000\n$$.filter({ active: true });\n$.recentOrders = $$$.orders.aggregate(o => {\n  $match(o.userId === $._id);\n  $sort({ createdAt: -1 });\n  $limit(5);\n  $.shipments = $$$.shipments.filter(s => s.orderId === o._id && s.userId === $._id);\n});\n$project({ name: 1, recentOrders: 1 });\n      ": {
      "entry": "pipeline",
      "src": "\n$$.filter({ active: true });\n$.recentOrders = $$$.orders.aggregate(o => {\n  $match(o.userId === $._id);\n  $sort({ createdAt: -1 });\n  $limit(5);\n  $.shipments = $$$.shipments.filter(s => s.orderId === o._id && s.userId === $._id);\n});\n$project({ name: 1, recentOrders: 1 });\n      ",
      "kind": "now-rejected",
      "was": "[{\"$match\":{\"active\":true}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"createdAt\":-1}},{\"$limit\":5},{\"$lookup\":{\"from\":\"ship",
      "now": "The outer document can't be written from inside a body over another collection — only read. Write the body's own document through its callback parameter ('o.x = …', 'delete o.x', 'o = { … }'), or as a stage ('$set({ x: …",
      "reason": "intended: `$.` is the OUTER document at every depth (HR4) and cannot be written from a body over another collection; the body's own document is its callback parameter (`o.x = …`, `o = { … }`) or a stage (`$set(…)`). The shipped compiler wrote the FOREIGN document for `$.x =` there — the one exception to the ruling."
    },
    "pipeline\u0000\n$$ = $$$.users.filter({ active: true }).map(u => ({\n  user:      u._id,\n  name:      u.name,\n  email:     u.contactDetails.email,\n  signupAt:  u.createdAt,\n  lastOrder: $$$.orders.find({ userId: u._id })\n}));\n          ": {
      "entry": "pipeline",
      "src": "\n$$ = $$$.users.filter({ active: true }).map(u => ({\n  user:      u._id,\n  name:      u.name,\n  email:     u.contactDetails.email,\n  signupAt:  u.createdAt,\n  lastOrder: $$$.orders.find({ userId: u._id })\n}));\n          ",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"users\",\"pipeline\":[{\"$match\":{\"active\":true}},{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"userId\",\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$f",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"users\",\"pipeline\":[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f1__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000\n$.recentOrders = $$$.orders\n  .filter({ userId: $._id })\n  .toSorted({ placedAt: -1 })\n  .take(5)\n  .map(o => ({ id: o._id, total: o.total, placedAt: o.placedAt }));\n          ": {
      "entry": "pipeline",
      "src": "\n$.recentOrders = $$$.orders\n  .filter({ userId: $._id })\n  .toSorted({ placedAt: -1 })\n  .take(5)\n  .map(o => ({ id: o._id, total: o.total, placedAt: o.placedAt }));\n          ",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"placedAt\":-1}},{\"$limit\":5}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"recentOrders\":{\"$m",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"placedAt\":-1}},{\"$limit\":5},{\"$replaceWith\":{\"id\":\"$_id\",\"total\":\"$total\",\"place",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000\n$$ = $$$.orders\n  .filter({ userId: $._id })\n  .toSorted({ placedAt: -1 })\n  .take(5);\n          ": {
      "entry": "pipeline",
      "src": "\n$$ = $$$.orders\n  .filter({ userId: $._id })\n  .toSorted({ placedAt: -1 })\n  .take(5);\n          ",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"placedAt\":-1}},{\"$limit\":5}],\"as\":\"__jsmql.tmp.1\"}},{\"$unwind\":\"$__jsmql.tmp.1\"}",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$sort\":{\"placedAt\":-1}},{\"$limit\":5}],\"as\":\"__jsmql.tmp.0\"}},{\"$unwind\":\"$__jsmql.tmp.0\"}",
      "reason": "equivalent: the scratch slots count from 0 in this compiler; the shipped compiler counted from 1."
    },
    "pipeline\u0000\nlet minSpend = $.tier === \"gold\" ? 500 : 100;\n$$ = $$$.orders\n  .filter(o => o.userId === $._id && o.total > minSpend)\n  .toSorted({ placedAt: -1 })\n  .take(10);\n          ": {
      "entry": "pipeline",
      "src": "\nlet minSpend = $.tier === \"gold\" ? 500 : 100;\n$$ = $$$.orders\n  .filter(o => o.userId === $._id && o.total > minSpend)\n  .toSorted({ placedAt: -1 })\n  .take(10);\n          ",
      "kind": "output",
      "was": "[{\"$set\":{\"__jsmql.var.minSpend\":{\"$cond\":{\"if\":{\"$eq\":[\"$tier\",\"gold\"]},\"then\":500,\"else\":100}}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\",\"jsmql_v0_minSpend\":\"$__jsmql.var.minSpend\"},\"pipeline\":[{\"$matc",
      "now": "[{\"$set\":{\"__jsmql.var.minSpend\":{\"$cond\":{\"if\":{\"$eq\":[\"$tier\",\"gold\"]},\"then\":500,\"else\":100}}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\",\"jsmql_v0_minSpend\":\"$__jsmql.var.minSpend\"},\"pipeline\":[{\"$matc",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.peers = $$$.users.filter(u => u.n === $$.length);": {
      "entry": "pipeline",
      "src": "$.peers = $$$.users.filter(u => u.n === $$.length);",
      "kind": "output",
      "was": "[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$lookup\":{\"from\":\"users\",\"let\":{\"jsmql_s0_length\":\"$__jsmql.length\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$n\",\"$$jsmql_s0_length\"]}}}],\"as\":\"peers\"}}",
      "now": "[{\"$lookup\":{\"from\":\"users\",\"pipeline\":[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$match\":{\"$expr\":{\"$eq\":[\"$n\",\"$__jsmql.length\"]}}},{\"$unset\":\"__jsmql\"}],\"as\":\"peers\"}}]",
      "reason": "intended: `$$` is the CURRENT stream (HR4); inside a body over another collection that is the body's stream, so `$$.length` there counts it — the shipped compiler captured the root count here and the sub-stream count in a block."
    },
    "pipeline\u0000$.x = $$$.s.filter((s, i, c) => s.k === $.k);": {
      "entry": "pipeline",
      "src": "$.x = $$$.s.filter((s, i, c) => s.k === $.k);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"s\",\"localField\":\"k\",\"foreignField\":\"k\",\"as\":\"x\"}}]",
      "now": "[{\"$lookup\":{\"from\":\"s\",\"let\":{\"jsmql_f0_k\":\"$k\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$k\",\"$$jsmql_f0_k\"]}}}],\"as\":\"x\"}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000\n        $$ = $$$.orders.filter(o => $._id === o.userId).map((o, i, ordersColl) => {\n          return {\n            totalShipments: $$$.shipments.filter((s, i, shipmntsColl) => s.orderId === o._id).length,\n            totalOrders: ordersColl.length,\n            totalUsers: $$.length,\n          };\n        });\n      ": {
      "entry": "pipeline",
      "src": "\n        $$ = $$$.orders.filter(o => $._id === o.userId).map((o, i, ordersColl) => {\n          return {\n            totalShipments: $$$.shipments.filter((s, i, shipmntsColl) => s.orderId === o._id).length,\n            totalOrders: ordersColl.length,\n            totalUsers: $$.length,\n          };\n        });\n      ",
      "kind": "now-rejected",
      "was": "[{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_s0_length\":\"$__jsmql.length\",\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$$jsmql_f0__id\",\"$us",
      "now": "`ordersColl` has no value inside `.map()` — the collection is the stream itself; write '$$.length' for its size.",
      "reason": "message: the index and collection parameters of a callback have no value on a stream (`$$.length` is the stream's size); same refusals, reworded."
    },
    "pipeline\u0000\n        const length = $.length + 1;\n        $$ = $$$.orders.filter(o => o.userId === $._id).map((o, i, coll) => {\n          return { l0: $$.length, l1: $.length, l2: length, l3: coll.length };\n        });\n      ": {
      "entry": "pipeline",
      "src": "\n        const length = $.length + 1;\n        $$ = $$$.orders.filter(o => o.userId === $._id).map((o, i, coll) => {\n          return { l0: $$.length, l1: $.length, l2: length, l3: coll.length };\n        });\n      ",
      "kind": "now-rejected",
      "was": "[{\"$set\":{\"__jsmql.var.length\":{\"$add\":[\"$length\",1]}}},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_s0_length\":\"$__jsmql.length\",\"jsmql_f0__id\":\"$_id\",\"jsmql",
      "now": "`coll` has no value inside `.map()` — the collection is the stream itself; write '$$.length' for its size.",
      "reason": "message: the index and collection parameters of a callback have no value on a stream (`$$.length` is the stream's size); same refusals, reworded."
    },
    "pipeline\u0000$$ = $$$.archive.filter(o => o.tier === 'gold').slice(0, 10);": {
      "entry": "pipeline",
      "src": "$$ = $$$.archive.filter(o => o.tier === 'gold').slice(0, 10);",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"archive\",\"pipeline\":[{\"$match\":{\"tier\":\"gold\"}},{\"$limit\":10}]}}]",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"archive\",\"pipeline\":[{\"$match\":{\"tier\":{\"$eq\":\"gold\",\"$not\":{\"$type\":\"array\"}}}},{\"$limit\":10}]}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.x = $$$.orders.shuffle();": {
      "entry": "pipeline",
      "src": "$.x = $$$.orders.shuffle();",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$addFields\":{\"__jsmql.tmp.1\":{\"$rand\":{}}}},{\"$sort\":{\"__jsmql.tmp.1\":1}},{\"$unset\":\"__jsmql\"}],\"as\":\"__jsmql.tmp.2\"}},{\"$set\":{\"x\":\"$__jsmql.tmp.2\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$addFields\":{\"__jsmql.tmp.0\":{\"$rand\":{}}}},{\"$sort\":{\"__jsmql.tmp.0\":1}},{\"$unset\":\"__jsmql\"}],\"as\":\"x\"}}]",
      "reason": "shippedBug: a nested join inside a predicate leaked `__jsmql.tmp` into the joined array (measured); the body's own chain now closes with its own `$unset`."
    },
    "pipeline\u0000$.slim = $$$.orders.filter(o => o.userId === $._id).pick([\"total\", \"placedAt\"]);": {
      "entry": "pipeline",
      "src": "$.slim = $$$.orders.filter(o => o.userId === $._id).pick([\"total\", \"placedAt\"]);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$project\":{\"total\":1,\"placedAt\":1,\"_id\":0}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"slim\":\"$__j",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$project\":{\"total\":1,\"placedAt\":1,\"_id\":0}}],\"as\":\"slim\"}}]",
      "reason": "intended: a read of the outer document inside a body over another collection — `$.x`, `$[\"ext-code\"]`, an outer `let`, a raw `$match({ f: $.x })` — is captured into the stage's `let` and read as `$$jsmql_…`; the shipped compiler read some of these as the FOREIGN document's field (HR4). See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$ = $$$.archive.filter(o => o.tier === 'gold').map(d => ({ n: d.name }));": {
      "entry": "pipeline",
      "src": "$$ = $$$.archive.filter(o => o.tier === 'gold').map(d => ({ n: d.name }));",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"archive\",\"pipeline\":[{\"$match\":{\"tier\":\"gold\"}},{\"$replaceWith\":{\"n\":\"$name\"}}]}}]",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"archive\",\"pipeline\":[{\"$match\":{\"tier\":{\"$eq\":\"gold\",\"$not\":{\"$type\":\"array\"}}}},{\"$replaceWith\":{\"n\":\"$name\"}}]}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$$.orders.filter(o => o.userId === $._id).map((o, _i, coll) => ({ id: o._id, n: coll.length }));": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.filter(o => o.userId === $._id).map((o, _i, coll) => ({ id: o._id, n: coll.length }));",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$userId\",\"$$jsmql_f0__id\"]}}},{\"$setWindowFields\":{\"output\":{\"__jsmql.length\":{\"$count\":{}}}}},{\"$replaceWith\":{\"id\":\"$_i",
      "now": "`coll` has no value inside `.map()` — the collection is the stream itself; write '$$.length' for its size.",
      "reason": "message: the index and collection parameters of a callback have no value on a stream (`$$.length` is the stream's size); same refusals, reworded."
    },
    "pipeline\u0000$$ = $$$.users.filter(u => u.active === true).map(d => ({ a: $$$.archive.find(x => x._id === d._id) }));": {
      "entry": "pipeline",
      "src": "$$ = $$$.users.filter(u => u.active === true).map(d => ({ a: $$$.archive.find(x => x._id === d._id) }));",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"users\",\"pipeline\":[{\"$match\":{\"active\":true}},{\"$lookup\":{\"from\":\"archive\",\"localField\":\"_id\",\"foreignField\":\"_id\",\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$fir",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"users\",\"pipeline\":[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}},{\"$lookup\":{\"from\":\"archive\",\"let\":{\"jsmql_f1__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$$ = $$$.users.filter(u => u.active === true).map(d => ({ archives: $$$.archive.filter(x => x.userId === d._id && x.tier === d.tier) }));": {
      "entry": "pipeline",
      "src": "$$ = $$$.users.filter(u => u.active === true).map(d => ({ archives: $$$.archive.filter(x => x.userId === d._id && x.tier === d.tier) }));",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"users\",\"pipeline\":[{\"$match\":{\"active\":true}},{\"$lookup\":{\"from\":\"archive\",\"let\":{\"jsmql_f0__id\":\"$_id\",\"jsmql_f0_tier\":\"$tier\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$and\":[",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"users\",\"pipeline\":[{\"$match\":{\"active\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}},{\"$lookup\":{\"from\":\"archive\",\"let\":{\"jsmql_f1__id\":\"$_id\",\"jsmql_f1_tier\":\"$tier\"},\"pipeli",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$$ = $$$.orders.filter(o => o.shipped === true).flatMap(d => d.items);": {
      "entry": "pipeline",
      "src": "$$ = $$$.orders.filter(o => o.shipped === true).flatMap(d => d.items);",
      "kind": "output",
      "was": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"orders\",\"pipeline\":[{\"$match\":{\"shipped\":true}},{\"$unwind\":\"$items\"}]}}]",
      "now": "[{\"$match\":{\"$expr\":false}},{\"$unionWith\":{\"coll\":\"orders\",\"pipeline\":[{\"$match\":{\"shipped\":{\"$eq\":true,\"$not\":{\"$type\":\"array\"}}}},{\"$unwind\":\"$items\"}]}}]",
      "reason": "intended: the array ruling reaches a `$match` a stream method emits, because a predicate lowers through the same filter target. See SR2 in docs/LANG_RULES.md."
    },
    "pipeline\u0000$.o = $$$.orders.groupBy(d => d.cat);": {
      "entry": "pipeline",
      "src": "$.o = $$$.orders.groupBy(d => d.cat);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$group\":{\"_id\":\"$cat\",\"__jsmqlTmp\":{\"$push\":\"$$ROOT\"}}},{\"$group\":{\"_id\":null,\"__jsmqlTmp\":{\"$push\":{\"k\":{\"$ifNull\":[{\"$toString\":\"$_id\"},\"null\"]},\"v\":\"$__jsmqlTmp\"}}}},{\"$repla",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$group\":{\"_id\":\"$cat\",\"__jsmqlTmp\":{\"$push\":\"$$ROOT\"}}},{\"$group\":{\"_id\":null,\"__jsmqlTmp\":{\"$push\":{\"k\":{\"$ifNull\":[{\"$toString\":\"$_id\"},\"null\"]},\"v\":\"$__jsmqlTmp\"}}}},{\"$repla",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.o = $$$.orders.find(o => o.cat === \"a\");": {
      "entry": "pipeline",
      "src": "$.o = $$$.orders.find(o => o.cat === \"a\");",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"cat\":\"a\"}}],\"as\":\"o\"}},{\"$set\":{\"o\":{\"$first\":\"$o\"}}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"cat\":{\"$eq\":\"a\",\"$not\":{\"$type\":\"array\"}}}},{\"$limit\":1}],\"as\":\"o\"}},{\"$set\":{\"o\":{\"$first\":\"$o\"}}}]",
      "reason": "intended: `.find` bounds its body with `$limit: 1` — the first match is all it keeps, and the server examines one index key per document instead of every match. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.o = $$$.orders.flatMap({ cat: 1 });": {
      "entry": "pipeline",
      "src": "$.o = $$$.orders.flatMap({ cat: 1 });",
      "kind": "message",
      "was": ".flatMap(...) names the array field to flatten, so it takes a bare-path arrow ('.flatMap(d => d.items)') or the equivalent field-name string ('.flatMap(\"items\")'). On a stream it lowers to '$unwind', which needs a field ",
      "now": "'.flatMap()' takes a field as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.o = $$$.orders.orderBy({ cat: -1 });": {
      "entry": "pipeline",
      "src": "$.o = $$$.orders.orderBy({ cat: -1 });",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"cat\":-1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"o\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"cat\":-1}}],\"as\":\"o\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.o = $$$.orders.orderBy([\"cat\"], [-1]);": {
      "entry": "pipeline",
      "src": "$.o = $$$.orders.orderBy([\"cat\"], [-1]);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"cat\":-1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"o\":\"$__jsmql.tmp.1\"}},{\"$unset\":\"__jsmql\"}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$sort\":{\"cat\":-1}}],\"as\":\"o\"}}]",
      "reason": "intended: a bare write `$.o = $$$.c.…` (or a `let`) makes the target the stage's `as` — no scratch slot, no `$set`, no cleanup — where the shipped compiler materialised into `__jsmql.tmp` and copied. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.o = $$$.orders.sortBy({ cat: 1 });": {
      "entry": "pipeline",
      "src": "$.o = $$$.orders.sortBy({ cat: 1 });",
      "kind": "message",
      "was": ".sortBy({ … }) isn't supported — an object here is a lodash matches-shorthand, not a direction. Use '.orderBy({ field: -1 })' or '.sort({ field: -1 })' for directions.",
      "now": ".sortBy({ … }) reads an object as a lodash matcher, not as directions. For directions write '.orderBy({ field: -1 })' or '.sort({ field: -1 })'.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.o = $$$.orders.keyBy(5);": {
      "entry": "pipeline",
      "src": "$.o = $$$.orders.keyBy(5);",
      "kind": "message",
      "was": ".keyBy(key) takes a field name ('.keyBy(\"status\")'), a bare-path arrow ('.keyBy(d => d.status)'), a computed iteratee ('.keyBy(d => d.status.toLowerCase())'), or a lodash matches shorthand ('{ active: true }' / '[\"status",
      "now": "'.keyBy()' takes a reshape as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.o = $$$.orders.uniqBy(5);": {
      "entry": "pipeline",
      "src": "$.o = $$$.orders.uniqBy(5);",
      "kind": "message",
      "was": ".uniqBy(key) takes a field name ('.uniqBy(\"status\")'), a bare-path arrow ('.uniqBy(d => d.status)'), a computed iteratee ('.uniqBy(d => d.status.toLowerCase())'), or a lodash matches shorthand ('{ active: true }' / '[\"st",
      "now": "'.uniqBy()' takes a reshape as a one-parameter arrow here — 'd => …' — and got something else.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$.o = $$$.orders.sortBy([5]);": {
      "entry": "pipeline",
      "src": "$.o = $$$.orders.sortBy([5]);",
      "kind": "message",
      "was": ".sortBy(...) keys on a field, so it takes a field name ('.sortBy(\"status\")'), the equivalent bare-path arrow ('.sortBy(d => d.status)'), or a computed key iteratee ('.sortBy(d => d.status.toLowerCase())').",
      "now": ".sortBy() sorts by a field NAME — a plain string like \"age\", with no leading '$'.",
      "reason": "message: same refusal, reworded by the join road."
    },
    "pipeline\u0000$$$.fooBar()": {
      "entry": "pipeline",
      "src": "$$$.fooBar()",
      "kind": "message",
      "was": "'$$$.fooBar(...)' is not a known diagnostic stage. '$$$' (database reference) has no diagnostic source stages — collection diagnostics use '$$', server/cluster diagnostics use '$$$$'.",
      "now": "Reading another collection produces a value, and this statement gives it no destination. Assign it to a field ('$.<field> = $$$.<coll>.…'), bind it ('let x = $$$.<coll>.…'), or make it the stream ('$$ = $$$.<coll>.…').",
      "reason": "message: same refusal — a read of another collection with no destination — reworded; a system stage on a context reference is a verified pending until its road lands."
    },
    "pipeline\u0000$.o = $$$.orders.find(o => o.uid === $.id)": {
      "entry": "pipeline",
      "src": "$.o = $$$.orders.find(o => o.uid === $.id)",
      "kind": "now-accepted",
      "was": "'$$$.<coll>' must be either followed by a stream chain and consumed as a value (a $lookup read — any lodash stream method may head the chain, e.g. '.filter(pred)' / '.toSorted(...)', and '.aggregate((o) => { ... })' runs",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0_id\":\"$id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$uid\",\"$$jsmql_f0_id\"]}}},{\"$limit\":1}],\"as\":\"o\"}},{\"$set\":{\"o\":{\"$first\":\"$o\"}}}]",
      "reason": "intended: the join road lowers a shape the shipped compiler refused — a nested join, a bare parameter (`$$ROOT`), the whole outer document as a correlation value (`let: { …: \"$$ROOT\" }` runs, measured), an outer binding read inside a stream switch, `$$ = $$$.c` alone, an empty `.aggregate([])`. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$.o = $$$.orders.find(o => o.uid === $._id);": {
      "entry": "pipeline",
      "src": "$.o = $$$.orders.find(o => o.uid === $._id);",
      "kind": "output",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"localField\":\"_id\",\"foreignField\":\"uid\",\"as\":\"o\"}},{\"$set\":{\"o\":{\"$first\":\"$o\"}}}]",
      "now": "[{\"$lookup\":{\"from\":\"orders\",\"let\":{\"jsmql_f0__id\":\"$_id\"},\"pipeline\":[{\"$match\":{\"$expr\":{\"$eq\":[\"$uid\",\"$$jsmql_f0__id\"]}}},{\"$limit\":1}],\"as\":\"o\"}},{\"$set\":{\"o\":{\"$first\":\"$o\"}}}]",
      "reason": "intended: one route for a join — `let` + `pipeline` + `$expr`, never `localField`/`foreignField`. The pipeline form compares the fields' OWN values as JavaScript does (null ≠ missing, an array is not its elements) and uses the foreign index just the same (measured: indexesUsed, keys examined = rows matched); the basic form silently broke both. See docs/specs/emit-pass.md § The join road; every shape runs on mongod 8.3.7 with its documents compared in test/compiler-join.test.ts."
    },
    "pipeline\u0000$lookup({ from: \"o\", pipeline: [$ = { t: $.total }], as: \"o\" });": {
      "entry": "pipeline",
      "src": "$lookup({ from: \"o\", pipeline: [$ = { t: $.total }], as: \"o\" });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"o\",\"pipeline\":[{\"$set\":{\"\":{\"t\":\"$total\"}}}],\"as\":\"o\"}}]",
      "now": "The outer document can't be written from inside a body over another collection — only read. Write the body's own document through its callback parameter ('o.x = …', 'delete o.x', 'o = { … }'), or as a stage ('$set({ x: …",
      "reason": "intended: `$.` is the OUTER document at every depth (HR4) and cannot be written from a body over another collection; the body's own document is its callback parameter (`o.x = …`, `o = { … }`) or a stage (`$set(…)`). The shipped compiler wrote the FOREIGN document for `$.x =` there — the one exception to the ruling."
    },
    "pipeline\u0000$lookup({ from: \"o\", pipeline: [$.a = 1], as: \"o\" });": {
      "entry": "pipeline",
      "src": "$lookup({ from: \"o\", pipeline: [$.a = 1], as: \"o\" });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"o\",\"pipeline\":[{\"$set\":{\"a\":1}}],\"as\":\"o\"}}]",
      "now": "The outer document can't be written from inside a body over another collection — only read. Write the body's own document through its callback parameter ('o.x = …', 'delete o.x', 'o = { … }'), or as a stage ('$set({ x: …",
      "reason": "intended: `$.` is the OUTER document at every depth (HR4) and cannot be written from a body over another collection; the body's own document is its callback parameter (`o.x = …`, `o = { … }`) or a stage (`$set(…)`). The shipped compiler wrote the FOREIGN document for `$.x =` there — the one exception to the ruling."
    },
    "pipeline\u0000$unionWith({ coll: \"c\", pipeline: [$match($.a > 0), $.o = $$$.orders.find(o => o.uid === 1)] });": {
      "entry": "pipeline",
      "src": "$unionWith({ coll: \"c\", pipeline: [$match($.a > 0), $.o = $$$.orders.find(o => o.uid === 1)] });",
      "kind": "now-rejected",
      "was": "[{\"$lookup\":{\"from\":\"orders\",\"pipeline\":[{\"$match\":{\"uid\":1}}],\"as\":\"__jsmql.tmp.1\"}},{\"$set\":{\"__jsmql.tmp.1\":{\"$first\":\"$__jsmql.tmp.1\"}}},{\"$unionWith\":{\"coll\":\"c\",\"pipeline\":[{\"$match\":{\"a\":{\"$gt\":0}}},{\"$set\":{\"o\":\"",
      "now": "'$unionWith' has no 'let': its body cannot read the outer document or a binding declared outside it. Filter or reshape the outer stream in a statement before it, or read the other collection through a join ('$.<field> = ",
      "reason": "intended: `$unionWith` has no `let` (measured: IDLUnknownField), so a read of the outer document inside its body is refused (HR4); the shipped compiler read the foreign document there."
    },
    "filter\u0000{ function: $.x }": {
      "entry": "filter",
      "src": "{ function: $.x }",
      "kind": "output",
      "was": "{\"function\":\"$x\"}",
      "now": "{\"$expr\":{\"$eq\":[\"$function\",\"$x\"]}}",
      "reason": "shippedBug: a runtime read inside a raw query document (`{ userId: $.other }`) was the string `\"$other\"` to the server — a literal that matches nothing; it is lifted into `$expr` now, the constant entries staying native."
    }
  }
}
